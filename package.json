{
  "name": "tunnex",
  "version": "0.1.0",
  "private": true,
  "description": "Tunnex.io — self-hosted multi-tenant VPN & Zero Trust platform",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.12.0",
  "pnpm": {
    "onlyBuiltDependencies": ["electron", "esbuild"]
  }
}
