# F08 released-route evidence

- Exact F08 web asset ran at `https://internal.tunnex.app/access` under the owner session.
- The agent selector contained and selected `f08-live-20260816T0805Z`.
- The result rendered the seven server-owned checks, first blocker, exact compiled rule/hash/version and runtime/gateway facts.
- The page stated that no packet, DNS query or policy change is sent and exposed no active-probe control.
- DEMO to demo2 to DEMO switching synchronously removed the prior F08 agent and diagnostic result from the mismatched-organization DOM.
- Fast-input and late-response suppression is additionally pinned by the exact-current released-page suite.

Owner live rendering SATISFIES the privileged route. Unrelated-member DOM absence is a SUBSTITUTE in this walk because no current separate member browser session was available; see `blocker-matrix.md`.
