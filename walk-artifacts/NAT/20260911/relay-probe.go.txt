package main
import("context";"crypto/hmac";"crypto/sha1";"encoding/base64";"fmt";"os";"strings";"time";"io";"github.com/tunnexio/tunnex/apps/node/internal/icewire")
func must(e error){if e!=nil{panic(e)}}
func relayOnly(s icewire.Signal)icewire.Signal{out:=[]string{};for _,c:=range s.Candidates{if strings.Contains(c," typ relay "){out=append(out,c)}};s.Candidates=out;return s}
func main(){ctx,cancel:=context.WithTimeout(context.Background(),45*time.Second);defer cancel();secret,e:=os.ReadFile(os.Args[1]);must(e);u:=fmt.Sprintf("%d:nat-walk",time.Now().Add(3*time.Minute).Unix());h:=hmac.New(sha1.New,[]byte(strings.TrimSpace(string(secret))));h.Write([]byte(u));r:=icewire.Relay{URL:"turns:relay.13.207.205.201.sslip.io:443?transport=tcp",Username:u,Password:base64.StdEncoding.EncodeToString(h.Sum(nil))};key:=base64.StdEncoding.EncodeToString(make([]byte,32));a,sa,e:=icewire.Gather(ctx,r,key);must(e);defer a.Close();b,sb,e:=icewire.Gather(ctx,r,key);must(e);defer b.Close();sa=relayOnly(sa);sb=relayOnly(sb);if len(sa.Candidates)==0||len(sb.Candidates)==0{panic("no TURN candidates")};errs:=make(chan error,1);go func(){c,e:=b.Connect(ctx,sa,false);if e!=nil{errs<-e;return};defer c.Close();c.SetDeadline(time.Now().Add(10*time.Second));buf:=make([]byte,64);n,e:=c.Read(buf);if e==nil{_,e=c.Write(buf[:n])};errs<-e}();c,e:=a.Connect(ctx,sb,true);must(e);defer c.Close();c.SetDeadline(time.Now().Add(10*time.Second));payload:=[]byte("tunnex-relay-live-packet-proof");_,e=c.Write(payload);must(e);buf:=make([]byte,len(payload));_,e=io.ReadFull(c,buf);must(e);must(<-errs);if string(buf)!=string(payload)||a.Path()!="relay"||b.Path()!="relay"{panic("relay echo mismatch")};fmt.Println("PASS TLS-verified TURN TCP443; relay-only ICE both ends; payload echoed. Synthetic credentials, not CP enrollment or native WG application proof.")}
