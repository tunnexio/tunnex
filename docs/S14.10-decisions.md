# S14.10 — Devices Redesign Decisions & Render Floor Audit

This document records the step-1 element extraction from the authoritative wireframe, the step-2 four-way panel classification, schema invariants, empirical failure logs, and verification census for the **S14.10 Devices** redesign pass.

---

## 1. Step-1 Wireframe Extraction (Element-by-Element Census)

Extraction of all visual and functional elements from the authoritative wireframe design (`TunneX Wireframes > Devices`):

| Section / Panel | Extracted Wireframe Element | Description / Properties |
|---|---|---|
| **Header** | **Page Title** | `Devices` |
| | **Subtitle / Health** | Subtitle reads `Enrollment, handshake liveness & posture`. Hardcoded static `control plane ● healthy` text present in wireframe title header. |
| | **Triage Filter Chips** | Filter tabs: `All (1000)`, `Online (830)`, `Idle (154)`, `Blocked (16)`. |
| **Top Canvas** | **Device Fabric Graph** | Visual topology graph displaying online (830), idle (154), and blocked (16) node orbits around central gateway node `gw-us-east` (`1000 peers · v1.9.2`). Bottom legend: `1000 devices · 830 online · hover a node · packets + live handshakes`. |
| **Main Table** | **Table Headers (6 Columns)** | `DEVICE / OWNER`, `LAST HANDSHAKE`, `TX / RX`, `PROTOCOL`, `POSTURE`, `TUNNEL`, plus `Actions` (Revoke button). |
| | **Row `DEVICE / OWNER`** | Top line: `d.name` (e.g. `macbook-alice`). Sub-line: `ownerEmail · platform` (e.g. `alice@acme.io · macOS 15`). |
| | **Row `LAST HANDSHAKE`** | `StatusDot` + recency label (`• 14s ago`, `6m ago`, `1h ago`). For OpenVPN keyless devices, renders `"liveness not reported"`. |
| | **Row `TX / RX`** | Cumulative traffic gauge: `↑2.1GB  ↓9.4GB`. |
| | **Row `PROTOCOL`** | Protocol badge: `<Badge tone="neutral">WIREGUARD</Badge>` or `<Badge tone="neutral">OPENVPN</Badge>`. |
| | **Row `POSTURE`** | Posture status badge: `COMPLIANT` (ok), `UNKNOWN` (neutral/unknown), `BLOCKED` (danger), `N/A` (neutral - OpenVPN keyless rows). |
| | **Row `TUNNEL`** | Routing scope: `split` or `full`. |
| **Right Panels** | **Config Ceremony Panel** | One-time export ceremony panel with transport tabs (`WIREGUARD` / `OPENVPN`), QR code canvas, credentials box, split/full tunnel toggle, and acknowledgment checkbox. |
| | **Device Approval Mode Panel** | Enterprise panel (`ENT`): `Device approval mode` toggle (`On - new devices enroll pending and cannot connect until approved.`). |

---

## 2. Step-2 Four-Way Panel Audit & Classification

Classification of every wireframe element under the 4-way test ((a) Functional render floor, (b) Visual advisory, (c) Deferred, (d) Cut):

| Wireframe Element | Classification | Rationale & Handling |
|---|---|---|
| **Header Title (`<h1 ...>Devices</h1>`)** | **(a) Functional render floor** | Preserved `<h1 style="...">Devices</h1>` + org subtitle `{org.name}`. Heading role mandatory for Playwright/A11y. |
| **Header Static `control plane ● healthy`** | **(d) Cut / redundant** | **Cut.** Footer `HealthStatus` ([`AppShell.tsx:322`](file:///Users/pawangupta/tunnex/apps/web/src/components/AppShell.tsx#L322)) owns `/healthz` telemetry. Hardcoding static text in page title is a duplicate-render defect. Recorded in `CUT-REGISTER.md`. |
| **Triage Filter Chips (`All`, `Online`, `Idle`, `Blocked`)** | **(a) Functional render floor** | Filter buttons deriving from served device state (`online`, `health_blocked`, `last_handshake_at`). |
| **Device Fabric Graph Panel** | **(c) Deferred** | **Deferred to its own slice.** The center node's `"1000 peers"` count is blocked on the exact same gap that made `PEERS` its own slice at S14.6 (DB `devices WHERE node_id` count vs gateway WireGuard peer graph). Recorded in `DEFERRAL-REGISTER.md`. |
| **Table Column: `DEVICE / OWNER`** | **(a) Functional render floor** | Top line: `d.name`. Sub-line: `ownerEmail · platform` (joins user email via members / user_id map). |
| **Table Column: `LAST HANDSHAKE`** | **(a) Functional render floor** | Renders `StatusDot` + `lastSeen(d.last_handshake_at, !!d.public_key)`. For OpenVPN (`public_key = ""`), renders `"liveness not reported"`. |
| **Table Column: `TX / RX`** | **(a) Functional render floor** | Renders cumulative `↑tx_bytes ↓rx_bytes` formatted via `bytes(n)` from `device_status`. Labeled as cumulative-since-handshake, never a rate. |
| **Table Column: `PROTOCOL`** | **(a) Functional render floor** | Renders `<Badge tone="neutral">WIREGUARD</Badge>` (when `public_key !== ""`) or `<Badge tone="neutral">OPENVPN</Badge>` (when `public_key === ""`). |
| **Table Column: `POSTURE`** | **(a) Functional render floor** | Renders posture badges: `posture blocked` (danger), `posture warning` (warn), `posture ok` (ok), `posture stale` / `posture not reported` (unknown), or `N/A` (neutral) for OpenVPN devices keyless by design. Null when org has zero checks configured. |
| **Table Column: `TUNNEL`** | **(c) Deferred / Absent Endpoint** | **ABSENT-PENDING-ENDPOINTS.** `full_tunnel` is accepted on `CreateDeviceRequest`, but is **NOT emitted on the `Device` response schema** in `openapi.yaml`! Adding `full_tunnel` to `GET /devices` requires spec + codegen + SQL select slice. Deferred & recorded in `DEFERRAL-REGISTER.md`. |
| **Actions Column: `Revoke` / `Approve` / `Reject`** | **(a) Functional render floor** | Action button: `Revoke` for active, `Approve`/`Reject` for pending approval devices. |
| **Config Ceremony: Persistent Panel vs Modal** | **(d) Cut / Form variant** | **Cut persistent panel form in favor of Modal.** Built as `OneTimeSecretModal` (modal dialog pops on creation/export ceremony to guarantee secret is shown once and dismissed cleanly). Recorded in `CUT-REGISTER.md`. |
| **Device Approval Mode Panel (ENT)** | **(a) Functional render floor** | Enterprise card/toggle for device approval mode (`getDeviceApproval` / `setDeviceApproval`), showing pending queue count & mode toggle. |

---

## 3. Status Representation (`active` | `revoked` | `pending`), `health_blocked`, and `approved_by`

- **`status` Enum (`active`, `revoked`, `pending`):**
  - `active`: Normal functioning device. Renders `StatusDot` (on/off) and `lastSeen(...)`. Shows `Revoke` button.
  - `revoked`: Device revoked. Renders `<Badge tone="danger">revoked</Badge>`. Posture badges, `re-export needed`, and actions are **SUPPRESSED** via `d.status !== "revoked"`. Address cell renders empty string `""` (no em-dash).
  - `pending`: Device awaiting admin approval under Enterprise device approval mode. State column renders `<Badge tone="warn">pending</Badge>` and **SUPPRESSES** active liveness (`StatusDot` + `lastSeen(...)`) so pending devices never render as active. During issuance ceremony, `pendingExport` surfaces: *"This device is pending approval - the profile is valid but won't connect until an admin approves the device."*
- **Tone Vocabulary & `BadgeTone` Primitive Extension:**
  - `Gateways.tsx` line 310 uses `<Badge tone="warn">` for degraded node warning states.
  - `Devices.tsx` uses `<Badge tone="warn">pending</Badge>` for pending approval state.
  - **Shared Primitive Widening:** `BadgeTone` in `apps/web/src/components/ui.tsx` is widened:
    ```typescript
    export type BadgeTone = "ok" | "warn" | "danger" | "neutral" | "unknown";
    ```
  - Posture cells render posture badges using the shared `<Badge>` primitive (`<Badge tone={pb.tone}>{pb.label}</Badge>`), ensuring posture badges are fully accessible and queryable by badge-level selectors.
- **`<Badge>` Component AST Call Site Census (19 Component Invocations):**
  - `IdentityBadges.tsx:60, 61`: 2 neutral badges
  - `Dashboard.tsx:516`: `<Badge tone={g.tone}>{g.label}</Badge>` (where `g.tone` $\in$ `"warn" | "danger" | "unknown"`)
  - `Dashboard.tsx:791`: `<Badge tone="neutral">GEN {hv.generation}</Badge>`
  - `Devices.tsx:375, 377, 397`: 3 badges (`revoked` [danger], `pending` [warn], posture [tone])
  - `Gateways.tsx:132, 151, 155, 310`: 4 badges
  - `Kubernetes.tsx:251, 325`: 2 badges
  - `Sites.tsx:395, 869, 884, 888`: 4 badges
  - `VisualGallery.tsx:86, 119`: 2 badges
- **`health_blocked: true`:**
  - Enforcement fact: Device is excluded from every gateway while `true`. When `health_state === "noncompliant"` and `health_blocked === true`, renders `"posture blocked"`. Tested positively on `blocked-device`.
- **`approved_by: null` (Grandfathered):**
  - `approved_by: null` indicates a grandfathered / auto-approved device enrolled when device approval mode was off. Functions as active without requiring device approval queue action.

---

## 4. Posture States & Exact TypeScript Type Declarations

- **Exact Type Declarations:**
  - In `apps/web/src/lib/healthview.ts`:
    ```typescript
    import type { BadgeTone } from "../components/ui";
    export type { BadgeTone };
    ```
  - In `apps/web/src/lib/postureview.ts`:
    ```typescript
    export interface PostureBadge {
      label: string;
      tone: BadgeTone;
    }
    ```
  - In `apps/web/src/components/ui.tsx`:
    ```typescript
    export type BadgeTone = "ok" | "warn" | "danger" | "neutral" | "unknown";
    ```

---

## 5. Control Plane Status Header Pattern Audit

- `HealthStatus` (`control plane operational` / `control plane unreachable`) already exists in `AppShell.tsx` (line 322) and renders in the footer of EVERY page after querying `/healthz`.
- Hardcoding static text `control plane ● healthy` in individual page title headers (`Access.tsx`, `Devices.tsx`) is a **NEW, ad-hoc pattern** that multiplies the registered duplicate-render defect across screens and desyncs from live `/healthz` telemetry.
- **Rule:** Page headers MUST NOT hardcode static `control plane ● healthy`. Page titles render `<h1 ...>Devices</h1>` with org subtitle `{org.name}`. Shell-level footer owns control plane health.

---

## 6. Seed Execution & Live Payload Verification

### `make seed-fixtures` Command Output
Executed against `tunnex-s141` stack:
```
{"time":"2026-08-02T12:42:26.736155846Z","level":"INFO","msg":"seed_fixtures_ok","org":"01900000-0000-7000-8000-000000000001","gateways":6,"sites":6,"subnets":10,"devices":11,"audit_entries":41,"k8s_clusters":2,"k8s_services":4,"policy_rules":6,"user_groups":3,"group_members":4,"resources":3,"access_events":3,"cli_credentials":3,"machine_credentials":1}
```

### PostgreSQL Live Database Query Verification
`docker exec tunnex-s141-postgres-1 psql -U tunnex -d tunnex -c "SELECT id, name, status, public_key, assigned_ip FROM devices WHERE org_id = '01900000-0000-7000-8000-000000000001' ORDER BY name;"`
- `unapproved-phone`: `status = 'pending'`, `assigned_ip = '10.99.0.15'`
- `stale-laptop`: `status = 'active'`, `assigned_ip = '10.99.0.16'`
- `ovpn-contractor`: `status = 'active'`, `public_key = ''`, `assigned_ip = '10.99.0.17'`
- `old-laptop`: `status = 'revoked'`
- `macbook-owner` / `thinkpad-erin` / `pixel-erin` / `ipad-owner`: `status = 'active'`

---

## 7. Em-Dash Registration & Decorative Protocol Finding

- **Finding:** The per-section obligation "each section clears its own em-dashes" has been **decorative** since written. The 163 $\rightarrow$ 19 burn-down was achieved during the S14.6 global sweep.
- **Discharge Rule:** All 4 user-facing UI string em-dashes in `Devices.tsx` have been cleared in this pass. The remaining 19 em-dashes across merged screens will be cleared via a dedicated global sweep with a lint/CI regression guard.

---

## 8. Fixture & Census Reconciliation

### Device Naming & Row Census Reconciliation
- `DEVICES` mock array in `deviceswiring.test.tsx` includes **7** devices: `old-laptop` (revoked), `work-laptop`, `unapproved-phone` (pending), `stale-laptop` (re-export needed), `ovpn-contractor` (OpenVPN), `stale-device` (unknown posture), and `blocked-device` (health blocked).
- Table row count census assertion in `deviceswiring.test.tsx:193` is explicitly set to **`toHaveLength(8)`** (1 header + 7 device rows).
- Positive test assertion added for `rowFor("blocked-device")`: `expect(within(rowFor("blocked-device")).getByText("posture blocked")).toBeTruthy()`.
