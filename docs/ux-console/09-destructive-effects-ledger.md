# Destructive-Effects Ledger

Each row is one OpenAPI operation. Evidence is current handler/service/SQL migration code at `fe7ccf5`; where a count is not returned by the server, the UI must not manufacture one.

| Operation | Handler/database evidence | Effects and refusal | Count/preview | Confirmation and verified post-state | Recovery / audit | Owner |
|---|---|---|---|---|---|---|
| `mfaDisenroll` | MFA service; current user credential | Removes TOTP enrollment; may violate future org MFA requirement | no affected count; no preview | “Disable your two-factor authentication”; refresh `/auth/me` | Re-enroll; MFA audit | Personal security |
| `adminResetMfa` | member/MFA handler | Disenrols target MFA only | no count | Name target; roster refresh | User re-enrols; audit | Users |
| `revokeCliCredential` | CLI credential `revoked_at` query | Invalidates named CLI credential | no count | Held UI | New CLI login; audit | CLI credentials |
| `revokeNode` | nodes service + device/OVPN sweep | Terminal node revoke; cascades homed device credentials, addresses, telemetry and OVPN certs | server lifecycle/refusal data, no generic preview | Name gateway and homed-device impact; refresh nodes/devices | `restoreNodeDevices` only for cascade devices; audit | Gateway lifecycle |
| `deleteNode` | `DeleteRevokedNode`, join-token cleanup | Only revoked node; deletes consumed join tokens; FK cleanup safe after transfer | refuses live node; no preview response | Name terminal gateway; refresh list | No recovery; `node.deleted` audit | Gateway lifecycle |
| `restoreNodeDevices` | node restore service | Restores cascade-revoked devices to named live gateway | response returns restored/readdressed counts | Name source/target; refresh nodes/devices | Explicit recovery; audit | Gateway lifecycle |
| `unmapIdpGroup` | `idpsync.Service.UnmapGroup` | Unbinds IdP and deletes all group members; pushes policy | audit records `members_removed`, response 204 | Name group/provider and member effect; refresh group/policy | Re-map/re-sync, not undo; `idp_sync.group_unmapped` | Directory |
| `revokeDevice` | device service; device/OVPN relations | Full credential/address/telemetry and certificate revocation sweep | no browser preview | Name owner/gateway/access loss; refresh device | Re-enrol/reissue, not undo; audit | Devices |
| `removeDevice` | device handler requires revoked roster row | Removes terminal device record | refusal unless revoked | Name device; refresh list | No recovery; audit | Devices |
| `rejectDevice` | device approval service | Rejects pending device and invalidates issuance path | server response only | Name pending device; refresh approval queue | Create again; audit | Devices |
| `deleteHealthCheck` | policy/device-health service | Removes enforcement/check configuration | no count | Name check and policy effect; refresh checks | Recreate check; audit | Access controls |
| `deleteGroup` | policy service, agent-access/template guards | Deletes group only without live JIT/template dependency | refusal names dependency; no preview | Name group; refresh subjects/rules | Recreate; `group.deleted` | Access |
| `removeGroupMember` | policy service | Withdraws member’s group-derived access | no count | Name user/group; refresh membership/rules | Re-add if manual group; audit | Access |
| `revokeMachineCredential` | machine credential service | Invalidates machine credential | no count | Name credential/owner; refresh credentials | Mint replacement; audit | Settings |
| `deleteResource` | policy service, agent-access/template guards | Removes destination resource only without references | refusal names live dependency | Name resource/access impact; refresh resources/rules | Recreate; `resource.deleted` | Access |
| `deletePolicyRule` | policy service | Withdraws allow rule and recompiles/pushes policy | no count; managed-rule refusal | Name subject/destination; refresh rules/convergence | Recreate; `policy.rule_deleted` | Access |
| `setPolicyRuleEnabled(false)` | policy service | Withdraws rule, recompiles/pushes; distinct disable audit | no count | State exact access withdrawal; refresh rule/policy state | Re-enable; `policy.rule_disabled` | Access |
| `revokeInvitation` | invitation service | Invalidates pending invitation | no count | Name recipient; refresh invitations | Send new invitation; audit | Users |
| `deactivateMember` | member service | Freezes access, revokes sessions and owned credentials | credential count is operator-visible prerequisite | Name member/credential impact; refresh roster/devices | `reactivateMember` cause-limited recovery; audit | Users |
| `deleteOrganization` | `SoftDeleteOrganization` | Soft deletes only empty org; refuses gateways/sites/devices/clusters/machine credentials | preflight endpoint exists; no force | Render server blockers; refresh org switcher | No undelete UI; `org.deleted` | Settings |
| `archiveAlertDestination` | alerting service | Archives destination and stops future delivery | no count | Name destination/subscriptions; refresh alerting | Recreate/reactivate only if API supports; audit | Settings |
| `removeAlertDestinationSubscription` | alerting service | Stops named event delivery | no count | Name destination/event; refresh subscriptions | Add subscription; audit | Settings |
| `deleteSite` | `sites.DeleteSite`; FK cascade | Deletes subnets and site policy rules; unbinds node; refuses live agent/template references | `getSiteReferences` returns rule/subnet counts | Render server counts and named site; refresh topology | Recreate manually; `site.deleted` | Sites |
| `removeSiteDNSForward` | DNS forwarding service | Withdraws forwarded zone on reconcile | no count | Name domain/site; refresh DNS/reconcile state | Re-add; `site.dns_forwarding_removed` | Sites |
| `unbindSiteNode` | site binding service | Removes gateway/site placement | no count | Name gateway/site; refresh both | Bind again; audit | Sites |
| `deregisterK8sCluster` | K8s service/FK cascade | Deletes cluster and dependent service/observation records | server cascade count used for audit; no preview response | Name cluster/cascade effect; refresh clusters/services | Re-register/re-expose; audit | Kubernetes |
| `unexposeK8sService` | K8s service | Withdraws VIP/DNS service exposure | reference refusal where applicable | Name service/VIP/DNS; refresh services | Expose again; audit | Kubernetes |
| `removeSiteSubnet` | sites service | Withdraws route; sweeps dependent DNS forwards | audit names swept zones; no response count | Name CIDR; refresh topology/reconcile state | Re-add/approve; `site.subnet_removed` | Sites |
| `archiveAgentGroup` | `ArchiveAgentGroup` → `agenttemplates.Service.ArchiveGroup` (`agent_template_handlers.go:276`, `agenttemplates/service.go:219`) | Sets `agent_groups.archived_at`; refuses with `409 agent_group_not_empty` if members or active assignments exist. | The handler puts member/assignment/generated-rule counts in the refusal text; success is `204`; no separate preview endpoint. | Confirm group name and state the server may refuse until members/assignments are removed; then refetch group list. | Recreate only; exact audit `agent_group.archived`. | AI Agents |
| `archiveAgentPolicyTemplate` | `ArchiveAgentPolicyTemplate` → `agenttemplates.Service.ArchiveTemplate` (`agent_template_handlers.go:398`, `agenttemplates/service.go:413`) | Sets `agent_policy_templates.archived_at`; existing assignments are preserved. | No affected-resource count or preview response; success is `204`. | Confirm template name and “active assignments remain in force”; refetch templates/assignments. | No unarchive operation found; exact audit `agent_policy_template.archived` with `live_assignments_preserved: true`. | AI Agents |
| `removeAgentGroupMember` | `RemoveAgentGroupMember` → `agenttemplates.Service.RemoveMember` (`agent_template_handlers.go:329`, `agenttemplates/service.go:295`) | Removes member, recompiles derived policy, pushes changed gateways when assignments exist. | `200 AgentPolicyTemplateRemovalImpact` reports assignments, generated rules, withdrawn tuples, changed gateways; no pre-action preview route. | Confirm agent and group; after `200`, render returned impact then refetch group/profile/policy evidence. | Re-add member; exact audit `agent_group.member_removed`. | AI Agents |
| `removeAgentPolicyTemplateAssignment` | `RemoveAgentPolicyTemplateAssignment` → `agenttemplates.Service.RemoveAssignment` (`agent_template_handlers.go:513`, `agenttemplates/service.go:623`) | Deletes bindings/orphaned derived rules, marks assignment removed, recompiles/pushes policy. | `200 AgentPolicyTemplateRemovalImpact` gives binding/rule/tuple/gateway effects after action; no pre-action preview route. | Confirm assignment plus access withdrawal; after `200`, show returned impact and refetch assignment/profile/policy evidence. | Reapply a template version; exact audit `agent_policy_template.assignment_removed`. | AI Agents |
| `rejectAgentAccessRequest` | agent access transition handler → `agentaccess.Service` (`agentaccess/service.go:315`) | Pending request becomes rejected; no policy rule is created. | No affected count/preview; `200 AgentAccessRequest` (or idempotent replay). | Confirm requester/destination/reason; refresh request detail/list. | Request again; exact audit `agent_access.rejected`. | AI Agents |
| `cancelAgentAccessRequest` | agent access transition handler → `agentaccess.Service` (`agentaccess/service.go:315`) | Pending request becomes cancelled; no policy rule is created. | No affected count/preview; `200 AgentAccessRequest` (or idempotent replay). | Confirm request; refresh request detail/list. | New request; exact audit `agent_access.cancelled`. | AI Agents |
| `revokeAgentAccessRequest` | agent access revoke transition → `agentaccess.Service` (`agentaccess/service.go:367`) | Approved request becomes revoked and its managed policy rule is deleted. | No pre-action count/preview; `200 AgentAccessRequest` (or idempotent replay). | Confirm agent/destination and immediate access withdrawal; refresh request, agent, policy and activity. | New request; exact audits `agent_access.revoked` and `policy.rule_deleted` with cause `agent_access_revoked`. | AI Agents |

## Implementation rule

The owning story must re-read the named handler and migration before implementing its dialog, replace any “audit” shorthand with the exact emitted action, and add a server preview endpoint only when the table marks a missing count and a safe confirmation requires one.
