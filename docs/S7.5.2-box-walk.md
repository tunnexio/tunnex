# S7.5.2 IdP-group sync — box-walk (live, against real Microsoft Entra)

Two-gateway box (`40.65.63.141`), enterprise API, **real Entra tenant** (not the fake harness —
that proves `entra.go`'s live Graph path: app-cred token + `@odata.nextLink` pagination +
`accountEnabled`→status). Directory mutated in the Azure portal between legs; each leg's evidence is
captured + committed in-session (the S7.5.1 unstrandable-artifact discipline).

## Fixtures (Leg 0 — Entra prep)

| Thing | Value |
|---|---|
| Tenant id | `4a898aae-0274-4843-9416-1a0c2bc97357` |
| App (registration) | `tunnex-dev` |
| Client id | `ff341a6d-3a06-4e10-8e3d-a6983784ebde` |
| Graph permission | `GroupMember.Read.All` (Application) — **admin consent granted** ✓ |
| Group | `tunnex-eng`, Security, cloud |
| Group object id (`idp_group_id`) | `24b3e9c5-893c-48ad-8fa0-74c31bcc932e` |
| Members | LInkUser (`a33bec5e-54ba-46c2-a6e8-47a97fd22bf1`), Test User (`4e53e105-f28c-4ff0-81ac-af4dd56a27e0`) |

The client secret is held on the box only (never in the repo / chat).

## Leg 1a — Graph credential probe (proves the read path + reveals the join keys) — PASS

Proves `entra.go`'s live path with these creds: client_credentials token mint, group-members read,
`accountEnabled`→status input, and the `mail=null → userPrincipalName` fallback.

Prep finding (caught by probing FIRST): `GroupMember.Read.All` alone enumerates member ids but
nulls their user attributes — reading `mail`/`userPrincipalName`/`accountEnabled` needs
**`User.Read.All` (Application)** too. Added + admin-consented; then a FRESH token (app-only tokens
bake granted roles at mint time — the stale token kept returning null until re-minted).

```
token length: 2007        # client_credentials mint OK
$ curl .../groups/$GROUP/members/microsoft.graph.user?$select=id,mail,userPrincipalName,accountEnabled
[
  { "id":"4e53e105-f28c-4ff0-81ac-af4dd56a27e0", "mail":null,
    "userPrincipalName":"testuser@iotunnexoutlook.onmicrosoft.com", "accountEnabled":true },
  { "id":"a33bec5e-54ba-46c2-a6e8-47a97fd22bf1", "mail":null,
    "userPrincipalName":"linkuser@iotunnexoutlook.onmicrosoft.com", "accountEnabled":true }
]
```

Join keys (lowercased mail||UPN): `testuser@iotunnexoutlook.onmicrosoft.com`,
`linkuser@iotunnexoutlook.onmicrosoft.com` — both active.

REVIEW LEDGER: `statusOf(nil)` treats a null `accountEnabled` as active — a fail-OPEN if Graph ever
returns it null (e.g. the missing-permission case above). Worth a defensive guard so a permission
misconfig can't silently skip deprovisioning. (Deferred to the story-end review.)

## Leg 1 — map + trigger → grant appears on the gateway forward chain — PASS

Real Entra → reconciler → group_members → compiler → nft. Fixtures: demo org, gateway `demo-gw`
(`019f66cb…`) enrolled + `agent_ready`, 2 seeded devices (`10.80.0.10` testuser, `10.80.0.11`
linkuser), enforcing, resource `walk-dst` = `10.80.9.0/24`, rule src=synced-group → resource.

```
--- PUT config ---   (Leg 5: fingerprint-only, NO client_secret echoed)
{ "client_id":"ff341a6d-…","provider":"microsoft","tenant_id":"4a898aae-…",
  "enabled":true, "secret_fingerprint":"35b3f0bc403f", "last_sync_ok":false, "sync_health":"degraded" }

--- map group ---
{ "id":"019f66d4-2472-…","origin":"idp_sync","idp_provider":"microsoft",
  "idp_group_id":"24b3e9c5-893c-48ad-8fa0-74c31bcc932e","name":"tunnex-eng" }

--- rule ---  201: src_group=019f66d4-2472 → dst_resource=019f66cf-4293 (10.80.9.0/24)

--- trigger ---
{ "provider":"microsoft","last_sync_ok":true,"sync_health":"ok","last_sync_at":"2026-07-15T17:30:20Z" }

--- group_members (pulled from REAL Entra) ---
 linkuser@iotunnexoutlook.onmicrosoft.com | idp_sync
 testuser@iotunnexoutlook.onmicrosoft.com | idp_sync

--- gateway forward chain (table ip tunnex) ---
    chain forward {
        type filter hook forward priority filter; policy drop;
        ct state established,related accept
        ip saddr 10.80.0.10 ip daddr 10.80.9.0/24 counter ... accept   # testuser
        ip saddr 10.80.0.11 ip daddr 10.80.9.0/24 counter ... accept   # linkuser
        counter ... comment "tunnex_default_drop"
    }
```

Both directory members' /32 grants are live on the data plane. Leg 5 (fingerprint-only) folded in.

## Leg 2 (Red 1) — group-removal death — PASS

Removed **Test User** from the `tunnex-eng` Entra group (portal), then triggered.

```
--- group_members (only linkuser remains) ---
 linkuser@iotunnexoutlook.onmicrosoft.com | idp_sync

--- testuser account status (STILL active — removal != deactivation) ---
 testuser@iotunnexoutlook.onmicrosoft.com | active

--- forward chain (10.80.0.10 GONE; .11 remains) ---
    chain forward {
        type filter hook forward priority filter; policy drop;
        ct state established,related accept
        ip saddr 10.80.0.11 ip daddr 10.80.9.0/24 counter ... accept
        counter ... comment "tunnex_default_drop"
    }

--- audit ---
 group.member_synced_removed | idp-sync | {"cause":"absent_from_directory_group","user_id":"…a1"}
```

Directory removal → group-membership removal → /32 leaves the forward chain (now hits policy drop),
account NOT deactivated, system-actored audit with cause. Red 1 killed.

## Leg 3 (Red 2) — full-disable sweep — PASS

Disabled **LInkUser** (Account status → Blocked = `accountEnabled=false`) in the portal, kept them
in the group, then triggered. Graph returns them as a DISABLED member → routes to the sweep.

```
--- trigger --- { "last_sync_ok":true, "sync_health":"ok" }

--- linkuser status --- deactivated        (the DeactivateMember sweep)
--- group_members --- 0                    (disabled member also leaves the group)

--- forward chain (NO /32 accepts) ---
    chain forward {
        type filter hook forward priority filter; policy drop;
        ct state established,related accept
        counter ... comment "tunnex_default_drop"
    }

--- audit ---
 user.deactivated | (actor_user_id NULL) | idp-sync | {"cause":"disabled_in_directory"}
```

Directory disable → full sweep (status deactivated + peers out of desired-state → /32 gone) +
NAMED system-actor audit with cause. "Revoked by idp-sync because disabled in the directory." Red 2
killed. Both reds proven on the wire against a live Entra tenant.

## Leg 4 — two-tier health (degraded → escalated → recover) — PASS

PUT a wrong secret → trigger → the real Graph `401` surfaces; `last_sync_at` stays FROZEN at the
last good sync (fail-static clock freeze, visible) while `last_sync_ok` flips false.

```
--- trigger (bad secret) / health = IMMEDIATE tier ---
{ "last_sync_ok":false, "sync_health":"degraded", "last_sync_at":"…17:39:58Z (FROZEN)",
  "last_sync_error":"list members of idp group 24b3e9c5…: entra token: status 401" }

--- age last_sync_at 31m (sustained-outage sim) → ESCALATED tier ---
{ "last_sync_ok":false, "sync_health":"escalated", "last_sync_at":"…17:11:04Z" }

--- restore real secret + trigger → RECOVER ---
{ "last_sync_ok":true, "sync_health":"ok", "last_sync_at":"…17:42:05Z" }
```

D2 proven: immediate `degraded` on ANY failure (clock frozen at last success), `escalated` past the
3×poll = 30-min ceiling, clean recovery to `ok`. Membership untouched throughout (fail-static).

## Leg 5 — config read-path is fingerprint-only (S4.5) — PASS (folded into Leg 1)

The PUT-config response returns `"secret_fingerprint":"35b3f0bc403f"` and NEVER the client secret
or the sealed bytes. Config-change is audited (`idp_sync.config_updated`, human actor, fingerprint
only in metadata).

---

## Walk result: ALL LEGS PASS (real Microsoft Entra, two-gateway-class box)

Leg 1a credential probe · Leg 1 grant-appears (real Entra → group_members → compiler → nft forward
chain) · Red 1 group-removal death (/32 leaves, account stays active) · Red 2 full-disable sweep
(deactivated + /32 dead + `actor_system=idp-sync` cause) · Leg 4 two-tier health · Leg 5 fingerprint.
`entra.go`'s live Graph path (client_credentials token, member read, `accountEnabled`→status, UPN
fallback) is now wire-proven, not just unit-mocked.

REVIEW LEDGER (carried to story-end review): `statusOf(nil)` treats a null `accountEnabled` as
active (fail-open); the Leg-1a permission gap showed how a misconfig can null it. Worth a defensive
guard so it can't silently skip a deprovision. → FOLDED (#0) + re-walked below.

## Leg 6 (post-fold) — delete-in-directory → full sweep (1(a)) — PASS

The story-end multi-finder review found a cluster of deprovision FAIL-OPENs on the error/edge paths
this happy-path walk didn't hit (a directory-DELETED user was only un-grouped, never swept; a null
accountEnabled read active; a continuation-page 404 mass-deleted; etc.). All folded (0,2,3,4,5,6,7,8,9
+ 1a) fail-closed with reds; a re-earned review came back with the tripwire CLEAR (no new fail-open).
This leg re-proves the sharpest one on the wire, on the folded image (migration 0028 records the
directory external id so a removed member can be resolved).

Setup: fresh clean-slate, both Entra users active + in `tunnex-eng`, synced (Leg 1) with
`idp_external_id` recorded (`testuser→4e53e105…`, `linkuser→a33bec5e…`), both /32 on the chain.
Then **Test User DELETED from the Entra directory** (portal) → trigger:

```
--- trigger --- { "last_sync_ok":true, "sync_health":"ok" }

--- testuser status --- deactivated     (ResolveUserStatus→404→StatusGone→FULL SWEEP)
--- linkuser status --- active          (untouched — the sweep is surgical)
--- group_members --- linkuser only

--- forward chain (10.80.0.10 GONE; .11 remains) ---
    chain forward {
        type filter hook forward priority filter; policy drop;
        ct state established,related accept
        ip saddr 10.80.0.11 ip daddr 10.80.9.0/24 counter ... accept
        counter ... comment "tunnex_default_drop"
    }

--- audit --- user.deactivated | idp-sync | {"cause":"disabled_in_directory"}
```

A user DELETED from the directory is now fully deprovisioned (deactivated + /32 dead + audited),
distinct from a mere group-move (Red 1, account stays active). Contrast holds on the wire.

FOLLOW (ledger, non-blocking): the audit `cause` is a single fixed string `disabled_in_directory`
for both the disabled and the deleted (gone) paths — a gone user should read `deleted_in_directory`.
Cosmetic audit-precision; threading a per-cause value re-touches the Deprovisioner interface, so
deferred to a small follow (S7.5.2b).
