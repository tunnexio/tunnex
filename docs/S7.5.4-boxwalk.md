# S7.5.4 per-user + temporary grants — box-walk (live wire, founder-present, 2026-07-16)

Two-gateway **enterprise** fixture on the dev VM; netns spokes; live nftables forward chains observed
on the node-agents. Evidence in `walk-artifacts/S7.5.4/` (committed in-session). Scratch WG configs +
private keys gitignored at creation (`scratch/`). **All legs PASS.** One walk-found finding (extend
spurious push) was fixed mid-walk and re-earned — see §Leg 4 + docs/S7.5.4-decisions.md §D8.

## Fixture (Leg 0)

Fresh seed (`make seed` + `make seed-enterprise`) — a prior wipe forced a true reset, so this is a
known-clean baseline, not leftover walk state (see honesty ledger). Enforcing set for the walk.

| Thing | Value |
|---|---|
| Box / edition / migration | dev VM `10.0.0.4` / **enterprise** / **30** |
| Org (`$ORG`) | `01900000-0000-7000-8000-000000000001` (Demo Organization, `zero_trust_mode=enforcing`, pool `10.99.0.0/24`) |
| Subject `$USER` (owner) | `01900000-0000-7000-8000-000000000002` |
| **Gateway A** = `demo-gw` | `019f6b37-5201-7e2b-b48c-ee38ddd5a7f4` — real agent, host-published `51820/udp`; hosts the live subject spoke |
| dev-A (live netns spoke `wa`) | `10.99.0.3` on Gateway A — the traffic subject |
| **Gateway B** = `gw-b` | `019f6b32-a434-7465-b248-6c6174d4a3d1` — real agent, NO published port; **ruleset-only** (no wire) |
| dev-B (ruleset-only) | `10.99.0.2` on Gateway B — the non-observation-gateway projection |
| Non-subject (member) | `01900000-0000-7000-8000-000000000003`; dev-M (live netns spoke `wm`) `10.99.0.4` on Gateway A |
| Resource `walk-dst` | `019f6b34-822d-7736-a55b-56525ec39385` = `10.80.9.0/24` (**dark** — no host listens) |
| Seed gateway node `010` | `demo-gateway` = `seeddata.DemoGatewayNodeID` (seed-enterprise fixed-ID row), agentless — non-participating (full analysis in the honesty ledger) |

**Observation surface:** the node-agent's `table ip tunnex / chain forward` (kernel forward-hook filter)
— the compiled+pushed artifact materialized. An allow line `ip saddr <src/32> ip daddr <dst> accept` =
the grant on the wire; per-rule `counter` = the forward decision exercised. `NFTA` = Gateway A ruleset,
`NFTB` = Gateway B. gw-b hosts B as a **source** device (per-user grant resolves to all the subject's
devices across nodes; allows bucket onto the **source** device's node — `compiler.go add(d.NodeID)`), so
one owner→walk-dst grant projects onto A (dev-A) AND B (dev-B).

---

## Leg 0 — baseline (`leg0-baseline-forward.txt`)  · PASS
Both gateways' forward chain = default-deny only (`ct established accept` + `tunnex_default_drop`), no
owner allow. dev-A probe to `10.80.9.1` → 100% loss; after routing walk-dst through the tunnel, A's
`default_drop` counter = **3 packets / 252 bytes** (the blocked probe reached the forward chain). Exclusion
real BEFORE any grant.

## Leg 1 — per-user grant on the wire + exclusion + F1 org-wide strip + re-grant  · PASS
Files: `leg1-after-grant.txt`, `leg1-after-strip.txt`.
- **Grant** owner→walk-dst → allow `10.99.0.3 → 10.80.9.0/24` on **A** AND `10.99.0.2 → 10.80.9.0/24` on
  **B**. Push latency **Δ = 0.19s** (≪5s).
- **Traffic (flood):** PERMIT dev-A→walk-dst → allow counter climbs `7,53,99,146,192`; EXCLUSION
  dev-A→`10.99.0.50` (non-granted tunnel IP) → `default_drop` climbs `10,56,102,148,194`.
- **F1 org-wide strip:** DELETE the rule → allow **vanishes from A AND B** (default-deny only),
  **Δ = 0.33s**. The org-wide push reaches the non-observation gateway B.
- **Re-grant-on-wire:** re-create the same grant → allow returns on A.

## Leg 2 + 3b — temporary lapse → swept + audited on the wire, delete-on-sweep, non-obs B strip  · PASS
File: `leg2-after-sweep.txt`. Temporary grant (`expires_at = +40s`) live on A + B, row present. On the
sweep tick:
- allow **stripped from A AND B** — **Δ(expiry→strip) = 7s** (≤60s). The BACKGROUND sweeper's `pushOrg`
  (no request context) reaches gw-b — the armed-guard call-site, discharged on the wire (ONE execution).
- **delete-on-sweep (Option A):** row **DELETED** (0 rows), not lingered.
- **`grant_expired` system audit** — verbatim:
  ```
  policy-grants|policy.grant_expired|019f6b47-3516-776d-a09b-43fcd52d27ed|{"cause": "grant_expired"}
  ```
  (`actor_system=policy-grants`, cause recorded.) **Re-grant after lapse** works (delete-on-sweep leaves
  no lingering row to conflict).

## Leg 3 — downtime lapse (stateless sweep closes the audit gap)  · PASS
File: `leg3-after-restart.txt`. Temporary grant created, API stopped **while the grant was still valid**
(sweeper is in-process → down across the expiry). Past expiry, API down: allow **lingered** on A
(bounded, safe-additive direction) + row present-but-expired + **0 audits yet**. On restart, TWO
mechanisms (see decisions §two-mechanism):
- **compiler active-rules filter** strips enforcement on the post-restart refetch — **≈3s** (fast).
- **stateless sweeper** DELETEs the row + writes the `grant_expired` audit on its next tick — **+62s**;
  `row_remaining=0`, audit then present. The stateless sweep (no window cursor) picks up the grant that
  expired DURING downtime → audit-gap closed by construction, nothing lost.

## Leg 4 — extend: WALK-FOUND FINDING → fixed → re-earned  · PASS (after fix)
Files: `leg4-reearn.txt`; fix commit `3a6d7d3`.
- **Finding (pre-fix):** extend churned the wire — allow handle A `426→430`, B `510→514`, counters reset.
  Root cause: `ExtendGrant` used `s.mutate` (unconditional `pushOrg`) → byte-identical org-wide re-apply.
  Contradicted the design intent. Halted, surfaced, dispositioned FIX-NOW. See decisions §D8.
- **Fix + red:** `ExtendGrant` uses `withTx` (no push); `TestExtendGrantWindow` red asserts no push (FAIL
  pre-fix on an isolated DB, GREEN post-fix; push-pins for create/member/mode/rule stay green). Focused
  mini-review: no issues.
- **Re-earn (post-fix, on the wire):** handles **IDENTICAL** across extend — A `720/720`, B `804/804`
  (`ZERO-CHURN: PASS`); node logs **silent** in the extend window; window moved (`+30m→+60m`);
  `grant_extended` audit `old=15:06:38 ≠ new=15:36:40` (D7/[6]). The comment's "no spurious push" intent
  is now TRUE on the wire and pinned by the red.

## Leg 5 — per-user ENFORCEMENT SEMANTICS  · PASS
File: `leg5-per-user-semantics.txt`. One live grant owner→walk-dst. Same destination, two subjects:
- **subject** (owner `10.99.0.3`) → walk-dst: allow line present; flood climbs allow counter `7,53,98`.
- **non-subject** (member `10.99.0.4`) → **same** walk-dst: **no** allow line; flood climbs `default_drop`
  `7,56,102`.
Only the named user passes — the grant discriminates by identity, not blanket. This is the semantics leg.

## Negatives
- **Idempotence:** `grant_expired` audit count = **1** for both the Leg-2 (`019f6b47…`) and Leg-3
  (`019f6b49…`) grants, checked ~20 min / many ticks later. Delete-on-sweep is idempotent — no re-audit.
- **IP-attribution pin:** exactly TWO owner-active devices — `10.99.0.2` (node `019f6b32…` = Gateway B)
  + `10.99.0.3` (node `019f6b37…` = Gateway A); no duplicate `assigned_ip`. Clean pool reuse, not ghosts.
  (Leg 5 adds member `10.99.0.4` on Gateway A as the non-subject.)

---

## Honesty ledger

- **`walk-dst` is dark** (no host listens) — replies never return (`ping` exits 1). The accepted
  observation is **forward-chain counter movement** (allow vs `default_drop`), i.e. the packet's VERDICT
  at the enforcement hook, not a round-trip. The forward chain is the compiled+pushed artifact in the
  kernel — watching its allow-set + counters IS the wire proof.
- **Counter resets:** a trailing ruleset listing showing `0` counters after a flood is a **fresh listing
  after a push/re-apply reset the chain**, not evidence the flood never happened — the in-flight climbing
  samples are the datum.
- **dev-M first-create `401`:** the walk script `cd`'d into `scratch/` then referenced the cookie jar by
  a repo-root-relative path → `curl` sent no cookie → 401. Retry from repo root = 201. **Client-side
  script path bug; the API behaved correctly (no 5xx).** Recorded per the "no reassuring-green" rule.
- **gw-b is ruleset-only** — dev-B has no wire; Leg-3b is proven by **ruleset observation** on B (allow
  appears then strips), not traffic through B. That is the entire intended proof for the non-observation
  gateway.
- **Source-node bucketing:** allows bucket onto the **source device's node** (`compiler.go add(d.NodeID)`,
  unchanged since S7.1). "Non-hosting gateway" (S7.3 wording) = non-hosting **of the destination**. The
  Leg-3b discriminator is "the sweep's `pushOrg` reaches every gateway hosting a **source** projection of
  the lapsed grant" — gw-b qualifies via dev-B.
- **Shared compose-DB hazard:** the Go integration suite + e2e point `TUNNEX_TEST_DATABASE_URL` at the
  compose postgres — running `make test*`/`e2e` against a live box-walk stack **wipes + repollutes the
  fixture** (hit twice mid-walk: 716 leaked orgs). The D8 fix was verified against an ISOLATED throwaway
  postgres. **Do not run gates/tests against a live walk stack.**
- **Pollution-vs-evidence reconciliation (every leg measured on the clean rebuilt fixture):** both
  mid-walk DB disturbances fall in the PRE-LEG recovery window. **Event 1** — seed WIPED (→ 716 test
  orgs) — was recovered by `down -v` + reseed (`seed_complete 13:44:00Z`, `seed_enterprise_complete
  13:45:00Z`). **Event 2** — a transient ADDITIVE burst (716 test orgs, never wiped the fresh seed) —
  appeared ≈13:46Z and drained back to clean `1|1|0|1` by ≈13:48Z (verified via `pg_stat_activity` + a
  node-count watch). The walk fixture was then rebuilt clean: enforcing + gw-b enrolled `13:51:48Z` +
  demo-gw enrolled `13:56:55Z` + devices. The **earliest** leg measurement (Leg 0, `14:02:50Z`) begins
  ~6 min after the fixture completed and ~14 min after the last pollution cleared; every leg window runs
  `14:02:50Z–14:41:25Z`, **entirely outside both events**. NO leg was re-run for pollution — the single
  re-run (Leg 4) was for the D8 extend-fix.
- **Seed node `010` (named, not a walk artifact):** it is `seeddata.DemoGatewayNodeID` (`demo-gateway`) —
  a fixed-ID gateway row that `seed-enterprise` INSERTs (`cmd/seed-enterprise/main.go:123`), agentless
  (no agent enrolled as it; the container enrolled as `demo-gw` `019f6b37`). `status=active`, so it IS in
  `ListActiveNodeIDsForOrg` (all active nodes) → an over-notify push TARGET on every org-wide push — but
  it has **no hub subscriber**, so the signal is a discarded no-op. **This walk's Δ timings are unaffected**
  (measured by polling A/B rulesets, never 010). Hosts 0 devices (strandable revoked). Harmless known seed
  remnant; S7.5.5 starts from the same seed, so it's expected, not inherited debt.
- **Pool/CIDR:** device pool = `10.99.0.0/24` (masquerade scoped to it — internet-egress NAT only,
  orthogonal to the forward-chain filter). `walk-dst 10.80.9.0/24` is a dark resource CIDR routed through
  the tunnel (spoke `allowed-ips` extended) purely so probes transit the forward chain.
- **Atomic apply:** the node applies its whole ruleset in ONE atomic `nft -f -` transaction — any
  re-apply swaps old→new atomically, no enforcement gap. Load-bearing (see decisions §atomic-apply).
- **v3 = safe-by-convention:** `ProtocolVersion` bumped 1→2→3 with no consumer fail-closed gate; safe ONLY
  because every bump is observability-additive + hash-blind + safe-ignore (each unit-pinned). The gate is
  a BLOCKING S8.1 item (PLAN.md, decisions §D5-reaffirmed). A convention holding is not a guarantee.

---

## Disposition

All legs PASS. One walk-found finding (extend spurious push) fixed + re-earned (`3a6d7d3`). Standing
fixture: **gw-b left up** as the two-gateway enterprise env for S7.5.5 (S7.3 precedent). netns spokes
`wa`/`wm` torn down post-walk; `walkA`/`walkB` kept as standing ruleset fixtures, `walkM` revoked (see
cleanup). **Both-green** against an ISOLATED DB (never the walk fixture) → **merge-time branch-protection
PRESENCE check** (`gh api …/branches/main/protection` ≠ 404, `required_linear_history.enabled=true`) →
ff-merge on Pawan's explicit in-session word → PLAN.md pointer update.
