# S20.5 — AWS re-entry and main integration

Status: implementation authorized; gates, independent review, live qualification,
and merge acceptance remain outstanding. This is not the PLAN checkpoint.

## Authority and preservation

On 2026-09-05 the user selected this laptop's repository and AWS, requested the
latest main, and authorized the work required to fix the Kubernetes blockers
and eventually merge without affecting unrelated work. The root checkout is
`codex/control-plane-ui-refinement` at
`ae266442a42e408484e97bba024f37b7e85c6286`, clean and unstaged at re-entry.
It, all other existing worktrees, and all existing stashes remain untouched.

Neither the local repository nor the committed source contains AGENTS.md.
Committed CLAUDE.md, the original explicit re-entry contract, and the fresh
user instructions are the available process authority. No missing instruction
is represented as having been read.

## Integration decision

- Exact main baseline: `0f3d9b0495aebd6c01dfd1c6791d1e54390a0c8d`.
- Exact S20.5 source: `8a1e411530eb477b9734db5b9c44db5df392f416`.
- Shared baseline: `b7a18f52b336f4d390ddfa382975115d740ba50a`.
- Work is isolated on `codex/s205-aws-reentry`. Bring the source change set onto
  latest main as a reviewed, linear integration slice. Preserve the old branch
  and its complete history; do not rewrite or force-push it. No merge commit or
  direct push to main is introduced.
- Resolve semantic overlaps by retaining both main's independently shipped
  behavior and the S20.5 lifecycle contract. Regenerate OpenAPI/sqlc/RBAC output
  from authored sources instead of hand-resolving generated types.

Main now owns migrations 0127–0130. The unmerged S20.5 chain moves intact from
0127–0132 to 0131–0136. This is a numbering reconciliation, not a SQL behavior
rewrite. Update references, migration tests, schema capability thresholds, and
current qualification instructions together. Main's migration bytes remain
unchanged. Historical evidence retains the version actually observed and is
never rewritten as new proof. The final migration and N-1/current bridge tests
must run on PostgreSQL; no prior schema result counts.

## Review dispositions and boundaries

The fresh authorization covers necessary fixes for confirmed S20.5 blockers,
including the recorded pre-walk defects and current CI failures. Re-verify
each finding against the integrated source before folding it; preserve the
existing ranked review record and add explicit per-finding dispositions and
proof rather than assuming all 48 reports are correct. A finding contradicted
by the locked design is rejected with evidence, not changed to satisfy prose.
New material product choices and repeated-fold tripwires are surfaced before
an implementation expands the existing contract. Feature-sized fixes earn
independent review again. Incomplete review never counts as clear.

The Windows enrollment/explicit-abandon tests remain in this story. Forcing a
Darwin persistence fixture to perform actual POSIX directory fsync on a
Windows runner is not evidence of the intended fault boundary. Repair that
test seam while preserving production durability and actual platform coverage.

The named deferrals remain unchanged:

- S20.5.1: automatic Windows terminal-uninstall crash recovery.
- S20.5.2: crash-safe private candidate rollout and automatic recovery.
- S20.6: ordinary host-reboot rebootstrap.

Operator recovery procedures do not satisfy their zero-touch acceptance.

## Local gate isolation

Use explicit `COMPOSE_PROJECT_NAME=tunnex-s205-aws-20260905a`. Read-only
inspection proved its project network and four named stores absent at re-entry.
Their names are the project prefix plus `_default`, `_postgres_data`,
`_redis_data`, `_tunnex_secrets`, and `_tunnex_node_state`. Verify labels and
database identity again before each database gate. Do not use the default
`tunnex` project or the existing `tunnex-cp-review` project.

Codegen and web Make recipes also hard-code three unlabelled dependency caches
(`tunnex-nm`, `tunnex-web-nm`, `tunnex-shared-nm`). Isolation must cover these
as well as Compose. Use configurable cache names with unchanged defaults;
the S20.5 gates select only fresh project-prefixed caches. This is test-harness
isolation, not permission to clear existing caches or stores.

Run the required codegen, migration, both-edition test/build, CLI, operator,
node, helper/race/cross-compile, web, desktop, and chart gates on final content.
Checks attached to source SHA 8a1e411 are red (`gates`, Windows client); macOS
passed. Main already contains the installer latest-release marker fix, but
only a new exact-final-SHA run can establish green. Old partial results and
PR synthetic-merge results are not final-tree qualification.

## AWS qualification boundary

Read-only STS verified AWS account `735391218823`, IAM principal `aws-cli`, and
configured region `ap-south-1`. EKS list-clusters in that region returned no
clusters. This is not a claim about other regions. Do not mutate Azure or
anything associated with Bolster.

AWS/EKS replaces AKS as the selected live qualification target. Preserve every
provider-neutral box-walk invariant and failure-injection limit; do not relabel
unrun AKS legs as EKS evidence. Before provisioning or deployment, present the
exact resource/cost/isolation plan and re-verify the target account, region,
cluster, and complete registry identity. Existing control planes, gateways,
DNS, IAM credentials, and workloads are not disposable test fixtures.

No SCP patches, Secret/PVC/CNI edits, manual host-network repairs, or remedial
rollout restarts earn a successful leg. Credential material stays outside Git;
only redacted evidence is committed during a live walk. A missing CNI mechanism
is a product qualification blocker, never permission for a manual cloud patch.

## Completion sequence

The user's later live-first instruction is recorded in
`S20.5-aws-live-walk-plan.md`: begin isolated AWS diagnosis before PR while
explicitly retaining red gates and held decisions. SCP is authorized transport;
manually repaired legs remain diagnostic until rerun from final source. Formal
qualification still requires the reviewed final candidate. All required
live legs must pass before merge readiness. After non-pointer content is final,
create or update a draft PR, obtain its real number, write PLAN as the final
commit naming that PR and the preceding content tip, push the exact head, and
obtain all required checks for that head. Merge uses the freshly read head SHA
with `--match-head-commit`, linear history, and no bypass. Any new content after
the pointer requires a new final pointer and fresh checks. Resource cleanup
still requires an exact target list and separate approval.
