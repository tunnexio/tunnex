# S15.1 — founder review script

**Branch** `story/S15.1-owned-machine-principal` · **PR #86**
**Not merged. This script is for looking, not for signing off.**

The screen is **Settings → GitOps operator credentials**. Settings is a **single scrolling page — no
tabs** — and the panel is the last section on it. It is gated `org && isAdmin` and then
`machine:manage` (owner-only), so it is *absent*, not empty, for every other role.

**Not edition-gated**, and that is verified at the handler rather than inferred from the bundle: the
list endpoint returns **HTTP 200 with the same rows on :8081**. Both stacks serve the same web image, so a bundle grep could never
have told you this.

---

## Before you start

```
http://localhost          enterprise edition   (verify: curl -s localhost/api/v1/meta)
http://localhost:8081     open edition         (same screen, same data shape)

login   owner@demo.tunnex.local
pass    tunnex-demo-password
```

Two orgs, one login. ⛔ **There is NO switcher — see below.**

| org | slug | what it is for |
| --- | --- | --- |
| Demo Organization | `demo` | the four per-row states |
| Demo EU | `demo-eu` | the banner state |

⚠ **`demo-eu` HOLDS CREDENTIALS AND NOTHING ELSE.** Its Overview, gateways, sites and devices
are all empty — that is the fixture, not a defect.

⛔ **AND THERE IS NO ORG SWITCHER IN THE PRODUCT — THE FIRST VERSION OF THIS TABLE WAS WRONG.** It said
*"the switcher is in the top bar."* There is none, anywhere: every page reads **`orgs?.[0]`** and the
list is ordered `ORDER BY created_at`, so you get whichever org you joined first and no control can
change it. Registered as row 2 of `docs/REGISTER-shipped-delivery-defects.md` — **a user in two orgs
cannot reach the second one at all.**

**Which org you see is chosen by `created_at`, so it is switched in SQL, not in the UI:**

```sql
-- show the all-owned org (currently ACTIVE — this is what you will land on)
UPDATE organizations SET created_at = now() - interval '10 years' WHERE slug = 'demo-eu';
-- put the four per-row states back
UPDATE organizations SET created_at = now() - interval '1 hour'   WHERE slug = 'demo-eu';
```

⛔ **This is a review procedure and is NOT the state being reachable in the product.** It needs database
access. No user or operator has that path.

---

## What this story actually changed

A machine credential could not name a person. `machine_credentials` had **no `user_id` column**, so
every machine principal that has ever authenticated against this control plane did so **ownerless** —
the audit trail recorded *a machine*, never *whose machine*. S15.1 adds the column, the principal
field, the refusal, and the surface that assigns it.

> ⛔ **THE REFUSAL IS THE STORY, NOT THE PICKER.** An unassigned credential no longer authenticates —
> `MachineAuth` returns `nil, nil` when `user_id` is NULL. The picker exists so that refusal is
> *recoverable* rather than a wall.

---

## 1. The four per-row states — org `demo`

Five rows. Read the right-hand column of each; the four combinations are all present deliberately.

| row | owner | last seen |
| --- | --- | --- |
| `gitops-prod` | — unassigned | never seen |
| `gitops-staging` | — unassigned | ~2 hours ago |
| `ci-runner` | owner@demo | ~11 minutes ago |
| `backup-agent` | owner@demo | never seen |
| `k8s-operator-us-east` | — unassigned | never seen |

**What to look at:** `never seen` must read as a *fact*, not as an error or a blank. It is the honest
rendering of `last_used_at IS NULL` — a credential minted and never used.

⚠ **The two unassigned-with-an-owner rows are the ones that matter.** They are what the assignment
flow produces, and they are seeded rather than clicked so you are reviewing the *rendering*, not your
own click.

## 2. The banner — org `demo-eu` (ACTIVE NOW; use the SQL above to go back)

Two rows, both owned. The banner appears: **every machine credential has an owner**.

> ⛔ **IT NEEDED A SECOND ORG, AND THAT IS NOT ARBITRARY.** The banner is
> `creds.data.every(c => c.owner_user_id)` — one unassigned row anywhere in the org suppresses it. So
> a second *set* inside `demo` could not reach the state; only a second *org* can.

⚠ **And assigning the two demo rows to reach it would have reviewed a different thing.** That
exercises the assignment flow and leaves the banner glimpsed at the end of an interaction, rather
than seen as the screen's own answer to *is the migration done*.

One row here has a last-seen and one has never been seen — so if the banner is accidentally coupled
to that field, it shows here rather than in production.

⛔ **THE COPY IS A CLAIM ABOUT OWNERSHIP, NOT ABOUT HEALTH** — *"Every machine credential has an owner
— none is being refused for want of one."* It previously said *"the migration is complete for this
organization"*, which reads as *everything is fine* **and is false on its own terms**: this is the
expand half, and step 4's `NOT NULL` contract has not run. An org can reach every-row-owned with the
column still nullable beneath it. A test now rejects `migration is complete`, `everything`,
`all good/well/fine` and `healthy`.

## 3. The assignment flow — back in `demo`

Assign an owner to `gitops-prod`. The picker opens with **"Choose an owner…"** selected, not with a
person pre-selected.

⛔ **Deliberate.** A pre-selected first-in-list owner means a mis-click writes a *plausible* wrong
owner into an audit-bearing field, and nothing downstream can tell it from a decision.

After assigning, the row moves to owned. The banner does **not** appear — four rows are still
unassigned, which is correct.

⛔ **§3 IS ONE-WAY, AND RE-SEEDING DOES NOT UNDO IT.** There is no unassign: the API exposes
`assignMachineCredentialOwner` and `revokeMachineCredential`, and nothing that clears an owner back to
NULL. Re-running `make seed-fixtures` will *not* restore the row either — the insert is
`ON CONFLICT (id) DO NOTHING`, so an already-present row is left exactly as you left it.

To put §1 back, org-scoped:

```sql
UPDATE machine_credentials SET user_id = NULL
WHERE org_id = '01900000-0000-7000-8000-000000000001' AND name = 'gitops-prod';
```

⚠ **Whether "no unassign" is right is a live question, not a settled one.** D19 ruled
assign-explicitly; it did not rule on reversal. A wrong owner is currently only correctable by
revoking the credential and minting a new one — worth your ruling, and out of scope for this slice.

## 4. The three empty states

⛔ **THE FIRST PASS DID NOT REACH TWO OF THESE, AND THE REVIEW IS NOT PASSED UNTIL IT DOES.** §2's
banner and `load-failed` were both unseen. `all-owned` is one org switch away. `load-failed` needed a
method, and there was none in the first version of this script — which is why it went unlooked-at.

**To see `load-failed`:** DevTools → Network → find the `machine-credentials` request → right-click →
**Block request URL** → reload Settings. The rest of the page keeps working; only this panel fails.

⚠ **Stopping the `api` container does NOT work for this** — the panel is gated on `org && isAdmin`, so
with the API down the org never loads and the panel does not render at all. The state you are looking
for is *this one query failing while everything else succeeds*, which is exactly the case that would
otherwise render as a reassuring "none".

| state | when | why it is not a blank |
| --- | --- | --- |
| `load-failed` | the list request fails | says the list could not be loaded — it does not render as "no credentials", which would be a **false zero** |
| `none` | org has zero credentials | says there are none, and offers the create path |
| `all-owned` | every row owned | the §2 banner |

⚠ **`load-failed` versus `none` is the point.** A failed load rendering as an empty list is a zero
nobody measured — this repo's recurring defect class, and the reason the three are distinct
`data-state` values rather than one falsy branch.

---

---

## Second pass — what changed since your first look

Two findings, both fixed; one question, surfaced and not decided.

**1. An assigned row now shows its owner** — `· owner owner@demo.tunnex.local`.

⛔ **AND THE FIRST FIX WAS WRONG, CAUGHT BEFORE YOU SAW IT.** I rendered `c.owner_email`, since the
DTO carries it. **The handler deliberately never populates it** — the web already fetches the member
roster for the picker and resolves ids there, so resolving server-side too would be a second source
of truth for one fact. Every assigned row would have rendered *"unknown"*. It reads the roster now.

The check that caught it was calling the real endpoint and looking at the values: `owner_email` came
back `null` on both owned rows. The unit test could not have caught it — the stub returns whatever
the fixture says, and my fixture said the field was populated.

⚠ **So the real defect is one layer worse than "nothing reads it": `owner_email` is served on every
response and is ALWAYS NULL.** A field that exists, is documented as *"resolved from owner_user_id
for display"*, and is never resolved. The next consumer reads it and gets the same bug I did.
**That is a decide-item, not a fold** — remove it from the spec (the roster is the one resolver), or
populate it server-side (and accept two resolvers). Registered as **D22**; not touched here.

An owner who is **not on the roster** renders `not a member of this organization` rather than blank —
a blank owned row is indistinguishable from an unowned one. Reachable for a real reason: the FK is
`ON DELETE RESTRICT` on `users`, but nothing pins the *membership*, so an owner who leaves the org
keeps the credential and drops off the roster.

**2. Unassigned now states the outage.** Above the rows, in the danger tone, with a count:
*"N machine credentials are being refused right now — a credential with no owner cannot
authenticate."* The per-row chip reads **`no owner — refused`**, not `unassigned`. The refusal
sentence moved out of the footnote under the list; what remains there is only why nothing is
pre-selected.

⚠ **The banner is placed above the rows for the same reason the all-owned banner is** — a qualifier
under a list is read after the list is already believed.

### Why the tests did not catch either one

The fixture had **no `owner_email` field at all**. The DTO serves it, the fixture omitted it, so no
test could fail when the component ignored it — a fixture narrower than the contract cannot catch a
consumer that ignores the difference. Fixed, plus four tests: owner-on-the-row, the unresolvable
owner, the refused banner and its count, and the banner's absence when all are owned.

Both fixes were mutation-checked: reverting the badge text fails 1 test, dropping the owner render
fails 2. Both mutations were confirmed applied by grep before the run.

### Your question 4 — surfaced, not decided

**Should an unverified account be assignable as the accountable owner of a machine credential?**

The picker lists all members, `unverified-admin@demo.tunnex.local` among them. The facts, so the
ruling is yours:

- An unverified account **cannot perform org mutations** — `canEdit={emailVerified}` gates the
  surrounding Settings controls.
- But ownership here is **accountability, not capability**: the owner does not act, the machine acts
  *for* them. Excluding unverified accounts would mean a credential cannot be assigned to a person
  who genuinely is responsible for it until they click a link in an email.
- And the inverse: an unverified address may not reach a real person at all, which makes it a weak
  thing to hang an audit trail on.

⛔ **Not resolved unilaterally** — this is a decide-item, and D19 ruled assign-explicitly without
ruling on who is eligible. It is registered as **D21**.

---

## What is NOT in this PR, and is not an oversight

- **D4 / D15 / step 4** — ruled, scheduled, deliberately out of this slice.
- **Backfill of existing credentials.** The column is nullable by design: this is the expand half of
  an expand/contract migration. Existing rows keep working as *unassignable* until assigned, and the
  NOT NULL lands only after the surface has been used.
- **Any revoke control over `managed_by_machine`** — ruled out, unchanged here.

## Known-unverified

- **CI: read it on whatever `gh pr view 86 --json headRefOid,statusCheckRollup` reports as the head
  RIGHT NOW — this document deliberately does not name a sha.**
  ⛔ Two runs have already been **cancelled by the next push** (`05ea7317`, then `df491d9d`), and a
  cancelled `gates` is evidence of neither green nor red. A sha written into a doc is falsified by the
  very next commit to that doc — which is how the first version of this line went stale one push after
  it was written. At the time of writing, `scope` had passed and `gates` and both `client` legs had
  not yet reported on any head.
- **NO BROWSER HAS RENDERED THIS COMPONENT — including mine.** No browser was available this session.
  The SUBSTITUTE is `apps/web/test/machinecredentials.test.tsx`: **7 jsdom tests, all passing**,
  covering `none`, `load-failed` (explicitly as *not* `none`), `all-owned`, mixed-fleet, the
  owned/unowned picker split, the empty picker default, and `last_used_at: null` rendering as
  *last seen* rather than a liveness verdict. That is every state in this script.
  ⚠ **jsdom proves it does not throw and that the text is right; it does not prove it LOOKS right.**
  Layout, overflow of a long owner email, and the banner's placement above the rows are exactly what
  your eyes are for here — the trigger for the real proof is this review.
- The picker was exercised against seeded data on localhost only. No wire walk.
