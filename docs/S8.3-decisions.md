# S8.3 — site management UI (commit-one, decision-first)

**Story:** the web consuming layer for EPIC 8's site model. S8.1 (site/gateway model + advertisement/approval) and S8.2 (route propagation + site-link health kinds) shipped **API-only**; nothing in `apps/web` fetches a site today. This story is the UI that makes the proven backend operable — plus a small backend rider (the S8.2b keepalive) and the folded Access-page polish.

**Scope discipline:** UI + the keepalive rider + the polish scope. **No new data-plane behavior. DNS forwarding is S8.4.** The only backend touches permitted are (a) the S8.2b keepalive on site-link peers, (b) wiring the already-authored-but-unwired `DeleteSite`, and (c) closing the OpenAPI Node health-kind enum gap (below) — each is enabling the UI to render wire-truth, not new enforcement.

**Re-entry anchor:** main @ `8f7bd65` (pointer) / `2df19df` (S8.2 merge). Branch `story/S8.3-site-ui`. Prior art: S7.4a (consuming a proven health backend from the web), S7.3 (device-approval queue = the advertisement-queue pattern), S7.5.4 (per-user + expiry grant fields).

---

## Backend truth this UI consumes (surveyed, not assumed)

1. **Site API surface (S8.1):** `listSites`, `registerSite`, `listSiteSubnets`, `addSiteSubnet` (→ pending), `bindSiteNode` / `unbindSiteNode` (D6 replace), `listPendingSiteSubnets` (the queue), `approveSiteSubnet`. All gate `authorize(PermSiteManage)` FIRST (owner+admin). No `deleteSite` on the wire yet.
2. **`Site` is thin** — `{id, name, link_transport, link_mtu?, created_at}`. It does NOT embed the bound gateway, health, or subnets. The topology/site cards must ASSEMBLE from three reads: `listSites` + per-site `listSiteSubnets` + the nodes list (the binding lives on `node.site_id`; a site's gateway = the node whose `site_id` is this site). This shapes D2.
3. **Health is per-node, via the S7.4b surface** (`node.policy_degraded` bool primary + `policy_degraded_kind` advisory). There is no per-site health rollup endpoint; a site's link health = its bound gateway's kind.
4. **BACKEND-TRUTH GAP (must close, item (c) above):** the server EMITS `site_hub_down` / `site_link_down` (S8.2 box-walk Leg 7 observed them on the wire), but the **OpenAPI `Node.policy_degraded_kind` enum, the generated TS union, and `healthview.policyHealthBadge` all lack them** — they currently fall through to the `default: "degraded"` badge. The honesty floor (D2) requires the real kind, so this story extends the enum → `make generate` → adds the two badge cases. `unsupported_policy_version` IS already in the enum (the v5-warning source, CW, is ready).
5. **`subnet_not_disjoint` already carries detail** — `apierr.Conflict("subnet_not_disjoint", "this subnet overlaps the {class} range {With}; approval refused")` + a `site.subnet_approval_refused` audit with `overlap_class` + `overlaps`. The refusal UX (D3) can render the typed message verbatim; **no API enrichment owed.**
6. **`DeleteSite` is authored + `DELIBERATELY UNWIRED`** (`db/queries/sites.sql`: `-- DELIBERATELY UNWIRED until S8.3 … needs the confirm-naming-target`). D4 wires it.
7. **Access already supports user-subject + expiry** in `RuleFormModal` (src_kind=user, `expiresAt`, `ExtendGrantModal`) and `policyview` has `resolveUser` + `grantExpiry`. The polish (CP) is LIST-ROW display + a summary line, not new create-capability — confirms "polish, not redesign."

---

## Carried decide-items (pre-ledgered at S8.2; instantiated here)

### CW — the add-second-site v5 upgrade warning (the named-limitation's UI layer)
**Context:** S8.2's content-derived version means the moment an org becomes multi-site with a routed grant, the compiled artifact bumps to v5; any gateway below v5 REFUSES it and goes deny-all (`unsupported_policy_version`). The limitation is named in the paper; this is its operator-facing surface.

**Which action gates?** Two candidates: (a) registering the second site, (b) the second site's first approved subnet (the action that actually makes a cross-site grant compilable). **Proposed: gate at approval of the first subnet on the org's 2nd site** — registration alone advertises nothing routable (a subnetless site compiles no grant, S8.1), so warning at registration would fire on a no-op and train dismissal. Approval is the first moment a v5 artifact can actually be produced.

**RULED: blocking confirm-with-count at first-approval-on-2nd-site, and the PRE-WARN signal is MANDATED (not optional).** Before the approval commits, if any of the org's gateways report a max-supported policy version below the ceiling (5), surface a confirm — *"Approving this subnet enables cross-site routing. N gateway(s) cannot apply the new policy version and will deny all traffic until upgraded: [named gateway list]. Approve anyway?"* Confirm over passive-warning because the consequence is a hard deny-all.

**Why pre-warn, not retroactive (ruling rationale):** a retroactive warning — "we deny-alled your gateways, here's why" — is the reassuring-green class wearing an apology. The whole point of the named-limitation's UI layer is *before*, and *before* requires a wire signal for "which gateways are below v5" that exists PRIOR to the bump (the `unsupported_policy_version` kind only appears AFTER the artifact goes v5 — too late to pre-warn).

**The signal — built in Slice 1 (backend-truth's natural home):** the agent REPORTS its max-supported policy version (the report path already carries version-adjacent data — EXTEND it, do not invent a new channel); the CP STORES it on the node row; the confirm dialog LISTS which gateways are below 5 by name. This signal is permanently useful — every future version bump inherits an upgrade-readiness surface (S11.4's docs will point at it).
**Red:** the confirm shows the named sub-v5 gateway list; a fleet that is all-v5 shows a CLEAN confirm (no gateways listed).

### CK — S8.2b keepalive lands BEFORE this story's box-walk
**Proposed disposition: a PRE-SLICE (Slice 0), backend-only, merged/walked first.** Add `PersistentKeepalive` to site-link `Peer`s (both `nodes.Peer` + `reconcile.Peer`, honoring `peersEqual` — keepalive is a desired-field so it must be compared or it churns). Rationale: the topology view (D2) renders link health from real handshake freshness; without keepalive an idle site link false-stales at 180s and the view would show `site_link_down` on a healthy-but-idle link DURING its own walk. The keepalive must be live on the wire before the UI walk so the topology view isn't proving against a false-degraded substrate. Sequenced first, its own reconcile-level red (keepalive present in desired peer + `peersEqual` treats it as significant).
**RULED: CONFIRMED as specified — peer field + `peersEqual` significance + 25s (wireguard-conventional).** ADDITION: when Slice 0 lands, the S8.2b registration entry gets its DISCHARGE LINE (the keepalive item closes, trigger "before S8.3's box-walk" met).

### CP — Access-page polish (the folded scope; named, not a redesign)
Three concrete items, all display-layer (helpers already exist in `policyview`):
- **Status summary line** atop the rules list: *"N rules → default-deny active"* when enforcing with rules; *"0 rules — all traffic denied"* when enforcing with none; nothing when policy is off. Wire-truth from `rules.length` + the mode. **RULED: the zero-state ("0 rules — all traffic denied") is the LEGIBILITY-LAW item — render it visually LOUD, not a caption.** (A default-deny with no rules is a live security posture the operator must not miss.)
- **User-subject + expiry visible in the rule ROWS** — surface `resolveUser(src_user_id)` and `grantExpiry(expires_at)` in the list rendering (the S7.5.4 capabilities the create-modal already writes but the list never shows). A per-user grant shows the member; a temporary grant shows its expiry (and the existing "expired/expiring" states).
- **Resource-panel layout** — the rule-builder's destination panel legibility (group/resource/site rows visually distinguished). Layout only.
**RULED: CONFIRMED — in-scope, one slice, no new API.**

### Slice 4 conditions (RULED) — polishing a PROVEN/WALKED surface; the risk inverts to REGRESSION, not missing features. The existing `policyview` tests are the regression net (must pass UNTOUCHED).
1. **Summary line states ENUMERATED, not improvised** (a pure fn off a `Loaded<T>`, never an empty default): enforcing + N → *"N rules — default-deny active"*; enforcing + 0 → *"0 rules — ALL traffic denied"* (LOUD, the legibility-law state); off → *"policy not enforced — open mesh"*; loading/failed → their own states. A FAILED rules load must NEVER render the 0-rules message (that is the reassuring-empty class on the loudest line) — the summary derives from the load result, not from `rules.length` of an empty default.
2. **User-subject + expiry rows render the S7.5.4 TRUTH:** a user-subject rule shows the user (not a blank group); an expiring grant shows its window; an expired-pending-sweep grant (if the API read returns it at all — CHECK the post-delete-on-sweep read) shows as expired, never live. Render only what the endpoint actually returns.
3. **Resource-panel = LAYOUT ONLY** — no behavior change to create/edit; the `policyview` tests pass untouched.

### CH — HA forward-compat check (founder-raised; review-attention item)
**Not a build decision — a review-probe the paper pre-registers.** The topology view + site cards must NOT structurally assume one-gateway-per-site: render the gateway as a **list-of-one, not a scalar** (`site.gateways[]` shape in the view-model even though v1 binds exactly one), so S-future HA (multi-gateway sites) is an additive render, not a refactor. **The story-end review WILL probe:** is there any place the view-model or JSX hard-codes `site.gateway` as a single object / assumes `nodes.filter(...)[0]` without a list path? Stated here so the review has the probe in hand.
**RULED: CONFIRMED as a pre-registered review probe** (correct to make it the review's job, not a build rule).

---

## Story decide-items

### D1 — Page architecture (where sites live in the IA)
Real nav today: Dashboard / Devices / Access / Users / Settings / Audit (`AppShell.tsx` `NAV`). Options: (a) a new top-level **Sites** page; (b) fold into Devices/Nodes; (c) a **Network** section grouping sites (+ future topology).
**Proposed: (a) a new top-level "Sites" nav entry**, edition-gated visibility (enterprise; the site ops are `site:manage`). Rationale: sites are a first-class ZT primitive with their own lifecycle (register/advertise/approve/bind/delete) — folding into Devices (which is per-user device credentials, a different noun) would muddy both. "Network" (c) is premature with one sub-page. The Access rule-builder's existing `dst_kind='site'` / `src_kind='site'` entries point AT these sites (already resolved to site labels in `policyview`); a Sites page gives those labels a home to click through to.
**RULED: CONFIRMED — top-level "Sites" nav, edition-gated.** Sites are an org-structural concept PEER to Devices, not a subtype of nodes; and the epic-close founder walk needs a coherent place to start. **ADDITION — the bidirectional link:** the Access click-throughs (dst/src site labels → the site page) bind the two surfaces; INCLUDE THE REVERSE LINK — the site page shows *"rules referencing this site"*. That reverse link needs a rules-referencing-site COUNT, which is **the same API addition D4's cascade preview needs** — build ONE endpoint, serve both (D1 reverse link + D4 cascade count).

### D2 — Topology view scope (what v1 renders + the honesty floor)
**Proposed render (v1):** per-site cards showing — site name, its bound gateway (as a list-of-one per CH), the gateway's **real** health badge (the S7.4b kind incl. the newly-wired site-link kinds), approved subnets, and hub designation (the endpoint-bearing gateway; derivable client-side from which gateway carries an endpoint, matching `siteLinkGraphFrom`'s hub rule — or surfaced from the backend if cleaner). Links between sites rendered from real state: a link is "up" only on fresh handshake truth, "down" on `site_link_down`/`site_hub_down`.
**Honesty floor (binding): render ONLY wire-truth.** No decorative animation implying live telemetry we don't have (no fake packet-flow pulses, no synthetic "activity"). Handshake ages and health kinds are real or the element shows "unknown" (the `desync_unknown` discipline). Stitch/design concepts are LAYOUT reference only; the never-render-unproduced-data rule binds over any concept art.
**RULED: render floor CONFIRMED** — static-truth cards + explicit link-health labels, no decorative telemetry animation (the honesty floor is law; a full node-graph canvas is S-future if warranted).

**RULED: hub source OVERRULED → BACKEND-DERIVED (not client-side).** Client-side hub derivation ("endpoint-bearing gateway") would re-implement `siteLinkGraphFrom`'s election logic in TS — a SECOND COPY of a load-bearing decision, the S8.2 #1 defect class (two paths disagreeing about one truth) relocated to the display layer. The moment hub election gains a tiebreak or a designation override (multi-hub is reserved → this logic WILL change), the UI's copy silently forks. **The CP computes the hub; the API says which node it is** (a field on the node/site payload or the health output — cheapest honest seam, implementer's pick); the UI renders what it is told. One line of API, zero divergence risk — the D2 render-floor law applied to DERIVED truth, not just telemetry.

### D3 — Advertisement / approval UX
- **Pending queue:** reuse the S7.3 device-approval-queue pattern — `listPendingSiteSubnets` → a review list with per-row Approve. Surface on the Sites page (or a sub-tab).
- **Refusal display:** on `409 subnet_not_disjoint`, render the API's typed message verbatim (it names the overlap class + colliding range — item 5). **RULED: render the message; NO client-side disjointness re-computation.** This is **the comparison-set law's UI corollary — one validator, never a second copy of the check in JS** (the server is the sole disjointness authority; a JS re-check is the S8.2 #1 two-paths-disagreeing class in the display layer).
- **Unbind / replace-gateway (D6 capability):** `bindSiteNode` / `unbindSiteNode` exist API-only. **RULED: surface bind (pick an unbound gateway node) + unbind (with a confirm — unbind is a full-sweep of site-link peers/routes, proven in the S8.2 walk Leg 6).** Replace = unbind-then-bind; v1 need not be atomic (stated).
**RULED: CONFIRMED** — queue placement (Sites page/sub-tab, admin-only per D5), verbatim-refusal, bind/unbind surface.

### D4 — Site lifecycle UI (DeleteSite — wire it + the destructive grain)
`DeleteSite` is authored + deliberately unwired pending this story. **Proposed disposition:**
- **Wire the endpoint** (`deleteSite` op, `authorize(PermSiteManage)` FIRST, edition-gated same as the others).
- **Destructive grain: confirm-with-name-typed** (the S8.1 registration/S6.5 destructive precedent) — the operator types the site name to enable Delete.
- **Cascade preview:** before delete, show what cascades — the site's subnets AND any `dst_kind='site'`/`src_kind='site'` policy rules that reference it. **RULED: the preview states what the cascade DOES from REAL counts — "N rules deleted, M subnets released, gateway unbound" — never "this may affect."** The 0033/0034 cascades handle the DB side; the UI must not let a delete silently orphan a grant the operator forgot.
- **The rules-count API:** build the "rules referencing site X" count endpoint — **SHARED with D1's reverse link** (one endpoint serves both the site page's "rules referencing this site" and this cascade preview).
**RULED: CONFIRMED** — wire `deleteSite`, confirm-with-name-typed + real-count cascade preview, build the shared rules-count API.
- **PREVIEW-vs-ACTUAL race (Slice-1 forward note → decided in Slice 3):** `getSiteReferences` gives the confirm dialog a PRE-DELETE count; the single-tx delete + its audit record the ACTUAL cascade count. A rule created between preview and delete makes the dialog's "N rules will be deleted" a PREVIEW, not a promise. The delete is correct (single-tx); the gap is purely the copy's honesty. **Slice-3 decision:** either the dialog copy owns it (phrase as "approximately N / N at last check", not a guarantee) OR a re-check-in-tx owns it (re-count inside the delete tx and surface if it diverged). **Proposed: dialog copy owns it** — a name-typed destructive confirm is already deliberate, and a re-check-in-tx adds a failure mode (delete aborts because the preview drifted) worse than the imprecision. The story-end review PROBES this one race. Noted here; ruled in Slice 3.
**RULED (Slice 3): RATIFIED — dialog copy owns it, NO re-check-in-tx.** The delete is single-tx and cascades whatever exists at execution (correct); the preview is advisory; the audit records the ACTUAL counts (truth always ledgered). Copy is PRESENT-TENSE, not a future promise: *"currently N rules, M subnets"*, never *"will delete N"*. Story-end review probes the one race.

### Slice 3 conditions (RULED)
1. **Refusal renders VERBATIM (D3):** `subnet_not_disjoint` message straight from the API (it carries `overlap_class` + colliding range). ONE view test per overlap class, so a future class addition can't render blank.
2. **CW confirm is the slice centerpiece — red family covers THREE states:** (a) all-fleet-v5 → CLEAN confirm, no gateway list; (b) mixed → NAMES the sub-v5 gateways from the caps field (absence counts as below); (c) ACTION ORDERING → the confirm gates first-approval-on-the-2nd-site specifically; a FIRST site's approval never shows it. The ceiling is server-sourced (`meta.protocol_version`), never hardcoded in TS (the bump-fork the D2 overrule warned against).
3. **Delete-site name-typed:** the typed value must match the SITE's name EXACTLY (the S4.5 one-time-ceremony grain); the Delete button stays dead until match.
4. **Every mutation lands via the AUDITED service path** — no mutation routed around the audited endpoints (401-walk + RBAC auto-arm; the audit trail is the discipline-guarded part). The walk traces queue-approve → audit row → UI.

### D5 — Edition / RBAC surfaces (what a non-holder sees)
`PermSiteManage` gates every mutation (owner+admin). **The B6 walk-class lesson: no affordance that leads to a guaranteed 403.**
**Proposed disposition:**
- **Non-enterprise org:** the Sites nav entry shows the standard `edition_required` upsell surface (the Access-page precedent at `Access.tsx:134`), not the live UI.
- **Enterprise, member role (no `site:manage`):** **read-only topology** — sites, gateways, health, subnets are VISIBLE (operational visibility is legitimate for a member), but every MUTATION affordance (register, advertise, approve, bind, unbind, delete) is ABSENT (not disabled-with-403, absent) — `canManage` gating exactly like `Access.tsx`'s `canManage` prop.
- **RULED sharpening: the member sees health but NOT the pending queue.** The queue is an ACTION surface; visible-but-inert is B6's cousin (an affordance the member can see but can't act on). Health/topology = legitimate read; the pending queue = admin-only.
- **Non-enterprise:** the edition upsell follows the established **`403 edition_required` mirror pattern** (the `Access.tsx:134` precedent).
**RULED: CONFIRMED with the queue-hidden-for-member sharpening.**

### D6 — Walk protocol for a UI-heavy story (division from the epic-close walk)
Two walks must not blur: **(i) this story's own box-walk** proves the site UI's legs against the S8.2 live backend; **(ii) the EPIC-8-close founder UI walk** (standing, after S8.4) proves the integrated operator journey end-to-end.
**Proposed division:**
- **S8.3 walk (this story):** the UI's own legs on the S8.2 3-agent topology (or a fresh stack) — render-truth legs: topology shows real hub + real link health; approve-a-subnet through the queue; a refusal shows the typed collision; the v5 upgrade confirm fires with a real sub-v5 gateway in the list; delete-site name-typed + cascade count; the member-role read-only surface (no mutation affordances). Each leg asserts the UI renders wire-truth (cross-checked against the API/health response), honoring the honesty floor.
- **EPIC-8-close walk (deferred, after S8.4):** the founder-driven integrated journey (register → advertise → approve → grant → connect → observe), covering DNS (S8.4) and the whole arc. NOT re-run here.
- **CK keepalive** is proven in its own pre-slice reconcile red + is LIVE before the S8.3 walk so link-health legs aren't false-degraded.
**RULED: CONFIRMED. S8.3's walk = render-truth legs — every badge/count/list on screen TRACED to its wire source:** kill a link → the UI badge flips; refuse an overlap → the UI shows the typed reason verbatim; the v5 confirm NAMES the pinned-old gateway (from Leg-2's setup). The founder walk after S8.4 = the integrated journey, untouched.

---

## Slice plan (RULED — approved as cut)
- **Slice 0 (pre-slice, backend-only):** CK keepalive on site-link peers + `peersEqual` significance + 25s + reconcile red. Merged/live before the UI walk. On landing → discharge the S8.2b keepalive registration line. **← BUILDING NOW.**
- **Slice 1 — backend-truth (the drift-guard's territory; `generate-check` earns its keep) — the load-bearing slice (5 seams, 3 serve later slices):**
  - **Enum extension:** OpenAPI Node health-kind enum gains `site_hub_down`/`site_link_down` → `make generate` (spec → TS → RBAC) → `healthview` badge cases. **The `default:"degraded"` fallback STAYS after the new cases** — it is the forward-compat guard for the next kind we add; exhaustiveness-deletion must not remove it. Gate report shows the regen was deterministic across all three layers.
  - **CW pre-warn signal (MANDATED) — CLASSIFICATION (carry-forward):** the agent reports its max-supported policy version. This is **OBSERVABILITY — a reported fact, NOT artifact content**: it rides the report path, sits OUTSIDE the compile hash, and needs NO ProtocolVersion bump (a version-*about*-versions field is exactly where someone later gets clever — stated so the D2-checklist habit holds). CP stores it on the node row. **Staleness semantics (S7.5.3 absence-is-not-compliance, applied to version readiness): a node that never reported a max-version (a pre-upgrade agent) reads as BELOW-5, never unknown-treated-as-fine** — absence IS the population the warning exists for. Permanently useful (upgrade-readiness surface; S11.4 docs point here).
  - **Hub-designation field (D2 overrule):** emitted from **the same code that ELECTS the hub** (`siteLinkGraphFrom` / its extraction) — a PROJECTION of the one election, never a parallel query that re-elects (the overrule's whole point). Exposed on the node/site/health payload; the UI renders what it is told.
  - **Rules-referencing-site count API (D1 reverse link ⋂ D4 cascade — ONE endpoint)** — org-scoped + `PermSiteManage`, honoring the query-lint cross-org guards.
  - **Wire `deleteSite` (D4 endpoint half)** — and UPDATE the S8.1 `DELIBERATELY UNWIRED` comment to point at the endpoint (the comment's discharge). Both-editions gate.
- **Slice 2:** the Sites page + top-level nav (D1) + read-only topology/site cards rendering wire-truth (D2 backend hub, CH list-of-one) + edition/RBAC gating (D5, member = no queue).
- **Slice 3:** the mutation surfaces — advertise/approve queue (admin-only) + verbatim refusal (D3), bind/unbind (D3), delete-site name-typed + real-count cascade (D4 UI half), the **CW v5-upgrade blocking confirm naming sub-v5 gateways.**
- **Slice 4:** the Access-page polish (CP) — loud zero-state summary line + user-subject/expiry rows + resource-panel layout.
- Then story-end multi-finder review (CH list-of-one probe in hand) → the S8.3 render-truth box-walk (D6).

## Ledger / carried forward
- **S8.2b keepalive** DISCHARGED — Slice 0 landed (`a1ea987`); named trigger "before S8.3's box-walk" met, ledger closes clean.
- **S8.2b MSS double-encap contrast** — untouched (trigger = client-over-site-link setup; not this story).
- **EPIC-8-close founder UI walk** — remains standing, after S8.4 (D6-ii).
- **HA multi-gateway sites** — not built; CH ensures the view-model doesn't foreclose it.

---

## Story-end review (multi-finder, high-effort) — 7 findings, all CONFIRMED, dispositioned + folded
Whole-branch fan-out (4 finders + 8 independent verifiers, 0 refuted). The five pre-registered probes came back CLEAN except where they surfaced #1 (keepalive) and #2 (gate composition): CH list-of-one ✓, preview-race copy ✓ (present-tense, no re-check-in-tx), authorize-before-edition ✓ (deleteSite/getSiteReferences gate first), enum-path ✓ (kinds survive all layers, `default:"degraded"` intact), swallowed-audit ✓, electSiteHub one-election ✓, crossesMultiSiteThreshold ✓, disjointRefusal ✓, loadOne no-false-empty ✓, cross-org ✓.

- **#1 (correctness, FOLDED) — `reconcile.go` NAT'd-spoke hub-peer perpetual churn.** A hub's view of a NAT'd spoke has desired `Endpoint=""` by construction; `peersEqual` compared the endpoint for ALL SiteLink peers, so once the spoke dials (now GUARANTEED by the CK keepalive) the kernel-learned roamed endpoint churned `wg syncconf` forever. **Fix:** three-clause guard `SiteLink && desired-endpoint-non-empty && differs` — the correct asymmetry (spoke→hub compares a present endpoint = B4 re-dial; hub→spoke is endpoint-blind like a device peer). **Arc-lesson:** a rider slice's interaction with the surface it rides on is REVIEW territory — Slice-0's keepalive turned a latent S8.2 edge certain; it surfaced here, not on a customer's hub. Red: `TestSiteLinkNatSpokePeerConverges` (both B4 directions). The S8.3 walk's health legs confirm it on the wire for free.
- **#2 (correctness/decide-item, RULED (a) + FOLDED) — member `canView` was an unkeepable promise.** D5 ruled member-read-only topology, but the site READ endpoints gated `site:manage` → a member 403'd → topology unreachable. **RULED (a): honor D5 — loosen the READS by REUSING the existing member-read gate (`org:view`, the same as ListNodes/ListDevices/Overview), NOT a new `site:view`.** (b) was refused — it would silently demote a ruling because the backend disagreed (wrong direction of authority). Scope guard held: `ListSites`/`ListSiteSubnets` → `org:view`; `getSiteReferences` (a deletion-preview) + the pending queue + EVERY mutation stay `site:manage`. Deliberate-red: `TestSiteReadVsManageSplit` (member reads topology ✓, member mutation still 403 ✓). No new perm needed — `org:view` was the legitimate precedent.
- **#3 (cleanup, FOLDED) — double topology load/hub election per `ListNodes`.** `PolicyHealthForNodes` + `NodeDisplayExtrasForNodes` each loaded + elected independently. **Fix:** an opaque `SiteTopoBatch` loaded ONCE in the handler, passed to both via a variadic optional param — existing callers/tests pass no batch → each loads its own → **health output byte-identical** (the net held). `siteTopoHasHub` semantics preserved (`hubExists = batch.hasHub`).
- **#4 (cleanup, FOLDED)** — `DeleteSite` called `GetSite` then `GetReferences` (which re-`GetSite`s). Dropped the redundant top-level check.
- **#5 (cleanup, FOLDED)** — `auditSite`/`audit` were near-duplicates. Parameterized a shared `auditTarget(…, targetType, targetID, …)`; both wrappers KEEP returning their error (swallowed-audit law survives the refactor).
- **#6 (cleanup, FOLDED)** — `Sites.tsx` per-site subnet fetches were serial `for-await` → `Promise.all`, any failure still a legible retry.
- **#7 (cleanup, FOLDED)** — `LoadRetry` copy-pasted in Access+Sites → extracted to `components/LoadRetry.tsx`, imported in both.

**Re-earn:** #1 + #2(a) + #3 = a feature-sized fold → a TARGETED RE-REVIEW over the fold diff (probes: #1 guard against both B4 directions, #2 RBAC matrix rows, #3 health-output equivalence). **DONE — re-review CLEAN across all three probes + general sweep, zero new defects.** Then the founder-grade walk (Pawan drove).

## Box-walk (live, founder-present) — see `walk-artifacts/S8.3/walk-record.md`
**7/8 legs PROVEN LIVE**, Leg 6 a named substitute. The two review folds proven on the wire: **#2 member-read-only via `org:view`** (member renders the topology, no queue, no mutations — Leg 2) and the **Slice-1 enum path** (`site_link_down` badge live on demo-gw — Leg 3). Refusal verbatim (Leg 5), delete name-typed + present-tense preview + real-count audit (Leg 7), Access summary line + fieldset panels (Leg 8). Render-floor held.
- **Leg 6 CW confirm → NAMED SUBSTITUTE, trigger = the EPIC-8-close founder walk after S8.5** (its fresh-org journey register → first-site → second-site fires the crossing confirm live). Proven now: live consequence (gw-d `unsupported_policy_version` on screen) + the gate/naming logic unit-proven (4 ordering reds, absence-below, ceiling from `meta.protocol_version`).

## Ledger — walk-found + metrics (founder-directed at walk close)
- **Walk-found (pre-existing, founder-found): stale Add-rule button after group-add.** Adding a group doesn't re-enable the Rules section's Add-rule button until a refresh (separate components, separate `groups` state loaded on mount — S7.4a structure, NOT an S8.3 change). **Rides S8.5 or the epic-close fold, whichever lands first** (founder-walk finds don't age). Fix = lift `groups` to the parent / reload RulesSection on group-add.
- **Metrics — founder-directed, split three ways per layer:**
  - **L1 (near-term): site-link byte counters (tx/rx) + handshake age on the S8.3 topology cards.** The agent already parses `wg show dump` (S3.6); this is a report-path extension + a UI join. **SIZE IT in S8.5's commit-one** (same report-plumbing neighborhood). **Render-floor binds:** cumulative-since-handshake totals labeled as EXACTLY that — no rate graphs, no sampling implied (that is S11.1's job).
  - **L2 (no new scope): add to S7.5.1b's registration** — site-to-site flows are a first-class consumer of the flow-log viewer (the per-rule counters already capture site grants; the viewer story surfaces them).
  - **L3: add to S11.1's ledger** — site-link throughput TIME-SERIES joins the Prometheus metric set when S11.1 opens. NOT pulled forward.

---

*Dispositions RULED and folded (this section). Deltas from proposal: CW pre-warn signal MANDATED into Slice 1 (agent max-version report → node-row store → named confirm); D2 hub source OVERRULED to backend-derived (no TS re-election); D1 reverse-link + D4 cascade share ONE rules-count endpoint; D5 member hides the pending queue; CP zero-state rendered loud. Slices approved as cut. **Slice 0 building now.***
