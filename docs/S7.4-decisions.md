# S7.4 — Zero Trust Policy UI + differentiated health + enterprise-e2e — DECISIONS (commit-one, paper only)

Decision-first. NO code until this is signed off. Grounded in a 3-investigator codebase map
(policy-UI server+web · health surface · e2e harness), cited inline.

---

## Grounding (what already exists — the UI is consumption, not new backend)

- **Policy CRUD is complete + box-proven** (S7.1/S7.2/S7.3). All endpoints in
  `apps/api/internal/http/policy_handlers.go`: groups (list/create/PATCH/delete + members add/remove),
  resources (list/create/PATCH/delete), rules (list/create/**delete only — NO update**), zero-trust-mode
  (get/set). RBAC `policy:view`/`policy:manage` (admin+owner only); uniform `403 edition_required` gate
  (`s.policy == nil` in open). Rule shape = `src_group → {dst_kind: resource | group}`; `PolicyRule`
  returns only IDs → UI joins names client-side.
- **Mode toggle has NO server confirm / preview / lockout.** `SetMode` obeys immediately
  (`service.go:346-384`). Affected devices come back ONLY as a **post-hoc PUT-response side-effect**
  (`affected_full_tunnel_devices`), **best-effort** (swallowed on error) and **egress/full-tunnel-only** —
  NOT a dry-run, NOT device-to-device. No `preview`/`dry-run` endpoint. No self-lockout guard.
- **Web policy UI is greenfield** — nothing under `apps/web/src` (only the generated `rbac-policy.json`).
  **The S7.3 device-approval admin UI is ALSO missing** — pending-queue/approve/reject/`device_approval`
  toggle are curl-only today (discovery). Mount under `RequireOrg → AppShell`, nav array `AppShell.tsx`.
  Fetch = generated `@tunnex/shared` client (auto-CSRF), `useState/useEffect/mutate`, no React-Query.
  Edition-gate = `meta.edition === "enterprise"` (Settings/SSO precedent). RBAC = `can(myRole, perm)`.
  **UI kit has NO Modal/Select/Table/Toggle** — only `OneTimeSecretModal` precedent.
- **Health: `policy_degraded` is a single derived bool** (`nodes/service.go:485-511`), recomputed per
  `ListNodes`, NOT stored. Rich signals live in `nodes.capabilities` JSONB (`policy_error`,
  `policy_failing_since`, `policy_hash`, `policy_version`) — **never crossing the wire today** (only the
  collapsed bool is in the `Node` response). NOT surfaced in the web UI at all (`Gateways.tsx`).

---

## STEER 1 — differentiated health surface, COLLAPSED AT DESIGN (the decision, not a mid-fold)

The ledgered "which-kind-of-degraded" surface is exactly the multi-signal→multi-field shape that produced
S7.2's staleness gap-states. Design it collapsed from the start:

**D-H1 — `policy_degraded` (bool) STAYS the single source of truth.** It answers "is this gateway's
policy healthy?" and remains the only field any LOGIC (alarms, gating) keys on. The differentiation adds
NO new decision logic and does NOT feed any branch.

**D-H2 — KIND is an advisory PROJECTION, not logic.** The three kinds are derivable from the EXISTING
JSONB caps at compute time (Investigator B, verified in code):
| Kind | Raw signature (existing JSONB) | New state? |
|---|---|---|
| `apply_failing` | `policy_error` set + `policy_failing_since` set | none — projection |
| `stuck_enforcing` | `policy_error` set + `policy_failing_since` EMPTY (+ server knows `pushed=="" && applied!=""`) | none — projection |
| `silent_desync` (existence) | server term-3 only: `pushed != applied`, error/since empty | none — server already computes term-3 in isolation |

Add ONE **read-only advisory field** `policy_degraded_kind` (enum `none|apply_failing|stuck_enforcing|silent_desync`)
to the `Node` response — computed alongside `policy_degraded` from the same caps, **pure projection, zero
new stored state, no migration.** The bool is authoritative; the enum is display detail. Web renders a
differentiated badge on the `Gateways.tsx` node row.

**DURABILITY CONDITION (Q4) — the property that keeps the collapse from eroding in a later story.** The new
field is a contract change (arms codegen-drift). A test MUST assert the enum is **COMPUTED from the SAME
JSONB caps the bool derives from, and is NEVER independently settable** — the kind can never silently acquire
its own state (a stored field, a separate write path). If a future story needs per-kind state (e.g. the
deferred desync-onset, D-H3), that is an explicit new decision, not a drift. This test is what makes "kind =
projection, not logic" durable rather than momentary.

**D-H3 — the ONE genuinely-new-state item is ISOLATED and DEFERRED.** Distinguishing *transient* vs
*stuck* `silent_desync` needs a **persisted `desync-onset` timestamp** (the server compare is instantaneous
today; `service.go:466-472` explicitly notes onset would be new state, against the reduce goal). That is
the coupled-temporal-state that spawned the arc. **It is NOT built in S7.4.** `silent_desync` ships as a
single advisory kind ("pushed≠applied") without a transient/stuck split. If the split is ever wanted, it is
its own decision-first item (a new `policy_desync_since` field, stamped on term-3 onset, cleared on
reconverge) — named here, deferred. Ledger trigger: a real "stuck desync read as a transient blip" incident,
or S7.x hardening. This keeps the arc's terminal heuristic applied PRE-EMPTIVELY.

---

## STEER 2 — the split (three separable PRs; lean split, per S4.5/S4.5b precedent)

Investigator C verdict: separable at the file/PR level; the only coupling is that any *automated e2e* of the
policy UI hits the same open-edition 403 that blocks S4.5 — so the enterprise-e2e enabler is a shared
prerequisite for e2e coverage, **build it once**, but it does NOT force one PR.

**D-S1 — three PRs:**
- **S7.4a — "the Zero Trust admin UI" (web-only consumption).** Rule builder (`src_group → resource|group`,
  join names client-side, delete+recreate since no update), groups/resources/members management, the
  zero-trust-mode toggle (with the Q2 count-based self-lockout advisory), AND — **FOLDED IN (Q1)** — the
  missing **S7.3 device-approval admin surface** (pending queue + approve/reject + `device_approval` toggle).
  Deciding factor: S7.3's approval feature is shipped-but-UNUSABLE (curl-only) — same admin persona, same
  primitives, same enterprise-gated-consumption shape. Edition-gated (`meta.edition`), RBAC-gated
  (`policy:manage` / `device:approve`), verified-email-gated to mirror the server. Adds the missing UI-kit
  primitives (a real `Modal`, a `Select`) — minimal, patterned on `OneTimeSecretModal`. **Size the estimate
  for this growth** — a is now the heaviest of the three; its own decide-before-code covers the UX
  primitives + page/nav structure.
- **S7.4b — differentiated health surface.** The `policy_degraded_kind` advisory projection (API contract
  change: openapi + handler + gen + TS) + the `Gateways.tsx` badge. Small but ISOLATED for focused review
  because it is the arc-prone surface + a contract change. Desync-onset DEFERRED (D-H3).
- **S7.4c — enterprise-e2e enabler. UN-DEFERRABLE (Q3 hard condition).** `TUNNEX_BUILD_TAGS=enterprise` into
  the CI e2e job + an enterprise seed (SSO config row + policy objects) + a **mem-backend enrolled node**
  (CI-friendly, no NET_ADMIN; `TUNNEX_WG_BACKEND=mem` + a join-token mint step) + a stranded device. It
  **SATISFIES (not substitutes)** the two twice-deferred assertions: S4.5 secret-payload (GET sso 200, no
  `client_secret`, fingerprint present) + S4.5b orphan-render (real 409 orphan list vs the current mocked
  one). **This work has been deferred TWICE with an "EPIC 7 trigger"; S7.4 is the last S7.x → it does NOT
  defer a third time. EPIC-7-close is its terminal trigger.** If it genuinely cannot land, that is an
  explicit conversation AT THE TIME — never a silent re-ledger. Its own CI wrinkles (2nd edition image build,
  added flakiness on an already-opportunistic job) — must not gate the UI review, but must not become an
  excuse to drop it either.

**Why split (argument for the record):** (c) is infra with CI-flakiness risk that must not block (a)'s
user-facing review; (b) is the contract-change + arc-prone surface deserving isolated scrutiny; (a) is the
value. This mirrors the S4.5/S4.5b precedent of separate reviewable PRs.

**D-S2 — sequence + substitutes.** (a) first (value; **manually wire-verifiable NOW on the standing live
two-gateway enterprise box** — the reason G2 was left up), (b) second (small), (c) third (enabler that also
clears S4.5/S4.5b). (a) and (b) ship with component/view-model tests + manual box verification as SUBSTITUTES;
(c) lands the automated e2e that SATISFIES. Ordering is a recommendation — reorderable.

---

## D-A1 — mode-toggle "warn-and-confirm": NO client-side compiler reimplementation

The server gives no pre-flight preview and no lockout guard; the affected list is post-hoc + egress-only.
Two ways to warn before the flip: (i) UI-compute the blast radius from list endpoints, or (ii) generic
warning + show the server's authoritative post-hoc list after.

**Decision: (ii) — CONFIRMED (Q2).** Building a UI-side blast-radius preview means REIMPLEMENTING the
compiler's who-loses-reachability logic in the client — a second, divergent source of truth (the exact bug
class as the S7.2 staleness surface / Steer 1). So: the confirm dialog states the consequence generically,
the flip proceeds, and the server's authoritative `affected_full_tunnel_devices` is shown prominently AFTER
as the real blast radius. A genuine pre-flight preview, if wanted, is a **server-side dry-run endpoint** (new
work) — DEFERRED, NOT a client reimplementation.

**PLUS a COUNT-BASED self-lockout advisory (Q2) — a rule COUNT read, NOT a reachability/compiler check.**
The mode-enable confirm surfaces the allow-rule count from the existing `listPolicyRules` endpoint:
- N > 0 → "Enforcing denies all traffic except what your rules allow — you have **N** allow rule(s). Continue?"
- N == 0 → the STRONGER gate: "You have **NO** allow rules — enabling Enforcing denies **ALL** traffic,
  including your own access. Continue?" (surfaces the S7.1 zero-grants footgun).

This is honest server-sourced COUNT data, not a divergent second source of truth — it makes no claim about
*which* devices are affected (that stays forbidden / server-authoritative-post-hoc). The real dry-run/preview
endpoint stays deferred per D-A1.

---

## Deferred / new-state items (named, not dropped)

1. **Windowed silent-desync (transient vs stuck)** — needs persisted `desync-onset` state (D-H3). Deferred.
2. **Server-side mode-flip dry-run/preview endpoint** (pre-flight blast radius incl. device-to-device +
   self-lockout) — deferred; S7.4a uses generic-warn + authoritative-post-hoc instead (D-A1).
3. **`useCurrentOrg` hook** — every page still does `orgs[0]` independently; S7.4 adds more call sites.
   Not blocking; ledgered pre-existing (do not expand it here, but do not regress it).

---

## SIGNED OFF — 2026-07-14 (all four dispositioned)

1. **Q1 — device-approval admin UI FOLDED into S7.4a** ("the Zero Trust admin UI"): shipped-but-unusable
   (curl-only), same persona/primitives/shape. Estimate sized for the growth. ✅
2. **Q2 — D-A1 CONFIRMED** (no client compiler, no computed blast-radius, dry-run deferred) **+ a
   count-based self-lockout advisory** (rule-COUNT read; zero-rules → the stronger "denies ALL incl. your
   own access" gate). ✅
3. **Q3 — three PRs a→b→c CONFIRMED**; a+b wire-verifiable on the standing two-gateway box, c automates.
   **HARD CONDITION: c is UN-DEFERRABLE** — deferred twice already, S7.4 is the last S7.x, EPIC-7-close is
   its terminal trigger, it SATISFIES (not substitutes) S4.5 + S4.5b; no third defer / silent re-ledger. ✅
4. **Q4 — `policy_degraded_kind` CONFIRMED** as advisory projection (zero new logic/state/migration);
   desync-onset deferred as the one new-state item; **durability test** = enum computed from the same JSONB
   as the bool, never independently settable. ✅

**Next:** commit this as S7.4 (1/n); then per-workload decide-before-code as I reach a/b/c (S7.4a's UX
primitives + page/nav structure; c's harness design). Report S7.4a's commit-one before any UI code.

---

# S7.4a commit-one — the Zero Trust admin UI (decide-before-code)

**SIGNED OFF 2026-07-14** — D-a1…D-a6 + kit-minimum approved. Three conditions folded: D-a5 transient
duplicate must be LEGIBLE (partial-success + both rules shown + retry); D-a6 distinguish "(deleted)" (absent
from a loaded set) from "(unresolved — refresh)" (list failed to load), copy must not lie about WHY; D-a4 as
specified. Toggle-rejection rationale retained (a live switch implies instant/reversible/low-stakes; a
confirm-gated consequential action must not wear switch clothing). Single story-end review; flag early if
D-a5's ordering or the mode confirm grow beyond paper.

Grounded in the existing web patterns: flat `NAV` array + one route under `AppShell` (`AppShell.tsx:11-16`,
`App.tsx:57-63`); pages are single scrolling columns of `Card` sections (`Settings.tsx` precedent, max-w-3xl);
`ui.tsx` kit = Button/Card/Field/Input/StatusDot/ErrorText (NO Modal/Select/Table/Toggle); modal precedent =
`OneTimeSecret.tsx` (overlay + explicit dismiss); fetch = generated `@tunnex/shared` client + `useState/useEffect
+ loadX + mutate(fn,fallback)`; edition-gate `meta.edition === "enterprise"` (Settings/SSO); RBAC `can(myRole,perm)`;
mutating controls also gated on `emailVerified`.

## D-a1 — structure: ONE nav item, ONE page, stacked Card sections (no tab/subroute primitive)
Add one nav item **"Access"** → route `/access` under `AppShell`, rendering one page of stacked `Card`
sections (mirrors `Settings.tsx`'s single-page-multi-Card shape — needs NO new tab/subroute primitive):
1. **Zero Trust mode** — current mode + enable/disable (confirm-gated, D-a4).
2. **Rules** — the rule list + the rule-builder (D-a5, D-a6).
3. **Groups & Resources** — CRUD for the rule building-blocks (source groups + their members; dst resources).
4. **Device approval** — `device_approval` toggle + the pending queue (approve/reject) — the folded S7.3 surface.
Whole page gated `meta.edition === "enterprise"` (muted upsell `Card` in open) + `can(myRole,"policy:view")`;
per-section manage controls gated `policy:manage` (sections 1-3) / `device:approve` (section 4) + `emailVerified`.
*Alternative considered:* separate "Device Approval" nav item — rejected for now (one admin persona, avoids a
second mount; sections keep it cohesive). If the page grows unwieldy, in-page anchor-nav or a split is a later
refinement, not built speculatively.

## D-a2 — UX-primitives kit: build EXACTLY two, story-defined (resist a design system)
The three sections need precisely two new primitives; build only these, pattern-matched to `ui.tsx` + the
`OneTimeSecret` overlay:
- **`Modal`** — a generic overlay + explicit-dismiss shell (title + children + actions), extracted from the
  `OneTimeSecretModal` overlay structure but content-agnostic. ONE modal, reused for: create/edit group ·
  create/edit resource · create/edit rule · the mode-enable confirm · the reject-device confirm.
- **`Select`** — promote the raw `<select>`+`selectCls` string (currently inline in `Users.tsx`) into a themed
  `Select` in `ui.tsx`. Used for the rule-builder pickers (src group / dst-kind / dst resource|group) + the
  add-member picker.
**Explicitly NOT built:** `Table` (lists render as inline Tailwind rows like `Gateways.tsx`/`Users.tsx` — no
sortable-column need) and `Toggle` (the two mode toggles are confirm-gated ACTIONS = `Button` + the `Modal`,
not live switches — a live switch can't carry the count-advisory confirm). Anything speculative waits for a
story that needs it.

## D-a3 — data/fetch: existing pattern, join client-side
On mount load groups + resources + rules + zero-trust-mode + pending-devices + org members via the generated
client; `mutate(fn, fallback)` refetches on success AND error (the `Users.tsx` pattern). No React-Query. Add
the policy DTO re-exports to `api.ts`. No `useCurrentOrg` (still `orgs[0]` per page — pre-existing ledger, do
not regress).

## D-a4 — mode-enable confirm = generic warning + COUNT advisory (D-A1 / Q2), never a computed blast radius
Enable-enforcing opens the `Modal` with the rule COUNT from the already-loaded `listPolicyRules`:
- N > 0 → "Enforcing denies all traffic except what your rules allow — you have **N** allow rule(s). Continue?"
- N == 0 → the stronger gate: "You have **NO** allow rules — enabling Enforcing denies **ALL** traffic, including
  your own access. Continue?"
The copy is a PURE FUNCTION of the count (view-model, unit-tested). After the flip, the server's authoritative
`affected_full_tunnel_devices` (PUT response) is shown as the real post-hoc blast radius. NO client-side
who-loses-reachability computation. Disable-enforcing = a plain confirm (returning to legacy mesh is not a
lockout risk).

## D-a5 — CONSEQUENTIAL: rule editing = CREATE-THEN-DELETE, gap-free (the one enforcement-semantics touch)
There is no `updatePolicyRule` (create/delete only). "Edit a rule" is implemented as **create-new-first, then
delete-old** — NEVER delete-first:
1. POST the new rule (create).
2. ONLY on create success → DELETE the old rule.
- **Failure modes are all safe:** create fails → old rule stays, edit aborts, NO access gap. Create succeeds +
  delete fails → a transient DUPLICATE grant persists (harmless + retryable), NO gap.
- **CONDITION (legibility) — the duplicate must be VISIBLE, not just harmless.** On create-succeeds +
  delete-fails the UI reports PARTIAL SUCCESS ("new rule created; the old rule could not be removed — retry")
  and the list shows BOTH rules, with retry available. A duplicate nobody knows about is how a "deleted" rule
  keeps granting access — harmless-but-invisible is unacceptable for an enforcement artifact (same
  failure-must-be-legible principle that closed S7.3's [0]). Test: create-succeeds + delete-fails → both rules
  listed + partial-failure surfaced + retry available.
- **Why gap-free:** the model is ALLOW-ONLY and grants are a UNION (S7.1 semantics), so a transient duplicate
  rule is a no-op. This is the create-first-swap reasoning from the S7.3 migration but WITHOUT its cap/atomicity
  trap — here NO resource is freed and re-claimed (grants are purely additive), so create-first has no downside.
- Delete-first would open a real access gap on an enforcing org (traffic denied mid-edit) and lose the rule on a
  failed recreate — FORBIDDEN. This ordering is stated because it is the single place S7.4a touches enforcement
  semantics; it gets an explicit ordering test.

## D-a6 — CONSEQUENTIAL: ID→name joins render "(deleted/unknown)", NEVER omit
`PolicyRule` returns only IDs; the UI joins `src_group_id`/`dst_resource_id`/`dst_group_id` against the loaded
groups/resources. The rule row is **never omitted** — a rule the server is ENFORCING must always be visible even
if its referents are broken (**the UI never hides live policy**).
- **CONDITION (deleted ≠ unresolved) — the copy must not lie about WHY.** Distinguish the two causes:
  - referent ABSENT from a **successfully-loaded** set → "(deleted)" — the referent is genuinely gone.
  - the groups/resources list **FAILED TO LOAD** → "(unresolved — refresh)" + the page surfaces the load error.
  Conflating them makes a transient fetch failure render HEALTHY policy as broken — the false-alarm class this
  project has hit three times (staleness, desync, migration). The rule stands either way; the label must be
  honest about the cause. Test BOTH branches (deleted-referent vs failed-load).
(Cascade-FK deletes dependent rules server-side per the S7.1 guard, so a genuine dangling referent should be rare —
but the UI stays honest if one appears, rather than silently hiding an enforced grant.)

### NAMED LIMITATION (S7.4a, a11y — Modal dialog pattern deferred)
The shared `Modal` has **no focus trap / autofocus / Esc** — keyboard + screen-reader users are not moved
into the dialog and Tab walks the background. Pre-existing to the story (the original Modal had no focus
management; fold-2's autofocus was Esc-scaffolding, removed with the Esc drop). Deferred as ONE designed
a11y dialog story (focus trap + first-field focus + panel Esc listener), NOT a patch. **Trigger: the a11y
pass.** No partial pull-forward (round-3 disposition). Dismiss today = backdrop-click + Cancel.

### S7.4a review rounds — ledger (2 folds + a Modal-Esc reduction + a notices reduction)
Story-end review → fold-1 (the loadOne legible-loads cluster) → confirm → fold-2 (parent-gating pure
`accessView`, compose-not-compete notice, misc) → confirm → round-3 (Modal-Esc DROP + notice/err hygiene).
**Modal Esc-to-dismiss was DROPPED** after a 3-finding churn (broken inline handler → too-global document
listener → autofocus focus-steal) on a nice-to-have that is also a data-loss footgun on a form modal;
Cancel + backdrop-click remain. **If a11y later requires Esc, it returns as the full designed dialog pattern
(focus trap + first-field focus + panel listener), never a patch.** Bug law recorded: legibility signals
COMPOSE, never compete (an error may replace CONTENT, never another WARNING). openapi-fetch no-throw footgun
+ the `loadOne` sanctioned pattern ledgered in PLAN.md (Armed Guards), carried into S7.4b.

---

# S7.4b commit-one — differentiated health surface (PAPER ONLY; no branch/code until S7.4a merges)

Refines the Steer-1 decision with the health-surface codebase map (Investigator B). Health signals live in
`nodes.capabilities` JSONB (`policy_error`, `policy_failing_since`, `policy_hash`, `policy_version`); the
server derives `policy_degraded` (bool, 3 OR-terms) per `ListNodes`. NONE of the rich signals cross the wire
today — only the collapsed bool.

## (a) JSONB read → a BOUNDED, ENUMERATED set of states; can't-determine is DISTINCT
The server projects the caps into ONE advisory `policy_degraded_kind` enum. The enumerated states, each
mapped to its raw signature:
| state | raw signature (server, at compute time) |
|---|---|
| `healthy` | not degraded |
| `apply_failing` | `policy_error` set + `policy_failing_since` set |
| `stuck_enforcing` | `policy_error` set + `policy_failing_since` empty (+ `pushed=="" && applied!=""`) |
| `converging` | term-3 desync (`pushed != applied`), desync age < debounce window T (see (c)) |
| `silent_desync` | term-3 desync, desync age ≥ T (stuck) |
| `desync_unknown` | the compiled-hash was UNAVAILABLE (compile fault) → the server CANNOT rule out a silent desync |

**`desync_unknown` is a first-class honest state — NEVER defaulted to `healthy` and NEVER claimed as a
specific kind.** This is the loadOne legibility law applied to the health KIND: can't-determine ≠ healthy
(the can't-determine window today makes an already-desynced gateway read not-degraded — acceptable for the
BOOL because it's on last-good fail-closed policy, but the KIND must SAY "can't determine", not imply health).

## (b) The projection stays ADVISORY — `policy_degraded` (bool) remains authoritative
`policy_degraded` is the ONLY signal any logic (alarms, gating) keys on; it answers "is this gateway
healthy?" The `policy_degraded_kind` enum is display detail — it adds NO decision logic. **Durability test
(carried from S7.4 D-H2/Q4):** the enum is computed from the SAME caps the bool derives from, and is NEVER
independently settable — a codegen-drift-armed contract test asserts it, so the kind can never silently
acquire its own state in a later story. **ADVISORY-READ-ONLY GUARD (amended):** the kind is UI-read-only —
a test (or structural boundary) asserts that NOTHING in enforcement, push targeting, or compilation reads
`policy_degraded_kind` (or `policy_desync_since` beyond the render derivation). Advisory that something
quietly starts consuming stops being advisory; `policy_degraded` (the bool) remains the sole load-bearing
signal — the S7.2 collapse must not silently un-collapse.

## (c) The debounce/window as FIRST-CLASS TEMPORAL STATE (the S7.2 fault class — decided on paper)
`converging` vs `silent_desync` cannot be told apart from the instantaneous compare (`service.go:466-472`
notes the onset is not tracked — that was deliberate in S7.2). Distinguishing them needs a **persisted
desync-onset**: a new server field `policy_desync_since` (on the node row / caps JSONB), **stamped when the
server FIRST observes term-3 for a node in the report/reconcile ingest path, cleared when the hashes
reconverge OR `policy_degraded` clears.** Then `age = now − policy_desync_since`; `age < T → converging`
(transient, expected during a push — advisory-only, NOT alarming), `age ≥ T → silent_desync` (stuck,
actionable). **T ties to the push SLA** (≈2× the <5s reconcile/push target, ~10–15s).

Modeled first-class to AVOID the S7.2 fault class: the onset is a single persisted timestamp with exactly
two write points (stamp on first term-3, clear on reconverge/heal) — NOT a second derived signal that can
gap-state against the bool. `converging` never alarms (so a normal push's brief desync is not a false
alarm — the legibility failure a naive term-3 badge WOULD cause).

**DECISION — OPTION X (build the temporal state now), SIGNED OFF, amended.** Rationale recorded (do not
re-litigate): the S7.2 ledger deferred this exact state to S7.4 "as new state" — S7.4b is the story
designated to build it honestly. Option Y (defer + render every term-3 as `desync_unknown`) would render a
KNOWN fact (a hash mismatch we DID observe) as "unknown" forever — a legibility violation of the mirror
polarity to a false alarm: `desync_unknown` must mean **we could not determine**, NEVER **we determined but
won't say**. X's risk is bounded because we do what S7.2 couldn't: model the temporal state on paper first —
single timestamp, enumerated write points, fault class named in advance.

**X-1 (required) — `silent_desync` requires CURRENT reports; the render is three-valued on a stamped node.**
The timestamp proves desync ONSET; it cannot prove desync is ONGOING — only a fresh report can. So the
render is a function of `stamp × report-freshness × applied-hash`, not the stamp alone:
- reports FRESH + hash still mismatched: `age = now − policy_desync_since`; `age < T → converging`, `age ≥ T → silent_desync`.
- reports FRESH + hash matches: reconverged → **clear the stamp** → `healthy` (bool governs).
- reports STALE (agent stopped reporting): → **`desync_unknown`, stamp RETAINED.** Silence is NOT evidence
  of reconvergence — never clear on silence, never claim `silent_desync` without current proof, never
  `healthy`. Clearing requires POSITIVE evidence (a fresh report with a matching hash).

**X-2 — ONE clock.** `policy_desync_since` is stamped by the CONTROL PLANE at report-processing time, never
taken from the agent-reported time. Two clocks on one temporal signal is a skew bug awaiting its finding
number.

**X-3 — the reds are the design (named now, a temporal-state surface):** (a) false-alarm — a fresh push →
transient mismatch → NOTHING renders `silent_desync` within T (`converging` shows, never alarms); (b) stuck —
mismatch persisting past T with reports flowing → `silent_desync`; (c) reports-stop — stamped, then reports
cease → `desync_unknown`, NOT `silent_desync`, NOT `healthy`, stamp intact; (d) reconverge — a fresh matching
report clears the stamp; (e) **X-4 write-ownership** — an agent report carrying a FORGED (or absent)
`policy_desync_since` leaves the CP-computed stamp UNAFFECTED (ingest never reads a reported value into the
column); plus the **exactly-T boundary** tested, and **T a named constant** with its 2×-push-SLA rationale.

**Persistence — `policy_desync_since` is a dedicated CP-owned COLUMN** on `nodes` (migration 0021), NOT a
`capabilities` JSONB key. **X-4 (write-ownership, the deciding hazard):** `capabilities` is AGENT-FED — the
report-ingest builds/writes that blob from the agent's `AppliedPolicy`. Putting the CP-owned, X-2-clocked
`policy_desync_since` there reopens exactly what X-2 closed:
- **Clobber** — a blob-replace on the next report erases the CP stamp → it can never accumulate past `T` →
  `silent_desync` becomes structurally UNREACHABLE.
- **Forgery** — any agent-supplied value for that key would be an agent writing the CP's independent
  observation OF that agent, defeating the signal.
A COLUMN removes the fault class by construction: CP-only writability, agent has no path to it, no
merge/namespace-strip discipline to maintain (an argued bound this surface's history says to avoid).
The T-window render reads it as a per-node PROJECTION (age = now − it, computed in Go at `ListNodes`), NOT a
query predicate — so "column only if a predicate" doesn't apply; the deciding factor is write-ownership, not
read shape. **WRITE-OWNERSHIP IS TRANSITION EVIDENCE** (extends the state table): the stamp/retain/clear
transitions are CONTROL-PLANE-WRITE-ONLY; there is NO agent-write transition — the ingest never copies a
reported value into this column.

## State × render × transition-evidence TABLE (the authoritative model — no evidence-less state)
Render is `f(policy_degraded bool × stamp × report-freshness × applied-hash)`. Every state has an explicit
evidence-IN and evidence-OUT; a state with no honest evidence rule would be a PAPER FINDING (fix before code).
The bool is the row's PRIMARY signal; the kind is the differentiated detail — **the badge is NEVER less
alarmed than the bool** (a `converging`/`desync_unknown` kind still shows the bool's degraded state, just
annotated).

| kind | render (over the bool) | evidence IN | evidence OUT |
|---|---|---|---|
| `healthy` | none / green (bool not degraded) | not degraded; hash matches; reports fresh | any degrade condition below |
| `apply_failing` | amber, "apply failing" | `policy_error` set + `policy_failing_since` set (reports fresh) | a fresh report clears `policy_error` |
| `stuck_enforcing` | amber, "enforcing a disabled policy" | `policy_error` set + `failing_since` empty (+ `pushed=="" && applied!=""`) | a fresh report clears the error / mode resync |
| `converging` | subtle, "syncing…" (NEVER a loud alarm) | term-3 mismatch; reports fresh; `age < T` | `age ≥ T` → `silent_desync`; hash match → `healthy`; reports stale → `desync_unknown` |
| `silent_desync` | RED, "silent desync — pushed policy not applied" | term-3 mismatch; reports fresh; `age ≥ T` | fresh matching report → `healthy`; reports stale → `desync_unknown` |
| `desync_unknown` | grey, "can't determine health" | compiled-hash UNAVAILABLE (can't compute `pushed`) **OR** (stamp set AND reports STALE) | a fresh report resolves to a determinate kind above |

Note the bool during `converging`: term-3 makes `policy_degraded` briefly true on every push (its existing
S7.2 conservative behavior). The kind ADDS "this degraded is a transient sync", never subtracts alarm —
`silent_desync` is only when that degraded PERSISTS past T with fresh reports (the actionable case).

## (d) Stated patterns (carried from S7.4a — review-refused otherwise)
- **`loadOne`** for the nodes fetch behind the badge: a failed health fetch renders "couldn't load — retry",
  NEVER a reassuring "healthy" (failure ≠ health — the same law as the notices/loads reductions).
- **Derived state**: the kind is a PURE projection from the caps (like `accessView`/`staleNoticeText`);
  `desync_unknown`/`converging`/`silent_desync` are computed, not stored as UI state — one source of truth.

## SIGNED OFF (S7.4b commit-one, amended) — no branch/code until S7.4a both-green + in-session merge
1. **(c) = OPTION X** (build the temporal state) — amended with X-1 (report-freshness three-valued render),
   X-2 (one control-plane clock), X-3 (named reds incl. exactly-T boundary + T rationale). ✅
2. **6-state enum + `desync_unknown` distinct** (never healthy / never a specific kind); the state × render ×
   transition-evidence TABLE is authoritative — an evidence-less state is a paper finding, fixed before code. ✅
3. **Advisory + durability + read-only guard** — bool authoritative; kind computed-from-same-caps, never
   independently settable, and asserted UNREAD by enforcement/push/compile. ✅
4. **Badge on the `Gateways.tsx` node row** — bool primary, kind the differentiated detail; never less alarmed
   than the bool. ✅
5. **Persistence — CP-owned COLUMN** `nodes.policy_desync_since` (migration 0021), NOT `capabilities` JSONB.
   **X-4:** the JSONB is agent-fed → clobber-on-rebuild + agent-forgeable; a column is CP-write-only by
   construction. Write-ownership (not read shape) decides. X-4 red added; who-may-write is transition
   evidence in the state table. ✅

**Standing order:** these amendments are IN the commit-one now; S7.4b gets NO branch/code until S7.4a's
both-green + explicit in-session merge sign-off executes (avoids stacking on an unmerged base).

### S7.4b story-end fold (review found the kind reading LESS alarmed than the bool)
- **[0]+[3]+[8] COLLAPSE — one compute for bool+kind.** The kind was projected SEPARATELY from the bool
  (two `CompiledHashesForNodes` calls); a push between them suppressed the badge on a genuinely-desynced
  gateway. Now `PolicyHealthForNodes` returns `{Degraded, Kind}` from ONE snapshot (bool+kind can't disagree);
  the kind picks up the bool's TERM-2 (`policy_failing_since` alone → `apply_failing`, never `converging`);
  the open build → `{false, healthy}` (not `desync_unknown` — no policy engine ≠ can't-determine). Residual
  two-read gap (ListNodes rows vs the compile) argued harmless: a push in that gap is a REAL just-pushed
  desync → correctly `converging`.
- **[1] freshness clock — `policy_reported_at` (migration 0022).** `last_seen_at` is bumped by DesiredState
  polls + cert renewal, so it can't answer "has the agent stopped REPORTING?". The desync freshness gate now
  reads `policy_reported_at` (stamped at `ReportWGInfo` ONLY, CP clock). NULL/never-reported = forever-stale =
  can't-determine, never fresh.
- **[2]/[5] log, don't swallow** Stamp/Clear errors — self-healing bound ≤ R (the next report re-evaluates +
  retries on the still-broken state); a swallowed error would show a false red / stuck-converging until then.
- **[7]** `Clear` made idempotent (`AND policy_desync_since IS NOT NULL`) — a healthy report no longer
  rewrites the row.
- **[4] ACCEPTED — NAMED SEMANTICS:** a matched-then-silent gateway (last report in-sync, then quiet) renders
  `healthy`, not `desync_unknown` — that's a LIVENESS concern (shown by `last seen`), NOT a policy-desync one.
  The freshness gate deliberately guards ONLY the mismatch/term-3 path (X-1): staleness turns an *unconfirmable
  desync* into can't-determine, but does not manufacture doubt about a confirmed in-sync hash.
- **[6] LEDGER (not a new item):** `trackDesync`'s per-report `CompiledHashesForNodes` rides the existing S7.2
  perf ledger #8 (cache-compiled-by-version) — the same compile-cost follow-up, now also on the report-ingest path.

### S7.4b confirm-pass dispositions (post-fold)
- **[578] ACCEPT-AS-DESIGNED:** after migration 0022 deploys, an existing node with a stored mismatch reads
  `desync_unknown` until its FIRST post-migration report (≤ one report cycle, ~30s) — `policy_reported_at` is
  NULL until then, and NULL = forever-stale (the signed-off semantics). Conservative (non-healthy,
  non-fail-open), self-healing, a one-time deploy artifact — not a regression.
- **[569] open-build kind semantics WIDENED (doc-of-record):** the original fold-8 note said the open build
  is "always healthy". The confirm pass hardened it: the open-build kind now REFLECTS a caps apply-error
  (`apply_failing`/`stuck_enforcing`) so `{Degraded, Kind}` agree STRUCTURALLY (not just architecturally). The
  new behavior is strictly more honest and stands; the "always healthy" phrasing is superseded.

## Proof obligations (per PR, at build time)
- **S7.4a:** view-model/component tests (electron-free where shared); RBAC-gate + edition-gate + verified-email
  gate render tests; **manual wire-verification on the live two-gateway box** (create rule → grant appears;
  toggle enforcing → post-hoc affected list; device-approval queue → approve → device active). Rule
  delete+recreate flow (no update) proven.
- **S7.4b:** `policy_degraded_kind` projection unit test per kind (A/B/C signatures → correct enum; `none`
  when healthy); boolean-unchanged regression; badge render test. Assert NO new stored state / no migration.
- **S7.4c:** the two deferred assertions go GREEN against the enterprise build (S4.5 payload, S4.5b real
  orphan); mem-backend enrolled node + stranded device reproducible in CI; e2e stays non-blocking.
