# CLI distribution review and publication checkpoint

Date: 2026-09-09. Branch: tunnex-cli-publish.
Decision paper: 7379e9e6. User approved GitHub hosting and dedicated signing.

## Published and verified

- Homebrew tap: https://github.com/tunnexio/homebrew-tap
  Formula publication commit cb6f2cc; macOS/Linux source install/test/uninstall
  passed in https://github.com/tunnexio/homebrew-tap/actions/runs/34312676916.
  Fetched public formula matches the tested generated bytes.
- Package release: https://github.com/tunnexio/packages/releases/tag/v0.1.25
  Signed DEB, RPM, APK, Arch packages and static Linux archives on amd64/arm64.
  Upstream v0.1.25 and source 26a36afc9be657af94127c474e173783c91318c3 remain unchanged.
- Hosted indexes: https://tunnexio.github.io/packages/
  APT, RPM/DNF/YUM/zypper, APK and pacman.
- Build/index/native proof: https://github.com/tunnexio/packages/actions/runs/34313209895
  Exact head 2cdb706923fec3b7ccde93f7c7728bac7a40b357 passed on Ubuntu 24.04,
  Debian 12, Fedora 42, Rocky 9, Amazon Linux 2023, openSUSE Leap 16.0,
  Alpine 3.22 and Arch Linux containers.
- Public endpoint native proof: https://github.com/tunnexio/packages/actions/runs/34313832784
  Exact head 63edd240998ce99f52e1605b26926b8342f9b847 passed APT/RPM/APK installs
  on native AMD64 and ARM64 runners, plus pacman on AMD64.
- Public indexes/signatures and sample package hashes independently verified
  from the Mac without authentication. Evidence is committed under
  walk-artifacts/cli-publish/. No live login, enrollment or VPN routing is claimed.

Private signing material is in dedicated packages Actions secrets. Public GPG
fingerprint: 9A565661A108388E34E4D9C6A0C5B3B1D39A8181. A permission-restricted local
recovery copy remains at /private/tmp/tunnex-cli-publish-signing, outside Git.
No personal GitHub token was copied into CI secrets.

## Reviewed findings F1-F3: approved, folded, retested

Two independent finders reviewed the initial package and Homebrew implementation.
User disposition: "Teeno fix karo aur retest karo".

1. P1: signed-manifest membership. Fixed with exact asset equality and
   verified-only copying; unlisted-package and path refusal regressions pass.
2. P1: APK filenames. Repository filenames now use validated actual .PKGINFO;
   concatenated gzip/tar fixture and native/public apk installs pass.
3. P2: Arch version ordering. --prevent-downgrade is used; the real repo-add
   boundary fixture keeps 0.1.100 instead of lexically later 0.1.99.

Both finders re-reviewed those folds with no actionable findings. Additional
observed CI issues were fixed at their responsible layer: allowed-action policy,
Homebrew runner identity/API resolution, nFPM key-ID format, APK index-builder
public trust key, and uninstall assertions checking filesystem rather than shell
command cache. Signature checks were retained throughout. APT itself rejects a
tampered InRelease with BADSIG.

## Nix/Arch recipe slice: two findings HELD

Draft sources are in deploy/cli-distribution/packages and the standalone local
checkout /private/tmp/tunnex-cli-publish-packages. They have NOT been published.
A user disposition request for the following two findings is pending:

1. P2: channels.py Arch source aliases are always `tunnex`. Include version and
   architecture so a cached prior release does not cause checksum failure.
2. P2: channels.py/channels.yml can replace recipes with an older valid upstream
   latest release. Reject lower versions and changed SHA at the same version,
   including a comparison with current main at publication.

No fixes to these findings have been applied. After disposition, add regression
proof, re-review the fold and run native Nix amd64/arm64 and Arch makepkg CI.
AUR submission, nixpkgs inclusion, Snap, desktop stores and official distro
archive acceptance remain separate uncompleted work; GitHub login alone does
not establish ownership of those external publisher accounts.
