# S8.1 — Site/Gateway model (EPIC 8 opener) · commit-one, decision-first

**Self-authenticating provenance.** Decide-items 1–6 are reconstructed from **PLAN.md § "EPIC 8 —
Site-to-Site Networking"** (the decide-before-code ledger accreted at S7.1/S7.2/S7.5.1/S7.5.4 — see
PLAN.md lines ~1307–1375, the ProtocolVersion-gate hardening at ~1316–1330, and the S7.2
subnet-advertisement / transport-enum ledger at ~1343–1364). Items 7–11 are additions from an
external review pass (Gemini, founder-run, 2026-07-17), dispositioned by the founder in-session. This
paper is the record; a future session re-enters from it, not from the conversation. All items LOCKED
below unless marked PARKED/DEFERRED.

**Scope (S8.1).** Register **site gateways** (each a `tunnex-node` agent) and their **subnet routing**.
Sites/subnets become a policy **destination kind** in the versioned `policyspec.Compiled` artifact.
Reserves transport-enum + DNS-forwarding seams for later stories. Routing and Zero-Trust enforcement
are **transport-agnostic** by design. S8.1 builds the MODEL + the fail-closed version gate + the
disjointness validator; route *propagation* on the wire is S8.2.

---

## D1 — ProtocolVersion fail-closed gate · LOCKED (blocking)

**Decision.** The agent gains a `maxSupported` protocol version. On receiving a `Compiled` artifact with
`Version > maxSupported`, it **refuses the artifact → deny-all + `policy_degraded`** rather than
applying it. Built in commit-one, BEFORE the first enforcement-significant wire field — and
sites-as-destination (D3) IS that field. Today the node applies any version, safe-ignoring unknown
JSON fields: safe **by convention only** (every bump so far observability-additive + hash-blind +
safe-ignore, each unit-pinned). This mechanizes the convention.

**Amendment (ruled).** The refuse path sets a **distinct `policy_degraded_kind`** —
`unsupported_policy_version` — following the S7.4b advisory-kind-over-the-bool pattern. An operator
MUST distinguish "agent too old for this artifact" from every other degradation, because its remedy is
**unique: upgrade the agent.** A bare `policy_degraded` bool would send the operator hunting.

**CP-side surface (answered).** A version-refusing gateway self-reports through the existing S7.4b node
policy-health path: the agent's reconcile loop sets `policy_degraded=true` +
`policy_degraded_kind=unsupported_policy_version`, stamped with the `policy_reported_at` freshness
clock. `PolicyHealthForNodes` (the one CP-side compute) surfaces it in the node/site health UI with an
"upgrade the agent" remedy hint — a first-class, honest degradation state, not a silent stale.

**Deliberate red.** Agent handed `maxSupported+1` → deny-all + `policy_degraded` +
`kind=unsupported_policy_version`; NEVER silent-accept. Retires the interim "every bump owes its own
hash-blind pin" law once this lands.

**Go-forward-only — the pre-gate residual (Slice-1 gate, ruled).** The gate protects only agents that
HAVE it. A genuinely OLD binary (v3-era, no gate code) does not deny-all on the Slice-3 sites bump — it
**silently accepts** the artifact and safe-ignores the sites field, i.e. **enforces stale policy while
reporting healthy** (the reassuring-green class, on the wire, for exactly the population most likely to
exist mid-rollout). Recorded consequences:
- **What the interlock red actually claims (Slice 3 states it):** the interlock red simulates a *gated*
  agent pinned to the OLD `maxSupported` (the go-forward guarantee). It does NOT — cannot — cover a
  pre-gate binary. Slice 3's red comment must name the simulated agent precisely so the claim isn't
  over-read.
- **The guarantee is go-forward only** — the honest sentence, said here and owed in the walk.
- **Deployment order (S11.4-class upgrade docs, not just this file):** the v3→sites transition is
  **upgrade agents FIRST, then the CP**. If the compose quickstart ships CP+agent as one unit this is
  mostly theoretical; **multi-gateway customers upgrade piecemeal** — name it in the S8 upgrade guide
  where they read it, not only here. REGISTERED as owed at the S8 upgrade-docs story.

**Reported hash under refusal (Slice-1 gate, ruled).** A refusing agent reports its **last-good (or
empty) applied hash**, NEVER the refused artifact's hash — structural: `SetPolicy` does not store the
refused artifact, so `applyAndTrack` only ever hashes the last-good/nil policy. Echoing the refused hash
would read synced-and-healthy to the desync detector, contradicting the kind. Pinned
(`TestUnsupportedVersionRefusedIsDenyAll` note-2 assertion).

## D2 — Enforcement-vs-observability projection checklist · LOCKED

**Decision.** Every new `Compiled`/`AllowEntry` field introduced for sites is classified into or out of
`CanonicalHash`'s projection (the S7.5.1 A-1 rule). Each observability field gets a hash-blindness unit
pin (the v3 `src_device_id` pattern).

Per-field table — **Slice-3 OUTCOME is Option A: NO new wire field** (the classification was RUN and the
answer is null; see the Slice-3 ruling section). The table records that affirmatively:

| field | kind | in CanonicalHash? |
|-------|------|-------------------|
| **(no new `AllowEntry`/`Compiled` field)** — a `device→site-subnet` grant reuses `DstCIDR` | n/a — reuses an existing in-hash field | (`DstCIDR` already YES) |
| `Version` 3→4 (the sites bump) | **enforcement-significant** (in-hash; gates the wire shape) | YES (bumping it is the change) |
| rule-level `dst_kind='site'` + `dst_site_id` | **CP-side schema only** — resolved at compile to `DstCIDR`; NEVER on the wire | N/A (never reaches the artifact) |
| `link.transport` / `link.mtu` / DNS-forwarding (Slice-2 reserved) | not compiled yet | N/A until S8.2/S8.4 |
| site `id` / display name / label | observability | NO (never compiled into a grant) |

The muddy Option-B `DstKind`-on-the-wire row was REJECTED (it failed this checklist — redundant or
observability-dressed-as-enforcement). Sites-as-destination is enforcement, but the enforcement rides
`DstCIDR` (already in-hash) + the `Version` bump; the KIND stays a CP-side schema fact.

## D3 — Sites/subnets as a policy DESTINATION KIND · LOCKED

**Decision.** Extend the S7.1 policy model: a rule's DESTINATION can be a **site** (`dst_kind='site'`).
Sites/subnets are a first-class destination kind **at the rule level (CP-side)**; the compiler resolves
a site to its subnet CIDRs, so on the wire the grant is a plain `AllowEntry{Src, Dst: site-subnet-CIDR}`
— **Option A (ruled): no new wire field** (see the Slice-3 ruling section). Zero-Trust governs
site-to-site by construction: under `enforcing`, a site subnet is reachable ONLY via an explicit grant;
under `off`, the legacy mesh. Mode-as-compiler-input (one code path, one artifact, mode selects what is
compiled) — the S7.1 lock. **Routing ≠ permission / approved ≠ reachable.** The version bump introduced
here (`Version` is in-hash) is what trips D1's gate (semantic change → deny-until-agent-supports).

**S8.2 boundary.** The deliberate-red — *enforcing + zero grants → a propagated site subnet is ROUTED
but DROPPED at the gateway forward chain* — is **named here, built and wire-proven in S8.2** (route
propagation is S8.2). S10.3 (cluster gateway) already presumes this seam; it is load-bearing beyond
EPIC 8.

## D4 — Site-link TRANSPORT enum · LOCKED

**Decision.** `link.transport` is a modeled enum from day one; **`wireguard` is the only implemented
value.** This RESERVES the parked IPsec-interop seam (agent-impossible endpoints: managed cloud VPN
gateways, hardware appliances) without a later migration. IPsec stays **PARKED** (trigger = a real
customer with an agent-impossible endpoint AND after EPIC 9; strongSwan-managed-by-agent, site-to-site
only, interop matrix bounded at story-open). Routing + ZT enforcement are transport-agnostic — stated
in the schema comment beside the enum.

**Promotion path (Slice-2 ruling 1).** `link_transport` lives as a **site-level column** (migration
0032) — because v1 is single-node (a site = one gateway = one uplink; the `nodes.site_id` partial
unique index enforces it), a `site_links` table now would model a cardinality that cannot exist. When
multi-link / HA unparks, the column **promotes to a per-link table** — a mechanical migration done WITH
the feature that needs it, and **the enum/CHECK semantics carry over unchanged**. **Refuse-don't-guess
(schema twin of D1's gate):** the `CHECK (link_transport IN ('wireguard'))` makes inserting any other
value (e.g. `'ipsec'`) a **loud 23514 failure**, not a silent accept — a new transport is unusable
until its migration lands. Pinned (`TestSiteTransportCheckRefusesUnknown`).

## D5 — Subnet-advertisement model (+ D7 disjointness, folded) · LOCKED

**Decision.** A site advertises the subnets it routes; advertisement requires **control-plane/admin
APPROVAL before propagation** (a compromised site gateway must not hijack routes by advertising subnets
it doesn't own). State machine: `pending → approved`. `approved ≠ reachable` — ZT grants still gate
reachability (D3). Overlap handling: **the one forbidden outcome is silent ambiguity.**

**D7 folded in as D5's bidirectional completion (BINDING).** The S7.2 ledger's overlap-refusal covered
**site-vs-site only**. It did not assert disjointness against the org's **device pool CIDR** or
**reserved ranges** — a site subnet overlapping the pool → gateways route device-/32s toward a site
link (the silent-ambiguity class via a collision the ledger never enumerated). And it is
**bidirectional**: S4.5b's **pool-resize (grow)** must equally refuse growing into an approved site
subnet, or the check is a race.

- **ONE disjointness validator**, three inputs: existing **site subnets** + the org **pool CIDR** +
  **reserved ranges**. Pure/deterministic.
- Enforced at **BOTH seams**: advertisement-approval AND pool-resize (S4.5b grow).
- **Typed errors both directions:** advertisement-into-pool → `gateway_no_egress`-class; resize-into-site
  → `illegal_resize`-class. Never a silent accept.
- **Deliberate red each way** (advertise-overlapping-pool refused; grow-into-approved-site refused).
- **Touch to existing audited behavior (flagged):** the S4.5b resize path gains a validator call →
  **inherits the existing resize tests + a new red.** No behavior change to the happy path; the new
  refusal is additive.
- Precedence / longest-prefix / manual route pinning **DEFERRED** (trigger = first real customer need).

## D6 — Site model + the site-entity argument (+ reserved seams) · LOCKED

**The argued decision (not just tables): is a site a new ENTITY that owns nodes, or a role/attribute of
the existing node?**

**Ruling: a site is an ENTITY that owns ≥1 node.** Argument:

- **Node replacement.** A site's subnets, approved advertisements, DNS-forwarding entries (S8.4), and
  identity are **site-scoped, not node-scoped.** If a site WERE its node (attribute model), replacing a
  failed gateway box would destroy the site's identity and every advertisement/grant hanging off it —
  unacceptable operationally. As an entity, replacement = rebind `site.node`; the site survives.
- **FKs.** `sites(id, org_id, name, …)`; a node references its site (`nodes.site_id` nullable — a plain
  non-site agent has none); `site_subnets(site_id, cidr, …)`; advertisements and grants reference the
  **site**, not the node.
- **Enrollment ceremony.** No new join protocol. The node still enrolls via the **existing join-token /
  agent flow** (S3/S6 pattern). The SITE is created as an org entity (admin action), then a node is
  **BOUND** to it — a site-flavored binding step, not a second enrollment mechanism.
- **HA seam (decide-or-name — NAMED + reserved).** "Can one site have two gateways?" — **v1 enforces
  single-node-per-site** (app-layer + a uniqueness constraint). The entity-owns-≥1-node shape models
  multi-node HA **by construction** when unparked; the attribute model could not (one node = one site).
  Multi-node/HA is a **reserved seam, not a v1 feature** (trigger = HA demand; ties to D10's parked
  cache + batch-3 #4 CP-HA).

**Reserved seams (built as columns/enums, not features):** `link.transport` (D4); site
**DNS-forwarding entries** (S8.4). Reserved, not populated in S8.1.

## D8 — Hub-topology model check · LOCKED (traced, not asserted)

The framing "bricked without relay" is **REJECTED** — the no-relay limitation stays named-and-deliberate
(batch-3 #3: NAT-traversal is a documented deployment requirement; STUN/relay parked). The model must
**not** assume full-mesh direct connectivity.

**Trace through the drafted schema (D6) — hub-and-spoke of existing pieces, no new mechanism:**

- Site A (NAT'd) — its node `nA` has a **link** `{src: nA, dst: nH, transport: wireguard}`, WG dialing
  **outbound** to hub node `nH`.
- Site B (NAT'd) — its node `nB` has a **link** `{src: nB, dst: nH, transport: wireguard}`, likewise
  outbound to `nH`.
- Hub `nH` (publicly reachable `tunnex-node`) — has both peers; its gateway **forwards** between the two
  WG interfaces (the same wg0→wg0 FORWARD the device-to-device path already uses).
- `site_subnets`: A's subnet + B's subnet, each **advertised → approved** (D5). ZT **grants** gate A↔B
  reachability (D3) exactly as for any destination.

The **link model is per-link (node↔node), not per-site-pair-mesh** — nothing in the drafted tables
requires an A↔B direct link. Hub topology is therefore a **supported arrangement of existing pieces**
(per-link modeling + normal WG outbound + forwarding + ZT grants), needing **no STUN, no relay fleet,
no new mechanism.** Holds by construction against the schema as drafted.

## D9 — MSS clamping · noted in S8.1, BINDING for S8.2 commit-one · LOCKED

Double encapsulation (a client-WG session riding a site-WG link) drops effective MTU; PMTUD fails
silently inside tunnels → the "large transfers freeze, pings work" class. This is data-plane behavior of
**propagated routes = S8.2's domain.** S8.1 owes only:

- **(a)** the schema **reserves a per-link MTU attribute** if the model needs one (a nullable `link.mtu`).
- **(b)** **S8.2 commit-one MUST decide** the agent's TCP **MSS clamp** on site-link forward paths (nft,
  in the existing `tunnex` table). *(recorded binding, not built here.)*
- **(c)** the **S8.2 box-walk owes a leg** — a large transfer across a double-encapsulated path,
  **wire-proven.**
- **(d)** the clamp rule is **classified under D2's checklist** — lean node-local / outside the hash
  (the masquerade-rule class), but **classify explicitly, don't assume.**

## D10 — Site-gateway cold-start · LOCKED (restatement + PARKED variant)

The S7.2 cold-start **deny-until-first-fetch** was chosen deliberately and **stands**: cached policy on
boot = a reboot-during-CP-outage **resurrects revoked grants** — for an enforcing org, a **breach**, not
an outage. A cache is that rejected alternative in an HA costume. The proposed cached-policy fallback is
**REJECTED as offered.**

BUT the review's observation is TRUE and gets named: a site gateway is a **LAN router**, so
deny-all-on-cold-start now **severs office-to-office** — a materially larger blast radius than a single
device.

- **(a) RESTATE** the cold-start limitation with the **site-sized blast radius named** (honesty-ledger
  discipline — the limitation is now bigger, say so).
- **(b) PARK a bounded-cache variant.** Trigger = design-partner / HA demand. Constraints **pre-stated
  so a future unpark cannot soften them:** signed artifact + staleness ceiling (minutes) + **NEVER
  applies under `enforcing`** (off-mode mesh restoration only) + org-level opt-in (standing convention).
- **(c) Cross-ref batch-3 #4** (control-plane HA posture, rides S10.1/S11) as the **systemic** answer —
  the cache is a bounded palliative, HA is the real fix.

Decided-in-paper, NOT built.

## D11 — Unlock-then-opt-in + edition gating · LOCKED (the edition split)

**Is site-to-site core or enterprise-gated?** The standing convention binds every new capability
(unlock-then-opt-in, default-OFF, org-level); any part that is an enforcement flag states its
downgrade-release at commit-one (the S7.5.3 law). **Argued split:**

- **Core (open, both edition suites):** the **site model** (D6), **subnet routing**, **advertisement +
  approval** (D5), the **disjointness validator** (D5/D7), the **fail-closed version gate** (D1), and
  **off-mode legacy-mesh reachability**. Rationale: this is plumbing, like the IP allocator / device
  pool — a self-hoster on the open edition can route between their sites.
- **Enterprise (unlocked by edition):** **ZT-governance of site traffic** — i.e. `enforcing`-mode
  gating of site-subnet reachability via grants. This is enterprise **by construction**, because the
  policy engine / `enforcing` mode is already enterprise-gated (`edition_required` 403). Site subnets
  ride the **existing `zero_trust_mode`** switch — **no new opt-in flag** (unlock = enterprise edition
  makes ZT-governance available; the org opts in via the existing `enforcing` mode).
- **Downgrade-release (S7.5.3 law).** The enforcement flag here is `enforcing`-mode gating of site
  traffic. On enterprise→open downgrade it **releases to off-mode legacy mesh**: site subnets become
  **routed but no longer grant-gated** (the same mode-as-compiler-input revert S7.5.3/S7.5.4/S7.5.5
  established). No new flag, so no new downgrade-clear beyond the mode revert — **state it explicitly**
  so it is not silently inherited.
- **Test placement (why the split matters):** core tests (model / routing / advertisement /
  disjointness / version-gate) live in the **open** suite (run in BOTH editions); the
  ZT-governance-of-site-traffic tests (enforcing gates a site subnet; downgrade releases it) live in the
  **enterprise** suite.

---

## What S8.1 builds vs reserves vs defers

- **Builds:** sites/site_subnets/links schema (site-as-entity, single-node v1); `dst_kind=site_subnet`
  in `Compiled` (+ version bump); the D1 fail-closed version gate (+ `unsupported_policy_version` kind +
  CP health surface); the D5/D7 disjointness validator at both seams; advertisement + approval state;
  the edition split (D11).
- **Reserves (columns/enums, unpopulated):** `link.transport` (D4), `link.mtu` (D9a), site
  DNS-forwarding entries (S8.4), multi-node-per-site / HA (D6).
- **Defers / parks:** route propagation on the wire → S8.2; MSS clamp decision → S8.2 (D9); IPsec →
  parked (D4); bounded-cache cold-start → parked (D10); precedence/longest-prefix/manual-pinning →
  deferred (D5).

## Deliberate reds owed at build

1. D1: `maxSupported+1` artifact → deny-all + `policy_degraded` + `kind=unsupported_policy_version`.
2. D5/D7: advertise a subnet overlapping the pool CIDR → refused (`gateway_no_egress`-class); resize the
   pool into an approved site subnet → refused (`illegal_resize`-class).
3. D6: replace a site's node → site identity + subnets + advertisements survive (entity, not attribute).
4. D11: enterprise `enforcing` gates a site subnet (grant required); enterprise→open downgrade →
   the same subnet reachable via off-mode mesh (release proven).

## Slice plan (approved 2026-07-17) + build conditions

Four slices, each gated (`generate-check` + `test-editions`/`build-editions` BOTH editions + web where
touched). Edition split proven, not asserted — tests live in their correct suite (Slices 2/4 core/open,
Slice 3 enterprise).

**Slice 1 — D1 fail-closed version gate** (foundational, site-free). Agent `maxSupported` + refuse
`Version > maxSupported` → deny-all + `policy_degraded` + distinct `unsupported_policy_version` kind;
CP surface via S7.4b `PolicyHealthForNodes` + remedy hint.
- *Red (synthetic):* `maxSupported+1` → deny-all + kind (never silent-accept). **Proves the MECHANISM.**
- **Condition 1 (ruled):** the kind must reach the **operator-visible surface** — the red asserts
  `unsupported_policy_version` lands in `PolicyHealthForNodes` **output** (badge/API), not just the
  node's self-report. Walk implication: S8.1's bounded wire proof shows this kind on the **live gateway
  health surface** ("upgrade the agent" is a remedy an operator reads, not a log line).

**Slice 2 — D6 site model (entity, single-node v1) + D11 core plumbing.** Migration
`sites`/`site_subnets`/`nodes.site_id` + register-site/bind-node APIs + RBAC.
- *Red:* replace a site's node → identity + subnets + advertisements survive (entity, not attribute).
- **Condition 2 (ruled):** the migration carries the **full reservation set, verified** — `transport`
  (enum, `wireguard` default), `mtu`, DNS-forwarding — and the **down-migration is tested with a site
  row present** (the S7.5.4 [7] class: down-migrations fail on populated edge rows). Test **up→down→up
  with a registered site + subnet + bound node**. `nodes.site_id` **nullable** (existing gateways are
  site-less and must stay valid — say so in the migration comment).

**Slice 3 — D3 destination kind + D2 projection + D11 enterprise governance.** `dst_kind=site_subnet` in
`Compiled` + **version bump**; per-field hash classification + blindness pins; `enforcing` gates
site-subnet reachability (enterprise); `off` = mesh.
- *Reds:* enforcing gates a site subnet (grant required); enterprise→open downgrade → same subnet via
  off-mode mesh.
- **AMENDMENT (ruled) — the interlock red is Slice 3's own test:** a **GATED agent pinned at the
  pre-S8.1 `maxSupported`** (NOT a pre-gate v3 binary — that silently accepts; see D1 go-forward-only)
  receives the **real Slice-3 artifact** (the actual sites bump, not a synthetic version) → deny-all +
  `unsupported_policy_version`. The red comment states which agent it simulates so the go-forward-only
  claim isn't over-read. Slice 1's synthetic red proves the mechanism;
  Slice 3's proves it **fires on the real event it was built for** (the claim EPIC 8 depends on). It
  ALSO guards against an **accidental non-bump** — if Slice 3 forgets to increment the version, the old
  agent silently accepts and the interlock red fails. Catches both directions.

**Slice 4 — D5 + D7 advertisement/approval + disjointness + THE HTTP/OpenAPI SURFACE.** Advertisement
`pending→approved` + the ONE disjointness validator wired at both seams (advertisement-approval AND
S4.5b pool-resize).
- **Absorbed (Slice-2 ruling 2):** the HTTP/OpenAPI surface deferred from Slice 2 lands HERE, as one
  coherent spec change: **register-site + bind-node + advertisement submit/approve endpoints + the
  RBAC perm(s)** (`site:manage` and/or an advertisement-approve perm). Rationale: the API's natural
  shape includes advertisement approval; splitting register/bind now from advertisement later would
  double the OpenAPI/401-walk/RBAC/generated-client review surface for no proof gained (the Slice-2
  service + reds already prove the model's semantics; HTTP is consumption).
- *Reds:* advertise-overlapping-pool refused (`gateway_no_egress`-class); resize-into-approved-site
  refused (`illegal_resize`-class). S4.5b touch inherits its resize tests + the new red.
- **Condition 3 (ruled):** the validator is **ONE function, table-driven inputs, called from both
  seams** — not two similar checks. The red suite includes **adjacency cases** (touching-but-disjoint
  CIDRs PASS; exact-boundary overlaps REFUSE — off-by-one CIDR math is where disjointness validators
  break). **Both seams' refusals are audited** (resize already audits; the advertisement-approval
  refusal leaves the same class of record).
- **Guards owed at Slice 4's gate (say so explicitly):** the new endpoints are picked up by the
  401-walk + the RBAC matrix, and each new perm gets its **deliberate-red** per the standing convention
  (a non-holder is refused). State this in the Slice-4 gate report.

**Wire-proof bounding (accepted):** S8.1 = gate + registration + health on the live agent; cross-site
traffic = S8.2's box-walk (route propagation is S8.2). Unit/integration SUBSTITUTES for the cross-site
wire, NAMED with trigger = S8.2 box-walk.

## Slice 3 — sites-as-destination: Option A (RULED 2026-07-17) + propagated deltas

**Fork ruled: Option A — version-only bump, NO new AllowEntry field.** The crux (`hash.go:30,41`):
`Version` is IN the CanonicalHash projection, so bumping `ProtocolVersion 3→4` alone changes every
gateway's hash (the twin goldens change from the version number) AND fires the S8.1 gate on any agent
at `maxSupported < 4`. A `device→site-subnet` grant compiles to a **same-shape** `AllowEntry{Src:/32,
Dst:site-CIDR}` — enforcement-identical to a resource grant — so there is no new wire field.

**Why A (deciding argument, recorded):** Option B (a `DstKind` field on the wire) FAILS the very D2
checklist it tried to satisfy — the CIDR already fully determines enforcement, so the label is either
redundant or observability-dressed-as-enforcement (an old agent has no enforcement reason to mis-handle
it until S8.2's routing). D2 exists to REFUSE muddy-classification fields; inventing one to have
something to classify inverts the checklist. Option A's honest answer — the enforcement-significant
change at Slice 3 IS the version bump declaring the sites boundary — is what the projection discipline
looks like when the answer is "no new field."

**A-4 discharge (closure, cite `hash.go:25-28`).** That comment warned Version-in-hash was safe ONLY
while the CP serves a single version fleet-wide, with the warning pointed at EPIC 8. EPIC 8 is here: the
bump makes mixed versions real (piecemeal multi-gateway upgrade). The **S8.1 D1 gate** is what makes
Version-in-hash safe under mixed versions — an old GATED agent receiving v4 **refuses loudly** (deny-all
+ `unsupported_policy_version`) rather than diverging silently. The warning fires and its answer ships
in the SAME slice. (The ungated pre-S8.1 residual stays papered, D1 go-forward-only; same-shape Option A
means even it enforces correctly, only the upgrade-order sentence is owed in the S8 upgrade docs.)

### Conditions (ruled)
1. **D2 records the NULL classification affirmatively** — an explicit "no new AllowEntry field;
   site-subnet dst reuses `DstCIDR` (already in-hash); the version bump itself is the
   enforcement-significant change" row, so a future reader sees the checklist was RUN, not skipped.
2. **Rule-level dst_kind schema integrity** — `policy_rules` gains `site` as a dst kind + `dst_site_id`
   → `sites(id)` **ON DELETE CASCADE** (deleting a site — or leaving it subnetless — must not dangle a
   grant). Mirrors the S7.1 resource/group cascade discipline. One red.
3. **Compiler resolution semantics (CP-side, clockless-compiler pattern) — edges pinned:**
   - a site with **zero subnets** compiles to **nothing** (no grant, not an enforce-time error);
   - a site with **N subnets** compiles to **one AllowEntry per subnet**;
   - **Rule-creation against a subnetless site is ALLOWED** (not refused) — the site is a valid target;
     subnets are added independently; refusing would impose an ordering dependency, and the compile
     honestly reflects current state (yields no grant until subnets exist). Stated, not silent.
   Both edges live in the enforcing-gates red's fixture family.
4. **Goldens change gets its own gate-report sentence** — both modules' fixtures bump together with
   IDENTICAL expected hex (the twin-goldens guard is EXERCISED by this slice); the old-hex→new-hex delta
   is attributable to the `version` field ALONE (a confirmation the projection picked up nothing else).

### Propagated deltas (recorded now)
- **(a) S8.2 commit-one inherits the REAL new-field classifications** — LAN-source CIDRs (a site subnet
  as a SOURCE, not a /32 device) and link routing — each walking D2 against the now-proven gate
  mechanism. The fork's Option-B analysis is their head start (the classification work is pre-done).
- **(b) Interlock red claim-language stands unchanged** (Slice-1 conditions): a GATED agent pinned at
  old max refuses v4; the ungated pre-S8.1 residual stays papered with the upgrade-docs sentence owed.

## Convention (Slice-3 gate): migration tests PIN their version

`db.MigrateTo(dsn, N)` — a migration up→down→up test migrates to EXACTLY its own version N (not "the
latest" via `db.Up`) before `DownOne`. Otherwise a LATER migration silently changes what `DownOne`
rolls back and breaks the earlier test (exactly what adding 0033 did to the 0032 test). Every future
migration inherits immunity from this class. Rule, not habit.

## LAW (Slice-4 gate) — an in-tx audit write's error is load-bearing

Swallowing an `InsertAuditLog` error inside a transaction is never acceptable — it has now bitten in
BOTH directions across two stories:
- **S7.5.5 #7:** the error was swallowed and the AUDIT was silently LOST (a reassuring comment claimed
  it was recorded).
- **S8.1 approval:** the error was swallowed and the OPERATION died with an unattributable
  commit-rollback (`commit unexpectedly resulted in rollback`) — the poisoned tx surfaced far from its
  cause.

Same root, opposite faces. **The generalization:** an in-tx audit write's error must PROPAGATE (failing
the transaction legibly, attributed) OR the audit must be deliberately OUT-of-tx (the outcome-not-error
refusal pattern — S7.5.5 counter law, applied here to the refusal audit so it survives the failed
approval). Swallowing is never an option, in either direction. `sites.audit` now returns its error;
the approve path propagates it; the refusal audit is a separate committed op. **Armed-guards inventory
candidate; review probe owed:** sweep older surfaces for in-tx audit calls whose error is discarded —
two hits in two stories means siblings are likely.

## LAW (story-end #1) — a validator's input filtering is part of its attack surface

The disjointness validator was BYPASSED by a one-line convenience filter at its call site
(`ApproveSubnet` excluded approved subnets whose CIDR equaled the candidate's, `if a.Cidr != sub.Cidr`).
Stated purpose: exclude the candidate from its own comparison list. But the candidate is PENDING and
the list is APPROVED-only, so that exclusion was already satisfied STRUCTURALLY — the filter was dead
code with a LIVE side effect: it exempted the exact-duplicate-CIDR collision (two sites can hold the
same CIDR — `site_subnets` uniqueness is per-SITE, not per-org), the one class the org-wide check most
needs. The original red passed green over it because no fixture had two sites sharing a CIDR.

**Generalization (checklist line for every future validator — S8.2's route-propagation checks inherit
it immediately):** a validator's COMPARISON SET is passed WHOLE. Any exclusion applied to it is a
bypass wearing a convenience costume until proven STRUCTURALLY necessary; and the red's FIXTURE FAMILY
(not just its assertions) must contain the collision the exclusion could hide — here, two subjects
sharing the disputed value. Fixed: filter dropped; red gains the two-sites-same-CIDR (exact +
containment) family.

## STANDING INSTRUCTION (founder-directed) — founder UI walk at EPIC 8 close

After **S8.4 merges (epic close)**, a **founder-driven UI walk** runs over the FULL EPIC 8 surface — the
S8.3 site-management UI, topology view, the Access page's site-destination builder, health surfaces —
**same protocol as the S7.5.5 UI walk** (Pawan clicks, Claude scripts step-by-step, findings HELD for
disposition, WF-numbered). **Binding precedent (S7.5.5):** the wire walk missed WF-3 because no
automated layer navigates a browser — so **every epic with UI scope owes a human click-through before
its LAST merge**. S8.3 is the epic's dedicated UI story, so its own box-walk should already be UI-heavy;
the epic-close walk then covers the INTEGRATED whole (rules → sites → topology → health in one journey).
Plan S8.3's walk with that division in mind. (Also record in PLAN.md's EPIC 8 section at the S8.1 merge.)

## S8.1 bounded walk — method notes (ruled)

- **Leg 1 method (stated before driving):** the honest leg for the SHIPPING mechanism is a **real
  v3-era agent** (pre-Slice-3 binary, gate present — the pre-bump commit's agent build, or a local
  build pinned `MaxSupported=3`) receiving the REAL current v4 artifact → refuses → deny-all + the kind
  on the health surface (the interlock on the wire, real rollout scenario). A hand-fed synthetic v99
  artifact tests the same code path but NOT the real scenario — if box constraints force it, record it
  as the SUBSTITUTE with the distinction named.
- **Leg 2:** the registered site's gateway binding must be visible **where an operator looks** (the
  health/site surface), not just a 200 on the API.
- **Leg 3 negative:** after the duplicate-CIDR refusal, **observe the pending row's state on the wire** —
  refused-but-still-pending (re-approvable after the conflict clears) vs terminally rejected — and record
  which the design gives (the approval refusal edge is not yet wire-observed). [Design: the subnet STAYS
  pending — approval is idempotent-retryable once the conflict is removed; the walk confirms it.]

## S8.1 bounded walk — RESULTS (executed on the wire; evidence walk-artifacts/S8.1/box-walk.md)

- **Leg 2 — LIVE.** HQ + Branch registered (201), operator-visible via `GET /sites`; node bound (204,
  `nodes.site_id` set). BOUNDING: node→site binding is NOT in the nodes API (`listNodes` has no
  `site_id`) — binding operator-surface is S8.3's UI. Honest bound, not a shipped-model gap.
- **Leg 3 — LIVE (incl. the #1 SECURITY fix + the negative).** HQ `10.20.0.0/24` approve → 204 +
  `site.subnet_approved`. Branch **same** `/24` approve → **409 `subnet_not_disjoint`** (the cross-site
  duplicate the dropped bypass would have admitted) + `site.subnet_approval_refused`. NEGATIVE: the
  refused Branch row **STAYS `pending`** (design confirmed — re-approvable once the conflict clears).
- **Leg 1 — accept-half LIVE, refuse-half NAMED SUBSTITUTE.** Revived the enroll-looping agent (stale
  join token, not S8.1/not infra); demo-gw at `MaxSupported=4` applied the v4 desired-state → healthy
  (go-forward accept, on the wire). Pinning `MaxSupported=3` left it healthy because demo-gw is in NO
  enforcing config, so it receives no compiled artifact to gate (`policy_surface_test.go:31` — only
  ENFORCING nodes get a pushed policy; `SetPolicy` `p!=nil && p.Version>max` correctly skips). Live
  refuse needs enterprise enforcement stood up for a live gateway (separate walk, exceeds S8.1 budget);
  refuse-half unit-pinned both editions (`TestInterlockOldMaxAgentRefusesSitesBump`,
  `TestUnsupportedPolicyVersionSurfaces` + `policyhealth_test` S8.1 rows). **Owed live-refuse trigger
  (PINNED):** S8.2's box-walk — S8.2 stands up cross-site route propagation on the wire, the point at
  which a live gateway carries a compiled v4 enforcement artifact, so the real refuse rides that walk.
  SUBSTITUTES ≠ SATISFIES.
