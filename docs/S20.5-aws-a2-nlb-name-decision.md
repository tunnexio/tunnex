# A2 replacement fixture NLB authorization

2026-09-05: the fresh C5 A2 gateway passed runtime CNI admission, but the
load-balancer controller refused CreateLoadBalancer because its narrow IAM name
allowlist includes A and B, not the replacement A2 fixture selected for this walk.

Disposition: add only `tunnex-s205-a2` alongside existing A/B resource ARNs in
the five existing load-balancer/listener statements. Keep all actions, region,
account, cluster tags, subnet and security-group conditions unchanged. Apply
through the existing controller CloudFormation stack, only after a changeset
shows the controller role policy modification without replacement. Let normal
controller reconciliation resume; no Service patch or rollout restart.

This is sandbox prerequisite repair, not additional product behavior or proof
of successful installation. Retain A permissions for existing fixture cleanup.
