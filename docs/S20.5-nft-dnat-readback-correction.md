# S20.5: IPv4 DNAT printer spelling and ownership receipts

Status: proposed bounded correction to a live C6 refusal; not live acceptance.

The AWS two-backend Service produced a valid nft rule, but its live listing
prints `dnat ip to jhash ...` where the emitter used `dnat to jhash ...`.
The ownership verifier currently rejects that spelling. Simply broadening
the regex is insufficient: hashing the printed text still differs from the
receipt computed over the emitted text.

Correction under the approved bug-fix scope: recognize only the exact IPv4
DNAT family qualifier, validate the entire normalized rule with the existing
strict IPv4/protocol/port/map grammar, then recompute the digest using the
original emitter spelling. Apply this same canonical representation when
creating receipts. Preserve every address, port, backend, map index/count,
hash expression and other token. IPv6, unknown grammar, altered targets,
trailing actions, and copied comments remain refusals.

No app replica reduction, manual nft repair, trusted-comment shortcut, or
weakened full-domain readback. The existing single/two-backend tests must gain
typed printer fixtures and negative tamper cases. Add an opt-in isolated
network-namespace kernel round-trip test so emission, nft listing, and receipt
validation are exercised together. Unit green does not replace AWS re-proof.

Independent review and a new immutable candidate are required before calling
this correction deployed. C6 cleared the duplicate-peer blocker but remains
HA-incomplete at this new DNAT verification boundary.

Review disposition (2026-09-06): user approved folding the P2 kernel-test
coverage correction. Use resolvedVIP fixtures through the real dnatRule
emitter and requested-receipt path, a fresh isolated `table ip tunnex`, and
plain `nft list table ip tunnex` as production does. Retain the explicit
typed-printer unit fixtures; rerun the kernel proof and independent review.

The user additionally overrode routine repository approval pauses for essential
work in this continuation. Essential corrections and sandbox retests may proceed;
this does not waive verification, preservation, or fresh final merge approval.
