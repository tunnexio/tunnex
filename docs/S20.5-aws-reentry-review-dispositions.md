# S20.5 AWS re-entry — current review dispositions

Status: **ranked remaining findings are HELD; final story review is incomplete.**
This record supplements, never replaces, the original 48-finding review. The
fresh user authorization permits confirmed S20.5 blocker fixes within locked
contracts. Under the repository's decision-first rule, new runtime-auth,
test-isolation, or recovery/serving-authority choices are surfaced for explicit
disposition before implementation. The node,
client, API, CLI, and chart reviews below are bounded slice reviews, not a
claim that the full 65,000-line integration has earned a clean final review.

## Authorized fixes and proof boundaries

| Original finding | Current disposition |
|---|---|
| 1, 5 | Fixed: durable restoring intent and three-way tunnel-CIDR observation. Real primitive/crash/retry tests; full node gate passed. S20.6 active-missing-link refusal remains. |
| 2 | Fixed: remint requires both Kubernetes management and existing gateway-credential authority before lookup/body processing. Owner/Admin positive and Operator no-oracle tests passed. |
| 3 | Fixed within D14/D14o: visible, side-effect-free foreign-owner refusal, no false removal success, original-owner recovery guidance. This does NOT allow B to enroll independently while A's origin slot is occupied. Full native client and web gates passed. |
| 9, 32 | Fixed for offline evidence: actual preflight lookup loop and owner/namespace/newer-generation refusals execute against a GET-only local API fixture. Live matrix remains required. |
| 10 | Fixed for offline evidence: real Helm/Go packaging, canonical archive bytes, decoded content/ELF checks, four-chart content/render parity. Actual final-source images/bundle and live versions remain required. |
| 12 | Fixed for CLI reuse: canonical PVC organization/claim equals selected membership and CP consumed UUID binding; re-proved after approval, during takeover, and after Helm. Full CLI gate passed. |
| 14 | Fixed: immutable consumed UUID/org/claim proof and serialized status read; complete node/migration packages pass both editions. No migration rewrite or weakening of initial name pins. Overall API suites remain red on separate failures below. |
| 16 | Fixed: prior ordinary base-authority read errors now stop authority issuance. Negative real-DB read witnesses, valid/replay paths, and both complete API editions passed before finding 14. |
| 21 | Partially fixed in CLI enroll retries. Direct Helm live-PVC adoption guard remains OPEN; do not count CLI proof as a complete chart fix. |

The prior Windows failure was a platform-mismatched test fixture, not evidence
authorizing S20.5.1 automatic terminal recovery. Five fixtures now use the actual
host platform; native tests pass and exact Windows runner proof is still needed.
Latest-main migration numbering, generated interfaces, flow-log configuration,
UI labels, and separated desktop release behavior were retained during import.

## Ranked remaining decisions — HOLD before further folds

**Additional ranked gate findings — HELD:**

- **G1 — P1:** runtime credential promotion is row-order-dependent against the
  immediate one-current unique index. A deterministic temporary-table witness
  and the original open-edition server log independently confirm the failure.
- **G2 — P2:** the enterprise bootstrap fixture reuses a global fixed token hash;
  organization cleanup fails when its audit FK invokes a forbidden UPDATE, and
  the test ignores the cleanup error. The failing session logs both events.

The relevant mechanisms are unchanged from exact main baseline `0f3d9b0`;
main itself was not separately rerun. See the
[diagnosis and proposed repair boundaries](S20.5-baseline-gate-failures.md).
Production runtime authentication and test-fixture isolation are additional
surfaces; do not silently fold them into the Kubernetes repair. Obtain explicit
disposition before changing them; do not retry until lucky green, bypass tests,
or weaken audit immutability.

1. **P1 — findings 6, 7, 29: startup and manager timing authority.** A working
   manager can publish stale heartbeat evidence, and an initial desired-state
   GET/ACK transport failure can terminate the node and drain its data plane.
   The proposed fix separates process progress from owner admission, bounds
   work, and establishes lease expiry before startup retries. It changes
   accepted timing/probe behavior and adds a startup state transition. Review
   and disposition the exact M1/M2/M3/S1/S2 choices in
   `S20.5-node-timing-and-startup-decision-proposal.md`; none is implemented.
2. **P1 policy boundary — finding 4: serving readiness during API uncertainty.**
   Endpoint reportability/readiness is withdrawn today. Preserving old endpoint
   bytes alone would not preserve readiness or traffic. Recommend retaining
   current fail-closed behavior until an explicit error-class/grace-period
   policy is selected (E1 in the proposal). Do not promise outage continuity.
3. **P1/P2 policy boundary — findings 11/22 and 8: CRD provenance and rollback.**
   Real fixtures prove generation-marked current/legacy owners bypass the
   ownerless fingerprint gate. D8 explicitly gives these owners different
   authority but does not fully settle drift after uninstall/manual changes.
   Decide whether to retain marker authority or require catalogued schema
   fingerprints for those adoption/re-install states. CRD Helm rollback remains
   explicitly unsupported; an admission/rollback engine is not a routine fold.
4. **P1/P2 unfinished review — findings 13, 19, 21, 25 and other original rows.**
   Retry deadline versus authority loss, hidden pool members, direct-Helm PVC
   relabelling, and stranded uninstall metadata require complete current-source
   re-verification/disposition before qualification. These are not declared
   fixed or silently deferred. Remaining original rows retain their ranked
   HELD status unless explicitly corrected below.

## Corrections to overbroad findings (no product changes)

- **31:** early watch EOF withdrawal is directly covered by
  `TestEarlyCleanWatchEOFRetractsLiveSnapshot`. S10.3 WF-K5 explicitly requires
  withdrawal on connection loss to avoid stale Pod-IP authority. Reject the
  missing-coverage claim; relaxing the policy requires a new decision.
- **30:** finite network-preparation refusal reasons exist in logs as D4
  requires. A typed CP field plus visible consumer is a useful product-surface
  extension, not an already-promised D4 behavior or a CNI qualification fix.
- **17:** the claimed suspended human-desktop path is not established: desktop
  creation is human, while suspension SQL requires agent kind; human capacity
  counts active/pending. Preserve deliberate terminal/ambiguous evidence rules.
  The narrower overpromising confirmation-copy issue remains held.
- **18, 36, 37, 38:** ownership/membership refusal and retention of unreadable or
  old-origin encrypted state are locked behavior. Misleading remediation is
  real UX debt; salvage, membership-independent clear, and cross-origin storage
  management are new policy. The original account/origin can still recover
  when its membership and credential proofs permit it; do not claim the data
  has no possible recovery path or silently erase it.
- **25:** absence of the D12 upgrade/rollback provenance gate does not alone
  prove uninstall violates D12. Its separate stranded-metadata behavior still
  needs review against D13g and is not rejected with the overbroad premise.

## Acceptance is still blocked

No row above satisfies the live EKS/desktop walk. Required exact-final CI,
final multi-finder review with user dispositions, AWS resource/cost/identity
verification, patch-free live legs, redacted committed evidence, real draft PR,
final PLAN pointer, and matched-head merge remain outstanding. S20.5.1,
S20.5.2 and S20.6 are unchanged, and no cleanup or deployment is implied.
