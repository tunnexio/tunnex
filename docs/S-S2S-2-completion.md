# S2S-2 completion contract

The current user request is to finish S2S-2 before stopping; intermediate helpers do not complete the story. All work stays local. S2S-3 advanced rotation/recovery and S2S-4 real AWS customer acceptance remain separate, but safe minimum enable/disable/delete and no-cleartext fallback are required for this slice.

## Required exit evidence

- Supported, patched Linux IPsec daemon/client choice recorded and packaged with required plugins/binaries and applicable notices; no insecure fallback or adoption of a foreign daemon.
- Authenticated assigned-node delivery marks exact revision potentially delivered before exposing credentials; stale/revoked/cross-org delivery refuses. Credentials absent from logs, public APIs, files, arguments and environment.
- Revision-checked enable/disable/delete state and precise cleanup acknowledgements; concurrency/rollback tests; unresolved retained-guard disposition settled before new schema implementation.
- Node serializes enforcement, apply and cleanup; exact ownership journal/readback; fail-closed on missing evidence or startup failures. Dedicated runtime stage returns errors, never converts logged policy failure into success.
- Existing shared policy remains authoritative. Remote ranges do not automatically become allow grants. Exact assigned tunnel identity anchors ingress and anti-spoofing; forwarding, OUTPUT and established-flow revocation cannot bypass refusal.
- Two configured tunnels are independently observed. Initial selected path obeys explicit policy; S2S-3 measured failover/rekey extensions cannot be falsely counted as completed here.
- Real isolated Linux encrypted payload succeeds only under an explicit allow rule. Deliberate deny, wrong PSK, missing return route, SA/interface/route/daemon loss and revocation preserve denial and no cleartext fallback. Preserve unrelated WireGuard/host objects. Unit tests and kernel-object readback alone do not satisfy this gate.
- Capability1 is reported only by a runtime that passed required probes. Unsupported versions remain refused; current management UI exposes activation only against qualified capability and exact state.
- API editions, node build/tests, generated clients, relevant frontend tests/build and independent review pass for integrated code. Local CP upgraded only after isolated migration gates and owned project checks; no cloud provisioning or publication.

## Current lanes

Engine/daemon qualification, delivery/state contract and enforcement/packet contract proceed in parallel. The user approved retained safety guards and range reservations on 2026-09-24; implementation follows `S-S2S-2-cleanup-disposition.md`. Lifecycle, authenticated delivery and serialized node controller work now proceed in parallel. No completion claim until every applicable exit row above has actual evidence.

## Local evidence checkpoint, 2026-09-24

Completed primitive evidence (not end-to-end story completion): bounded VICI transport and typed dedicated-daemon adapter; real strongSwan6.1.0 startup with required algorithms; exact kernel XFRM/interface/route inventory parsing; strict firewall rendering/readback and kernel-limited permit sets; in-memory nonce-bound lease calculation; explicit assigned-gateway policy projection. Native host-origin encrypted traffic traversed both manually selected paths with ESP capture, route loss refused with zero plaintext capture, and targeted daemon cleanup preserved the other tunnel before full removal. Native DNAT regression proved a translated baseline reachable and then denied by original-tuple protection. Forwarded TCP/UDP, both manually selected paths, normal permit expiry after applier exit, established TCP revocation, missing return route, interface-down, exact outbound-SA loss, wrong PSKs, and daemon loss followed by kernel lease expiry passed in isolated Linux fixtures. This proves the tested primitives, not an integrated persistent controller.

Implemented and integrated locally: migration160 lifecycle/delivery ownership; migration161 disposable tunnel observations; revision-checked human intent API and generated clients; authenticated agent material/cleanup/acknowledgement/lease/status endpoints; authoritative transaction-scoped policy projection; serialized node controller and nonsecret journal; bounded mTLS polling; default-off startup and offline refusal restoration; actual platform probes and typed capability reporting. Isolated database tests prove commit-before-first-response-byte, broken-response cleanup obligation, material/disable/revoke serialization, lease/policy-writer serialization and report freshness/identity. The missing-journal cleanup path uses refusal plus independent absence proof, never foreign-object adoption.

NativeARM64 integrated acceptance passed on final candidate `sha256:f198e0e42f53e507b00ebf3e2450c65dc7dc2a2278b3b8beec3386841bd4fe90`: real encrypted TCP/UDP, SA-preserving refresh, two independent tunnel statuses, restart refusal, exact cleanup, lost-response absence-only cleanup, and simultaneous WireGuard/OpenVPN payload preservation. `/private/tmp/s2s-runtime-final-evidence.json` binds image, test binary, controller packet log and coexistence log. CP lease responses in the packet fixture are synthetic; real authenticated API/store proofs remain separate from customer AWS acceptance (S2S-4).

Remaining story blocker: nativeAMD64 qualification. The repository release matrix includes AMD64 and ARM64, while available local Docker engines are ARM64. NativeAMD64 package/packet/compatibility execution still needs an authorized runner before the complete architecture matrix can be claimed. Neither cross-compilation nor emulation is substituted for native proof. The candidate now admits LinuxARM64 and LinuxAMD64 only after identical live qualification probes; native AMD64 release acceptance remains pending the authorized GitHub Actions run. This is an explicit temporary readiness restriction, not a change to the intended product matrix. Advanced failover/rekey and real AWS acceptance remain S2S-3/4.

The runtime guarantee is bounded permit expiry on a running kernel; no wall-clock expiry guarantee across arbitrary host suspend or hypervisor pause is asserted. Resume detection invalidates a probe receipt but does not prove no packet ran before userspace resumed. Local CP was backed up and additively upgraded159→161 after isolated gates, preserving existing inventory and seeding no fake runtime state. Feature-branch publication and the native AMD64 GitHub Actions run are now user-authorized; merge and deployment are outside this work. S2S-2 remains incomplete until its AMD64 qualification gap is resolved.

Latest local gates: node/control races9.846s/8.204s; DB161/status/authwalk races4.099s/10.551s; both API editions, generated contracts and LinuxARM64 node build pass. Full frontend1615tests/136files passed; final focused28tests include two additional malformed-response regressions, followed by a successful production build. Browser DOM/screenshot verified the consistent theme and two tunnel badges in a clearly labelled synthetic preview; the live local CP inventory remains empty. Current production build-input hashes match the candidate.

Latest scoped local gates: HTTP/authwalk/private-delivery race PASS12.087s and policy race PASS2.527s (`/private/tmp/s2s-runtime-final-authwalk.log`); material-vs-disable/revoke observed-lock races PASS5.901s (`/private/tmp/s2s-runtime-material-races.log`); startup discovery tests PASS1.691s; both API editions, generated CLI compilation, and Linux ARM64 node build pass. These are scoped source/service gates, not native runtime readiness.

Legacy VPN compatibility evidence: `/private/tmp/s2s-vpn-compat-evidence-pnicvrgk/result.json`. The exact candidate image passed WireGuard handshake/payload, OpenVPN mutual TLS/payload, concurrent WireGuard/OpenVPN payload, and interface/daemon-loss refusal controls. This fixture did not run IPsec concurrently. Owned fixture containers/networks and synthetic credentials were removed.


Final source regression checkpoint: `apps/node/internal/ipsec` race PASS4.023s, `apps/node/internal/control` race PASS7.344s, `apps/api/internal/policy` race PASS2.131s. Evidence `/private/tmp/s2s-runtime-final-unit.log` and `/private/tmp/s2s-policy-final-unit.log`. Candidate image `sha256:7e29f9e72a30513b486afd68aca925c757d436081c1bfe0808db248033a949ef` passed the exact-image forwarded packet suite; executed-harness/binary/log hashes are in `/private/tmp/s2s-engine-final-image-evidence.json`. No API/DB lifecycle mutation is implied by these isolated tests.
