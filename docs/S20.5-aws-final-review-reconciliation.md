# S20.5 AWS repair — final review reconciliation

2026-09-06; product `e6a56a4`, base `origin/main` `0f3d9b0`.
Status: **two-finder bounded reconciliation complete; original unresolved
findings remain HELD. Not a clean review of every file or merge clearance.**

The user explicitly narrowed acceptance to broken/changed behavior and retained
valid proof. Do not turn the historical eleven-leg runsheet into a fresh blocker
or restart already-reviewed slices. This does not silently disposition existing
product findings, change security contracts or qualify unrun recovery behavior.

## Reusable review and current correction

- Lifecycle identity/remint, retained CLI state, cleanup and packaging reviews:
  [recorded dispositions](S20.5-aws-reentry-review-dispositions.md).
- AWS CNI/schema-4/startup/cleanup source and packet proof at `dc70c9b` remain
  applicable: node production and charts are unchanged by subsequent API fixes.
- Credential/health correction `48f96b0` has independent bounded review, full
  both-edition API gates and successful exact-source CI.
- HA correction `e6a56a4` has two independent bounded reviews with no concrete
  P1/P2 found. Real PostgreSQL red/green and safety controls, HTTP exact-tuple
  tests, both-edition builds and the full ownershiplease race suite pass.
- Second finder rechecked credential transactions, remint authorization,
  immutable lifecycle binding and constructor wiring; no new P1/P2 found.

The complete branch includes roughly 412 changed files against verified
`origin/main`. A stale local `main` comparison initially overstated that count;
it is not the review base. Bounded reviews above do not imply full clean coverage.

## Ranked surviving findings / disposition gaps — HELD

1. **High, original #21: direct-Helm retained-PVC provenance.** The reviewed CLI
   guard does not establish a live-PVC adoption guard for direct Helm. Preserve
   the explicit remainder in [retained-state review](S20.5-retained-state-review-fix.md).
   This is not a request to repeat the successful CLI AWS lifecycle now.
2. **P2, original #19: pool editor can silently omit a configured standby.**
   `apps/web/src/components/K8sConnectorPoolPanel.tsx` builds its draft from
   eligible node inventory, not the complete `configuration.members`. A member
   missing from that inventory, inactive, or without an endpoint can disappear
   from an unrelated priority-edit PUT; the server treats the submitted list
   as the desired membership and removes omitted non-owner members. An offline
   node that remains active and has an endpoint is not by itself this trigger.
   No change is folded here. Explicit fix/defer disposition is needed before merge.
3. **Original lifecycle review gaps:** retry deadline versus heartbeat authority,
   stranded uninstall metadata, rollback/purge truthfulness and remaining original
   CLI/Helm/CRD rows retain their recorded ranks. They are not newly discovered
   regressions and not declared fixed through the HA patch. Reconcile their
   actual locked contracts before choosing a fix or follow-up scope.

Manager timing/general startup transport recovery, broad CRD adoption/drift,
and unrun rollback/posture cases remain explicit qualification limitations.
Do not silently implement the larger manager/startup proposal, waive fail-closed
behavior or reopen named S20.5.1/S20.5.2/S20.6 deferrals to complete this repair.
Desktop foreign-owner coverage includes source-wiring assertions rather than a
full Electron IPC integration run; preserve that distinction.

## PR boundary

Complete the current bounded AWS HA sequence and attach truthful source/digest
and outage evidence before opening the draft PR. The draft may expose these
remaining dispositions; it must not call the whole story merge-ready. Finish
non-pointer content and dispositions before writing PLAN's final PR/content-tip
checkpoint. Required checks must be green on the actual final PR head, followed
by fresh explicit matched-head merge approval. No public release qualification
is inferred from private candidate publication or mixed-component testing.
