# S8.5 — open-edition routed subnets (split-tunnel push routes) — commit-one (decision-first paper)

**Status:** PAPER — dispositions PROPOSED, HELD for founder ruling. Nothing built until ruled.
**Branch:** `story/S8.5-routed-subnets`. **Sequencing:** S8.4 (MERGED) → **S8.5** → S8.6 hub-HA → S8.7 → epic-close walk. EPIC 8 story 6 of 8.
**Founder rationale:** Pritunl push-routes parity — the migration wedge's table-stakes config.

---

## Re-entry survey (cited, not assumed) — what remains genuinely S8.5's

The registration warned S8.5 SHRINKS by what S8.2c absorbed + what the S8.4 coupling reserved. The survey confirms a LARGE shrink — most of the data-plane already exists:

- **(a) enterprise — do granted resource-CIDRs reach device `AllowedIPs` today?** NO. `allowedIPsFor(fullTunnel, poolCIDR)` (`devices/service.go:265`) returns `[poolCIDR]` (split) or `[0.0.0.0/0, ::/0]` (full). No resource/site CIDR path.
- **(b) open — device-reachable gateway-LAN routes today?** MISSING. Devices get only the pool.
- **Device peering:** a device peers with ONE node (`NodeID`, `devices/service.go:77` "the gateway the peer connects through"). So device→remote-LAN rides device → its-gateway → (S8.2 site link) → far-gateway → LAN.
- **Gateway data-plane already EXISTS (the shrink):**
  - **NAT:** the S3.7 masquerade `ip saddr <pool> oifname != "wg0" masquerade` (`egress_linux.go:289`) already SNATs the device pool out ANY non-tunnel iface — including the LAN iface. **Device→LAN return-path NAT is already there.**
  - **Forward:** mesh `iifname "wg0" oifname != "wg0" accept` (`egress_linux.go:360`) already forwards wg→LAN; enforcing gates it via grants (`allowMatch`).
  - **Site→site transit** (S8.2 Routes + WF-4 DOCKER-USER accept) already carries a device's remote-LAN packet across the hub when the LAN is on another gateway.
- **Disjointness validator already has THREE classes** (`subnetguard.go:18-20`: `site_subnet` / `pool` / `reserved`) — the registration's "third input class" is ALREADY SATISFIED if routed ranges are modeled as site subnets.
- **The S8.4 abstraction** (`docs/S8.4-decisions.md` coupling clause): a resolvable internal zone is *"a reachable range with an optional resolver,"* keyed to the range, never a device/node. **A declared LAN is that same range** — it plugs into the SAME `dns_forwarding` table + the SAME device-resolver handoff, no redo.

**CONCLUSION — what is genuinely S8.5's (the un-absorbed core):**
1. **Push declared LAN ranges into split-tunnel device `AllowedIPs`** (the one missing data-plane piece — devices currently never route a LAN).
2. **The device-side route handoff** = the S8.4b/crash-sweep trigger + the client resolver path going live.
3. **The model + edition-gating** (open = push to all; enterprise = grant-gated destination — one mechanism).
4. **Version/hash + device-config-blast-radius classification** (argued, not assumed).
5. The carried riders (L1 metrics, WF-1, S8.4b NRPT, crash-sweep with its ordering precondition, stale-add-rule-button).

---

## Scope discipline — the SHRINK stated explicitly

**S8.5 does NOT rebuild:** the container→host routing / host-networking (S8.2c D3/D4), the emitted-install-command shape (S8.2c D4), the device→LAN masquerade (S3.7), the wg→LAN forward + grant enforcement (S3.7/S8.2), the site-to-site transit (S8.2), the disjointness validator (S8.1), or the DNS-forward range abstraction (S8.4). **S8.5 BUILDS:** the device `AllowedIPs` push of declared ranges + the model/edition-gating + the classification + the device-config push blast-radius handling + the un-gated client resolver path (with its ordering precondition) + the riders. **No new NAT, no new forward mechanism, no new disjointness class** (per D1 reuse).

---

## Decide-items (proposed dispositions, HELD for ruling)

### D1 — The core model: a routed range REUSES the site approved-subnet (the coupling's "sibling"), consumed by DEVICES
**Proposed:** a "routed subnet" is NOT a new table — it is a **site's APPROVED subnet** (S8.1), now compiled into **device `AllowedIPs`** (not only gateway Routes). One model, one disjointness class, one DNS abstraction. Edition-gates the DEPTH: **open/off** → every device gets every approved subnet in `AllowedIPs` (the Pritunl push-route, mesh-reachable); **enterprise/enforcing** → the range is a policy DESTINATION, device→range gated by a grant (the S8.2 "routed ≠ permitted" red already holds). **One mechanism, edition-gated depth** — exactly the registration's phrasing.
- **Why reuse, not a parallel `routed_subnets` table:** the site subnet is ALREADY the "reachable range fronted by a gateway," already in `subnetguard` (ClassSiteSubnet), already the DNS-forward range. A parallel table would add a 4th disjointness class + a second model of the same concept + divorce it from the S8.4 DNS coupling — the dormant-machinery/one-truth smell.
- **Alternative (noted, proposed-rejected):** a lightweight per-gateway `routed_subnets` table for a Pritunl-simple "just add a CIDR" flow without the register-site→bind→advertise→approve ceremony. REJECTED because it forks the model; the ceremony is the S8.1 all-editions-core price, and a "declare a LAN" is genuinely "advertise + approve a site subnet."
- **Open-edition site ceremony concern:** the site model is all-editions core (D11), so open orgs already have sites/gateways/subnets. "Declare a LAN to push" = advertise + approve a site subnet. If the ceremony proves too heavy for open-edition parity, a **thin UI affordance** ("Advertise a routed LAN") over the same tables is the answer — NOT a second model. HELD.
- **S8.4 coupling honored:** the range carries its optional resolver (S8.4 `dns_forwarding`); when a device gains a route to a range that declares a resolver, the client resolver handoff fires (D4 below) — "site today, S8.5 LAN tomorrow, same code."

### D2 — Disjointness: NO new class (routed ranges ARE site subnets)
**Proposed:** because D1 reuses site subnets, a routed range is already validated by `subnetguard.Check` as `ClassSiteSubnet` against pool + reserved + other site subnets. The registration's "third input class" is **already satisfied** (the validator has had 3 classes since S8.1). **No new validator input, no new typed refusal** — the existing `subnet_not_disjoint` covers it. If D1's alternative (separate table) were chosen instead, THEN a 4th class would be owed; reuse avoids it.

### D3 — NAT / return-path: SNAT (pool→gateway-LAN-addr), stated explicitly; existing S3.7 rule covers it
**Proposed:** device→routed-LAN traffic is **source-NAT'd** at the fronting gateway (device-pool source → the gateway's LAN address) so a LAN host replies to the gateway and needs **ZERO route back to the device pool** — the open-edition/Pritunl expectation (the LAN admin adds nothing). This is ALREADY the S3.7 masquerade `ip saddr <pool> oifname != "wg0" masquerade` (`egress_linux.go:289`), which SNATs the pool out any non-tunnel iface. **State it explicitly in the paper as the chosen return-path model.**
- **Alternative (noted, proposed-rejected for open):** documented static route on the LAN (no NAT, preserves the real device source IP). REJECTED as the DEFAULT — it demands the customer's LAN/router add a route to the pool (breaks the zero-touch/Pritunl expectation). RESERVED as a future per-gateway "no-NAT + documented route" toggle IF a customer needs source-IP preservation (trigger = first real need); silent-ambiguity forbidden.
- **Cross-gateway case:** when the LAN is on a DIFFERENT gateway than the device's peer, the packet rides the S8.2 site link (wg0→wg0, not masqueraded — spoke↔spoke stays un-NAT'd), and the FRONTING gateway SNATs to its LAN. Stated so the two-hop NAT is unambiguous.

### D4 — The device→site route handoff (the S8.4b / crash-sweep TRIGGER — the ordering precondition)
**Proposed:** split-tunnel device `AllowedIPs` gains the org's declared routed ranges → the device routes them via its gateway. **This is the named trigger** for two deferred S8.4 items:
- **The client resolver path goes LIVE** — `config.dns_forwards` (empty in S8.4) is now populated for a device that has a route to a range declaring a resolver (the S8.4 D3 handoff). The macOS `set_resolvers` install path activates; S8.4b Windows NRPT lands here (its own slice, own review, the kill-switch/revocation-untouched probe inherited).
- **THE ORDERING PRECONDITION (binding, from the S8.4 round-3 reduce-by-removal):** the **crash/owner-loss resolver sweep MUST land BEFORE the first `set_resolvers` install path activates** in this story. A resolver file must never be able to exist without its teardown mechanism present. **Slice ordering: the crash-sweep slice precedes the resolver-handoff slice.** The crash-sweep is built exercisable + red-able + walk-provable (a crash-mid-session leg), NOT dormant lifecycle code (the dormant-machinery law). See `docs/laws.md` shared-territory #4.
- **Which resolver does a device use for a range?** the range's declared resolver (S8.4), reachable via the device's route to that range. Emitted only when the device HAS the route (split-tunnel includes it) — "a device with a route to a range that declares a resolver."

### D5 — Version / hash classification (argued per the D2-checklist row, NOT assumed)
**Proposed:** two distinct artifacts, classified separately:
- **The GATEWAY `policyspec.Compiled` artifact:** a routed range = a site subnet → it already appears as a `Route` (out-of-hash plumbing, S8.2) and, under enforcing, as a grant (`AllowEntry`, in-hash). **S8.5 adds NO new gateway-artifact field** → **NO new `ProtocolVersion` bump for the gateway artifact.** The enforcement significance (device→range permitted) is already carried by the existing grant path; the routing is already the out-of-hash `Routes`.
- **The DEVICE config (WG peer `AllowedIPs`):** this is device-config (control-plane→client), NOT the gateway `Compiled` artifact — it does not touch `CanonicalHash`. BUT the **device-config blast radius is FLEET-WIDE**: declaring/removing a routed range changes EVERY device's `AllowedIPs` → every device must re-fetch + re-apply its config. Stated: a routed-range change triggers a **device-config push to all devices in the org** (the S3.7 push path), the same way a pool resize does. **Enforcement-adjacent but hash-neutral** — it changes what a device ROUTES, gated (enforcing) by the unchanged grant path.
- **Argue-don't-assume result:** classified **hash-neutral / no version bump**, because the enforcement-significant fact (permission) rides the existing grant, and the routing rides the existing out-of-hash `Routes` + the device `AllowedIPs` (never hashed). The NEW behavior is a device-config recompute+push, not a new wire field. **VERIFY at build:** a twin-golden that a routed-range declaration leaves the gateway `CanonicalHash` unchanged (no false-desync on an enforcing gateway), mirroring the S8.4 `DNSForwards` hash-blind test.

### D6 — L1 metrics rider (WF-1 + the S8.3-close metrics L1): site-link bytes + handshake age on the topology cards
**Proposed:** the S8.3-close "Metrics L1" + the S8.2c WF-1 "positive site-link health" land here as a rider. The agent already parses `wg show dump` (S3.6) — extend the report path with per-site-link tx/rx byte counters + last-handshake age; the Sites topology card renders them. **RENDER-FLOOR bound (the law):** cumulative-since-handshake TOTALS labeled EXACTLY as that — NO rate graphs, NO sampling implied (that is S11.1). **WF-1 positive health:** a site link with a fresh handshake + moving bytes reads POSITIVELY (not merely "not-degraded") — the honest inverse of `site_link_down`. HELD.

### D7 — Carried riders (registered, fold into this story's build)
- **S8.4b — Windows NRPT domain-scoped resolvers** (own slice, own review; the kill-switch/revocation-untouched probe inherited; the S8.5 walk gains the Windows name-resolution leg). Triggered by D4 (client resolver path live).
- **Stale Add-rule button after group-add** (S8.3 walk-found; lift `groups` to the parent / reload RulesSection on group-add) — rides here or the epic-close fold, whichever first.

### D8 — Enforcement-vs-observability + fail-closed gate (inherited discipline)
**Proposed:** no new enforcement-significant wire field (D5), so the S8.1 `ProtocolVersion` fail-closed gate is NOT re-triggered. Every new field (metrics counters) is classified observability → OUT of `CanonicalHash` (S7.5.1 A-1). The "routed ≠ permitted" red (S8.2) is RE-ASSERTED: under enforcing, a pushed routed range is ROUTED into device `AllowedIPs` but DROPPED at the gateway forward chain without a grant — routing is not permission.

---

## Walk shape (defined BEFORE code)

Cross-cloud, split-tunnel device + declared LANs + ZT layering. Inherits the behind-hub leg (hub-forwarding verify) + the Windows NRPT leg (S8.4b).
1. **Pritunl-parity (the money leg):** a **split-tunnel** device gets a declared LAN range in its `AllowedIPs`, reaches a host on that LAN **via the gateway**, and **everything else routes DIRECT** (not through the tunnel) — the split-tunnel proof.
2. **Routed ≠ permitted (enforcing red):** same device, ZT enforcing, 0 grants → the range is in `AllowedIPs` (routed) but the gateway forward chain DROPS it; add a grant → it flows. Routing ≠ permission, on live iron.
3. **Cross-gateway routed range:** the declared LAN is on a DIFFERENT gateway than the device's peer → the packet rides the S8.2 site link + the fronting gateway's SNAT. Two-hop, un-NAT'd inner + SNAT at the front.
4. **Device→site DNS goes live (the D4 handoff):** the declared range carries a resolver → the split-tunnel device resolves that range's names (S8.4 client path, now non-empty) — macOS `/etc/resolver` + **the S8.4b Windows NRPT leg**.
5. **THE CRASH-SWEEP ORDERING LEG (the precondition proof):** device up with resolvers installed → **crash/force-quit the client** → the owned resolver files are swept (the crash-sweep that landed BEFORE the resolver path, per D4) — no stale `/etc/resolver` residue pointing at a dead tunnel. Both macOS + Windows.
6. **L1 metrics render:** the Sites topology card shows site-link tx/rx totals + handshake age (WF-1 positive health) — render-floor (totals, not rates).
7. **Blast-radius:** declare a new routed range → EVERY device re-fetches + gains the route within the push interval (fleet-wide device-config push).

---

## Slice plan (sketch, ordered by the D4 precondition)

1. **Slice 1 — the crash/owner-loss resolver sweep** (the ordering precondition — MUST precede the resolver-handoff): built exercisable + red + walk-provable (crash-mid-session leg). Its own review attention (helper/privileged surface).
2. **Slice 2 — device `AllowedIPs` push** (D1/D3/D5): compile approved site subnets into split-tunnel device `AllowedIPs` (open = all; enforcing = the grant already gates the forward); the fleet-wide device-config push; the hash-blind twin-golden (D5 VERIFY). Reds: split-tunnel AllowedIPs gains the range, full-tunnel unchanged, routed≠permitted under enforcing, hash-unchanged.
3. **Slice 3 — the client resolver handoff goes live** (D4): `config.dns_forwards` populated for a device with a route to a resolver-declaring range; macOS install path active.
4. **Slice 4 — S8.4b Windows NRPT** (own slice, own review, inherited probe).
5. **Slice 5 — L1 metrics + WF-1 positive health** (D6) + the stale-add-rule-button fix (D7).
6. Story-end review → the cross-cloud walk (7 legs above; Pawan drives).

---

**Build order:** ruling on D1-D8 → slice plan ratified → build Slice 1 (the crash-sweep precondition FIRST). Nothing built until ruled.

## RULINGS (founder, 2026-07-19) — all 8 CONFIRMED, folded

- **D1 CONFIRMED — reuse site subnets, one model, WITH a UX condition (hedge promoted to requirement).** The open-edition path MUST compress to ONE SCREEN: a gateway card (or empty-Sites affordance) "route a LAN through this gateway" that does site+bind+advertise+approve in a SINGLE action for the solo-admin case (self-approval theater there — they own everything). Same model underneath, thin affordance on top. Full ceremony stays for multi-admin enterprise. **Red: the one-screen path yields the IDENTICAL DB state the long path does.**
- **D2 CONFIRMED — the "already satisfied since S8.1" answer is the registration's third-input clause DISCHARGING ITSELF.** Note as DISCHARGED, not merely absent.
- **D3 CONFIRMED — SNAT default; no-NAT toggle reserved with its named trigger** (first customer needing source-IP preservation; the un-NAT'd site-to-site proves the machinery exists — it's a per-gateway customer decision).
- **D4 CONFIRMED — ordering precondition BINDING: crash-sweep is Slice 1, before any resolver install exists.** The dormant-machinery law's FIRST scheduled compliance (the sweep lands in the story where it's exercisable, as the deferral promised). The walk's crash-sweep leg is that promise's proof.
- **D5 CONFIRMED — hash-neutral, no bump; twin-golden is the mechanization. ADDITION: the verify covers BOTH sides.** Gateway artifact `CanonicalHash` unchanged by a routed-range declaration AND — the S8.2 v4-byte-identical precedent applied to the device side — **a no-declared-ranges org's device configs stay BYTE-IDENTICAL** (no accidental fleet churn from the feature merely existing). Two assertions.
- **D6 CONFIRMED** (totals not rates, render-floor). **D7 CONFIRMED** (NRPT own slice + inherited probe; stale-button fix lands). **D8 CONFIRMED** — "routed ≠ permitted" re-asserted with the walk red = the epic's thesis sentence getting its open-edition verse.
- **Slice order APPROVED as cut** (crash-sweep → AllowedIPs push → resolver handoff → NRPT → metrics+fix → review → walk). The 7 walk legs stand + inherit the behind-hub quadrant leg (standing registration).

**Build proceeds: Slice 1 = the crash/owner-loss resolver sweep (the D4 precondition), built exercisable + red + walk-provable.**

## Slice 2 — the AllowedIPs-push FORK, RULED: Option A (volatile-routes channel + live apply)

**The fork (surfaced before code, condition-#3 as designed):** the client bakes `AllowedIPs` at device mint and NEVER re-fetches (`deviceconfig.ts` — the D2 "client owns creation, never re-fetch" invariant; same-mode+device-exists → `return existing.config`). `PushOrgNodes` pushes to gateway NODES, not client DEVICES. So there is NO dynamic AllowedIPs sync to clients — the D5 "fleet push" mechanism does not exist client-side.

**RULED: Option A** — the client config model is two-tier (never named before): **stable identity** (keys/endpoint/pool — baked at mint, never re-fetched, the D2 security invariant) + **volatile operational state** (revocation status — polled via the S6.4 RevocationMonitor). **Routed ranges are the second kind** (admin-declared, change over a device's lifetime, zero identity material) → they ride the exact precedent (a lightweight authenticated poll) the client already trusts for a more security-critical signal. Option A files routes where they belong; it adds NO new architecture. **B refused** (a route change → tunnel bounce per device + re-opens never-re-fetch for a whole-config pull when one volatile field changed — max blast radius, min cause). **C refused** (stranding the installed fleet fails the story's own reason to exist — Pritunl-parity for the installed fleet is the migration promise).

**PAPER SENTENCE (the invariant survives, stated):** identity is still NEVER re-fetched; routes were never identity. The channel carries RANGES ONLY — no keys, no endpoint, no pool, no policy. One field, one direction.

### Slice 2 build (Option A) — the acceptance set, twin-goldens in from the START
- **2a — helper live-apply verb `set_allowed_ips`:** protocol ADDITIVE at ProtocolVersion 1 (posture/set_resolvers precedent) · dispatch **state-changing-but-NOT-tunnel-owning** (same class as set_resolvers; kill-switch-untouched probe inherited) · `Backend.SetAllowedIPs` = uapi live update (`public_key=<hex>\nupdate_only=true\nreplace_allowed_ips=true\nallowed_ip=…` via `dev.IpcSet` — peer keys/endpoint untouched, NO handshake reset) **+ OS-route full-sweep** (diff desired-vs-applied, `route add/delete -net <t> -interface`) · the applied set = **baked-stable ∪ current-declared-ranges**, the pool + full-tunnel base NEVER touched by this verb · **platform parity STATED** — both platforms are wireguard-go/uapi, same live path, so neither silently gets reconnect-semantics.
  - **KILL-SWITCH-UNTOUCHED BY STRUCTURE (its own paper line):** routed ranges apply ONLY to split-tunnel; the kill-switch (pf/WFP) exists ONLY in full-tunnel; the verb NO-OPs on full-tunnel (subsumed by `0.0.0.0/0`). Probe inherited AND the surface is disjoint by construction — state both, red the no-op.
  - **Reds:** dispatch-route · tunnel-untouched · smuggle-reject · **no-bounce** (handshake age + transfer counters SURVIVE the update) · full-tunnel-no-op · **`update_only=true` ASSERTED in the no-bounce red** (its absence would CREATE peers on a typo — the smuggle-class edge for this verb).
- **2b — CP routed-ranges channel:** org-scoped, **device-authenticated** (same auth class as the revocation poll) endpoint returning the org's declared ranges (= approved site subnets, D1). Probe: a device fetches ONLY its own org's ranges.
- **2c — client poll + merge:** a `RoutedRangesMonitor` (S6.4 RevocationMonitor precedent) → merge `stable ∪ ranges` → the verb. **Fail-static** (poll fail → last-known persists, NEVER strip-to-baked) · **full-sweep** (undeclared → gone next poll) · **stability** (no churn when unchanged) — **and the compare CANONICALIZES first** (sort + normalize prefix forms; the /32-print + peersEqual + DOCKER-USER ordering lessons pre-applied) · convergence = one poll interval (reuse the revocation cadence, or justify divergence).
- **2d — D1 one-screen affordance** (byte-identical DB state red; the audit-shape decision papered) **+ D5 two-sided twin-goldens** (gateway `CanonicalHash` unchanged · no-ranges org device configs byte-identical + empty-poll→zero-helper-calls · a declaration DOES land in split AllowedIPs).

**Story-end review inherits two new probes:** the channel's AUTH (device only fetches its org's ranges) + the merge's STABILITY (no AllowedIPs churn when ranges unchanged — peersEqual at the client). **Slice order (approved):** 2a → 2b → 2c → 2d → review → walk. **APPROVED as specified — no re-ruling next session.**

## Slice 2 progress + the mint-path RULING (2026-07-19)

**Option A is functionally END-TO-END:** declare a range → CP channel → client poll → merge → live wg update, NO bounce. Three sub-slices DONE + gated green:
- **2a (helper live-apply):** `VerbSetAllowedIPs` (additive, not-tunnel-owning) + `allowedIPsUAPI` (update_only+replace_allowed_ips, NO private_key/replace_peers/endpoint → no bounce BY CONSTRUCTION) + `Backend.SetAllowedIPs` both platforms (uapi crypto-route identical; OS-route full-sweep darwin `route` / windows `winipcfg` — stated) + `UpdateAllowedIPs` (read-under-lock/apply-outside — RR2 lesson; full-tunnel NO-OP). Reds: no-bounce/update_only-asserted, dispatch-route/tunnel-untouched, smuggle-reject, routes-to-backend, full-tunnel-no-op, not-up.
- **2b (CP channel):** `GET /organizations/{orgId}/routed-ranges` — revocation-poll auth class (bearer + `authorize(OrgId, PermOrgView)`, org-scope = cross-org guard); RANGES-ONLY; APPROVED-only; canonical+sorted+dedup (deterministic); empty first-class. Reds: cross-org+approved-only (comparison-set hidden collision), deterministic, empty.
- **2c (client monitor):** `RoutedRangesMonitor` (RevocationMonitor sibling — 30s self-scheduling, start/stop, throw=keep) — fail-static, no-churn (canonical compare), full-sweep (base ∪ ranges always), seeded `lastApplied=base` (empty-org = zero calls); `TunnelController.setAllowedIPs` (throws→retry) + `baseAllowedIPs()`; wired SPLIT-TUNNEL ONLY in ipc.ts (the ruled client-layer full-tunnel skip), recreated per session (mode-change safe). Reds (7): dedup/sort, empty-unchanged, no-churn, full-sweep, fail-static, apply-retry, stop-abandons.

**MINT-PATH FORK RULED: (A) — mint stays STABLE-CORE-ONLY; the poll covers new devices too.** The two-tier model's defining test: (B) baking ranges at mint would make routes live in BOTH tiers (baked + polled) → the two-truths class in temporal form (same range known two ways, aging differently) → the monitor's base carries a stale range forever, OR a base-vs-baked distinction that only un-mixes what (B) mixed. (A) keeps the invariant CATEGORICAL: **the baked config never contains a routed range, for any org, ever** → the D5 golden is `allowedIPsFor` byte-identical UNIVERSALLY (not just no-ranges orgs), and full-sweep is trivially correct (the base can't retain what it never held). Cost: a new device's first connect gets ranges within one poll interval, not at t=0 — the same window every installed device lives with, now UNIFORM.
- **CONDITION on (A):** the RoutedRangesMonitor's FIRST poll fires IMMEDIATELY on session start (then the 30s cadence) — shrinks the new-device gap to seconds, making (A)'s trade nearly free. **2d must fix the monitor's `start()` to fire an immediate first poll** (RevocationMonitor's `start()` schedules the first after `baseMs`; the routed-ranges monitor must NOT wait a full interval).

## Slice 2d (NEXT — build FRESH, founder-facing UI + goldens; the tail-of-turn rule applies undiscounted)
- **RouteLAN composite:** one action → RegisterSite + BindNode + AddSubnet + ApproveSubnet, composing the FOUR EXISTING service methods → the four constituent audit events BY CONSTRUCTION (audit-grain RATIFIED: same code, not just equivalent state). On approve-refusal the site+bind+pending-subnet persist (byte-identical to the long path's advertise-then-refused). Red: **byte-identical DB state** (RouteLAN vs the four manual steps → same rows, same statuses, same audit events). Endpoint `POST /routed-lans` (site:manage).
- **Immediate-first-poll fix** (the (A) condition): monitor `start()` fires an immediate poll, then cadence. Red: start → one fetch before any interval elapses.
- **D5 two-sided twin-goldens:** (a) gateway `CanonicalHash` UNCHANGED by a routed-range declaration (a range = an approved site subnet → out-of-hash `Routes`); (b) `allowedIPsFor` byte-identical UNIVERSALLY (mint = stable-core-only, ruling A — the feature is config-tier-free); (c) the intended change verified via the 2c channel (already red'd).
- **Subnet-conflict teaching text:** the `subnet_not_disjoint` refusal gains teaching text ("both sites use X — options: renumber, or subnet-mapping [roadmap]") — the affordance is where solo admins first hit it.
- **One-screen affordance:** "Route a LAN through this gateway" on the gateway card / empty-Sites — open-edition reachable (the Pritunl-migrator's first screen); full ceremony untouched for multi-admin.

**Then:** Slice 3 (resolver handoff — un-gates S8.4 client DNS) → Slice 4 (NRPT) → Slice 5 (metrics + WF-1 + stale-button) → review → walk.

## Slice 3 — the resolver handoff goes LIVE (D4), DONE (2026-07-19)

**Tier RULED: VOLATILE** (the two-tier law, not baked). Forwards change when ranges change (same lifecycle) → they ride the SAME poll channel, never `config.dns_forwards` at mint. Baking them would re-open the exact staleness class ruling (A) closed for ranges — so the 2b channel gains a `forwards` field (ranges + forwards, one poll, still identity-free) and the client's apply path calls `set_resolvers` alongside `set_allowed_ips`. The symmetry with the ranges tier is exact.

- **Server — the GATED forwards field:** `RoutedRanges` schema gains `forwards: [DNSForward]`; `sites.ListRoutedForwards(orgID, ranges)` returns the org's DNS forwards **whose resolver_ip falls inside a routed range** — the DNS walk's split-horizon honesty extended to the client tier: *a resolver you can't reach is a SERVFAIL generator wearing a feature's face, so it is never handed over.* Computed BY CONSTRUCTION, not assumed (in practice, every forward's resolver lives in an approved subnet the device routes → the set is normally "all org forwards"; but the gate drops any range the device does not route). The handler returns ranges + gated-forwards in ONE body. **Negative red:** `TestListRoutedForwardsGating` — both ranges routed → both forwards; drop the branch range → the branch forward VANISHES; empty ranges → non-nil `[]`.
- **Client — the second writer, fail-static + full-sweep at the resolver tier:** `DeviceApi.routedConfig` returns `{ranges, forwards}`; `TunnelController.setResolvers` is the POLL-DRIVEN resolver apply (full-sweep + reconcile helper-side, S8.4) — **THROWS on genuine failure** (monitor keeps last, retries) **EXCEPT `resolvers_unsupported`** (Windows stub — dark until Slice 4 NRPT, skips cleanly, no throw/retry-thrash). Latches `resolversActive` on any non-empty attempt so `down()`'s sweep can never STRAND monitor-installed files (the F5 strand class — the monitor is a SECOND writer to helper resolver state). The `RoutedRangesMonitor` now applies BOTH tiers **INDEPENDENTLY**: a `set_resolvers` refusal never blocks a routes apply, and vice-versa (DNS and routing degrade separately). Reds: forwards apply-on-change + no-churn (`canonForwards` order/case-invariant) + empty-unchanged + **independent fail-static** (resolver-write throw keeps last + retries, routes untouched).
- **Platform scope STATED:** macOS-live (the S8.4 `set_resolvers` verb exists); Windows stays dark until Slice 4's NRPT — the client SKIPS cleanly (`resolvers_unsupported` returns, never errors).

**TRIGGER DISCHARGE (formal, per D4):**
- **Crash/owner-loss resolver sweep** — the ordering precondition — **landed in Slice 1** (helper `onCrashSweep` on dead-man release, exercisable + red + walk-provable). Precondition HONORED: the sweep mechanism exists BEFORE this slice's resolver install path went live. ✔ FIRED.
- **The S8.4 client resolver path** — `config.dns_forwards`/`set_resolvers` was staged-but-empty in S8.4 — **goes LIVE here** (volatile forwards, poll-driven, gated). ✔ FIRED. (S8.4b Windows NRPT remains its own Slice 4 — the Windows half of this trigger.)

**Gates (all green):** codegen deterministic · sites/devices/http both editions (`TestListRoutedForwardsGating` + prior) · test-node · client 92 (+4 forwards-tier reds) · helper UNTOUCHED (macOS-live via the existing S8.4 verb). **Then:** Slice 4 (S8.4b Windows NRPT) → Slice 5 (metrics + WF-1 + stale-button) → review → walk.

**Slice-3 gate CONDITION discharged (env-hygiene characterization, 2026-07-19):** the dismissed
agentca/tenancy/nodes failures PROVEN pre-existing — packages byte-identical to main on the branch
(`git diff --stat main...HEAD` empty) AND the SAME failures reproduce on `main` at the branch point
(run from a `git worktree --detach main`). Root cause: persisted postgres volume holds CA rows sealed
under an OLD `master.key` a later `make up` regenerated (AES-GCM decrypt fails; the CA's
refuse-to-regenerate guard surfaces it). Ledgered `docs/dev-env-hygiene.md` (fix = reset DB volume +
re-seed under the current secrets dir; NEVER delete master.key). Gate rule recorded there: red in those
three suites is dismissable ONLY after confirming untouched-vs-main + the decrypt-CA-key message.

## Slice 4 — S8.4b Windows NRPT (DECISION-FIRST paper; build FRESH — helper most-proven surface, Windows-only/un-testable-locally)

**MECHANISM RULED: PowerShell `*-DnsClientNrptRule`** (not raw-registry `DnsPolicyConfig`).
- **Consistency with the helper's proven Windows surface:** `posture_windows.go` already reads
  BitLocker via `powershell.exe -NoProfile -NonInteractive -Command` — NRPT rides the SAME established,
  reviewed shell pattern rather than introducing a second Windows privileged mechanism.
- **Documented vs undocumented on a privileged path:** the `Get/Add/Remove-DnsClientNrptRule` cmdlets are
  the SUPPORTED API; the raw `HKLM\...\Dnscache\Parameters\DnsPolicyConfig\<GUID>` schema (Version/
  ConfigOptions bitmask/GenericDNSServers multi-string encoding + the Dnscache reload notify) is
  undocumented and version-fragile — exactly the most-proven-component risk the discipline forbids.
- **Native owned-marker:** `-Comment "tunnex-managed"` gives the ownership tag for free (same string
  discipline as the macOS `# tunnex-managed` marker + the DOCKER-USER `tunnex-site-fwd` comment).
- **Shared-territory (D condition #4):** GPO-managed NRPT (common on the very fleets that need this) is
  authored via the SAME cmdlet/CSE layer — we co-exist on the documented surface, never racing the
  registry Dnscache reads. Registry-direct would fight the GPO on a lower layer.
- **Cost dismissed:** a ~200ms shell spawn on a route change (rare, monitor-gated to actual changes) is
  irrelevant; registry-direct's speed buys nothing here and its fragility is the whole risk.

**RECONCILE STRUCTURE (mirrors macOS `reconcileResolvers` exactly):** validate-first (REUSE
`safeResolverDomain` — the path-safety floor; namespace shape is the CP's one-truth, not re-judged) →
enumerate OWNED (`Get-DnsClientNrptRule` filtered by our `-Comment`) → FOREIGN-DETECT (a desired
namespace carrying a NON-owned rule → refuse `nrpt_domain_conflict`, LOGGED, GPO rule NEVER touched) →
apply desired → FULL-SWEEP owned-not-desired. Add-DnsClientNrptRule creates a NEW rule per call (no
upsert) → reconcile = **remove-all-owned-then-add-desired** (idempotent by construction, no duplicate
accumulation); the brief per-namespace resolve gap = the accepted DNS-down≠tunnel-down window (the macOS
overwrite-window precedent). Foreign rules are enumerated but their namespaces excluded from BOTH the add
set (refuse) and the sweep set (never remove what we don't own).

**COMMAND-RUNNER SEAM (how a Windows-only privileged path gets red coverage with no Windows box):**
`reconcileNRPT(run cmdRunner, desired)` takes an injectable runner (the `writeResolverFileFn` precedent).
The reconcile LOGIC — owned-set parse, foreign-refuse, add/sweep set-diff — is unit-tested on ANY
platform with a fake runner returning canned `Get` output; the real `powershell.exe` binding is a thin
Windows-tagged adapter. The reds carry the logic; the WALK carries the real binding.

**DISPATCH CLASS:** state-changing-but-NOT-tunnel-owning (identical to set_resolvers/set_allowed_ips) —
the kill-switch/revocation-untouched probe is INHERITED; NRPT never touches tunnel state, owner tracking,
or the WFP kill-switch. The stub flips: `resolver_other.go` (`!darwin`) splits into `resolver_windows.go`
(live) + a narrowed `!darwin && !windows` stub; `resolvers_unsupported` RETIRES on Windows. The Slice-3
client `resolversActive` latch + sweep semantics apply UNCHANGED — the client already treats a Windows
`ok` as live; the verb simply stops returning `resolvers_unsupported`.

**CRASH-SWEEP COVERS WINDOWS FOR FREE + THE VERIFY OBLIGATION:** the Slice-1 dead-man-release rider calls
`CleanStaleResolvers` → `setResolvers(nil)` → on Windows now reconciles NRPT to EMPTY (sweeps owned rules,
foreign untouched). So a client force-quit/crash → dead-man release → owned NRPT rules gone, no stale
split-DNS pointing at a dead tunnel. **VERIFY this path exercises NRPT cleanup + RED it** (the D-walk-5
crash-sweep leg, already macOS+Windows, is its live proof).

**REDS (logic via the fake runner; binding via the walk):**
1. reconcile add + update + sweep — desired set-diff produces the right Add/Remove calls (idempotent).
2. **foreign-refuse** — a planted foreign NRPT rule (no marker) for a DESIRED namespace → refuse
   `nrpt_domain_conflict`, nothing applied, the foreign rule SURVIVES (never Removed).
3. full-sweep — desired shrinks → owned-not-desired Removed; a foreign rule with our marker absent is
   left alone.
4. **foreign survives sweep-to-empty** (the crash-sweep / `CleanStaleResolvers` case, condition #4) — a
   planted foreign rule survives `reconcileNRPT(run, nil)`; only owned rules are Removed.
5. validate-first — a bad namespace fails the whole call with zero Add/Remove issued (all-or-nothing).
6. dispatch tunnel-untouched — `set_resolvers` on Windows perturbs no tunnel/owner/kill-switch state
   (the inherited ipc probe, re-asserted for the live-Windows path).

**WALK:** inherits the Windows name-resolution leg (S8.4b registration) + the Windows crash-sweep leg now
exercises NRPT cleanup. **Own review attention** (helper privileged surface) at story-end, per protocol.

**Build FRESH next sitting** (tail-of-turn undiscounted — this is the single most-privileged surface,
un-testable locally): mechanism is ruled + structure/reds are laid; the next turn is pure execution.

### Slice 4 BUILT (2026-07-19, fresh sitting — pure execution of the ruled paper)
- **`nrpt.go` (UNTAGGED):** `reconcileNRPT(run cmdRunner, desired)` — validate-first (reuse `safeResolverDomain`, moved to the untagged `resolver.go` with `sortedKeys` so one path-safety floor serves both platforms) → enumerate owned (`Get-DnsClientNrptRule`, filtered by `-Comment` marker) → **foreign-refuse** (`nrpt_domain_conflict`, nothing applied) → **remove-all-owned-then-add-desired** (idempotent full-sweep, no upsert/dup) → foreign never removed. `psQuote` renders every value as a single-quoted PowerShell literal (doubling quotes) — **injection-proof** on the privileged SYSTEM service, since `safeResolverDomain` rightly does NOT forbid shell metachars (`; $ \` & |`). `parseNrpt` tolerates CRLF + missing-comment(→foreign).
- **`resolver_windows.go` (`//go:build windows`):** `setResolvers = reconcileNRPT(psRun, ...)`; `psRun` shells `powershell.exe -NoProfile -NonInteractive -Command` (the posture precedent) with a 15s timeout. `resolvers_unsupported` RETIRES on Windows.
- **`resolver_other.go`:** tag narrowed to `!darwin && !windows` (the remaining-platform stub).
- **Client:** UNCHANGED — the Slice-3 `resolversActive` latch + monitor sweep already treat a Windows `ok` as live; `resolvers_unsupported` simply stops being returned.
- **Crash-sweep covers Windows (condition #4) — VERIFIED + RED:** the Slice-1 dead-man rider → `CleanStaleResolvers` → `setResolvers(nil)` → `reconcileNRPT(psRun, nil)` sweeps every owned NRPT rule, foreign untouched (`TestReconcileNRPTSweepToEmptyLeavesForeign`).
- **7 reds (linux CI, fake runner):** add/update/sweep · foreign-refuse (nothing applied) · full-sweep-leaves-foreign · sweep-to-empty-leaves-foreign (crash-sweep) · validate-first (zero commands) · psQuote-injection-proof · parseNrpt. Red #6 (dispatch tunnel-untouched) INHERITED from S8.4 `TestIPCSetResolversRoutesAndLeavesTunnelUntouched` (+ typed-error twin) — same dispatch class, `srv.resolvers` seam, platform-agnostic.
- **Gates:** `test-helper` (linux, 7 nrpt reds green) · `helper-crosscompile` (windows/amd64 — `resolver_windows.go` compiles) · darwin host resolver tests green (post-move) · gofmt clean. **Live binding = the walk** (Windows client device: enrolled, split-tunnel, resolve a cross-site name via NRPT, then sweep-on-disconnect — folded into walk prep, staged not discovered). **Then:** Slice 5 (metrics + WF-1 + stale-button) → review → walk.

## Slice 5 — the cleanup closer, with the L1 FORK ruled (2026-07-19)

**A mid-build fork HALTED and surfaced (halt-and-surface at ruling-premise depth):** the L1 ruling said
"existing plumbing extended, not new telemetry." FALSE for site-links — `UpsertDeviceStatus` maps a peer
pubkey to the *active DEVICE* on the node (`devices.sql:208` — "an unknown pubkey is a no-op"). A
site-link peer's pubkey is the remote **gateway's** wg key, which has NO device row → the reported bytes/
handshake are DROPPED at the CP. And `SiteLinkStale` reaches the CP as an agent-computed **bool**
(`service.go:638`), not a numeric handshake. So the CP holds a fresh/stale flag for site-links, never a
numeric age or byte count. "reported" ≢ "stored" — the write path, not the wire, is the truth.

**FORK DISPOSITION (founder-ruled):**
- **Slice 5 ships the two clean items + the sweep** (below). L1's numeric bytes+handshake-age are the only
  casualty — NOT silently dropped.
- **L1 re-homed to S8.6's commit-one as a DECIDE-ITEM (not S11.1):** S8.6 (hub HA) has a structural claim
  on the same storage — HA failover decisions ride link-health observation, and the node-peer status table
  (keyed `node_id, public_key`) L1's bytes need is plausibly the SAME substrate S8.6's per-hub link
  tracking wants. S8.6's paper answers: does HA need node-peer status storage anyway? **If yes** → L1's
  bytes+age ride it as a costed rider (storage designed ONCE, decision-first, serving both). **If no** →
  L1 lands in S11.1 where telemetry storage is designed properly, and the registration says so with that
  reasoning. Either way L1 gets a decided home, not a drift.
- **WF-1 sufficiency (sequencing survives):** the fresh/stale signal WF-1 ships IS the site-link liveness
  observability S8.6's walk needs to OBSERVE failover — numeric age is richer, not required.

**The two clean items (both PURE, no new data):**
- **WF-1 positive health** (`sitesview.gatewayOnline` + the GatewayRow "online" badge): an active gateway
  reporting FRESH (the SAME `gatewayLiveness` clock) with NO degraded-health badge reads affirmatively
  **online** — the fresh side of the same clock + health bool the offline/degraded badges already read.
  NO third vocabulary, threshold from the CP's clock (`GATEWAY_OFFLINE_MS`). Reds: active+fresh+healthy →
  online · offline → not · `site_link_down` health → not · revoked → not.
- **Stale add-rule button** (`Access.tsx`): the ROOT was the one-truth law at the React tier — `RulesSection`
  and `GroupsResourcesSection` each held a copy of the groups list, and a group add in the latter never
  invalidated the former, so the "Add rule" enabled state read a stale copy. FIX = a parent-owned
  `subjectsRev` invalidation signal (GroupsResourcesSection bumps on group/resource mutation → RulesSection
  re-loads), chosen over lifting groups because RulesSection's subject data is a cohesive batched load a
  partial lift would fracture. **Invalidate the copy, not the symptom** (patching the disabled expression
  would leave the stale copy feeding the rule modal too). View-verified (no page-render harness; the
  UI-walk confirms) — the fix is state-wiring, not a pure fn.

**LEDGER SWEEP (the carried-items table the story-end review reads clean):**
- ✔ **WF-1 DISCHARGED** — positive site-link liveness shipped (the S8.2c→S8.3→S8.5 deferral chain closed).
- ✔ **Stale-add-rule-button DISCHARGED** — the S8.3 founder-walk item fixed at its root (one-truth, React tier).
- **L1 site-link metrics RE-DISPOSITIONED** (not dropped) → S8.6 commit-one decide-item (storage coherence
  with HA), fallback S11.1 — reasoning recorded above.

**Gates:** web typecheck clean · web tests **136** (+4 `gatewayOnline` reds) · web build ok. **Then:**
story-end multi-finder review (pre-registered probes: two-tier channel auth+content across 4 slices · NRPT
foreign-rule discipline · monitor independent-degradation seams · RouteLAN composite authorization ·
standing probes) → fold → hold at walk-ready → S8.6 commit-one (carrying the L1 decide-item + the
branch-discipline proposal).

## Story-end review — 10 findings dispositioned (2026-07-19)

17-agent high review, 12 verified → 10 ranked, 0 refuted. Probe-1 (channel auth/cross-org) + probe-2's
injection/foreign-rule surface returned ZERO candidates — the two-tier channel auth and NRPT
`psQuote`/owned-marker discipline are clean. Addition-B (RouteLAN partial-failure ladder) landed #2;
addition-A (channel growth) found nothing (the `canonForwards`/`canonRanges`/`ListRoutedForwards` sorts
absorb wire order end-to-end — confirmed safe).

| # | verdict | disposition |
|---|---------|-------------|
| 1 route-sweep wedge on partial-add | CONFIRMED | **REDUCE** (route cluster) |
| 3 OS-route add non-idempotent (no delete-first) | CONFIRMED | **REDUCE** |
| 4 add-before-delete + early-return skips sweep | CONFIRMED | **REDUCE** |
| 8 route full-sweep dup darwin/windows | CONFIRMED | **REDUCE** (#8 IS the reduce) |
| 9 Up rebuilds routeOp's arg list | CONFIRMED | **REDUCE** (dissolves in) |
| 10 next-hop family copy-pasted 3× (win) | CONFIRMED | **REDUCE** (dissolves in) |
| 2 RouteLAN not retry-safe (BindNode re-homes) | CONFIRMED | **FIX** (independent) |
| 5 full-tunnel cross-site DNS breaks | CONFIRMED | **FIX** (independent) |
| 6 setResolvers/applyResolvers latch dup | CONFIRMED | **FOLD** (rides #5) |
| 7 monitor re-implements RevocationMonitor scheduler | CONFIRMED | **FOLD** (shared base) |

### REDUCE PAPER — the ONE OS-route reconciler (decision-first; build FRESH after ruling)

**Root:** `backend.SetAllowedIPs`'s route sweep (Slice 2a) has no shared, idempotent reconciler. `Up`'s
endpoint-route path already delete-before-adds (`backend_darwin.go:142`, "so the re-add can't fail File
exists"); the reconcile path FORGOT the same lesson — two copies of one discipline, one drifted. #1/#3/#4
are three faces of that drift; #8/#9/#10 are its duplication. ALL SIX resolve as ONE reduce.

**The reduce:** ONE OS-route reconciler, BOTH platforms, the per-target syscall/CLI call INJECTED — the
`cmdRunner`-seam pattern from NRPT (Slice 4) applied to routes, so the reconcile LOGIC is red-able on any
platform and the platform binding is a thin adapter (#8 IS this; #9/#10 dissolve by construction — Up's
baked routes and the pushed routes go through the SAME reconciler + the SAME next-hop derivation).

**STATE MODEL — per-route applied set, RULED (2026-07-19).** Replace `appliedAllowedIPs []string` (a batch
watermark that, unadvanced on partial failure, re-wedges) with `applied map[string]bool` = the route-targets
we believe are in the kernel. Each successful add → `applied[t]=true`; each successful delete/failure →
`delete(applied,t)`. MINIMAL state that makes partial success first-class: a transient failure on r2 leaves
r1 marked, so the retry never re-issues `route add r1` (#1 dies). Rejected: query the kernel route table each
pass (route -n get / GetIpForwardTable2) — more truthful but re-imports the per-platform parsing divergence
the reduce exists to kill, and heavier per poll.

**The map is a BELIEF CACHE, not kernel truth — drift stance NAMED (founder ruling):** the kernel can lose a
route the map believes applied (sleep/wake, interface events, an admin's manual `route delete`), and the
reconciler will not re-add it until the belief is invalidated. The drift is bounded by a HEALING TRIAD, all
already structural (no kernel-query, no periodic-resync cleverness — the dormant-machinery law's spirit:
don't build lifecycle logic for a window the session lifecycle already covers):
- **(a) session lifecycle heals it** — Up re-applies baked routes and the per-session monitor recreation
  re-applies pushed ranges on every reconnect; the interface events most likely to strip routes are exactly
  the ones that bounce the session.
- **(b) any failed op invalidates that route's belief** — per-route advance made explicit: a failed add/delete
  clears (never sets) the entry, so the next reconcile re-attempts from delete-before-add.
- **(c) Down clears the map entirely.**
This bounds the drift window to "mid-session, kernel-only route loss with NO interface event" — rare,
self-healing on next change/reconnect, RECORDED as accepted residue (not discovered later).
**Two conditions on the map:** keys are CANONICAL route-target strings (the same canonicalization the compare
uses — the /32-print class pre-killed); the map is PER-SESSION state, NEVER persisted (a persisted belief
cache across restarts would be the staleness class in a file).

**FIVE SEMANTICS (each kills a defect):**
1. **Delete-before-add per route** — idempotent by construction; a pre-existing identical route (home-LAN
   collision, #3) no longer fails "File exists". (The Up:142 discipline, now shared.)
2. **Full compare-and-sweep every pass, delete-stale FIRST then add-missing** — the crypto layer
   (`replace_allowed_ips`) already dropped a de-advertised range; the OS layer must never lag it (#4). The
   sweep runs REGARDLESS of add outcomes (an add error never skips the delete pass).
3. **Per-route advance** — `applied` updates for each route that succeeded, so a transient failure on one
   never re-wedges another (#1). Partial success is REPORTED with specificity: the existing
   `allowed_ips_apply_failed` names WHICH targets failed (gains detail, not spam) — the health surface
   already carries the code.
4. **Home-LAN collision STANCE (named):** when the office range == the user's local LAN, delete-before-add
   makes OUR route win while the tunnel is up — this is CORRECT split-tunnel semantics (declaring a range
   means it rides the tunnel). Down/full-sweep must restore the local route's world: we delete our applied
   set and the OS re-derives the connected route. Per-platform behavior (does the kernel auto-restore a
   connected route after our static delete?) is a NAMED verify+red obligation + a walk leg — NOT assumed.
5. **Both platforms through the one reconciler via the seam** (#8).

**REDS for the reduce (logic via fake route-runner; binding via the walk):**
- partial-add-then-retry CONVERGES (r1 ok, r2 fails, retry reaches r2 with NO "File exists" on r1) — #1.
- pre-existing identical route → apply SUCCEEDS (idempotence) — #3.
- add-fails-but-sweep-RUNS (B's stale route gone even when A errored) — #4.
- both platforms through the one reconciler via the injected seam — #8.
- Down/full-sweep restores the local route world (per-platform, red where unit-reachable; walk otherwise) — semantic 4.

**Scope:** a Slice-2a-revision-sized reduce on the most-privileged surface → build FRESH (S8.4-arc rule,
undiscounted). The feature-sized fold RE-EARNS a targeted re-review over the fold diff.

### #2 FIX (independent) — RouteLAN retry-safe by RESUME, not re-create
On resubmit after a refusal, the composite must ADVANCE its own surviving partial state (site+bind+pending
from the prior attempt) — re-advertise on the EXISTING site — not register a second. Shape: `BindNode`
REFUSES to silently re-home a bound gateway (`node_already_bound_to_site` unless same-site no-op); RouteLAN's
entry checks "does this gateway already carry my half-built site?" → resume it. Reds: refusal → correct →
resubmit → ONE site, one pending subnet, gateway still homed · the bind-refusal red standalone.

### #5 FIX (independent) — the mode ruling's premise LAPSED
The monitor's split-tunnel-only wiring was ruled when it carried ONLY routes (full-tunnel `0.0.0.0/0`
subsumes routes — correct). Slice 3 grew the payload to carry RESOLVERS too, and resolvers are NOT subsumed
by `0.0.0.0/0` (full-tunnel's baked DNS is `1.1.1.1`, which can't answer internal zones). The premise
lapsed when the payload grew. Fix: the monitor runs in BOTH modes; the ROUTES tier stays split-only
(full-tunnel no-op — already structural in the helper `UpdateAllowedIPs`), the RESOLVER tier applies in
BOTH. The independent-degradation seam (Slice 3) is exactly the machinery that makes this a WIRING change,
not a redesign. Red: full-tunnel device gets NRPT/resolver files + NO route calls; split gets both.

### #6/#7 FOLD (ordinary dedupe)
#6 rides #5's edit (one `set_resolvers` path + one `resolversActive` latch — the F5 invariant in one place).
#7 extracts the self-scheduling scheduler into a shared base for both monitors (behavior-identical; the
existing reds are the net).

**SEQUENCE:** reduce paper → **founder ruling** → build (reduce + #2 + #5 + #6/#7 folds) → gates →
targeted re-review over the fold diff (probes: the reconciler's five semantics as reds · RouteLAN's resume
path · the mode matrix) → hold at walk-ready. **Walk manifest gains:** the home-LAN-collision leg (a real
laptop whose local subnet collides with the office range) + the full-tunnel-DNS leg.

**HELD for the founder's ruling on the reduce paper before any build.**
