# S20.5 live correction: shared site/Kubernetes WireGuard peer

Status: local correction and focused tests complete; not a live pass or merge approval.

## Evidence and existing invariant

The C5 AWS walk reached A3's `ownership full-domain readback mismatch:
wg_peers`. A read-only diagnostic using the current compiler's peer path and
database connections forced `default_transaction_read_only=on` returned one
desktop peer and **two entries for the same edge public key**: ordinary site
transit owned `10.99.0.0/24` plus `172.31.0.0/16`; Kubernetes transit owned
`10.99.0.0/24`. Endpoint, SiteLink and keepalive were identical. The retained
old demo site makes the ordinary and Kubernetes graphs coexist.

WireGuard has one peer per public key. Concatenating these contributions
cannot produce the multiset the exact ownership check expects. Deleting the
old site would hide the product defect and affect unrelated configuration.

## Bounded correction under the approved bug-fix scope

Compose compatible contributions into one peer before serving/hashing the
ordinary desired state. Union and sort AllowedIPs; preserve endpoint,
SiteLink and keepalive. Reject conflicting transport intent rather than use
last-writer-wins. Retain exact ownership/readback checks and fence filtering;
do not relax them, change protocol/schema, delete the other site's route, or
force HA authority. Test simultaneous ordinary and Kubernetes contributions,
empty warm peers, disjoint prefixes, and conflicting transport metadata.

Independent review must re-check the implementation. A new immutable
candidate must re-earn affected gates and live proofs; C5 is not repaired by
these source changes. The separately observed CP version-binding/retry issue
remains distinct and is not claimed fixed by peer composition.

Slice review: no concrete P1/P2 findings. Duplicate non-topology peers are
also refused rather than hiding an identity collision. Tests reproduce both
graph builders, union ordering, non-aliasing, conflict refusal, and the node
fence preserving unrelated routes. Full public/authority compiler execution
and hash equivalence remain a coverage limitation, not a claimed test pass.

Live precondition: the desktop must be reassigned from connector A3 to the
separate edge using the existing device-move workflow. Keeping a desktop /32
on A3 while the serving overlay claims the device pool through another peer
correctly hits the existing overlap guard; do not weaken that guard.
