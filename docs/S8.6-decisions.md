# S8.6 — Hub high-availability (commit-one, decision-first)

**Status:** commit-one RULED (D0–D6 + L1 dispositioned, all priors confirmed with conditions, 2026-07-19).
Slice cut proposed below. No product code until the slice cut is accepted at the first gate.
**Predecessor:** S8.5 (open-edition routed subnets) HELD at walk-ready (branch `story/S8.5-routed-subnets`,
HEAD `5daf729`), not merged. This story is **STACKED** on it (D0=A).

## The problem

EPIC 8's site-to-site mesh is **hub-and-spoke**: one elected hub gateway carries transit between spokes
(S8.2 `siteLinkGraph`). The hub is a **single point of failure** — if the hub box dies, every spoke-to-spoke
path through it goes dark, and the CP's site-link health surfaces `site_hub_down` (S8.2 H5, honest) but
nothing FAILS OVER. S8.6 makes the hub role survivable.

Zero-touch + unlock-then-opt-in still bind: HA is founder-directed enterprise-adjacent, default OFF, and
the data-plane agent owns reconciliation (the CP never touches WireGuard).

---

## D0 — Branch discipline: STACKED (RULED)

S8.6 is built on branch `story/S8.6-hub-ha`, forked off `story/S8.5-routed-subnets` (this paper is its
first commit). HA reads S8.2's `siteLinkGraph` + the S8.5 route reducer + site-link health directly —
building blind to them would invite the two-truths class. The batch directive already fixed the merge
order **S8.5 → S8.6 → S8.7** (linear), so stacking makes rigid what was already rigid, at no cost.

**BATCH-DAY REBASE RULE (pre-agreed, so the ordering isn't litigated on walk day):** any defect the S8.5
walk surfaces that touches SHARED machinery (the route reducer, site-link health, `siteLinkGraph`, the
device-status/node-peer storage) is folded on `story/S8.5` and this branch is **rebased onto the amended
S8.5 BEFORE S8.6's own story-end review** — so S8.6's review always runs against the final shared base, and
the merge train stays S8.5 → S8.6 → S8.7.

---

## HA scope — the decide-items (RULED, with conditions folded)

### D1 — Hub-set election (CONFIRMED: CP-owned, ordered, generation-stamped, agents observe-never-vote)
`electSiteHub` (S8.3, CP, deterministic) is extended to elect an ORDERED `{primary, standby[]}` per site.
One-truth at its highest-stakes tier — a voting/gossip protocol among agents would be building Raft in a
weekend (how products die). Agents OBSERVE the pushed hub set and reconcile; they never vote.
- **Election inputs, stated EXHAUSTIVELY (condition):** a gateway is hub-CAPABLE iff it has a public
  endpoint present (a NAT'd spoke can't be a hub — it dials out) AND a fresh WG key. Among capable
  gateways, order by: (1) **admin priority override** (RULED: YES — an optional per-node `hub_priority`
  field; operators distrust magic, so the admin OUTRANKS the election, which only breaks ties); (2) node
  health (fresh handshake / not degraded); (3) a deterministic tiebreak (node-id) so the order is stable
  and re-computable. Absent an admin priority, it degrades to health-then-id (today's determinism).

### D2 CORRECTION (2026-07-20, Slice-3 build) — promotion = AllowedIPs reassignment, NOT route metric
The D2 condition below ("route-metric preference") verified the WRONG LAYER: it checked that Linux `ip route`
honors metric among duplicate prefixes (true) but never checked it against WireGuard's crypto-routing, where
**AllowedIPs owns peer selection within an interface and the OS metric never gets a vote**. With one `wg0`
there is ONE interface, so the metric would arbitrate a choice that doesn't exist (the REPORTED ≢ STORED
lesson's routing sibling — a ruling must cite the layer that actually DECIDES: the OS route picks the
interface, AllowedIPs picks the peer). RULED **(A)**:
- **Promotion = AllowedIPs REASSIGNMENT at compile**, not a metric flip. The D2 promise SURVIVES INTACT and
  wins on the merits: the standby tunnel is warm (handshaked + keepalive'd), so promotion still builds
  NOTHING — no handshake wait, no tunnel establishment; just a re-compile moving the remote subnets from the
  primary peer's AllowedIPs to the standby's, applied by the agent's existing uapi peer-update reconcile (no
  bounce). (B) per-hub interfaces REFUSED — buys literal metric fidelity with a multi-interface data plane
  the product doesn't have and the walk doesn't need (speculative-complexity class).
- **THE INVARIANT (replaces the metric reds):** a remote subnet lives in EXACTLY ONE peer's AllowedIPs per
  device, EVER — never both hubs (WG's undefined-which is a nondeterminism generator; the compiler makes
  the assignment single-valued BY CONSTRUCTION). Red: compile with primary+standby → subnets in the
  PRIMARY's AllowedIPs only, the standby's = its tunnel /32 exactly.
- **Standby peer shape:** endpoint present (so the handshake fires) · AllowedIPs = the standby hub's tunnel
  /32 ONLY · keepalive per S8.2b — warm + observable (the spoke's handshake with the standby lands in
  `node_peer_status`, Slice 1's substrate proving the warmth). Red: the standby peer's handshake age is
  visible in the table.
- **PROMOTION LATENCY (honest sentence):** the flip rides compile → push → reconcile (seconds), consistent
  with D3's CP-driven design — failover speed IS push-path speed, and CP-down = no failover is the named
  residue. The mechanism's dependency chain is stated (the metric version wouldn't have had it).
- **Reducer-identity-key concern WITHDRAWN** — no duplicate OS routes exist under (A); the red list carries
  no ghost of it.

### D2 (ORIGINAL condition — SUPERSEDED by the correction above; kept for the record)
The standby pre-establishes keepalive'd transit tunnels to every spoke (S8.2b keepalive), installs its
transit routes but at a WORSE preference, so promotion is a **preference flip, not a tunnel build** — the
design's core idea (failover becomes a preference problem, not a build problem).
- **Platform-capability VERIFIED (condition a):** the HA route-preference tier is GATEWAY-side (Linux,
  `apps/node`) — NOT the client helper. The darwin/windows S8.5 reducer is the CLIENT tier and is not on
  the HA path. Linux `ip route` already carries a metric on every site route (`wgctrl_linux.go:311`
  `siteRouteMetric = 8021`, `:356` `ip route replace ... metric`) and honors metric-based selection among
  DUPLICATE-prefix routes (lower wins). So the standby installs its per-spoke transit routes at a HIGHER
  metric than the primary's; both present, primary wins; on primary death its routes prune (stale tunnel)
  and the standby's survive. **No presence-flip fallback is needed — the one HA platform (Linux) expresses
  preference cleanly.** (Fallback stance, unused: were a tier unable to express metric, it would be
  presence-flip — standby routes absent until promotion, colder but honest.)
- **Cost STATED (condition b):** every spoke holds N warm keepalive'd tunnels (one per hub in the set).
  Trivial at N=2 (one primary + one standby); the keepalive traffic is the S8.2b cadence × N. Said, not
  hidden.

### D3 — Failover trigger: CP-driven off the existing staleness clock (CONFIRMED, residue named)
The CP's existing site-link staleness (`SiteLinkStale`/`site_hub_down`, S8.2 H5 — REUSED, no new health
vocabulary, the WF-1 discipline) crosses a threshold → CP re-elects the hub set (promote standby) → pushes
→ agents reconcile. The promotion is a desired-state change, not a new control path.
- **RESIDUE NAMED (condition, in the paper's own voice → docs):** the CP is the failover BRAIN, so
  **CP-down = no failover** — everyone keeps their last role (fail-static). A SIMULTANEOUS primary + CP
  failure has NO automatic answer in v1. This is the honest v1 trade, consistent with the product's whole
  CP-authoritative architecture; it is stated here and ships in the docs. An HA feature whose limits are
  unstated is the reassuring-green class at the feature tier.
- Per-hub freshness requires per-hub-PEER observation — which is exactly L1's storage question answering
  itself (the CP must see each hub's per-spoke handshake to know which hub is alive). See L1.

### D4 — Flap damping: asymmetric hysteresis (CONFIRMED, concrete numbers)
An oscillating primary (marginal link) must not ping-pong the role (each flip re-routes every spoke).
- **Concrete numbers (condition), derived from the S8.2 staleness clock cadence** (the reconcile/report
  loop; the exact tick is pinned in the build against `SiteLinkStale`'s clock): **promote the standby after
  N = 3 consecutive stale ticks** (~ the S8.2 site-link-down threshold), **fail back to the primary only
  after it is fresh for M = 5 ticks AND a hold window has elapsed** (fast-over, slow-back). The precise
  tick maps to the existing clock at build time, cited.
- **The flap RED is the design's proof:** an oscillating primary (stale/fresh/stale/fresh…) produces
  EXACTLY ONE failover, not a metronome.

### D1/D5 KEYING CORRECTION (2026-07-19) — org-level, not per-site
Slice-2 build surfaced that "per-site ordered hub set + per-site generation" (my earlier D1/D5 wording) was
a DRIFT from the verified model, not a design intent: `electSiteHub` is ORG-WIDE (one transit hub for the
org's whole site mesh, `service.go:425`) — the wording imported "per-site" from D5's phrasing without
re-checking the substrate (the one-truth law applies to rulings too). RULED **(A) org-level**:
`electSiteHubSet` = the org's ORDERED hub-capable gateways; `members[0]` = the transit hub (today's
election, now ordered); the rest = failover candidates IN ORDER. **ONE generation per ORG** (D5 fencing
re-keyed: per-ORG monotonic, CP-persisted, survives restart). Schema keyed by **org_id** — named
`org_hub_set` so it can't be misread as a site attribute (this is the transit-hub set, not per-site).
- **CONSEQUENCE (stated):** with an org-level set the standby need NOT share the primary's site — an org can
  fail over from an AWS-site hub to an Azure-site hub. The walk's same-site standby is ONE case, not the
  definition; the capability gate (public endpoint + fresh key) is the ONLY membership criterion. Red: a
  capable gateway from a DIFFERENT site enters the set in correct order.
- **(B) NOT foreclosed (reserved evolution):** if a future customer needs independent per-site failover
  (multi-hub topologies, the S8.2 "reserved" note), a per-site election layers UNDER the org transit
  election — the org set's schema does not block it. Recorded, not built.

### D5 — Split-brain: the fence is CP-SIDE (CORRECTED 2026-07-20, Slice-4 build — item-1 survey)
**The original wording — "an agent applies ONLY a strictly HIGHER generation" — imported fencing
vocabulary from architectures where nodes receive state from MULTIPLE authorities and must arbitrate
(Raft peers, gossip meshes, dual-master DBs). This product has NONE of those.** The Slice-4 apply-path
survey found NO monotonic (refuse-older) check on the agent, and the trace shows one isn't needed:
- Agents have EXACTLY ONE artifact source — the CP. No peer-to-peer push; a demoted primary can push to
  no one. The Watch is monotonic by construction (long-poll on `since`, the server only advances). So a
  stale artifact CANNOT reach an agent.
- The split-brain D5 feared decomposes into (a) stale-artifact-reaches-agent — PRECLUDED (single source +
  monotonic Watch); (b) demoted-primary-keeps-serving — REAL but INERT: the spokes moved their AllowedIPs
  on the CP's word, so the stale primary receives NOTHING (the fail-static residue, already named).
- An agent-side generation check would fence a scenario the architecture precludes — dead code whose only
  future is convincing a maintainer the scenario is live (speculative-complexity class, refused).

**RULED: the fence is CP-SIDE.** It is the ATOMIC monotonic generation bump in `UpsertOrgHubSet` (Slice 2,
`generation = CASE WHEN members IS DISTINCT FROM ... THEN +1`) — which makes two concurrent hub-set writes
converge to ONE monotonic result — PLUS single-source artifact delivery. Agents need NO generation
awareness because they have no second authority to arbitrate against. The `generation` stays in
`org_hub_set` as the PROMOTION EVENT's identity (audit, debugging, the UI's "hub set v7"); its fencing
role IS the upsert's atomicity.
- **DEFERRED agent-side fence — trigger NAMED:** IF the architecture ever gains a SECOND artifact path
  (agent-to-agent relay, CP federation, offline artifact bundles), the agent-side monotonic check becomes
  MANDATORY in that story's commit-one. Recorded so the deferred fence can't be forgotten if the
  precondition changes.
- **CP-side fence RED (explicit, so the fence can't rot):** the two-writer convergence fixture (concurrent
  reconciles, different member views → one monotonic generation-ordered result), cited at the
  `CASE ... IS DISTINCT FROM` statement.
- **Demoted-primary-inert RED (structural, not narrated):** post-promotion compile → the demoted primary's
  pubkey appears in ZERO spokes' subnet-carrying AllowedIPs (it may persist as a keepalive-only peer — the
  warm fail-back line). The walk's tcpdump (spokes stop sending) confirms on iron what the red pins.
- **Spoke-side partition (Slice 5, red):** a spoke that can reach NEITHER the CP NOR the primary uses its
  last-known config against whichever hubs it CAN reach — a reachable standby carries traffic even while
  the CP is unreachable (fail-static). RED the reachable-standby case (Slice 5).

**COMPILER CONSUMES THE PERSISTED ACTIVE ORDER (Slice-4 integration):** the failover controller maintains
`org_hub_set.members` as the ACTIVE order (configured minus stale under hysteresis). The compiler
(`siteLinkGraphFrom`) reads `org_hub_set.members` (the ONE truth), NOT `electSiteHubSet(topo)` directly —
so a promotion (a change to the persisted members) flows through the ordinary compile+push with no
failover-special path. `electSiteHubSet` is the CONFIGURED intent that `ReconcileHubSet` (bind/unbind/pin)
and the tick DERIVE from; the persisted set is what agents see. (Empty `org_hub_set` → fall back to
`electSiteHubSet` so a not-yet-reconciled org still compiles single-hub.)

### D6 — The proof: three-VM primary-kill walk (CONFIRMED, infra ask staged NOW)
HA cannot be proven on the 2-gateway topology. The walk needs a **THIRD gateway VM** (a second hub-capable
box, either cloud) so a real primary-kill → standby-promotion → spoke-continuity is observed on the wire:
kill the primary hub, watch a spoke's transit fail over to the standby with bounded interruption, then the
primary rejoins as standby (no split-brain — generation fencing holds). **INFRA ASK staged now (below) so
it's provisioned before batch day**, not discovered on it.

---

## Carried decide-items (from prior stories, land in THIS commit-one)

### L1 — node-peer status storage (RULED: YES, rides S8.6; table designed here)
D3's per-hub freshness IS node-peer handshake state, so HA earns the storage the S8.5 L1 metrics also
needed — designed ONCE, serving the failover clock AND the topology cards. REPORTED ≢ STORED gets its fix
where the law was minted (the `UpsertDeviceStatus` gateway-peer no-op finally gets its sibling that STORES).

**The table (designed now):**
- **`node_peer_status`** — keyed **`(node_id, public_key)`**: the reporting gateway node + the peer's WG
  pubkey (a remote gateway for a site-link, distinct from `device_status` which is device-keyed and no-ops
  on a non-device pubkey — the exact S8.5 gap). Columns: `last_handshake_at`, `rx_bytes`, `tx_bytes`,
  `updated_at`.
- **Write path:** the SAME agent report the CP already receives (`ReportStatus` / `UpsertDeviceStatus`,
  `nodes/service.go`). Today it maps a pubkey → the active DEVICE and no-ops an unknown (gateway) pubkey.
  S8.6 adds a SIBLING upsert: every reported peer that resolves to a NODE (a site-link peer) lands in
  `node_peer_status`. One report, two sinks (device_status for device peers, node_peer_status for gateway
  peers) — no new agent field, no new report.
- **Retention stance:** last-write-wins gauge (like `device_status`) — NOT a time series (that's S11.1's
  job). One row per `(node_id, public_key)`, upserted each report; cascade-deleted with the node. No
  history, no growth beyond the peer count.
- **Reads:** (1) D3's failover clock — per-hub per-spoke `last_handshake_at` freshness; (2) the L1 topology
  metrics — bytes + handshake-age on the site-link cards (render-floor: cumulative totals labeled "since
  last handshake", refresh-on-load, no rates/sparklines, absent-report → absent-number never zero-as-data,
  per the S8.5 L1 ruling).

---

## WALK MANIFEST — D6 infra (staged from EXISTING fleet, no new VM; 2026-07-19)
- **Primary hub** = `aws-gw` — public `16.176.32.176`, private `172.31.28.80`, VPC `172.31.0.0/16`.
- **Standby candidate** = **AWS instance 2** — public `3.25.125.203`, private `172.31.17.64`, SAME VPC
  `172.31.0.0/16`. NOT yet a gateway: **enrolls as a gateway at walk time**; needs **SG UDP 51820 open** +
  **source/dest-check OFF** (so it can forward). This is the second hub-capable box the primary-kill leg
  promotes to.
- **Spoke** = `azure-gw` — public `20.245.69.218` (CHANGED from `172.184.125.139`, DYNAMIC — Pawan makes it
  static before batch day), private `10.0.0.5`, VPC `10.0.0.0/16`.
- **CP** = `40.65.63.141` / `10.0.0.4` (Azure).

**TOPOLOGY NOTE (walk-prep, per D1/D2):** the primary + standby share AWS VPC `172.31.0.0/16`. HA's hub set
(D1) is **two gateways of the SAME site** (the AWS site fronting `172.31.0.0/16`) — so the standby is a
**second gateway of that site**, NOT a second site. This REQUIRES lifting S8.1's single-node-per-site
(that's exactly D1/Slice 2's hub-set change). Disjoint-range carving only matters in the rejected
"second-site" framing; the ruled hub-set design keeps one AWS site, two gateways, so there is no
same-VPC disjointness conflict. Walk-prep confirms second-gateway (the D1/D2 design) — recorded here so
batch day doesn't re-litigate it.

---

## Proposed SLICE CUT (decision-first; refine nothing until accepted at the first gate)
1. **`node_peer_status` storage + the sibling upsert** (L1 substrate): migration + query + the second sink
   in `ReportStatus`. Reds: a gateway-peer report LANDS (the S8.5 no-op's fix, red'd); device peers still
   land in `device_status` (no regression). The foundation D3 + the metrics rider both read.
2. **Hub-set election** → ordered `{primary, standby[]}` per site (D1): CP-owned, `hub_priority` override,
   generation-stamped + CP-persisted (D5 fencing). Reds: exhaustive-input ordering; generation monotonic +
   survives restart.
3. **Warm standby + route-preference metric** (D2): the standby's transit tunnels + higher-metric routes,
   gateway-side. Reds: standby routes present-but-deprioritized; promotion = metric flip not tunnel build.
4. **Failover trigger + flap damping** (D3/D4): the staleness-clock threshold → CP re-election → push, with
   asymmetric hysteresis. Reds: the FLAP red (oscillating primary → exactly ONE failover); the CP-down
   fail-static residue.
5. **Spoke-side partition behavior** (D5): last-known preference against reachable hubs. Red: reachable-
   standby carries traffic while CP unreachable.
6. **Web hub-set surface**: primary/standby, generation, last-failover + the L1 site-link metrics on the
   cards (render-floor). No new health vocabulary (reuse the badge kinds).
7. **Story-end review → three-VM HA walk** (D6): the primary-kill failover leg + the rejoin-as-standby leg.

**HELD at the first gate as usual** — build Slice 1, gate report, founder go, per the established cadence.


## Slice 2 BUILT (2026-07-20) — hub-set election + single-node lift, against (A) org-level
- **Migration 0037:** DROP `nodes_site_id_uniq` (the lift — a site may hold multiple gateways) + `nodes.hub_priority` (nullable admin pin) + `org_hub_set` (org_id PK, ordered `members`, `generation`). The **down honestly REFUSES** if a multi-gateway HA site exists (can't recreate the unique index) — proven in the round-trip (failed exactly because the test left a two-gateway site), the designed stance vs a silent row-drop.
- **`electSiteHubSet(topo, now)`** — the ONE election, now ordered: capability gate (public endpoint + WG key — the only membership criterion, so a cross-site capable gateway enters) → hub_priority (pin, asc) > health (fresh<stale, `hubStaleWindow`) > id. `electSiteHub` = the head (members[0]); every existing consumer (site-link graph, health, is_site_hub) projects it UNCHANGED (single-hub v1 — compiler uses only the primary).
- **`ReconcileHubSet`** — atomic `UpsertOrgHubSet` (generation bumps in-statement ONLY when members change — no idle tick, concurrency-safe). Triggered on membership/order change: bind + unbind (handler layer, best-effort) + `SetHubPriority` (the pin, audited + re-elect). `GetHubSet` reads it.
- **Reds:** ordering (capability gate · id-order · pin-outranks-healthier · health>id · cross-site enters) · generation (bump-on-change · idempotent-no-bump · **survives restart** · monotonic · **pin end-to-end** → pinned becomes members[0]). Existing `TestReplaceNodePreservesSiteEntity` updated (second-bind now SUCCEEDS — the lift).
- **DEFERRED to Slice 6 (web hub-set surface):** the HTTP pin endpoint (`SetHubPriority` service is built + red'd; the operator/UI surface lands with the cards) + the hub-set GET endpoint. Slice 2 is the backend election machinery; Slice 6 surfaces it.
- **Boundary honored:** the set is persisted + reconciled + served (service), but the COMPILER still consumes only members[0] — standbys don't grow tunnels until Slice 3. This slice's review reads a storage+election change, NOT a topology change.

## Slice 3 BUILT (2026-07-20) — warm standby + AllowedIPs preference, two-tier membership (3)
- **Membership RULED (3), two-tier** (`electSiteHubSet`): ANY pins → the set = the PINNED capable gateways (HA opt-in — the pin declares "carry the org's transit"; capability still gates, a pinned-NAT'd/keyless is excluded). NO pins → a SINGLE auto-elected hub (today's zero-config behavior, byte-identical, no standbys). This dissolved the `capable ⇒ hub-posture` collision — an endpoint-bearing LEAF (the walk's azure-gw) is no longer drafted into hub duty.
- **Compiler (`siteLinkGraphFrom`):** a MEMBER compiles HUB posture (peers with every non-self, subnet-advertising gateway NOT in its own site — **same-site exclusion** kills the spurious same-L2 primary↔standby link; primary AND standby both carry it → hub-symmetry, promotion changes nothing hub-side). A non-member LEAF compiles SPOKE posture: the remote subnets in the PRIMARY (members[0]) peer ONLY (the **single-AllowedIPs invariant**), each standby a **keepalive-only empty-AllowedIPs** peer.
- **Standby `/32` → EMPTY AllowedIPs** (all gateways share the pool-gateway `10.99.0.1`, so there is no distinct per-hub tunnel address): empty makes "no traffic ever rides the standby" STRUCTURAL (WG cannot crypto-route to a peer with no AllowedIPs) while keepalive keeps it warm + observable in `node_peer_status`.
- **The asymmetric pre-promotion link (paper line, for the walk's tcpdump):** a leaf's standby peer is empty-AllowedIPs; the standby's leaf peer carries the leaf's subnets (full transit posture). WG AllowedIPs is per-END local policy — the leaf DROPS anything the standby sends beyond keepalives until promotion flips its side. That drop is the warm-standby's SAFETY property, not a bug.
- **Version/hash — NO bump (argued both faces):** *for-bump* — standby peers are new topology an old agent can't fail over with; *for-no-bump (wins)* — a standby is just another peer; an old agent holds it as an idle empty-AllowedIPs keepalive peer and serves single-hub transit EXACTLY as today (degraded-not-broken). Structural: standby peers add NO routes (routes = remote subnets, peer-count-independent), so the CanonicalHash (routes+policy) is unchanged → no RequiredVersion bump. Red'd (leaf routes identical with/without pins).
- **Failover does NOT happen this slice** (boundary, as Slice 2): the order is static (members[0] primary) until Slice 4 wires the trigger. On the wire the only change is the added keepalive peers — dormant-machinery law SATISFIED because the warm tunnels are LIVE (keepalives flowing, handshakes in node_peer_status — Slice 1's substrate earning its keep immediately). Keepalive cost: N warm tunnels per spoke (peers × the S8.2b cadence).
- **Reds:** two-tier membership (capability gate · no-pins-single-hub · pins-exclude-leaf · pin>health · pinned-incapable-excluded · pinned-cross-site-enters) · generation (bump-on-change · idempotent-no-bump · restart-persist · **leaf-add-no-bump** · pin-changes-membership) · compiler HA walk topology (**leaf no-duplicate-subnet** — the trace immortalized · single-AllowedIPs invariant · same-site exclusion · hub-symmetry · **zero-config golden** single-hub · version/hash routes-identical). Existing site-link tests GREEN (byte-identical single-hub).
- **WALK SETUP STEP (manifest):** Pawan PINS the two AWS gateways (`SetHubPriority` service exists; the UI surface is Slice 6 — the walk exercises the real operator flow via the API). azure-gw stays unpinned (the leaf).

## Slice 4 — failover trigger (DESIGN LOCKED, builds FRESH; scope-call ratified 2026-07-20)
Builds fresh: the story's single most defect-prone shape — a stateful control loop with hysteresis,
testable only by scripted clock, on the highest-stakes path (the S8.4-arc profile). Design locked here;
next sitting is pure execution.

**Locked design:**
- **The tick rides the existing CP-loop precedent** (`main.go:216` runs a `time.NewTicker` goroutine for
  the accesslog sweep). The failover tick is the same shape: a ticker goroutine calling ONE
  `nodes.Service` method per interval.
- **Reads, never measures:** member freshness comes from `node_peer_status.last_handshake_at` (Slice 1) /
  `SiteLinkStale` — no new clock, no new health vocabulary. The tick asks ONE question: is `members[0]`
  stale by the D4 numbers? N=3 stale → re-elect (promote the next capable member), bump generation, push.
- **Asymmetric hysteresis:** promote after N=3 stale ticks; fail back only after the demoted primary reads
  M=5 fresh + the hold window. FAIL-BACK IS A PROMOTION EVENT — a membership-order change, generation bump,
  push; one mechanism, two directions, the asymmetry living entirely in the thresholds. Pin-restoration
  (the pinned primary resumes `members[0]` when healthy) IS the fail-back.
- **Promotion is COMPILE-ONLY:** re-election changes `members[0]` → the Slice-3 compiler moves the subnets'
  AllowedIPs → push → agents reconcile. No new push path, no failover-special messages. The demoted primary
  (if reachable) keeps its hub posture (hub-symmetry); only spoke-side AllowedIPs move.
- **In-memory tick state** (consecutive-stale counts, hold timers), rebuilt from stored freshness on a CP
  restart — conservative (a restart mid-window restarts the count, delaying failover by ≤N ticks, NEVER a
  spurious one). Dormant-machinery's cousin: no persistence for state the substrate re-derives.
- **Every transition audited:** promotion + fail-back land as audit rows naming old-primary → new-primary +
  the triggering condition (the operator's "why did my hub change at 3am" trail).

**ACTIVE-ORDER SEMANTICS (banked before building — two orderings is where one-truth dies):**
- The **CONFIGURED order** = `electSiteHubSet`'s output (pins + health + id) — the PERSISTENT INTENT, a pure
  re-derivable function.
- The **ACTIVE order** = the configured order with stale members SKIPPED under the hysteresis counts —
  **DERIVED, never independently stored as a second authority.** `org_hub_set.members` holds the active
  order; the configured order stays re-derivable (electSiteHubSet is pure). Every persisted change to
  `members` is a PROMOTION EVENT (generation bump + audit row) — NEVER a silent reorder.
- **THE CONVERGENCE RED (keeps the two orders from drifting into two truths):** active ≠ configured ONLY
  while a hysteresis condition holds; when ALL members read fresh, the orders CONVERGE (fail-back = the
  convergence event = the pin-restoration semantics already ruled).

**RESOLVE-AT-BUILD:**
- **Fencing location:** find where (or whether) the agent's apply path (`apps/node`) checks monotonicity
  today — likely the artifact's existing version/generation handling. Red proves a stale-generation
  artifact is refused, CITED at the enforcing line. **If NO existing monotonic check exists on the apply
  path → SURFACE IT before building the fence** (an assumed seam is this story's four-times-proven hazard).
- **The convergence red** above.

Then Slice 5 (spoke partition), Slice 6 (web + metrics cards), story-end review, hold at walk-ready.

## Slice 4 BUILT (2026-07-20) — failover trigger, boring-every-path
- **Item-1 fencing RESOLVED (surfaced, ruled CP-side):** no monotonic apply-check exists on the agent, and the trace shows none is needed (single-source + monotonic Watch; a demoted primary is inert, not a twin). Fence = the atomic `UpsertOrgHubSet` bump (CP-side). Deferred agent-side fence trigger recorded (a second artifact path). See the D5 correction above.
- **Compiler consumes the PERSISTED active order:** `loadSiteTopology` loads `org_hub_set.members` → `topo.hubMembers` (resolved gateway rows); `activeHubMembers` returns it (or falls back to `electSiteHubSet` when no row — a fresh org still compiles single-hub). `siteLinkGraphFrom` + `electSiteHub` read it. So a promotion (a change to the persisted order) flows through the ORDINARY compile+push — no failover-special path. Existing site-link tests GREEN (fallback = electSiteHubSet, byte-identical).
- **The controller (`failover.go`):** ONE ticker (rides the `main.go` retention-sweep pattern, `FailoverTickInterval`) → `RunFailoverTick` → per multi-member org, `failoverOrg`: read configured (electSiteHubSet) + member freshness (`node_peer_status`, reads-never-measures) → `FailoverController.Step` (in-memory per-`(org,member)` stale/fresh counts) → active order → if it differs from persisted, PROMOTION EVENT (atomic `UpsertOrgHubSet` bump + `hub_set.promotion` audit naming old→new + condition). N=3 demote (fast), M=5 restore (slow); active = configured with demoted moved to the back; convergence when all fresh (fail-back = convergence). In-memory counts rebuilt on restart (conservative: delays ≤N, never spurious).
- **Reds (all green):** convergence (active≠configured ONLY while demoted; all-fresh converges) · flap-exactly-ONE (oscillating primary → one failover, not a metronome) · restart-conservative (fresh controller needs N ticks again) · demoted-primary-INERT (post-promotion compile: the demoted primary's pubkey in ZERO subnet-carrying AllowedIPs — keepalive-only) · generation-fence (atomic bump: change→bump, same→no-bump, concurrent→monotonic) · promotion-audits (end-to-end: stale primary N ticks → demote + audit old→new+condition; <N no promote). Fixed `TestReportStatusNodePeerSibling`'s global-count test-isolation bug (now node-scoped).
- **Gates:** nodes/sites/http both editions · failover reds · codegen deterministic (ListFailoverOrgs) · apps/node untouched (compiler CP-side). Walk: the third-VM primary-kill leg exercises this on iron.

## Slice 5 — spoke-side partition: HONESTY + verification (D5 2nd/3rd amendment, 2026-07-20)
Founder-flagged HALT-before-build: the D5 spoke-partition prior ("a spoke uses its last-known preference
order against whichever hubs it CAN reach") was written under the ROUTE-METRIC model (a spoke holding
multiple usable paths, local preference arbitrating). Under (A)+(3) it is UNACHIEVABLE without violating
the architecture: a spoke's standby peers are EMPTY-AllowedIPs — WG crypto-routing cannot send subnets via
a standby unless someone rewrites the spoke's AllowedIPs, and the ONLY someone is the CP. A spoke-local
rewrite would be agent-side election — exactly what observe-never-vote (D1) refused and the CP-single-
authority fence (D5) re-proved the architecture lacks. **SURVEY CONFIRMED:** `reconcile.go:242` — `runOnce`
→ `Reconcile(ctx, ds.Peers)` → `ApplyPeers(ctx, desired)`; the agent applies the CP's peers VERBATIM. No
local election, no AllowedIPs mutation, no promote path anywhere in `apps/node`. So Slice 5 builds NOTHING
new — it is honesty + absence-proofs.

**THE PARTITION MATRIX (D5 second amendment, → docs):**
- **(a) spoke↔CP up, primary dead** → the Slice-4 path: CP re-elects, pushes, the spoke re-compiles onto the
  standby. Converges.
- **(b) spoke can't reach CP, primary alive** → FAIL-STATIC, traffic flows — the ordinary CP-outage case,
  proven since S3.1. A standby present doesn't perturb it.
- **(c) spoke can't reach CP AND primary dead, standby reachable** → that spoke is OUT until CP reachability
  returns. The warm standby keepalive still handshakes (observability survives) but carries NOTHING, by
  design — no CP → no re-election, per-spoke. This residue joins "CP-down = no failover" as ONE family:
  **failover requires CP reachability, per-spoke.**
- **(d) total partition** → out, trivially.

**THE ORIGINAL D5 RED WITHDRAWN (third amendment):** "reachable-standby carries traffic while CP
unreachable" is FORMALLY WITHDRAWN — it assumed a mechanism the chosen data plane doesn't have (the same
cause as the D1-keying and D2-metric drifts; the SIXTH model-vs-ruling catch, this one PRE-EMPTED by the
founder rather than surfaced by the build).

**REDS (absence-proofs, apps/node, all green):**
- `TestReconcileAppliesDesiredVerbatimNoLocalPromotion` — the dead-code assertion INVERTED: the agent
  applies the CP's peers verbatim; the standby stays EMPTY-AllowedIPs; NO local path moves subnets onto it
  (spoke-side failover is impossible by the absence of any local-election code).
- `TestReconcileFailStaticKeepsStandby` — case (b): a CP outage does NOT change the applied peers (a
  standby present is unperturbed); the spoke keeps routing to the primary, never locally fails over.
- Case (c) standby-keepalive-continues = WG persistent-keepalive behavior → the walk's tcpdump (the
  observability half survives the partition).

## Slice 6a — the operator-surface BACKEND (2026-07-20); 6b UI builds FRESH (tail-of-turn on UI)
- **`PUT /organizations/{orgId}/nodes/{nodeId}/hub-priority`** (`setHubPriority`) — the deferred pin endpoint. **site:manage** (topology-consequential — pinning creates the HA set). Audited **old→new** (`node.hub_priority_set`, enhanced from new-only via `GetNodeHubPriority`). 401-walk covers it.
- **`GET /organizations/{orgId}/hub-set`** (`getHubSet`) — the persisted active order + generation + per-member L1 metrics. **org:view** (MEMBER-readable — the D5/S8.3 read-only-topology precedent; the pin control is manage-gated). ONE truth — the same `org_hub_set` the compiler + health read.
- **`GetHubSetView` service:** ordered members (role: primary=members[0]/standby) + generation + per-member metrics from `node_peer_status`. **Render-floor storage-cited:** metrics PRESENT only when a `node_peer_status` row exists (an idle link = a row with rx/tx=0 — honest); a NOT-reporting link = NIL metrics (absent ≠ zeroes-as-data). The L1 fork-ruling condition honored (the view reads the STORAGE schema, not the report).
- **Reds:** `TestGetHubSetView` (ordered role · idle-reporting → metrics-with-zeroes · not-reporting → NIL metrics · generation served) · 401-walk (setHubPriority). Web typecheck+build green (new generated types).
- **6b (web UI) — FRESH:** the pin control on the gateway card (set/clear, one-sentence HA-opt-in consequence) · the hub-set render (ordered candidates, PRIMARY badge on members[0] evolving the HUB badge, STANDBY + warm/handshake state, generation as version tag) · promotion-visible (active≠configured → the demoted member's stale/demoted state + audit-trail link) · the L1 byte-counter cards (bytes tx/rx + handshake age per member, cumulative-since-handshake, refresh-on-load, absent→absent). Render-floor: all stored truth. Member-readable, pin manage-gated, no dead affordances. Deferred to a fresh sitting per the tail-of-turn rule (UI is the most-dangerous class).

## Slice 6b BUILT (2026-07-20) — the web HA surface + pin control + L1 cards
- **`hubsetview.ts` view-model (PURE, sitesview precedent):** `hubSetView(hubSet, nowMs)` → ordered rows (role primary/standby, warm/stale from the handshake clock, L1 bytes + handshake age) + `promotionInEffect` (the active primary ≠ the configured primary = the lowest pin) + per-member `demoted`. Render-floor: a reporting member shows its metrics (idle = 0 B, honest); a NOT-reporting member shows "—" (absent, NEVER 0). `formatBytes` raw gauge. `HubMember.hub_priority` added to the GET (6a) so the UI has the configured order.
- **`HubSetSection` (Sites page):** hub set render — PRIMARY badge on members[0] (evolving the HUB vocabulary) + STANDBY + warm/stale + L1 counters + generation as the version tag; a **failover-in-effect banner** (promotionInEffect) naming the demoted member + the `hub_set.promotion` audit pointer ("why did my hub change"). Member-READABLE; the **pin control is manage-gated** (set/clear hub_priority via the PUT, next-available priority, one-sentence HA-opt-in consequence at the control). No dead affordances (member sees the read surface, no pin buttons).
- **Reds:** `hubsetview.test.ts` (7 — null-when-unpinned · ordered role + generation · **idle→0 B vs not-reporting→"—"** · warm fresh/stale · **promotion-in-effect + demoted flag** · no-promotion-when-aligned · formatBytes) — the view-model precedent.
- **Gates:** web typecheck + 143 tests + build · nodes both editions · codegen deterministic (`hub_priority` on HubMember through the pipeline) · gofmt clean.

**S8.6 build COMPLETE (Slices 1–6).** NEXT: story-end multi-finder review (probes pre-loaded) → fold → hold at walk-ready → S8.7 commit-one.

## STORY-END REVIEW — 10 findings dispositioned (2026-07-20)
20-agent high review. The correctness cluster #1/#2/#4 = ONE REDUCE (the one-truth law applied to hub
election, properly). #3/#5 independent fixes. #6–#10 fold. Accountability: the Slice-4 ruling claimed
"the compiler consumes the persisted active order — one truth" and the gate was accepted on that WITHOUT a
census of every hub-election site. The policy compiler's lowest-id election (S8.3-era) — its own comment
"MUST match siteLinkGraphFrom" was the tripwire — was never re-walked when Slice 4 changed what
siteLinkGraphFrom means. The six-fork probe aimed at paper-vs-code drift; this miss was CODE-vs-CODE. The
review outranks the gate.

### REDUCE PAPER — one hub-election authority (decision-first; build FRESH after ruling)
**Root:** three hub authorities disagree — the enterprise policy compiler (`compiler.go:140`, lowest-id,
places the transit forward-chain grant), the data-plane compiler (`siteLinkGraphFrom`, `activeHubMembers[0]`),
and `GetHubSetView` (raw persisted). Under HA (pin a non-lowest-id / failover) the grant and the routing
diverge → **enterprise-enforcing cross-site BLACKHOLE** (#1); and `ReconcileHubSet` (configured) clobbers the
controller (active) (#2); and a deleted member silently shifts the primary (#4).

**THE OWNERSHIP MODEL (ruled — the paper instantiates it): ONE ROW, TWO FIELDS, ONE DERIVATION.**
- `org_hub_set` carries **`configured uuid[]`** (ReconcileHubSet's output — pins/capability/order) AND
  **`demoted uuid[]`** (the controller's output — members currently promoted-past). The **ACTIVE order is
  DERIVED** by ONE shared function `deriveActive(configured, demoted)` (non-demoted in configured order, then
  demoted at the back) — **never stored as its own authority, never computed a second way.**
- **Writer partition (resolves #2 at the root):** `ReconcileHubSet` writes `configured` ONLY; the controller
  writes `demoted` ONLY. Disjoint fields → no clobber. A bind/pin DURING a failover updates membership while
  the demotion stands until the controller's own fail-back clears it. Each field's upsert bumps the
  generation atomically (`... IS DISTINCT FROM ...`) — a superset of "active changed" (a demote/un-demote
  ~always changes active; the rare no-op-field over-bump is safe — it's a version tag).
- **THE CENSUS (exhaustive, red'd) — every consumer reads the one derivation:** (a) data-plane compiler
  `siteLinkGraphFrom` via `activeHubMembers` → `deriveActive`; (b) **policy compiler `hubNode` (#1 fix)** —
  the caller passes the DERIVED active hub into the compiler input; the transit grant lands on it; the
  lowest-id election + the "MUST match" comment are DELETED (a "must match" comment is an apology for a
  missing abstraction); (c) `GetHubSetView` → `deriveActive`; (d) the controller tick reads `configured` +
  `demoted`, derives, writes new `demoted`. Grep-level proof: NO election logic survives outside the shared
  function.
- **#4 folds as a MEMBERSHIP EVENT, not a read-time skip:** a member ceasing to be a gateway (unbind/delete)
  is a `configured` change → the unbind/delete paths trigger `ReconcileHubSet` (unbind does; **verify delete
  does — red it**) → generation bump + audit. `loadSiteTopology` then never skips (reads the persisted
  `configured`); a stale row is a reconcile-owed transient the trigger coverage closes.
- **THE #1 RED (the blackhole as a fixture):** pinned non-lowest-id primary, enterprise enforcing → the
  transit accept sits on the ACTIVE primary, cross-site traffic FLOWS; + the failover variant (post-promotion
  compile → the grant moved with the subnets). **#4 reds:** delete a standby → generation bumps, view +
  compiler agree; delete the primary → promotion-shaped event, audited, never silent.

**WRITER-OWNERSHIP LAW (candidate):** a persisted authority with MULTIPLE WRITERS must PARTITION its fields
by writer; two writers to one field is the clobber class (the counter-not-in-failing-tx family's structural
sibling).

### #3 FIX (independent) — UnbindSiteNode takes an explicit node id
Dropping `nodes_site_id_uniq` made `GetSiteNode` (`:one`, no ORDER BY) nondeterministic on a multi-gateway
site → unbind detaches an arbitrary gateway. FIX: `UnbindSiteNode` takes the explicit node id (the caller
always knows which). Every remaining `:one` against a now-plural relation either takes an id or gains a
deliberate ORDER BY with stated semantics. Red: two-gateway site → unbind removes exactly the named one.

### #5 FIX (independent) — promotion + audit in ONE transaction
The promotion upsert (`UpsertOrgHubSet`, auto-commit) and its `hub_set.promotion` audit are separate → a
durable-but-unauditable transit switch (swallowed-audit law at its highest-stakes seam). FIX: both in ONE
`withTx` — an in-tx audit error ROLLS BACK the promotion; the next tick retries. Red: audit-write failure →
no silent generation bump; the promotion did not happen.

### FOLDS — #6 (counts pruned on membership change; the demoted-set-in-row largely dissolves the in-memory
staleness) · #7 (the tick loads only gateways, one GetOrgHubSet) · #8 (the `freshestByKey` fold extracted to
ONE shared helper — rides the census extraction) · #9 (ONE byte formatter, the divergent twin deleted) · #10
(one `siteTopoLoad` nil-guard convention).

**SEQUENCE:** reduce paper → **founder ruling** → build (reduce + #3 + #5 + folds) → gates → targeted
re-review over the fold diff (feature-sized on the highest-stakes path — undiscounted; probes: census
completeness [grep-proof no election outside the shared fn] · the two-writer field partition under concurrent
bind+failover · #5's rollback red · the stacked-branch seam re-verified) → hold at walk-ready → S8.7
commit-one.

**HELD for the founder's ruling on the reduce paper before any build.**

### BUILD MAP — the threading seam (discovered pre-build; condition 2 refined)
Condition 2 named "the nodes-service call site" (SINGULAR). The code has THREE compile passes, each an
independent (topo, snapshot) pair that today elects its own hub:
- `DesiredState` (service.go:260) — `s.policy.CompiledForNode` at :298 (the served policy) + `siteLinkGraphFrom`
  at :360 (the served graph). **The policy compile runs BEFORE the topo load (:347).**
- the single-node pushed-hash path (service.go:971) — `CompiledArtifactsForNodes` + `finalizeArtifact`.
- the batch pushed-hash path (service.go:1180) — `CompiledArtifactsForNodes` + per-node `finalizeArtifact`.
And BOTH policy methods (`CompiledForNode`, `CompiledArtifactsForNodes`) run `Compile(snap)` off
`BuildSnapshot`; the hub election is INSIDE the compiler (`compiler.go:130`, lowest-id, the "MUST match" comment).

**The faithful realization of "one derivation per compile, fed to both" (condition 2 honored, not widened):**
- `deriveActive(configured, demoted)` lives in `nodes`; the active hub = its head, derived ONCE per pass from
  the pass's already-loaded `topo`.
- The policy port gains the active hub as INPUT (not the row): `Compiler.Snapshot` grows an `ActiveHub
  uuid.UUID` field; `compiler.go` uses `snap.ActiveHub` and the lowest-id election + "MUST match" comment are
  DELETED (the pass-it-in strengthening — the compiler is structurally incapable of electing). The two port
  methods take the active hub from the caller and set it on the snapshot before `Compile`.
- `DesiredState` restructures: load `topo` BEFORE the policy switch (for a site node), derive the active hub
  once, thread it to `CompiledForNode` AND `siteLinkGraphFrom`. The fail-closed switch semantics are
  PRESERVED byte-for-byte; only the load moves up. (A non-site node: no site grants land on it; active hub =
  Nil, no election either way — the transit grant only appears in the HUB node's artifact, and the hub is a
  site gateway whose SiteID.Valid path loads topo.)
- The two pushed-hash paths already hold `topo` (they call `finalizeArtifact(topo, ...)`) — derive the active
  hub there once and thread to `CompiledArtifactsForNodes`.
- Red (condition 2's): one compile pass yields data-plane routing AND policy transit grants citing the SAME
  hub, by construction of the single threaded value — assert the served `ds.Peers` hub and `ds.Policy`'s
  transit-grant node are the identical id, and flip the active hub (failover) → BOTH move together.

**Blast radius:** the enterprise enforcement port (2 methods) + the fail-closed `DesiredState` switch + two
pushed-hash paths. This is the most-dangerous code (enforcement + fail-closed) — the build earns an
undiscounted re-review over the fold diff (already in the sequence) and is NOT a turn-tail build.

### BUILD OUTCOME — reduce BUILT + gated (commits 6d94b79, 1bf1e47)
The reduce shipped as mapped. Migration 0038 (members → configured + demoted), deriveActive (the ONE shared
derivation), field-partitioned writers (UpsertOrgHubSetConfigured / UpsertOrgHubSetDemoted, per-field atomic
generation bump), loadSiteTopology derive-then-filter, the policy-port ActiveHub thread across ALL THREE
compile passes (DesiredState loads topo before the fail-closed switch; both pushed-hash paths thread
electSiteHub/b.hubID) with the compiler's lowest-id election DELETED, the failover controller repartitioned
(Step returns the demoted SET; failoverOrg writes demoted only + promotion/failback audit in one tx, #5),
ReconcileHubSet audits hub_set.membership (its own kind, #4/condition-1b), UnbindSiteNode explicit-node-id
(#3), #8 latestByPubKey shared helper.

**Reds (all green):** TestSiteTransitGrantHonorsThreadedActiveHub (#1 — grant follows the derived hub, never
lowest-id) · TestReconcileHubSetMembershipAudit (#4 — configured change bumps + audits membership, view
agrees) · TestUnbindSiteNode (#3 — the named gateway unbinds, the other stays) · TestFailover* + the
promotion-audit integration (#5 tx coupling) · the web hubsetview + sites suites.

**Gates:** generate-check up-to-date · build-editions both (exit 0) · test-node + test-helper +
helper-crosscompile green · web typecheck + 143 tests + build green · test-editions: every reduce-relevant
test passes; the residual failures (TestCA*, TestNodeEnrollmentLifecycle, TestOrg*, TestSessionlessRequests)
are the documented seeded-DB env issue (4597 seeded orgs → open-edition org-limit; CA master-key mismatch —
the decrypt-CA-key signature), zero overlap with the reduce's surfaces.

### TWO DECISIONS RESOLVED FORWARD (surfaced, reversible)
1. **#2 landing = (a)** named audit-action constants (hub_set.promotion/failback/membership) via the standard
   audit() path — because verify found NO typed AuditAction registry or lint exists in the product (all audit
   actions are bare string literals). The registry #2's disposition named is a mis-premise. A real typed
   registry + lint is a cross-cutting refactor = its OWN story (REGISTER: audit-action typing). (a) is the
   honest minimum (crown-jewel events are named symbols, in sight) and forecloses nothing.
2. **#4 revoke-path GAP (surfaced, NOT silently expanded):** RevokeNode sets status='revoked' but does NOT
   clear wg_public_key/site_id, and ListSiteGatewaysForOrg filters only on `site_id IS NOT NULL AND
   wg_public_key <> ''` (NO status filter) — so a revoked gateway stays a hub candidate, and revoke neither
   drops it nor reconciles. The CLEAN covered membership-removal path is UNBIND (clears site_id → drops from
   the list → derive-then-filter → reconcile+audit), which #4's red exercises. The revoke path is a decide-item
   for the founder: add `status='active'` to ListSiteGatewaysForOrg + a RevokeNode reconcile trigger (touches
   S8.2 shared machinery), or defer as a named follow-up. HELD — not resolved unilaterally.

### GIT-TRUTH CENSUS + RE-BASELINE (2026-07-20 protocol finding)
The story train re-baselined against `git log --oneline` (5daf729 → story/S8.6-hub-ha HEAD; NO S8.7 branch).
Every previously-reported gate mapped to its backing commit, or marked BELIEF-ONLY:

| Reported gate / acceptance | Backing commit(s) | Verdict |
|---|---|---|
| S8.5 walk-ready (held) | 5daf729 + train | REAL |
| S8.6 commit-one paper | ee91e27 | REAL (paper) |
| S8.6 Slices 1–6 built | 6d8477d f4f4db6 ccc6ce3 329faec 496cd56 2d6573a bbf3cea | REAL |
| S8.6 reduce paper approved | fba643c | REAL (paper, not code) |
| S8.6 build-map | 440be19 | REAL (paper) |
| **"S8.6 walk-ready CLOSE"** | — | **BELIEF-ONLY** — reduce unbuilt; #1 blackhole LIVE in-branch |
| S8.6 reduce BUILT + gated | 6d94b79 1bf1e47 599409e | REAL (shas) |
| **"S8.7 commit-one RULED"** | — | RULINGS STAND; **gate VOID** (no branch) |
| **"S8.7 Slice 1 ACCEPTED" (warn-not-refuse)** | — | RULING STANDS; **gate VOID** (no commit) |
| **"S8.7 Slice 2 GO"** | — | premised on a phantom Slice 1 |

Void where no commit backs it: the S8.6 walk-ready CLOSE, the S8.7 commit-one gate, the S8.7 Slice-1 gate.
The RULINGS (warn-not-refuse and all S8.7 D1–D6 dispositions) stand as rulings — design decisions survive;
no gate is passed until the census shows the commit. S8.7 restarts from its ruled commit-one on the real
base; the code gets built + gated with shas.

**MECHANISM (one honest paragraph, not blame):** the gap was produced by narrative momentum across a
context boundary. The reduce was fully dispositioned and papered (`fba643c` reduce paper, `440be19` build
map) — a complete, confident PLAN. A compaction summary then carried that plan forward as accomplished state,
and each subsequent turn's "next" built on the prior turn's asserted-done, so gate-shaped language ("walk-
ready close", "Slice 1 accepted") attached to work that had a paper but no commit. The producer half: several
gate reports omitted the sha, and a sha-less report reads identically to a real one — so the drift was
invisible, not hidden. The acceptor half (founder-owned): gates were accepted without the sha demanded as a
hard field. Both halves are closed by the GATE-REPORT-NEEDS-SHA law (docs/laws.md): sha in the report, or
it is a plan.

### REGISTER — audit-action typing (post-EPIC-8, hygiene class)
The typed AuditAction registry + lint (the correct form of #2, deferred because it is a PRODUCT-WIDE latent
finding — every audit action is a bare string literal, not S8.6's to fix) registers as its own named story:
**audit-action-typing** — an `Action` type + the known-action set + a vet analyzer, replacing bare-string
audit calls across the codebase. Trigger: post-EPIC-8 (hygiene). S8.6's landing (a) — named constants via
the standard audit() path — is the correct same-class interim (crown-jewel events are named symbols, in
sight) and forecloses nothing.

### RE-REVIEW FOLD OUTCOME — S8.6 WALK-READY @ ae2405b (2026-07-20)
The targeted re-review over bbf3cea..70c3002 found 13 verified (0 refuted). Founder-dispositioned + folded
(commits b13863c fold, ae2405b law clause):
- **1/3/4/5 = ONE REDUCE (the failover-tick corrector):** the tick re-derives configured from the live
  election every pass (electSiteHubSet), carries demoted (hysteresis, rehydrated on restart), derives active
  (deriveActive) — one loop owns the whole reconciliation; every removal path covered by construction;
  event-triggers = belt-and-suspenders. Writer-ownership law gains the two-writers-one-pure-function clause
  (docs/laws.md). #1 seedDemoted rehydration; #3 GetHubSetView derive-then-filter; #4/#5 corrector heals.
- **#2** migration nulls demotion honestly (corrector re-converges within N ticks; release-note). **#6**
  unbind body OPTIONAL (bodyless → sole gateway, 409 multi). **#7** per-member transition kind. **#8** one
  read/tick. **#9** revoke reconcile gated on gateway-ness.
- **Reds (green):** rehydration-persists-demotion · corrector-heals-configured-in-one-tick ·
  view-filters-phantom · corrector-idempotent-no-gen-churn · bodyless-unbind. The scoped verify rides these
  green reds (no third full pass — the fold did not surprise).
- **GATE REPORT @ ae2405b (sha-first, per the law):** build-editions both (exit 0) · nodes/sites/policy
  reduce+fold tests GREEN (14 nodes + sites + policy) · web typecheck + 143 tests + build · test-node +
  test-helper + helper-crosscompile · generate-check up-to-date. test-editions residual failures =
  TestNodeEnrollmentLifecycle/TestCA*/TestOrg* = the documented seeded-DB env issue (4597 orgs + CA
  master-key), zero reduce overlap.

**S8.6 HOLDS AT WALK-READY @ ae2405b.** NEXT (S8.7): the census VOIDED the S8.7 Slice-1 gate (belief-only) —
Slice 1 (enum+compiler+validation) must be BUILT before Slice 2, contra the completion sequence's start at
Slice 2. S8.7 = commit-one (ruled: D1 warn-not-refuse, D2 no-bump, D3 modal, D4 conntrack-mechanism-verify,
D5 one-fn-two-triggers, D6 device-revoke-exempt) → Slice 1 → Slice 2 (conntrack, D4 verify front-loaded) →
Slice 3 (modal) → review → walk-ready. Then the batch-day manifest.

---

## S8.6b — wg0→wg0 hub-transit DOCKER-USER accept (WF-4's third orientation) — commit-one

**Origin:** the EPIC-8 batch walk (2026-07-21) A3 leg. A split-tunnel DEVICE reaching a REMOTE site THROUGH
its hub gateway (`apple` 10.99.0.3 → `10.0.0.0/16` behind azure-gw, via the aws-gw hub) is DROPPED on a
Docker-host hub: the packet arrives at aws-gw wg0, the tunnex grant counter stays 0, azure-gw receives
nothing — Docker's `filter FORWARD` policy DROP swallows the **wg0→wg0** transit (device-peer wg0 → azure
site-link wg0). Characterization (S8.2c walk-record leg list, git-verified): device→remote-site-via-hub was
**NEVER wire-proven** — every S8.2/S8.2c forwarding fixture was an eth0-side behind-HOST, and both DOCKER-USER
accepts that exist cover the tunnel↔LAN edge (WF-4 route `eth0→wg0`, WF-4-local fold `wg0→eth0`). wg0↔wg0 was
never covered because nothing exercised it. A **never-worked path on Docker-host hubs, not a regression** —
and it blocks Deck B (failover routes device traffic through hubs on every leg).

### D-transit-1 (LEAD, RULED B) — relax the route rules' orientation predicates; NOT a second marked set

The two orientations (`eth0→wg0` route, `wg0→wg0` transit) were never two policies. They are ONE policy:
*"Docker must not structurally drop traffic whose daddr/saddr is a Route — the ZT chain adjudicates it."*
The separate-rule model (a `iifname wg0 oifname wg0 daddr∈Routes` accept) **COLLIDES**: it re-derives to the
same `d:route`/`s:route` key as the existing route rules (the idempotent key + `dockerUserRuleRE` capture
daddr/saddr only, NOT iif/oif) → the map holds one entry per key, the sweep can't tell the orientations
apart → broken idempotence + orphans. Building a second marked-set engine (distinct comment) to preserve a
distinction that carries **no security content** is the wrong shape: the enforcement boundary was never the
DOCKER-USER match — it is the `ip tunnex` chain (D-transit-3). So narrowing Docker's match by iif bought
nothing and cost a collision.

**RULED (founder): (B) — relax the existing route rules.** Drop the ingress/egress predicates so the one
rule says what the policy means:
```
forward: oifname wg0  ip daddr <route>     # was iifname != wg0 → now covers eth0→wg0 (route) AND wg0→wg0 (transit)
return:  iifname wg0   ip saddr <route>     # was oifname != wg0 → covers wg0→eth0 AND the wg0→wg0 reply
```
Keys stay daddr/saddr-only (the policy is daddr/saddr-only); the map, sweep, regex remain ONE engine. The
WF-4-local rules (daddr=LocalSubnet, disjoint from Routes) are UNCHANGED. Same lesson as metric-vs-AllowedIPs:
cite the layer that actually decides — Docker's chain decides structural drop, the tunnex chain decides
permission, the accept's job is only the former.

**WHY THE PREDICATES WERE THERE (history, for the future reader — narrowing-was-incidental):** the WF-4 fixes
were minimal-scope responses to specific walk findings — S8.2c found eth0→wg0 (route), S8.5 found wg0→eth0
(local), each honest at its moment and each scoped its accept to exactly the direction its fixture exercised.
The `iifname != wg0` on the route forward was never a security predicate — it was "the direction this walk
proved," frozen into the rule. The third orientation (wg0→wg0) revealed the union was the true shape all
along. **A future "security tightening" PR MUST NOT re-add the iif/oif predicates** — doing so silently
re-breaks transit while the ZT chain (the real boundary) is untouched. This paragraph is the guard rail.

### D-transit-2 (OPEN — needs ruling) — the TRANSITION mechanism (the sweep-hygiene red's engine)

The relaxed rules share the `d:route`/`s:route` key with the OLD orientation-predicated rules a pre-fold agent
placed. Key-only idempotence sees the key present → **skips the insert → the OLD rule lingers → transit stays
broken after upgrade** (live for the walk: aws-gw currently runs the WF-4-local fold, whose route rules are
still `iifname != wg0 …`). Clean convergence (old swept, relaxed placed, one pass, no orphan) needs a
mechanism, and each trades against a stated constraint:

- **(1) drift-detection** — capture each current rule's iif/oif signature (extend `dockerUserRuleRE`),
  compare to the desired render; on mismatch delete+reinsert. HONORS daddr/saddr-only keying + no-orphan (one
  pass), one engine. COST: the regex/fake must capture + normalize an orientation signature (format-fragile).
- **(2) marker-version key** — bump the comment to `tunnex-fwd2`, key by (marker, dir, addr); old-marker keys
  ∉ desired → swept, new placed, one pass. CLEANEST convergence. COST: keys gain a marker namespace (diverges
  from "daddr/saddr-only," though the version is a migration artifact, not a policy distinction).
- **(3) boot-clean** — on startup delete all comment-marked DOCKER-USER rules, re-place fresh. Simplest code.
  COST: a brief orphan window (diverges from "no orphan window").

**RULED (founder): (1) drift-detection** (from "one-pass convergence (sweep-hygiene red)"). BUILT: `current`
carries an orientation SIGNATURE per key (`orientSig`/`argOrientSig`, spaces+quotes stripped so nft's printed
form and our insert-args normalize identically); the insert loop, on a key present with a DIFFERENT signature,
deletes the stale-orientation rule then inserts the relaxed form — one pass, no orphan. `dockerUserRuleRE`
gained a leading capture group for the iif/oif prefix. Honors both constraints (daddr/saddr-only keys +
no-orphan); the fragility is contained to `orientSig` + the fake's renderer, pinned by
`TestDockerForwardTransitionConverges`.

### D-transit-3 (RULED) — the accept opens Docker's drop ONLY; ZT still adjudicates

The relaxed accept lifts Docker's structural FORWARD DROP; it does NOT grant. Under enforcing the `ip tunnex`
chain default-denies without a grant. A3's walk evidence (grant counter 0 = dropped before the chain) inverts
to the proof: post-fold the counter increments and the grant gates.

### Reds (staged)

1. **spoke-isolation (carries the fork's weight):** a device→device-pool-daddr packet at the hub's forward
   chain is untouched by the relaxed accept — pool ∉ Routes, so no accept's daddr/saddr is a pool addr
   (structurally). The S8.2 D1 spoke-isolation sibling, now the DOCKER-USER tier. Proves (B)'s widening
   opened nothing that matters.
2. **ZT both faces:** enforcing-no-grant → dropped AT the tunnex chain (counter shows it); enforcing-with-
   grant → flows (counter-0 inverts to counter-increment).
3. **failover-symmetry:** the promoted standby renders the SAME DOCKER-USER set for the same Routes (the
   accept derives from Routes; Routes ride the compile; the standby's artifact carries the same Routes).
4. **sweep-hygiene / transition (D-transit-2):** old orientation-predicated rules converge to the relaxed
   forms in ONE reconcile pass — old swept, relaxed placed, no orphan window.
5. **idempotence re-verified** against the relaxed render (second reconcile inserts nothing).

**Sequence:** ruling on D-transit-2 → paper folded → build → reds → gate on S8.6 → S8.7 rebases → git-verified
shas reported → A3 re-proof on the box (apple → azure LAN via the hub, zero manual rules, both enforcing
faces) → Deck B.

---

## Post-walk fold — failover-does-not-fire (Deck B, merge-gating S8.6)

Deck B killed the primary hub; 5 blind minutes, zero demotion. Investigation
(walk-artifacts/S8.6/INVESTIGATION-failover-does-not-fire.md) traced the liveness
substrate and, against an iron datum, REFUTED the first theory (a node_peer_status
writer clobber) and CONFIRMED the root = **the stale window**.

### D1 (RULED) — spoke-observed liveness is the ONE truth for hub-member death
Liveness is testimony from the living: a member's OWN report proves it can reach the
CP, never that its data plane serves. A dead hub's peer handshakes freeze (no living
peer can complete a new handshake with a corpse) — the spokes' observations are the
only signal that MOVES when the subject dies (the S7.4b class). Already the model:
`freshness[id]` reads `latestByPubKey` over node_peer_status, keyed by the member's
pubkey — cross-observations by OTHER nodes. Quorum: with one spoke, that spoke IS the
verdict; with N, any-fresh = alive (over-report on death, damped by the N=3
hysteresis). No-spokes edge: hubs with zero living observers = NO verdict, no
demotion possible (honest; joins the CP-down residue family).

### ROOT (RULED) — the window, not a writer bug
`failoverStaleWindow` was 90s. WireGuard renews handshakes on the REKEY_AFTER_TIME
(120s)/REJECT_AFTER_TIME (180s) cycle, so a LIVING link's observed handshake age
sawtooths up to ~180s; the box-walk saw healthy steady-state ages 1:29–3:22 (to
202s). A 90s window marked living hubs dead every cycle (the #4 false-stale badge)
and the rekey kept resetting the consecutive-stale counter, so a real death fired
erratically. Fix: window → **240s**, DERIVED (REJECT_AFTER_TIME 180s + report/tick
slack, above the observed 202s ceiling), derivation cited in-code. Relationship to
apps/node `siteLinkStaleWindow` (180s): NOT one shared constant — deliberately
distinct across the api↔node module boundary, same protocol cadence, tuned in
OPPOSITE error directions (the agent's 180s is over-report-safe for a health BADGE;
the CP's 240s is under-report-safe for a DEMOTION, the costlier error) — paired
cross-reference comments so neither drifts silently.

### D2 (REFUTED by iron) — no writer clobber
node_peer_status PK is (reporter, peer): each observer's row is distinct by
construction; the EXISTS guard blocks self-rows; latestByPubKey takes MAX across
observers. Iron query showed distinct coexisting observer rows. No clobber.

### NULL / no-spokes (RULED) — three-state: no observer, no verdict
`Step` now distinguishes fresh / stale / ABSENT. A member with no VALID observation
(no spoke peers it, or only NULL/never-handshaked entries — the same-site hub pair)
is absent from the freshness map; Step HOLDS its counters (no demote, no restore on
silence). Demotion fires ONLY on a PRESENT-but-STALE observation. NULL rows never
enter `latest` (Valid guard), never poison the MAX. Reds lock both.

### D3 (RULED) — one derivation, both consumers
failoverOrg and GetHubSetView both read `latestByPubKey`; the #4 flicker dies with
the window fix (same root). Badge red rides.

### Defect A (RULED IN) — observability
A controller whose decisions are invisible is unfalsifiable; the walk paid five blind
minutes. Added: ONE structured `failover_tick` line per multi-member org per tick
(per-member observed-age/verdict/counter + the decision), at Info, so the rematch
confirms the ticker live and reads WHY it fired-or-not from CP logs at the default
level BEFORE the kill. AND the swallowed per-org `slog.Warn` becomes a surfaced
ERROR + `errors.Join` return (finding-(b) made permanent otherwise). Diagnosis
machinery for a live decision loop — NOT dormant lifecycle code (dormant-machinery
law does not bite).

### D4 (RULED) — bind-2nd-gateway UI
Sites.tsx `hasGateway ? Unbind : Bind` was the list-of-one assumption in the action
path: the 2nd gateway (an HA hub pair) was unreachable via the product's own UI.
Now Bind and Unbind are independent — Bind shows whenever an unbound gateway exists.

### Reds (built, unit — SUBSTITUTE)
- window clears WG cadence: healthy 2–3min ages → ZERO demotions (the flicker
  fixture inverted).
- no-observer-no-verdict: an unobserved member never demotes on silence; a
  present-but-stale one still demotes.
- NULL-handshake guard: never enters `latest`, never poisons MAX.
- (existing) clean-kill promotion+audit (integration, DB-gated).

### The proof (SATISFIES — trigger: Deck B rematch on iron)
Unit reds SUBSTITUTE; they do not SATISFY. The Deck B rematch SATISFIES:
CP verified at the fold sha + the `failover_tick` line confirmed flowing pre-kill;
kill = docker stop + ip link del wg0 PLUS azure-gw `wg show` confirming aws-gw's
handshake age climbing post-kill (the freeze, live) before the stopwatch; then
kill→N ticks→promotion (banner + generation + audit) → first surviving behind-host
ping via the promoted standby; the enforcing face (transit grant counter on the
standby's chain); fail-back on restart (M=5 + hold → convergence, audited).
Conclusive by construction: if it non-fires with tick lines flowing, the lines say
why in one tick.

---

## A3b — device-pool cross-site transit (WF-1 + census PD-1..PD-4) — commit-one

**Origin:** Deck D Leg 10 (2026-07-23, walk-artifacts/S8.6/DECK-D-leg10-client-connect-C5.md).
A split-tunnel device (10.99.0.2) reaching a REMOTE site through its hub is dropped at the far
gateway's **wg crypto-routing tier** — the spoke's hub-peer AllowedIPs carry remote SITE subnets
only (`siteLinkGraphFrom`, routeCIDRs); the device pool is compiled into NO site-link peer.
Below it, Docker's FORWARD DROP eats pool-saddr/daddr traffic even on the hub's own site
(pool ∉ Routes — S8.6b red #1 proved the exclusion deliberately). Wire-proven fixable: manual
`wg set` pool allowed-ips + manual DOCKER-USER pool accepts + cloud pool return-routes → full
4-leg path alive (~7 s, until reconcile correctly reverted the mutation). A NEVER-worked path,
not a regression — S8.6b's fourth orientation.

**FOUNDER-RULED (scope + shape):** FULL A3b now — the narrow hub-lane patch leaves the far
gateway's cryptokey rejection to resurface as a new finding on main, failing the smooth-walk
bar. Shape = **(b) widen-AllowedIPs + far-grants**, NOT NAT-at-hub: (i) the un-NAT'd invariant
is a walked, wire-proven property (S8.2) — machinery bends to invariants, not invariants to
machinery; (ii) per-device attribution at the far gateway is the ZT substance — NAT collapses
every device into the hub's identity and blinds the far gateway's grant counters (C3-class
evidence); (iii) the compiler one-derivation machinery is what every fork this epic
strengthened. **Escape hatch (pre-agreed, budget rule):** if the build spawns a defect
cluster, fall back to NAT-at-hub as interim with (b) registered — a one-message close.

### Decide-items (decision-first; leans marked, HELD for ruling)

**D-A3b-1 — AllowedIPs grain at the far gateway.**
  (a) per-device /32s (only devices holding grants toward that site) in the far spoke's
      hub-peer AllowedIPs — reachability tracks permission tightly. COST: every device
      connect/revoke/grant-change recompiles every far spoke's peer set (churn amplifier);
      conflates layers — AllowedIPs becomes a permission surface, re-deriving what the chain
      already owns.
  (b) **the pool CIDR** in the far spoke's hub-peer AllowedIPs; PERMISSION enforced by
      compiled far-grants in the `ip tunnex` chain — AllowedIPs is reachability, grants are
      permission: the routed≠permitted split applied at the far hop. COST: any device can
      crypto-REACH the far gateway's wg tier; under enforcing the chain default-denies
      (D-transit-3's exact logic), under open mode reachability matches open-mesh semantics.
  **RULED (founder): (b).** Per-device /32s would make the reachability layer track grant
  churn (every grant change rewrites peer configs fleet-wide — the churn class); pool-CIDR +
  compiled far-grants keeps each layer moving at its own cadence. CONDITION (as AMENDED by
  the mid-build fork ruling, verbatim): "pool-daddr never in a ZT-chain accept absent a
  device-to-device grant; Docker's match tier never structurally drops what the chain is
  entitled to adjudicate — the D-transit-3 boundary, applied uniformly to the pool class."
  The original "structurally dark at Docker tier" phrasing collapsed two different accepts —
  Docker's (adjudicates nothing; lifts a structural drop) and the ZT chain's (where
  permission lives); enforcing darkness at the wrong layer would have shipped PD-3
  known-broken. The spoke-isolation red re-targets the tunnex chain with the C3-class
  evidentiary standard: enforcing-no-grant device↔device → dropped at the chain WITH the
  counter incrementing (proven at the adjudicating layer, not inferred from Docker's
  silence) · enforcing-with-grant → flows · mesh → passes by design (mesh means the blanket
  wg0↔wg0 accept; that is its meaning, not a leak). Grant-class scope (stated, verified in
  code): device↔device grants ALREADY compile — dst_kind='group' expands to member-device
  /32 AllowEntries — so under enforcing the pool path is adjudicated by an EXISTING class;
  PD-3 "fixed" = mesh works + enforcing gated by group-dst grants, nothing awaits a new class.

**D-A3b-2 — far-gateway grant compilation.** Device→remote-site grants land on BOTH the hub
chain AND the destination gateway's chain (founder-stated). Sub-item: does the hub REMAIN an
enforcement point for transit it forwards, or forward-only with the far gateway as single
adjudicator? **RULED (founder): both-enforce.** Defense-in-depth at zero marginal cost (both
chains compile from the same grants; the far chain exists anyway per this fix). Forward-blind
would make the far gateway's security depend entirely on every hub's integrity — wrong trust
direction for customer-operated hub boxes. RED CONDITION: the drop is observable at whichever
chain the fixture targets — hub-face AND far-face both proven.

**D-A3b-3 — Docker tier + kernel route for the pool (PD-1/PD-3 fold-in).** Pool accepts join
the ONE marked-set engine as a new key class (`d:pool`/`s:pool`), same drift-detection
transition (D-transit-2's machinery, already built + red-pinned) — NOT a second engine, NOT
pool-as-Route (pool is not a site subnet; keeping the classes distinct preserves the
spoke-isolation red's structural argument). Rider: the far spoke needs a KERNEL route for the
pool CIDR via wg0 — its wg0 addr today is a /24 slice of the /16 pool, so out-of-/24 device
addrs would fall to the default route silently (the metric-8021 family owns the fix shape).
**RULED (founder): as stated** — pool as a new key class in the ONE engine (no parallel
reconcile, the S8.6b lesson pre-applied), kernel-route rider included. CONDITION: the
key-class addition gets a CENSUS red — grep-proof that the engine's key space enumerates
exactly the classes the paper names.

**D-A3b-4 — which peers carry the pool.** Far spoke's hub-PRIMARY peer only; standbys stay
AllowedIPs-empty (the S8.6 single-valued invariant); promotion recompiles the pool onto the
new primary exactly as it does routeCIDRs. Hub-side device peers unchanged (/32s). Treated as
settled unless objected.

**D-A3b-5 — what does NOT change.** Device-side config (already routes all approved subnets —
verified live Leg 10); the masquerade (un-NAT'd invariant preserved, founder-ruled); mint-path
stable-core-only (pool transit is compiled state, not identity).

**PD-4 rider — fabric teaching text.** The cloud-fabric panel's console steps gain the pool
return-route line (AWS route table / Azure UDR: pool CIDR → gateway), beside the existing
site-range steps. Walk-proven wording source: Leg 10's actual console fixes.

### Reds (founder-named + descendants)

1. **The walk's exact fixture:** device → remote-site behind-host THROUGH the hub, zero
   manual commands, grant-gated both faces (enforcing-no-grant → far-chain drop with counter
   evidence; enforcing-with-grant → flow + counter increment).
2. **Spoke-isolation descendant:** the widened pool AllowedIPs must NOT open device↔device
   transit at the far gateway (pool→pool daddr stays outside every accept, hub-side red #1
   re-verified unchanged).
3. **Un-NAT'd invariant stays green:** far behind-host sees the device's pool address as
   source (the S8.2 walked property, now crossing two gateways).
4. **Failover symmetry:** promotion moves the pool AllowedIPs with routeCIDRs; the demoted
   member's peer drains to empty (single-valued invariant red re-run against the pool class).
5. **Drift-transition:** pre-A3b agents' rule/peer state converges in one pass (the
   D-transit-2 engine against the new key class).

**Sequence (founder-ruled Option A; D-A3b-1/2/3 RULED above, 4/5 settled-unobjected):**
build (A3b + PD-3/PD-4 + WF-2-if-real — WF-2 classification HELD on Pawan's dashboard
observation) → gates → targeted review (compiler peer model = the epic's most-reviewed
surface) → S8.7 re-rebase → CI both-green → the three merge words → main → SMOOTH WALK from
main (single provenance). The smooth walk's manifest EXPLICITLY carries the entire
outstanding proof set so nothing silently drops: Legs 1–9 + the CW v5-confirm discharge +
the five harvest verdicts + Test A (HA failover with device traffic) + Test B (ZTNA
enforcing + rules, local and cross-cloud) + the census anti-checklist (every BWF/PD row
proven dead; the two named exceptions the only hand commands).
