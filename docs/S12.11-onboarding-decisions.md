# S12.11 — onboarding. Commit-one.

> ⛔ **RENUMBERED FROM S12.6, AND THE COLLISION IS THE REASON.** PLAN.md has held **S12.6 = the compliance
> pass** (India DPDP, US state privacy, export control) since the EPIC-12 restructure. This paper was
> written as S12.6 in a session that did not check, so one number named two unrelated stories — and the
> next session to re-enter from a number would have read whichever it found first.
>
> ⚠ **12.11, not 12.7**, because 12.7–12.10 were consumed later in the same session (licence management,
> organization deletion, the trial's SSO grant, the Access-identity cutover). Reusing a number to make the
> sequence look tidy is the same defect with better cosmetics.

⛔ **Everything here is FOUNDER-RULED. This paper records the model and scopes what is left — there are no
decide-items in it.**

---

## The model

> ## ⛔ **THERE IS NO PUBLIC SIGNUP. EVER.** Not conditional, not behind `setup_complete`.

A self-hosted control plane is owned by ONE COMPANY. Everyone inside arrives by invitation or by SSO domain
capture — both acts by someone already here.

| # | | state |
|---|---|---|
| 1 | Install mints ONE account — the CP admin — and prints a one-time credential | ✅ built |
| 2 | First login forces a password change | ✅ built |
| 3 | The bootstrap admin is pre-verified | ✅ built |
| 4 | The CP admin creates the first organization (`can_create_orgs`) | ✅ built |
| 5 | Users arrive by INVITATION ONLY; the invitee sets their own password | ⭐ **built — see below** |
| 6 | The CP admin can grant any role in ANY org | ✅ built — see *Built* below |
| 7 | Enterprise SSO: sync a group, everyone in it signs in through their IdP | ⚠ exists; unverified end to end |
| 8 | Domain capture survives unchanged | ✅ measured, no change needed |

### ⭐ 5 IS ALREADY BUILT — MEASURED, NOT ASSUMED

The scope handed to this paper marked it *not built*. **It works today, end to end, on a live rig:**

```
POST /organizations/{id}/invitations   → 202  {"invite_token": "..."}
POST /auth/invitations/accept          → 200  (invitee supplies name + THEIR OWN password)
POST /auth/login                       → 200
```

`invites.go:158` calls `CreateUser` itself and `/auth/invitations/accept` is `security: []`, so **the invite
mints the account** — being invited IS the admission, and the account is only the credential.
`AcceptInvite.tsx:15` already says *"the invitee sets a password here"*. **An admin never sets someone
else's password**, and never could.

⛔ **So the remaining work on 5 is a WALK, not a build** — the API and the screen exist; nobody has driven
invite → email → link → password → sign-in through the UI with mail delivery. Recorded as a proof owed, not
a feature owed.

### ⭐ 3 — why the bootstrap admin is pre-verified

`admin@tunnex.local` is **a synthetic address the deployment minted for itself.** It has no mailbox, so no
verification mail can arrive, and there is no claim to prove — nobody asserted ownership of it.

⛔ **Guarded by `WHERE email_verified_at IS NULL` on the verify path**, so a pre-verified row can never be
used to verify a REAL address. Pre-verifying an identity is only safe when the identity is not a claim.

### ⭐ 8 — domain capture, and why it needed nothing

It mints its user on the **SSO callback** (`LinkCreate`) and never touched `/auth/signup`, so closing signup
could not break it. And it admits only on a claim that is `verified_at IS NOT NULL` **and** re-proven by a
**live DNS-TXT check at every login**, suspending the claim when that proof disappears.

⚠ **Stronger than "verified once".** Claiming a domain admits nobody.

---

## The axis — settled

⛔ **Generalise `users.can_create_orgs` into `users.cp_admin`.** `can_create_orgs` becomes IMPLIED by it,
never a second column.

> ⚠ **Two booleans that must agree is the `bandGateways`/`BANDS` shape again** — one set of facts with two
> homes, drifting silently until something built on the stale one ships.

⛔ **NEVER FOLD IT INTO `Principal.Roles`.** The tempting shortcut is to synthesise an `owner` entry for
every org at session-build time.

> ⛔ **That would make `RoleIn` LIE, and every `authorize()` call site would start returning true for
> tenants the user is not in** — a cross-tenant grant leaking into every org-scoped check in the product.

⭐ **The CP-admin check is a separate question asked BESIDE `RoleIn`**, never inside it.

---

## Invariants — reds required, both directions

| ⛔ | why |
|---|---|
| CP admin cannot remove an org's **last owner** | `CountOwners` exists because this went wrong once; a cross-tenant actor is the one most likely to do it by accident |
| CP admin cannot demote **themselves when last** | a deployment with no CP admin **and no signup** is unrecoverable — today's ruling removed the escape hatch |
| Every cross-tenant grant is **audited** — who, what, which org | it is the only act in the product that crosses a tenant boundary |
| The org's **own owner** sees it in THEIR audit log | ⚠ a privilege change inside your tenant, by someone outside it, is something you are owed sight of |

⚠ **Both directions, or it is a refactor:** the holder can grant, and a non-holder is refused. The second is
the ruling; without it the boundary is just "signed in vs not".

---

## Scope

**Build:** `cp_admin` (absorbing `can_create_orgs`) · cross-org role grants · the four invariants ·
⚠ **repair the fixture and every e2e spec that assumes signup** — nine are currently `test.skip`'d with this
story as their named trigger.

> ⛔ **A green suite testing a flow the product no longer has is worse than a red one.**

**Walk, not build:** the invitation flow end to end through the UI, with mail delivery (5).
**Confirm:** the IdP group → membership path end to end (7).

**Out:** payments · paid-band issuance (registered separately — the queue mints `trial` only, so three sold
tiers have no issuance path) · S12.3's in-app upgrade affordance.

---

## Built — what landed, and what it cost

| | |
|---|---|
| **6** cross-org role grants | ✅ `PUT /api/v1/admin/organizations/{orgId}/members/{userId}/role` |
| the axis, both directions | ✅ `PUT /api/v1/admin/users/{userId}/cp-admin` — **a surface the invariant forced** |
| the four invariants | ✅ reds both directions, mutation-checked |
| the nine e2e specs | ✅ 6 rewritten, 1 deleted with reason, **2 were never about signup** |

### ⛔ THE SECOND ROUTE EXISTS BECAUSE INVARIANT 2 HAD NOTHING TO REFUSE

`cp_admin` was grant-only — written once at bootstrap, no way back — so *"a deployment administrator cannot
demote themselves when last"* was a rule about **an act nobody could perform**. A guard on an unreachable
path is a check that cannot fail.

⭐ The guard is **"never leave ZERO holders"**, which *contains* the self-demotion case (when one holder
remains, they are the only account that can reach the endpoint) and is stronger than a self-check. It counts
holders who can actually **sign in** — live and active — because a deactivated holder is not a recovery path.

### ⭐ TWO OF THE NINE WERE MIS-SWEPT, AND MEASURING FIRST IS WHAT FOUND IT

`audit.spec.ts` and `settings.spec.ts` touch no signup path. They were red in the same run and a batch
reason was written over an unread failure — both were S12.5 **org-switcher collateral** (a positional
`combobox` locator, and an `sr-only` "Organization" label tripping strict mode). Unskipped and green with
named locators.

⚠ **The sequencing mattered more than the fix.** The cross-tenant grant writes rows into the demo org's
audit feed; running these afterwards would have measured my own feature and buried the real reason.

### ⛔ THE RENDERING HALF OF INVARIANT 4 WAS THE HALF WORTH HAVING

The row lands in the target org's feed — `audit_logs.org_id` carries no membership constraint, so that part
was free. But the audit screen resolves `actor_id` against the org **roster**, and a deployment
administrator is a member of none of the orgs they act on: the row would have rendered
**"former member 019fc421"** — a confident false claim about somebody who was never a member — on the one
event class an org's owners most need to read correctly. Fixed with a fifth actor arm keyed on the row's own
metadata, asked *before* the roster lookup.

### Registered, not built

- **No discovery endpoints.** A holder must know the org and user UUIDs; nothing in the web calls either
  route. The capability is reachable, not navigable.
- **Deployment-scoped audit rows are written and unread.** `cp_admin.granted/revoked` carry `org_id NULL`
  (they belong to no tenant); every audit read is org-scoped, so nothing serves them.
- **`CreateOrg.tsx`'s `org_limit_reached` re-check branch is dead** — a non-holder never reaches the form,
  so nobody can submit and be refused into it.
- **`/api/v1/auth/signup` still exists** and answers `403 signup_closed`. The route and page remain; the
  ruling says there is no public signup **ever**, which argues for removing the surface, not only closing it.
- **`can_create_orgs` remains** — the inert contract half of expand/migrate/contract, deferred by design.
