# S8.2c — site-to-site end-to-end via UI (gateway zero-touch) — commit-one (decision-first)

> **ZERO-TOUCH GATEWAY LAW (the acceptance bar):** a gateway is brought online by pasting the ONE install command the dashboard emits — and nothing else. Sites, subnets, enforcing, the site→site grant, and a genuinely-separate host *behind* the gateway reaching a far site are ALL achieved by clicking in the dashboard — never by SSH'ing the gateway to add networking. **Any manual networking step on the gateway is a DEFECT, not a runbook line.** (`docs/laws.md`)

**Origin:** the cross-cloud demo (`walk-artifacts/cross-cloud-demo/demo-record.md`) proved AWS Sydney↔Azure West US site-to-site at **138ms** — but only after **6 manual gateway touches + 3 UI gaps**. S8.2c closes that gap. **Sequencing:** ahead of S8.4; S8.5 shrinks by the overlap.

**Box-walk (defined BEFORE a line is written):** the cross-cloud demo re-runs CLEAN under the Zero-Touch Law — fresh org, two cloud VMs, the ONLY terminal action a pasted join command; register/advertise/approve/enforce/grant/behind-host-reaches-far-site all CLICKED. That re-run IS the story's walk. Fixture: a genuinely-separate FORWARDED host behind the gateway (the fixture-fidelity TOPOLOGY-SIBLING law — S8.2's dummy-LANs-in-netns hid the forward path).

---

## Decide-items (proposed dispositions — HELD for founder ruling)

### D1 — behind-host reach + mesh-mode forwarding (#5, re-characterized by demo leg (b))
**(b) EXONERATED the S8.2 compiler:** under enforcing+grant it emits the correct **symmetric, iifname-agnostic** forward rule (`saddr <src-site> daddr <dst-site> accept`). The demo's behind-host (the CP) failed for **deploy-class** reasons, not a data-plane defect: the packet was dropped by **Azure SDN before the gateway** (no UDR), and **mesh mode emits no LAN→tunnel rule** at all.
**Proposed:** (a) **mesh-mode forwarding** — decide whether OFF/mesh should forward a behind-host's site→site traffic, or whether site-to-site-for-behind-hosts DELIBERATELY requires enforcing+a grant (my lean: **require enforcing+grant** — mesh is device-mesh; a routed site grant is a governance act, consistent with unlock-then-opt-in — but state it, don't leave it an accident). (b) The **red** (the fixture-sibling): a genuinely-separate behind-gateway host initiates cross-site **end-to-end**, with the cloud route (D3) in place. HELD.

### D2 — src-hint in ApplyRoutes (#4b)
The agent programs the site route with **no `src`**, so a gateway-host-originated packet mis-sources (overlay addr) AND a manual `src` fix is **clobbered every reconcile tick**.
**Proposed:** `ApplyRoutes` sets `src <the gateway's site-subnet IP>` on the proto-static site routes. **Red:** gateway-host-originated traffic sources correctly AND **survives reconcile** (today it clobbers). Note: real behind-hosts don't hit this (they source naturally); it's the gateway-host-as-source case — but the Zero-Touch Law says even that must Just Work. HELD.

### D3 — host-networking stance + cloud-fabric routing (#3)
Bridge mode traps `wg0` in the container netns (only the overlay addr; can't reach host LANs). The demo needed manual `--network host`. Separately, cloud fabric needs **Azure UDR / AWS route-table + source-dest-check** for a behind-host to reach the gateway.
**Proposed:** (a) **host networking is REQUIRED and emitted** in the install command (D4). (b) The agent **refuses LOUDLY when it can't reach what it advertises** (an approved site subnet it can't route to the host is a health-degraded/blocked state, not silent). (c) Cloud-fabric routing (UDR/route-table) is **documented as a named prerequisite** per cloud (it's outside Tunnex's reach — but the Zero-Touch Law's "click-only" applies to Tunnex surfaces; cloud-fabric setup is stated honestly as the one unavoidable cloud-console step, minimized + documented). HELD.

### D4 — the emitted install command (#1/#2)
The join-token screen emits a `docker compose -f tunnex.yml … node-agent` line (co-located assumption); a remote gateway needs a bare `docker run` with `wgctrl`, host networking, all envs, and the public endpoint — the compose line **broke twice** when pasted, and defaulted `backend=mem`.
**Proposed:** the join-token screen emits the **ONE true `docker run`** for a **remote gateway** (public CP URLs, `TUNNEX_WG_BACKEND=wgctrl`, `--network host`, `TUNNEX_NODE_ENDPOINT`, the token) — copy-paste-correct as-is. The **compose form remains** for co-located single-host installs (a toggle/two tabs on the enroll screen). HELD.

### D5 — site rules in the Access builder (GAP-2, the headline UI gap)
S8.3 built site-rule **display** (`ruleRow` resolves site labels) but **NOT create** — the Add-rule modal is `group/user → group/resource` only. The demo's site→site grant had to be a **raw DB insert** (bypassing API validation + audit — demo-only, never documented).
**Proposed:** add **`site`** to the modal's Source AND Destination type options, creating `src_kind='site'`/`dst_kind='site'` rules **through the EXISTING policy API** (disjointness/validation/audit ride the same path — no new bypass). HELD.

### D6 — org creation + add-rule-with-zero-groups (GAP-1, GAP-3)
GAP-1: an existing owner can't create a 2nd org in-session (`/create-org` is `RequireNoOrg`-gated). GAP-3: the Add-rule button stays disabled after adding a group until a page refresh (pre-existing cross-section staleness).
**Proposed:** GAP-1 — an in-session "New organization" affordance for an existing user (or an explicit decision that multi-org is signup-only). GAP-3 — lift `groups` to the parent / reload RulesSection on group-add. Both same slice as the UI work (D5). HELD.

---

## Proposed slice plan (held; folds after ruling)
- **Slice 1 (backend/agent) — COMPLETE** (`4ab3b84` D1, `606c7e9` D2, `9371138` D3): D1 mesh symmetric site-forward (scoped to Routes, spoke-isolation held); D2 CP-sent `LocalSubnets` + agent route src-hint (survives reconcile); D3 `site_subnet_unreachable` health kind (the bridge-trap reassuring-green trap, B2 pattern). All gated green (node + both editions + web + generate-check).
- **Slice 2 (deploy surface) — BUILDING:** D4 the emitted remote-gateway install command (+ cloud-fabric prerequisite docs). **D4a (endpoint source) RULED: admin-entered at token creation** — the UI can't detect a not-yet-enrolled remote gateway's public IP; the admin knows the cloud VM's. An OPTIONAL "Public endpoint (ip:port)" field: blank → NAT'd spoke (no `TUNNEX_NODE_ENDPOINT`), set → hub/reachable gateway. **D4b (copy render) RULED: single-line `docker run`, its own block + copy button** — tonight's double paste-failure was a multi-line compose line that LOOKED copyable; a single-line `docker run` (all envs inline: wgctrl, `--network host`, endpoint, token, public CP URLs, servername) can't be mis-pasted. Compose stays for co-located (separately labeled). **Red: the emitted command, pasted verbatim on a clean VM, reaches `agent_ready` on real WG with zero edits** (tonight's failure encoded — unit-pinned via the pure builder asserting all pieces + single-block; the live paste is a walk leg).
- **Slice 3 (UI) — D5 + GAP-3 DONE; GAP-1 DEFERRED (with reason).** D5: the Access Add-rule modal now creates `site` src AND dst rules (through the EXISTING policies API — validation + audit intact; the demo's raw DB insert was the anti-pattern this closes). Site rules are DELETED via the existing list Delete (works for any rule). GAP-3: Add-rule enables with a group OR a site (site/resource rules never needed a group — the disabled state was a false dependency). Red: pure `ruleBody` (site→site, group→site, existing kinds, create-only expiry). **GAP-1 DEFERRED:** the app selects the active org as `organizations[0]` with NO org switcher (`useCurrentOrg` is ledgered-deferred) — a "New organization" button creating an org the user can't switch to is worse than the gap. **Rides the org-switcher follow-on; the acceptance walk uses fresh-signup for a clean org (as the demo did), so it is NOT blocked.** (Advisor consult was requested per the standing directive but the advisor was disabled this session → decided on disciplined judgment.)
- Then story-end review → the **demo-re-run walk** under the Zero-Touch Law (Pawan drives, the fixture = a separate forwarded behind-host + the cloud route).

---

## RULINGS (folded 2026-07-18 — all six RULED; build S8.2c before S8.4 opens)
- **D1 RULED — mesh mode emits SYMMETRIC site-forward rules (LAN↔tunnel BOTH directions) for approved subnets.** Mesh's contract is "everything open, no doors"; a mode that means no-restrictions silently not forwarding a behind-host is a broken promise, not a design choice. Enforcing keeps grant-gated ((b)-proven correct). **Red:** a behind-gateway host initiates cross-site in mesh (passes); in enforcing-without-grant it **drops at the gateway's chain with the counter incrementing** — the drop MOVES from the invisible fabric to our VISIBLE chain wherever the fabric permits.
- **D2 RULED — mechanism (b): the CP SENDS the local site subnet** (`Compiled.LocalSubnets`); the agent picks its host address inside it for the src-hint. **The one-truth law, 4th instance:** the agent guessing its own site address (egress probe / interface heuristic) is a SECOND independent derivation of a fact the CP already owns authoritatively — which subnet this gateway fronts is exactly what the admin advertised + approved in the dashboard. Heuristics agree on cloud VMs, quietly diverge on-prem (WAN≠LAN) / multi-homed — the exact shapes S8.2c exists to make safe. (b) makes the agent a CONSUMER of the one election — same as the D2-topology overrule (backend hub, no TS re-election), the S8.3 `meta.protocol_version` seam. Four instances = the pattern is the architecture.
  - **CLASSIFICATION (D2-checklist row, explicit RequiredVersion analysis):** `LocalSubnets` is **out-of-hash plumbing like `Routes`** — an old agent ignoring it loses ONLY the src-hint (gateway-host-originated sourcing), **never reachability or enforcement — degraded-not-broken → NO version bump.**
  - **No-site edge:** no local subnet in the artifact → no src-hint, **logged ONCE (not per-tick)**, the route still programs identically to today. Red.
  - **Multi-subnet site:** the agent picks the advertised subnet containing one of its host addresses; if **NONE match** (advertised a subnet the host isn't on) → no src-hint **+ the D3 health kind fires** (refuse-loudly, not a guess).
  - **Reds:** correct sourcing (plain `ping` from the gateway host, no `-I`) · **survives reconcile** (program → manually strip → next tick restores) · the no-site + no-matching-address edges.
- **D3 RULED** — host-mode is the REQUIRED + emitted shape; the agent **detects bridge-trapped wg0 / unreachable advertised subnets → refuses LOUDLY (a health kind, the B2 pattern), never a silent no-op.** Cloud-fabric steps (Azure UDR, AWS route-table + src/dst-check) are un-codeable → **GUIDED SETUP: the site/subnet UI surfaces per-cloud "your fabric needs this route" instructions** (detected-or-declared cloud, copy-paste snippets, doc links). **ZERO-TOUCH LAW BOUNDARY CLAUSE (folded into the law):** the law's boundary is the GATEWAY VM itself — zero SSH after join stands; the cloud console gets ONE named, guided visit per side.
- **D4 RULED as proposed** — the join screen emits the one true `docker run` for remote gateways (wgctrl, host networking, endpoint, all envs, token) + a bare-metal variant; compose stays for co-located. **Red:** the emitted command, pasted verbatim on a CLEAN VM, enrolls to `agent_ready` on real WG with ZERO edits.
- **D5 RULED as proposed** — `site` src/dst options in the modal, riding the EXISTING API path (validation + audit intact; the demo's DB insert is the named anti-pattern). **Include site-rule DELETION in the same pass.**
- **D6 RULED into the UI slice** — org creation for an existing owner; **Add-rule usable with ZERO groups** (site/resource rules don't need a group — the disabled state was a FALSE dependency).
- **Slice order approved:** agent → deploy surface → UI → review → the demo-re-run walk (the acceptance: Pawan re-runs cross-cloud with the only terminal action a pasted join command + one guided cloud-console step per side). **BUILDING Slice 1.**

---

## STORY-END REVIEW → FOLD (2026-07-18, `d04092f`)

Story-end multi-finder review over the 3 slices. Findings presented ranked, dispositioned, then folded (a feature-sized fold — RE-EARNS a targeted re-review of the folded code, which ran after gates green; the advisor was disabled this session so the workflow review is the check, not a founder call).

**Folded (8):**
- **#1 (correctness) — the emitted gateway command must derive CP URLs from the CP's AUTHORITATIVE public base URL, not the browser origin.** Added `Meta.public_base_url` (= `APP_BASE_URL` / install.sh's public address); `cpEndpoints(publicBaseURL, fallbackOrigin)` prefers it, falling back to `window.location.origin` ONLY when unset/blank. An admin opening the dashboard via a tunnel/alias/bare IP no longer bakes an unreachable endpoint into the zero-touch command. **The one-truth law, 5th instance** — "where do gateways reach me" is a fact the CP owns, not one the browser URL should re-derive.
- **#2 (correctness) — enroll-modal caption** dropped the stale co-located token-less line; points at `docs/deploy-cloud-gateway.md` for the same-host compose form (which carries the same token).
- **#3 (correctness) — the admin-entered endpoint is shell-quoted** (same charset as the name): a space/metachar can't corrupt the one-line `docker run`.
- **#4 (correctness) — rule-modal default kind → `site`** when an org has no groups but has sites (was defaulting to an unusable group source).
- **#5+#6 (cleanup) — single host-address enumeration per tick.** The D2 src-hint AND the D3 unreachable signal now derive from ONE `siteRouteSrc(localSubnets, r.hostAddrsFn())` call in `runOnce` (was re-enumerated inside the wgctrl backend). `WGBackend.ApplyRoutes` takes a reconcile-derived `srcHint string`; the backend threads it VERBATIM, never re-derives — the one-truth law applied inside the agent (the reconcile loop is the single deriver, the backend a consumer). The onset warning is de-duped to once per onset (reset on recovery OR empty-advertisement). Derivation reds moved UP to a reconcile-level test (`TestRunOnceDerivesSrcHintAndUnreachableSignal`) + a pure `TestSiteRouteSrc`; the backend test (`TestApplyRoutesSrcHint`) now pins verbatim threading.
- **#8 (docs) — `canEditRuleInModal` comment** reflects D5: site rules are CREATED via the Access builder now; only in-place swap-edit is withheld.
- **#9 (docs) — `policy_degraded_kind` enum** documents `site_subnet_unreachable` + its rank (below both link-down kinds — a dead link masks it).

**Deferred (1):**
- **#7 — duplicate precedence ladders** (the degradedKind precedence expressed in two places). Noted, not folded this pass — a mechanical consolidation, no behavioral risk; folds when the health-kind surface is next touched.

**Gates green:** build-editions · test-editions (both) · test-node · test-helper · helper-crosscompile · web typecheck/test(118)/build · migrate. `make generate` regenerated (the api.gen.go churn is the embedded-spec gzip blob + the new doc-commented `PublicBaseUrl` field forcing a struct re-emit — no behavioral/type change).

---

## RE-REVIEW (fold of the fold) → FOLD (2026-07-18, `f2eab27`)

The story-end fold (`d04092f`) was feature-sized → RE-EARNED a review of the folded code. That re-review (high-effort workflow, advisor disabled → the workflow IS the check) surfaced **5 CONFIRMED findings + 1 correctly-refuted** — all fold-induced defects in already-dispositioned decisions, so dispositioned on disciplined judgment and folded (not held — the standing directive is advisor-judgment + keep-moving, and these are correctness bugs in execution, not new design forks).

**The budget-rule bind:** findings #1+#2+#3 were three defects in ONE component (`Gateways.tsx` URL consumption) from ONE fold → the rule says *reduce not patch, paper the state model*. Done:

- **#1+#2+#3 (correctness) — ONE URL-consumption state model.** `cpEndpoints` returns a **discriminated union**: `{ok:true, apiURL, agentURL, serverName, usedFallback}` | `{ok:false, reason}`. `ok:false` ONLY when a NON-BLANK configured `public_base_url` fails `new URL()` (an operator `APP_BASE_URL` typo) — no longer a silent empty `agentURL` (#1). The component **BLOCKS the one-time-token mint** on `!ok` (a token minted against a broken URL is worse than refusing; the remedy is operator-side) and surfaces `reason`. `metaError` state distinguishes a **FAILED** meta fetch from an **unset** field — a failure that falls back to the browser origin is now flagged with an amber caveat (#2: the tunnel/alias origin no longer bakes in silently). All env values shell-quoted uniformly via one `q()` — `apiURL`/`agentURL`/`serverName` included, not just name/endpoint (#3).
- **#4 (correctness) — dst dead-end.** The modal dst default fell to an empty group select for a no-groups org that HAS resources. Extracted **pure `defaultDstKind`/`defaultSrcKind`** (editing-kind → groups → resource/site), unit-pinned — the src-side fix's blind spot on the dst side.
- **#5 (cleanup) — reconcile syscall guard.** `net.InterfaceAddrs()` enumerated every tick even for the common non-site node; now gated `if len(localSubnets) > 0` (byte-identical D2/D3 behavior, siteRouteSrc discards addrs on empty advertisement anyway).
- **Refuted (correctly):** the `srcKind` no-groups-no-sites dead-end — unreachable, the Add-rule button is gated off in exactly that state.

**Loop discipline:** a LOOP-TERMINATOR re-review of `f2eab27` is running (the #1-#3 reduce touched the security-adjacent emitted-command path). Terminal branch: clean/trivial → done; more same-component defects → **HALT and surface to the founder** (the budget rule's deferred-not-shipped branch, per the S7.5.1 JSONL precedent).

**Gates green:** test-node · reconcile (darwin + linux compile) · web typecheck/test(123)/build.

---

## RE-REVIEW ROUND 3 (loop-terminator) → REDUCE COMPLETION + LOOP HALT (2026-07-18, `4fe05e1`)

The loop-terminator re-review of `f2eab27` found **2 PLAUSIBLE findings, both ONE root**: the async `/api/v1/meta` window. Before the fetch settles, `publicBaseURL` is undefined → `ep` transiently narrows to the origin fallback with Generate ENABLED → minting there could **strand a one-time token** (a late-arriving broken URL flips `ep.ok` false and hides the modal) or **silently bake the browser origin** (the review-#1 harm re-entering via a race). Exactly the timing hole flagged pre-review.

**Disposition — the budget rule's prescribed remedy (reduce, not a 4th patch round):** make the in-flight fetch a **FIRST-CLASS state** (`metaLoaded`, set in `.finally`). Generate is gated `busy || !metaLoaded || !ep.ok` → the emitted command is only ever built from a **SETTLED** CP address; the whole in-flight window becomes a disabled "Checking control plane…" button. `ep` is stable by mint time → no post-mint flip, no strand, no silent origin. This **completes** the cpEndpoints state model (in-flight was the missing state), it does not patch it.

**LOOP HALTED here.** Defect count converged **3 → 2 → 0-new-root**; the two round-3 findings shared one root and closed at that root. A 4th review round would be the whack-a-mole the budget rule forbids (the S7.5.1 JSONL precedent). The gate is component wiring (no pure view-model to unit-test, consistent with this codebase) — **its proof is the live enroll ceremony in the demo-re-run walk** (Leg 1). Refuted-correctly: the `q()`-vs-`enrollCommand` inline-escape duplication (not a defect; left untouched for scope discipline).

**Review arc closed. Gates green** (web typecheck/test(123)/build; node/reconcile unchanged this round). **NEXT: the demo-re-run walk** (`walk-artifacts/S8.2c/walk-record.md`, Pawan drives, Zero-Touch Law the gate). NOT merged — awaits explicit in-session sign-off.

---

## WALK-DRIVEN FOLD (2026-07-18, founder-dispositioned) — 6 fold, 2 defer

The live cross-cloud walk (`walk-artifacts/S8.2c/walk-record.md`) FAILED the Zero-Touch Law on WF-4 (story OPEN). Founder ruled: fix before merge — WF-4 + WF-8 + WF-3 + WF-5 + WF-2 + WF-6 FOLD; WF-1 (→ S8.5 L1-metrics rider) + WF-7 (site-rule editor, → first-request / epic-close UX harvest) DEFER with those triggers.

### WF-4 (BLOCKER) — decision-first: the agent owns a Routes-scoped accept in Docker's FORWARD path

**The defect (wire-proven):** a Docker-host gateway sets `filter FORWARD` policy `DROP` (`-A FORWARD -j DOCKER-USER -j DOCKER-FORWARD …`). The agent's D1 accept lives in its OWN `ip tunnex` forward base chain; Docker's `filter FORWARD` is a SEPARATE base chain whose policy-drop is terminal → the forwarded behind-host packet dies (100% loss) despite the agent accepting it. Site-to-site forwarding silently fails on EVERY Docker-host gateway until a manual `iptables -I DOCKER-USER -j ACCEPT` — a gateway touch the Zero-Touch Law forbids. This is the true root of the demo's leg-(b) failure (UDR was necessary-not-sufficient; Docker-FORWARD-DROP the deeper blocker).

**Decide-items (founder-named), dispositioned:**
- **D-WF4-a — foreign-chain ownership + idempotent insertion + survival across dockerd restart/reload.** The agent inserts its accept into `DOCKER-USER` (Docker's designated user hook, jumped FIRST from FORWARD; an accept there is terminal for the hook, bypassing Docker's later DROP). The rule carries a COMMENT MARKER (`tunnex-site-fwd`) so the agent finds + sweeps ONLY its own rules, never Docker's or a human's. The agent has `nft` only (no iptables) — `DOCKER-USER` is an iptables-nft chain in `ip filter`; the agent manages it via `nft` (add/list/delete by handle, matched on the comment). Insertion is IDEMPOTENT (list → insert only if absent) and re-run EVERY reconcile tick, so a dockerd reload (which flushes+recreates `DOCKER-USER`) self-heals within one interval. Managed OUTSIDE the atomic `ip tunnex` transaction (can't flush a Docker-owned table).
- **D-WF4-b — scope EXACTLY to Routes.** The accept mirrors the D1 rule: `iifname != wg0 oifname wg0 ip daddr <route> accept` per approved remote route. The workaround's blanket `-j ACCEPT` NEVER ships (it would defeat Docker's isolation for all containers). Full-sweep: the `DOCKER-USER` rule set is reconciled to exactly the current Routes — a route withdrawn removes its accept (same full-sweep contract as the kernel routes).
- **D-WF4-c — non-Docker hosts conditional (honest D4 bare-metal path).** No `DOCKER-USER` on bare-metal (no Docker) → the agent PROBES for the chain (`nft list chain ip filter DOCKER-USER`) and only manages it when present. A bare-metal gateway forwards via its own `ip tunnex` accept with no foreign chain to satisfy — stated honestly, not silently assumed.
- **D-WF4-d — D3 detects the Docker-DROP blackhole (never green-blackhole).** If `DOCKER-USER` exists, `FORWARD` is policy-drop, AND the agent's accept is NOT in force (insertion failed / permission), the gateway CANNOT forward its advertised remote traffic — it must surface LOUD, not sit reassuring-green. Reuses the D3 family: report the unreachable/forward-blocked condition as a health kind (site_subnet_unreachable or a sibling `site_forward_blocked`) so the health surface shows it. The B2 pattern — refuse loudly, never a silent no-op.

**Reds:** the rendered `DOCKER-USER` rule set equals the Routes (add/prune) · idempotent (second reconcile inserts nothing) · comment-marker sweep touches only tunnex rules · chain-absent (bare metal) → no-op, no error · the forward-blocked health signal fires when the accept can't be placed. **Re-walk (Pauline drives):** delete the manual `DOCKER-USER -j ACCEPT`, restart the agent, behind-host ping survives on agent-managed rules alone.

### The five smaller folds
- **WF-8 (FOLD)** — Rules list renders site endpoints by NAME, not raw truncated UUID. Red: two UUIDv7 same-prefix sites (`019f762b…`) render distinguishably (`azure-site → aws-site`).
- **WF-3 (FOLD, minimal-honest)** — the site page shows per-cloud fabric steps (UDR / route-table + IP-forwarding copy-paste), STATIC per cloud (no cloud-detection this pass — that rides S8.5). Closes the D3-ruled in-UI guided setup that shipped docs-only.
- **WF-5 (FOLD)** — unadvertise/remove-subnet on the site card, through the existing audited path; the confirm states the full-sweep consequence (routes withdrawn from all gateways).
- **WF-2 (FOLD, cheap-honest)** — the emitted install command pins the node image by DIGEST (not `:latest`) — one-truth applied to the artifact version, making the stale-image mis-conviction structurally impossible; volume-reuse gets a boot-time log line naming the reused identity; a doc line for the rest.
- **WF-6 (FOLD as investigation)** — diagnose why an out-of-hash `LocalSubnets`/`Routes` change caused a multi-minute `converging`: either the desync path wrongly considers out-of-hash content (real defect) or the UI's converging state is over-broad (display fix). Fix what's found; if benign, one sentence closes it.

**Sequence:** WF-4 slice (decision-first, this paper) → the five folds → gates → targeted re-review over the full fold diff → Leg-2 re-walk + WF-8/3/5 spot-checks (Pawan drives, ~20 min) → merge is Pawan's word.
