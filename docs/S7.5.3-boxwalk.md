# S7.5.3 device posture — box-walk (live, founder-present)

Two-gateway box, **enterprise** API. Posture is CLIENT-REPORTED; on the box we drive the report
endpoint directly (curl as the device owner) so the walk proves the SERVER state machine + the
on-the-wire exclusion, independent of a packaged desktop client. The desktop `posture_blocked` UI
is covered by the client unit tests (`uxwiring`/`healthmonitor`) + is a S6.5-class packaged-client
smoke deferral (SUBSTITUTES ≠ SATISFIES — same discipline as the S7.3 client-wire-smoke).

Each leg's evidence is captured + committed IN-SESSION to `walk-artifacts/S7.5.3/` (the unstrandable-
artifact discipline). Scratch WG configs (private keys) are gitignored at creation (`scratch/`).

## Env (Leg 0) — PASS

Two-gateway VM (`Tunnex-dev-vm`, host `10.0.0.4`), **enterprise** build of S7.5.3 (`78675d9`),
migration **29**. Kept the S7.5.2 DB volume; enrolled two fresh Demo-Owner-owned spokes as live
netns WireGuard peers via `deploy/testing/s72-spokes.sh` (pool `10.99.0.0/24`, gateway `10.99.0.1`).

| Thing | Value |
|---|---|
| Edition / migration | `enterprise` / `29` |
| Org (`$ORG`) | `01900000-0000-7000-8000-000000000001` (Demo Organization) — `zero_trust_mode=enforcing` |
| Gateway node | `019f6902-…-97b9489e1db1` (demo-gw, `10.99.0.1`, wg pub `M4jAJZBL…`) |
| Spoke A / `$DEV` (device under test) | `019f6a52-e39a-7d13-9459-d722c96d9876` · `10.99.0.2` · wg `PDlq86dr…` |
| Spoke B / `$DEVB` (control) | `019f6a54-6634-7c7a-a86d-1f84a2ea7825` · `10.99.0.3` · wg `dn+34+5G…` |
| Owner | `owner@demo.tunnex.local` (owner role; owns both spokes → self-reports; configures checks) |

**Baseline (both artifacts):** both spokes handshook + present in `wg show wg0`
(`leg0-baseline-wg.txt`); A→gw and B→gw both 0% loss (`leg0-baseline-ping.txt`);
`org_health_checks` empty; no device pre-`health_blocked`.

**Client-half deferral (declared up front):** this headless VM runs no packaged desktop, so the
desktop `posture_blocked` UI (Leg 1's client half) is **SUBSTITUTED by the client unit tests**
(`uxwiring.test.ts` posture_blocked tray/notify + `healthmonitor.test.ts`) and carried as a
S6.5-class packaged-client smoke deferral — SUBSTITUTES ≠ SATISFIES, same discipline as the S7.3
client-wire-smoke. The box proves the SERVER state machine + on-the-wire exclusion.

`NFT()` helper (run inside the node netns), reused from S7.2/S7.3:
`sudo nsenter -t $(docker inspect -f '{{.State.Pid}}' $(docker compose ps -q node-agent)) -n <cmd>`

Report helper (curl as the device OWNER — self-report; `X-Tunnex-CSRF: 1` on the mutating POST):
`POST /api/v1/organizations/$ORG/devices/$DEV/health  {"platform":"macos","os_version":"14.5","disk_encrypted":<bool>}`

---

## Leg 1 — require-mode exclusion <5s + client posture_blocked  · **PASS**

`leg1-block-proof.txt` + `leg1-after-block-wg.txt`. Configured `require` disk_encryption → control
spoke B reported compliant (stayed) → spoke A reported non-compliant → **blocked in 0.069s** (peer
gone from `wg0`, ping A→gw 100% loss; spoke B 0% loss — per-device targeting). DB `health_blocked=t`
(A) / `f` (B); `device_health` rows match; audit `device-health`/`noncompliant_report`.

**Timing note (diagnosed, not hand-waved):** the first read was 7.87s — traced to the `NFT()`
helper running `docker inspect` + `compose ps` every poll. Re-measured with the node PID resolved
once: **block 0.069s, re-admit 0.215s** — sub-100ms push-driven reconcile, the same fast path
approve/revoke proved. `<5s` confirmed.

**Client half — SUBSTITUTED (deferred):** the desktop `posture_blocked` state (tray/window/notify)
is covered by `uxwiring.test.ts` + `healthmonitor.test.ts`; live packaged-desktop smoke rides the
S6.5-class deferral. SUBSTITUTES ≠ SATISFIES.

## Leg 2 — compliant report re-admits, block clears  · **PASS**

`leg2-readmit.txt`. Spoke A reported compliant → `{state:compliant, blocked:false}` → peer back in
`wg0` (0.215s, timed Leg 1) → ping A→gw restored 0% loss → both devices `health_blocked=f`. The
re-admit rides the same exclude-then-push machinery in reverse.

## Leg 3 — stale sweep clears a >30min block (staleness = absence)  · **PASS**

`leg3-stale-sweep.txt`. Spoke A re-blocked, its `device_health.reported_at` backdated 31min (A goes
silent, never re-reports). Backdate alone did NOT clear the flag; the real 5-min sweep cleared it at
10:00:42 — **with no compliant report** — peer back in `wg0`, audited `device.health_unblocked` cause
**`report_stale`** (distinct from `compliant_report`). Proves staleness = absence: a silent blocked
device is released after the TTL, honestly (not faked as compliant); only a FRESH non-compliant
report gates.

## Leg 4 — grandfather flip-on = 0% loss  · **PASS**

`leg4-grandfather.txt`. With both spokes last reporting non-compliant, enabling `disk_encryption=
require` returned `would_fail_count:2` — yet **disconnected nobody**: post-flip both `health_blocked=
false`, both peers present, both ping 0% loss. The config write is 0-loss; `would_fail` is an honest
advisory (2 would block at their *next* report), enforcement is per-next-report, never retroactive.
A fleet on old/non-reporting clients survives a flip-on.

## Leg 5 — enterprise→open downgrade releases the block ([1] wire-proof)  · **PASS**

`leg5-downgrade-release.txt`. Blocked spoke A under enterprise (peer gone, ping dead), then rebuilt
as the **open** edition. On open-build boot `ReleaseAllHealthBlocks` fired
(`health_downgrade_release released=1`): `health_blocked→false` with no report and no sweep, peer
back in `wg0`, ping restored, audited `device.health_unblocked` cause **`posture_disabled`**. Wire-
proves the highest-severity review fix — disabling the feature RELEASES its enforcement instead of
petrifying it (the downgrade mirror of unlock-then-opt-in).

---

## Disposition — ALL 5 LEGS PASS

| Leg | Result |
|---|---|
| 0 env + baseline | PASS — 2 live spokes, both peers, 0% loss, clean config |
| 1 require-mode exclusion | PASS — block 0.069s (<5s), per-device targeted, ping dead, audited |
| 2 compliant re-admit | PASS — 0.215s, peer back, ping restored |
| 3 stale sweep | PASS — silent block cleared, cause `report_stale` (staleness = absence) |
| 4 grandfather flip-on | PASS — `would_fail=2` advisory, 0 disconnected by the flip |
| 5 downgrade-release [1] | PASS — open-build boot releases the block, cause `posture_disabled` |

Server state machine + on-the-wire exclusion + the review fix [1] all proven live. Client
`posture_blocked` UI substituted by unit tests (packaged-client smoke deferral, SUBSTITUTES ≠
SATISFIES). Both-green (CI) → merge on the founder's explicit in-session word.
