# S20.5 foreign unfinished enrollment — contract-preserving repair

Status: FIX BEFORE WALK, finding 3; implementation authorized within locked
D14/D14o owner-bound identity. No multi-user identity-storage redesign.

D14o explicitly refuses a different user at the same normalized origin. A
foreign unfinished anchor must not project its organization, owner, keys,
device identifier, or preferences into the new user's session, and logout
must not overwrite or destroy unresolved identity. Origin-keyed final state
already enforces the same bound-owner restriction.

The defect is that this mandatory refusal is discovered only after helper and
lifecycle side effects, is hidden by the renderer, and remove-device falsely
reports absence. The narrow repair is:

- prove foreign-anchor ownership in the read-only connect preflight, before
  helper installation, epoch changes, quiescing, or monitor changes; retain
  the later check as a drift guard;
- expose only a boolean/static blocked reason to the current renderer, never
  foreign identity data or an anchored organization;
- render an actionable static explanation to sign out and return with the
  originating account to resume recovery; do not promise immediate abandon
  across a new credential unless its rebind proof actually permits it;
- refuse remove/abandon early and honestly for a foreign anchor, preserving
  its exact bytes and making no control-plane/helper mutation; and
- prove that the originating account can still recover and that normal
  no-anchor/same-owner flows remain valid.

This fixes the invisible lockout/false-success path, not independent parallel
enrollment for two users at one origin. Independent users would require a
feature-sized owner-keyed managed-identity model, final-store migration, and
separate policy decisions; merely re-keying unfinished anchors moves the
collision to the final store. That expansion is not silently included here.
The resulting UI boundary and regressions must earn independent review.

## Implementation and local proof (2026-09-05)

The connect preflight now durably reads the anchor immediately after capturing
the fixed user/origin lease and refuses a foreign owner with the static
`managed_enrollment_owner_mismatch` error. This precedes legacy-device proof,
helper quiescence/installation, epoch advancement, and monitor changes. The
later exact anchor-context check remains in the config provider. Remove reads
and rejects the same foreign anchor during its owner-proof phase, before
organization/roster requests, helper work, revoke, or clear; it no longer
reports false absence. Organization-selection mutation also rejects that state.

The organization envelope adds only `enrollmentBlockedByOtherUser: boolean`.
The existing foreign-anchor organization projection stays null and its
`enrollmentRecoveryRequired` stays false. The renderer explains that the user
must return with the originating account and use Connect to resume recovery;
it offers an explicit sign-out action and disables managed Connect, enrollment
selection, and removal while that block is known. Imported-profile connections
and Disconnect remain available. A stale envelope cannot bypass MAIN's guard;
a later removal refusal receives the same static guidance instead of the
generic retained-enrollment retry message.

Verified locally with Node 20.20.2 on macOS:

- Client typecheck passed.
- Full client suite passed: **292 tests**, including temporary local helper
  sockets. The first sandboxed run could not create those sockets (`EPERM`);
  the permitted local-listener rerun passed without a product change.
- Web typecheck passed and the complete `clientapp.test.tsx` component file
  passed: **64 tests**. The isolated harness initially lacked the shared
  `openapi-fetch` dependency; resolving it from the original dependency cache
  read-only restored the typecheck. No dependency versions or source changed.
- New behavioral regressions prove foreign-owner connect/removal proof refusal,
  unchanged lifecycle lease, zero helper/monitor/API effects, byte-identical
  encrypted anchor retention, static secret-free errors, the late config
  drift guard, and originating-owner response-loss recovery with the same
  key/device UUID and no second create. Absent and same-owner cases remain
  accepted. Existing direct-abandon fingerprint refusal tests still pass.
- New renderer regressions exercise the boolean blocked view, disabled managed
  controls, explicit sign-out/original-account return, late removal refusal,
  and continued imported-connect/Disconnect availability.
- An AST-based `entry.test.ts` check verifies the actual IPC guard statements
  precede managed connect/removal effects. It is wiring evidence, not an
  execution of Electron's registered IPC handlers. The behavioral lifecycle
  tests execute the pure production coordinator/flows with injected proof
  callbacks. These tests do not satisfy a live Electron/helper wire proof;
  that remains part of the desktop client leg of the S20.5 live qualification.

The integrating agent independently reviewed the complete production/test
delta and actual IPC/coordinator ordering and reported no high or medium
defect. Full final-tree web/build gates remain with the integrating agent.
Task-local dependency links point to the original installed packages for
read-only reuse; no original dependency tree was modified. They are retained
for the separately approved exact-list cleanup.

## Remaining boundary

This repair makes the required cross-user refusal visible and side-effect
free. It does not permit user B to enroll while user A's anchor occupies the
origin. If A cannot return or lacks membership, no new recovery authority is
invented. Returning credentials may rebind only through the existing Connect
proof; direct abandon still requires its existing exact fingerprint. Findings
17, 18, 25, 36, 37, and 38 are not folded by this change. Owner-keyed storage,
cross-origin recovery management, unreadable-store salvage, and membership-
independent abandonment remain outside this repair.
