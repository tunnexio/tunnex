# EPIC 13 — review pass 3 PARTIAL (SALVAGED FROM DISK)

> ## UNRANKED · UNVERIFIED · NEVER CRITIQUED · DUPLICATED
>
> This is **not a review pass.** Run `wf_2e99572b-650` was launched, its 7 finder agents all returned, 22 of 44
> verifier agents returned, and **Critic and Synthesize never ran** — so nothing here has been ranked, deduplicated,
> or checked for what the finders could not see. It was salvaged from the run's journal rather than re-run, under a
> standing policy of no further multi-agent passes on this epic.
>
> **Read every entry as a CLAIM, not a finding.** Where a verifier verdict exists it is quoted beneath; most have
> none. Duplicates across finder angles are left in — collapsing them is exactly the work that did not happen.
>
> Surface: the agent recovery loop (`apps/node`) — attemptRekey and its retry loop, identity.Decide, saveCreds,
> control/rekey.go error mapping and backoff, the pending-keypair path, boot-time credential state.


## 1. [CRITICAL] A 200 re-key response is promoted to disk without ever checking cert_pem is a certificate — an empty/garbage cert destroys key.pem and the pending key, making re-key permanently impossible

`apps/node/internal/control/rekey.go:203` · error-mapping

**Claimed sequence**

```
Starting state: /var/lib/tunnex-node holds cert.pem (serial S1, NotAfter 3 days ago), key.pem (the key the CP recorded), ca.pem; TUNNEX_JOIN_TOKEN unset. Decide → Recover. attemptRekey mints and persists rekey-pending-key.pem. The challenge answers 200 with a nonce. The submit answers 200 with a body that does NOT carry the fields — a JSON-emitting proxy returning {"status":"ok"}, an ingress error page rendered as JSON, or a CP bug. json.Decode at rekey.go:200 succeeds (unknown fields are ignored, there is no `required` enforcement client-side), out.CertPEM=="", out.CAPEM=="", and rekey.go:203 returns (empty, empty, nil). I verified this against the real package: `err=<nil> len(cert)=0 len(ca)=0`. main.go:857 `if err == nil` is taken; main.go:861 calls saveCreds(certDir, "", pending, ""). saveCreds writes and renames ca.pem (empty), cert.pem (empty), key.pem (= the pending key, which the CP never issued a certificate over), then main.go:454 REMOVES rekey-pending-key.pem. attemptRekey returns cert="" — and main.go:77's `len(c) > 0` guard now reads false, so main correctly treats it as a failure, but the state directory has already been overwritten. With no token: agent_unrecoverable, idle. Wrong outcome: the expired cert (the only thing that carried the serial) and the private key the CP recorded are both gone, and the pending key is gone too — all three inputs re-key needs. Every subsequent boot reads three empty files, loadCreds returns nil error, and Decide's len(certPEM)==0 branch logs "no credentials in the state directory", which is false. The node can now only be recovered by a join token, i.e. by destroying its id, site binding and devices — the exact brick S13.1 exists to remove, caused by one malformed 200.
```

**Why it would matter:** This is the single response the entire recovery path trusts, it arrives over the PUBLIC listener (default plain http://api:8080), and it is written over the recovery inputs before anything looks at it. Distinct from the pass-1 ca_pem/trust-anchor finding: that one is about what the written ca.pem authorizes; this one is about the irreversible destruction of the serial + PoP key + pending key, which no later code path can undo. The package's own test asserts success against a server returning cert_pem:"CERT" (rekey_test.go:45), so the red that would have caught this is instead pinned to the defect.

**Suggested direction (NOT ruled):** In Rekey, before returning: require out.CertPEM and out.CAPEM non-empty, pem.Decode + x509.ParseCertificate the leaf, and assert its public key equals the pending key's public half (tls.X509KeyPair(certPEM, pendingKeyPEM) does both). Anything else is a refusal, not a success. Nothing may be written to the state directory until that check passes.

> **Verifier verdict** — refuted: `False` (confidence high)
>
> Verified every step by reading the code and by executing control.Rekey against a stub server. (1) rekey.go:193-203: after the status check, Decode into an anonymous struct (no DisallowUnknownFields, no required-field enforcement) then `return []byte(out.CertPEM), []byte(out.CAPEM), nil` — no length check, no pem.Decode, no x509 parse, no key-match check. Empirically confirmed with an httptest server answering the challenge with a nonce and the submit with `{"status":"ok"}`: `err=<nil> cert=""(len=0,nil=false) ca=""(len=0)`. (2) main.go:856-861: `newCert, newCA, err = control.Rekey(...); if err == nil { if serr := saveCreds(certDir, newCert, pending, newCA); ... }`. Nothing sits between the r
>
> **Correction:** The finding stands; two refinements. (a) The garbage (non-empty, non-PEM) variant is worse than the empty one and should be the headline. With cert_pem set to any non-empty non-certificate (an HTML error page rendered into a JSON string field, a truncated PEM), saveCreds promotes identically, but main.go:77's `len(c) > 0` now PASSES, so main takes the success branch and control.NewClient at main.g

> **Verifier verdict** — refuted: `False` (confidence high)
>
> Mechanism verified line by line. rekey.go:196-203 decodes a 200 into a hand-written anonymous struct with no presence check, so a JSON 200 lacking cert_pem returns ("", "", nil). main.go:859 tests only err==nil and calls saveCreds at 861 BEFORE any length check; saveCreds (431-455) renames ca.pem, cert.pem, key.pem and then removes rekey-pending-key.pem at 454. attemptRekey then returns the empty cert at 869, so main.go:77's len(c)>0 declares failure over a state directory already overwritten. Both re-key identifiers (StoredSerial from cert.pem, fingerprint of the CP-recorded key) are gone; loadCreds returns nil error over three empty files and Decide takes the len(certPEM)==0 branch on ever
>
> **Correction:** Three framing corrections. (1) The terminal state the finding names (agent_unrecoverable, idle) is the NO-TOKEN case, which is not the shipped Helm shape — decide.go:105-107 records that TUNNEX_JOIN_TOKEN is injected on every pod start, so the dominant outcome is the automatic silent fallback at main.go:95 to enrollWithToken: a NEW node with the site binding lost and devices needing re-issuing, wh

## 2. [CRITICAL] Expiry is decided ONCE at boot: the reconcile loop detects the expired cert at runtime, logs a now-false remedy, and never reaches attemptRekey

`apps/node/cmd/agent/main.go:64` · root2-input-decided-once

**Claimed sequence**

```
Starting state: agent running, /var/lib/tunnex-node holds cert.pem (NotAfter = T+48h), key.pem, ca.pem; no join token in env (the Helm shape injects one, the compose/systemd shape does not). (1) main.go:64 runs identity.Decide ONCE with time.Now(); the cert is valid, verdict = UseStored; the switch falls through and attemptRekey is never a candidate again for the life of the process. (2) The cert later expires in-process (see the renewLoop finding, or simply a 48h CP outage). (3) reconcile.go:412 client.Watch fails with `remote error: tls: expired certificate`. (4) reconcile.go:419 calls logBrickedOnce → certexpiry.go:26 CertExpiredRemedy matches → reconcile.go:444 logs `agent_cert_expired_cannot_reconnect` ONCE with the remedy string at certexpiry.go:42-46: "Retrying will NOT recover it. RE-ENROLL this gateway: create a join token...". (5) The loop then warns and sleeps forever; renewLoop warns forever; nothing re-runs identity.Decide, nothing calls attemptRekey. (6) serveHealth's /healthz (main.go:386) returns 200 unconditionally, so no orchestrator restarts the process; /readyz is 503 but readiness probes do not restart pods. Wrong outcome: the exact condition S13.1 was built to recover from is DETECTED in-process, and the response is a log line telling the operator to do the identity-destroying thing (re-enroll → new node, site binding lost, devices re-issued) that S13.1 exists to avoid. The gateway stays down until a human restarts the process. A second concrete trigger for the same defect: a box powered off over a weekend with a dead RTC boots believing it is still Friday — Decide at main.go:64 sees a valid cert and returns UseStored, NewClient succeeds (tls.X509KeyPair does not check dates), every request fails with tls: expired certificate, and when NTP steps the clock forward 20 seconds later nothing re-evaluates the verdict.
```

**Why it would matter:** S13.1's entire value is that an expired gateway recovers IN PLACE. The recovery is reachable only from a cold boot, and the one code path that recognises the condition at runtime actively steers the operator to the destructive fallback. certexpiry.go's remedy text and reconcile.go:440's comment ("The condition is terminal") were true before this branch and are false after it — a load-bearing comment vouching for a property the code no longer has.

**Suggested direction (NOT ruled):** Give the running agent a transition into recovery: have logBrickedOnce (or a supervisor around r.Run/renewLoop) signal a re-evaluation that re-reads cert.pem, re-runs identity.Decide with a fresh clock, and calls attemptRekey when the verdict is Recover; rebuild control.Client on success. At minimum, update certexpiry.go's remedy to name re-key first and make the process exit so an orchestrator restarts it into the boot recovery path (an exit is only acceptable if the boot path can actually fix it).

> **Verifier verdict** — refuted: `False` (confidence high)
>
> CONFIRMED by construction, not plausibility. CALL-SITE CENSUS. `grep -rn "identity.Decide\|attemptRekey" apps/node --include='*.go'` returns identity.Decide at main.go:64 and main.go:79, and attemptRekey at main.go:77 only. Lines 77 and 79 are both inside `case identity.Recover:` of the one-shot boot switch (main.go:73-101). No goroutine, no loop, no error handler anywhere in the binary re-invokes either. The recovery machinery S13.1 added is reachable only from the boot verdict. DETECTION PATH VERIFIED LINE BY LINE. reconcile.go:412 `go func() { watchCh <- client.Watch(ctx, r.version.Load()) }()`; on error reconcile.go:419 `r.logBrickedOnce(err)`; certexpiry.go:31/36 match either tls.AlertE
>
> **Correction:** The finding stands as written; one mechanism detail should be sharpened, and one clause is imprecise. SHARPENING — the sequence does not require a 24h control-plane outage. renewLoop's ticker (main.go:497, `TUNNEX_AGENT_RENEW_INTERVAL` default 24h) is anchored to PROCESS START, not to the certificate's NotAfter, and there is no renewal attempt at boot. So: a gateway is restarted (deploy, host rebo

> **Verifier verdict** — refuted: `False` (confidence high)
>
> MECHANISM — verified line by line, all claims check out. `identity.Decide` is called exactly twice, both at boot: main.go:64 (the real decision) and main.go:79 (a name-refresh after a successful re-key). Nothing in any loop calls it again, and `attemptRekey` has exactly one caller — the `case identity.Recover:` arm of the boot switch (main.go:76). Grep for `os.Exit` in main.go returns 155, 163, 251 — all pre-loop boot failures. After `r.Run(ctx, ...)` starts (main.go:373), there is no code path that exits, restarts, or re-decides identity for any reason short of SIGTERM. The runtime detection exists and is a dead end: reconcile.go:412 `client.Watch` fails → 419 `logBrickedOnce` → certexpiry.
>
> **Correction:** Two parts, both needed. 1. Re-evaluate identity at runtime instead of only at boot. When the reconcile/renew path sees the expired-certificate rejection (the condition certexpiry.CertExpiredRemedy already recognises at reconcile.go:443), route it back into the recovery path rather than into a log line — either by re-running identity.Decide + attemptRekey in-process, or, if in-process re-keying aga

## 3. [CRITICAL] A 200 re-key response with no cert_pem destroys the only recovery material and is logged as success

`apps/node/cmd/agent/main.go:857` · correctness

**Claimed sequence**

```
Starting state: cert.pem = expired cert serial S_old, key.pem = K_old (the key the CP recorded), ca.pem = the CA, no token. `TUNNEX_API_URL` is the plain-HTTP public listener (default `http://api:8080`), so anything on that path can answer: a proxy/WAF/captive portal, a mis-pointed service, or a future CP that renames the field.

1. main.go:64 Decide → Recover. main.go:77 attemptRekey. loadOrCreatePendingKey mints K_new and persists it (main.go:812).
2. control.Rekey posts; the responder answers 200 with a JSON object that simply has no `cert_pem` key (e.g. `{"status":"ok"}`, `{}`, `{"error":"..."}`). rekey.go:196-203 decodes into an anonymous struct, unknown fields are dropped, missing fields stay zero, `json.Decode` returns nil. Rekey returns `certPEM=[]byte("")`, `caPEM=[]byte("")`, `err=nil`.
3. main.go:857 `if err == nil` is TRUE. Nothing parses the certificate, nothing checks it is non-empty, nothing checks it matches `pending`, nothing checks it chains to the returned CA.
4. main.go:861 `saveCreds(certDir, newCert, pending, newCA)` writes a ZERO-LENGTH ca.pem, a ZERO-LENGTH cert.pem, and key.pem = K_new — then main.go:454 deletes rekey-pending-key.pem. S_old and K_old are now unrecoverable.
5. main.go:865 logs `agent_rekeyed … "recovered by proof of possession — same node, same identity, new key"`.
6. main.go:869 returns `newCert` (empty). main.go:77's guard is `len(c) > 0` → FALSE, so the caller treats the same call as a FAILURE the log just called a success. With no token: main.go:85 `agent_unrecoverable`, idle forever.
7. Next boot: loadCreds succeeds (all three files exist and read), `len(certPEM)==0` → decide.go:70 branch 1 → `no_identity_no_token` → Idle. The expired cert and its key — the only proof-of-possession material — are gone, so re-key can never be attempted again. With a token present, step 6 instead enrols a NEW node and discards the site binding and devices.

The same shape with a present `cert_pem` but absent `ca_pem` writes a zero-length ca.pem, and main.go:152 then fails `AppendCertsFromPEM` → os.Exit(1) → permanent crash loop against a certificate that is otherwise perfectly good.
```

**Why it would matter:** This is distinct from the known ca_pem/trust-anchor finding: the defect here is that NOTHING in the success path validates the response at all, and the write is irreversible. One well-formed-but-wrong 200 on an unauthenticated, unencrypted endpoint converts a gateway that was one retry away from recovering into one that cannot be recovered by any code path — while the log says it recovered.

**Suggested direction (NOT ruled):** Before saveCreds, require the response to be a usable identity: non-empty cert_pem and ca_pem, `tls.X509KeyPair(newCert, pending)` succeeds (it proves the cert was issued over the pending key), the leaf parses, and it verifies against the returned CA. Treat any failure as a refusal for retry purposes and leave the old set on disk untouched. Also make attemptRekey's success log and its return value derive from one condition — right now they disagree.

> **Verifier verdict** — refuted: `False` (confidence high)
>
> CONFIRMED by reading every function on the path; no guard prevents it. (1) rekey.go:189-203 — after a 200, `json.NewDecoder(resp.Body).Decode(&out)` into `struct{CertPEM string `json:"cert_pem"`; CAPEM string `json:"ca_pem"`}` drops unknown fields and leaves missing ones zero, returning `[]byte(""), []byte(""), nil`. There is no length check, no `pem.Decode`, no `x509.ParseCertificate`, no check that the cert matches `pending`, and no chain check — confirmed by grep: main.go contains no `ParseCertificate`, no `X509KeyPair`, no `len(newCert)`. (2) main.go:857 `if err == nil` is therefore true, and nothing sits between it and main.go:861 `saveCreds(certDir, newCert, pending, newCA)`. (3) saveC
>
> **Correction:** The finding stands as written. Two additions: (a) the same missing-field hole exists one call earlier in rekeyChallenge (rekey.go:213-223) — an absent `nonce` decodes to an empty byte slice with a nil error, so the replay-binding property asserted at rekey.go:128-131 is void and the signature covers only the CSR DER; a responder answering `{}` to both endpoints reaches saveCreds with zero valid da

## 4. [CRITICAL] Boot-time clock skew classifies a valid identity as expired, and the re-key loop never re-reads the clock

`apps/node/cmd/agent/main.go:64` · correctness

**Claimed sequence**

```
Starting state: a healthy gateway, cert.pem valid with NotAfter 2026-07-31T12:00Z, key.pem matching, CP reachable. The host's clock reads 2026-08-01 at boot — NTP has not stepped yet (chrony typically corrects within seconds-to-minutes of start), or the VM was resumed from a snapshot, or the RTC drifted.

1. main.go:64 `identity.Decide(certPEM, err, nodeName, joinToken != "", time.Now())`; decide.go:96 `expired := now.After(leaf.NotAfter)` → true → Action = Recover.
2. main.go:77 attemptRekey. main.go:792-849 sample `serial` and build `identities` ONCE, before the loop at 852.
3. control.Rekey reaches the real CP. nodes/rekey.go:177 calls RekeyAuthorized, which at policyhealth.go:351 returns false — "the node's certificate is still valid until … a live gateway must never be re-keyed" — and returns the uniform 403.
4. rekey.go:194 → ErrRekeyRefused. main.go:908 logs `most_likely_cause: enrolled before the control plane recorded agent public keys … or it was REVOKED` — both false.
5. 20 seconds later chrony steps the clock back to the correct time. The certificate is valid; the mTLS channel would work immediately.
6. The loop at main.go:852 has exactly two exits (success and ctx cancel). It never calls time.Now() again, never re-parses cert.pem, never re-runs Decide. It backs off to the 1h ceiling and re-submits the same refused request forever.
7. main() is blocked inside attemptRekey, so r.Run never starts, no reconcile ever happens, the data plane never converges. serveHealth (started at main.go:51) answers /healthz 200 for the whole life of the process, so a liveness probe stays green and nothing restarts the pod. `ready` stays false silently.

Wrong outcome: a gateway holding a valid certificate is permanently offline after a few seconds of clock skew, and only a human restart recovers it.
```

**Why it would matter:** The comment at main.go:74-76 vouches for the opposite property — "The trigger is local: identity.Decide reached this verdict from this agent's own clock … so a transient outage cannot land here." Locality is not stability: the local clock is the one input that is routinely wrong for the first seconds of a boot, which is exactly when Decide is sampled. decide_test.go:196 (`TestExpiryBoundaryIsExclusive`) acknowledges the hazard — "a clock a millisecond fast must not re-enroll a live gateway" — and then guards only the exact-nanosecond boundary, while real skew is minutes. Worse, the obvious fix to the known 'loop never exits' finding makes this destructive rather than merely fatal: once the loop can fall through, a fast clock reaches enrollWithToken and creates a NEW node, discarding the live gateway's site binding and devices — the WF-2 disaster this package claims to prevent.

**Suggested direction (NOT ruled):** Re-evaluate the verdict inside the retry loop rather than sampling it once: re-read cert.pem and the clock each iteration and abandon Recover if the certificate is no longer expired. Separately, treat 'expired' as needing a margin (e.g. only Recover once now is past NotAfter by more than the plausible skew, and log the local clock alongside NotAfter so the refusal diagnosis can name skew as a candidate cause).

## 5. [CRITICAL] renewLoop's ticker is anchored to process start, not to the certificate's NotAfter — any restart past cert half-life guarantees expiry while running, and a single failed renew is unrecoverable

`apps/node/cmd/agent/main.go:497` · correctness

**Claimed sequence**

```
Starting state: gateway enrolled at T0, agentca.CertTTL = 48h (apps/api/internal/agentca/ca.go:33), state dir on the chart's default PVC (deploy/helm/tunnex-gateway/values.yaml:75 persistence.enabled=true), so cert.pem survives restarts.

(1) T0+25h: the pod/container restarts for any routine reason — image bump, node drain, OOM kill, `docker restart`, host reboot. cert.pem is still valid (NotAfter T0+48h).
(2) main.go:64 identity.Decide → decide.go:143 UseStored. No renewal is performed at boot: the ONLY call to client.Renew in the whole agent is main.go:504, driven by main.go:497 `t := time.NewTicker(every)` with every = 24h, whose FIRST fire is 24h after PROCESS START — i.e. T0+49h. Nothing anywhere reads leaf.NotAfter to schedule renewal (grep for NotAfter in apps/node returns only identity/decide.go, all boot-time).
(3) T0+48h: the certificate expires. FetchDesired/Watch/ReportInfo/ReportStatus all fail `tls: expired certificate`; reconcile.go:319 stores healthy=false; the whole tunnel is down.
(4) T0+49h: the first renew tick fires, client.Renew posts over the same dead mTLS channel, fails, main.go:506 logs a WARN and `continue`s — the next attempt is T0+73h, equally dead. Forever.
(5) The S13.1 recovery the agent is fully capable of (the recorded keypair is right there on disk) is never attempted, because identity.Decide/attemptRekey run ONLY in the boot sequence (main.go:64-150). The process must be restarted by a human; the shipped chart has no probe that would ever restart it (see the readiness finding).

Second, independent sequence from the same root — no restart needed: process starts at T0; renew tick at T0+24h; the CP is unreachable for 30 seconds (rollout, ingress blip) → main.go:505-507 WARN + `continue` → next attempt at T0+48h, which is later than NotAfter by everything except the enrolment round-trip (~1s). One transient failure at the only renewal opportunity brings the certificate down; the retry cadence equals the entire remaining lifetime.

This also falsifies the load-bearing claim in walk-artifacts/S13.1/clock-record.md:16 — "A RUNNING agent never expires. renewLoop renews every 24h ... so a live gateway refreshes its certificate at half its lifetime, forever" — on which the walk's whole staging plan rests. The record even states (point 2) that the ticker has no immediate first tick, and uses that fact for staging, without drawing the consequence for a restart past 24h.
```

**Why it would matter:** Every gateway that restarts in the second half of its certificate's life goes silently dark 48h after issuance and stays dark, taking every device and site link on it down, with no self-recovery and no orchestrator signal. Restarts of a K8s pod are routine, so this is not an edge case; it is the expected steady-state failure of a fleet.

**Suggested direction (NOT ruled):** Schedule renewal from the certificate itself, not from process start: parse cert.pem's NotAfter at boot (identity already does), renew immediately when now > NotAfter - TTL/2, and re-arm the timer from each new cert's NotAfter after every successful renew. On a renew failure retry with a bounded backoff (seconds→minutes) rather than waiting a full interval, and escalate to the boot recovery path (attemptRekey) once NotAfter has passed.

## 6. [CRITICAL] The lost-response recovery path cannot work: after a committed re-key the control plane's gate refuses the fingerprint identity the pending key exists to serve

`apps/node/cmd/agent/main.go:838` · correctness

**Claimed sequence**

```
Starting state: gateway A, cert expired, key recorded by the CP, no join token yet.

(1) Boot: Decide → Recover → attemptRekey. main.go:812 mints and persists pending key P (wasOnDisk=false), so identities = [cert_serial S] only.
(2) control.Rekey(serial S) succeeds ON THE SERVER: apps/api/internal/nodes/rekey.go consumes the nonce, resolves node A, RekeyAuthorized passes (cert expired), verifies the PoP, signs the CSR over P, and COMMITS cert_serial=S2, cert_public_key=SPKI(P), cert_not_after=now+48h.
(3) The response is lost — connection reset, ingress timeout, the pod is killed. This is the exact scenario main.go:800-811 says the pending key exists for.
(4) Restart. cert.pem is still the OLD expired cert, rekey-pending-key.pem survives → loadOrCreatePendingKey returns wasOnDisk=true → main.go:838-844 puts the fingerprint identity FIRST, exactly as designed.
(5) The CP resolves it correctly (nodes.rekeyident/GetNodesByCertKeyFingerprint finds node A — cert_key_fingerprint is generated from cert_public_key = SPKI(P)), and then REFUSES at the gate one step later: apps/api/internal/nodes/rekey.go:167 calls RekeyAuthorized, and apps/api/internal/nodes/policyhealth.go:351 returns false because node A's cert_not_after is now 48h in the FUTURE — "a live gateway must never be re-keyed". Uniform 403.
(6) The serial identity is tried next and fails too (GetNodeByCertSerial(S) has no rows; the row now holds S2). Every subsequent attempt, forever, hits the same two refusals.

Wrong outcome: the agent holds private key P, the CP holds the matching certificate, and there is no path that delivers it — the agent has no mTLS channel and the re-key endpoint refuses because the re-key already worked. Recovery requires an operator-minted join token, which is precisely the loss (new node, site binding gone, devices re-issued) that main.go:800-811, D10, TestPendingKeyIsPersistedBeforeAnySubmit and TestSaveCredsClearsThePendingKey all vouch for having eliminated. Note this survives fixing the pass-1 finding about pendingWasOnDisk being sampled before the loop: re-reading it in the loop still lands on a refusal.
```

**Why it would matter:** An entire subsystem — persist-before-submit, the second identifier kind, a nullable DB column, a generated column, three golden-vector implementations, the wasOnDisk ordering and its tests — is inert for the case it was built for, while the comments and tests read as proof that the case is covered. The one dropped packet still costs a gateway its identity, and now it does so invisibly behind machinery that says otherwise.

**Suggested direction (NOT ruled):** The gate and the recovery path have to agree. Either let RekeyAuthorized authorize when the request identifies by key_fingerprint AND the CSR's public key equals the currently recorded cert_public_key (a re-assertion of the identity the CP just issued, not a re-key of a live gateway — idempotent re-delivery of the same certificate), or have the CP retain the last issued cert for a bounded window and serve it to a PoP over the recorded key. Whatever is chosen, the agent-side comment block at main.go:800-844 must state the actual coverage, and a test must exercise the committed-then-lost-response sequence end to end rather than only the on-disk persistence of the key.

## 7. [CRITICAL] attemptRekey throws away a certificate the control plane has already issued when the local write fails, and the CP will never issue it again

`apps/node/cmd/agent/main.go:863` · correctness

**Claimed sequence**

```
Starting state: gateway A, expired cert, state volume full (ENOSPC), quota-exceeded, or remounted read-only — a routine ops condition on a 128Mi PVC that also holds logs/flow buffers.

(1) attemptRekey succeeds on the wire: control.Rekey returns newCert/newCA, and the CP has COMMITTED the new identity (cert_serial=S2, cert_not_after=now+48h).
(2) main.go:861 saveCreds fails on the first os.WriteFile (ENOSPC). main.go:862-863 logs and `return nil, nil, nil` — the SAME signal used for "refused persistently" and for "the context ended". newCert is discarded from memory although it is a fully valid credential the agent could have used for the next 48h.
(3) main.go:77 `len(c) > 0` is false → main.go:84: on the shipped Helm shape TUNNEX_JOIN_TOKEN is injected on every pod start, so HaveToken is true → main.go:92 logs "agent_falling_back_to_join_token ... This creates a NEW node" → enrollWithToken.
(4) Either the enrolment succeeds and a NEW node row is created, abandoning the node the CP re-keyed IN PLACE seconds earlier (site binding lost, devices need re-issuing) — the exact outcome the Recover branch exists to prevent — or the CP refuses because the name is still held by node A's row (which is now ACTIVE with a fresh 48h cert), and main.go:102 idles forever.
(5) There is no way back on a later boot either: cert.pem is still the old expired cert, and every re-key attempt — by serial S (no rows) or by fingerprint (resolves node A, whose cert_not_after is now in the future) — is refused by apps/api/internal/nodes/policyhealth.go:351.

Same conflation, second instance: when the context is cancelled during the retry loop (main.go:899/924 sleepCtx returns false), attemptRekey also returns nil,nil,nil, so every SIGTERM during recovery makes main.go:92 log a false "we are creating a NEW node" WARN and then call enrollWithToken with an already-cancelled context, which necessarily fails and logs agent_enroll_failed at ERROR — an alarming, entirely fictional pair of lines emitted by an ordinary `docker stop`.
```

**Why it would matter:** A transient local disk condition converts a successful in-place recovery into a permanent identity loss — the strongest failure this story exists to prevent — and the agent had the working credential in a local variable when it did so. The single nil return also makes 'refused', 'shutting down' and 'recovered but could not persist' indistinguishable to the caller, so the fallback that costs the node its identity is triggered by two conditions that are not refusals.

**Suggested direction (NOT ruled):** Return a typed outcome from attemptRekey (recovered / refused / interrupted / persisted-failed) and let main branch on it: never fall back to the join token on interrupted or persisted-failed. On a persist failure, keep using the in-memory credentials for this run (the CP already holds them) and retry the write on an interval, logging loudly; on ctx cancellation return without the fallback log and without calling enrollWithToken with a dead context.

## 8. [CRITICAL] The re-key response is written to disk before anything about it is validated — one unauthenticated 200 destroys the identity

`apps/node/cmd/agent/main.go:861` · unvalidated-wire-input

**Claimed sequence**

```
Starting state: /var/lib/tunnex-node holds cert.pem (expired, serial S), key.pem (the key the CP recorded), ca.pem; TUNNEX_API_URL=http://api:8080 (plain HTTP, chart default, no server authentication of any kind). Boot → Decide → Recover → attemptRekey. Anything that can answer the plain-HTTP API URL once — an ingress/mesh returning a JSON error object with 200, a caching proxy, or an off-path attacker who wins a DNS/ARP race — replies to POST /api/v1/agent/rekey with `200 {"error":"unauthorized"}`. control.Rekey (rekey.go:193-203) sees StatusOK, json.Decode succeeds, CertPEM="" and CAPEM="", and returns (nil, nil, nil-error). attemptRekey takes the err==nil branch at main.go:857 and calls saveCreds(certDir, newCert="", pending, newCA="") at main.go:861. saveCreds renames ca.pem←0 bytes, cert.pem←0 bytes, key.pem←the pending key (overwriting the key the CP recorded), then os.Remove()s rekey-pending-key.pem (main.go:454). Only AFTER the disk is destroyed does main.go:77 evaluate `len(c) > 0`, find it false, and fall through to "agent_unrecoverable". Every subsequent boot: loadCreds succeeds (0-byte files are not read errors), Decide hits branch 1 (len(certPEM)==0) → Idle or UseToken; StoredSerial is "" so attemptRekey is unreachable forever. Nothing is checked: not that cert_pem parses as a certificate, not that it matches the `pending` key it is being filed next to, not that it chains to any CA, not that ca_pem is non-empty. The identical hole exists on the token path at main.go:974 (res.CertPEM/res.CAPEM go straight to saveCreds), where the additional cost is a consumed one-time join token plus os.Exit(1) at main.go:155 when NewClient rejects the pair.
```

**Why it would matter:** This is the exact brick S13.1 exists to remove — serial gone, recorded key gone, pending key gone, recovery only by a join token that creates a NEW node with the site binding and devices lost — and the recovery path itself inflicts it, from an unauthenticated response, with no crypto on the attacker's side. The authenticated sibling already does the check the unauthenticated one skips: client.go:134 runs tls.X509KeyPair(body, keyPEM) and refuses a cert that does not match the key it just generated. The dangerous path is the one missing it.

**Suggested direction (NOT ruled):** Validate the whole response in control.Rekey before it can reach disk: cert_pem and ca_pem non-empty, both PEM-decode and x509-parse, the leaf's public key equal to the pending key's public key, and the leaf verifiable against the returned CA (and, per pass-1 finding #1, against the CA already on disk). Return ErrRekeyRefused otherwise. Apply the same three checks to Enroll's result before enrollWithToken's saveCreds.

## 9. [HIGH] A 429 from any hop pins the agent at a 30s retry forever: no escalation, no exit, the join-token fallback and its remedy are never reached, and the log asserts the CP is throttling when the agent cannot know that

`apps/node/cmd/agent/main.go:891` · error-mapping

**Claimed sequence**

```
Starting state: expired cert + key on disk, TUNNEX_JOIN_TOKEN set, and a CDN/WAF/ingress in front of the API that returns 429 to POSTs from this source range (Cloudflare and most WAFs use 429 for their own rate limits). Decide → Recover. attemptRekey: rekeyChallenge gets 429 → rekey.go:216 returns ErrRekeyThrottled → main.go:871 `break` out of the identity loop → main.go:891 the throttle branch is taken → wait is hard-coded to rekeyBackoffFloor (30s) → sleep → `continue` at main.go:904, which deliberately does NOT advance `backoff` (main.go:902-903). The loop at main.go:852 has exactly two exits: a successful re-key and ctx cancellation. So this repeats every 30s for the life of the process. Wrong outcome: (a) `attemptRekey` never returns, so main.go:92 agent_falling_back_to_join_token is never reached and the operator's join token — which would recover the gateway — is never used; (b) the ERROR-level agent_rekey_refused block at main.go:908, which is the ONLY place the remedy string ("mint a join token in the control plane and restart this agent") is ever printed, is never reached either, so the operator gets no remedy at all; (c) every 30s the agent logs, at WARN, "the control plane is rate-limiting re-key attempts; this is NOT a refusal and says nothing about whether this gateway can recover" — three assertions the code cannot support, because nothing in Rekey or rekeyChallenge distinguishes a 429 emitted by rekeythrottle.go:99 from a 429 emitted by any intermediary. The pod stays liveness-green (serveHealth /healthz is unconditional), so no orchestrator restarts it.
```

**Why it would matter:** The gateway is down, a working recovery credential is sitting in its environment, and the agent will never try it or tell the operator to. This is not the pass-1 'no exit on persistent refusal' finding: that path at least prints the remedy on every iteration and escalates toward the hour ceiling. This one prints an authoritative denial that anything is wrong, at the floor, forever. The distinguishability question is answerable on the wire — the CP's own 429 is `http.Error(w, "too many requests", 429)` with Retry-After: 60 and no X-Request-Id header, where every CP response carries X-Request-Id — and the agent looks at none of it.

**Suggested direction (NOT ruled):** Count consecutive throttles and, past a bound, fall through to the refusal path so the remedy is printed and the token fallback is reached; treat a 429 that carries no X-Request-Id (or whose body is not the CP's error envelope) as 'throttled by something in front of the control plane' and say so in the log instead of claiming the CP said it.

> **Verifier verdict** — refuted: `False` (confidence high)
>
> Verified by construction, line by line. (1) Provenance conflation is real: rekey.go:215-217 (rekeyChallenge) and rekey.go:190-192 (Rekey) both return ErrRekeyThrottled for ANY 429, with no inspection of body, headers or source — nothing distinguishes rekeythrottle.go's 429 from a CDN/WAF/ingress 429. (2) Control flow confirmed: err is declared at main.go:853 and assigned by `newCert, newCA, err = control.Rekey(...)` at 856, so the `break` at main.go:872 leaves err == ErrRekeyThrottled; `errors.Is` at main.go:891 matches through the fmt.Errorf %w wrap; main.go:892 hard-codes wait = rekeyBackoffFloor (30s, main.go:779); main.go:904 `continue` re-enters the loop at main.go:852 without advancing
>
> **Correction:** Finding stands; three refinements. (a) Claim (b) is overstated: the remedy string also appears at main.go:795 (agent_rekey_impossible) and main.go:86 (agent_unrecoverable). Neither is on this path either, so the outcome — the operator sees only a repeating WARN and never a remedy — holds, but "the ONLY place" is inaccurate. (b) The starting state does not require a CDN/WAF. apps/api/internal/http/

> **Verifier verdict** — refuted: `True` (confidence medium)
>
> MECHANISM: real, and I confirmed every line of it. main.go:891-904 takes the throttle branch, hard-codes `wait := rekeyBackoffFloor`, sleeps, and `continue`s without touching `backoff`; the loop at main.go:852 has only two exits (success, ctx cancel); rekey.go:189 and 216 both map a bare 429 to ErrRekeyThrottled with no provenance. So a 429 that never clears does pin the agent at 30s forever. Nothing in the mechanism is wrong. WHY IT IS NOT THE FINDING IT CLAIMS. Three reasons, in order of weight. (1) Consequence (a) and (b) are the ALREADY-FOUND defect, not a new one. Pass-1 #2 (main.go:852) is exactly "the retry loop has no exit on persistent refusal, so the join token is never reached." T
>
> **Correction:** Not a standalone defect. The unbounded 30s throttle loop is a SECOND INSTANCE of the missing loop exit already reported as pass-1 #2 (main.go:852), differing only in retry cadence (30s vs the 1h ceiling the refusal path reaches) — the terminal state, the unreached join-token fallback at main.go:92, and the unreached remedy string are the same outcome from the same missing exit. Its incremental pre

## 10. [HIGH] Retry-After is parsed, formatted into the error string, then discarded; the agent retries at 30s against a control plane that asked for 60s and whose window is 60s, and logs two contradictory numbers

`apps/node/cmd/agent/main.go:892` · error-mapping

**Claimed sequence**

```
Starting state: a 100-gateway fleet behind one NAT recovering together (the exact scenario rekeythrottle.go:107-127 sizes the budget for); the shared bucket is exhausted. rekeythrottle.go:99 answers 429 with `Retry-After: 60`. rekey.go:41-48 retryAfter parses "60" → 60s; rekey.go:191/216 formats it into the error text and returns; NOTHING reads it. main.go:892 sets `wait := rekeyBackoffFloor` = 30s unconditionally. The agent retries at t+30s, lands inside the CP's still-open 60s fixed window (rekeythrottle.go:67 sets reset = firstRequest+1m), is 429'd again, waits 30s again. Wrong outcomes: (1) the server's explicit pacing instruction is computed and thrown away, so roughly half of all retry attempts are structurally guaranteed to fail and each still costs two requests against the shared bucket the fleet is contending for — the agent actively worsens the congestion the CP asked it to relieve; (2) the operator's log line reads `retry_in=30s` beside `error=control plane throttled the re-key attempt (retry after 1m0s)` — two different numbers for the same wait, in one record, and the one the code obeys is the one the server contradicted. Same line with Retry-After absent, an HTTP-date (RFC 7231's other legal form), a negative value, or anything ≥3600: retryAfter returns 0 and the error reads "(retry after 0s)", telling the operator the server asked for an immediate retry when the server said nothing at all or asked for an hour.
```

**Why it would matter:** The CP's throttle comment derives its 600/min budget from the assumption that a gateway makes 4 requests/min. A throttled gateway that ignores Retry-After keeps making exactly that many while contributing zero progress, and the honest-diagnosis property the throttle branch was written to protect is defeated by the log printing two conflicting waits.

**Suggested direction (NOT ruled):** Return the parsed duration from Rekey (a typed error carrying RetryAfter, or an out-param) and have main.go:892 use max(retryAfter, rekeyBackoffFloor) as the wait, logging that one number only. Accept the HTTP-date form, and when the header is absent/unparseable say 'server gave no retry hint' rather than emitting 0s.

> **Verifier verdict** — refuted: `False` (confidence high)
>
> Verified by construction, not plausibility. (a) rekey.go:41-47 retryAfter returns a time.Duration; rekey.go:191 and :216 are `fmt.Errorf("%w (retry after %s)", ErrRekeyThrottled, retryAfter(resp))` — the duration reaches only %s. `grep -rn retryAfter apps/node/` yields exactly 3 hits (definition + those two format calls): no typed error, no field, no errors.As target, so the value is unreachable to any caller. (b) main.go:892 `wait := rekeyBackoffFloor` with rekeyBackoffFloor = 30*time.Second (main.go:779); `wait` is assigned once, logged at :896, slept at :898, never reassigned — a local that exists only to be logged. (c) CP side confirmed: rekeythrottle.go:99 sets `Retry-After: 60`, and :6
>
> **Correction:** The mechanism (parsed -> formatted into a string -> discarded; caller waits 30s unconditionally; doc comment vouches for a caller reaction that does not exist) is CONFIRMED. But the congestion half of the claimed impact is wrong on three counts, and the finding should be restated as a diagnostic-honesty defect rather than a load defect. 1. "each still costs two requests against the shared bucket" 

> **Verifier verdict** — refuted: `True` (confidence high)
>
> The MECHANISM is real and I confirm it: `retryAfter` (apps/node/internal/control/rekey.go:41-48) is computed, `%s`-formatted into the error text at rekey.go:189 and rekey.go:216, and never returned as a value; `attemptRekey` (apps/node/cmd/agent/main.go:892) sets `wait := rekeyBackoffFloor` = 30s unconditionally. Nothing reads the parsed duration. That part is accurate. But under the operational-consequence lens the finding fails on all three of its claimed outcomes, and its stated trigger scenario cannot fire. (1) THE STATED STARTING STATE NEVER PRODUCES A 429. The finding says "a 100-gateway fleet behind one NAT ... the shared bucket is exhausted." The budget comment it cites (rekeythrottl
>
> **Correction:** Real mechanism, no operational consequence — downgrade to a log-text nit. What is true: `retryAfter` at apps/node/internal/control/rekey.go:41 is parsed, formatted into the error string at rekey.go:189/216, and discarded; main.go:892 waits a fixed 30s. What is not true: - The claimed trigger (100 gateways behind one NAT) produces 400 req/min against a 600/min budget and never yields a 429. The 600

## 11. [HIGH] SIGTERM during re-key is reported as "re-key was refused and no join token is available", and with a token it triggers a join-token enrolment attempt on an already-cancelled context

`apps/node/cmd/agent/main.go:84` · misleading-diagnosis

**Claimed sequence**

```
Starting state: gateway with an expired cert, re-key being refused (say the CP is mid-restart, returning 502 through the ingress), agent in the attemptRekey backoff at main.go:924. An operator or the orchestrator sends SIGTERM (kubectl delete pod, docker stop, a node drain, a Helm upgrade). ctx is cancelled; sleepCtx returns false; main.go:925 returns nil,nil,nil. main.go:77's `len(c) > 0` is false. Nothing between main.go:77 and main.go:95 consults ctx.Err(), so the ctx-cancelled return is indistinguishable from exhaustion. Case A (no token): main.go:85 logs at ERROR `agent_unrecoverable`, reason "re-key was refused and no join token is available", remedy "mint a join token in the control plane and restart this agent" — then main.go:89 `<-ctx.Done()` returns instantly and the process exits. The statement is false: re-key was not refused, the process was asked to stop. Case B (token set): main.go:92 logs at WARN `agent_falling_back_to_join_token`, "This creates a NEW node: its site binding must be re-applied and devices homed on the old node need re-issuing" — then calls enrollWithToken(ctx, ...) with the dead context, whose Enroll fails immediately with "context canceled", producing an ERROR `agent_enroll_failed` whose note says "NOT exiting — this may be resolvable from the control plane (a name still held by this node's own expired row, a consumed token, a restarting CP)". Three log records, all naming causes that did not occur, emitted on every single graceful restart of a gateway that is mid-recovery.
```

**Why it would matter:** attemptRekey's own contract comment (main.go:783-784) states it 'Returns empty slices if the context ends first' — the caller was told about this case and does not handle it. The consequence is the destructive one: the log the operator reads during a routine pod restart tells them re-key was refused and instructs them to mint a join token, which discards the node's id, site binding, devices and metrics series. Every rolling upgrade of an expired-cert gateway prints an instruction that destroys it.

**Suggested direction (NOT ruled):** Check ctx.Err() immediately after attemptRekey returns; on cancellation log a shutdown record and return without claiming a refusal and without attempting enrolment. Better: have attemptRekey return a typed outcome (recovered / refused / cancelled) so the caller cannot conflate them.

## 12. [HIGH] renewLoop's ticker is anchored to process start, not to the certificate's NotAfter, and a failed renew waits a full interval — any restart in the 24h–48h cert-age window guarantees expiry before the first attempt

`apps/node/cmd/agent/main.go:496` · root2-input-decided-once

**Claimed sequence**

```
Starting state: cert issued at T with NotAfter = T+48h (agentca.CertTTL = 48h, a constant). The agent restarts at T+30h — a node reboot, an image update, an OOM kill, a k8s eviction; the cert has 18h left. (1) main.go:64 Decide → UseStored (valid). (2) main.go:347 renewEvery = 24h (default, and grep shows no deployment sets TUNNEX_AGENT_RENEW_INTERVAL anywhere in the repo). (3) main.go:497 `t := time.NewTicker(every)` — the ticker starts NOW, at T+30h, and has no immediate first tick. (4) The first tick fires at T+54h. The cert expired at T+48h, six hours earlier. (5) main.go:504 client.Renew fails with tls: expired certificate; main.go:506 logs a Warn and `continue`s — back to a select that waits another FULL 24h. Nothing in the loop ever reads leaf.NotAfter, so the loop cannot know it is scheduling past its own deadline. Wrong outcome: a single ordinary restart in the second half of a certificate's life deterministically brings the gateway to expiry with zero renewal attempts, and (per the finding above) there is no in-process route from there to recovery. The same defect fires without any restart: one transient failure at the T+24h tick (a 60-second CP restart) puts the next attempt at T+48h, i.e. at or after NotAfter.
```

**Why it would matter:** walk-artifacts/S13.1/clock-record.md:16 states as fact that "A RUNNING agent never expires — renewLoop renews every 24h, so a live gateway refreshes its certificate at half its lifetime, forever." That is the premise the whole EPIC 13 walk is staged on, and it is false. It also means the population that needs S13.1's recovery is far larger than "the box was off over the weekend" — it includes every gateway that restarts in a routine maintenance window.

**Suggested direction (NOT ruled):** Schedule renewal off the certificate, not off process start: parse NotAfter at boot and sleep until half-life (fire immediately if already past it), and on failure retry with a bounded backoff whose ceiling is well under the remaining lifetime — never a full interval. Re-derive the schedule after each successful renew.

> **Verifier verdict** — refuted: `False` (confidence high)
>
> Every step of the sequence verified by reading the code. main.go:347 renewEvery defaults to 24h and TUNNEX_AGENT_RENEW_INTERVAL is set nowhere in the repo (grep hits only main.go:347 and walk-artifacts/S13.1/clock-record.md:16 — not docker-compose.yml, not deploy/tunnex.yml, not deploy/helm/tunnex-gateway/templates/deployment.yaml). agentca/ca.go:33 CertTTL = 48h is a constant, stamped at ca.go:198 NotAfter: time.Now().Add(CertTTL). main.go:497 `t := time.NewTicker(every)` anchors phase to process start with no immediate first tick and no t.Reset. main.go:503-507 on error logs Warn and bare `continue`, returning to the same select for another full interval. grep -rn NotAfter over apps/node (
>
> **Correction:** The finding is confirmed; two refinements make it strictly worse, not weaker. (1) The no-restart variant is not confined to the first tick — it is a general zero-margin property of EVERY renewal. After any successful renew at time R, ca.go:198 sets NotAfter = R+48h while the ticker's next two firings are R+24h and R+48h, so any single failed tick at any point in the agent's life places the next at

> **Verifier verdict** — refuted: `False` (confidence high)
>
> MECHANISM — CONFIRMED BY READING, all four legs. (1) main.go:497 `t := time.NewTicker(every)` — anchored to process start, no immediate first tick. (2) main.go:347 default 24h, and grep across compose/helm/docs/walk-artifacts finds nothing setting TUNNEX_AGENT_RENEW_INTERVAL. (3) agentca/ca.go:33 CertTTL = 48h, a constant. (4) main.go:504-507: a failed Renew logs Warn and `continue`s to a select that waits a full interval; control/client.go:118-143 Renew makes ONE HTTP attempt with no internal retry. Nothing in the loop reads the leaf's NotAfter. OPERATIONAL CONSEQUENCE — it reaches an operator-visible bad outcome and does NOT self-heal. On expiry the agent does not exit and does not re-key:
>
> **Correction:** CONFIRMED, with the trigger restated and one stronger instance added. Correct precondition: the agent BOOTS with a stored cert already older than the renew interval (24h) — not "any restart in the 24h-48h window". An instantaneous OOM kill or eviction of a healthy agent is harmless, because the prior process renewed at its own tick and the successor inherits a cert younger than 24h. The reachable 

## 13. [HIGH] attemptRekey never re-evaluates the expiry premise, so a clock that is fast at boot traps a gateway holding a perfectly valid certificate in an unbounded refusal loop

`apps/node/cmd/agent/main.go:851` · root2-input-decided-once

**Claimed sequence**

```
Starting state: an edge gateway / appliance / suspended VM with no synced clock at boot (no RTC, or an RTC 2h fast). cert.pem NotAfter = real-time now+1h; the OS clock reads now+2h. (1) main.go:64 Decide(certPEM, nil, name, haveToken, time.Now()) — decide.go:96 `expired := now.After(leaf.NotAfter)` is true against the skewed clock → verdict Recover. (2) main.go:77 attemptRekey. (3) main.go:852 the outer `for attempt := 1; ;` loop begins; its only exits are a nil error from control.Rekey and ctx cancellation. It samples nothing about the clock and re-reads neither the clock nor cert.pem. (4) Each attempt reaches the CP, which gates on ITS OWN clock: policyhealth.go:347 `if certNotAfter.Before(now)` is FALSE — the node's cert is still valid server-side — so policyhealth.go:351 returns not-authorized ("a live gateway must never be re-keyed") and rekey.go:194 returns the uniform ErrRekeyRefused. (5) chronyd/NTP steps the clock back 30 seconds after boot; the certificate is now provably valid to this agent too. Nothing notices. (6) main.go:927-931 doubles the backoff to the 1h ceiling and stays there; main.go never proceeds past line 77 to control.NewClient, so no client, no reconciler, no data plane. Wrong outcome: a gateway with a valid, usable certificate is permanently down — liveness green, readiness false, /healthz 200 so nothing restarts it — from a transient clock condition that self-corrected within a minute.
```

**Why it would matter:** main.go:74-76 explicitly vouches for the opposite: "The trigger is local: identity.Decide reached this verdict from this agent's own clock against its own stored certificate, with no network call, so a transient outage cannot land here." The local clock IS the transient input, and it is the only input the verdict rests on. decide_test.go:194 even names the hazard ("a clock a millisecond fast would otherwise re-enroll a live gateway") and pins only the exact-boundary case, not the skew case. The same trap catches a deliberately fast clock set by an operator debugging something else.

**Suggested direction (NOT ruled):** Re-read the premise inside the loop: at the top of each attempt, re-parse cert.pem and re-check expiry against a fresh time.Now(); if the certificate is no longer expired, abandon the recovery and return the stored credentials so main proceeds normally. Independently, refuse to start a Recover on a clock that has not been established as synced (or bound the loop and re-run Decide on exit).

> **Verifier verdict** — refuted: `False` (confidence high)
>
> CONFIRMED by construction; no guard prevents it. (1) The premise is local-clock-only and has no skew tolerance: main.go:64 `identity.Decide(certPEM, err, nodeName, joinToken != "", time.Now())`, decide.go:107 `expired := now.After(leaf.NotAfter)`. There is no minimum-plausible-time check, no NTP-sync wait, and `grep -rn "skew|clock" apps/node` finds nothing on this path. A boot clock fast by more than the cert's remaining validity yields Recover for a cert that is valid in real time. (2) The loop genuinely never revisits it. attemptRekey samples `serial := identity.StoredSerial(certPEM)` (main.go:792) and builds `identities` (836-849) before `for attempt := 1; ; attempt++` at 852. Inside the
>
> **Correction:** The finding stands as written; two refinements to the mechanism. (a) Cite the vouching comment, because it is what makes this survivable in review. main.go:75-76 states "The trigger is local: identity.Decide reached this verdict from this agent's own clock against its own stored certificate, with no network call, so a transient outage cannot land here." Falsified by construction: the local clock I

> **Verifier verdict** — refuted: `True` (confidence high)
>
> The mechanism is real and confirmed: identity.Decide (decide.go:96) judges expiry on the agent's local clock, attemptRekey samples serial/identities/pendingWasOnDisk before the loop at main.go:851, the loop's only exits are success and ctx-cancel, main.go never proceeds past line 77, and /healthz (main.go:385) returns 200 unconditionally so no orchestrator restarts it. The CP gate (nodes.RekeyAuthorized, policyhealth.go:349-353) does refuse a still-valid certificate. But the claimed outcome — a permanent trap, gateway permanently down, identity at risk — does not follow, because the CP gates on the SAME certificate's NotAfter (agentca.Issued.NotAfter is the cert's own expiry taken from the s
>
> **Correction:** Downgrade from "permanently down / unbounded refusal loop" to "a self-healing outage bounded by the clock skew, plus a wrong diagnosis printed during it." Correct statement: a fast-at-boot clock makes identity.Decide (decide.go:96) declare an unexpired certificate expired, and attemptRekey never re-reads the clock or the certificate, so the agent spins in refusals while the control plane — gating 

## 14. [HIGH] The server's Retry-After is computed and then thrown away, and the throttle branch never advances the backoff — the throttled loop has no exit reachable from inside it

`apps/node/internal/control/rekey.go:191` · root2-input-decided-once

**Claimed sequence**

```
Starting state: a fleet of ~200 gateways behind one NAT/ingress egress IP (a single VPC NAT gateway, or an ingress that terminates for the cluster), all expired after an outage longer than 48h. (1) rekey.go:41-48 retryAfter(resp) parses the server's Retry-After correctly, but rekey.go:191 and :216 interpolate the DURATION INTO THE ERROR STRING only — `fmt.Errorf("%w (retry after %s)", ErrRekeyThrottled, retryAfter(resp))`. The value is unrecoverable by the caller: errors.Is yields only the sentinel. (2) main.go:892 therefore hardcodes `wait := rekeyBackoffFloor` = 30s, discarding the server's instruction. The CP sends `Retry-After: 60` (rekeythrottle.go:99) against a 60-SECOND FIXED WINDOW (rekeythrottle.go:65), so a 30s retry lands inside the same window that just refused it. (3) main.go:902-904 comments that the throttle branch "Deliberately does NOT advance the backoff" and `continue`s. (4) rekeythrottle.go:127 sets the shared budget at 600/min, derived (rekeythrottle.go:107-127) from "an un-recovered agent retries on a 30s floor ... 4 requests/min per gateway" — 200 gateways is 800 requests/min through ONE bucket keyed on remote IP. (5) Every agent is throttled, waits 30s, retries, is throttled again — forever. The backoff never escalates, the loop never exhausts, main.go:84's join-token fallback is never reached, and the fleet's own retry traffic is what keeps the bucket saturated. Wrong outcome: a self-sustaining livelock in which no gateway recovers by re-key AND none falls through to the token fallback, and the only two knobs that could break it (the server's Retry-After, and backoff escalation) are respectively discarded and disabled.
```

**Why it would matter:** main.go:766-777 is a load-bearing comment claiming "One attempt per hour, sustained, is two requests per hour per bricked gateway ... negligible load." Under throttling the sustained rate is 1-2 requests per 30 SECONDS per gateway, forever — the derivation the CP's own budget was sized from is invalidated by the branch that runs precisely when the budget is exceeded. This is also directly the question 'is there a loop whose exit condition can never be satisfied from inside it?': yes, this one.

**Suggested direction (NOT ruled):** Return the Retry-After as a typed value (a RekeyThrottledError carrying a Duration, or a second return) rather than as text, and have main.go honour it with jitter. Cap consecutive throttles: after N of them, escalate the wait toward the ceiling and/or surface the throttle to the caller so the join-token fallback becomes reachable when the CP is refusing at the door.

> **Verifier verdict** — refuted: `True` (confidence high)
>
> MECHANISM: partly real. `retryAfter` is genuinely computed at rekey.go:41-48 and then interpolated into a string only (rekey.go:191, :216) with no accessor, so main.go:892 does hardcode `wait := rekeyBackoffFloor` (30s); the throttle branch does `continue` without advancing `backoff` (main.go:902-904). All three facts check out. CONSEQUENCE: does not reach an operator-visible bad outcome. The livelock arithmetic is wrong in three independent ways. (1) THE FLEET NEEDS 400 REQUESTS, NOT 800, AGAINST A 600 BUDGET. The finding's 4 req/min/gateway is the CP comment's figure for an UN-RECOVERED agent (rekeythrottle.go:113). A fleet whose re-key WORKS spends 2 requests each, ONCE, and then leaves t
>
> **Correction:** The mechanism is real but the consequence is not: this is an efficiency/API-shape wart, not a livelock. Downgrade to at most a nit, or drop. Accurate statement: "rekey.go:191/:216 render the server's Retry-After into an error string with no accessor, so main.go:892 retries on a fixed 30s instead of the server's 60s. Effect is bounded at one extra 429 per agent per minute, refused ahead of the Open

> **Verifier verdict** — refuted: `True` (confidence high)
>
> Mechanism half is accurate but the failing sequence does not occur; it breaks at steps (4)-(5). VERIFIED TRUE: apps/node/internal/control/rekey.go:191 and :216 interpolate retryAfter(resp) into the error string only (`fmt.Errorf("%w (retry after %s)", ErrRekeyThrottled, retryAfter(resp))`), so errors.Is yields only the sentinel and the duration is unrecoverable. apps/node/cmd/agent/main.go:892 hardcodes `wait := rekeyBackoffFloor` (30s), and the throttle branch `continue`s at main.go:904 without advancing `backoff`. apps/api/internal/http/rekeythrottle.go:99 sends Retry-After: 60 against a 60s fixed window (line 67) with rekeyAttemptsPerMinute = 600 (line 127) keyed on the raw peer IP (regis
>
> **Correction:** If any part is kept, keep only the narrow, benign observation and drop the livelock claim: rekey.go:191/:216 discard the parsed Retry-After into a string, so main.go:892 always waits its own 30s floor against a 60s server window, costing one wasted (free-to-the-server, non-budget-consuming) round trip per window. That is an inefficiency, not a defect with a wrong outcome. The genuine loop-exit gap

## 15. [HIGH] A saveCreds failure after a re-key the control plane already COMMITTED discards the recovery and falls through to the join token, destroying the identity that was just recovered

`apps/node/cmd/agent/main.go:861` · correctness

**Claimed sequence**

```
Starting state: gateway expired, TUNNEX_JOIN_TOKEN present (the shipped Helm shape injects it on every pod start), /var/lib/tunnex-node at 100% (ENOSPC — flow logs, a full root volume on a small appliance) or remounted read-only after an ext4 error. (1) attemptRekey succeeds: control.Rekey returns a cert; the CP has already COMMITTED the new serial, the new cert_public_key and the succession audit row in one transaction (nodes/rekey.go step 6) and pushed. The old serial and the old recorded key are dead server-side. (2) main.go:861 saveCreds fails on the very first os.WriteFile of ca.pem.tmp (ENOSPC/EROFS). (3) main.go:862-863 logs and `return nil, nil, nil`. (4) main.go:77 sees len(c)==0 and treats it as "re-key exhausted". (5) verdict.HaveToken is true, so main.go:92-96 enrolls with the join token → a NEW node id, site binding gone, every device homed on the old node needs re-issuing. Wrong outcome: the MORE destructive path is chosen precisely when the LESS destructive one has already succeeded. Note the state on disk at that moment: the pending key is still there (saveCreds' os.Remove at main.go:454 is never reached), and the CP holds its fingerprint — so a plain restart would have recovered cleanly by the fingerprint identity. The code throws that away instead. The new cert also exists in memory in newCert and is simply dropped.
```

**Why it would matter:** The whole point of ranking Recover above UseToken (decide.go:33-38) is that a token enrolment discards node id, site binding, devices and metrics. Here a transient local disk fault converts a successful in-place recovery into exactly that loss, and does so silently from the operator's view — the log says agent_save_creds_failed, then agent_falling_back_to_join_token, whose text claims "re-key did not succeed" when it did.

**Suggested direction (NOT ruled):** Distinguish 'the control plane refused' from 'I could not persist a success'. On a persist failure after a committed re-key, retry the save with backoff, and if it still fails, idle loudly (liveness up, readiness false) naming the state — never fall through to the token, because the pending key on disk plus the CP's new record still makes a clean restart recoverable.

## 16. [HIGH] identity.Decide inspects only cert.pem; any key.pem or ca.pem fault falls through to os.Exit(1) with an unused join token

`apps/node/cmd/agent/main.go:152` · correctness

**Claimed sequence**

```
Three reachable disk states, all classified `stored_identity_valid`:

(a) MISMATCHED PAIR. saveCreds is interrupted between the cert.pem rename (main.go:446, second in the sequence) and the key.pem rename (third) — a SIGKILL, an OOM kill, a node eviction. Disk: cert.pem = NEW cert (valid, not expired), key.pem = OLD key, ca.pem = new.
(b) ZERO-LENGTH key.pem. `os.ReadFile` returns `[]byte{}` with err == nil, so loadCreds (main.go:408) reports success.
(c) ZERO-LENGTH or non-PEM ca.pem (see the renewLoop finding below for one way to get here).

In all three: main.go:56 loadCreds returns nil error → main.go:64 Decide sees a parseable, unexpired cert → decide.go:143 `UseStored` → main.go:144 logs `agent_reusing_stored_identity` with a reassuring evidence string → main.go:152 `control.NewClient` → client.go:90 `tls.X509KeyPair` fails on (a) and (b) ("private key does not match public key" / "failed to find any PEM data"), client.go:95 `AppendCertsFromPEM` returns false on (c) → main.go:155 **os.Exit(1)**.

Under any supervisor this is CrashLoopBackOff, forever. `TUNNEX_JOIN_TOKEN` may be set and would resolve it in one enrolment; it is never read on this path. Recover is unreachable because the certificate is unexpired, so the CP's own gate would refuse a re-key anyway. In case (a) the material that WOULD work — the pending key matching the new cert — may still be on disk in rekey-pending-key.pem and is never consulted.
```

**Why it would matter:** The whole point of internal/identity is that 'unusable' is a determination rather than an assumption, and the story's stated law is that the agent must never exit on a condition a control-plane change could resolve. Here the determination is made from one of the three files, and the two it ignores route straight to the one os.Exit(1) in the boot path — the outcome the Idle branch and enrollWithToken's ok=false were both written to avoid. Every credential fault that is not a *read* error bypasses the entire decision function.

**Suggested direction (NOT ruled):** Give Decide the key and CA bytes (or add a boot-time pair check before NewClient) so a mismatched/unparseable key or CA is a classified verdict — `stored_identity_unusable` — that routes to Recover (when the pending key matches the cert) or UseToken, not to process exit. Replace the os.Exit(1) at main.go:155 with the same idle-with-liveness behaviour the other unrecoverable branches use.

## 17. [HIGH] loadCreds collapses three files into one error, so an unreadable ca.pem is reported as 'no credentials' and a token enrolment overwrites a valid identity

`apps/node/cmd/agent/main.go:403` · correctness

**Claimed sequence**

```
Starting state: cert.pem and key.pem are a valid, matching, unexpired identity for node `gw-7f4c` (CN = gw-7f4c). ca.pem exists but its read fails — EIO on a failing disk, a stale NFS handle, or an operator having chmod'd/removed it to force a CA refresh. `TUNNEX_NODE_NAME` is unset, so main.go:46 uses `hostname()`; the gateway runs as a Deployment pod, so the hostname is a fresh random suffix on every restart (`gw-9b21`). A join token is injected on every pod start (the shipped Helm shape, per decide.go:105).

1. main.go:56 loadCreds: cert.pem reads, key.pem reads, ca.pem fails → main.go:412-414 discards the two good buffers and returns `nil, nil, nil, err`.
2. main.go:64 Decide gets certPEM == nil and loadErr != nil → decide.go:70 branch 1 → UseToken, with evidence "no credentials in the state directory; enrolling with the join token" — a claim about the directory that was never checked (the error may be EACCES/EIO, never distinguished from ENOENT).
3. main.go:69 EffectiveName returns `requestedName` for a UseToken verdict, so the stored CN is discarded too: the agent enrols as `gw-9b21`, not `gw-7f4c`. The unique-name conflict at nodes/service.go:297 that would otherwise have blocked this does not fire, because the name is different.
4. enrollWithToken succeeds and main.go:974 saveCreds OVERWRITES cert.pem and key.pem with the new node's material.

Wrong outcome: a working gateway's identity is destroyed and a duplicate node created — devices homed on `gw-7f4c` are orphaned, its site binding is gone, its metrics series ends — because one of three files would not read.
```

**Why it would matter:** This is the exact WF-2 incident the stored-identity preference exists to prevent, reached through the one input Decide cannot see. ca.pem is also the *least* load-bearing of the three — it is the only one that is re-derivable (every enrol and re-key response carries it) — yet its read failure is the one that authorises discarding the two that are not.

**Suggested direction (NOT ruled):** Return the files that DID read plus a per-file error, and let Decide rule on them: a readable cert+key with an unreadable CA is a CA problem, not an absent identity. At minimum, refuse to treat a non-ENOENT read error as 'nothing stored' — an I/O or permission error is a reason to idle loudly, never a reason to spend the join token.

## 18. [HIGH] An unreadable cert.pem spends the join token even though key.pem still identifies the node to the re-key endpoint

`apps/node/internal/identity/decide.go:83` · correctness

**Claimed sequence**

```
Starting state: the gateway was re-keyed or renewed; saveCreds wrote cert.pem via write-tmp-then-rename (main.go:442-448) with NO fsync of the file and NO fsync of the directory. The VM loses power moments later. On reboot the rename is visible but the data is not: cert.pem is zero-length (or garbage). key.pem is intact and holds K, whose SPKI digest is exactly what the CP recorded in `nodes.cert_key_fingerprint` and exactly what `control.KeyFingerprintFromPEM` computes. A join token is present (Helm injects one on every pod start).

1. main.go:56 loadCreds succeeds — a zero-length file is not a read error.
2. Decide: `len(certPEM)==0` → decide.go:70 → UseToken; or, for the garbage variant, decide.go:83 → UseToken with the comment "Nothing can be recovered from it, so a token loses nothing."
3. main.go:126 enrollWithToken creates a NEW node (name differs from the stored CN, which was never read), and main.go:974 saveCreds overwrites key.pem — deleting K, the last thing that named the old node.

Wrong outcome: the old node is orphaned with its devices and site binding, and the material that identified it is erased, because a certificate FILE was lost. Nothing was wrong with the identity.
```

**Why it would matter:** decide.go:82's comment states a property the code no longer has. Before D10 the certificate serial was the only identifier, so an unreadable cert.pem really did mean nothing was recoverable. D10 added the key-fingerprint identifier precisely so an agent can name itself WITHOUT a certificate (rekey.go:54-57 says so explicitly), which makes key.pem sufficient — the CP resolves it at nodes/rekey.go:97. Decide's precedence never learned about the new capability, so the branch that reads as authoritative is now the one that throws away a recoverable node. The missing fsync/rename-durability is what makes the input reachable.

**Suggested direction (NOT ruled):** When cert.pem is unusable but key.pem parses, route to Recover with `Identifier{KeyFingerprint: fingerprint(key.pem)}` and treat the token as the fallback, the same ranking the expired branch already uses (Recover is refused for a still-live node, which is the correct answer there and costs one request). Separately, fsync the temp file and the directory in saveCreds before returning success, so the promotion is durable before the pending key is removed.

## 19. [HIGH] Decide's table never reads key.pem, so every not-usable-cert row throws away an identity the CP can still prove

`apps/node/internal/identity/decide.go:70` · precedence-truth-table

**Claimed sequence**

```
Starting state: /var/lib/tunnex-node holds cert.pem (CN gw-1) and key.pem (the key the CP recorded — cert_public_key is stamped on every renewal, service.go:341), but ca.pem is absent (a pre-S13.1 agent using the old three-WriteFile saveCreds was SIGKILLed/evicted between two of its writes; or a partial PVC restore). The Helm chart injects TUNNEX_JOIN_TOKEN on every pod start (deploy/helm/tunnex-gateway/templates/deployment.yaml, F5).
1. main.go:56 loadCreds → os.ReadFile(ca.pem) fails → line 412-414 returns (nil,nil,nil,err); the cert and key it already read successfully are discarded with the error.
2. main.go:64 Decide(certPEM=nil, loadErr=err, haveToken=true) → decide.go:70 takes the first branch → UseToken, Reason "no_stored_identity", Evidence "no credentials in the state directory".
3. main.go:126 enrollWithToken → control.Enroll → CP service.go:292-298 CreateNode hits the unique-name constraint → 409 node_exists.
4. enrollWithToken returns ok=false → main.go:129 `<-ctx.Done()`. The gateway idles forever, readiness false.
Same row, no token: Idle forever.
Second instance of the same class: cert.pem present but truncated/unparseable with key.pem intact → decide.go:83 → UseToken/Idle, identical outcome.
Wrong outcome: in both rows the CP holds this node's cert serial AND its key fingerprint, control.Identifier supports KeyFingerprint precisely for the case where the cert is unusable, and Rekey returns a fresh ca_pem — so recovery in place needed no operator action at all. Decide cannot reach it because key.pem is not one of its inputs.
```

**Why it would matter:** These are exactly the rows the prompt asks about: a token (or Idle) winning over a recoverable identity. The Helm fix is therefore incomplete — it covers the expired-cert row and leaves three rows where a single missing/corrupt file converts an in-place-recoverable gateway into either a permanently idle pod or a new node. The Evidence string compounds it: "no credentials in the state directory" is asserted for a directory that contains the cert and the key, and it steers the operator to wipe-and-re-enroll, which is the one action that loses the site binding and the devices.

**Suggested direction (NOT ruled):** Give Decide the key material (or a loaded-credential struct with per-file status) instead of a single collapsed loadErr. Distinguish os.IsNotExist from a read error per file; when cert.pem is absent/unreadable but key.pem parses, return Recover and let attemptRekey identify by control.Identifier{KeyFingerprint: fp(key.pem)}. ca.pem should never be part of the identity determination — it is public and every recovery response returns it.

## 20. [HIGH] attemptRekey's loop never re-reads the clock that authorized it, so an NTP correction cannot end a re-key of a healthy gateway

`apps/node/cmd/agent/main.go:64` · inputs-decided-once

**Claimed sequence**

```
Starting state: healthy gateway, cert.pem valid until 2026-07-31 (CP row cert_not_after = same), pod restarts at real time 2026-07-30 06:00 but the host RTC reads 2026-08-05 (hypervisor RTC drift / dead battery / NTP not yet stepped).
1. main.go:64 Decide(..., now=2026-08-05) → decide.go:96 expired = now.After(NotAfter) = true → Recover.
2. main.go:77 attemptRekey → main.go:812 loadOrCreatePendingKey MINTS AND PERSISTS a pending key — a durable disk side-effect of a false premise.
3. control.Rekey → CP nodes/rekey.go:175 RekeyAuthorized("active", 2026-07-31, true, now=2026-07-30 real) returns false, "the node's certificate is still valid ... a live gateway must never be re-keyed" → 403 → rekey.go:194 ErrRekeyRefused.
4. main.go:908 logs agent_rekey_refused with most_likely_cause "enrolled before the control plane recorded agent public keys ... or it was REVOKED". Both are false; the actual cause is the agent's own clock, which the agent can check and never does.
5. 30s later chrony steps the clock to 2026-07-30. The certificate is now valid by the agent's own rule. The loop at main.go:852 re-enters at :856 with the identities built at :837-849 and the verdict taken at :64; it never re-evaluates now.After(leaf.NotAfter). Its only exits are a successful re-key — impossible while the CP correctly refuses a live node — and ctx cancel.
Wrong outcome: the gateway never reaches main.go:152/378, so WireGuard is never programmed. It stays down for the remaining life of a perfectly good certificate, retrying at up to 1/hour, and the one input whose change would release it is the only one never re-read.
```

**Why it would matter:** The recovery loop can take a healthy gateway down and hold it down. It is also the ROOT-2 shape in its purest form: the loop's premise is a clock reading sampled once at line 64, and the loop is the code most likely to still be running when that reading is corrected. decide_test.go:249-251 vouches that a re-key trigger 'must rest on a locally-provable fact' — the wall clock is not locally provable; it is set over the network by NTP, which is the same class of input the test was written to exclude.

**Suggested direction (NOT ruled):** Make attemptRekey re-evaluate its own premise each iteration: re-run identity.Decide (or just now.After(NotAfter)) at the top of the loop and return a distinct 'no longer expired' outcome that lets main fall through to UseStored. Separately, treat a refusal against a certificate the agent now believes valid as a clock-skew hypothesis in the diagnosis, since the agent holds both facts.

## 21. [HIGH] The re-key response is committed to disk with no validation, destroying the only material that can prove this node's identity

`apps/node/cmd/agent/main.go:77` · response-validation

**Claimed sequence**

```
Starting state: expired cert.pem + key.pem on the state PVC; the chart's documented shape is controlPlane.apiURL = http://tunnex-cp-api…:8080 (deploy/helm/tunnex-gateway/values.yaml:28), so /api/v1/agent/rekey is plain HTTP with no server authentication whatsoever. An on-path device (or a proxy/CP bug) answers the POST with 200 and body {"cert_pem":"garbage","ca_pem":""}.
1. rekey.go:193-203 checks only the status code, decodes the body and returns it verbatim — no PEM parse, no check that the certificate matches pendingKeyPEM, no chain check against the ca.pem the agent already holds and already trusts.
2. main.go:857 err == nil → main.go:861 saveCreds(certDir, "garbage", pending, "") writes ca.pem = empty, cert.pem = garbage, key.pem = pending, and main.go:454 then REMOVES the pending-key file. Both remaining copies of provable identity (the old key.pem the CP recorded, and the pending key) are now gone.
3. main.go:77 `len(c) > 0` is the caller's entire acceptance test, so the result is adopted.
4. main.go:152 control.NewClient → tls.X509KeyPair fails → main.go:155 os.Exit(1) → CrashLoopBackOff; every subsequent boot repeats step 4, and Decide never gets a chance to route anywhere.
Wrong outcome: the CP still holds the old serial and old public key; the agent can no longer prove possession of either. Recovery now requires wiping the volume and minting a join token — a NEW node, site binding gone, devices re-issued — the exact outcome the story exists to prevent, from one HTTP 200 on an unauthenticated channel, and irreversible by any control-plane change.
(Distinct from the pass-1 ca_pem trust-anchor finding: the defect here is that nothing about the response — cert included — is checked before it OVERWRITES the recoverable identity, and the resulting state is terminal at main.go:155 rather than merely mis-anchored.)
```

**Why it would matter:** attemptRekey is the one place in the product that deliberately trusts an unauthenticated channel, and it commits that channel's answer over the only credentials that make in-place recovery possible, deleting the fallback material in the same call. It also breaks the law stated two functions away at main.go:956 ('an agent must never exit on a condition a control-plane change could resolve') — here the agent exits on a condition NO control-plane change can resolve, because it destroyed the evidence first.

**Suggested direction (NOT ruled):** Before saveCreds: parse the returned cert, require tls.X509KeyPair(newCert, pending) to succeed, require it to verify against the EXISTING on-disk ca.pem (never the returned one), and require CN/NotAfter to be sane. Treat any failure as a refusal and keep the stored identity. Retain the pending key until the promoted identity has completed one successful mTLS call.

## 22. [HIGH] The lost-response fingerprint recovery the pending key exists for is refused by the control plane's liveness gate for 48h; the CP's own test forces the row into a state the field cannot be in

`apps/node/cmd/agent/main.go:811` · correctness

**Claimed sequence**

```
Claim under test — main.go:809-811 ("the key is persisted FIRST, and reused across attempts... One pending key converges") and internal/control/rekey.go:138-143 ("a lost response leaves this agent able to prove possession of the key the control plane just recorded").

Starting state: gateway with cert.pem expired 3h ago, key.pem = K1, CP row: cert_serial=S1, cert_public_key=SPKI(K1), cert_not_after = now-3h, status active.

1. Boot. Decide → Recover. attemptRekey (main.go:812) mints pending K2, persists it, wasOnDisk=false, so identities = [serial S1 / popKey K1] only.
2. control.Rekey succeeds ON THE WIRE. CP (apps/api/internal/nodes/rekey.go) consumes the nonce, gate RekeyAuthorized passes (cert_not_after < now), signs a CSR over K2, commits cert_serial=S2, cert_public_key=SPKI(K2), **cert_not_after = now + CertTTL = now+48h** (agentca.CertTTL, ca.go:33), restores cascade-revoked devices, pushes the org.
3. The 200 is lost (ingress timeout / pod evicted between commit and write). Agent has no new cert; K2 is still on disk as rekey-pending-key.pem.
4. Agent restarts. loadOrCreatePendingKey returns K2 with wasOnDisk=true, so this time the fingerprint identity IS tried first (main.go:838-844): Identifier{KeyFingerprint: fp(K2)}, popKey=K2.
5. CP resolveRekeyIdentity matches the row on cert_key_fingerprint (migration 0061) — the lookup works exactly as designed.
6. CP then runs RekeyAuthorized(status, cert_not_after=now+48h, true, now) → apps/api/internal/nodes/policyhealth.go:351 returns **false**, "the node's certificate is still valid... a live gateway must never be re-keyed" → uniform ErrRekeyRefused.
7. The serial identity is then tried with S1, which no row carries → refused. Both identities refused. attemptRekey backs off to the 1h ceiling and logs agent_rekey_refused with most_likely_cause = "enrolled before the control plane recorded agent public keys... or it was REVOKED" and remedy = "mint a join token" (main.go:908-922) — both wrong, and following the remedy creates a NEW node and discards the site binding and devices, which is precisely the loss D10 was built to prevent.

The gate only opens 48h later, when the certificate the agent never received finally expires. For those 48h the CP believes the gateway is live and healthy (it restored its devices and pushed to the org in step 2) while the gateway is down.

Why this was not caught: apps/api/internal/nodes/rekey_integration_test.go:243-248 (TestLostResponseDoesNotBrickTheGateway) has to execute `UPDATE nodes SET cert_not_after = now() - interval '2 days'` before step 4 will pass, and the comment rationalises it as "To reach the gate, the row must be in the state a gateway needing recovery is in — expired — which is what it will be by the time the agent gets here in the field." It will not: the agent gets there within 30s (rekeyBackoffFloor) or immediately on restart, and the row is 48h from expiry. The test passes only because the fixture is mutated into a state the field reaches two days after the failure.
```

**Why it would matter:** D10 — the second identifier, migration 0061's generated column, the pending-key file, the three-way golden vector — is the whole answer to "one dropped packet must not cost a gateway its identity". In the field that answer does not fire: the fingerprint resolves the node and is then refused by the liveness gate, and the agent's own diagnostic tells the operator to do the identity-destroying thing.

**Suggested direction (NOT ruled):** The gate must distinguish "live gateway, unknown caller" from "this caller holds the key I recorded for this row". A fingerprint-kind identifier that matches cert_public_key exactly IS proof the caller is the node the row names, so RekeyAuthorized needs a carve-out for it (still refusing revoked, still refusing serial-kind on a live row). Alternatively the CP could keep the previous serial+key recoverable until the agent's next authenticated reconcile confirms receipt. Either way, delete the fixture mutation at rekey_integration_test.go:243 — the test must run against the row state the field actually produces.

## 23. [HIGH] "DO NOT EXIT... idle with liveness up and readiness false" — the agent idles forever without retrying, and the shipped gateway chart has no probes at all, so the idle pod reports Ready

`apps/node/cmd/agent/main.go:98` · correctness

**Claimed sequence**

```
Claim under test — main.go:98-101 and 950-957: "An enrolment refusal... is a condition a control-plane change can resolve, and exiting forfeits the reconciliation that would have fixed it — turning a recoverable state into CrashLoopBackOff. Idle with liveness up and readiness false." Same claim repeated at main.go:128 and enrollWithToken's doc.

Starting state: a gateway pod with an empty state PVC and TUNNEX_JOIN_TOKEN set (the shipped Helm shape, deploy/helm/tunnex-gateway/templates/deployment.yaml:123). The control plane is mid-rollout and answers 503 for ~20 seconds.

1. loadCreds fails → Decide → UseToken (decide.go:72).
2. enrollWithToken → control.Enroll returns "enroll failed (503)" → logs agent_enroll_failed and returns ok=false (main.go:965-971).
3. main.go:127-131 blocks on `<-ctx.Done()`.
4. The CP finishes rolling out 20 seconds later. **Nothing re-attempts.** enrollWithToken is called exactly once per process; there is no retry loop, no ticker, and identity.Decide is never re-run. r.Run is never reached, so "the reconciliation that would have fixed it" does not exist in this branch — the code the comment says exiting forfeits is code this path never starts.
5. The pod stays in this state until a human deletes it.

Both halves of the stated compensating property are also absent on the shipped chart: deploy/helm/tunnex-gateway/templates/deployment.yaml declares NO livenessProbe and NO readinessProbe (grep 'Probe' returns nothing in that file). With no readinessProbe, kubelet marks the container Ready as soon as it starts, so `kubectl get pods` shows 1/1 Running for an agent that holds no identity and is doing nothing. The `ready` atomic and /readyz that the comment leans on are read by nobody in the shipped deployment.

The rejected alternative is strictly better on the named recovery axis: os.Exit(1) under restartPolicy: Always produces a restart, which re-runs Decide and re-attempts enrolment with backoff — i.e. the control-plane change DOES resolve it — and CrashLoopBackOff is visible in `kubectl get pods` where the current behaviour is not.

The same shape applies to the two sibling exits: main.go:89 (agent_unrecoverable) and main.go:112 (identity.Idle) both `<-ctx.Done(); return` with no periodic re-decision.
```

**Why it would matter:** The comment is the reason no retry was written, and it is the load-bearing one: a next engineer reading "never exit on a condition the control plane can resolve" will conclude the recoverable case is handled. It is not handled — it is converted from a self-healing crash loop into a silent, invisible, permanently-not-ready pod.

**Suggested direction (NOT ruled):** Idling is only correct if something retries. Wrap the identity decision in a bounded-backoff loop that re-runs loadCreds + identity.Decide + the chosen action on an interval, so a CP change actually lands without a pod delete. Independently, add livenessProbe /healthz and readinessProbe /readyz to the gateway chart — the comment's whole premise is that an orchestrator can see the not-ready state.

## 24. [HIGH] saveCreds commits renames one at a time and returns an error mid-sequence; every caller treats a saveCreds error as "nothing was written" and abandons a re-key that already succeeded on the control plane

`apps/node/cmd/agent/main.go:861` · correctness

**Claimed sequence**

```
Claim under test — main.go:419-430: "writes the credential set so that a crash leaves EITHER the old set or the new set, never a mixture." No crash is needed to falsify it; an ordinary error return does it, and the callers' error handling is built on the assumption that the write was all-or-nothing.

Starting state: expired gateway, state dir on a PVC that is at its quota (or a tmpfs that fills). CP row: S1 / SPKI(K1) / expired.

1. attemptRekey mints pending K2, control.Rekey SUCCEEDS: the CP commits cert_serial=S2, cert_public_key=SPKI(K2), restores the node's cascade-revoked devices and pushes the org (apps/api/internal/nodes/rekey.go steps 6-7).
2. main.go:861 calls saveCreds(dir, newCert, K2, newCA). The loop at main.go:441 writes and renames ca.pem — **committed**. It then writes cert.pem.tmp and gets ENOSPC/EDQUOT → `return err` at main.go:444.
3. Disk now holds: NEW ca.pem, OLD cert.pem (S1, expired), OLD key.pem (K1), and rekey-pending-key.pem still present (the os.Remove at main.go:454 is never reached). That is a mixture, and it is reached by the error path, not by a crash.
4. attemptRekey logs agent_save_creds_failed and `return nil, nil, nil` (main.go:862-863) — the caller cannot distinguish "the re-key never happened" from "the re-key happened and I failed to write it down", so it treats a completed identity change as a no-op.
5. main.go:77 sees len(c)==0 and falls through to main.go:84: with a token present it enrols as a NEW node (identity, site binding and devices discarded — the exact loss the story exists to prevent); with no token it logs agent_unrecoverable and idles forever.
6. On the next restart, disk is back at S1/K1 plus pending K2, so Decide→Recover and both identities are refused (S1's row moved; fp(K2) hits the liveness gate — see the first finding), i.e. 48h down on top of the abandoned identity.

The same partial commit is reachable from enrollWithToken (main.go:974) and renewLoop (main.go:510), which likewise treat the error as "nothing was persisted".
```

**Why it would matter:** The atomicity comment is what justifies every caller's "error means nothing was written" handling. Because the function commits ca.pem before it can fail on cert.pem, a single ENOSPC on a state volume throws away a re-key the control plane has already committed and devices it has already restored — and does it while the log says the re-key did not succeed.

**Suggested direction (NOT ruled):** Either make the set genuinely atomic (write a generation directory and swap one symlink/rename, so one rename commits all three) or make the error path honest: on a saveCreds failure after a successful Rekey, retry the persist in-loop and, if it still fails, refuse to fall back to the join token — the CP has already moved, and a token enrolment is now a strictly destructive action. A caller that cannot roll back must not treat the failure as a no-op.

## 25. [HIGH] attemptRekey persists whatever the unauthenticated endpoint returned before the caller checks it succeeded, and the same call deletes the pending key

`apps/node/internal/control/rekey.go:196` · correctness

**Claimed sequence**

```
Starting state: gateway A, expired cert, re-key runs over the PUBLIC listener (rekey.go:131), so any intermediary on that path — an auth proxy, a WAF, a CDN error page, a load balancer health shim — sits between the agent and the CP.

(1) The POST at rekey.go:183 gets a 200 whose body is valid JSON but carries no certificate: `{}`, `{"status":"ok"}`, or `{"cert_pem":"","ca_pem":""}`. rekey.go:200 Decode succeeds, rekey.go:203 returns ([]byte(""), []byte(""), nil) — err is nil, so this is a SUCCESS. Nothing validates that cert_pem is non-empty, parses as a certificate, or matches the pending key.
(2) main.go:857 `if err == nil` → main.go:861 saveCreds(certDir, "", pending, "") writes a ZERO-BYTE cert.pem and a ZERO-BYTE ca.pem over the still-usable ones, and main.go:454 then removes rekey-pending-key.pem.
(3) main.go:869 returns newCert (length 0) → main.go:77 `len(c) > 0` is FALSE. The caller's own success check runs AFTER the destructive write; it correctly decides the re-key failed, but the identity is already gone.
(4) Next boot: loadCreds succeeds (empty files read without error), main.go:64 identity.Decide hits decide.go:70 `len(certPEM) == 0` → "no_stored_identity" → UseToken or Idle. The Recover branch is now UNREACHABLE for this gateway forever: there is no serial to re-key with (StoredSerial returns "") and no pending key to identify by. A gateway that was recoverable in place by proof of possession is now recoverable only by a join token.

The same shape reaches the disk from enrollWithToken (main.go:973-974 persists res.CAPEM unvalidated) — a CP or proxy response missing ca_certificate writes an empty ca.pem with the same effect.
```

**Why it would matter:** One malformed 200 from anything on the public path permanently costs a gateway the in-place recovery this story is built to provide, and it does so on the path where the agent explicitly cannot authenticate its peer. saveCreds's header comment promises 'EITHER the old set or the new set' — a set of three empty files, written by the ordinary non-crash path, is neither.

**Suggested direction (NOT ruled):** Validate before persisting: in control.Rekey, refuse (as a refusal, not a success) unless cert_pem parses as an X.509 certificate whose public key matches the pending key and ca_pem parses as at least one CA certificate. Independently, make saveCreds reject empty cert/key/ca inputs — it is the last gate before the only copy of the identity is overwritten — and defer the pending-key removal until after the caller has accepted the promotion.

## 26. [HIGH] The 'liveness up, readiness false' contract that justifies never exiting has no consumer in the shipped gateway chart

`apps/node/cmd/agent/main.go:100` · correctness

**Claimed sequence**

```
Starting state: `helm install` of deploy/helm/tunnex-gateway with a join token that has already been consumed (or an expired one — joinTokenTTL is 1h, apps/api/internal/nodes/service.go:48, and a Secret installed yesterday is stale).

(1) main.go:115 UseToken → enrollWithToken → control.Enroll returns non-200 → main.go:965-971 logs agent_enroll_failed and returns ok=false.
(2) main.go:127-131 deliberately does NOT exit, on the stated grounds (main.go:98-101, repeated at 128 and 106-107) that the agent will 'idle with liveness up and readiness false' so an orchestrator sees the true state. `ready` is never stored (the goroutine at main.go:356 is never reached), so /readyz would return 503 forever.
(3) Nobody asks. deploy/helm/tunnex-gateway/templates/deployment.yaml declares no readinessProbe and no livenessProbe — `grep -rn Probe deploy/helm/tunnex-gateway/` matches only `preflight.dnsProbe` — and does not even declare the health container port (only the WireGuard hostPort). docker-compose.yml has no healthcheck for the node either.
(4) Result: `kubectl get pods` shows the gateway 1/1 READY, the Deployment reports 1/1 available, the rollout is reported successful, and no alert fires — for a process that has no identity at all and will never acquire one. The only trace is a log line. The same holds for every other never-exit path: identity.Idle (main.go:112), agent_unrecoverable (main.go:89), and the running-cert-expiry state where reconcile.go:319 flips healthy=false.
```

**Why it would matter:** Readiness is the entire justification, cited three times in this diff, for turning hard failures into silent idling. Without a probe wired to it the trade is one-sided: the crash-loop signal was removed and nothing replaced it, so a gateway with no identity is indistinguishable from a healthy one at every layer an operator actually watches.

**Suggested direction (NOT ruled):** Add readinessProbe (httpGet /readyz on the health port) and livenessProbe (/healthz) to the gateway Deployment, expose the port, and pick the failureThreshold deliberately so that a not-ready gateway is visible without being restarted out of a recovery it is legitimately performing. Confirm the same for docker-compose. Whoever owns the who-reads-this probe should treat /readyz as a channel field with no consumer until this lands.

## 27. [HIGH] A fast local clock sends a healthy gateway into a recovery the control plane must refuse, permanently, and its valid certificate is never tried

`apps/node/internal/identity/decide.go:96` · unvalidated-local-determination

**Claimed sequence**

```
Starting state: a working gateway holding a certificate issued 40h ago (NotAfter = t0+48h), CP healthy, node status active. The host's clock jumps forward >8h — a VM resumed from a snapshot, a dead RTC, NTP not yet converged at boot — and the agent restarts. main.go:64 → decide.go:96 `expired := now.After(leaf.NotAfter)` is true on the skewed clock → Verdict{Action: Recover}. main.go:77 enters attemptRekey. The CP evaluates the same question on ITS clock: nodes.RekeyAuthorized (policyhealth.go:347-352) sees certNotAfter still in the future and returns (false, "a live gateway must never be re-keyed"), so every attempt is refused with the uniform 403. attemptRekey's loop has only two exits (success, ctx cancel), so the agent retries to the 1h ceiling forever, logging most_likely_cause="enrolled before key recording ... or it was REVOKED" (main.go:916-918) — a total misdiagnosis. The certificate on disk is valid and the mTLS channel would have worked on the first try (the CP validates the client cert against the CP's clock; the CP's server cert is a 365d leaf that still verifies under the skew), but nothing in the Recover branch ever tries it, and there is no path from Recover back to UseStored. The gateway is down until a human notices.
```

**Why it would matter:** An expiry verdict reached entirely from an unvalidated local clock is treated as irreversible: it discards a working identity for a condition the agent cannot confirm and the server actively contradicts. main.go:74-76 vouches for the opposite — "The trigger is local ... with no network call, so a transient outage cannot land here" — arguing that locality implies trustworthiness. It rules out one hazard and admits a worse one: a clock jump takes a healthy site offline, and the operator is pointed at revocation.

**Suggested direction (NOT ruled):** Do not let the Recover verdict be terminal. Either try the mTLS channel first when the cert is only recently expired (a successful handshake proves the local clock wrong and should fall back to UseStored), or treat a refusal that arrives while the cert is within, say, one CertTTL of NotAfter as evidence of skew and re-attempt UseStored. At minimum have Decide flag implausible clock state (leaf.NotBefore in the future, or now far beyond NotAfter) as its own reason with its own remedy.

## 28. [HIGH] Re-key is boot-only: a certificate that expires while the agent is running is never recovered, and the agent tells the operator to do the destructive thing

`apps/node/cmd/agent/main.go:77` · recovery-not-wired

**Claimed sequence**

```
Starting state: agent running, cert valid, renewLoop ticking. The 24h renewal at main.go:504 fails once (CP restart, brief partition) — renewLoop logs a warn and `continue`s, next attempt in another 24h (see the separate renew-schedule finding). At t0+48h the certificate expires in-process. Every subsequent call fails: reconcile.Run gets `tls: expired certificate`, reconcile.go:419 → logBrickedOnce → CertExpiredRemedy matches and logs ONCE, then the loop keeps backing off forever. attemptRekey is called from exactly one place, main.go:77, on the boot path only; nothing in the running agent can reach it. readiness goes false, but /healthz (main.go:386) always returns 200 and the gateway chart defines no liveness or readiness probe at all, so nothing restarts the pod. The gateway sits alive, unready, forwarding stale WireGuard state with a dead control channel, indefinitely. Worse, the single ERROR it prints is pre-S13.1 text (reconcile/certexpiry.go:42-46): "Retrying will NOT recover it. RE-ENROLL this gateway: create a join token ... and run the enrollment command on this host" — steering the operator to the path that creates a NEW node and discards the site binding and devices, when a mere process restart would now have recovered it in place.
```

**Why it would matter:** The story built proof-of-possession recovery and wired it to exactly one trigger — process start — leaving the most likely way a cert expires (it expires while the agent is up) with no recovery at all. The stale remedy string then converts an in-place-recoverable gateway into an operator-executed identity loss. The comment calling the condition "terminal" was true before this branch and is load-bearing precisely because it still reads as authoritative.

**Suggested direction (NOT ruled):** Make expiry recoverable from the running loop: when CertExpiredRemedy matches (or when the cert on disk is within minutes of NotAfter), invoke the same attemptRekey path, hot-swapping the client's cert on success. Rewrite the remedy text to name restart/re-key first and the join token as the last resort, and give the gateway chart a liveness probe that is not unconditionally green.

## 29. [HIGH] Renewal is scheduled from process start against a 48h cert and is not retried sooner after a failure, so one transient failure reaches expiry

`apps/node/cmd/agent/main.go:496` · scheduling

**Claimed sequence**

```
Starting state: cert issued at t0, NotAfter = t0+48h (agentca.CertTTL). renewLoop (main.go:496-517) creates a ticker at TUNNEX_AGENT_RENEW_INTERVAL (default 24h, not overridden anywhere in deploy/) measured from PROCESS START, not from the certificate's NotAfter, and on failure it logs a warn and continues to the next 24h tick. Sequence A: the t0+24h attempt fails for any reason (CP rolling restart, 30s of packet loss) — the next attempt is at t0+48h, at or after expiry, so it fails with `tls: expired certificate` and every attempt thereafter does too. Sequence B (no failure needed): the pod restarts every ~20h — a nightly rollout, a node drain, an autoscaler event — so the ticker is reset before it ever fires; the cert is never renewed and expires at t0+48h purely from restarts. In both cases the agent then holds an expired cert with no in-process recovery (see the boot-only finding), so the gateway is dead until a human restarts it.
```

**Why it would matter:** A single transient failure, or an ordinary restart cadence, is enough to walk a healthy gateway into expiry — the state this whole story exists to dig out of. The renewal clock is derived from an env default rather than from the one fact that determines the deadline (the cert's own NotAfter), and it is never revisited while the loop runs.

**Suggested direction (NOT ruled):** Schedule from the certificate: compute the next attempt as NotAfter minus half the remaining life, re-derived after every renewal, and on failure retry on a short bounded backoff (minutes) rather than waiting the full interval. Cap so that at least several attempts land before NotAfter regardless of how long the process has been up.

## 30. [HIGH] renewLoop ignores the ca.pem read error and persists an empty trust anchor, which is then fatal at boot

`apps/node/cmd/agent/main.go:509` · swallowed-error

**Claimed sequence**

```
Starting state: a healthy agent with valid cert.pem/key.pem/ca.pem. At a renewal tick, client.Renew succeeds and returns a fresh cert+key. main.go:509 does `ca, _ := os.ReadFile(filepath.Join(certDir, "ca.pem"))` — the error is discarded. Any transient read fault makes ca nil: EMFILE/ENFILE under load (the agent holds nflog sockets, a keep-alive pool, the in-cluster EndpointSlice watch), or EIO on a lazily-attached state volume. saveCreds is then called with ca=nil and, by its own deterministic order, writes ca.pem FIRST: os.WriteFile(ca.pem.tmp, nil) + rename → ca.pem is now 0 bytes, and the real trust anchor is gone. Nothing notices; the running client keeps its in-memory pool. On the next restart: loadCreds succeeds (a 0-byte file is not a read error, main.go:412), Decide sees a valid certificate and returns UseStored, then control.NewClient → pool.AppendCertsFromPEM(nil) returns false → "bad CA PEM" (client.go:95-97) → main.go:155 os.Exit(1). CrashLoopBackOff, permanently, with a perfectly valid certificate and key on disk and no code path that repairs or re-fetches ca.pem.
```

**Why it would matter:** An ignored error on a read converts a momentary fault into permanent destruction of the trust anchor, and the resulting state is the one thing this story says must never happen — the agent exits, repeatedly, on a condition the control plane could resolve (main.go:950-957 states the rule explicitly). Nothing validates ca.pem at any point: not on read, not on write, not before it is used.

**Suggested direction (NOT ruled):** Fail the renewal persist if ca.pem cannot be read, rather than writing what the read returned; and have saveCreds refuse to write empty material for any of the three files. At boot, treat an unusable CA as recoverable (attempt re-key / token enrolment, which return a fresh ca_pem) instead of os.Exit(1).

## 31. [HIGH] The server's Retry-After is parsed, embedded in a string, and discarded; the throttled branch retries at twice the requested rate and never decays

`apps/node/cmd/agent/main.go:892` · ignored-wire-directive

**Claimed sequence**

```
Starting state: a fleet recovering after an outage longer than 48h — the exact scenario rekeyAttemptsPerMinute was sized for. The CP throttle answers 429 with `Retry-After: 60` (rekeythrottle.go:98). control.Rekey/rekeyChallenge call retryAfter(resp) (rekey.go:41-48), which correctly parses 60s — and then only interpolates it into an error string (rekey.go:191, 216). attemptRekey's throttled branch at main.go:891-904 sets `wait := rekeyBackoffFloor` (30s) unconditionally, ignores the parsed value, and deliberately does not advance the backoff. So every throttled agent retries at 30s, i.e. 4 requests/min, exactly double what the server asked for, forever. Because the throttle keys on the raw peer address, an entire fleet behind one ingress shares a single 600/min bucket; a 200-gateway fleet offers 800/min, the bucket stays saturated, and since no agent's backoff ever grows the aggregate load never decays — a livelock in which some agents are starved indefinitely and none reaches the join-token fallback. rekey.go:39-40 vouches for the behaviour the code does not have: "Returns 0 when absent or unparseable — the caller then picks its own short delay rather than guessing long," which reads as "the caller honours it when present." No caller ever reads the value.
```

**Why it would matter:** The one explicit backoff instruction the control plane can give an unauthenticated caller is discarded, and the agent's fixed 30s floor is what the CP's own budget arithmetic (rekeythrottle.go:107-118) assumes as the FLOOR, not as the behaviour under throttling. Mass recovery — the case both sides were designed for — is the case where it fails.

**Suggested direction (NOT ruled):** Return the parsed Retry-After from Rekey/rekeyChallenge as a typed value (not text) and have the throttled branch wait at least that long, with jitter, so a fleet spreads instead of resonating. Keep the no-escalation rule but make the floor the server's number.

## 32. [MEDIUM] renewLoop discards the ca.pem read error and persists whatever it got, so one failed read writes an empty trust anchor and the next boot os.Exit(1)s in a loop identity.Decide can never reach

`apps/node/cmd/agent/main.go:509` · swallowed-error

**Claimed sequence**

```
Starting state: a healthy gateway, cert valid, renewLoop firing at half-life. main.go:509 `ca, _ := os.ReadFile(filepath.Join(certDir, "ca.pem"))` — the error is dropped. Any read failure (an EIO on the state volume, a PVC remounted read-in-flight, an operator/init-container that cleared the directory, a CSI blip) yields ca == nil with err silently discarded. main.go:510 then calls saveCreds(certDir, certPEM, keyPEM, nil), which at main.go:443 writes ca.pem.tmp with zero bytes and renames it over the real CA (the rename is atomic and therefore complete — the atomicity the comment at main.go:419-430 is proud of is what makes the destruction total). logger.Info("agent_cert_renewed") — success is announced. The running process keeps working because its TLS pool is already built. On the next restart: loadCreds succeeds (ca.pem exists, empty); Decide parses a VALID, unexpired cert.pem → UseStored, so the whole S13.1 recovery apparatus is bypassed by construction; main.go:152 NewClient → client.go:95 `pool.AppendCertsFromPEM(nil)` returns false → "bad CA PEM" → main.go:155 os.Exit(1). CrashLoopBackOff, permanently: every restart repeats identically, Decide will never return Recover because the certificate is valid, and re-key would not help anyway since the failure is the trust anchor, not the identity.
```

**Why it would matter:** It is an exit on a condition the control plane could resolve — the general rule this story wrote down at main.go:956-957 ("an agent must never exit on a condition that a control-plane change could resolve") and enforced everywhere on the enrolment path, while main.go:155 still os.Exit(1)s on material the agent now accepts from an unauthenticated endpoint. The same convergence happens from the re-key path: attemptRekey passes the response's ca_pem straight into saveCreds with no non-empty check, so a CP or proxy that omits ca_pem produces the identical unrecoverable crash loop.

**Suggested direction (NOT ruled):** Fail the renewal (log and `continue`) when ca.pem cannot be read, rather than persisting a hole; make saveCreds refuse a zero-length ca or cert outright; and replace main.go:155's os.Exit(1) with the same idle-with-liveness-up behaviour the enrolment paths use, plus a named remedy.

> **Verifier verdict** — refuted: `False` (confidence high)
>
> Every link in the chain checks out in the code, and none of them self-heals. 1. `apps/node/cmd/agent/main.go:509` — `ca, _ := os.ReadFile(filepath.Join(certDir, "ca.pem"))`. Error dropped, confirmed verbatim. 2. `main.go:431-448` `saveCreds` — no length/validity check on any of the three inputs. `os.WriteFile(tmp, nil, 0o600)` creates a 0-byte `ca.pem.tmp`, and `ca.pem` is the FIRST of the three renames. So an empty CA lands atomically and completely. 3. `main.go:412-415` `loadCreds` — `os.ReadFile` on a 0-byte file returns `(nil, nil)`. There is no `len(ca) == 0` check, so loadCreds returns success on a destroyed trust anchor. 4. `identity.Decide` (decide.go, branch 1) only tests `loadErr !
>
> **Correction:** Confirmed as a defect, but narrow the trigger and re-state the fix locus. Correct title: "saveCreds accepts empty credential material, so renewLoop's discarded ca.pem read error zeroes the trust anchor — latent until the next restart, then a permanent os.Exit(1) loop." Two corrections to the sequence as written: (1) the volume-level triggers it names (read-only remount, CSI blip, volume-wide EIO) 

> **Verifier verdict** — refuted: `False` (confidence high)
>
> I could not refute it — every link is present in the code with no intervening guard, and I verified the two runtime facts by construction rather than by plausibility. LINK 1, the dropped error (apps/node/cmd/agent/main.go:509). `ca, _ := os.ReadFile(filepath.Join(certDir, "ca.pem"))` is the only read; the error is discarded and `ca` is passed straight into saveCreds at :510. Nothing between :509 and :510 tests `len(ca)`. LINK 2, saveCreds does not validate what it is handed (main.go:431-455). The loop is `for _, w := range []f{{"ca.pem", ca}, {"cert.pem", cert}, {"key.pem", key}}` → `os.WriteFile(tmp, w.data, 0o600)` → `os.Rename(tmp, filepath.Join(dir, w.name))`. There is no emptiness/parse
>
> **Correction:** Directionally correct and the mechanism holds, but three claims in the write-up need correcting before it is folded: 1. NOT PERMANENT — bounded at roughly one certificate lifetime. The finding says "CrashLoopBackOff, permanently: Decide will never return Recover because the certificate is valid". `Decide` takes `now time.Time` (decide.go:68) and is re-evaluated on every boot, so once the renewed c

## 33. [MEDIUM] Neither re-key response body is length-limited, unlike every other call in the package, so an unauthenticated plain-HTTP peer can drive the agent to OOMKill

`apps/node/internal/control/rekey.go:200` · unbounded-io

**Claimed sequence**

```
Starting state: default TUNNEX_API_URL=http://api:8080 (cleartext — the re-key path must run off the mTLS channel by construction), gateway with an expired cert, Decide → Recover. A peer on that path (an on-path device, a hijacked service name, or a mis-pointed ingress) answers the challenge POST 200 with `{"nonce":"AAAA...` and never stops. rekey.go:224 `json.NewDecoder(resp.Body).Decode(&out)` reads and buffers the whole string with no io.LimitReader — compare Enroll (client.go:61, io.LimitReader 1<<20) and Renew (client.go:130, 1<<16), which both bound theirs. The only bound is the http.Client Timeout of 20s at rekey.go:210, which covers body reading, so the ceiling is 20 seconds of the link's line rate: ~2.5 GB on a 1 Gbps link, and rekey.go:227's base64.DecodeString then allocates another 3/4 of it. Wrong outcome: the agent's RSS exceeds the container memory limit and the kernel OOMKills it; the pod restarts, Decide returns Recover again, and attemptRekey walks straight back into the same response — a self-sustaining crash loop driven entirely by the remote side. The same hole exists on the submit round trip at rekey.go:200.
```

**Why it would matter:** This is the KILL-SWITCH-NO-UNBOUNDED-I/O law's shape on the recovery path: unbounded I/O reached from a state where the gateway has no other way home. The CP bounded its side of exactly these two routes (maxRekeyBodyBytes, rekeythrottle.go:129-136, with the comment noting no other route in apps/api has a body cap) — the agent's side of the same pair of routes was not given the matching bound.

**Suggested direction (NOT ruled):** Wrap both bodies in io.LimitReader before decoding (a cert + CA is a few KiB; a nonce is 32 bytes — 64 KiB is generous for both) and reject a nonce whose decoded length is not what the CP issues.

> **Verifier verdict** — refuted: `True` (confidence high)
>
> MECHANISM: confirmed exactly as written. rekey.go:200 and rekey.go:224 both call json.NewDecoder(resp.Body).Decode with no io.LimitReader, and the asymmetry with client.go:61 (1<<20) and client.go:130 (1<<16) is real. http.Client.Timeout does cover body reading, so 20s of line rate is the only bound, and the gateway chart does cap memory at 256Mi (deploy/helm/tunnex-gateway/values.yaml:109-111) with a plain-HTTP apiURL (values.yaml:29, "e.g. http://tunnex-cp-api.tunnex.svc.cluster.local:8080"). I am not disputing any of that. WHAT MUST BE TRUE FOR IT TO FIRE — and this is the whole finding: an ACTIVE HOSTILE PEER must answer at TUNNEX_API_URL with HTTP 200 and then stream hundreds of megabyt
>
> **Correction:** The finding should be restated and demoted: "rekey.go:200 and rekey.go:224 decode the two re-key response bodies without the io.LimitReader that Enroll (client.go:61) and Renew (client.go:130) apply — a defense-in-depth inconsistency on the unauthenticated path, to be closed in the same fold as the main.go:861 trust-anchor defect, which is the actual consequence-bearing bug on that response." It d

> **Verifier verdict** — refuted: `False` (confidence high)
>
> CONFIRMED by construction. rekey.go's import block (lines 3-21) contains no "io" at all, so neither decode is length-limited: rekey.go:224 `json.NewDecoder(resp.Body).Decode(&out)` for the challenge nonce (followed by base64.StdEncoding.DecodeString at :227) and rekey.go:200 for the cert/CA submit. The contrast is real: client.go:61 `io.ReadAll(io.LimitReader(resp.Body, 1<<20))` in Enroll and client.go:130 `io.ReadAll(io.LimitReader(resp.Body, 1<<16))` in Renew. No guard prevents the sequence. The only bound is `&http.Client{Timeout: 20 * time.Second}` at rekey.go:210 (challenge) and rekey.go:185 (submit); http.Client.Timeout keeps running after Do returns and interrupts Response.Body reads,
>
> **Correction:** The defect stands; two subclaims are imprecise and should be restated. (1) "unlike every other call in the package" is false — FetchDesired at client.go:271 also uses an unlimited json.NewDecoder. The correct distinguishing property is server authentication, not the limit: FetchDesired/Renew/ReportInfo all run over c.http, whose tls.Config pins `RootCAs: pool` built from the node's CA (client.go:9

## 34. [MEDIUM] The throttle break exits the identity list before cert_serial, so under a partially-exhausted shared bucket the only identity that can succeed is starved by one the control plane provably does not hold

`apps/node/cmd/agent/main.go:872` · starvation

**Claimed sequence**

```
Starting state: gateway restarting after a previous boot in which re-key was attempted and refused (a CP outage). rekey-pending-key.pem is on disk from that boot, but no commit ever happened, so the CP does not hold its fingerprint. main.go:838 sees pendingWasOnDisk==true and puts the fingerprint identity FIRST; the serial identity — the one the CP can actually resolve — is second. Now the fleet is recovering behind one NAT and the shared 600/min bucket (rekeythrottle.go:127, keyed on raw peer IP by design) is intermittently exhausted. Each round: the fingerprint identity's challenge or submit draws a 429 → main.go:871-873 `break` exits the identity loop → main.go:891 waits 30s → `continue` → the list is walked from the top again, fingerprint first. The serial identity is reached only in rounds where BOTH fingerprint requests get through the bucket AND come back 403. Wrong outcome: under sustained partial throttling the agent spends its entire share of the shared budget on an identifier that cannot resolve, and the identifier that would recover it is never sent — while every round burns 30s and two requests of the fleet's allowance.
```

**Why it would matter:** The comment at main.go:827-831 justifies fingerprint-first with "that is the only situation in which the control plane can already hold it" — true, but 'can' is doing all the work: after any failed attempt the pending file survives, so fingerprint-first fires on every restart including the overwhelming majority where nothing was ever committed. Under contention the ordering is not merely a wasted challenge (as the comment concedes) but the difference between recovering and not.

**Suggested direction (NOT ruled):** Do not restart the identity list on a throttle — remember which identity was in flight and resume there, or alternate the order between rounds so neither identity can monopolise a constrained budget. Separately, record on disk whether a submit was actually sent (not merely that a key was minted) so pendingWasOnDisk means 'the CP may hold this' rather than 'a key file exists'.

## 35. [MEDIUM] Redirects are followed with Go's default policy, so a 301/302/303 silently downgrades the POST to GET and drops the proof body; the resulting 405 is reported to the operator as a revocation

`apps/node/internal/control/rekey.go:185` · error-mapping

**Claimed sequence**

```
Starting state: TUNNEX_API_URL=http://gw.example.com (the documented default scheme is http), the deployment sits behind an ingress configured with the near-universal HTTP→HTTPS redirect, and the gateway's cert has expired. Decide → Recover. rekey.go:210/185 use a bare &http.Client{} with no CheckRedirect, so Go's default follows up to 10 redirects and, for 301/302/303 on a POST, rewrites the method to GET and drops the body. I verified this: the final server saw `method=GET contentLength=0`. So the CP (or the second hop) receives a GET with no identifier, no nonce, no CSR and no signature on /api/v1/agent/rekey — a route registered for POST only — and answers 405. rekey.go:193 maps every non-200/429 to ErrRekeyRefused. main.go:908 then logs, at ERROR, `agent_rekey_refused` with server_said="refused, without a reason", most_likely_cause="this gateway was enrolled before the control plane recorded agent public keys ... or it was REVOKED", and remedy="mint a join token in the control plane and restart this agent". Wrong outcome: the operator's actual fault is one character of scheme in an env var; the diagnosis names revocation and the printed remedy destroys the node's id, site binding, devices and metrics series to work around a redirect.
```

**Why it would matter:** The request the control plane sees is not the request the agent sent, and nothing anywhere reports that a redirect occurred — the round trip that is supposed to carry a cryptographic proof arrives carrying nothing, and the agent's most authoritative log line attributes it to the most destructive possible cause. Materially different from the pass-1 'non-200 maps to refused' mapping: here the CP never refused anything, and the body loss happens inside the HTTP client before any status is produced.

**Suggested direction (NOT ruled):** Set CheckRedirect on both re-key clients to refuse redirects outright and surface a distinct error ("the re-key endpoint redirected to X — check TUNNEX_API_URL's scheme/host"), which is a local diagnosis the operator can act on, unlike a refusal.

## 36. [MEDIUM] Boot exits with os.Exit(1) on credential-shaped failures, so an interrupted saveCreds becomes permanent CrashLoopBackOff instead of routing to the recovery path three lines above

`apps/node/cmd/agent/main.go:155` · never-exit-rule-violation

**Claimed sequence**

```
Starting state (a): the process is killed (SIGKILL, OOM, node reset) inside saveCreds between the cert.pem rename at main.go:446 and the key.pem rename — the state main.go:419-430 exists to prevent and, given the [ca, cert, key] order at main.go:441, the state that order actually produces. Disk now holds the NEW cert.pem beside the OLD key.pem. (1) On restart, loadCreds succeeds (all three files exist). (2) identity.Decide (main.go:64) parses only cert.pem — it never checks that key.pem matches — sees a valid NotAfter and returns UseStored. (3) main.go:152 control.NewClient → client.go:90 tls.X509KeyPair(certPEM, keyPEM) fails ("private key does not match public key"). (4) main.go:155 os.Exit(1). The container restarts, replays exactly the same sequence, and CrashLoopBackOffs forever — the recovery machinery at main.go:77 is never reachable because Decide returned UseStored. Starting state (b), same landing: main.go:509 `ca, _ := os.ReadFile(...ca.pem)` in renewLoop IGNORES the read error, so a transient EIO/EMFILE hands nil to saveCreds, which atomically renames a 0-byte ca.pem over the trust anchor. In-process nothing breaks (the RootCAs pool was built at boot), so the damage is silent; on the next restart loadCreds returns an empty ca (os.ReadFile of an empty file returns nil error), client.go:95 AppendCertsFromPEM fails → "bad CA PEM" → main.go:155 os.Exit(1) → CrashLoopBackOff.
```

**Why it would matter:** main.go:948-957 records the general rule this branch adopted — "an agent must never exit on a condition that a control-plane change could resolve" — and enrollWithToken was rewritten specifically to honour it. Three os.Exit(1) calls (main.go:155, 163, 251) survive on the same boot path, and the first one is where every mixed-credential and bad-CA state lands. CrashLoopBackOff is strictly worse than idling: it destroys the log the operator needs and, on k8s, backs off to 5-minute restart intervals.

**Suggested direction (NOT ruled):** Treat a NewClient failure as a credential verdict, not a fatal: on X509KeyPair mismatch, route into the recovery path (the pending key or the join token) rather than exiting; on a bad CA, idle loudly with liveness up. Have identity.Decide (or loadCreds) verify that key.pem matches cert.pem so the mismatch is a verdict input rather than a crash. Separately, stop ignoring the ca.pem read error at main.go:509 — a failed read must abort the save, not persist an empty trust anchor.

> **Verifier verdict** — refuted: `False` (confidence high)
>
> CONFIRMED by construction — nothing on the path prevents either sequence, and both land on the same unguarded exit. THE LANDING (shared by both states). apps/node/cmd/agent/main.go:152-156: client, err := control.NewClient(agentURL, serverName, nodeName, certPEM, keyPEM, caPEM) if err != nil { logger.Error("agent_client_failed", ...); os.Exit(1) } control.NewClient (apps/node/internal/control/client.go:89-96) fails on exactly two credential-shaped inputs and nothing else: cert, err := tls.X509KeyPair(certPEM, keyPEM); if err != nil { return nil, err } pool := x509.NewCertPool(); if !pool.AppendCertsFromPEM(caPEM) { return nil, fmt.Errorf("bad CA PEM") } So a cert/key mismatch and an unusable
>
> **Correction:** The finding stands as written; two refinements to the mechanism. (1) "routing to the recovery path three lines above" is not what a fix would do — identity.Decide returns UseStored for state (a) (the cert is valid; only the key is wrong), so the Recover case at main.go:73 is not merely skipped, it is not selectable. A correct fix validates the pair before the switch — parse key.pem and compare its

## 37. [MEDIUM] renewLoop discards the ca.pem read error and writes the result back, replacing the trust anchor with a zero-length file

`apps/node/cmd/agent/main.go:509` · correctness

**Claimed sequence**

```
Starting state: a healthy, enrolled gateway. The renew tick fires (main.go:503).

1. main.go:504 `client.Renew` succeeds — the CP returns only the new certificate, no CA.
2. main.go:509 `ca, _ := os.ReadFile(filepath.Join(certDir, "ca.pem"))`. The error is discarded. On any failure — transient EIO, a stale handle, an operator who removed the file — `ca` is nil.
3. main.go:510 `saveCreds(certDir, certPEM, keyPEM, nil)` → main.go:441-448 writes a zero-length ca.pem.tmp and renames it over the working ca.pem. The trust anchor is now gone from disk. main.go:514 logs `agent_cert_renewed`.
4. The running process is unaffected (the CA pool lives in memory in the existing Client), so nothing surfaces. The pod runs for days.
5. Next restart: loadCreds succeeds, Decide sees the freshly renewed, valid certificate → UseStored → main.go:152 `control.NewClient` → client.go:95 `AppendCertsFromPEM([]byte{})` returns false → "bad CA PEM" → main.go:155 os.Exit(1).

Wrong outcome: permanent CrashLoopBackOff. Recover is unreachable (the certificate is valid), the join token is never read, and the only remedy is a human restoring ca.pem or wiping the state dir — which discards the node.
```

**Why it would matter:** saveCreds is documented as the function that guarantees a crash leaves either the old set or the new set, and this caller hands it a third thing: a new set with the CA deliberately blanked. It is also the one place in the whole boot/renew path that answers 'does anything overwrite a working credential set?' with yes, silently, on an ignored error.

**Suggested direction (NOT ruled):** Handle the error: if ca.pem cannot be read, skip persisting it (keep the on-disk file) rather than writing what you got, and log loudly. saveCreds should also refuse a zero-length CA or cert outright — no caller has a legitimate reason to pass one.

## 38. [MEDIUM] loadOrCreateWGKey regenerates the gateway's WireGuard identity on any read error, not only on corrupt content

`apps/node/cmd/agent/main.go:660` · correctness

**Claimed sequence**

```
Starting state: an established gateway. wg.key holds the private key whose PUBLIC half is embedded in every device configuration already handed out (apps/api/internal/devices/service.go:379 puts `node.WgPublicKey` into the peer block of each generated config) and in every site-link peer.

1. main.go:660 `if data, rerr := os.ReadFile(path); rerr == nil {` — the read fails for any reason other than corruption: EIO, EACCES after a `runAsUser`/fsGroup change on the mounted state dir, a stale handle.
2. Control falls out of the `if` (the same path a corrupt file takes) to main.go:669: a brand-new X25519 key is generated and main.go:674 `os.WriteFile(path, …)` overwrites wg.key — no tmp+rename, unlike every other key this file writes.
3. reportKeyLoop reports the new public key; the CP re-pushes peers.

Wrong outcome: every previously issued device config is now pointing at a public key the gateway no longer has. Those configs are one-time-secret downloads — the user's file cannot be updated by a CP push — so every device on the gateway must be re-issued, from a transient read error. The non-atomic write at main.go:674 also means a crash mid-write leaves a truncated key that regenerates on the next boot, producing the same rotation a second time.
```

**Why it would matter:** The comment at main.go:653-657 vouches for this as self-healing: "A missing OR unparseable file … triggers regeneration rather than a hard error — otherwise a corrupt key file would wedge the agent in a permanent crash-loop." The self-heal is real for a corrupt file, but the code cannot distinguish 'the bytes are bad' from 'I could not read the bytes', and in the second case it destroys a key that was fine. main.go:157-159 reinforces the impression that rotation is cheap ("Re-key = delete the file"); it costs every issued device config.

**Suggested direction (NOT ruled):** Regenerate only when the file is absent (os.IsNotExist) or its CONTENT fails to decode. Treat any other read error as fatal-but-idle (liveness up, readiness false) with the remedy named, rather than silently minting a new WireGuard identity. Write via tmp+rename like the credential files.

## 39. [MEDIUM] NotBefore is never read: a not-yet-valid certificate takes UseStored and the Evidence asserts validity that was never checked

`apps/node/internal/identity/decide.go:96` · clock-trust

**Claimed sequence**

```
Starting state: gateway VM/appliance with a dead RTC boots reading 2026-07-20; its cert.pem was issued 2026-07-29 (NotBefore 2026-07-29T…-1min, NotAfter 2026-07-31, agentca/ca.go:197-198). TUNNEX_JOIN_TOKEN present.
1. decide.go:96 computes only `expired := now.After(leaf.NotAfter)` → false. leaf.NotBefore is never consulted anywhere in the package.
2. decide.go:143 returns UseStored with Evidence "stored certificate for \"gw-1\" is valid until 2026-07-31T…" — a validity claim made without checking the lower bound.
3. main.go:152 NewClient succeeds (tls.X509KeyPair does not check validity windows). The first mTLS dial verifies the CP's SERVER certificate — issued NotBefore = real now-1min (ca.go:233) — against the lagging clock, so it is 'not yet valid' and every handshake fails.
4. renewLoop (main.go:504) fails every tick, reportKeyLoop never succeeds, readiness stays false, and the only output is a wall of identical x509 warnings with no verdict, no re-key attempt, and the join token untouched.
Mirror row, same missing check: a clock lagging by more than the certificate's remaining life makes a GENUINELY expired certificate read as not-expired → UseStored → the pre-S13.1 'tls: expired certificate' loop verbatim, with the operator's freshly supplied token ignored.
```

**Why it would matter:** decide_test.go:144-167 claims the package eliminated 'a wall of identical TLS warnings with no remedy in them'; for both clock-skew directions that symptom is fully intact, and no row of the table names a clock problem. The Evidence string is a load-bearing comment-shaped claim ('is valid until') that the code does not establish — an operator reading it will look anywhere except at the clock.

**Suggested direction (NOT ruled):** Read leaf.NotBefore alongside NotAfter and give not-yet-valid its own verdict/Reason (it is not recoverable by re-key either — the CP would refuse a still-valid node — so the honest verdict is a loud Idle naming clock skew as the determination). At minimum, stop asserting 'is valid' in Evidence for a window whose lower bound was never examined.

## 40. [MEDIUM] The Recover branch logs neither Evidence nor NameMismatch, so the expiry determination and the wrong-host signal are silent on the path they were written for

`apps/node/cmd/agent/main.go:72` · producer-without-consumer

**Claimed sequence**

```
Starting state: azure-gw's state volume; cert.pem CN=azure-gw expired 3h ago; the operator pastes the wrong install command, setting TUNNEX_NODE_NAME=aws-gw-1 (the EPIC 11 walk incident) with a token pinned to aws-gw-1.
1. Decide → decide.go:107-119 returns Recover with NameMismatch=true, StoredCN="azure-gw", and the Evidence string assembled at :108-116 (expiry age, NotAfter, the re-key attempt, the token's fallback role).
2. main.go:72-81, the Recover case, logs nothing at all: verdict.Evidence and verdict.NameMismatch are never read on this path. The other three cases each consume them (Idle at :110, UseToken at :119 and :124, UseStored at :148-149).
3. attemptRekey succeeds; the CP re-keys the azure-gw row (SignCSR uses node.Name, ca.go:196, so the requested name is discarded server-side). main.go:865 logs agent_rekeyed with the old serial only.
4. main.go:79 re-Decides purely to recompute the name; that second verdict — which now carries NameMismatch=true against a valid cert, the case main.go:138-143 escalates to ERROR — is discarded.
Wrong outcome: the operator gets no message that the certificate had expired, no message that the host is not the one they targeted, and no trace of the token being held in reserve. The valid-cert twin of this exact situation logs an ERROR naming the remedy; the expired twin logs nothing. On the ordinary (non-mismatch) recovery the agent is also silent for the whole first attempt — the operator watching a gateway that will not come up sees nothing until either agent_rekeyed or the first refusal.
```

**Why it would matter:** decide.go:44-47 states that the verdict's evidence exists so 'unusable' is a determination the agent can defend, and decide.go:53-55 states NameMismatch is reported separately from Action specifically so it can never be silent. On the Recover path both are produced and neither is consumed — the standing who-reads-this rule (name the consumer, cite the reading line) has no answer here. The wrong-host case is the one the mismatch field was created for, and it is exactly the case where an expired certificate is likely (a stale box being re-purposed).

**Suggested direction (NOT ruled):** Log the verdict once, before the switch, from the fields every branch shares (Reason, Evidence, StoredCN, NameMismatch, HaveToken), and escalate to ERROR when NameMismatch is set regardless of Action. Do the same for the post-re-key verdict at main.go:79 rather than discarding it.

## 41. [MEDIUM] Retry-After is parsed and thrown away; the throttled path waits a fixed 30s forever with no escalation, falsifying both the "ceiling is the load-bearing number" comment and the control plane's own throttle sizing

`apps/node/cmd/agent/main.go:892` · correctness

**Claimed sequence**

```
Claims under test — control/rekey.go:39-40: "retryAfter reads the server's Retry-After... Returns 0 when absent or unparseable — **the caller then picks its own short delay** rather than guessing long" (implying a non-zero value is used by a caller); and main.go:766-777: "THE CEILING IS THE LOAD-BEARING NUMBER... One attempt per hour, sustained, is two requests per hour per bricked gateway."

No caller ever reads retryAfter's value. It is formatted into an error string at control/rekey.go:191 and 216 and nowhere else; grep for retryAfter shows exactly those two uses. The agent's throttle handler at main.go:891-904 does `wait := rekeyBackoffFloor` — a hardcoded 30s — and the comment at main.go:902 states it "Deliberately does NOT advance the backoff", so the hour ceiling is unreachable on this path.

Starting state: a 200-gateway fleet behind one ingress, all certificates expired together after a CP outage longer than 48h. The CP throttle keys on the raw peer address, so all 200 share one bucket of 600/min (apps/api/internal/http/rekeythrottle.go).

1. Each agent's attempt is up to 2 identities x 2 requests (challenge + submit) = 4 requests, and with a 30s floor that is 8 requests/min per gateway — twice the "4 requests/min per gateway" the CP's rekeyAttemptsPerMinute derivation assumes. 200 gateways = 1600/min against a 600/min bucket.
2. Every agent that loses the race gets 429 with `Retry-After: 60` (rekeythrottle.go, throttled()).
3. control.Rekey/rekeyChallenge return ErrRekeyThrottled. attemptRekey breaks out of the identity loop, logs agent_rekey_throttled with retry_in=30s, sleeps 30s, and loops — **forever, at the same 30s, ignoring the 60s the server asked for**, with the attempt counter never escalating.
4. The offered load never drops below the bucket, so the fixed window is exhausted in its first seconds every minute and recovery becomes a lottery. Agents that are genuinely unrecoverable (pre-0057 keys) also sit in this loop at 8 req/min instead of backing off, because a 429 never advances their backoff.

Honouring the server's 60s would halve the sustained load; escalating on repeated throttles would drain it.
```

**Why it would matter:** The two comments together read as "the load question has been worked through and bounded", so the next engineer will not re-derive it. The bound only holds on the refusal path; on the throttle path the agent sustains 8 requests/minute indefinitely against the exact endpoint the throttle exists to protect, at the exact moment the fleet is recovering.

**Suggested direction (NOT ruled):** Use the parsed Retry-After: have Rekey/rekeyChallenge return the duration (typed error or an out-param) and have main.go:892 wait max(Retry-After, floor). Add a separate, escalating throttle backoff (still capped well below the refusal ceiling) so repeated 429s drain rather than sustain. And re-derive rekeyAttemptsPerMinute for 2 identities x 2 requests per attempt.

## 42. [MEDIUM] TestRekeyCannotBeTriggeredByConnectionFAILURE's "VERDICT-SET assertion" is tautological, and its structural guard covers the one entry point a future author would not use

`apps/node/internal/identity/decide_test.go:246` · test-validity

**Claimed sequence**

```
Claim under test — decide_test.go:209-222: "the condition on the agent's re-key trigger, asserted structurally rather than behaviourally, which is the stronger form... This test's real value is that it must be EDITED before that can change."

The verdict-set half cannot fail. decide_test.go:233 defines `rekeyable := map[string]bool{"stored_identity_expired": true}`. The loop at 245-254 enters its body only when `rekeyable[v.Reason]` is true, i.e. only when v.Reason is the literal string "stored_identity_expired", and the assertion inside is `strings.Contains(v.Reason, "expired")` — true by construction of the map key. Construct the violating input: add to decide.go a branch `return Verdict{Action: Recover, Reason: "control_plane_unreachable", ...}` and append `Decide(...)` producing it to `all`. `rekeyable["control_plane_unreachable"]` is false, the body never runs, and the test passes green while a reachability-derived verdict now authorises re-key. The test never inspects v.Action at all, which is the field that actually selects the Recover path in main.go:71-77. The real property ("no verdict with Action==Recover may rest on a non-local fact") is unasserted.

The signature half is real but guards the wrong door. `var decide func(certPEM []byte, loadErr error, requestedName string, haveToken bool, now time.Time) Verdict = Decide` does break compilation if a parameter is added to Decide. But an author making the trigger "smarter" about handshake failures would not add a parameter to Decide — attemptRekey is a package-level function in package main (main.go:791) with no guard on who calls it, so the natural implementation is a call from renewLoop or from the reconcile error path ("renew has failed N times, try re-key"), which compiles cleanly, touches neither Decide nor this test, and produces exactly the unauthenticated-endpoint flood during a CP outage that the test's comment says is forbidden.

The stale comment at decide_test.go:243 (`// expired (uses token)`, for a case that now returns Recover) is a symptom that the list is not being read as live.
```

**Why it would matter:** This repo has a named law about exactly this shape (COULD THIS CHECK HAVE FAILED? / tautological guards), and this test's own comment claims it is the stronger, structural form. A green tautology in the file that documents the re-key trigger is worse than no test: it is the artifact a reviewer will point at when asked whether the trigger can be reached from a network condition.

**Suggested direction (NOT ruled):** Assert the property over the whole verdict set without a lookup table: iterate `all` and require `v.Action != Recover || strings.Contains(v.Reason, "expired")`, so a new Recover verdict under any new reason fails the test without anyone opting it in. Then move the structural guard to the actual trigger — e.g. give attemptRekey a parameter typed on the verdict (`func attemptRekey(v identity.Verdict, ...)`) and assert it panics/refuses for any Action other than Recover, so a call from renewLoop cannot compile or cannot run.

## 43. [MEDIUM] The re-key signed-message construction exists in three independent copies with no cross-module pin, including one in the agent's own test — while the control plane's comment claims "one definition, imported by both sides' tests"

`apps/node/internal/control/rekey.go:170` · correctness

**Claimed sequence**

```
Claims under test — control/rekey.go:168: "The signed message must match the server's construction exactly. Kept adjacent to the request that carries it so the two cannot drift apart silently"; and apps/api/internal/rekey/pop.go's SignedMessage doc: "Exported because the AGENT must construct the identical bytes, and the two implementations agreeing by accident is not a property worth relying on — **one definition, imported by both sides' tests**."

The agent cannot import it: apps/node is a separate Go module (apps/node/go.mod, module github.com/tunnexio/tunnex/apps/node) and does not depend on apps/api. Grep for SignedMessage across apps/ returns exactly two call sites, both in apps/api (rekey/pop_test.go:48 and nodes/rekey_integration_test.go:122). The agent has its own inline copy at control/rekey.go:170, and the agent's test has a THIRD copy at control/rekey_test.go:118 (`sha256.Sum256(append(append([]byte{}, srv.nonce...), blk.Bytes...))`) — an expectation derived from the same construction it is checking, which is why TestProofIsSignedByThePoPKeyAndBoundToTheCSR would stay green through any drift.

Failing sequence: someone acts on pop.go's own invitation ("with a fixed-length nonce and length-prefixed hashing this is belt-and-braces") and changes SignedMessage to prepend a domain-separation tag or a length prefix. apps/api's tests are updated because they import the function; make test-editions passes. apps/node is untouched: control/rekey.go:170 still builds nonce||csrDER, and rekey_test.go:118 still builds nonce||csrDER, so `make test-node` passes too. CI is fully green. Every re-key in the field then fails at apps/api/internal/rekey/pop.go's VerifyPKCS1v15 and returns the uniform ErrRekeyRefused — which the agent reports as "most likely cause: this gateway was enrolled before the control plane recorded agent public keys... or it was REVOKED", sending operators to mint join tokens fleet-wide.

The contrast is the point: the key FINGERPRINT — the less load-bearing of the two shared constructions — is pinned by a golden vector in all three places the comment claims (control/fingerprint_test.go:28, nodes/rekeyident_test.go:14, and nodes/rekey_integration_test.go:292 against the generated column; verified, that claim holds). The signed message, which every successful re-key depends on, has no golden anywhere.
```

**Why it would matter:** Both comments assert that the drift hazard has been closed by shared code, and one of them names a mechanism ("imported by both sides' tests") that does not exist for the side that matters. That is exactly what makes the next engineer skip adding the golden, and the failure it produces is indistinguishable from "these gateways cannot be recovered".

**Suggested direction (NOT ruled):** Pin it the same way the fingerprint is pinned: a golden vector (fixed nonce, fixed CSR DER, expected SHA-256 of the signed message, or an expected signature over a fixed test key) asserted in apps/node/internal/control and again in apps/api/internal/rekey. Derive nothing in either test from the construction under test.

## 44. [MEDIUM] loadOrCreatePendingKey's "loudly-by-consequence (the caller logs the mint)" — no caller logs anything, so the one unrecoverable pending-key case is silent and actively mis-diagnosed

`apps/node/cmd/agent/main.go:476` · observability

**Claimed sequence**

```
Claim under test — main.go:475-477: "Unreadable pending material is worse than none: it would be submitted, refused, and looked like a server-side refusal. Replace it **loudly-by-consequence (the caller logs the mint)** rather than carrying it."

The caller does not log the mint. attemptRekey calls loadOrCreatePendingKey at main.go:812 and uses the returned `pendingWasOnDisk` only to order the identities slice (main.go:838). There is no log statement on the success path between main.go:812 and main.go:852; the only logs are agent_rekey_impossible on error and agent_rekey_refused after a failed attempt. `wasOnDisk=false` is never surfaced anywhere.

Starting state: a gateway that re-keyed, lost the response, and had its state volume snapshot-restored / hit a page-cache truncation, leaving rekey-pending-key.pem present but truncated to 900 bytes.

1. Boot → Decide → Recover → loadOrCreatePendingKey reads the file, `len(b) > 0` so it enters the branch, KeyFingerprintFromPEM fails, execution falls through to mint a fresh K3 and overwrite the file (main.go:478-493), returning wasOnDisk=false.
2. The fingerprint the control plane actually recorded — fp(K2) — is now permanently unrecoverable by this agent. This is the only state in the whole design where the D10 identifier is truly gone.
3. identities therefore contains only the serial identity, which the CP no longer carries. Refused.
4. The operator sees agent_rekey_refused (main.go:908-922) whose most_likely_cause reads "this gateway was enrolled before the control plane recorded agent public keys... or it was REVOKED". Both are false. Nothing in any log line mentions that a pending key was found, found unusable, and discarded — the single fact that explains the refusal.

TestUnreadablePendingKeyIsReplacedNotCarried (main_test.go:97) asserts the replacement happens and its comment repeats the same diagnosis rationale, but asserts nothing about it being observable.
```

**Why it would matter:** The comment claims the loudness requirement is satisfied by the caller, so nobody adds the log. The result is that the one genuinely unrecoverable pending-key state is invisible and the agent's headline diagnostic points at two causes that are both wrong — the same "sends the operator to the wrong side of the wire" failure the comment says it is preventing.

**Suggested direction (NOT ruled):** Have loadOrCreatePendingKey report which of the three cases it took (reused / minted-fresh / replaced-unusable) and have attemptRekey log it at Warn for the replaced case, including it in the agent_rekey_refused most_likely_cause list. Or move the log into loadOrCreatePendingKey and stop claiming the caller owns it.

## 45. [MEDIUM] "The trigger is local... so a transient outage cannot land here" — the trigger's real external input is the host clock, sampled once at boot and never revisited

`apps/node/cmd/agent/main.go:76` · correctness

**Claimed sequence**

```
Claims under test — main.go:74-76: "The trigger is local: identity.Decide reached this verdict from this agent's own clock against its own stored certificate, with no network call, so a transient outage cannot land here"; decide_test.go:250: a re-key trigger "must rest on a locally-provable fact"; decide.go:96 `expired := now.After(leaf.NotAfter)`.

The network claim holds. The stronger claim the comments trade on — that the trigger rests on a fact the agent can check without depending on anything external — does not: `now` is time.Now() on a host whose clock is set by NTP, and Decide is evaluated exactly once, at main.go:64, before the process does anything else.

Starting state: a gateway VM restored from a snapshot, or a bare-metal box with a dead RTC, or a container started before chrony's first step. cert.pem is valid for another 30h. The clock reads 3 days ahead at process start; NTP corrects it 8 seconds later.

1. main.go:64 Decide(certPEM, nil, name, haveToken, time.Now()) → now.After(NotAfter) is true → Action=Recover, Reason=stored_identity_expired.
2. main.go:77 attemptRekey. The CP evaluates the same question against ITS clock: RekeyAuthorized sees cert_not_after in the future → policyhealth.go:351 → uniform refusal ("a live gateway must never be re-keyed").
3. NTP steps the clock back. The certificate is valid again. Nothing re-evaluates: Decide is not called again, and attemptRekey's loop (main.go:852) has no exit other than success or ctx cancellation, so it never returns to the boot path.
4. The agent never reaches control.NewClient (main.go:152) or r.Run (main.go:378). A healthy gateway with a valid certificate stays down, retrying an endpoint that will refuse it for the next 30h, logging most_likely_cause "enrolled before key recording... or REVOKED", until a human restarts the pod.

Decide's own TestExpiryBoundaryIsExclusive (decide_test.go:196) shows the authors were alive to "a clock a millisecond fast must not re-enroll a live gateway" — but the mitigation stops at the exact NotAfter boundary and there is no re-evaluation and no sanity check on the clock at all.
```

**Why it would matter:** "No network argument" is being used as a proof that the trigger is trustworthy, in a comment and in a test's rationale. It is not: the clock is as much an external input as the socket, and unlike the socket it is read once and cached for the process's lifetime, so a few seconds of boot-time skew costs a healthy gateway an unbounded outage.

**Suggested direction (NOT ruled):** Re-evaluate rather than latch: on each re-key attempt (or at least each backoff wake), re-read the clock and re-run identity.Decide against the stored certificate, and abandon the Recover path the moment it no longer says expired. A cheap corroboration also exists — leaf.NotBefore in the future, or NotAfter more than CertTTL in the past on a fresh boot, both indicate the clock rather than the certificate, and should be logged and refused rather than acted on.

## 46. [MEDIUM] renewLoop ignores the ca.pem read error and writes a zero-length CA, which makes the next boot os.Exit(1) into CrashLoopBackOff

`apps/node/cmd/agent/main.go:509` · correctness

**Claimed sequence**

```
Starting state: healthy gateway, renew tick fires, client.Renew succeeds.

(1) main.go:509 `ca, _ := os.ReadFile(filepath.Join(certDir, "ca.pem"))` — the error is discarded. Any transient read failure (EIO/ESTALE on a CSI or NFS-backed PVC, fd exhaustion under EMFILE, the volume briefly remounted) yields ca = nil.
(2) main.go:510 saveCreds(certDir, certPEM, keyPEM, nil) writes a ZERO-BYTE ca.pem over the trust anchor and reports success; the agent logs agent_cert_renewed and keeps running normally on its in-memory client, so the damage is invisible until the next restart.
(3) Next restart: loadCreds succeeds (an empty file reads without error), identity.Decide sees a valid cert → UseStored, and main.go:152 control.NewClient → client.go:95 `pool.AppendCertsFromPEM(caPEM)` returns false → "bad CA PEM" → main.go:155 os.Exit(1).
(4) The process now crash-loops. No recovery path covers it: the certificate is valid so Decide never returns Recover, a join token would not be consulted either, and the exit happens after the identity decision — so this is exactly the 'never exit on a condition a control-plane change could resolve' law (main.go:98-101, main.go:956) violated by the one exit the diff left in place, for a condition the agent inflicted on itself.
```

**Why it would matter:** An ignored error on a read that did not need to happen at all silently destroys the only trust anchor and converts a healthy gateway into a crash loop that no operator action short of re-provisioning the state directory will fix.

**Suggested direction (NOT ruled):** Do not re-read ca.pem — main already holds caPEM in memory; pass it to renewLoop (and update it whenever a path replaces it). If a read is kept, treat the error as fatal to that renew cycle rather than persisting nil. Separately, make the NewClient failure at main.go:152 non-fatal in the same way enrolment failure is: log the remedy and idle rather than exit.

## 47. [MEDIUM] The runtime cert-expiry remedy still tells the operator to re-enroll with a join token, the identity-destroying path S13.1 replaced

`apps/node/internal/reconcile/certexpiry.go:42` · correctness

**Claimed sequence**

```
Starting state: a gateway whose certificate expires while the process is running (reachable by the renewLoop scheduling defect above, or by any renew outage spanning the remaining lifetime).

(1) reconcile.go:443 detects the condition precisely — CertExpiredRemedy matches the TLS alert — and logs agent_cert_expired_cannot_reconnect once.
(2) The remedy string it prints (certexpiry.go:42-46) says 'Retrying will NOT recover it. RE-ENROLL this gateway: create a join token ... and run the enrollment command on this host', and asserts that certificates 'renew automatically while the agent is running'. After S13.1 the first claim is wrong (a restart re-keys IN PLACE by proof of possession, keeping the node id, site binding and devices), the instruction points at the fallback that discards all three, and the last claim is the same falsified property as walk-artifacts/S13.1/clock-record.md:16.
(3) Nothing in the running process acts on the detection either: identity.Decide and attemptRekey are called only in the boot sequence (main.go:64-150), so the agent sits in the one state it now knows how to fix, printing instructions for the destructive fix.
```

**Why it would matter:** The one message an operator reads at the exact moment in-place recovery is available steers them to the join-token path the story exists to avoid, and vouches for an automatic-renewal property the agent does not have. An operator who follows it does extra work and, on any CP where the enrolment collides with the node's own row, ends up worse off.

**Suggested direction (NOT ruled):** Rewrite the remedy to name the in-place route first ('restart the agent — it will recover this gateway in place by proving possession of its recorded key; mint a join token only if that is refused'), drop the 'renews automatically while running' claim, and consider having the detection trigger the recovery instead of only describing it — the condition is the same one identity.Decide already rules on at boot.

## 48. [MEDIUM] No response size limit on either unauthenticated re-key call, while both authenticated siblings have one

`apps/node/internal/control/rekey.go:200` · unbounded-input

**Claimed sequence**

```
Starting state: gateway pod with the chart's 256Mi memory limit (values.yaml:111), recovering over plain HTTP. rekeyChallenge decodes with `json.NewDecoder(resp.Body).Decode(&out)` (rekey.go:224) and Rekey with the same at rekey.go:200 — neither wraps the body in an io.LimitReader. Whoever answers the plain-HTTP API URL replies 200 with `{"nonce":"<hundreds of MB of base64>"}`; encoding/json allocates the whole string, plus the decoder's buffer, plus the base64 output. The 20s http.Client timeout bounds the duration, not the volume — on a normal link that is far more than 256Mi. The container is OOM-killed mid-recovery and restarts into the same loop, so the kill repeats on every attempt. Contrast the two authenticated calls in the same package, which both bound their bodies: Enroll uses io.LimitReader(resp.Body, 1<<20) (client.go:61) and Renew io.LimitReader(resp.Body, 1<<16) (client.go:130). The two calls that talk to an unauthenticated endpoint are the two without a limit. (FetchDesired at client.go:271 is also unbounded, but that peer is CA-pinned.)
```

**Why it would matter:** The only two requests the agent makes to a server it cannot authenticate are the only two that will read an unbounded body from it, and the CP side already decided this class matters — it added maxRekeyBodyBytes for exactly the inbound direction. Killing the agent during recovery is cheap, needs no crypto, and is indistinguishable from a crash-looping gateway.

**Suggested direction (NOT ruled):** Wrap both response bodies in io.LimitReader with a few tens of KiB (a cert + CA bundle and a 32-byte nonce are far below that) and treat truncation as a refusal. Consider the same for FetchDesired for consistency.

## 49. [MEDIUM] The challenge nonce is accepted with no validation, so a broken or hostile challenge drives a permanent misdiagnosed refusal loop

`apps/node/internal/control/rekey.go:227` · unvalidated-wire-input

**Claimed sequence**

```
Starting state: expired gateway, Recover path, plain-HTTP API URL. Whoever answers /api/v1/agent/rekey/challenge replies `200 {"nonce":""}` (or any wrong-length value). rekey.go:227 base64-decodes it — an empty string decodes to an empty slice with a nil error — and returns it as the challenge. Rekey then builds msg = nonce ‖ csrDER (rekey.go:170) over what is effectively no challenge at all, signs SHA-256 of it with the node's recorded private key (which is also its mTLS client key), and submits. The CP's ConsumeRekeyChallenge finds no row for that nonce and returns the uniform refusal (rekey.go:150-155 on the CP side). attemptRekey cannot distinguish that from a real refusal, so it logs most_likely_cause="enrolled before the control plane recorded agent public keys ... or it was REVOKED" and retries to the 1h ceiling forever. The agent had everything needed to catch this locally — the nonce contract is 32 random bytes (nodes/rekey.go:55) — and checked nothing. As a secondary consequence, the same missing check makes the agent an unbounded chosen-prefix signer: the server picks arbitrary bytes and the agent signs PKCS1v15 over them with its identity key, with only its own CSR DER as the fixed suffix.
```

**Why it would matter:** rekey.go:134-136 vouches for a property the agent does not verify — "the nonce makes a captured request unreplayable" — which is a server-side guarantee stated as if it were something this code establishes. The observable cost is that a broken intermediary on the challenge hop is indistinguishable from revocation in the logs, which is exactly the diagnosis this slice was built to make honest.

**Suggested direction (NOT ruled):** Require the decoded nonce to be exactly the expected length (32 bytes) and non-zero before signing; treat anything else as a transport fault distinct from ErrRekeyRefused so the log says "the control plane's challenge was malformed" rather than pointing at revocation.

## 50. [MEDIUM] loadCreds is all-or-nothing, so losing ca.pem alone makes the agent discard a recoverable identity and enroll as a new node

`apps/node/cmd/agent/main.go:412` · input-coupling

**Claimed sequence**

```
Starting state: cert.pem (expired, serial readable) and key.pem (the key the CP recorded) intact; ca.pem absent or unreadable — a partial restore of the state directory, an operator removing the trust anchor while chasing a CA problem, or an I/O fault that happens to hit the third read (cert.pem and key.pem are read first, main.go:404-411). loadCreds returns (nil, nil, nil, err) — it nils out the certificate it successfully read. main.go:64 calls Decide(nil, err, ...) which takes branch 1 at decide.go:70 and reports "no_stored_identity": with a token (the shipped Helm shape injects TUNNEX_JOIN_TOKEN on every pod start) it enrolls a NEW node, discarding the site binding and forcing every homed device to be re-issued; without one it Idles forever. Recover is never considered, even though re-key needs only cert.pem's serial and key.pem — both present and intact — and the re-key response would have supplied a fresh ca_pem anyway.
```

**Why it would matter:** decide.go:65 states "THE SAFE DIRECTION IS `UseStored`, and every uncertain case resolves there." This is a case where the loss of a public, replaceable file — not the identity, not the key — routes straight past both UseStored and Recover to the one action that destroys the node, and Decide cannot even see it because loadCreds erased the evidence before the pure function ran.

**Suggested direction (NOT ruled):** Have loadCreds return what it actually found per file (cert/key present, CA missing) and let Decide reason about identity material separately from the trust anchor: an expired cert plus a key with no CA should still be Recover, since the response restores the CA.

## 51. [LOW] The guard protecting 'no network signal may trigger a re-key' is tautological — its inner assertion cannot fail for any input

`apps/node/internal/identity/decide_test.go:234` · vacuous-check

**Claimed sequence**

```
decide_test.go:234 builds `rekeyable := map[string]bool{"stored_identity_expired": true}` — one key, hardcoded. decide_test.go:248-255 then iterates every verdict and asserts: `if rekeyable[v.Reason] { if !strings.Contains(v.Reason, "expired") { t.Errorf(...) } }`. The outer condition is true only for the single literal key the test itself wrote, and that literal contains the substring "expired" — so the inner Errorf is unreachable for every possible input. Concretely: add a new Decide branch returning Reason "control_plane_unreachable" with Action Recover, the exact failure the test's 25-line header says it forbids, and the loop passes green because rekeyable["control_plane_unreachable"] is false. Only the final assertion (decide_test.go:257) and the compile-time signature binding at decide_test.go:227 can actually fail.
```

**Why it would matter:** docs/laws.md records the tautological-guard law and 'COULD THIS CHECK HAVE FAILED?' from the S7.5.5 and EPIC 11 arcs. This test's own comment claims it "must be EDITED before that can change" — only the signature line has that property; the verdict-set half reads as authoritative coverage of the trigger set and provides none. Given the clock finding above (a Recover verdict that IS effectively triggered by a transient condition), this is the check that should have caught the shape and structurally cannot.

**Suggested direction (NOT ruled):** Invert the assertion: enumerate every verdict Decide can produce, and assert that the set whose Action == Recover is exactly {stored_identity_expired}. That fails the moment a new Recover-producing branch appears, whatever its Reason string.

## 52. [LOW] renewLoop discards the ca.pem read error and hands the result to saveCreds, which atomically replaces the trust anchor with an empty file

`apps/node/cmd/agent/main.go:509` · correctness

**Claimed sequence**

```
Claim under test — main.go:419-430 vouches that saveCreds leaves "EITHER the old set or the new set". It says nothing about a caller handing it a silently-empty member of the new set, and renewLoop does exactly that.

main.go:509: `ca, _ := os.ReadFile(filepath.Join(certDir, "ca.pem"))`. On any error — transient EIO on the PVC, an ESTALE handle on NFS-backed storage, the file having been removed by a partially-failed earlier saveCreds — `ca` is nil and the error is dropped.

1. renewLoop ticks at half-life. client.Renew succeeds; the agent now holds a fresh cert in memory.
2. The ca.pem read fails. ca = nil.
3. saveCreds(certDir, newCert, newKey, nil) writes ca.pem.tmp with zero bytes and renames it over ca.pem (main.go:441-448) — atomically, and therefore irreversibly. cert.pem and key.pem are then updated correctly. saveCreds returns nil, so renewLoop logs agent_cert_renewed. Nothing is logged about the CA.
4. The running process is unaffected: its x509.CertPool was built at main.go:152 and lives in memory.
5. The pod is next restarted (node drain, image bump, OOM). loadCreds succeeds — os.ReadFile of an existing empty file returns no error (main.go:412) — Decide sees a valid certificate and returns UseStored.
6. control.NewClient reaches `pool.AppendCertsFromPEM(caPEM)` (client.go:95), which returns false for empty input → "bad CA PEM" → main.go:155 `os.Exit(1)`.
7. Permanent CrashLoopBackOff. Every restart repeats step 5-6, and no code path rewrites ca.pem: attemptRekey is never reached (the cert is valid, so Decide never returns Recover), and renewLoop is never reached. Recovery requires a human wiping the state directory and re-enrolling with a join token — the identity loss the story exists to prevent.
```

**Why it would matter:** A dropped error on a two-token line converts a transient read failure into a permanently unbootable gateway, and the boot-time failure is an os.Exit rather than any of the recovery paths S13.1 built. The atomicity comment above saveCreds makes the write look safe by construction, which is what keeps this line from being read as the hazard it is.

**Suggested direction (NOT ruled):** Do not let saveCreds accept an empty member: reject zero-length cert/key/ca inside saveCreds (it can never be a legitimate credential set) so the caller's dropped error becomes a logged refusal instead of a committed rename. Separately, handle the error at main.go:509 — if ca.pem cannot be read, skip the persist and warn rather than renewing over it. Consider validating the loaded CA at loadCreds so a blanked anchor is a Decide input rather than an os.Exit.
