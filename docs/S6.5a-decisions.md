# S6.5a Packaging (UNSIGNED) — decisions (commit-one, decision-first)

Full review round — **NOT pre-authorized build-through**. This is where the three
S6.3/S6.4-deferred live proofs come due, and scoping decides whether they discharge at
S6.5a or slip. No packaging code until this commit-one is reviewed.

S6.5a ships **installable unsigned artifacts** (`.dmg` + `.exe`) + install docs + the
live proofs the available machines can run. **Signing / notarization / auto-update-ON =
S6.5b** (trigger: public beta OR first outside-circle distribution; Windows EV also waits
on legal-entity formation). No cert work here.

---

## A. The signed-vs-unsigned residue-smoke question — RESOLVED (definitive, per platform)

Carried from the S6.3 merge: *can the packaged install→run→uninstall residue smoke run
against the UNSIGNED S6.5a package, given SMAppService (macOS) / SCM registration
(Windows) may require a signed daemon?* Answer is platform-split:

- **macOS via `SMAppService` → NO (defers to S6.5b).** `SMAppService.register()` validates
  the app bundle's **Developer ID** code signature; an unsigned / ad-hoc-signed bundle
  fails registration. So the SMAppService install/uninstall path — and its residue smoke —
  **cannot be exercised unsigned**. Trigger to run it = S6.5b (Developer ID signing). This
  matches what S6.3 already ledgered (`scripts/macos-dev-install.sh` comment: "the
  production SMAppService installer ships a real Developer ID signature — S6.5b").

- **macOS via the INTERIM manual-LaunchDaemon path → YES (runs at S6.5a).** The S6.3 dev
  lifecycle installs an **ad-hoc-codesigned** helper at
  `/Library/LaunchDaemons/io.tunnex.helper.plist` and loads it with `launchctl bootstrap
  system`. `launchctl` loads an ad-hoc-signed daemon fine — only **SMAppService** demands
  Developer ID. So S6.5a ships this interim lifecycle (behind an in-app GUI privileged
  install; see §D), and its residue smoke runs unsigned. **S6.5b swaps this for
  SMAppService** — same helper, different registration front-end.

- **Windows via SCM service → YES (runs at S6.5a, machine permitting).** Windows SCM does
  **not** enforce Authenticode on **user-mode** service binaries (only kernel-mode drivers
  need KMCS). So `CreateService` → run → `DeleteService` + the residue smoke run **unsigned**.
  SmartScreen only gates the installer's first-launch UX (§C), not service registration or
  execution.

Net: the residue smoke **is** exercisable at S6.5a on both platforms — on macOS against the
interim path (not SMAppService), on Windows against the real SCM service. Only the
**SMAppService-specific** macOS residue smoke defers to S6.5b.

---

## B. Per-proof disposition (the three ledgered proofs — machine, trigger, pass bar)

No "validate at packaging" hand-waving. Each proof states **runs-at-S6.5a-on-which-machine**
or **defers-to-S6.5b-with-reason**, plus the **pass bar**. **Windows box CONFIRMED available
this cycle** (2026-07-10) — so all three Windows-gated proofs run at S6.5a on it (hardware was
the only gate; signing never was).

| Proof | Disposition | Machine | Pass bar |
|---|---|---|---|
| **1. Windows WFP live pcap** (kill-switch never proven live on Windows — S6.3 cross-compiled only) | **Runs at S6.5a** (Windows box confirmed). | Windows 10/11, admin, reachable WG endpoint; pcap captured **inside the guest/host** on its adapter (a NATed VM NIC is fine for the cleartext-leak proof). | `taskkill /F` the helper mid-tunnel → pcap shows **zero cleartext** to non-tunnel destinations while WFP filters persist; then **bounded fail-closed recovery** proven (DisableFirewall on helper restart / dead-man release) → normal networking returns. |
| **2. Packaged residue smoke** (install→run→uninstall, no residue) | **macOS: PASS (2026-07-10)** against the interim manual-LaunchDaemon path (per §A). **SMAppService residue smoke defers to S6.5b** (needs Developer ID). **Windows: runs at S6.5a** against the real SCM service (unsigned services register fine). | macOS: Pawan's Apple-Silicon Mac. Windows: the confirmed box. | **macOS:** after uninstall — no `/Library/LaunchDaemons/io.tunnex.helper.plist`, no helper binary, `launchctl print system/io.tunnex.helper` = not found, no `/var/run/tunnex/*`, pf anchor + `pf.token` gone, `/etc/pf.conf` restored from backup. **Windows:** `sc query tunnex-helper` = not found, wintun adapter removed, WFP provider/sublayer/filters gone, no leftovers under `%ProgramData%`/`%ProgramFiles%`. |
| **3. Tray / notification live display** (Electron never launches in CI) | **macOS: PASS (2026-07-10)**. **Windows: runs at S6.5a** on the confirmed box. | macOS: Pawan's Mac. Windows: the confirmed box. | Tray icon appears in the menu bar / system tray; menu + tooltip track connect / disconnect / **failed (kill-switch)** / **revoked**; native notification fires on each transition; tray click shows the window; Quit exits. Mirrors `trayStateFor` (connecting vs connected) live. |

**Acceptance test (§E) — macOS PASS (2026-07-10):** unsigned universal `.pkg` → install → sign
in → Connect → one GUI admin prompt → Connected + live handshake, zero terminal.

**Windows proof results (live on the box, 2026-07-10):**
- **Install + connect + device-to-device: PASS.** NSIS `.exe` installs, registers the SCM
  service, caller-auth works (after the go-winio pipe-handle fix), split-tunnel connects,
  Mac↔Windows peer traffic verified. Two live-found fixes folded: the go-winio caller-auth
  bug, and `sc sidtype unrestricted` (the WFP arm needs the service's own SID or it fails
  "The specified group does not exist").
- **WFP full-tunnel kill-switch: FAIL → LEDGERED to S6.6.** The pcap showed traffic egress
  DIRECTLY out the physical NIC after `taskkill /F` the helper — the kill-switch did NOT
  hold. Root cause (code-proven): wireguard-windows opens its WFP engine with
  `FWPM_SESSION_FLAG_DYNAMIC` (`blocker.go:34`), so every filter is auto-deleted when the
  process exits. **The Windows kill-switch is NOT fail-closed on process death** — the
  S6.3 "death = enforcement" model holds on macOS (persistent pf anchor, proven) but NOT on
  Windows. **Do not claim Windows fail-closed.**
- **Residue smoke + Windows tray:** NOT run (the box session pivoted to the kill-switch
  finding) → run at the S6.6 box session (which needs the box anyway).

### LEDGER — Windows kill-switch persistence gap (→ S6.6)
Windows full-tunnel is **not yet fail-closed**: a hard-killed helper releases the WFP block
→ traffic leaks. **Trigger to fix: before Windows full-tunnel is offered to real users.**
Fix = a NON-DYNAMIC WFP session (persistent filters) + a fixed provider GUID + an explicit
enumerate-and-delete `DisableFirewall`, with startup `CleanStale`/dead-man/reboot recovery —
its own story **S6.6 (Windows kill-switch persistence)**, decision-first, box-proven, reviewed.
S6.5a ships the PACKAGING; the Windows kill-switch is gated/caveated until S6.6.

---

## C. Gatekeeper / SmartScreen — a NAMED deliverable (goes in `docs/install.md`, not a footnote)

Unsigned means the OS actively blocks first launch. "Packaged" ≠ "installable by the people
you hand it to" unless the bypass is documented. `install.md` must spell out, per platform:

- **macOS (`.dmg`, unsigned + un-notarized):** double-click → Gatekeeper "cannot verify
  developer / may be malware". Documented bypasses, in order of preference:
  1. **`curl`-install avoids quarantine entirely** — files fetched via `curl`/`wget` get **no**
     `com.apple.quarantine` xattr (only browser/Finder downloads do), so a `curl -LO <url>`
     of the `.dmg` opens without the Gatekeeper prompt. Preferred hand-off path.
  2. **Right-click (Control-click) → Open → Open** — the classic per-app override (macOS ≤ 14).
  3. **System Settings → Privacy & Security → "Tunnex was blocked" → Open Anyway** — required
     on **macOS 15 Sequoia**, which removed the Control-click bypass for un-notarized apps.
  4. Manual: `xattr -dr com.apple.quarantine /Applications/Tunnex.app` (documented as the
     escape hatch, not the primary path — it is a terminal step).
- **Windows (`.exe` NSIS installer, unsigned, no EV reputation):** SmartScreen "Windows
  protected your PC" blue dialog → **"More info" → "Run anyway"**. Note that SmartScreen warns
  on every unsigned/low-reputation installer until signing + install volume build reputation
  (that is exactly what S6.5b buys); document it as expected, not a defect.

`install.md` also covers: **verify the download** against `SHA256SUMS` (the integrity
substitute for a signature until S6.5b), and the **helper-install consent** (why the app asks
for an admin password on first connect — it installs the root privilege helper; §D).

---

## C-bis. Spike results (gate before the packaging build) — BOTH PASS → outcome (a)

Two spikes gated the build (the flagged risks). Both passed on Pawan's Apple-Silicon Mac
(2026-07-10).

- **Spike 1 — macOS in-app privileged install: PASS → zero-terminal holds.** An UNSIGNED
  caller (`osascript … 'do shell script … with administrator privileges'`, a proxy for the
  unsigned app) got the native GUI admin prompt and performed the privileged
  `/Library/LaunchDaemons` install: `launchctl bootstrap` LOADED the daemon (`state = running`,
  live pid), then bootout + rm left no residue. `osascript`'s admin-privileges path is
  non-deprecated and OS-mediated — the caller's signature is irrelevant to whether the prompt
  works (unlike SMJobBless / AuthorizationExecuteWithPrivileges, both deprecated, and
  SMAppService, which needs Developer ID → S6.5b). One prompt at install; launchd then runs
  the helper as root persistently (no per-connect prompt). Combined with S6.3 having already
  loaded the ad-hoc-signed REAL helper under launchd live, the zero-terminal install is
  confirmed viable unsigned. Repro: `scripts/spike-macos-privileged-install.sh`.
- **Spike 2 — universal macOS helper: PASS → ship UNIVERSAL.** The helper (CGO/libproc
  caller-auth) builds for both arches: arm64 native, amd64 via `CC="clang -arch x86_64"`
  CGO cross; `lipo`-merged clean. The x86_64 slice RUNS (under Rosetta) reporting
  `caller-auth: native` — the CGO libproc path is functional in the Intel half, not merely
  linked. So the universal `.dmg` is real, not an unverified Intel shell. (Caveat: the Intel
  slice is Rosetta-validated, not real-Intel-hardware validated — noted; acceptable for S6.5a,
  real-Intel verification is a nice-to-have not a blocker.)

## D. Packaging scope

- **electron-builder**, targets: **macOS `dmg`** + **Windows `nsis` `.exe`**. (Auto-update
  channels/`zip`/`latest.yml` stay OFF — that's S6.5b.)
- **macOS arch: `universal`** (electron universal2 + the helper built for arm64 **and** amd64
  then `lipo`-merged) so one `.dmg` runs on Apple Silicon and Intel. *Fallback if the universal
  helper merge proves fiddly:* arm64-only `.dmg` for S6.5a (Pawan's machine + most modern Macs)
  with x64 as a fast follow — decide at build time, record which shipped.
- **Bundled helper:** the Go privilege helper (built **CGO=1** for native caller-auth, per
  arch) ships inside the app resources. macOS: staged for the LaunchDaemon install. Windows:
  staged for the SCM service install + wintun.
- **In-app GUI privileged install (the crux for "zero terminal"):** end users must NOT run
  `macos-dev-install.sh`. S6.5a adds an in-app first-connect **"install helper"** step:
  - **macOS:** a GUI admin-authorization prompt (`osascript … "with administrator privileges"`,
    or a tiny privileged installer) that ad-hoc-codesigns + copies the helper and
    `launchctl bootstrap`s the LaunchDaemon. Zero terminal for the user (they type a password
    in a GUI dialog). The dev scripts stay for development only.
  - **Windows:** the NSIS installer runs elevated (UAC) and registers the SCM service + wintun
    at install time — no per-connect prompt.
- **`SHA256SUMS`** over the shipped artifacts, published alongside them.
- **`docs/install.md`** — the §C bypasses + hash-verify + helper-consent, per platform.

---

## E. Acceptance test (the real bar)

**Nothing-installed machine → install → connect → ping, with ZERO terminal commands.**
- macOS: mount `.dmg` → drag Tunnex to Applications → launch past Gatekeeper (§C) → sign in →
  Connect → the app's GUI admin prompt installs the helper → tunnel up → **ping through the
  tunnel succeeds**. No `sudo`, no script.
- Windows: run `.exe` → past SmartScreen (§C) → UAC-elevated install registers the service +
  wintun → launch → sign in → Connect → **ping through the tunnel succeeds**. No terminal.

The macOS in-app privileged install (§D) is the **primary risk** — it is the piece that makes
"zero terminal" real on an unsigned build. If it can't be made reliable unsigned, that is a
commit-one finding to surface, not a silent slip.

---

## F. Non-goals / deferred to S6.5b (explicit)

- No Developer ID / EV signing, no notarization, no stapling.
- No `SMAppService` registration (needs signing) — S6.5a uses the interim LaunchDaemon path.
- No auto-update channel turned on (scaffold from S6.1 stays dormant).
- SMAppService-specific macOS residue smoke → S6.5b.
- No helper/protocol/kill-switch behavior change — S6.5a is packaging + install UX only.

---

## Windows box — CONFIRMED (2026-07-10)

A Windows 10/11 machine is available this cycle, so Proofs 1, 2-Windows, and 3-Windows run
at S6.5a on it. The checklist keeps all three Windows proofs in scope. (Signing was never the
gate for Windows — hardware was, and it's here.)
