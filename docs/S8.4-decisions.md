# S8.4 — cross-site DNS (commit-one: decision-first paper)

**Status:** PAPER — dispositions PROPOSED, HELD for founder ruling. Nothing built until ruled.
**Branch:** `story/S8.4-dns`. **Sequencing:** S8.2c (MERGED) → **S8.4** → S8.5. EPIC 8 story 5 of 6.
**Acceptance precondition:** the S8.5-coupling clause (below) — a DNS decision blind to S8.5's declared LANs bakes in a redo.

## Re-entry survey (cited, not assumed)

- **The seam this story was promised** — `apps/api/db/migrations/0032_sites.up.sql:25-27`:
  `dns_forwarding jsonb NOT NULL DEFAULT '[]'` — *"RESERVED (S8.4): cross-site DNS forwarding entries (domain -> that site's internal resolver). Empty until S8.4 compiles them; a JSONB array keeps the shape open without a migration."* → **the seam is per-SITE, domain→resolver FORWARDING** (not a CP-hosted per-host name registry).
- **Current device DNS** — `apps/api/internal/devices/config.go:11-14,34-36,58-65`: full-tunnel clients get `DNS = 1.1.1.1` (a public resolver, so name resolution survives once `0.0.0.0/0` captures all traffic); **split-tunnel clients get NO `DNS` line** (keep their own resolver). Today there is ZERO internal-name resolution — DNS exists only to not-break public resolution under full-tunnel.
- **Client/helper DNS path** — `apps/client/src/main/wgconf.ts:51` writes `DNS =` from the device config's `iface.dns`; the privilege helper applies it to the OS (`apps/helper/config.go`, `peer_{linux,windows}.go`). The helper is where OS-level resolver programming already lives.
- **Agent / gateway** — no DNS today. The node image (`deploy/docker/node.Dockerfile:17`) ships `wireguard-tools iproute2 nftables` — **no resolver binary**.
- **Compiled artifact / policyspec** — carries no DNS (`policyspec.Compiled` = Version/NodeID/Mode/Mesh/Allow + out-of-hash Routes/LocalSubnets).
- **Org zone source** — an org can hold a **verified domain** (enterprise SSO domain claim: `apps/api/internal/enterprise/sso/domain.go`). Open orgs have none.
- **Two-part verify prereq (PLAN:1338), ANSWERED:**
  - **(a) enterprise — do granted resource-CIDRs / site subnets reach a DEVICE's AllowedIPs today?** NO. `allowedIPsFor(fullTunnel, poolCIDR)` returns `[poolCIDR]` (split) or `[0.0.0.0/0, ::/0]` (full); the sole caller (`devices/service.go:265`) passes only those. No path adds resource/site CIDRs — a device routes ONLY to the org pool; the pool is kept DISJOINT from site subnets (`devices/service.go:356`).
  - **(b) open — device-reachable gateway-LAN routes today?** MISSING. `Routes[]` are GATEWAY-only (`nodes/service.go:490`, hub-forwards between spokes); devices never receive site-subnet routes.
  - **Consequence:** cross-SITE DNS (a behind-host resolving a REMOTE site's host) rides the existing gateway routes and works NOW; DEVICE→site resolution only becomes useful once **S8.5** routes site subnets to devices. This is exactly why the coupling clause is the acceptance precondition.

## Scope discipline
DNS only. NO new routing behavior, NO S8.5 build-ahead beyond the coupling clause. Carried: WF-1 (positive site-link health → S8.5 L1-metrics) and WF-7 (site-rule editor → UX harvest) deferrals STAND with their triggers; the AWS-side cloud fabric (route-table + src/dst-check + IP-forward) remains **documented-owed** for a *separate* AWS behind-host.

---

## S8.5 COUPLING CLAUSE (acceptance precondition — every decide-item states its S8.5 behavior)

S8.5 pushes **open-edition routed subnets** (declared LANs) into split-tunnel devices' AllowedIPs — a known **sibling** of site subnets. **The clause:** S8.4 models a resolvable internal zone as *"a reachable range with an optional resolver,"* keyed to the **site/range**, never to a device or a node. So an S8.5 declared-LAN plugs into the SAME `dns_forwarding` table and the SAME device-resolver handoff without a redo — S8.4 builds the mechanism generically over ranges-with-resolvers; S8.5 adds more ranges. **A design that special-cases "site" in the DNS path fails this clause.** Concretely: (1) the forwarding table is keyed by domain→resolver-reachable-via-a-range, agnostic to whether the range is a site subnet or an S8.5 LAN; (2) the device-side resolver handoff (D3) is emitted whenever a device has a route to a range that declares a resolver — site today, S8.5 LAN tomorrow, same code.

---

## Decide-items (proposed dispositions, HELD for ruling)

### D1 — Resolution model: FORWARDING, not a name registry (one-truth)
**Proposed:** S8.4 does **zone forwarding**, exactly per the seam — a site declares *"domain `X` is resolved by my internal resolver at `<IP-in-my-subnet>`"* (`dns_forwarding[] = {domain, resolver_ip}`). Tunnex makes those zones resolvable **cross-site over the tunnel**; it does NOT host per-host records. Names live in the org's existing site DNS (the org already runs DNS for `nas.mumbai.acme.com`); S8.4 forwards the query there over the WG link.
- **Shape:** `nas.mumbai.acme.com` resolves because `mumbai.acme.com` is forwarded to mumbai-site's resolver. No synthetic `<host>.<site>.<org-zone>` scheme invented by tunnex — the org owns the zone; tunnex owns the FORWARDING TABLE (which domain → which site's resolver). **One-truth:** the CP owns sites/subnets/nodes and now the forwarding table; the resolver_ip must be VALIDATED to lie inside that site's approved subnet (no second registry, no free-floating IP — the `subnetguard`/site-subnet truth gates it).
- **Split-horizon:** the forwarded zones resolve to internal IPs ONLY inside the tunnel; a device/site keeps its own resolver for everything else. Public names untouched.
- **Cross-site:** rides the tunnel (works today on gateway Routes — see verify (b)).
- **S8.5:** identical — an S8.5 LAN can declare a `dns_forwarding` entry; the mechanism doesn't branch on site-vs-LAN.
- **Open question for ruling:** enterprise verified-domain integration (auto-derive the org-zone from the SSO domain claim) vs purely admin-entered domains. Proposed: **admin-entered domain + resolver_ip in v1** (open + enterprise), verified-domain auto-suggest as an enterprise nicety deferred — keeps v1 edition-symmetric and avoids coupling DNS to SSO.

### D2 — Where resolution lives + the failure mode
**Proposed:** **gateway-as-forwarder** (what the seam implies). The agent runs a tiny DNS forwarder; a query for a remote zone goes device/host → the answering gateway's forwarder → tunnel → the target site's gateway → that site's declared resolver_ip. The CP compiles the org's forwarding table into the desired-state artifact (a new out-of-hash section, D5); the agent programs its forwarder from it.
- **Resolver component:** the node image has none → add a **minimal Go DNS forwarder in the agent** (miekg/dns or stdlib) rather than shelling to dnsmasq/CoreDNS (fewer moving parts, no extra process, testable). Listens on a tunnel-side address (a well-known port on the gateway's overlay IP).
- **FAILURE MODE (four-word model, stated):** **DNS-down ≠ tunnel-down.** The forwarder is a SEPARATE best-effort section — a forwarder crash/miscompile must NEVER fail the reconcile, tear down peers/routes, or imply data-plane failure. **fail-static applies to resolver state:** the agent keeps last-good forwarding config; a bad DNS section is dropped, the rest of the desired state still applies (the DesiredState-atomic law's sibling — but DNS is convenience, so a DNS-only compile error should NOT fail the whole fetch; **decide-item sub-point for ruling:** does a malformed `dns_forwarding` entry fail the whole artifact (atomic) or just skip that entry (degraded)? Proposed: **skip-the-entry, degraded**, because DNS is not an enforcement boundary — reachability-by-IP must survive a DNS typo).
- **Health:** a resolver that can't reach its declared upstream is surfaced advisory (a `dns_forward_unreachable`-style signal, B2 pattern) — never green-blackhole, never a tunnel alarm.
- **S8.5:** the same agent forwarder answers for S8.5 LAN zones; no new resolver path.

### D3 — Client/device integration + the HA-posture sentence
**Proposed:** a device learns the internal resolver via **domain-SCOPED** resolver config, NOT a blanket `DNS =` replace. wg-quick `DNS =` sets the WHOLE system resolver → for split-tunnel that would tunnel ALL queries (wrong). So the **helper** does conditional/scoped resolver programming for the forwarded zones only (macOS `/etc/resolver/<domain>` or SCDynamicStore; Windows NRPT rule per domain; Linux systemd-resolved per-link domain). The CP hands the device the set of `{domain → tunnel-side resolver IP}` for zones it can reach.
- **What changes:** the helper gains domain-scoped resolver programming; the CP device-config emits the scoped-DNS set. **What does NOT change:** full-tunnel still gets its blanket `1.1.1.1`; split-tunnel keeps its own resolver for everything outside the forwarded zones.
- **HA-POSTURE (the sentence the plan owes):** **DNS is keyed to SITES, never to a node.** The resolver a device forwards to is the SITE's forwarder (reached via the site, whichever gateway currently backs it), and the resolver_ip is a SITE-subnet address. So **gateway replacement (S8.1 D6) does NOT orphan names** — the site identity + its `dns_forwarding` persist across a node swap; the new gateway forwards the same zone.
- **S8.5 coupling (bites here):** a split-tunnel device only benefits from a forwarded zone once it has a ROUTE to that zone's resolver — which is exactly S8.5's device-routes. **Proposed:** S8.4 emits the scoped-DNS set for any range the device can route to; today that's the pool only (verify (a)), so device-side S8.4 DNS is INERT until S8.5 lands the routes — S8.4 ships the mechanism + the cross-SITE proof (behind-hosts, which route today), and device→site resolution activates when S8.5 routes arrive. **No S8.5 build-ahead — just the composable handoff.**

### D4 — Edition split (argued)
**Proposed:** the FORWARDING mechanism (D1/D2) is **all-editions core**, like the site model (S8.1 D11). Rationale from the coupling clause: **S8.5's open-edition routed subnets want basic resolution too** — making DNS forwarding enterprise-only would leave S8.5's open LANs nameless, a guaranteed redo. Enterprise ADDS: (proposed, held) governance over DNS — verified-domain auto-derivation, and/or policy over which sites may resolve which zones (a DNS-scoped grant). v1 keeps the split minimal: **core = the forwarding table + agent forwarder + cross-site resolution; enterprise = the verified-domain nicety + any DNS-policy governance, both deferrable past v1.** Argue against: DNS as enterprise-only contradicts the S8.5 coupling and the "sites are core" precedent.

### D5 — Version/hash classification (D2-checklist row, explicit)
**Proposed:** the `dns_forwarding` artifact section is **degraded-not-broken PLUMBING — OUT of CanonicalHash, NO RequiredVersion bump** — the same class as `Routes`/`LocalSubnets` (S8.2/S8.2c). RequiredVersion analysis: an old agent that ignores the DNS section loses ONLY name resolution — never reachability (reach by IP still works), never enforcement (grants gate by IP; DNS resolves a name to an IP the grant still governs — DNS can NOT bypass the enforcement boundary). So it is convenience/observability, not enforcement-significant → `projectForHash` excludes it, twin goldens stay byte-identical, no version bump, a no-DNS org's artifact is unchanged. (Contrast: `Routes` is ALSO out-of-hash but triggered v5 because a route-carrying artifact an old agent can't interpret is a reachability gap it must REFUSE — DNS has no such refuse-worthy gap, so it does not even trigger the version like Routes did. **Confirm at ruling:** DNS out-of-hash AND no version trigger.)

### D6 — Walk shape (defined before code)
**Proposed:** cross-cloud pair (AWS Sydney ↔ Azure West US, the S8.2c topology) resolving each other **by name** from a **genuinely-separate FORWARDED behind-host** (fixture-fidelity topology law — NOT the gateway host, NOT a dummy-in-netns).
- **Leg 1 — cross-site name resolution:** from the Azure behind-host, `dig nas.aws-site-zone` (a name in aws-site's declared `dns_forwarding` zone) resolves via azure-gw → tunnel → aws-gw → aws-site's resolver → an AWS host IP; then reach it by that name. Zero gateway SSH (Zero-Touch Law).
- **Leg 2 — fail-static / DNS-down ≠ tunnel-down:** kill the site resolver (or break the forward) → name resolution fails, BUT reach-by-IP and the tunnel stay UP (the four-word model on the wire — a DNS failure never tears down the data plane).
- **Leg 3 — HA (D6/keyed-to-site):** replace the gateway node bound to a site → the zone still resolves (the forwarding config rode the SITE, not the node).
- **Leg 4 — split-horizon:** a public name still resolves via the host's own resolver (tunnex only captured the declared internal zones).
- **Device→site resolution is a NAMED SUBSTITUTE** (unit-pinned) with trigger = S8.5's device-routes (per verify (a)/(b) — inert until then).

### D7 — Config surface + audit + validation
**Proposed:** per-site `dns_forwarding` managed on the Sites page (an admin adds `{domain, resolver_ip}`), through the audited site path (`site.dns_forwarding_set` / `_removed`), `authorize(PermSiteManage)` FIRST, all-editions core. **Validation (one-truth):** the resolver_ip MUST be inside one of that site's APPROVED subnets (the CP's authoritative subnet truth — no free-floating resolver); a domain must be well-formed. Full-sweep: removing an entry withdraws the forward on the next reconcile (the S8.2c WF-5 pattern).

---

## Proposed slice plan (held; folds after ruling)
- **Slice 1 (agent + CP artifact):** the agent DNS forwarder + the CP compiling `dns_forwarding` into an out-of-hash artifact section (D1/D2/D5). Reds: forward-to-declared-resolver, fail-static on a bad section, no-hash-change.
- **Slice 2 (config surface):** per-site `dns_forwarding` CRUD on the Sites page, audited, resolver-in-subnet validated (D7).
- **Slice 3 (client/device handoff):** the scoped-DNS set in the device config + the helper's domain-scoped resolver programming (D3) — cross-site proven; device→site inert-until-S8.5 (substitute).
- Then story-end review → the cross-cloud **name-resolution walk** (D6), Zero-Touch Law + fixture-fidelity the gates.

## Carried / owed
- WF-1 (→ S8.5 L1-metrics) · WF-7 (site-rule editor → UX harvest) — deferrals STAND.
- AWS-side cloud fabric (route-table + src/dst-check + IP-forward) — documented-owed for a separate AWS behind-host (surfaces in the D6 walk if a separate AWS host is used).
- The EPIC-8-close founder UI walk lands after S8.5 (S8.3 Leg-6 CW-confirm substitute + this story's device-resolution substitute both discharge there).

---

## RULINGS (founder-ruled 2026-07-19) — all 7 CONFIRMED + a pre-build verify finding

### VERIFY-0 (founder-found, pre-build) — dead-gateway-renders-healthy on the site surface: CONFIRMED
**Question:** Pawan stopped BOTH cloud gateway VMs; the Sites page showed no down-state. Does S7.4b report-freshness reach the site card, or only Devices?
**Answer (cited):**
- (a) It does NOT reach the site card. `GatewayRow` (`apps/web/src/pages/Sites.tsx:318-324`) renders name + version + HUB + `g.health` = `policyHealthBadge(n)`. `policyHealthBadge` (`apps/web/src/lib/healthview.ts`) is `if (!node.policy_degraded) return null` — driven ONLY by the `policy_degraded` bool + kind, NEVER by `last_seen`/report-freshness. `GatewayView` (`sitesview.ts:51`) carries NO `last_seen`; `assembleTopology` maps id/name/status/isHub/health/maxPolicyVersion/agentVersion — no liveness. `status` is `active|revoked` (an admin state — a stopped gateway stays `active`). Only **Devices → Gateways** renders `last seen Ns ago` (`relativeAge(last_seen_at)`).
- (b) Threshold: none on the site card — it renders no liveness at all. The health-kind freshness (S7.4b `desync_unknown`, F≈1m) fires ONLY for a node with a policy MISMATCH; a healthy-then-stopped gateway never mismatches (pushed==applied) → the kind stays null forever. So after ANY elapsed time, `/sites` shows nothing for a stopped gateway.
- **Named finding:** dead-gateway-renders-healthy on the site surface (reassuring-green class). **WF-1 UPGRADES** from deferred-metrics-nicety to a legibility defect.
- **DISPOSITION (ruled, now confirmed):** fold the MINIMAL honest form into S8.4 as a **liveness rider** in the UI slice — the site card shows the gateway's **last-seen / offline** state from the EXISTING freshness data (`node.last_seen_at`, already fetched, already rendered on Devices). No new signals — just rendering truth already known. **WF-1's POSITIVE-health / handshake-age display stays DEFERRED to S8.5's metrics rider** as registered. Walk gains a leg: **stop a gateway → the site card says so.**

### D1 — forwarding, not a registry: CONFIRMED
A registry would make the CP a DNS authority (new failure domain / sync / attack surface) when the customer's NAS already has a name in their DNS. Tunnex reaches their resolver across the bridge. **Sub-question ruled:** admin-entered domain in v1; verified-domain auto-derive DEFERRED (enterprise polish, S12-era). Validation that matters: `resolver_ip` inside that site's approved subnet — typed refusal otherwise. **ADDITION (decide-at-build, Slice 1):** **domain overlap across sites** — two sites claiming `corp.local` must be REFUSED (the disjointness law's DNS cousin) or explicitly ordered; propose the answer in Slice 1, do NOT discover it in the walk. (Proposed: refuse a duplicate forwarded-domain within an org — one zone → one resolver; state it, red it.)

### D2 — in-agent Go forwarder, no dnsmasq: CONFIRMED
External daemons are the EPIC-9 pattern; a forwarder is small Go inside the agent's lifecycle. **Sub-question ruled:** SKIP-DEGRADED — a malformed entry skips with a logged warning, the rest serve; atomic-fail would let one typo take down every site's names (inverted blast-radius). **Conditions (paper + reds):**
- **Bind scope:** the forwarder binds ONLY wg-facing addresses (the overlay/site-subnet interface) — NEVER a public interface. An open resolver on a cloud VM is an abuse vector. State the bind scope; RED it (a red proves it does not listen on a non-wg address).
- **Per-source rate limiting** as cheap hygiene.
- **fail-static DNS meaning, pinned exactly:** resolver unreachable → **SERVFAIL**, tunnel untouched, last-good table retained.

### D3 — domain-scoped resolver (macOS /etc/resolver, Windows NRPT), never blanket DNS=: CONFIRMED
**Condition:** this touches the PRIVILEGE HELPER — the most expensively-proven component. **The helper change is its OWN slice** with its own review attention; existing helper guards stay green; **kill-switch / revocation paths shown UNTOUCHED by the DNS additions** (probe PRE-REGISTERED for the story-end review). HA sentence accepted: **DNS keyed to sites, never a node** (gateway replacement / D6 does not orphan names).

### D4 — forwarding is all-editions CORE: CONFIRMED
The coupling forces it; the S8.5 funnel logic seals it. Enterprise verified-domain / DNS-governance DEFERRED past v1 — registered, not built.

### D5 — out-of-hash AND no version trigger: CONFIRMED
**The distinction from Routes, verbatim for the paper:** *Routes bump because an agent that can't program them serves a DEAD bridge while green (a reachability gap it must refuse); a DNS-blind agent serves a WORKING bridge where names don't resolve — visibly degraded, user-diagnosable, nothing silently unsafe.* LocalSubnets precedent cited. The D2-checklist row exercised as the law intends — argued, not assumed. → `dns_forwarding` excluded from `projectForHash`, no `RequiredVersion` trigger, twin goldens byte-identical, a no-DNS org's artifact unchanged.

### D6 — cross-cloud name-resolution walk: CONFIRMED, two legs added
Fixture-fidelity (genuinely separate behind-host), zero gateway SSH. Legs: (1) cross-site name resolution · (2) fail-static: kill resolver → SERVFAIL, tunnel + reach-by-IP survive · (3) HA: node-swap, zone still resolves · (4) split-horizon: public name via the host's own resolver · **(5 ADDED) negative-scope: a domain in NO forwarding table resolves via the host's normal path, untouched — the forwarder's scope honesty proven on the wire** · **(6 ADDED, the rider) stop a gateway → the site card says offline.** Device→site = named SUBSTITUTE (trigger = S8.5 device-routes).

### D7 — per-site dns_forwarding CRUD: CONFIRMED as written
Audited pair (`site.dns_forwarding_set/_removed`), `PermSiteManage` first, resolver-in-subnet validation, full-sweep on remove. **Ride the WF-3 panel's card layout** — DNS config lives where the fabric steps live: one site card, one story.

## RULED slice plan
- **Slice 1 — artifact/table + forwarder core** (D1/D2/D5): CP compiles `dns_forwarding` into an out-of-hash artifact section (+ the domain-overlap refusal, D1-addition); the in-agent Go forwarder (bind-scope RED in from the START, skip-degraded, SERVFAIL fail-static, rate-limit). Reds: forward-to-declared-resolver, bind-only-wg, skip-malformed, no-hash-change, duplicate-domain refusal.
- **Slice 2 — config surface** (D7): per-site `dns_forwarding` CRUD on the Sites card (rides the WF-3 layout), audited, resolver-in-subnet validated, full-sweep.
- **Slice 3 — helper / device handoff** (D3, ITS OWN SLICE): domain-scoped resolver programming in the helper + the client scoped-DNS handoff; kill-switch/revocation-untouched probe pre-registered. Cross-site proven; device→site inert-until-S8.5 (substitute).
  - **PLATFORM DISPOSITION (founder-ruled 2026-07-19): STAGE — macOS `/etc/resolver` in S8.4; Windows NRPT registered as S8.4b, trigger = S8.5's device-routes slice.** Parity proposal OVERRULED with the reasoning locked: NRPT now = the first-ever helper touch on a novel Windows subsystem (WFP proofs don't transfer), inert-by-construction (device→site is S8.5-gated), verified only by unit fakes, real proof deferred to a different story — the exact shape of S8.2's #5 finding (the un-exercisable path is where the defect lives). Staging costs nothing real: the "second review" S8.4b earns IS the better review (a Windows-helper change reviewed where a Windows box resolves a live cross-site name). macOS-first is principled: file-drop simple, S6-trodden, gives S8.4's walk one live client-integration leg (a Mac resolving by name once S8.5's routes exist — or a named substitute until then, same trigger).
  - **BUILT (S8.4 Slice 3):** helper `VerbSetResolvers` (additive at ProtocolVersion 1 — old helper → `unknown_verb` → client fail-STATIC; NO version bump, mirrors the posture precedent) + `ResolverForward{domain,resolver_ip}` payload with envelope validation (`unexpected_resolvers` on any other verb). macOS `reconcileResolvers` — owned-marker (`# tunnex-managed`, foreign files NEVER touched: refuse `resolver_domain_conflict` rather than clobber), full-sweep, atomic temp-then-rename, validate-before-apply (path-traversal + non-IP refused, nothing half-written), inert no-op when nothing owned/desired. Non-darwin stub returns `resolvers_unsupported` (Windows = S8.4b). Client `TunnelController.applyResolvers` — best-effort/fail-static, INERT (zero wire calls when no forwards ever set), installs on up + full-sweep on down. Reds: reconcile scoped-write/idempotent/full-sweep/empty-clears/inert-no-op/foreign-untouched/bad-input; dispatch route + **tunnel-untouched probe** (set_resolvers never changes tunnel state or owner) + smuggle-rejected; client inert (no set_resolvers) + active (install-then-sweep). Helper crosscompiles darwin+windows; client 79 green.
- **UI slice (with Slice 2 or its own):** the **liveness rider** — `last_seen`/offline on the site card from existing `node.last_seen_at` (VERIFY-0).
- Then story-end review (helper gets its own review attention) → the cross-cloud name-resolution walk (D6, 6 legs).

**Build order:** VERIFY-0 answered (above) → slice plan landed (above) → build **Slice 1**.

## Story-end review — 14 verified findings, founder-dispositioned (2026-07-19): 9 FOLD, 1 DEFER

High-effort multi-finder review (`code-review` workflow, 18 agents) over the whole story diff (merge-base `e7e9c3b`..`47fae99`). Probe scorecard: ① kill-switch/revocation-untouched PASS (dispatch never touches tunnel/owner; smuggle-reject enforced by ValidateRequest) · ③ out-of-hash PASS · ④ one-validator UI PASS / server FAIL (F8) · ⑤ comparison-set org-scoped-correct · ⑥ writer/reader seam PASS · ⑦ authorize-before-edition + swallowed-audit + liveness PASS; SERVFAIL-honesty broken by F2. 14 verified (0 refuted), top 10 reported. Fold sequence: F1 first (story-blocker) → honesty pair (F2/F3) → lifecycle pair (F5/F6) → rest.

- **F1 (CRITICAL, `cmd/agent/main.go`) — forwarder dead-on-arrival on a fresh gateway.** `Serve` was launched once at boot before the reconcile loop creates wg0 → `wgBindAddrs` failed, the one-shot goroutine died forever. FOLD: `Serve` is now a **bind-reconcile loop** (one-truth applied to lifecycle) — re-reads the wg interface's addresses each tick, binds when wg0 appears, re-binds on flap, closes on address-gone. Red: bind-after-start + rebind-after-flap (`TestServeBindReconcileLifecycle`).
- **F2 (HIGH, `dnsforward_net.go`) — truncation-as-malformed.** Fixed 1500B UDP read silently clipped larger answers. FOLD: 65535 read buffer (never self-truncate) + upstream-**TC → TCP fallback** for the full record set; TCP-also-fails → SERVFAIL (degraded honestly). Reds: no-self-truncate + TC-follows-to-TCP + TCP-fail-SERVFAIL.
- **F3 (HIGH, `resolver_darwin.go`) — one-truth 6th instance, cross-layer.** Helper `safeResolverDomain` rejected single-label zones (`internal`/`corp`) the CP + agent both accept. FOLD: the helper drops domain-SHAPE validation (path-safety ONLY — separators/traversal/leading-trailing-dot, additive), leaving the CP `NormalizeDomain` as the shape truth mirrored by the agent. Single-label now flows API→artifact→helper. Reds: helper single-label accept + agent single-label compile + API `NormalizeDomain` single-label.
- **F4 (MED, `sites/service.go`) — stale forward after subnet un-approve.** FOLD: `RemoveSubnet` sweeps forwards whose resolver lived in the removed CIDR (full-sweep law's DNS instance), same tx, named in the audit; the confirm names them (present-tense advisory, `forwardsInSubnet` pure helper). Reds: server sweep integration + `forwardsInSubnet` web.
- **F5+F6 (MED, lifecycle pair) — resolver residue on unclean exit.** FOLD as one: (a) helper `reconcileResolvers` is **all-or-nothing** (rolls back this apply's new files on a mid-write failure); (b) client `resolversActive` set on ATTEMPT not success (down always sweeps); (c) helper sweeps owned resolvers on **owner-loss** (`onClose`, crash/force-quit) AND on **startup** (`CleanStaleResolvers`, reboot/uninstall-without-relaunch) — mirroring the kill-switch SelfHeal precedent. Reds: partial-write-rolls-back + foreign-survives + owner-loss-sweeps-nil + client-sweeps-after-failed-set.
- **F7 (MED, `dnsforward_net.go`) — unbounded bucket map.** FOLD: periodic idle-eviction of rate-limit buckets (an idle bucket refills to full → eviction is lossless → sweep beats LRU). Bounds the map to sources-within-idle-TTL; an OOM can't convert DNS-down into tunnel-down. Red: 1000 sources collapse to ~1 after a sweep.
- **F8 (CLEANUP, `sites/dnsforward.go`) — tested≠shipped.** FOLD: `SetDNSForward` now calls the exported `DNSDomainConflict` (the inline copy the test never exercised is gone). One validator, tested = shipped.
- **F10 (CLEANUP, `Sites.tsx`) — unhandled reload rejection.** FOLD: `add`/`remove` `load().catch()` parity with the mount path.
- **F9 (DEFER) — `SetDNSForward` `ListSitesByOrg` full-scan per write.** Correctness-neutral perf; org site-counts at stake are tens, not thousands. Ledgered to the **correctness-neutral perf class** (S7.2 #8/#9/#10 + `siteLinkGraph` N+1 + `peersEqual` sorts), **same trigger**.

**LAW LIFTED:** the **SHARED-TERRITORY OWNERSHIP law** (`docs/laws.md`) — mark / touch-only-own / refuse-on-collision / **AND sweep on every exit** (the F6 addendum is IN the law text, not a footnote). Three prior instances (foreign routes logged, DOCKER-USER comment-marked, resolver files refuse-not-clobber) + the sweep-on-every-exit addendum with the `backend_darwin` SelfHeal/CleanStale precedent.

Gates GREEN post-fold: `generate-check`, `test-editions` (both, incl. F4/F8 integration), `test-node`, `test-helper` (+ darwin-native reconcile reds), `build-editions`, `helper-crosscompile`, web (132), client (80). **NEXT: targeted re-review over the fold diff (9-fold spanning agent lifecycle / net path / helper / CP validation re-earns review) — probes: F1 rebind-on-flap, F5/F6 sweep-never-touches-foreign, F3 single normalizer actually single — then the D6 walk.**

## Targeted re-review of the fold — 7 findings, founder-dispositioned (2026-07-19): 3 PATCH, 1 REDUCE (4-finding cluster), law updated

The 9-fold re-earned a review (`code-review` workflow over `47fae99..aabb04a`); it did NOT come back clean — 7 fold-induced findings. **The budget-rule tripwire fired: 4 of 7 sat in the freshly-built F5/F6 resolver-sweep lifecycle (the S8.2-arc signature — clever at every exit instead of correct at one).** Founder ruling: patch the 3 isolated findings; REDUCE the cluster.

- **R1 (CRIT, `resolver_darwin.go`) — F3 didn't fully land.** The helper's ASCII charset floor still rejected CP-accepted domains (underscore `_msdcs.corp` AD SRV zones, unicode) and — with all-or-nothing — ONE such domain sank the whole set. PATCH: helper validation is now PATH-SAFETY ONLY (separators / traversal / NUL / leading-trailing dot); charset/shape is the CP's one-truth. Reds: underscore+unicode install end-to-end without sinking the set; traversal floor intact.
- **R6 (CONFIRMED, `dnsforward_net.go`) — reconcileBinds treated ANY bindSource error as empty→closed all listeners.** PATCH: ERROR ≠ ABSENCE (the S8.2 F3 -4/-6 ruling at the bind layer) — a transient read glitch KEEPS the current listeners; only a SUCCESSFUL empty read closes. Red: transient error → listeners persist; empty read → close.
- **R7 (cleanup) — gofmt.** Folded.
- **R2/R3/R4/R5 CLUSTER — REDUCE granted, "one sweeper, one authority."** The eager multi-exit sweep is REPLACED by a rider on the kill-switch's OWN release decision (`Supervisor.SetOnRelease`, fired under `s.mu` at the dead-man fire + graceful `Down`) + startup `CleanStaleResolvers` backstop. The eager `onClose` sweep is DELETED.
  - **R2 dissolves** — grace is INHERITED (same branch as the block release), cannot diverge.
  - **R4 dissolves** — the kill-switch's lock is the lock; a fresh `Up` resets the dead-man so no stale sweep hits a reconnected session.
  - **R5 dissolves** — overwrite-rollback dropped; **ACCEPTED WINDOW ledgered:** a failed mid-apply may leave an already-owned domain pointing at its NEW resolver until the next reconcile tick (client re-applies) or a restart (startup CleanStale) heals it. Bounded + honest; chasing atomic overwrite-rollback was the over-clever path backed out of.
  - **R3** — client `resolversActive` reset on `ok:false`/unsupported (no redundant empty sweep every down); a THROW (unknown helper state) still leaves it attempted so a down sweeps once.
  - Reds: wedged-owner grace → resolvers survive the window, swept only when the dead-man fires · fast-reconnect → successor's resolvers untouched · graceful down → swept once · Windows refused set → no redundant down sweep · (helper-side reconcile foreign-survives/partial-rollback reds retained).

**LAW updated** (`docs/laws.md` instance #4): the resolver sweep RIDES the kill-switch release (one lock, one trigger, zero independent timing) — "a sweep that is a decision-maker is the smell; a sweep that rides a decision is the cure." Gates GREEN post-reduce (generate-check, test-editions both, test-node, test-helper + darwin-native, build-editions, crosscompile, web 132, client 80). **NEXT: one targeted pass over the reduced cluster only (a reduction replaced a model — re-earns eyes; probes: the sweep has NO independent lock/timing, the kill-switch paths are byte-equivalent for non-resolver behavior, the two reds). If clean-or-trivial → the D6 walk. A third round of cluster defects = the HALT signal.**

## Round-3 HALT — reduce-by-removal (2026-07-19): the loop terminator

The targeted pass over the REDUCE re-earned eyes and did NOT come back clean — the resolver-sweep cluster spawned a THIRD round (RR2 FS-under-supervisor-lock, RR3 StateDown persistence gap, an unsynchronized `SetOnRelease` callback), plus RR1 (my R6 patch regressed the open-resolver guard) and RR4 (loosened floor). **The budget-rule HALT fired.** Founder ruling: reduce-by-removal, with the accountability that the inert-until-live principle (already ruled this story for Windows NRPT) applies uniformly.

- **RR1 (LIVE, gateway forwarder) — FIXED, the round's real lesson.** The R6 "error ≠ empty" ruling collapsed TWO different errors into one. Correct **trichotomy** (now cited in the code so the next simplifier meets the reasoning): interface-NOT-FOUND → CLOSE (the open-resolver security guard — a departed wg0 address must not keep a live :53 socket); other/TRANSIENT error → KEEP (availability); genuine EMPTY set → CLOSE. `wgBindAddrs` returns a sentinel `errWGIfaceNotFound` for a missing interface vs a plain error for an unreadable-addrs glitch. Red: `TestReconcileBindsTrichotomy` (transient keeps · not-found closes the leak · empty closes) + bind-scope red re-verified.
- **RR2 + RR3 + the SetOnRelease race — REMOVED.** The release-rider is ripped out entirely (no `onRelease` field, no `SetOnRelease`, no `releaseLocked`, no `NewServer` wiring, eager `onClose` sweep already gone). All three dissolve BY CONSTRUCTION: no FS-under-supervisor-lock, no StateDown gap, no unsynchronized callback — because no rider. S8.4 keeps ONLY startup `CleanStaleResolvers` (race-free, S6-precedented, covers the only residue path that could exist — a future downgrade/mixed-version edge). **The Supervisor lock section is back to its pre-rider width; `state.go` kill-switch lifecycle is byte-equivalent for non-resolver behavior.**
- **RR4 — FIXED trivially.** The path-safety floor now rejects control characters (`unicode.IsControl`) in addition to separators/traversal/NUL/leading-trailing-dot. Nothing else — shape/charset stays the CP's one-truth.

**DEFERRAL registered (not silent):** the crash/owner-loss resolver sweep moves to **S8.4b/S8.5** (same trigger as Windows NRPT — the client resolver path going live), with a **NAMED ORDERING PRECONDITION** in the PLAN S8.4b block: the sweep must land BEFORE the first `set_resolvers` install activates — a resolver file must never exist without its teardown present. Until S8.5 the client installs NO resolver files (`dns_forwards` empty), so there is no crash residue to sweep — the honest bounded window.

**LAW** (`docs/laws.md` #4, terminal form + the dormant-machinery addendum): *build lifecycle machinery only for code paths LIVE in the story that builds them; a sweep built for code that isn't live yet is the disease.* Gates GREEN (generate-check, test-node, test-helper + darwin-native, test-editions both, build-editions, crosscompile, web 132, client 80, gofmt clean). **This pass is the LOOP TERMINATOR for S8.4's review arc — clean-or-trivial → the D6 walk (Pawan drives): 6 legs + liveness-rider leg, cross-cloud name resolution from a genuinely-separate behind-host, negative-scope leg, zero gateway SSH.**

## Review arc CLOSED (2026-07-19) — loop terminator clean, FF-fold trivial

The loop-terminator pass came back CLEAN on its real target: zero orphaned rider references, the Supervisor lock section restored to pre-rider width, the kill-switch lifecycle unperturbed — NO fourth cluster round. Two residual trivials on the RR1 trichotomy, FOLDED:
- **FF1 — the classification question's TERMINAL form.** Absence is now determined from a SUCCESSFUL enumeration (`net.Interfaces()` succeeds and wg0 is not among them → `errWGIfaceNotFound` → close), NEVER inferred from a call failure (a failed enumeration is transient → keep). This closes the `net.InterfaceByName`-conflates-transient-with-absent edge for good; the un-regressable landing of error≠absence (S8.2 -4/-6 → R6 → RR1 → FF1). Red: `TestWgBindAddrsAbsenceIsEnumerated`.
- **FF2 — stale `wgBindAddrs` doc comment** rewritten to match the sentinel semantics (was inviting a regression back to the leak).
- Refuted nit folded: dropped the redundant `r == 0` NUL check (subsumed by `unicode.IsControl`).

**S8.4's review arc is CLOSED** (story-end → fold → re-review fold → REDUCE → round-3 HALT reduce-by-removal → loop-terminator). Gates GREEN. **NEXT: the D6 box-walk (Pawan drives), then the S8.4 merge gate.**

### D6 walk deck (Pawan drives; zero gateway SSH is the Zero-Touch bar)
1. **Cross-site name resolution from a genuinely-separate behind-host** — a real host BEHIND a spoke gateway resolves a remote site's internal name over the tunnel (the gateway forwarder path, live in S8.4). Fixture-fidelity: originate from a forwarded host, not an in-gateway interface.
2. **Negative-scope leg** — an out-of-zone name is REFUSED by the forwarder (split-horizon; the host's own resolver handles it), not answered.
3. **Fail-static leg** — kill the site's internal resolver; the forwarder SERVFAILs but the TUNNEL SURVIVES (DNS-down ≠ tunnel-down). Names fail honestly, traffic-by-IP keeps flowing.
4. **Stopped-gateway → card says OFFLINE** (the VERIFY-0 liveness rider) — stop a gateway; its Sites-card renders the last-seen fact + the OFFLINE badge past the threshold; and the inverse (a fresh gateway is not-offline).
5. **Bind-scope leg** — the forwarder answers ONLY on the wg-facing address, never a public interface (no open resolver on the cloud VM).
6. **Config surface leg** — add/remove a DNS forward in the UI; the typed refusals (`dns_domain_conflict`, `dns_resolver_not_in_site_subnet`) render VERBATIM; subnet-removal names the dependent forwards it sweeps.
- **Walk note (from the hub-forwarding verify item):** the behind-host legs must NOTE any manual iptables currently present on the hub (feeds the VERIFY-THEN-FIX item — the hub's local-LAN↔tunnel forwarding gap).
