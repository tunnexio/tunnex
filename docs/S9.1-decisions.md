# S9.1 — OpenVPN server management in the node agent — commit-one (decision-first, HELD for ruling)

**EPIC 9 opener.** The wedge epic: OpenVPN is the migration on-ramp for Pritunl/OpenVPN
shops. This is a **port, not greenfield** — the reference spec is the existing Bolster stack
(`openvpn-auth-oauth2` patterns + `genclient`-style PKI), reimplemented inside the node agent.
Nothing built — this paper states the seams, carries the pre-existing ledgered bindings intact,
and holds the S9.1 decide-items for the founder's ruling before any product code.

## Ledgered bindings (PRE-EXISTING — carried unchanged, NOT re-litigated)

These are already ruled (PLAN "EPIC 9", ZTNA coverage batch item 6, S3.3 cross-cutting rule,
S9.3 protocol-selection lock). They frame S9.1; they are not decide-items.

- **B1 — ZT-coverage guarantee (S9.1 DECIDE-BEFORE-CODE, item 6).** OVPN devices are policy
  subjects in the **SAME `policyspec.Compiled` artifact** as WireGuard devices. Grants are
  **transport-agnostic**; cert-auth alone is NOT enforcement. **S9.1 deliberate-red:** an org in
  `enforcing` posture with zero grants → an authenticated OVPN client's traffic is **DROPPED at
  the gateway forward chain, identically to WireGuard**. A parallel, non-compiled OVPN data path
  is a two-door breach — **rejected in advance**. (The compiler already keys a device subject by
  its assigned `/32` + owner + gateway node — `policy.Device{ID, owner, node, address}`,
  `compiler.go:123` — with zero knowledge of transport. An OVPN device is just another `Device`
  row. B1 is therefore a *placement* obligation, not a new compiler concept: see D-S9.1-4.)
- **B2 — identity-binding + full-sweep revocation parity (S3.3 rule, transport-agnostic).**
  Revoking an OVPN device releases *everything it ever claimed*, exactly as WireGuard's
  `devices.Revoke` does today (`devices/service.go:517`): its **pool address** (freed for reuse),
  its **live telemetry/status** (`DeleteDeviceStatus` — no stale "online"), and its **enforcement
  presence** (`PushOrgNodes` strips the compiled grants org-wide). OVPN **adds one member to the
  sweep: a CRL entry** so the running openvpn server rejects the revoked cert on its next connect.
  Cert-revocation ALONE ≠ the sweep; the address/telemetry/grant strip are equally required.
- **B3 — S9.3 protocol-selection LOCKED (not re-opened here).** OpenVPN is consumed via the
  `.ovpn` export (S9.2) by **standard OpenVPN clients** (OpenVPN Connect / Tunnelblick / mobile).
  The **Tunnex desktop client stays WireGuard-only BY CONSTRUCTION** — native OVPN inside the
  privilege helper is the **rejected tier** (the rejected-call-home class, not parked-with-trigger:
  a second data-plane engine in the most expensively-verified component, 2× proof burden forever,
  for a population whose endgame is the WG client we already ship). S9.1 builds the SERVER; it
  builds NOTHING in `apps/client` or the helper.

## The seams (cited)

- **Node agent data plane** (`apps/node/internal/`): today `egress`, `dnsforward`, `reconcile`,
  `nodepolicy`, `control`, `flowlog`. WireGuard is owned via `wgctrl` in a **desired-state reconcile
  loop**. S9.1 adds a **managed openvpn server process** the agent owns the same way it owns wg0 —
  a reconcile target, not a fire-and-forget spawn (D-S9.1-2).
- **Client-cert PKI.** `apps/api/internal/agentca/ca.go` is the **CP↔agent mTLS CA** — 48h leaves,
  **revocation = refuse renewal, NO CRL** (`ca.go:31`, short-TTL IS the revocation). This model
  does **NOT** transfer to OVPN clients: they hold long-lived certs and reconnect at will, so a
  **real CRL is mandatory** (B2). Whether the OVPN client CA is a NEW trust root or reuses the
  agent CA is D-S9.1-1.
- **Device model + revocation** (`apps/api/internal/devices/service.go`): `Revoke` = the 3-part
  sweep above; the device carries a pool `/32` and an owner. An OVPN device is a device row whose
  credential is a client cert instead of a WG public key (D-S9.1-3 addresses address allocation).
- **Policy compiler** (`apps/api/internal/enterprise/policy/compiler.go`): subjects keyed by `/32`,
  transport-agnostic (B1). **Enterprise-tagged** (`//go:build enterprise`) — enforcement is an
  enterprise capability; the OVPN server + PKI scaffolding is edition-independent (D-S9.1-6).
- **Enroll seam** (`apps/api/internal/nodes/service.go:189` `Enroll`, CSR → `SignCSR`): the shape
  a device-cert issue path mirrors — CSR in, signed leaf out, serial recorded for later CRL.

## Decide-items — RULED (founder, 2026-07-24) with conditions

The load-bearing finding frames all six: **B1 is a placement obligation, not a new compiler
concept** — the ZT-coverage guarantee is achievable by NOT forking anything, and every ruling
below protects that.

- **D-S9.1-1 — CONFIRMED (a): a NEW, separate OVPN client CA.** The CRL finding decides it: the
  agent CA is no-CRL / refuse-renewal, built for short-lived agent enrollment; long-lived OVPN
  client certs need real revocation, and reusing agentca would retrofit CRL semantics onto a CA
  deliberately designed without them. Separate CA also gives trust isolation — **an OVPN client
  cert must never authenticate as an agent.** *Condition (paper obligation):* the **CRL is
  data-plane desired state** like everything else — the CP owns it, the agent RECONCILES it, never
  a side channel; and per B2 the cert revocation + CRL push + address release + status clear are
  **ONE sweep, not four independent actions.**
- **D-S9.1-2 — CONFIRMED: agent-owned reconcile target.** A sidecar would create a second lifecycle
  owner for data-plane state — against the day-one desired-state principle. **Sub-question: YES,
  emphatically — the B1 red is SLICE 1.** Prove the enforcement boundary before building the feature
  on top of it (the S8.6 storage-first / crash-sweep-before-resolver ordering discipline). If B1
  can't be proven early, everything after is built on an assumption.
- **D-S9.1-3 — RULED: ONE shared address world; the existing allocator stays authoritative.** This
  is what makes B1 free: an OVPN client's `/32` must be indistinguishable from a WG device's `/32`
  in the compiled artifact — transport-agnostic **by construction, not by a second code path.**
  *Mechanism to state + verify:* OpenVPN self-allocates by default (`server` / `ifconfig-pool`) —
  that must be **DISABLED** in favor of per-client `ifconfig-push` via `client-config-dir` (CCD), so
  Tunnex's allocator remains the single authority. *Conditions:* the allocator's reserved-collision
  logic covers the **OVPN server's own endpoint address**; and the **disjointness validator gains no
  new class** — an OVPN client address is a pool address, full stop.
- **D-S9.1-4 — CONFIRMED (a): co-terminate on the gateway — WITH the condition that decides whether
  B1 is actually free: the forward chain must adjudicate by ADDRESS, not by INTERFACE.** The S8.6b
  arc is the warning (three rounds of orientation predicates — `iifname != wg0`, `wg0→eth0`,
  `wg0→wg0` — because rules keyed on interface geometry rather than the addresses the policy names).
  *Verify BEFORE Slice 1's red:* does the chain's grant emission carry interface predicates that
  would MISS `tun` traffic? If yes, that is the first thing the B1 slice fixes — and D-transit-1's
  "one rule, orientation-agnostic" relaxation is the shape.
- **D-S9.1-5 — CONFIRMED: port PKI/config, re-derive auth from our identity plane.** No parallel
  oauth2 path — that is **B1's sibling risk in its exact form:** an OVPN client authenticating
  through a path the policy engine doesn't see is the two-door breach, rejected in advance alongside
  the parallel non-compiled data path. The Bolster `openvpn-auth-oauth2` stack is a reference for
  MECHANICS, **never an auth authority.**
- **D-S9.1-6 — CONFIRMED: open-edition server + PKI, enterprise inherits enforcement.** Matches the
  S8.5 pattern (one mechanism, edition-gated depth), keeps `test-editions` clean, and matches the
  positioning: **OpenVPN is connectivity, Zero Trust is the enterprise tier.**

## Reds (proposed acceptance set — refined at build after ruling)

- **B1 deliberate-red (the headline):** org `enforcing`, an OVPN device with zero grants → its
  traffic is **DROPPED at the gateway forward chain**; add one allow grant → it flows — byte-for-byte
  the WireGuard behavior. This is the epic's load-bearing proof (wire, on the S9.x walk).
- **compiler transport-agnosticism:** an OVPN `Device` and a WireGuard `Device` with the same `/32`,
  owner, and node compile to the **identical `AllowEntry` set** (the compiler cannot distinguish them).
- **B2 full-sweep parity:** revoking an OVPN device → pool address freed + status cleared + grants
  stripped org-wide + **CRL entry added**; the running server rejects the revoked cert on reconnect.
  A red asserting all four members fire (cert-revoke ALONE is a deliberate FAIL of this red).
- **managed-process reconcile:** killing the openvpn process → the agent restarts it to desired state
  (the wg0 self-heal analog); a config change regenerates + reloads without a full bounce where
  possible.
- **edition:** the open build compiles + serves OVPN + issues `.ovpn`; the enterprise build ADDS the
  policy-subject enforcement — `make test-editions` green both ways (no bleed).

## D-S9.1-4 pre-verification (done before Slice 1, founder-mandated) — B1 IS FREE ON THE VERDICT

The founder's condition: "verify BEFORE Slice 1's red — does the chain's grant emission carry
interface predicates that would MISS `tun` traffic?" **Verified — it does not.** The enforcing
per-grant accept (`apps/node/internal/egress/egress_linux.go:520`, `allowMatch`) renders:

```
ip saddr <src> ip daddr <dst> <proto/port> counter accept
```

keyed **purely by `ip saddr` / `ip daddr` — NO `iifname`, NO `oifname`.** An OVPN client whose
packet arrives on the openvpn `tun` with source = its pool `/32` is adjudicated by the IDENTICAL
rule a WireGuard device hits. **B1 is free on the enforcement verdict — there is no interface
predicate to fix on the grant.** Masquerade is likewise already source-address-keyed (`ip saddr
<pool> oifname != wg0`, line 329, the review #8/#5 fix) → an OVPN pool `/32` egresses for free.

Interface-keyed machinery that SURROUNDS the verdict but does NOT gate it, registered
Slice-1-adjacent (not enforcement-critical): the **MSS clamp** (`iifname wg0 oifname wg0`, line 349
— cosmetic/MTU; likely wants `tun` coverage) and the **OPEN-build mesh accepts** (line 400,
`iifname wg0` — the `pol.Mesh` path, NOT the enforcing path B1 tests). Consequence for the cut:
Slice 1 is mostly **RED (proof), not FIX** — the boundary is already address-adjudicated, exactly
what the load-bearing finding predicted.

## Mesh mode-gap (D-S9.1-6 consequence) — PROMOTED to Slice 3, verified now

The enforcing path is address-keyed (free). The **mesh path is a SEPARATE render branch**
(`pol == nil || pol.Mesh`, `egress_linux.go:399`) and **IS interface-keyed** — both blanket
accepts are `iifname wg0` (line 400):

```
iifname "wg0" oifname "wg0"   counter accept     # device↔device / spoke↔spoke
iifname "wg0" oifname != "wg0" counter accept    # spoke→egress
```

D-S9.1-6 ships the **open edition** with an OVPN server but NO policy engine → the open edition
runs the **mesh path**. An OVPN client arrives on `tun` (≠ `wg0`) → matches NEITHER blanket accept
→ its spoke↔egress and device↔device traffic is **structurally DROPPED — not a policy denial, a
mode gap.** B1 tests the enforcing path and comes back free; the open edition would ship an
OpenVPN server whose clients reach nothing. **This is the S8.2c #5 lesson exactly** (mesh emitting
no LAN→tunnel rule while enforcing+grant did — a mode gap found on iron).

**Ruling: the mesh relaxation is SLICE 3's scope** (or its own micro-slice), shape = D-transit-1
("one rule, orientation-agnostic, keyed on what the policy names" — here, source-address / pool
membership, as the masquerade already does, not interface geometry). **Reds:** (a) open-edition
mesh + OVPN client → forwarded · (b) enterprise enforcing + grant → forwarded · (c) enterprise
enforcing + no grant → DROPPED (B1). **MSS clamp stays genuinely peripheral** (cosmetic/MTU) —
REGISTERED with trigger = first OVPN MTU complaint OR the box-walk showing fragmentation.

## Slice 1 — SUBSTITUTES ≠ SATISFIES (the wire proof is OWED, not discounted)

A live-client B1 red cannot precede a live server (honest sequencing). So Slice 1's code reds are a
**named SUBSTITUTE; the wire proof is OWED with a NAMED trigger.**

- **Red (i) — compiler transport-agnosticism:** SATISFIED at unit (pure data equality — the
  compiler `Device{ID,owner,node,address}` has no transport field; the AllowEntry is keyed by the
  `/32`). Slice 1 LOCKS this as a regression guard so a future OVPN slice can't add a transport
  branch.
- **Red (ii) — chain adjudicates by address:** **RENDER-SATISFIED at unit only.** The test harness
  stubs `m.apply` (no nft/netns in CI — `egress_policy_test.go:24`), so the unit red asserts the
  rendered enforcing ruleset SHAPE (grant line = `ip saddr … ip daddr … accept`, no `iifname`; an
  ungranted pool `/32` renders to the drop tail only). It does **NOT** construct a real `tun`
  ingress or prove kernel packet fate — the harness cannot inject at the chain's real netfilter
  entry. **Per the founder's condition, this is stated plainly and the walk carries the packet-fate
  weight.**
- **OWED to the wire (trigger = the S9.1 box-walk, after Slices 2–3):** a real OpenVPN Connect
  client, org `enforcing`, zero grants → traffic **DROPPED byte-for-byte like WireGuard**; add one
  grant → flows. This DISCHARGES the substitute explicitly (recorded here so it can't discharge
  quietly). Plus the mesh reds (a)/(b) on the wire in the open edition.

## Slice cut (proposed — B1 red first; founder's ordering, one sharpening)

Agreed with the founder's ordering; one honesty sharpening on Slice 1's form (below).

1. **Slice 1 — the B1 enforcement-boundary proof, IN CODE.** Two reds, neither needing a running
   openvpn server: (i) **compiler transport-agnosticism** — an OVPN `Device` and a WG `Device` with
   the same `/32`/owner/node compile to the byte-identical `AllowEntry` set; (ii) **forward chain
   adjudicates by address** — a synthetic pool `/32` "as if from `tun`", enforcing + zero grants →
   the rendered nft ruleset DROPS it (no `iifname` exemption exists — verified above); add one grant
   → accept. **Sharpening:** the *live-client* wire form of B1 (a real OpenVPN Connect client,
   enforcing, zero grants, DROPPED byte-for-byte) **cannot precede the server that makes a client
   connectable** — it necessarily rides the box-walk after Slices 2–3. Slice 1 proves the boundary
   **in code**; the walk proves it **on the wire.** This keeps the founder's discipline exactly —
   prove the boundary before building on it — while being honest a live-client red can't come before
   a live server. (Open items for Slice 1's neighborhood: MSS-clamp `tun` coverage; leave the
   open-build mesh path as-is — B1 is an enforcing-path guarantee.)
2. **Slice 2 — the OVPN client CA + PKI + issuance** (D-S9.1-1): separate CA (own storage/sealing
   mirroring `agentca.LoadOrCreate`), CSR→leaf issue, serial recorded for the CRL.
3. **Slice 3 — agent-owned openvpn server lifecycle + CCD/`ifconfig-push` allocation + MESH
   RELAXATION** (D-S9.1-2 / D-S9.1-3 / mesh mode-gap): managed reconcile target; `server`/
   `ifconfig-pool` DISABLED, per-client `ifconfig-push` via `client-config-dir` so Tunnex's
   allocator stays the single authority; co-terminate on the gateway (D-S9.1-4) so `tun` packets
   hit the Slice-1 forward chain; **relax the mesh path off `iifname wg0`** (D-transit-1 shape,
   source-address keyed) so open-edition OVPN clients forward — reds (a)/(b)/(c) above.
4. **Slice 4 — `.ovpn` profile export + the download ceremony** (feeds S9.2; per-OS import + QR).
5. **Slice 5 — revocation full-sweep + CRL reconcile** (B2): cert-revoke + CRL push (data-plane
   desired state, agent reconciles) + address release + status clear, as ONE sweep — parity with
   `devices.Revoke`, extended by the CRL member.

## Slice 2 decide-items — RULED (founder-delegated with leans + cited precedents, 2026-07-24)

Founder GO on Slice 2 with two conditions to "decide and state" before the build. Decided here,
aligned to the cited precedents; surfaced in the Slice-2 gate for ratification.

- **D-S9.2-1 — key custody: SERVER-SIDE keygen, ephemeral, ONE-TIME delivery (S3.4/D2 ceremony).**
  Standard OpenVPN clients (OpenVPN Connect / Tunnelblick, per B3) import a single `.ovpn` with the
  private key inline — the one-click download B3 requires. So the SERVER generates the client
  keypair, signs the leaf, and returns (cert, key) to the caller. **The private key is NEVER
  persisted** — generated in memory, streamed into the `.ovpn` exactly ONCE (Slice 4, bound by the
  S3.4/D2 one-time / never-re-fetchable ceremony + client `safeStorage`), then discarded. Only the
  **cert + serial + device binding + not_after** are stored (what Slice 5's CRL sweep needs — B2).
  *Residual, stated honestly:* the server holds the key in memory for the generate→deliver window;
  a **client-side-CSR path (key never leaves the client)** is the higher-assurance tier — REGISTERED
  with trigger = a customer who mandates keys-never-server-side (it trades the one-click download for
  client tooling). This mirrors S3.4/D2 exactly: a served-once secret the server does not retain.
- **D-S9.2-2 — cert lifetime + renewal: 365-day leaf, NO in-band renewal, re-issue via new profile.**
  Long-lived certs are precisely what forced the separate CA (D-S9.1-1) + the CRL. Lifetime =
  `ClientCertTTL = 365d` (a named constant, tunable). **At expiry the client re-downloads a fresh
  `.ovpn`** (new keypair + new cert) — there is NO silent auto-renewal (a long-lived cert that
  self-renews would erode the revocation story; expiry must be a real event that re-runs the identity
  check). Pre-expiry revocation = the CRL (Slice 5). Contrast the agent CA (48h, refuse-renewal,
  no-CRL) — the OVPN client lifecycle is deliberately the opposite: long-lived + CRL-revoked.

## Slice 3→4 coupling — NAMED (mechanism complete, wiring deferred by precondition)

The `ovpnserver.Manager` (Slice 3 part 4) is COMPLETE + unit-proven but deliberately UNWIRED to a
live process. This is a named coupling, NOT an oversight: wiring a live openvpn process before its
preconditions exist would spawn a **crash-looping** process on enabled gateways (openvpn exits on a
missing `ca.crt`) — worse than dormant machinery, because it fails loudly on customer hardware.

**The live wiring's preconditions, in the order they must hold (Slice 4):**
1. **CA + server certs present** — the OVPN client CA (Slice 2 `ovpnca`) + a SERVER cert (server-auth
   leaf, Slice 4a) placed at the gateway's `<cfgDir>/ca.crt` / `server.crt` / `server.key`.
2. **Roster populated** — the CP knows which OVPN devices are homed to this gateway (Slice 4c), so
   `SetDesired` carries a real client list (else `ccd-exclusive` refuses everyone).
3. **`Reconcile` called from the reconcile loop** — a sibling loop (like `egressLoop`) on the tick.
4. **exec supervisor** — `ensureProc` wired to a real supervisor that spawns/self-heals openvpn.
5. **`egress.SetOVPNTun(TunName)`** — threads the one-truth, ONLY once the server is actually up.
6. **`OnPolicy → SetDesired`** — carries pool + roster each fetch.

**Honest failure mode (Slice 4, ruled):** a gateway with OVPN ENABLED but certs ABSENT must **refuse
loudly** — a typed health kind / refusal surfaced to the CP — NEVER a crash-loop. Enabled-without-
material is a real misconfiguration state, made first-class, not a silent restart storm.

## Slice 4 decide-items / terms (RULED, founder, 2026-07-24)

- **Export ceremony inherits S3.4/D2 exactly:** the `.ovpn` (carrying the client private key, D-S9.2-1)
  is served ONCE, never re-fetchable — the same one-time-secret UX as the WG config + join tokens
  (explicit "I've saved this" gate, no route back, KEYED FINGERPRINT in logs/audit, never the material).
- **Live wiring in precondition order** (the 6 steps above); enabled-without-certs = refuse loudly.
- **Indistinguishable-`/32` graduates unit→wiring:** the roster's CCD entries carry the CP-assigned
  addresses; the compiled artifact still cannot tell an OVPN client from a WG device (B1's data half,
  now on the real path).
- **Box-walk:** a real OpenVPN Connect client, enforcing + zero grants → DROPPED byte-for-byte like
  WireGuard, then grant → flows — discharges Slice 1's named SUBSTITUTE (the epic's thesis on the wire).

## D-S9.4-MODEL — the create fork (RULED, founder, 2026-07-24)

**Model boundary (state it so the next transport knows where its fork belongs):** *transport
determines HOW a device is reachable, never WHETHER it is governed.* The shared `devices.Service.Create`
path owns everything address-and-identity — cap check, pool allocation, device row, audit, revocation
(the D-S9.4-MODEL payoff: an OVPN device inherits all of it with no new code). The transport-specific
part is ONLY the credential + the peer materialization:

| | credential | peer materialization |
|---|---|---|
| **wireguard** | mint a WG keypair (or accept a client pubkey) | push a WG peer to the gateway |
| **openvpn** | mint a client cert (`ovpn.Service.Issue`) | materialize a CCD entry (the roster → `ovpnserver`, Slice 4c) |

**Condition (one seam, not scattered branches):** the fork lives at a SINGLE seam in Create — the
credential+materialization step. If implementing it needs transport checks at three points, the seam
is wrong — SURFACE it, do not sprinkle conditionals through Create. Everything before the seam (cap,
pool, row) and after (audit, the return) is transport-agnostic.

**The fork red (4b-wiring):** an OVPN-transport create → **no WG keypair generated, no WG peer pushed**
to the gateway, BUT the **pool address allocated** and the **`/32` present in the compiled artifact
exactly as a WG device's** (B1's data half AT THE CREATE PATH — not merely in the roster). This proves
the shared path still governs the OVPN device identically while the WG-specific materialization is
skipped.

## D-S9.5-OPTIN — OpenVPN is unlock-then-opt-in (RULED, founder, 2026-07-24; binds 4d)

OpenVPN honors the **unlock-then-opt-in** convention (like `zero_trust_mode` / `device_approval` /
flow logs): **OFF by default at the org level** (owner opt-in), and **enabled per-gateway** within an
opted-in org — not every gateway terminates OVPN. The migration story is "turn it on where the
`.ovpn` fleet is." (Open-edition provides the *capability*, D-S9.1-6; the org *opts in* to use it —
capability ≠ enforcement, the convention's whole point.)

**Conditions:**
- **(a) OFF means NOTHING exists.** No CA generated (the deferred `ovpnca.LoadOrCreate` already honors
  this — it's lazy/unwired), no server config, no roster, no OVPN device type offered in the UI, ZERO
  rendered rules. **The zero-config golden extends:** an OVPN-disabled deployment is byte-identical to
  today (already proven at the egress tier — `ovpnTun=""` → `tunnelIfaces()={wg0}` → bare rules; and
  `ovpnserver.Reconcile` is a no-op with no pool).
- **(b) The `openvpn` BINARY stays UNCONDITIONALLY in the agent image** — do NOT make its presence
  conditional on the feature flag. That is the conntrack-tools packaging trap: a feature guarded by an
  unstated runtime dependency that breaks silently on a base-image change. The binary ships always; the
  FEATURE is what the org toggles.
- **(c) Refuse-loudly has TWO siblings (4d):** enabled-but-**certs**-absent refuses loudly (already
  ruled), AND enabled-but-**binary**-absent refuses loudly too — both are typed health kinds surfaced
  to the CP, never a crash-loop. (Given (b), binary-absent should never happen in a real image — but a
  broken/custom image is a real misconfiguration state, made first-class.)
- **(d) Flipping OFF is a FULL SWEEP:** the server is stopped, the tun removed (its interface LEAVES
  `tunnelIfaces()` → egress re-renders without it, the Slice-3 sweep-on-departed-tun path), and the CCD
  is swept. **Issued client certs SURVIVE a disable — disable is NOT revocation.** Re-enable restores
  service with the same certs (the clients' `.ovpn` profiles keep working). A client cert only dies by
  an explicit revoke (Slice 5). Rationale: disable is an operational toggle (turn the door off), not a
  trust decision (revoke the key); conflating them would silently invalidate a fleet on a config flip.

**Reds (for the 4d build):** org-OFF → zero OVPN artifacts anywhere (no CA row, no server, no roster,
no rendered rules) · gateway-OFF within an ON org → no server on that gateway, others unaffected ·
disable→enable round-trip clean (sweep on off, restore on on, issued certs intact across the cycle).

## Founder-walked findings (registered 2026-07-24; Parts 2/3/4 held for ruling)

**PART 2 — static-config WG clients can't reach site subnets — RULED (founder, 2026-07-24).**
- **Enrichment DERIVES FROM THE EXPORT PATH, no `external_client` flag.** A profile leaving the system
  as a file/QR is BY DEFINITION consumed by a non-polling client, so the enrichment (approved ranges +
  reachable DNS baked into `AllowedIPs`/`DNS`) attaches to the EXPORT PATH — not a device attribute
  someone must keep correct.
- **RECORD what was baked, for legibility (the ruling's important half).** When a range is added later,
  polling clients get it in ~30s and static-config clients NEVER do, silently. So the export records
  **provisioning mode + the ranges snapshot at export time**, and the UI answers honestly: "3 devices
  were provisioned from static profiles and don't include the new subnet — re-export." The
  never-silently-broken law applied to a device's routing truth.
- **Full-tunnel export REFUSED as the answer, offered as a choice.** It reroutes ALL traffic, needs
  egress capability (`gateway_no_egress` exists for a reason), and changes the user's whole internet
  path — a deployment decision, not a fix for "reach site subnets." Keep the existing full-tunnel
  option where it is; do NOT present it as the mobile answer.
- **Honesty line RULED IN:** the export ceremony states plainly "This profile includes your current
  site routes and DNS. Add a subnet later? Re-export and re-import on the device." Stated at issuance,
  not discovered in the field (the one-time-secret discipline).
- **Reds:** exported profile carries the approved ranges + reachable DNS · a range added AFTER export
  does NOT appear in the old profile (limitation proven) · the stale-profile surface renders after a
  range change · zero-ranges org → profile identical to today.
- *Lands WITH the WEB tier (one coherent ceremony: modal + download + QR + honesty line).*

**PART 2 (superseded — original HELD statement kept for the trail):** The mint-path
ruling (A) keeps routed ranges OUT of the baked config by design — Tunnex's own client learns them
from the routed-ranges poll. An **official WireGuard app has no poller**, so its `AllowedIPs` is
pool-only (`10.99.0.0/24`) and site traffic egresses the phone's normal internet path. Founder proved
it live (Android WG app traceroute showed carrier/CGNAT hops, not the tunnel; hand-adding
`10.0.0.0/16, 172.31.0.0/16` fixed it). The **DNS field is empty for the same reason** (cross-site DNS
forwards ride the poll too). This contradicts the plan's "mobile via the official WireGuard apps"
positioning. **Proposed fix (held):** distinguish POLLING clients from STATIC-config clients — for
QR/exported WG configs, BAKE the org's currently-approved ranges + reachable DNS forwards into the
profile at export time; ruling (A) stays where it applies (Tunnex clients keep polling).
**Decide-items to argue:** (i) a device flag `external_client` vs DERIVING it from the export path
itself · (ii) whether full-tunnel export is the simpler mobile answer · (iii) the honesty question —
the export UI should say plainly "this profile includes your current site routes — re-export after
adding a subnet" rather than baking silently (the one-time-secret discipline: a stale profile's
failure is otherwise invisible). *This is a WireGuard-side finding; the OVPN answer is Part 3.*

**PART 3 — OVPN server config does NOT push routes/DNS (VERIFIED; fold into 4c/4d).** Confirmed
read-only: `ovpnserver.serverConfig()` emits no `push "route"` / `push "dhcp-option DNS"`, and the
`Desired{PoolCIDR, Clients}` channel carries no ranges/DNS — config-gen doesn't know the approved
subnets (the third outcome, the plumbing finding). Unlike WireGuard, **OpenVPN pushes routes
DYNAMICALLY server-side**, so the fix is server-side (`push "route <range>"` +
`push "dhcp-option DNS <resolver>"`), NOT baked-at-export. **FOLD:** the 4c roster channel gains
`Routes` + `DNS`, and `serverConfig` emits the pushes — done while 4c is open (cheaper than at the
box-walk). No decide-item; this is a fold.

**PART 4 — QR provisioning — RULED (founder, 2026-07-24).**
- **WG QR in the web ceremony — BUILD, with the ENRICHED config.** `qrcode.react` is already a dep. The
  QR encodes the SAME enriched profile the download produces — one truth, two renderings. It carries
  the private key → inherits the one-time-secret discipline EXACTLY (displayed once, never
  re-displayable, behind the "I've saved this" gate, no route back, fingerprint-only audit). **Red: no
  path re-renders the QR after the modal closes** (a re-viewable QR breaks D2).
- **QR in the desktop client — REGISTER as its own story.** Rendering a QR to provision a phone FROM a
  laptop is client-INITIATED creation of a second device — a new capability, not a rendering change.
  Own paper, own ruling.
- **OVPN QR — NOT APPLICABLE (recorded).** OpenVPN Connect has no native QR import; `.ovpn` stays
  file-based. Never promise QR for OpenVPN.

**PART 4 (superseded — original registration kept for the trail):** `qrcode.react@4.2.0` is a web
dependency but has ZERO usage in code (verified) — the S3.3/S3.4 plan anticipated it; never built.
**Register:** (a) **WireGuard QR in the web device-create ceremony** — official WG apps import QR
natively; the QR carries the private key → it inherits the one-time-secret discipline EXACTLY
(displayed once, never re-displayable, "I've saved this" gate, fingerprint-only audit; a re-viewable
QR breaks D2). (b) **QR in the Tunnex desktop client** — render a QR for a newly created profile so a
phone provisions from a laptop; scope in the CLIENT's own story (client enrollment ≠ exported static
profile). (c) **HONEST CAVEAT for `.ovpn`:** OpenVPN Connect has NO native QR import (takes `.ovpn`
files / profile URLs) — QR is a **WireGuard-side feature; do not promise it for OpenVPN**. (d) **QR ×
Part 2 interact:** if Part 2 rules bake-at-export, the QR must encode the SAME enriched config — one
truth, two renderings (QR + file), never divergent.

## Sequence

This paper (HELD) → founder rules D-S9.1-1..6 → dispositions folded back into this paper → build in
reviewable slices on `story/S9.1-openvpn` (proposed cut: PKI/CA + address allocation → managed
openvpn process + config-gen → co-termination + the B1 deliberate-red as its own slice) → gates
(both editions) → targeted review → box-walk (the B1 wire proof rides it) → merge on the founder's
literal in-session word. S9.2 (`.ovpn` export + per-user certs + CRL revocation) and S9.3 (protocol
selection, B3-locked) follow.

## D-S9.6-CERT-DELIVERY — OVPN server certs are desired state (RULED, founder, 2026-07-24)

An enabled gateway that refuses forever is not a shippable state — the guards are correct, but they
were guarding a hole in OUR OWN delivery path, not operator misconfiguration. A box-walk that passes
only after a human scp's certs is the Zero-Touch Gateway Law violated (the demo-era blanket iptables
in a new costume). So the CP DELIVERS the gateway's OVPN server cert as desired state.

- **Delivered on the EXISTING channel, no side channel:** the CA + server cert + server key ride the
  same desired-state fetch as policy and pool assignments. The agent writes `ca.crt` / `server.crt` /
  `server.key` at `cfgDir` as part of reconcile, and RE-ASSERTS them idempotently (a hand-deleted
  `server.key` heals on the next tick, like wg0's rules).
- **SECURITY POSTURE (written down before a reviewer asks):** the server PRIVATE KEY crosses the wire.
  This introduces NO new trust assumption — the control channel is ALREADY mTLS with a pinned cert
  (S3.1 / agent-CA), the same channel that already carries policy + pool assignments. Conditions: the
  key is NEVER logged, NEVER in audit metadata (fingerprint only, per convention), and written 0600.
  NOTE the distinction from CLIENT keys: a client key is EPHEMERAL (one-time, never stored — D-S9.2-1);
  a SERVER key is STORED SEALED (under the master key, like the agent-CA key) so redelivery is
  idempotent, not a fresh mint every tick.
- **Mint-once, re-deliver-idempotently:** the server cert is minted per gateway (IssueServer,
  server-auth leaf) and recorded (serial, expiry, cert, sealed key) — one row per node. Redelivery
  returns the SAME recorded material. **Renewal:** 365d like clients; re-mint on expiry via the
  ordinary reconcile (if that proves harder than it reads, it is DEFERRED with a trigger = first
  server-cert expiry in production, not hand-waved).
- **Disable sweeps DISK, not the DB record:** OVPN OFF → the agent stops the server, removes the tun,
  AND removes the three cert files from disk (disable means "nothing exists"). The DB record SURVIVES;
  re-enable re-delivers the SAME serial from the recorded mint. This is DISTINCT from the client-cert
  DB semantics (issued client certs survive a disable in the DB — D-S9.5-OPTIN d — because a client
  cert is a user's credential; a server cert is gateway material that follows the enable state).

**Reds:** enabled gateway → certs delivered → server spawns → health CLEARS live (refuse-loudly kind
resolving on its own) · a hand-deleted cert file healed on the next reconcile · disable → swept,
re-enable → re-delivered SAME serial · an OVPN-off gateway never receives cert material at all.

## WF-OVPN-1 + WF-OVPN-2 — server-mode completeness + the iroute topology (RULED, founder, 2026-07-24; rc1-walk finding)

The rc1 box-walk PROVED the entire control path zero-touch (opt-in from the UI, cert auto-delivery
with the 0600 key, refuse-loudly cleared, process spawned) — and then found the data plane didn't come
up: the openvpn process ran but `tunnex-ovpn` never existed and a client's connect never completed.
Two coupled defects, dispositioned as ONE fix.

- **WF-OVPN-1 — the config was not a runnable server.** `serverConfig` emitted `dev`/`topology`/`ccd`/
  certs/`push route` but OMITTED `mode server`, `tls-server`, `proto`, `port`, `ifconfig`, `dh`,
  `keepalive` — so openvpn started in point-to-point mode and never served.
  **Reds-gap lesson (written into the paper, founder-directed):** the `ovpnserver` unit reds asserted
  what the config OMITS (`ifconfig-pool`) and that SOME keys are PRESENT (`dev`, `ccd-exclusive`) — and
  proved neither runnability nor completeness. *A generated config for an external process needs a red
  that the external process ACCEPTS it.* Fixed: `TestServerConfigAcceptedByOpenVPN` runs the real
  openvpn binary against the rendered config (data-plane assembly reached, no options error); `make
  test-node` now installs openvpn so it RUNS in CI, not just on the walk.

- **WF-OVPN-2 — the OVPN server tun cannot share wg0's `/24`.** Two L3 interfaces both claiming the pool
  `/24` gives the kernel two routes to the pool → ambiguous. **Topology RULED: (a) iroute** — the only
  option that preserves D-S9.1-3's ACTUAL meaning ("one shared address world" = an OVPN client's `/32`
  is indistinguishable from a WG device's IN THE COMPILED ARTIFACT, which is what makes B1 free and
  keeps grants transport-agnostic). (b) a separate pool would fork the allocator, the disjointness
  validator, and every "is this a device address" assumption; (c) bridging touches the proven WG path
  to solve an OVPN problem (wrong blast radius). (a) keeps the invariant and pays for it with a small
  transit range — the honest cost.

  **How (a) is built:**
  - `tunnex-ovpn` `ifconfig`s the TRANSIT `/30` (`ovpnserver.TransitCIDR = 100.127.255.0/30`) — so it
    installs a connected route for ONLY the transit range. **wg0 stays the sole route for the pool
    `/24`.** No `route <pool>` is ever emitted here.
  - Each client's CCD carries `ifconfig-push <pool-/32> <pool-mask>` + `iroute <pool-/32>/32`. The
    client's tun IP IS its CP-assigned pool `/32` (the indistinguishable-`/32`), so it SOURCES from it;
    the `iroute` is the server-side demux that makes an address outside the tun subnet routable to that
    client.
  - A `learn-address` hook adds a per-client kernel `/32` route to the tun on connect and removes it on
    disconnect — so a connected client's `/32` wins over wg0's `/24` by LONGEST-PREFIX, only while
    connected. Exactly one route to the pool at all times (wg0's `/24`); the `/32`s are transient
    overrides for live clients. The hook validates its address arg as IPv4 before it touches `ip route`
    (defense-in-depth; the arg is the roster `/32`, not attacker input).

  **Transit-range disjointness (condition 1) — EXEMPT, with the reason recorded:** `TransitCIDR` is
  RFC 6598 shared address space (`100.64.0.0/10`, carrier-grade-NAT space, never a customer-routable
  LAN) and is NEVER advertised — not a policy subject, not in any AllowedIPs, not pushed. It is purely
  the local tun endpoint, so it is EXEMPT from the cross-org/cross-gateway disjointness validator (two
  gateways sharing it is harmless — neither sees the other's tun). *But the tun address is not
  "validated by nobody":* the agent GUARDS it LOCALLY every reconcile (`transitConflicts` → refuse with
  `ovpn_transit_conflict` on the health surface if the pool or a pushed route overlaps it on THIS
  gateway).

**Reds (the more important half of the fix):**
- `TestServerConfigAcceptedByOpenVPN` — the real openvpn binary accepts the rendered config (the
  completeness red; runs in CI via `test-node`, carried by the walk where the binary is absent).
- `TestServerModeComplete` — mode server / tls-server / proto / port / ifconfig-transit / dh / keepalive
  / learn-address all present.
- `TestTunnelNeverClaimsPool` (condition 3) — the tun ifconfig is the TRANSIT address; NO bare `route`
  directive and NO `ifconfig 10.99…`, so wg0 keeps the only pool `/24` route.
- `TestCCDPushesCPAssignedAddress` (condition 2, now on the real path) — the CCD pushes the CP-assigned
  pool `/32` AND iroutes it `/32`; the pushed address is the indistinguishable-`/32`.
- `TestTransitConflictRefuses` (condition 1 local guard) — a pool overlapping the transit range refuses
  to serve and surfaces `ovpn_transit_conflict`.

**Walk-carried (the runtime routing proof rides rc2's resumed 4e walk):** client connects; `tunnex-ovpn`
comes up; `ip route` shows the pool `/24` on wg0 with transient `/32`s on `tunnex-ovpn` for connected
clients; B1 (enforcing + zero-grant → DROPPED, grant → flows) on the real OVPN path; Part-3 pushed
routes/DNS reach a site.

## WF-OVPN-3 — OpenVPN full-tunnel parity (RULED, founder, 2026-07-24; 4e-walk finding)

The rc2 walk (server working) surfaced a device-create surface asymmetry: the WireGuard form has a Full
tunnel checkbox; OpenVPN had none. OVPN's default is split BY CONSTRUCTION (no `redirect-gateway`;
routes come from the Part-3 pushes) — the correct default, but the CHOICE was missing. Transport
asymmetry in the create surface is a parity gap, not a nice-to-have — sequenced AFTER WF-OVPN-1/2 (the
server must work before its tunnel mode is a choice) but BEFORE EPIC 9 closes.

**Built, mirroring WG's `full_tunnel` flag (per-DEVICE, never server-wide):**
- **Per-client CCD** `push "redirect-gateway def1 bypass-dhcp"` — the OVPN twin of WG's full-tunnel, keyed
  by the device's `full_tunnel` column (already persisted since 0019; the roster query now returns it,
  the roster wire carries it, the agent renders it in the CCD). A full-tunnel client also gets a pushed
  resolver (`dhcp-option DNS`, mirroring WG's full-tunnel DNS) so name resolution survives the redirected
  default route. Split clients emit neither.
- **UI checkbox for BOTH transports** — the Full tunnel checkbox is no longer WireGuard-only; the OVPN
  export request carries `full_tunnel`.
- **`gateway_no_egress` inherited VERBATIM** — full-tunnel OVPN rides the SHARED create path, so it hits
  the same transport-agnostic refusal a full-tunnel WG device does: a gateway without egress capability
  refuses it with the identical typed code, and it's allowed once the gateway reports `egress_nat`.

**Reds (both directions):** `TestFullTunnelPushesRedirectGateway` (full-tunnel CCD pushes redirect-gateway
+ resolver; split does not) · `TestOVPNFullTunnelRequiresGatewayEgress` (no-egress → `gateway_no_egress`
verbatim; split always allowed; egress reported → allowed).

**One-truth note:** `fullTunnelDNS = "1.1.1.1"` is deliberately duplicated in `ovpnserver` and
`devices/config.go` — separate modules (node vs api) that cannot share a Go constant, exactly like the
AES-256-GCM cipher already duplicated across the client profile and the server config. A change to the
product's full-tunnel resolver must touch both; recorded here so it is not a silent drift.

## REGISTER (decide-item, NOT now) — OpenVPN server-settings surface

Today `server.conf` is fully generated with nothing operator-exposed. A curated per-gateway set is a real
MIGRATOR need: **listen port** (1194 is blocked on some networks) and **proto UDP/TCP** (TCP fallback for
UDP-filtered networks — a classic Pritunl requirement). **Crypto stays product-owned and is NEVER
configurable** (never let a form weaken ciphers). Open question for its commit-one: org-level vs
per-gateway. Registered — not built now.

## WF-OVPN-7 — push topology + gateway to the client (FOLDED, 4e-walk finding)

rc2 walk: roster + CCD delivery worked (auth passed), then the client failed with `tun_prop_error:
ifconfig addresses are not in the same /30 subnet (topology net30)`. Root cause: this server uses manual
`mode server` (not the `--server` helper), so OpenVPN does NOT auto-push the topology — the client
defaults to **net30** and rejects the `/24` ifconfig-push. The server-only acceptance red (WF-OVPN-1)
cannot catch this — it is CLIENT-side behavior, exactly what only a wire walk surfaces.

**Fix (folded, node-only):** `serverConfig` pushes `topology subnet` and `route-gateway <pool-gw>` (the
IN-SUBNET pool gateway, e.g. `10.99.0.1`). The transit-`/30` server-tun address would be an invalid
off-subnet gateway for the client; the pool gateway is in the client's pushed `/24`, the gateway host
owns it on wg0, and on a p2p tun the client sends via it into the tunnel. Red:
`TestServerPushesTopologyAndGateway`.

## WF-OVPN-5 — FOLDED (UX, reassuring-success class): OVPN export surfaces pending-approval

A pending OVPN device exported a working-LOOKING `.ovpn` that cannot connect until approved (the
`ccd-exclusive` refusal reads as an opaque "User authentication failed") — the reassuring-success trap.
Folded: the OVPN export response already carries `device.status`; the web now shows a "pending approval —
won't connect until approved" note at issuance (`Devices.tsx`, alongside the one-time secret).

## WF-OVPN-6 — FIXED (edition line inverted): device-approval enforcement is enterprise-only

**Class — the edition line INVERTED.** `GetDeviceApproval`/`SetDeviceApproval` are enterprise-gated (403
`edition_required` in open — `device_approval_edition_open_test.go`), but the ENFORCEMENT in
`devices.Create` (pending when `device_approval='on'`) had NO edition check. So the open tier ENFORCED a
gate it could not MANAGE — an enterprise→open org's stored `device_approval='on'` trapped every new device
pending, with no open surface to approve or disable. A gate must never make the open tier LESS functional.
Device approval is an enterprise feature (S7.5.3 unlock-then-opt-in); enforcement must follow the edition.

**Fix:** `devices.Service.approvalEnforced` (wired from `apphttp.NewDeviceApprovalEdition()` — true only on
the enterprise build; the MFA-enforce wire precedent). `devices.Create` enrolls PENDING only when
`approvalEnforced && device_approval='on'`; open edition enrolls ACTIVE regardless — the downgrade-release
seam by construction. **Reds both ways:** `TestApprovalEnforcementIsEnterpriseOnly` (same org, `approval=on`
→ enterprise PENDING, open ACTIVE); posture_test now sets `SetApprovalEnforced(true)` (it tests the
enterprise feature).

## WF-OVPN-9 — REGISTERED (sequenced after Slice 5): OVPN device re-home to the active hub

An OpenVPN device's server is pinned to its `node_id` gateway (the `.ovpn` `remote` is baked at export);
WG's WF-A re-home moves the DIAL endpoint, but OVPN has no equivalent — a device homed to a hub-set member
that later becomes STANDBY is stranded (its server has no active site-link). **Fix shape (RULED, build after
Slice 5):** the `.ovpn` lists EVERY hub-set member as a `remote` in priority order — OpenVPN's NATIVE
client-side failover, the protocol-native analog of WF-A. The client dials the top `remote`, falls through
on failure; whichever gateway is active serves it. Registered, not built now.

## WF-OVPN-10 — one OpenVPN client bricks the WireGuard fleet (MERGE-BLOCKER, FOLDED)

The 4e cross-cloud walk found what no unit test or single-gateway walk could: on an **HA hub-set** org, an
OpenVPN device **bricks WireGuard reconcile on the active hub**. `widenedDevicePeers` (WF-A hub-set device
hosting) built a WG peer for every hub-set member device — including the keyless OVPN device (`public_key
= ''`) — so the agent rendered `PublicKey = ` (empty), which makes `wg syncconf` reject the **ENTIRE**
config. The active hub (aws-gw-2) then couldn't apply ANY peer state → every device's AllowedIPs went
stale → nothing transited, WG or OVPN. **One OpenVPN client, the whole WireGuard fleet on that gateway.**

**Class — guard-not-mirrored (one invariant, two consumers, one guard):** the S8.6 policy-compiler
second-election in a new costume. D-S9.4-MODEL's keyless-device skip was correctly applied at the MAIN
peer path (with the comment and all), but `widenedDevicePeers` predates keyless devices by an entire epic
(WF-A, EPIC 8), so nobody looked. The blast radius — a single bad peer rejecting the whole config, not
degrading one peer — is what made it merge-blocking rather than cosmetic.

**Census (condition 2):** exactly TWO consumers of device rows via `ListActivePeersForNode` — the main
per-node peer path (guarded) and `widenedDevicePeers` (unguarded). The three other `Peer{PublicKey: …}`
sites build from GATEWAY rows (`WgPublicKey`, always keyed) — a different source, not this rule.

**Fix — the invariant moved from convention into the SOURCE (condition 2 shape):** the query is renamed
`ListActivePeersForNode` → **`ListActiveWireGuardPeersForNode`** with `AND d.public_key <> ''` (reusing
0043's exact keyless predicate). The name + WHERE now OWN the invariant, so BOTH consumers — and any
future one — get keyed devices only; a fourth consumer can't be written wrong. Subordinate one-line
assertions remain at both consumers (cited D-S9.4-MODEL / WF-OVPN-10) given the fleet-brick blast radius.
A **node-side boundary skip** (`buildSyncConf` drops empty-PublicKey peers) is the last-line guarantee —
the WF-OVPN-1 lesson applied: a generated config for an external tool must be one that tool ACCEPTS.

**Reds (condition 3):** `TestOVPNDeviceNeverAWireGuardPeerAcrossHubSet` (hub-set + an OVPN device → NO
empty-pubkey peer on any member's DesiredState, the keyed WG device still hosted, AND the OVPN `/32` still
delivered via the OVPN roster — the B1 data half intact) · `TestSyncConfSkipsKeylessPeer` (the renderer
skips a keyless peer → the emitted wg config is valid, the `/32` never leaks into it).

**Deploy:** the fix is CP-side — **rebuild the control plane** and the fleet recovers (the agent gets a
clean peer list; no empty PublicKey; `wg syncconf` succeeds). The node-side boundary skip ships with the
next agent image; it is NOT required for the recovery (belt-and-suspenders against a future CP regression).

## WF-OVPN-11 — an OVPN client must reach the LAN behind its OWN gateway (FOLDED)

4e walk, post-WF-OVPN-10 recovery: an OVPN device on the active hub reached the REMOTE site (Azure
10.0.0.x) but NOT the LAN behind its own gateway (AWS 172.31.x). A WG client reached both. Root: the OVPN
push (agent `main.go`) emitted `ds.Policy.Routes` (remote site subnets) but NOT `ds.Policy.LocalSubnets`
(this gateway's own approved LAN).

**Class — WF-4-local's OpenVPN twin.** That EPIC-8 finding was merge-gating for exactly this reason:
reaching the LAN behind your own gateway is the PRIMARY VPN use case ("dial in, reach the office file
server" — a Pritunl shop's daily reality), never peripheral. An OVPN client that reaches remote sites but
not its own gateway's LAN has the priority inverted.

**Parity claim (the sentence for the next reader — in code at `reconcile.OVPNPushRoutes` and here):** an
OVPN client's pushed routes = **Routes ∪ LocalSubnets** — EXACTLY the reachable set a WireGuard client
assembles from the routed-ranges poll (`sites.ListRoutedRanges`). ONE truth — the org's approved site
subnets — delivered TWO ways: WG POLLS the control plane; OVPN PUSHES at connect.

**Census (condition 3):** the union is assembled in exactly ONE agent-side place (the OVPN push), now the
single named `reconcile.OVPNPushRoutes`. The WG route-set is a SEPARATE control-plane mechanism
(`sites.ListRoutedRanges`, already one helper feeding both the static export and the poll) — the CP/agent
boundary means they cannot and should not share code. This is NOT the WF-OVPN-10 multi-consumer-one-guard
shape: it is one DATA truth (the site subnets) flowing into two delivery derivations, each single-site. No
shared helper warranted beyond the one named agent-side union.

**Fix:** `main.go` builds `routes` from `reconcile.OVPNPushRoutes(ds.Policy)` (Routes ∪ LocalSubnets).
**Reds:** `TestOVPNPushRoutesUnion` — LocalSubnets AND Routes pushed; a gateway with no local subnets is
identical to remote-Routes-only (zero-config golden); nil policy pushes nothing; the pushed set is exactly
`Routes ∪ LocalSubnets`. The cross-mechanism wire equality (== WG AllowedIPs minus the pool) is walk-proven
(WG reached 172.31.x; OVPN reaches it once rc4 ships). Node-side → rc4.

## Slice 5 — CRL revocation (RULED, founder, 2026-07-24; the last EPIC-9 runtime)

Long-lived OVPN client certs (D-S9.2-2, 365d) are revoked BEFORE expiry via a CRL — the opposite of the
agent CA's refuse-renewal. The disclosed local-mac profile is its first real customer.

- **D-S9.5-1 fork RESOLVED (founder): PER-ORG CRLs, signed by the one shared CA.** "Per-CA" meant a
  CA-SIGNED CRL artifact (vs an unsigned list), NOT one global CRL. A global CRL delivered to every gateway
  would leak one org's revoked-serial COUNT + issuance cadence to every other org's gateways — a cross-tenant
  info leak that flunks the org-scoping discipline every query here follows; per-org also matches the
  substrate (ListRevokedOVPNSerialsByOrg + per-node delivery). **5b build conditions:** (i) the CRL number is
  MONOTONIC PER ORG (never a global counter — that re-introduces the cross-org signal subtly); (ii) the stored
  CRL row is ORG-SCOPED at the query layer like every tenant row (query-lint enforces); (iii) RebuildCRL is
  called from the ONE revocation seam, never sprinkled per-caller (the WF-OVPN-10 guard-not-mirrored lesson
  applied preemptively — if >1 path revokes a client cert, the rebuild lives at the shared seam).

- **D-S9.5-1 — CRL generation + storage: STORE in the DB, per-CA, rebuilt on revoke.** Recompute-on-fetch
  would re-sign on every desired-state read (a signing op on a hot reconcile path — wasteful + a failure
  mode mid-reconcile). Stored = redeliverable exactly like the server material (D-S9.6's one-delivery-model).
  Conditions: (a) the CRL carries a **nextUpdate**, and the CRL is regenerated **on revoke AND on a schedule
  well inside nextUpdate** so it never expires — OpenVPN's expired-CRL behavior is version-dependent and can
  **fail-OPEN** (silently un-revoking the fleet), so the cadence must guarantee freshness (VERIFY the binary's
  behavior, pin the cadence in the paper). (b) The CRL is **rebuilt from the FULL current revoked set, never
  appended** — a CRL is a complete statement; an incremental one that drops an entry is a silent un-revocation.

- **D-S9.5-2 — delivery mirrors D-S9.6.** Rides desired state; the agent writes `crl.pem`, `crl-verify`
  activates; idempotent re-assert; swept on disable. **An empty CRL is FIRST-CLASS**: an org with zero
  revocations delivers a valid, signed, EMPTY CRL — never a missing file (a `crl-verify` pointing at a
  missing file is a server that won't start — the WF-OVPN-1 lesson). RULED shape: `crl-verify` is
  **always-on with a real (possibly empty) CRL** whenever OVPN is enabled. Red it.

- **D-S9.5-3 — mid-session kill: RENEG-based, bound named + documented.** Rely on OpenVPN's periodic
  renegotiation (`reneg-sec`); the management interface is a new privileged surface on the helper's
  least-reviewed sibling — not worth it for a bounded delay. But the bound is the **product claim**: default
  `reneg-sec` is 3600s (an hour of post-revocation access — inconsistent with the WireGuard path's immediate
  crypto-death, walk-proven at seq 150). So set `reneg-sec` deliberately low (**RULED: the low end, these are
  hub gateways not battery-constrained clients — 60s**) and name the window as a documented transport
  difference, in the paper and eventually the docs: **"WireGuard revocation is immediate (peer removal is a
  cryptographic stop); OpenVPN revocation takes effect within one renegotiation interval (60s)."** The
  honest-limitation discipline aimed at exactly the comparison a Pritunl migrator will make.

- **SHARED-SEAM revocation law (5b, WF-OVPN-10 applied preemptively): ANY path that revokes a client cert
  calls the shared rebuild; the rebuild lives at the seam (`ovpn.Service.RebuildCRL`), NEVER re-implemented
  at each caller.** Two paths revoke device certs today — device-revoke and node-revoke — and BOTH mark the
  certs in-tx then call the one `RebuildCRL` after commit. A third path must call the same seam.

- **D-S9.5-4 — reconcile + reload: CONFIRMED, verify is a HARD GATE.** OpenVPN is claimed to re-read the
  `crl-verify` file per verification (a new CRL takes effect with NO restart — unlike the `push`-route case).
  This is exactly the class that burned this epic twice (config-completeness, push-route plumbing) — so it is
  **VERIFIED on the actual binary**, not the docs; the box-walk's revoke→death leg is the proof, and if a
  reload turns out to be needed, that is a FINDING BEFORE the walk, not during.

- **D-S9.5-5 — full-sweep parity, both faces, ONE transaction where the substrate allows (B2).** Revoking an
  OVPN device = CRL entry (kills the live session at the next reneg) + roster/CCD removal (`ccd-exclusive`
  blocks reconnect) + pool-address free. Red all three: a revoked OVPN device's session DIES **and** it cannot
  RECONNECT **and** its address RETURNS TO THE POOL — the same three-part guarantee the WireGuard path proved
  live.

**Walk (deferred to the enterprise CP):** one enterprise walk proves BOTH B1 (enforcing + grant → flow) AND
revoke → CRL → client-dies-mid-session (within 60s).

## Story-end review folds (5 findings, all FOLDED before the enterprise walk)

A multi-finder review (7 finders, adversarially verified) over the S9.1 diff found 9 → 5 distinct defects,
all folded. It earned its keep: run BEFORE the expensive enterprise-CP walk, so a review-found defect
didn't force a re-run.

- **#1 [walk-critical] — identity-binding leaked on the OVPN roster.** `ListActiveOVPNDevicesForNode`
  omitted the users + memberships joins + `NOT health_blocked` that the WG peer query and the policy
  compiler enforce — so a deactivated / off-boarded / removed-member / health-blocked user kept a LIVE
  OpenVPN tunnel (open-edition mesh = full access) while their WireGuard device was severed. The same
  missing-mirror class as WF-OVPN-10, caught by the census probe. **Fix:** mirror the compiler's gate onto
  the roster. NOT a cert revocation — deactivation is REVERSIBLE, revocation is TERMINAL (the D-S9.5-OPTIN(d)
  disable≠revocation reasoning); roster removal severs access within the reneg bound. **Parity sentence
  (paper + docs):** *WireGuard severance is immediate (peer removal is a cryptographic stop); OpenVPN
  severance for a deactivated/blocked/removed-member user takes effect within one renegotiation interval
  (60s).* Red: deactivated → absent · health_blocked → absent · non-member → absent · WG/OVPN parity.
  **CENSUS-WIDER (flagged for the re-review):** the WG PEER query itself LACKS the memberships join (only
  the compiler has it) — so a removed member's WireGuard device still gets a peer in open-edition mesh. Not
  touched in this fold (it breaks 4 test files that seed no memberships); surfaced for disposition.

- **#2 — openvpn self-heal was green-while-broken (zombie).** `realSpawn` never `Wait()`d the child, so a
  crashed openvpn became a zombie whose PID still answered `kill(pid,0)`; `procAlive` reported it alive,
  self-heal never respawned, `Reconcile` reported serving/HealthOK. **Fix:** a reaping `Wait()` goroutine
  (marks the process done → `procAlive` returns false → respawn). Red exercises the REAL `procAlive` against
  a real reaped process (not the injected fake — the WF-OVPN-1 lesson: test the mechanism, not the seam).

- **#3 — data race on `ovpnTun`.** A bare `string` written by `SetOVPNTun` (reconcile goroutine) + read by
  `tunnelIfaces()` (egress goroutine), no guard — every sibling field is atomic. **Fix:**
  `atomic.Pointer[string]`; the node tests run under `-race`.

- **#4 — the OVPN opt-in toggle wrote no audit.** `SetOVPNEnabled` unlocks the whole server+PKI surface but
  had no `writeAudit` — D-S9.5-OPTIN required it. **Fix:** `withTx` + `writeAudit` both directions (mirror
  `SetDeviceApproval`). Red asserts `org.ovpn_enabled` / `org.ovpn_disabled`.

- **#5 — a red proved the wrong thing.** `strings.Contains(cfg, "reneg-sec 60")` passes for `reneg-sec 600`.
  **Fix:** assert the exact rendered line `"reneg-sec 60\n"`. **Class (alongside WF-OVPN-1's):** a red
  asserting substring-presence proves the wrong thing — assert the exact rendered line.

## D-S9.2-SCOPE — EPIC 9 = S9.1: S9.2/S9.3 absorbed, WF-OVPN-9 folded in (RULED, founder)

**Lesson (record it): commit-one-first slicing collapses artificial story boundaries.** Slicing by DECISION
(the commit-one) rather than by original-plan-story means capabilities land where their commit-one put them,
not where the roadmap guessed. S9.2 (`.ovpn` export + per-user certs + CRL) and S9.3 (protocol selection)
turned out **~90% shipped inside S9.1** — that's the model working, not scope drift.

- **S9.2 satisfied within S9.1:** the `.ovpn` export + one-time ceremony (Slice 4), per-DEVICE certs (better
  than the originally-phrased per-USER — revoke one device without cutting the user's others; D-S9.1-3), and
  the full CRL revocation RUNTIME (Slice 5). CRL-UX is satisfied by the existing **`revoked` device status** —
  revoke exists, revoke sweeps (three-part), revoked renders: the complete operator loop. A separate "is this
  cert on the CRL" indicator would surface an implementation artifact the operator can't act on — NOT built
  (own trigger if a customer ever asks; recorded so a future reader doesn't think it was missed).
- **S9.3 satisfied within S9.1:** B3 already LOCKED protocol selection (desktop=WireGuard-by-construction,
  standard-clients=OVPN-via-`.ovpn`); the user-facing selection is the device Type dropdown (WF-OVPN-3).
  Native-OVPN-in-client = rejected tier. Nothing to build.
- **EPIC 9 = S9.1.** The genuine residual delta (multi-remote HA, WF-OVPN-9) folds into S9.1; no S9.2/S9.3
  branches (ceremony the batch model doesn't need). The merge train is ONE story.

## WF-OVPN-9 — multi-remote HA failover (FOLDED into S9.1, two parts)

An OVPN device's server is on its home node; when the hub flips, a single-remote `.ovpn` is stranded on the
now-standby (no transit). The fix is the OpenVPN analog of WF-A (which was ALSO two parts — endpoint
derivation + peer widening), and the census caught that Part A alone is a decorative fix:

- **Part A — multi-remote profile:** `BuildProfile` emits one `remote <host> 1194` per hub-set member in
  PRIORITY ORDER (`nodes.OVPNRemotes`, from `activeHubMembers` — the SAME hub-set authority the WG dial +
  the roster widening read, never a fourth "who's a hub" truth). OpenVPN's native client-side failover
  cycles them. STATIC-SNAPSHOT honest (baked at export, like the WG routed-ranges bake) — re-export after a
  hub-set change; the failover degrades gracefully meanwhile. Non-hub-set device → one remote (zero-config golden).
- **Part B — widened OVPN roster (the REQUIRED enabler):** `ccd-exclusive` refuses any client with no CCD, so
  a multi-remote profile listing gateways the device has no CCD on **fails over to a REFUSAL** — Part A alone
  names hubs it can't use (green unit reds, dead on the wire — the WF-OVPN-1 config-completeness lesson in a
  new costume). `widenedOVPNRoster` unions the OVPN clients across all hub-set members (mirroring
  `widenedDevicePeers`), so every member holds the device's CCD **warm** — whichever gateway the client fails
  over to ACCEPTS. Chosen over roster-follows-active-hub (which re-pushes on every promotion — the churn
  WF-A's warm-widening was chosen to avoid). Same authority, same warm-standby philosophy, out-of-hash.

**One derivation, two consumers:** `OVPNRemotes` (profile) and `widenedOVPNRoster` (delivery) both read
`activeHubMembers`, so the profile's remote node set and the CCD-hosting member set are byte-identical.
**Indistinguishable-/32 survives:** a widened device keeps its ONE pool /32, identical in every member's
roster — widening is about which gateways host the CCD, never the address (B1's data half unmoved).

**Reds:** `TestBuildProfileMultiRemote` (members in priority order) · `TestOVPNWidenAndRemotesParityAcrossHubSet`
(CCD widened to g2, `OVPNRemotes` = both members in priority order, parity, non-member → nil). **Walk-leg
(added to the batch manifest, discharges WF-OVPN-9's substitute):** kill the OVPN client's primary hub →
confirm it fails over to the next remote + reconnects, on the wire.

## EPIC-9 batch walk manifest (one enterprise-CP session, four proofs)

1. **B1** — enforcing + grant → flow (Slice-1's named substitute).
2. **revoke → CRL → death** within the 60s reneg bound.
3. **D-S9.5-4** — crl-verify reload-no-restart, verified on the binary.
4. **WF-OVPN-9** — multi-remote failover (kill primary hub → client reconnects to the next remote).

## Walk record (4e) — the one-time ceremony, demonstrated live

The founder's exported test `.ovpn` profile was disclosed in-session; the device is being REVOKED and
re-issued — a live demonstration of the one-time-ceremony's premise (a leaked profile is re-MINTED, not
re-served) and the first real customer for Slice 5's CRL revocation.

## EPIC-9 enterprise batch walk — RESULT + dispositions (RULED, founder, 2026-07-25)

The one enterprise-CP walk (batch model) ran **4/4 GREEN**, cross-cloud (AWS ↔ Azure). Full record +
evidence: `docs/S9.1-boxwalk.md`. Proofs: B1 enforcing+grant→flow (OVPN /32 reached cross-site, default-deny
holds for the un-granted host — discharges the Slice-1 substitute); revoke→CRL→death (conntrack-flush instant
kill + `VERIFY ERROR: certificate revoked` reconnect rejection); D-S9.5-4 reload-no-restart (openvpn PID
stable, CRL re-read); WF-OVPN-9 failover (2 remotes, warm CCD on both hubs, `VERIFY OK` on the standby).

**Finding profile: 5 findings, ALL UX/observability/timing — ZERO enforcement or security defects.** The
healthiest possible story-end shape (the enforcement boundary, CRL revocation, and HA failover all came back
clean on the wire; only the surfaces around them wanted polish).

- **walk-4 (RTO) — FOLDED.** Failover recovery was ~5 min; recovery was AUTOMATIC (the client walked its
  remote list unaided), so this is tuning, not a gap — but 5 min is a bad number beside WireGuard's ~14-21s
  re-home, and a migrator WILL compare them. Fix: bake `connect-timeout 10` into the minted profile
  (`ovpn/profile.go`) — arguing the value: these are dial attempts against a KNOWN gateway, not a flaky WAN,
  so 10s is generous for a live gateway's UDP+TLS handshake even at ~420ms cross-region RTT while abandoning a
  DEAD remote fast; re-home is then bounded by `connect-timeout × dead-remote count` (~10-12s for a 2-hub set).
  Red: the profile carries `connect-timeout 10\n` (`profile_test.go`).
  **Honest re-home note (joins the reneg-bound sentence from Slice 5):** *"WireGuard re-homes in seconds via a
  control-plane-driven peer swap; OpenVPN re-homes by client-side remote-walk, bounded by connect-timeout ×
  dead-remote count."*

- **walk-1 (OVPN "never connected") — LIE FOLDED, TELEMETRY SURFACED.** A device with a live session rendering
  "never connected" is the green-while-dead law INVERTED (dead-while-green, same class). OVPN clients are not
  WireGuard peers (WF-OVPN-10) — they carry no WG public key, so there is no handshake-telemetry analog and
  `last_handshake_at` is always null. Scope split honestly:
  - **Fold (no new machinery):** render **honest-unknown** — an OVPN device (empty `public_key`) shows
    "liveness not reported", not the false "never connected" (`web/pages/Devices.tsx` `lastSeen`). The
    `desync_unknown` honest-state law: absence of a signal is not a negative claim.
  - **Surface (its own small story):** real OVPN last-seen needs an `openvpn status`/management telemetry
    channel + a device-id-keyed liveness report — a SECOND liveness plane. Per the "don't build a second
    liveness plane silently" rule it is REGISTERED as its own story, not built at epic-end.

- **walk-2 (group-member UI + misleading label) — LABEL FOLDED, GROUP-UI DEFERRED.** The
  "Groups (… device-to-device targets)" / "(device/user source)" labels described a device-grant path that
  does not exist (members are USERS; a device inherits from its owner via a user/group rule) — actively
  misleading, one-line fold (`web/pages/Access.tsx` → "Groups of users …"). The MISSING manual
  group-membership UI is the already-registered EPIC-8 Deck-D harvest finding — it stays DEFERRED to the UI
  batch. **Note the SECOND sighting:** a founder hitting the same missing-UI across two epics is the priority
  signal the harvest was waiting for.

- **walk-5 (failback premature adoption) — REGISTERED with trigger (NOT folded).** A recovered primary accepts
  OVPN clients before its hub/site-link is warm — the **readiness-vs-liveness** distinction (up ≠ ready). Brief
  2nd outage, SELF-HEALING (~10s, bounded by hub-promotion hysteresis). Registered rather than folded to avoid
  adding a readiness gate to the hub-promotion path at epic-end. **Trigger:** any customer or walk sees it
  exceed the hysteresis bound, OR the same class appears on the WireGuard path.

- **walk-3 (Min-OS "Off" no Save) — FOLDED, trivial UI.** "Off" hid the inputs AND the Save button, so the
  setting could not be persisted; added a standalone Save for the off case (`web/pages/Access.tsx`).

Code folds landed on `story/S9.1-openvpn` after the walk record. Re-gated sha-first. **Still HELD — no merge
until the merge word.** Deferred/registered: OVPN liveness telemetry (walk-1 surface), group-member UI (walk-2,
Deck-D 2nd sighting), failback readiness gate (walk-5, triggered).
