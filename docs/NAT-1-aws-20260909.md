# NAT-1 AWS candidate qualification

User authorized consolidation, required AWS sandbox infrastructure and SCP of
candidate artifacts. Source candidate: `2afd25278a2b81fd770cfc076cd90d9f39337cbd`;
remote main verified `26a36afc9be657af94127c474e173783c91318c3`.

Scope: qualify the implemented durable mailbox and device-side HTTP API on a
real Linux EC2 host with isolated PostgreSQL. This is NOT a complete NAT relay
product walk. Gateway mTLS signaling, profile configuration, issuance wiring,
rate limits, client consumers and UI remain outstanding. Prior NAT-0 transport
proof remains separate evidence; do not merge its experimental code wholesale.

Verified AWS account 735391218823, region ap-south-1. Reuse dedicated stopped
instance `i-0d320abd28be9eaa9` (tunnex-nat0-aws-20260906a), security group
`sg-0bfd623ee6d39467d`. Add one temporary SSH /32 rule for current laptop,
record its ID, remove exactly that rule afterward. No public API/Postgres ports.
CP and old DB instances stay stopped; no Azure, deletion or existing DB changes.

SCP exact Linux test/server/migration binaries into a fresh private remote
directory. Verify matching SHA256 sums. Run a dedicated Docker PostgreSQL
fixture with loopback-only publication; no default Compose project/network/store.
Apply embedded migrations, run both edition test binaries against that database,
and smoke the production server entrypoint if its prerequisites are available.
HTTP integration tests inject a principal into the actual router; they do not
prove browser login or gateway mTLS authentication. Report that distinction.

Keep secrets outside Git/logs. Stop only newly started fixture containers and
dedicated EC2 at the end; retain disks/artifacts for diagnosis. Commit redacted
results with exact tested SHA. SCP testing does not qualify signed installation
or release readiness. No push/PR/merge is implied.

## Results: implemented slice PASS on AWS

Native Linux/amd64 binaries compiled with CGO_ENABLED=0 from the candidate's
unchanged product/test content. Local and SCP destination SHA256 values matched:

| Artifact | SHA256 |
| --- | --- |
| connectivity-open.test | 8612d0fd49d2a6f501928b00d68f09b8987111bdbf462eb1966fb562f1040c25 |
| connectivity-enterprise.test | 870d429e50486e43b27013dba0266bc1b886c4538017ca4131dd2d1e4c4c185a |
| http-open.test | da8bf61dbed00468bf7fdaaae714ef61938c0bc71dd72fadeec0388b73519e94 |
| http-enterprise.test | f112a64e128be4bfc1d4f5377c556c2d0610fcfb68e1040eaba45e7b434822dc |
| migrate | a688a85ce0c9201733949b94f8ce170d04eb2232593363c7dadacfb043f9cef6 |
| server | ff4e0b6ff737e899156144b78f7a090a55e094d9823d091b64812a6b8c2a3d8f |

Run A (`nat1-candidate-20260909a`) failed before migration: default Unix-socket
pg_isready succeeded during PostgreSQL's temporary initialization server, before
the database/final TCP listener was ready. Server log corroborated that order.
Not a product regression. Runner `8c17c89` waits for an authenticated SQL query
against the final TCP listener; no product binary rebuild or remote source patch.
Failed fixture retained stopped, not overwritten.

Fresh run B (`nat1-candidate-20260909b`), 2026-09-09 04:08 UTC, exited zero:
- All migrations →139, dirty=false.
- Open edition connectivity suite PASS, including real database concurrency,
  replay, expiry, default-off, current identity/policy posture and byte bounds.
  Durable test 0.34s; actual router/DB HTTP test 0.36s.
- Enterprise edition same tests PASS: durable 0.35s; HTTP 0.29s.
- Rollback 139→138 and reapply 139 PASS, dirty=false.
- Actual server binary started with development configuration, private secrets,
  loopback PostgreSQL/Redis; /healthz PASS. Server then terminated normally by
  runner cleanup. This is startup smoke, not browser authentication acceptance.

AWS tests were native Linux, not race-instrumented (local race evidence is
separate). HTTP tests inject a principal, use actual router/authorization/store,
and seed synthetic test eligibility. No gateway mTLS, real login, issued relay
credential delivery, installed client, NAT traffic or customer UI was exercised.

Resolved fixture images:
- PostgreSQL 17.11: `sha256:18cfe3ef5e6815560c98237d6216d1e5119702fb0f3894c8785dd58b8bbe5d73`.
- Redis: `sha256:ff02b58f971e7d7d156a1267e283fcbbeee91773b6aa36c49dac28ecfe28eadf`.

Cleanup verified all three created containers exited, no server process remained.
Temporary SSH rule `sgr-0713e454e9dc93257` removed; dedicated instance stop
requested. Existing CP/DB remained stopped. No public API/DB ingress added,
no data deleted. Fixture networks, volumes and private logs retained; EBS costs
remain. No credentials/secrets included in committed evidence.
