# S14.7 — Routed Ranges. Commit-one.

**Status: PAPER. Held. Nothing built.**

**Bucket: BUILD** (endpoint exists, nothing renders it). Branch cut from `main` @ `70e4642`, which carries
S14.6 merged.

---

# 0. PRE-FLIGHT

| check | result |
|---|---|
| **1 SCALE** | a list screen. Canonical sorted table; teaching text once. **N=many is a /8 fully allocated: 256 `/16`s, or thousands of `/24`s.** |
| **2 N=1 AND N=MANY** | N=0 is the common case for a fresh org and is **a first-class answer per the spec**. N=1 is one approved LAN. Fixtures give 4 approved + 2 pending. |
| **3 ANIMATION** | the only graph was the heatmap, **already cut** (see §0.1). Nothing left to animate; **no `tnxRangePlot`, so the second GSAP surface disappears with it.** |
| **4 HANDOFF + REGISTER GREP** | `grep -i 'routed\|heatmap\|pending' docs/CUT-REGISTER.md` → **4 hits, all live.** See §0.1. |
| **5 LIVE SCHEMA** | `site_subnets.cidr` type read from `pg_attribute`, not from a migration. **It is decisive — see §2.** |
| **6 TOKEN PAIRINGS** | no new tokens |

## 0.1 THE REGISTER GREP — both S14.7 cuts are already recorded

- **Address-space heatmap — CUT.** Two independent reasons on file: a `/24` lights a whole `/16` cell
  (coarse-granularity), and the grid domain is `10.0.0.0/8` while real customers use `172.16/12` and
  `192.168/16`, which **have no cells at all.**
- **Pending queue — CUT FROM THIS SCREEN.** ⚠ **Its reason was DEPENDENT and is now corrected**: it read
  *"cutting the grid leaves Routed Ranges as a read-only projection"* — un-cut the grid and the row loses its
  justification. **The independent reason: the queue lives on Sites because that is where the MUTATION
  ENDPOINT is.** `/site-subnets/pending` + `/approve` are `site:manage`; `/routed-ranges` is `org:view` and
  has no approve verb. **True whether or not the heatmap was ever cut.**
- The two *pending* rows are the same panel under two names; **cross-referenced rather than merged**, because
  a reader greps for either.

---

# 1. ⛔ THE PERMISSION CHECK — RUN AGAINST THE HANDLER, AND IT CHANGES THE ANSWER

**The founder's framing and my earlier census both said `listSiteSubnets` is `site:manage`. THE HANDLER SAYS
OTHERWISE.**

```go
// internal/http/site_handlers.go:33   ListSites
if _, err := authorize(ctx, req.OrgId, rbac.PermOrgView); err != nil {

// internal/http/site_handlers.go:53   ListRoutedRanges
if _, err := authorize(ctx, req.OrgId, rbac.PermOrgView); err != nil {

// internal/http/site_handlers.go:195  ListSiteSubnets
// S8.3 D5: member-readable (org:view) — part of the read-only topology; approval/advertise stay site:manage.
if _, err := authorize(ctx, req.OrgId, rbac.PermOrgView); err != nil {
```

**ALL THREE ARE `org:view`.**

## Where the wrong belief came from — and it is the law, twice

```yaml
summary: List a site's subnets (S8.1; site:manage)      # openapi.yaml:2081 — WRONG
```

**The SUMMARY says `site:manage`. The HANDLER authorizes `org:view`.**

**⚠ AND MY FIRST EXPLANATION OF HOW IT SPREAD WAS ITSELF UNMEASURED — the founder caught it.** I wrote that
*"my S14.5 census copied the summary into a table and the founder read the census."* **MEASURED:**

```
docs/S14.5-decisions.md:118
| list / add subnet | `listSiteSubnets` / `addSiteSubnet` | 2080 / 2090 | ✅ |
```

**The census never reproduced `site:manage`.** It cites line numbers and a verdict. **The founder read
`openapi.yaml:2081` directly.** So there was no chain — one wrong line misled two readers independently.

> ## **I DIAGNOSED THE PROPAGATION OF AN UNMEASURED CLAIM WITH ANOTHER UNMEASURED CLAIM, IN THE PARAGRAPH
> ## ANNOUNCING THE LAW AGAINST IT.**

> ## **VERIFY AGAINST THE SWITCH, NOT AGAINST THE NAME — minted in this very spec at S14.5, on a
> ## `policy_degraded_kind` description that read as enterprise-only and was wrong, whose own first
> ## correction was also wrong. IT HAS NOW MISLED TWICE FROM THE SAME FILE.**

**SECOND SOURCE, corroborating:** `PLAN.md`'s S8.3 entry records review finding #2 as ruled —
*"loosen site READS to org:view (member-read precedent, no new perm); mutations/preview/queue stay
site:manage."* **Handler and ruling agree; only the summary disagrees.**

**⛔ REGISTERED AS A SPEC DEFECT** — same class as the `policy_degraded_kind` paragraph corrected in S14.5,
and the fix is the same: **correct the description, do not annotate it elsewhere.** On an OpenAPI-first
codebase a wrong summary is a wrong source of truth. Landed as a slice of this story.

## AND THE ATTRIBUTION DECISION FALLS OUT DIFFERENTLY

Because subnets are **member-readable**, a member CAN see attribution. **So the question is no longer
"admin-only or nothing" — it is N+1 cost, not permission.**

- `/routed-ranges` returns ranges with **no `site_id`** (it is a device-facing projection: *ranges only, no
  keys, endpoints, pool or policy*)
- `site_id` lives on `SiteSubnet`, reachable via `listSiteSubnets`, **per site**

**RULED IN THIS PAPER: attribution is rendered for everyone, built from a per-site fan-out issued ONCE per
visit** — the same shape D1 ruled for cross-site DNS on Sites. **No member-facing degraded state is needed,
because the earlier premise for it was false.**

### ⛔ THE FAN-OUT BOUND, STATED — because "bounded by site count" is not a bound

**N sites = N+1 requests**, issued in parallel, once per visit.

| N | requests | honest assessment |
|---|---|---|
| 4 (fixtures) | 5 | fine |
| 20 | 21 | fine — parallel, browser caps at ~6 concurrent per origin |
| **50** | **51** | **~9 sequential waves at 6-per-origin. Noticeable. Still one page load.** |
| 200 | 201 | **not acceptable** |

**WHAT HAPPENS AT 50, CONCRETELY:** the ranges table renders immediately from `/routed-ranges` (one request),
and the `SITE` column **fills in progressively as the fan-out lands.** It must therefore have an honest
in-flight state — **not blank, which would read as "unattributed", the reassuring-empty shape at row level.**

**THE REAL FIX IS `site_id` ON `RoutedRange`, AND IT IS NOT THIS SCREEN'S TO MAKE.** `/routed-ranges` is a
DEVICE-FACING projection — *ranges only, no keys, endpoints, pool or policy* — and adding an org-structure
field to it needs a decision about whether a device should learn site topology. **Registered, not assumed.**

**SO THE BOUND IS A TRIPWIRE, NOT A LIMIT:** the fan-out ships, and **if an org crosses ~50 sites the column
needs the served field instead.** Named here so the next reader inherits the threshold rather than
rediscovering it at a customer.

---

# 2. ⛔ THE JOIN TRAP — TESTED, NOT REASONED

**The concern:** `RoutedRanges.ranges` is *"canonical masked form, sorted"*; `SiteSubnet.cidr` is documented
only as *"IPv4 CIDR of the routed LAN"*. A subnet stored as `10.20.0.1/24` would never string-match a range
emitted as `10.20.0.0/24`, **and the row would render unattributed — reassuring-empty at row level.**

**MEASURED. THE TRAP CANNOT FIRE, AND THERE ARE TWO INDEPENDENT REASONS:**

```
$ … pg_attribute → site_subnets.cidr type = cidr        ← the POSTGRES cidr type
$ SELECT '10.20.0.1/24'::cidr;
  ERROR:  invalid cidr value: "10.20.0.1/24"
  DETAIL: Value has bits set to right of mask.
```

**⚠ AND THE FIRST VERSION OF THIS PROOF WAS A CAST, NOT THE COLUMN — founder-caught.** `SELECT '…'::cidr`
proves the TYPE rejects it; it says nothing about `site_subnets.cidr` actually BEING that type. Both now
measured:

```
$ \d site_subnets
   cidr  |  cidr  | not null            ← the column, from the live catalogue

$ BEGIN; INSERT INTO site_subnets (site_id,cidr,status) VALUES (…,'10.20.0.1/24','approved'); ROLLBACK;
  ERROR:  invalid cidr value: "10.20.0.1/24"
```

**A REAL INSERT INTO THE REAL TABLE, REJECTED.** The cast was evidence about Postgres; this is evidence about
our schema.

**1 · THE COLUMN TYPE REJECTS HOST BITS.** A non-canonical subnet cannot be stored *at all* — not by the API,
not by a migration, not by a hand-written `INSERT`. **This is the strong guarantee: it holds against write
paths that do not exist yet.**

**2 · AND THE WRITE PATH MASKS ANYWAY** — `service.go:96`: `AddSiteSubnet(… Cidr: cidr.Masked())`. Belt and
braces, and the belt is the database.

**SO A STRING JOIN IS SAFE — but the code will still normalise both sides**, because the *reason* it is safe
lives in a column type two layers away, and a future migration to `text` would break the join silently.
`routedranges.go:211` already sets the precedent: `ss.Cidr.Masked() == cidr.Masked()`.

**⚠ THE OWED RED IS STILL OWED, IN A DIFFERENT FORM.** The founder asked for a test proving a non-canonical
stored subnet still attributes. **That test cannot be written — the insert is rejected.** So the guard becomes
the honest one: **a test asserting the DB refuses a non-canonical CIDR**, which is what actually protects the
join. *A test that cannot be written because the state is unreachable is a stronger result than a test that
passes.*

---

# 3. RENDER-FLOOR ITEMS

**`forwards` IS GATED.** A forward appears only when its `resolver_ip` falls inside a routed range. **Empty
means "none reachable", NEVER "none configured."** Copy must not say *"no DNS forwards configured"* — the
honest empty state is *"No forwarded zones are currently reachable from a routed range."* The handoff already
carries the rule verbatim: *"A forward is handed to devices only when its resolver falls inside a routed
range — never a SERVFAIL generator."*

**`dial_endpoint` / `dial_pubkey` ARE NULL IN TWO DIFFERENT CASES**: no `device_id` was passed, **and** the
device's node is not a hub-set member. **Null must not render as "no hub."** This screen passes no
`device_id`, so they are *always* null here — **the fields are therefore not rendered at all**, rather than
rendered as an absence that means something.

---

# 4. THE SCREEN

**Columns (handoff):** `RANGE (CANONICAL, SORTED)` · `SITE` · `STATUS` · `PUSHED TO`.

`RANGE` ✅ · `SITE` ✅ via the fan-out (§1) · `STATUS` — **⚠ `/routed-ranges` is APPROVED-ONLY**, so a status
column has exactly one value and is **cut as a constant column**; pending ranges are Sites' surface ·
`PUSHED TO` — the handoff shows *"split-tunnel AllowedIPs · 126 devices"*; **the device count is not served**
(same class as the gateway peer count) and is cut with its reason on the panel.

**Reachable DNS forwards** panel, with the gating copy above.

---

# 5. DECIDE-ITEMS

**D1 — REVIEW DEPTH FOR THIS STRETCH.** Unruled. **S14.6's depth is not assumed to carry.** Naming it now
because the paper is ready and the answer changes how much goes into one slice.

**D2 — The spec-summary correction: ride S14.7, or its own slice?** It is comment-only but regenerates three
targets. **Recommend ride**, as the `policy_degraded_kind` fix did in S14.5.
