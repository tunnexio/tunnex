# S14.12 — Access Policies. Commit-one.

**Status: PAPER. Held. No code.** Bucket: **REDESIGN** — `Access.tsx` exists, is routed, and is the largest
unpassed surface (1,600+ lines, 15 org-scoped reads).

---

# 0. ⛔ THE HEADLINE IS THE OPEN-EDITION QUESTION, AND IT MUST BE RULED BEFORE ANY CODE

> ## **`accessView` LINE 135 IS EDITION-FIRST — THE EXACT BUG S14.11 FIXED IN `usersview`, STILL LIVE HERE,**
> ## **AND UNOBSERVABLE ON THE STACK THE FOUNDER REVIEWS ON.**

```ts
if (!i.isEnterprise) return "upsell";   // [75]: role irrelevant here — never role_retry
```

**The whole screen collapses to ONE state on the open build** — `"upsell"`, for **every role**, because the
edition is checked before the role. Measured against the server by S14.11's own 43-handler census:

| caller | server answers | this screen renders |
|---|---|---|
| enterprise + member | `403 forbidden` | `member_gate` (role-named) ✅ founder-verified |
| **open + member** | **`403 forbidden`** | ⛔ **`upsell`** — sells Enterprise to a role that could never use it |
| open + owner | `403 edition_required` | `upsell` ✅ |

**That is the S14.5 halt, forward** — the same defect, the same shape, on a second screen. The founder's
S14.11 review confirmed Access degrades correctly **on enterprise**; the open-edition arm is a different code
path and has never been seen.

## THE RULING REQUIRED NOW, not after half a screen is built

**The open-edition render of this screen is exactly ONE state.** That cuts both ways and both are stated:

- **AGAINST a second stack:** one state is the cheapest possible thing to cover by test. A wiring test can
  assert the open-edition member sees a role-named sentence and never the word "Enterprise", with no stack.
- **FOR a second stack, and I recommend it:** *this screen is 100% edition-gated.* Users & Roles was
  reviewable on enterprise because only two columns were gated; **here the ENTIRE screen is.** A founder
  review on the enterprise stack sees the admin body and **cannot see the open-edition screen at all** —
  and the one state it renders is the state I have just measured as probably wrong.

> ## **⛔ RECOMMENDATION: BUILD THE OPEN-EDITION STACK BEFORE THE SCREEN, NOT AFTER.** The epic-level debt
> ## (registered since S14.1, `docs/DEFERRAL-REGISTER.md`) named its trigger as *"before the next
> ## edition-gated section pass."* **This is that section**, and it is the most edition-gated screen in the
> ## product. Deferring again means shipping a screen whose only open-edition state is unreviewed **and
> ## already suspected defective**.

**Cost, honestly:** a second compose project on non-conflicting ports, a second seed pass, and the open
fixture set is genuinely smaller (`policy_rules`, `user_groups`, `group_members`, `access_events` are all
enterprise). **S12.1's runtime license gate dissolves this debt rather than paying it** — so the question is
whether S12.1 is close enough to wait for. **Founder rules; I do not pick.**

---

# 1. PRE-FLIGHT — all six, run

| # | check | result |
|---|---|---|
| 1 | **SCALE at 10×/100×** | 15 org-scoped reads on one screen. **No per-row fan-out found** (no N+1) — but 15 sequential `loadOne` calls is the largest read set in the product; measure wall-clock before adding a 16th |
| 2 | **N=1 AND N=MANY** | groups **3** · resources **3** · policy rules **6** · access events **3**. **N>1 everywhere — no N=1 trap.** But see the KIND gap below |
| 3 | **ANIMATION is a requirement** | the rule graph/table has no motion today; motion gate rejects unregistered animation (S14.8 proved it rejects) |
| 4 | **grep the registers first** | **4 live rows**: Access em-dashes (2 sites, now in PR #59) · `Histogram` has no consumer · date-range picker cut · D2 group edges |
| 5 | **LIVE SCHEMA is authority** | `policy_rules` carries **4 CHECK constraints**, including two `src_kind`/`dst_kind` discriminated-union checks. **The spec is not the authority; these are** |
| 6 | **token/foreground pairing** | deferred to the build; no token value moves in the plan below |

## ⛔ CHECK 2's REAL FINDING — THE FIXTURE COVERS 5 OF 8 KINDS

```
src_kind  CHECK allows: group  user  site  cidr     seeded: group(5) cidr(1)
dst_kind  CHECK allows: resource  group  site  k8s_service   seeded: resource(4) group(1) k8s_service(1)
```

**3 of 8 kinds have NO fixture row: `src_kind='user'`, `src_kind='site'`, `dst_kind='site'`.**
All three are **shipped features with CHECK-constraint backing** — `src_kind='user'` is S7.5.4's per-user
grants, `site` on both axes is S8.2's site-to-site transit. **A screen that renders a discriminated union can
only be reviewed on the arms the fixture produces**, and this is the S14.10 lesson exactly: the states that do
not render are the ones that ship broken.

*(I wrote "2 of 8" while listing three, and caught it in my own output. Counts-and-lists disagreeing — sixth
instance this epic.)*

---

# 2. ENDPOINT CENSUS — with citations, not a spec summary

| surface | endpoint | permission | edition |
|---|---|---|---|
| rule list | `GET /organizations/{orgId}/policies` (`openapi.yaml:1482`, `listPolicyRules`) | `PermPolicyView` | `policyPort == nil` → 403 |
| rule create/delete | same path + `/{ruleId}` | `PermPolicyManage` | ″ |
| rule enable/disable | F3 toggle, `SetPolicyRuleEnabled` (`policy_handlers.go:357`) | `PermPolicyManage` | ″ |
| grant extend | `ExtendGrant` (`policy_handlers.go:330`) | `PermPolicyManage` | ″ |
| groups | `/groups` (`:1221`), `/groups/{id}/members` (`:1294`) | `PermPolicyView` / `PermPolicyManage` | ″ |
| resources | `/resources` (`:1409`) | `PermPolicyView` / `PermPolicyManage` | ″ |
| zero-trust mode | `/zero-trust-mode` (`:1582`) | `PermPolicyView` / `PermPolicyManage` | ″ |
| access events | `/access-events` (`:750`) | (S7.5.1) | ″ |

**Every one authorizes BEFORE the edition check** — verified by S14.11's census
(`TestEditionGateNeverPrecedesPermissionGate`, 43 handlers, 0 leaks). **The screen does not mirror that**, §0.

**Served rule fields, measured from the payload** (not the spec): `cidr_outside_org_ranges` · `created_at` ·
`dst_group_id` · `dst_k8s_service_id` · `dst_k8s_service_vanished` · `dst_kind` · `dst_resource_id` ·
`dst_site_id` · `enabled` · `expires_at` · `id` · `managed_by_operator` · `org_id` · `src_cidr` ·
`src_group_id` · `src_kind` · `src_site_id` · `src_user_id`. **18 fields — the richest DTO on any screen**,
and two of them (`cidr_outside_org_ranges`, `dst_k8s_service_vanished`) are **derived warnings** whose
consumers must be named per the who-reads-this probe.

---

# 3. CUTS — under the four-way test. **NONE PROPOSED YET.**

⛔ **DELIBERATELY EMPTY, and that is the finding.** S14.10 opened with *"presentational only"* and that phrase
foreclosed two genuine findings before the audit ran. **The wireframe extraction is NOT yet done** — this
paper stops at the pre-flight because §0's ruling changes what the extraction is even for: if the open-edition
stack is built, the extraction must cover **two** renders, not one.

**The audit runs first; the scope is its output, not its premise.**

---

# 4. DECIDE-ITEMS

**D1 — THE OPEN-EDITION STACK.** ⛔ **BLOCKING; nothing else can be scoped until it is ruled.**
Recommend: **BUILD IT, before the screen.** §0 has the argument and the cost.

**D2 — `accessView`'s edition-first ordering.** Recommend: **fix it in this story**, mirroring
`groupAccessState`. It is a 2-line reorder plus a wiring test, the server's own order is the specification,
and it is the second instance of a defect this epic already has a law about. **It cannot be verified without
D1.**

**D3 — the three unseeded rule kinds.** Recommend: **seed all three** (`src_kind='user'`, `src_kind='site'`,
`dst_kind='site'`) before building, as S14.11 seeded its member states. **The fixture is a precondition of the
build, not of the review.**

**D4 — 15 sequential reads.** Recommend: **measure, do not refactor.** Record the wall-clock; only act if it
crosses a threshold the founder sets. A refactor here is a fold nobody asked for.

**D5 — review depth.** Not asked. Settled by practice since S14.6 and treated as RULED.

---

# 5. RULED DURING THE BUILD (S14.12 slice 2)

**A DESIGN ARTIFACT MAY DEMONSTRATE A RESULT WITHOUT SPECIFYING THE RULE THAT PRODUCES IT.** `polFlow` is a
hardcoded literal — 4 sources, 4 destinations, 5 hand-authored edges, `cy` typed at 54/122/190/258 — that
**never reads the rule table.** Its 9-row table and its 5-edge graph are independent fixtures. **The picture is
legible because a human curated it; the product must derive it.** When they differ, **the derivation is OURS to
design and is recorded as a decision, never as an implementation of the design.**

| ruled | decision |
|---|---|
| **selection** | cap each column at **4**, choose by **edge degree**, render the remainder with the table authoritative — withheld-says-why, one level below D5, reusing that shape so the two cannot drift |
| **ordering** | **barycentric**, ours. The design's zero crossings are not incidental (`d_eng` sits in slot 3 so `oncall-grp` fans up one row). **A cap without ordering still tangles at 4×4** |
| **animation** | **entry-once `tnxDraw 1.4s ease-out .15s`**, diverging from the source's infinite `tnxFlow`. A policy graph depicts CONFIGURATION, not traffic. **Second time the design's motion contradicts our rule, and the rulings differ:** on Sites we KEPT the flowing link because that link IS live. Same mechanism, opposite ruling; **the discriminator is whether the thing depicted is happening now** |
| **hover-to-trace / click-to-pin** | **CUT to its own slice.** Spec already read and recorded: `hovPol`/`selPol` → focus, unfocused edges dim to **0.14** and nodes to **0.24**, `transition .2s`, focused edges thicken 2 → 2.6/2.8, plus the readout box (`"All access flows"` / `"hover a node to trace · click to pin"`, replaced by the focused node's name and meta) |

## 5b. REMAINING SCOPE OF THIS SECTION — named now, not discovered later

- **the mode toggle as a real switch**, with the blast-radius confirm inline
- **the two-column panel layout** — flow + rules left; warn legend, device-approval queue, temporary grants right
- **the rule table's `SOURCE · DESTINATION · STATE` headers** (ours is an unheadered stacked list)

---

# ⛔⛔ S14.12 STAYS OPEN AFTER THIS MERGE — THE SECTION IS NOT CLOSED

**FOUNDER-RULED before the merge word, deliberately, so nobody has to infer it later.** The merge covers
**THE SLICE AS REVIEWED**, not the section.

> ## **A SECTION CLOSED BY AMBIGUITY IS HOW REGISTERED SCOPE BECOMES FORGOTTEN SCOPE.** A future reader
> ## finding `S14.12 — MERGED` in the PLAN pointer would reasonably conclude Access Policies had its pass.
> ## **It did not. Three items were extracted at commit-one, never built, and they do not lapse.**

## THE THREE ITEMS, NAMED — S14.12's REMAINING SCOPE, CARRIED FORWARD AS ITS OWN SLICE

1. **The mode toggle as a REAL SWITCH, with the blast-radius confirm inline.** The wireframe shows
   `MODE · Off · Enforcing` as a switch, and states the confirm verbatim: *"Enforcing turns on default-deny;
   2 full-tunnel devices (win-bob, iphone-dana) become egress_policy_denied. Type acme to confirm."* Ours is
   an `Enable enforcing` button with no blast-radius preview. ⚠️ **`SetMode` already returns
   `[]AffectedDevice`** — the server owns those counts, so unlike the cascade confirm this one CAN name real
   numbers. That asymmetry is the reason it is worth building rather than restating as risk-only.
2. **The two-column panel layout.** The design places flow + rules LEFT, and warn-legend / device-approval
   queue / temporary grants RIGHT. Ours is a single stacked column. (The device-approval queue itself is
   **cut** — different permission axis, S14.10b — so the right column carries the other two.)
3. **The rule table's `SOURCE · DESTINATION · STATE` headers.** Ours is an unheadered stacked list; the
   design is a table with column headers, which is what makes a rule scannable at N rows.

## WHAT THE MERGE DOES COVER

The gate-order fix (D2) · the three empty states (D3) · the flow graph with its two thresholds, degree cap and
barycentric ordering (D5) · the group-membership surface · `src_group_empty` · the cascading-delete typed
confirms · the prose em-dash clearing on this screen · and the open-edition review stack that made half of it
measurable.

**AND WHAT IT CARRIES, UNFIXED, EACH REGISTERED WITH ITS OWN ROW:** the `CountOwners` lockout · the Audit
Log's unread `actor_system` · `Kubernetes.tsx:403`'s placeholder regression · the 51 omitted-and-read mocks ·
**the 11 remaining unreachable mutations** · D1/D1b/D2's held projections.
