# S20.5 — Customer-grade Kubernetes zero-touch installation and lifecycle

Status: **LOCKED — FOUNDER DIRECTED 2026-08-30**.

Founder direction: build the Kubernetes installation and lifecycle as one
provider-neutral product path, use parallel implementation lanes, keep the work
on the current branch, and do not publish it yet. The next live walk must not
need a walk-time source patch, Secret/PVC edit, host sysctl command, CNI edit,
or rollout workaround.

This paper is commit-one in the story sense: decisions precede product edits.
It authorizes the local implementation and its tests. It does not authorize a
commit, push, deployment, cloud mutation, PR, merge, or release.

## Measured starting boundary

- S20.3b explicitly leaves deployment and Helm changes to a later story.
- The `tunnex-gateway` chart deploys one in-cluster connector, but the web UI
  emits `joinToken.secretRef`, a value shape the chart does not accept. Helm
  coerces that map into a literal string instead of refusing it, so the emitted
  command cannot enroll a gateway.
- The UI also names a `tunnex/operator` Helm chart that does not exist.
- The chart has no values schema, Kubernetes probes, exact-node host bootstrap,
  state-retention contract, existing-claim reuse, digest input, or gateway
  render gate. Its preflight checks only `/dev/net/tun` and DNS and may run on a
  different node from the gateway.
- The node already serves `/healthz` and `/readyz`, but Kubernetes does not use
  them. A pod can therefore be `Ready` while enrollment or the data plane is
  not ready.
- The AKS HA walk required manual host `rp_filter`, CNI masquerade, Secret/PVC,
  and rollout changes. Dirty branch work makes those datapath repairs possible,
  but the CNI mechanism is embedded in the generic egress manager, called AKS
  specific, and treats every chain-read error as not applicable.
- Provider/platform on a registered cluster is presentation and guidance only.
  It is not evidence of the actual CNI, CSI, endpoint controller, or node
  posture and must never select a privileged mutation.

## Locked outcomes

### D1 — One common lifecycle, capability adapters at the edge

**LOCKED.** AKS, EKS, GKE Standard, and self-managed Kubernetes use one CLI
lifecycle, one gateway chart, one operator chart, one node agent, and one
state/health model.

Common lifecycle is:

`discover → plan/digest → approve → apply → verify → ready`, with an explicit
rollback path. Provider/CNI/CSI/endpoint adapters may return typed facts and
narrow actions, but provider names never branch the datapath. Detection uses
the Kubernetes or host mechanism actually present. `unknown`, `ambiguous`,
`blocked`, and `not_applicable` remain distinct results.

The first live-qualified adapter is the nft `IP-MASQ-AGENT` mechanism observed
on the AKS walk. Its name and match are mechanism based, so the same adapter may
apply wherever that exact chain exists. EKS/GKE/self-managed fixtures prove the
common contract now; support is not claimed live until each provider leg runs.

### D2 — The customer entry point is `tunnex k8s`, not a secret-bearing Helm command

**LOCKED; supersedes EPIC-10 D2's raw-token Helm transport.** A Helm value and
the rendered Secret are retained in Helm release history. A one-time secret is
still a secret before it is consumed, so a customer-grade flow cannot put it
there.

`tunnex k8s install` uses the caller's existing CLI login to:

1. resolve one explicitly selected organization (or the sole organization);
2. read the control-plane public/gateway endpoints from server metadata;
3. discover and print a redacted Kubernetes plan plus stable SHA-256 digest;
4. persist the opaque claim/request/generation in an owned token-blind
   Kubernetes lifecycle anchor before any credential is minted;
5. mint or idempotently redeliver one claim-bound, node-name-bound,
   single-use gateway token;
6. stream a create-only Kubernetes Secret manifest to `kubectl create -f -`
   without putting the
   token in argv, a values file, logs, or Helm history;
7. re-read the release/Secret/anchor/PVC fingerprint, acknowledge the exact
   persisted generation, then run create-only
   `helm install --atomic --wait` with non-secret values;
8. verify the real Deployment readiness, connected endpoint, and exact
   control-plane claim consumption; and
9. CAS-delete the consumed bootstrap Secret and lifecycle anchor only after
   success. A failed install retains the minimum typed recovery state for a
   bounded retry and names objects, never the token value.

The command may prompt for plan approval; `--yes` is the deliberate non-
interactive form. It refuses a multi-organization guess, an unlogged-in caller,
an unverified/unauthorized token mint, an ambiguous default StorageClass, an
unsupported Service mode, or missing `kubectl`/Helm context before mutation.

Raw `joinToken` remains a backwards-compatible chart escape hatch but is
labelled insecure and is not emitted by the product or used by the walk.

### D3 — Host posture is common and exact-node

**LOCKED; lifecycle ownership refined by D10/D10a.** `/dev/net/tun`, IPv4
forwarding, WireGuard reverse-path filtering, nftables, and UDP port
availability are Linux gateway requirements, not Azure requirements.

The final design does not let a release-scoped gateway init container mutate
broad host state. The separate cluster-singleton `tunnex-host-posture` release
from D10/D10a runs one privileged manager on each eligible node, journals the
pre-Tunnex baseline before mutation, owns restoration, and is the sole writer
for broad host sysctls plus the exact journal-owned `wg0` reverse-path filter
from D10c. The gateway keeps its narrow `NET_ADMIN`/`NET_BIND_SERVICE`
capability set and no host `/proc/sys` mount. Its credentialless,
non-privileged init container only waits for two advancing manager heartbeats
which contain that exact Pod UID. It receives no service-account token, join
token, state PVC, network credential, or arbitrary command input.

The manager validates the complete fixed gateway Pod authority contract rather
than trusting the owner label alone. A malformed label-selected Pod is ignored
with bounded change-only diagnostics and cannot become an owner; ambiguous API
readback or conflicting Pod UID identity blocks mutation. The manager and real
gateway probes are authoritative because they observe the actual selected
node. Restricted admission fails before false readiness with the missing
posture named; there is no privileged gateway sidecar.

### D4 — CNI compatibility is reconciled by observed mechanism

**LOCKED; writer split refined by D10c.** Extract a small Linux `k8snetprep`
reconciler from the generic egress manager. It performs read-only common-host
verification and owns mutation only for registered CNI adapters. A host
posture mismatch blocks readiness while the shared manager heals it; the
gateway never writes a host sysctl.

For the nft ip-masq adapter:

- exact chain-not-found is `not_applicable` only with no active tunnel; an
  active tunnel with no registered mechanism is `blocked` with
  `no_registered_adapter`; tool/permission/parse failures are likewise
  `blocked`, never silently absent;
- only a Tunnex-commented return rule scoped to the current tunnel CIDR and
  WireGuard egress is owned;
- insert is idempotent and read back;
- controller deletion heals on the next reconcile;
- CIDR/interface change replaces the old owned rule; withdrawal removes every
  obsolete owned rule; and
- unrelated foreign rules are never flushed, reordered, or rewritten.

Adapter state is logged in a finite, redacted form. Provider metadata remains
guidance only.

### D5 — Endpoint discovery uses Kubernetes Service truth

**LOCKED.** For a `LoadBalancer` gateway Service, an empty explicit endpoint
means the agent discovers `status.loadBalancer.ingress[].ip|hostname` from its
own named Service through the existing read-only Kubernetes credential. The
Service port supplies the WireGuard port. It waits without claiming readiness,
publishes the endpoint only after a valid read, and re-reads so controller
changes converge.

An explicitly configured endpoint wins. `NodePort` requires an explicit
reachable node address in v1; the product does not guess public node identity.
The lifecycle never reserves a cloud IP, adds provider annotations, or calls a
cloud API. Operator-supplied `service.loadBalancerIP`, repeatable Service
annotations, and existing image-pull Secret names remain provider-neutral
Kubernetes inputs: the CLI validates them, includes them in the redacted plan
and approval digest, passes them unchanged to the chart, and verifies their
non-secret Service/Deployment readback. It never generates a provider
annotation, creates a registry credential, or reads a pull Secret body.

### D6 — Identity storage is retained by default and never silently reset

**LOCKED.** A gateway PVC contains the node certificate, WireGuard key, HA
fences, leases, and authority evidence. Helm uninstall therefore retains it by
default. The chart supports an explicit existing claim for reinstall/recovery.

Lifecycle modes are:

- `enroll`: empty/new state plus a one-time token;
- `reuse`: an existing retained claim and no token; and
- `rotate`: **not automated in this story**. It refuses with guidance until the
  old node is safely detached/revoked and a separate new claim is named.

There is no automatic PVC delete or fence removal. `tunnex k8s purge-state`
requires the release to be absent, the exact claim name, and a typed
confirmation. The command states that recovery is impossible after deletion.

### D7 — Kubernetes probes, immutable rollout inputs, and lifecycle verbs are real

**LOCKED.** The gateway Deployment consumes the existing agent health server:

- startup/liveness: `/healthz`;
- readiness: `/readyz`; and
- named `health` container port `9091`.

The chart accepts an image digest as the preferred immutable input and a tag as
the compatibility fallback. External configuration changes use a non-secret
rollout revision; changing or deleting a consumed join Secret is not required
for ordinary restart/upgrade.

CLI lifecycle verbs are `plan`, `install`, `status`, `upgrade`, `rollback`,
`diagnostics`, `uninstall`, and guarded `purge-state`. Upgrade and rollback are
Helm-revision based, atomic, wait for real readiness, and preserve the claim.
Diagnostics exclude Secret bodies, Helm values that may contain legacy raw
tokens, private keys, certificate bodies, and service-account tokens.

One invocation manages one connector identity/release. HA A and B are two
independent releases with distinct names, endpoints, Services, and PVCs; this
prevents the shared-Secret/shared-PVC surgery seen in the walk. Pool creation,
HA enablement, promotion, and safe drain remain owned by S20.3b and its UI/API,
not inferred by Helm.

#### D7a — The Leg 9 sustained-failure action is the scale subresource

**LOCKED (founder-approved 2026-08-31).** For the HA wire proof only, the
supported sustained-failure injection is a write to the exact gateway
Deployment's Kubernetes `scale` subresource: replicas `1 → 0` to remove that
one connector, then `0 → 1` to restore it. This is consistent with D7 because A
and B are independent one-replica releases, preserves the release, Service,
PVC, claim, and immutable Pod template, and lets the ordinary control-plane
health and S20.3b ownership scheduler observe a sustained member absence.

This narrow evidence action is not a lifecycle remedy, customer management
surface, manual promotion, safe-drain substitute, Helm values change, or new HA
behavior. It is allowed only in Leg 9 after both releases are ready and the pool
is already `fenced_ha`; it must use the scale subresource rather than editing or
patching the Deployment template. Restoring replicas does not declare recovery:
the walk waits for the exact identity to become ready and for automatic
ownership/failback evidence. Any manual pool promotion, fence change, Pod spec
edit, rollout restart, or scaling performed to repair an install remains
disqualifying.

### D8 — Package the existing GitOps operator truthfully

**LOCKED.** Package the existing operator as `tunnex-operator` and its three
cluster-singleton CRDs as a separate, retained `tunnex-operator-crds` lifecycle
chart. The operator uses an externally created one-time machine Secret,
least-privilege RBAC, no data-plane privilege, and the existing API-only
reconcile/finalizer/status behavior. Packaging adds no GitOps feature.
Its readiness probe authenticates against the configured control-plane org, so
bad URL/org/token inputs cannot make `helm --wait` report a false success.

The CRD chart upgrades monotonically before the rollbackable operator chart.
It contains no workloads or credentials, validates the exact previous Helm
owner before the narrowly scoped adoption of legacy install-only CRDs, and
keeps CRDs on uninstall. `--take-ownership` is not a general force switch: an
ownerless same-name CRD is adoptable only when its complete spec, after removing
only Kubernetes' exact `conversion: {strategy: None}` API default, matches a
source-controlled fingerprint of a Tunnex raw-manifest schema. Unknown,
manually changed, or newer unannotated schemas fail closed before Helm applies
anything. The current CRD release and the known legacy `tunnex-operator` Helm
release remain valid owners. Operators must not run `helm rollback` on the CRD
release; ordinary operator rollback cannot touch CRD schemas.
Both operator charts require Kubernetes 1.29 or newer, where the CRD's CEL
admission semantics are stable; the gateway lifecycle keeps its separate floor.

The lifecycle CLI may install it through a separate explicit verb. The web UI
must never show the operator command until that real chart contract exists, and
must use exact supported value names thereafter. Operator registration and
later reconciliation carry provider/platform presentation only when the CR
explicitly supplies a valid pair; omission leaves the control-plane value
unchanged and never infers or resets it.

### D9 — Distribution is part of the feature

**LOCKED.** CI lints and semantically renders all four charts
(`tunnex-host-posture`, `tunnex-gateway`, `tunnex-operator-crds`, and
`tunnex-operator`) on every relevant change. A version tag packages the charts
with the exact app version, publishes them as OCI artifacts, attaches checksums
to the release, and publishes the operator image alongside the existing node
image. The CLI defaults to the version matching its own release and allows an
explicit local chart path for development.

A local chart that only works from a source checkout is not called customer-
grade or released.

Private pre-release qualification is a separate, non-shipping provenance
surface. `deploy/k8s-walk-candidate-package.sh` accepts only a clean committed
`HEAD` plus explicit digest references for every release image and writes one
local bundle. Its single bounded candidate scalar is stamped into the CLI and
the version/appVersion of all four deterministic chart packages; the manifest
binds that version, full source SHA, toolchain inputs, chart and CLI checksums,
and the six digest-only image references. The script accepts no credential and
performs no registry, Kubernetes, cloud, or control-plane mutation.

After an independently authorized private-registry publication preserves those
exact chart bytes and image digests, that bundle may establish the box-walk's
pre-release provenance. It is never called released, never changes the public
support claim, and never substitutes for the stable version-tag workflow before
customer release. A dirty checkout, mutable image tag, mismatched chart bytes,
or manually restamped artifact is refused rather than qualified.

#### D9a — Private candidate runtime version is bounded, not authoritative

**LOCKED (walk-blocker disposition 2026-08-31).** The enrollment and re-key API
contract caps `agent_version` at 50 characters. A private candidate therefore
defines `0.0.0-walk.sha<first-32-lowercase-hex-of-source-sha>` as its runtime,
CLI, Helm version, and appVersion scalar: 46 characters for every supported Git
object format. The 128-bit abbreviation is sufficient to distinguish bounded
private walk candidates, but it is never a provenance root and no consumer may
reconstruct or accept a full source identity from it.

The package script stamps the CLI and charts but accepts an already-built node
image digest; it cannot inspect or restamp that image. The separate node-image
build **MUST** pass the exact bounded candidate scalar through the Dockerfile's
`VERSION` input. Qualification **MUST** record that build input and, after
enrollment with no `TUNNEX_AGENT_VERSION` override, prove the control plane's
exact `agent_version` readback equals the manifest candidate version. The
bundle alone does not prove the embedded node version, and missing or
mismatched evidence leaves the candidate unqualified.

The manifest's complete `source.sha`, artifact checksums, and digest-only image
references remain the immutable provenance authority. Qualification and
publication **MUST** compare that full identity and **MUST** refuse the same
bounded candidate version paired with a different full source SHA as a
collision. This is an acceptance condition, not collision detection performed
by the local packager. Truncating the full manifest identity, weakening image
digest pins, or raising the API limit merely for a private packaging convention
is rejected.

### D10 — Ownership and restoration of broad host sysctls (HELD-1 resolved)

**LOCKED (founder-approved 2026-08-30).** A chart-owned privileged posture
manager runs once per eligible node as a DaemonSet. Before the first Tunnex
gateway owner mutates host posture, it atomically journals the exact original
values in a versioned hostPath record under Tunnex ownership. Its owner set is
made from live gateway Pod UIDs scheduled to that exact node, not an in-Pod
counter. While at least one owner exists it continuously reconciles
`ip_forward=1` plus `conf/all` and `conf/default` `rp_filter=0` while a gateway
is active. On last-owner removal it restores each journalled value only when the
live value still equals the Tunnex-owned desired value; an external change is
never overwritten. The journal update and owner-set update are crash-safe and
bounded, and the manager refuses mutation when it cannot prove its state.

Rejected alternatives remain: an operator-owned prerequisite would not be
zero-touch; deliberately leaving values changed for node lifetime violates
reversible lifecycle; guessed restoration and single-Pod reference counts are
not ownership proof.

### D11 — Crash cleanup for host-network data-plane artifacts (HELD-2 resolved)

**LOCKED (founder-approved 2026-08-30).** The same D10 per-node posture manager
is the crash authority for host-network data-plane artifacts. It reconciles an
authoritative live gateway Pod-UID owner set, prunes dead owners after API
readback, and on last-owner removal cleans only exact Tunnex-owned WireGuard,
nftables, route, and CNI artifacts recorded in the host journal. Cleanup is
bounded, idempotent, and fails closed on ambiguous ownership. The ordinary
gateway process still performs fast SIGTERM teardown, but its `defer` is an
optimization rather than the lifecycle authority.

The chart runs the connector in the
node network namespace. Normal SIGTERM can execute the process teardown, but
SIGKILL, container-runtime failure, and forced Pod deletion cannot. Therefore a
dead or rescheduled Pod can leave its WireGuard interface plus Tunnex-owned nft
and CNI rules on the old node; container-netns destruction is not a cleanup
authority for this deployment.

A Helm hook or process `defer` alone remains rejected because both are
bypassable.

#### D11a — Journalled staged-link ownership transaction

**LOCKED (founder-approved 2026-08-31, revised direction A; supersedes the
2026-08-30 alias-on-create mechanism).** The AKS wire run disproved the premise
that an `IFLA_IFALIAS` supplied with a WireGuard `RTM_NEWLINK` create is applied
atomically: the create succeeded but live readback returned an alias-empty
`wg0`. Tunnex therefore does not rely on alias-on-create on any Kubernetes or
cloud provider. The provider-neutral contract consumes only the Linux
rtnetlink API and the existing host journal.

The journal schema advances to v2 and makes creation a monotonic staged-link
transaction:

1. before link mutation, persist one bounded collision-resistant opaque
   Linux-safe staging identity/name, the fixed final name `wg0`, expected kind
   `wireguard`, expected ownership alias, node/epoch, and phase `planned`; the
   same baseline must have proved both names absent;
2. create only the uniquely named staging link, read back its exact name, kind,
   and positive ifindex, and persist that receipt as phase `created` before
   proceeding;
3. set the ownership alias on that existing staging link, read back the same
   name/kind/ifindex plus the exact alias, and persist phase `marked`;
4. only while final `wg0` is still absent, atomically rename the marked staging
   link to `wg0`; read back the same kind, alias, and ifindex under the final
   name and persist phase `committed`; and
5. only a committed receipt may unlock nftables, routes, CNI, sysctl ownership,
   or an active admission heartbeat. The staging link must then be absent.

The crash authority is the conjunction of the durable transaction record and
exact kernel readback; a staging name, alias, or ifindex alone is never
authority. Restart resumes only the next monotonic phase for that exact
transaction. An absent expected link may be recreated only from `planned`; an
unexpected final link, wrong kind/alias/ifindex, simultaneous staging and final
links, phase regression, malformed transaction identity, or any unjournalled
candidate blocks without marking, renaming, configuring, or deleting it. Last-
owner cleanup applies the same conjunction and removes only the exact staged or
committed link proved by that journal. The unmarked crash window is confined to
the opaque staging name: an alias-empty final `wg0` is never a normal
intermediate and is never adopted.

V1 compatibility is explicit and fail-closed. A v1 `preparing` journal never
executes link creation or marking. A v1 `active` journal may continue only
after read-only validation of its exact recorded final name, WireGuard kind,
ownership alias, and positive ifindex; it is not translated in place. Exact
last-owner cleanup remains bounded by that same recorded identity. A restored,
artifact-free v1 epoch may start a fresh v2 transaction only after the complete
D10/D11 baseline is clean. The observed failed-create shape — v1 `preparing`,
no recorded ifindex, and an alias-empty final `wg0` — remains ambiguous and is
never automatically marked, renamed, deleted, or migrated.

For this disposable pre-release qualification only, that ambiguous v1 node is
reset through a provider-managed full-node reimage or replacement after the
failed atomic Helm install has converged to absence. The provider operation
must remove the node-local journal and kernel state together, and the walk
restarts from a freshly proved Leg 0 baseline. Manually setting the alias,
deleting `wg0`, deleting the hostPath journal, editing a chart object, or
changing the PVC remains disqualifying and is not a customer recovery claim.

**DEFERRED TO S20.6 — host-reboot rebootstrap.** A normal node reboot can
preserve the hostPath journal while removing kernel-only WireGuard and nftables
state. S20.5 fails closed on that shape rather than silently resetting a
committed receipt or restoring stale sysctl originals. S20.6 must define a new
journalled rebootstrap epoch, recapture the post-boot baseline, and wire-prove
access recovery before Tunnex claims ordinary node-reboot self-heal. Trigger:
before any customer-facing claim that Kubernetes host posture survives an
ordinary node reboot. Manager/container process crashes and the explicitly
recorded disposable full-node fixture reset remain in S20.5 scope.

The rejected two-command fixed-name create/mark sequence remains rejected: a
crash leaves an ambiguous final `wg0`. A temporary/random name or ifindex
without the durable phased journal likewise remains rejected.

#### D11b — nft structural-handle readback

**LOCKED (founder-approved 2026-08-31, direction A).** `nft -a list chain`
annotates both the chain declaration and executable rules with handles. The
ownership validator accepts only the complete anchored readback shape: the
exact `ip` or `ip6` Tunnex table wrapper, one declaration of
`chain tunnex_posture_owner { # handle <positive-decimal>`, the sole exact
Tunnex counter marker rule with decimal packet/byte counters and one positive
handle, and the two structural closing braces. A missing or second wrapper or
declaration, extra brace, second or foreign rule, duplicate marker, extra
prefix or suffix, or malformed handled line fails closed before mutation.

This is a provider-neutral Linux nftables readback contract. It adds no cloud
SDK, provider label, CNI assumption, or provider-specific recovery branch.

#### D10a — Shared manager release ownership

**LOCKED (founder-approved 2026-08-30, direction A).** D7 requires gateway A and B to
remain independent Helm releases, while every object in the current gateway
chart is release-scoped. A DaemonSet inside each release creates multiple
privileged managers on one node; a shared fixed-name object cannot be safely
owned or removed by either independent release. `keep`, live `lookup`, and a
host lock merely orphan ownership and cannot prove the last cluster owner.

Ship a separate cluster-singleton
`tunnex-host-posture` chart/release which the CLI installs and upgrades once per
cluster under the fixed release `tunnex-host-posture` in namespace
`tunnex-system`. The chart refuses any other release identity; the CLI discovers
that identity cluster-wide and gateway releases in any namespace register live
Pod UIDs and assert its contract/readiness. Redesigning one gateway release to own multiple identities
remains rejected as a D7 rewrite. A per-release DaemonSet plus lock or orphaned
`keep` object remains rejected as unsafe lifecycle ownership.

#### D10b — Shared-manager eligibility and gateway placement

**LOCKED (founder-approved 2026-08-30, direction A).** The normal zero-touch
manager release is eligible on every Linux node and tolerates every taint. Its
privileged presence does not authorize mutation: the exact-node live gateway
Pod-UID owner proof from D3/D10 remains the only trigger for host posture or
artifact ownership. This avoids a circular install on dedicated or tainted
gateway pools and lets independent A/B releases select different pools without
rewriting shared-manager eligibility.

The CLI exposes separate, provider-neutral gateway node-selector and toleration
inputs. It validates and canonicalizes them, binds them into the redacted plan
and approval digest, passes them only to the gateway chart, and verifies the
exact live Deployment placement before claiming success. No cloud/provider
label is inferred. Applying one gateway's selector to the shared DaemonSet is
rejected because a later B selector could evict the manager from A's node; a
manual manager preinstall as the normal customer path is likewise rejected
because it breaks the one-command clean-cluster contract.

#### D10c — One writer for the manager-owned WireGuard reverse-path filter

**LOCKED (founder-approved 2026-08-31, direction A).** The live AKS walk proved
that setting `conf/default/rp_filter=0` after creating the staged WireGuard link
does not change the value already inherited by that interface. It also proved
that `hostNetwork` plus `NET_ADMIN` does not make the gateway container's
`/proc/sys` writable. These are provider-neutral Linux/Kubernetes facts, not an
Azure repair branch.

The cluster-singleton host-posture manager is the sole writer. After it proves
the exact journal-owned WireGuard alias and positive ifindex and publishes the
same link as `wg0`, preparation must set and read back
`net/ipv4/conf/wg0/rp_filter=0` before an active heartbeat. Every later manager
reconcile must verify and heal that exact value. The per-interface value is not
added to the broad baseline receipts: `wg0` did not exist before Tunnex, its
original value is therefore undefined, and exact last-owner cleanup deletes
the owned interface. A foreign, alias-empty, wrong-kind, wrong-name, or
wrong-ifindex link blocks before this sysctl mutation.

Gateway `k8snetprep` reads and verifies the per-interface value but never
writes it. It remains the owner of only the exact registered CNI marker rules.
Mounting writable host `/proc/sys` into each gateway, allowing both manager and
gateway to race as sysctl writers, or treating a strict readback as ready are
rejected.

Leg 5 may inject one bounded provider-neutral drift only after the exact
journal-owned `wg0` identity and active manager heartbeat are captured: a
short-lived privileged Kubernetes test Pod on that same disposable node writes
only `net/ipv4/conf/wg0/rp_filter=1`, then exits. The walk must observe manager
self-heal back to `0` without another mutation and delete the completed fixture
Pod. This is failure injection, not a remedy or customer install step; any
other sysctl, chart object, CNI, route, nftables, or WireGuard mutation remains
disqualifying.

**LOCKED (founder-approved 2026-09-01, existing direction A; live fixture
clarification).** The test Pod uses `hostNetwork: true` so the bind-mounted
`/proc/sys/net` resolves in the host network namespace that owns `wg0`; a
hostPath mount alone does not change the Pod's network namespace. It still
automounts no service-account token, mounts only host `/proc/sys`, and receives
no Secret, PVC, identity state, or gateway credential. A terminal setup attempt
whose log proves the target path was absent before the write and whose live
host readback stayed `0` performed no drift injection: record and delete that
exact failed fixture before one corrected replacement. Only one successful
`0 -> 1` injection is permitted.

### D12 — Upgrade and rollback provenance (HELD-3 resolved)

**LOCKED (founder-approved 2026-08-30).** Every lifecycle-created Helm revision
is stamped with exact contract `tunnex-zero-touch/v1` in both the Helm history
description and a fixed live Deployment annotation. Upgrade requires the
currently deployed revision description and live annotation to match before
any reuse of values. Rollback requires the target history description to match
before mutation and verifies the live annotation plus normal readiness after
mutation. A missing, malformed, or conflicting marker refuses; neither path
reads Secret bodies or legacy Helm values. A legacy release needs a separate
explicit token-blind migration and is not silently adopted by this story.

Helm can roll a release back to any
positive revision. A revision created before this lifecycle contract may lack
Kubernetes mode, host preparation, real probes, immutable inputs, and external
one-time-Secret transport. Upgrade currently uses
`--reset-then-reuse-values`; on a legacy release that can carry a raw
`joinToken` into the new Secret manifest and Helm history again. Deployment
rollout plus Service ingress is therefore not enough evidence that either an
upgrade source or rollback target is customer-grade and token-blind.

Version-only age gates remain rejected because chart age is weaker than exact
lifecycle provenance. Silently upgrading from or rolling back to an unproven
revision remains forbidden.

#### D12a — Provenance of a rollback-created revision

**LOCKED (founder-approved 2026-08-30, direction A).** Helm `install` and `upgrade`
accept `--description`; `helm rollback` does not and writes its own
`Rollback to N` history description. Rewriting Helm storage would require
decoding a release body which can contain legacy values and Secret manifests,
violating D12's token-blind boundary.

The CLI accepts Helm's native `Rollback to N` only when a bounded, cycle-safe
history walk proves revision `N` ultimately descends from an exact
`tunnex-zero-touch/v1` revision, then verifies the fixed live Deployment
annotation after rollback. A malformed target, missing history entry, forward
edge, cycle, or exhausted bound refuses before mutation. A separate render/apply
rollback engine remains rejected as a new release engine rather than a narrow
lifecycle fold.

#### D12b — Authoritative image and placement convergence

**LOCKED (founder-approved 2026-08-30, direction A).** A lifecycle upgrade may
reuse only the exact non-secret gateway inputs it can prove from the live,
contract-marked Deployment. Before approval, the CLI snapshots the canonical
gateway node selector, tolerations, image-pull Secret names, and current image;
the requested image replaces only that image in the approved target. Those
inputs are visible in the redacted plan and bound into its digest. A default
image is resolved from strict `helm show chart` name/version/appVersion
metadata for the exact local or OCI chart, with that metadata included in the
plan and rechecked after approval; unverifiable or changed metadata refuses
before token mint or Helm mutation. The Helm
mutation then sends complete authoritative image, pull-secret, and placement
value blocks, including explicit empty tag, digest, or pull-secret collections,
so `--reset-then-reuse-values` cannot resurrect a value from an older revision.
Success requires exact live Deployment readback of the approved target as well
as the existing provenance, readiness, endpoint, and state-identity checks.

The shared host-posture manager follows the same convergence rule, with its
fixed all-Linux/all-taints placement rather than gateway placement. Treating
chart defaults as an unverifiable wildcard, trusting Helm success without live
image readback, or requiring customers to rediscover and re-enter unchanged
provider-neutral placement on every upgrade remain rejected.

#### D12c — Immutable chart-byte provenance across approval

**LOCKED (founder-approved 2026-08-30, direction A).** Before printing a plan,
the CLI materializes each selected chart exactly once into a private temporary
`.tgz`: `helm pull` for an OCI reference and `helm package` for a local source
chart. It hashes the raw archive bytes with SHA-256, parses strict chart
name/version/appVersion metadata from that artifact, and binds the digest,
metadata, and operator-facing source reference into the approval plan. The
post-approval Helm operation consumes that same private artifact path; it never
re-fetches or re-packages a mutable source. Temporary artifacts are removed on
success, refusal, cancellation, and error without ever containing a join token
or Secret body.

The chart appVersion must be one exact OCI tag scalar and resolve only to the
fixed default node-agent registry/repository with no embedded registry,
repository, digest, slash, or oversized tag semantics. Rechecking only
Chart.yaml metadata before re-fetch, or requiring customers to supply only a
prebuilt immutable artifact and losing the local development path, remain
rejected.

#### D12d — Truthful chart-artifact cleanup failures

**LOCKED (founder-approved 2026-08-30, direction A).** Failure to remove the
private chart staging directory before any Kubernetes or Helm mutation is a
command error. Once an approved Helm install or upgrade has succeeded, the CLI
must not misreport that remote mutation as failed merely because local cleanup
failed; it emits an explicit stderr warning naming the exact private path and
remediation instead. The warning contains no join token, Secret body, or chart
contents. Silently discarding cleanup errors and reporting a completed remote
mutation as failed both remain rejected.

### D13 — Expired retained bootstrap-token recovery (HELD-4 resolved)

**LOCKED (founder-approved 2026-08-30).** The owned immutable bootstrap Secret
carries token-blind expiry and lifecycle claim metadata. On retry after expiry,
the CLI CAS-remints only after Kubernetes proves that the planned release,
owned Secret/claim, PVC, and connector identity have not progressed past the
retry-safe state and the control plane proves that no enrolled identity exists
for that exact lifecycle claim. The immutable Secret is replaced with UID and
resource-version preconditions; any conflict or ambiguous readback refuses.

An explicit typed `abort-install` recovery is also required. It names the exact
context, namespace, release, and lifecycle claim, presents the retained-state
impact before mutation, and requires typed confirmation. It CAS-cleans only
objects proven owned by the claim and dispositions any partial control-plane
identity before a fresh plan. Raw `kubectl delete secret` is never the customer
recovery path.

An atomic/preflight failure retains
the owned bootstrap Secret for bounded retry, while a control-plane join token
expires after one hour. Reusing that exact Secret forever eventually creates a
dead-end retry loop. Manual Secret deletion is not a zero-touch recovery path,
and blindly minting a replacement can create a second identity after a token
was consumed but readiness verification failed.

Blind reminting remains rejected because a consumed token plus failed readiness
can otherwise create a second identity. Manual Secret deletion is not an
accepted lifecycle operation.

#### D13a — Crash-consistent remint protocol

**LOCKED (founder-approved 2026-08-30, direction A).** The current token request and
node record carry only mutable `node_name`; there is no persisted immutable
lifecycle claim. A consumed node can therefore be renamed and an absence check
by name can wrongly authorize a duplicate remint. Kubernetes cannot atomically
replace an immutable Secret: delete-before-create has a crash gap, while a
fresh raw token is currently stored hash-only and cannot be redelivered after
that gap.

Persist an opaque lifecycle claim on token and node, add exact claim
status/remint/ack/abort operations, and keep an
idempotent sealed remint response only until Kubernetes CAS acknowledgement or
token consumption. Retry can recover the same bounded secret without
introducing a second live Secret name. Before the first mint, the CLI persists
the client-generated claim UUID, request ID, and expected generation in an
owned non-secret lifecycle-claim ConfigMap; this is the crash anchor when no
bootstrap Secret exists yet. The immutable Secret later mirrors only that
token-blind metadata. A sealed response is redeliverable only while its raw
credential is still unexpired. If it expires after control-plane mint but
before a durable Secret exists, the CLI proves the exact expired generation and
absence of a bound node, persists a fresh request ID and expected generation in
the anchor, and performs the normal generation CAS remint; neither side ever
persists or deploys the expired credential. Generation-suffixed Secret switching,
name-only proof, and a lossy delete/mint/create sequence remain rejected.

Migration 0130 follows the established EPIC-11 database contract: schema
movement is forward-only and an operational rollback restores a verified
pre-migration backup. Its down migration works while no lifecycle claim has
ever been persisted and otherwise refuses rather than discarding claim/token
provenance. A migration-owned usage sentinel is set transactionally by the
first claim-bearing token or node write and survives later token/node deletion;
the down migration locks writers before checking it, so live-row emptiness can
never erase the fact that the new protocol was used. The N-1 application remains compatible with the expanded nullable
schema; aborting a claim or deleting its Kubernetes anchor is not misrepresented
as permission to erase the durable control-plane record.

#### D13b — Mixed-version enrollment binding

**LOCKED (founder-approved 2026-08-30, direction A).** A rolling control-plane
deployment may temporarily serve both the lifecycle-aware application and its
N-1 predecessor. Compile compatibility from nullable columns is insufficient:
an N-1 replica can consume a lifecycle token and create its node without
copying the token's immutable claim, leaving recovery unable to identify the
enrolled node.

Migration 0130 therefore owns a private, transaction-scoped database bridge.
On the first consumption of a lifecycle token it records the exact token,
organization, pinned node name, and claim for that database transaction. The
following node insert must consume exactly one matching authorization: an N-1
NULL claim inherits the immutable token claim, a non-NULL mismatch is refused,
and the token is linked to the exact created node. A deferred constraint refuses
commit if a consumed lifecycle token is not bound to that exact node and claim.
Legacy tokens create no authorization and keep their existing behavior.

The bridge is keyed by database backend, transaction, and token identity; it
does not infer from a name, timestamp, latest row, or PostgreSQL row version.
This preserves provider-neutral rolling deployment without a lifecycle minting
outage. An all-at-once maintenance rollout remains rejected as a customer
runbook burden, and a fleet-wide replica-version activation protocol is
deferred as broader distributed coordination than this database seam requires.

#### D13c — Mixed-version lifecycle-route availability

**LOCKED (founder-directed 2026-08-30, direction A).** During an N/N-1
control-plane roll, an N-1 replica does not know the lifecycle status, remint,
acknowledge, abort, or D13h install-operation routes. Its OpenAPI validator
returns HTTP 404 with the
exact `validation_failed` / `no matching operation was found` route-missing
envelope. This is transport-route absence, not the domain
`lifecycle_claim_not_found`, and must never be interpreted as claim absence.

For lifecycle protocol operations only, the CLI retries that exact
route-missing signature on a fresh HTTP connection. It reuses the byte-exact
method, path, body, claim, request ID, and generation, with at most five total
attempts within five seconds and bounded backoff. Domain 404, authentication or
authorization failure, CAS conflict, any other validation response, 5xx, or a
transport ambiguity returns immediately without retry. Exhaustion reports a
typed `control_plane_rollout_incomplete` recovery error and retains all local
lifecycle state. A fleet activation/drain barrier remains rejected here as
broader distributed coordination than the database compatibility bridge.

#### D13d — Race-safe abandonment before the first mint

**LOCKED (founder-approved 2026-08-30, direction B).** A generation-zero
pending lifecycle anchor cannot be deleted on the strength of a control-plane
absence read: a concurrent installer could mint immediately afterward and
leave a token with no recovery anchor. Typed pre-mint abandonment first
CAS-transitions the exact owned Kubernetes anchor from `pending` to `aborting`.
The control plane then atomically creates a durable, credentialless
generation-zero aborted tombstone for the exact claim/request/node when the
claim is absent, or aborts the exact same-request generation-one successor if a
racing mint won. A different request, node, organization, generation, Secret,
release, mounted claim, or anchor fingerprint refuses.

Only an exact typed `aborted` response authorizes UID/resource-version-CAS
anchor deletion; any PVC remains for explicit state recovery. An installer
whose control-plane mint raced the abort cannot persist its response because
the anchor resource version/state changed, so it never creates a Secret. A
crash after the anchor fence or tombstone resumes the same abort idempotently;
an anchor-absent rerun reports completion only after proving the exact durable
tombstone and absence of release/Secret. New install/recovery attempts refuse
an `aborting` anchor and direct the operator to resume `abort-install`.

A CLI-only GET-absent/delete sequence remains rejected because it has an
uncloseable distributed TOCTOU. Generation-zero tombstones carry no usable raw
credential, bypass no licence or gateway ceiling, cannot be consumed by current
or N-1 enrollment, audit only their first transition, and make the
forward-only migration usage sentinel durable.

#### D13e — Race-safe abandonment after mint and before readiness

**LOCKED (founder-approved 2026-08-30, direction A).** Typed abort uses the
same exact UID/resource-version `aborting` fence for every abortable lifecycle
anchor state: generation-zero `pending` and positive-generation `pending`,
`issued`, or `acknowledged`. Immediately before creating the immutable
bootstrap Secret, an installer must prove the exact anchor UID, resource
version, generation, request, and non-aborting state. The Secret carries an
exact same-namespace owner reference to that anchor UID, and all lifecycle
readback validates it. This makes any residual read-to-create race
Kubernetes-GC-owned if abort deletes the fenced anchor; a read-only pre-create
check alone remains rejected because abort can fence and delete after the read.

Positive-generation abort/resume handles every crash point after the fence,
control-plane abort, Secret cleanup, and anchor cleanup idempotently. A stale
installer cannot persist, acknowledge, or retain a Secret after the anchor has
been fenced. Control-plane abort response validation accepts only the
canonical shortened expiry `min(previous expiry, aborted_at)`; accepting an
arbitrary expiry or demanding the pre-abort expiry after the server has
shortened it both remain rejected.

#### D13f — Abort-safe retained PVC identity

**LOCKED (founder-approved 2026-08-30).** Abort may retain an exact
Tunnex-owned lifecycle PVC in either `Pending` or `Bound` phase when its UID,
resource version, claim metadata, namespace, release, and unmounted status all
match the fenced lifecycle anchor. This allows an unschedulable
WaitForFirstConsumer install to be abandoned without deleting recoverable
state. A mounted PVC, a foreign or ambiguous owner, an unexpected phase, or an
identity mismatch refuses. Abort never deletes the PVC; `purge-state` remains
the separate destructive operation.

#### D13g — Enforced abort-before-purge ordering

**LOCKED (founder-directed 2026-08-30, direction C).** `purge-state` refuses while an owned
lifecycle anchor, bootstrap Secret, or non-terminal control-plane lifecycle
claim remains. The operator must first resume `abort-install` or complete the
normal uninstall path until the exact lifecycle metadata is terminal and the
cluster objects are absent. Only then may typed confirmation remove the exact
unmounted retained PVC. A documentation-only ordering warning remains rejected
because it permits irreversible PVC deletion while recovery credentials or a
live claim still exist.

Zero-touch CLI enroll-mode PVCs carry token-blind organization and
lifecycle-claim annotations sourced from the same approved anchor values, and
the CLI proves their exact live readback before lifecycle cleanup. Purge reads and
validates those annotations, resolves the organization, and requires the exact
control-plane claim to be terminal before deletion. Reuse never invents or
overwrites provenance on an existing claim. A PVC created before this contract
or by a direct/manual Helm install without lifecycle provenance has no provable
lifecycle binding and follows an explicit legacy path with a separate loud
acknowledgement; missing provenance is never silently treated as an absent or
terminal control-plane claim. Manual Helm compatibility is not represented as a
proof-bearing zero-touch lifecycle install.

#### D13h — Install-versus-abort linearization

**LOCKED (founder-approved 2026-08-30, direction A).** The control plane owns a
provider-neutral lifecycle install operation identified by an opaque operation
UUID and monotonically increasing epoch bound to the exact organization,
claim, generation, and request. `BeginInstall` is the linearization point
before Helm mutates the claim-bound gateway release. The credentialless,
cluster-wide host-posture manager remains a separately proved prerequisite and
does not consume, mount, abort, or complete this lifecycle claim. `BeginInstall`
returns a server-bounded absolute `not_after` deadline and the
anchor CAS-mirrors `installing`, operation ID, epoch, and deadline before any
Helm mutation. Repeating the same exact operation is idempotent; another holder
or stale epoch refuses.

**LOCKED (founder-approved 2026-08-31, direction A; expired pre-Begin
recovery).** A CLI may durably CAS-persist the exact operation UUID, release
scope, and install-intent digest in the lifecycle anchor, then crash before its
`BeginInstall` request creates the control-plane operation. If the join token
expires before retry, `BeginInstall` keeps lookup-first idempotency while
holding the exact lifecycle-token row lock: an existing exact operation is
returned even after token expiry. Only when the supplied operation is absent,
the immutable claim/generation/request tuple is still acknowledged,
unconsumed, unaborted, and has no bound node, the latest-operation lock proves
that no active, abort-requested, completed, taken-over, aborted, or otherwise
foreign operation owns the claim, and one database-clock value proves the
token expired, may the existing Begin route return typed HTTP 409
`lifecycle_install_operation_absent_after_expiry`. A clean released predecessor
without an abort request remains the only permitted predecessor. A failed
insert, generic conflict, application clock, or route/domain 404 is not this
proof.

Only that exact typed response, after D13c route retry, allows the CLI to enter
the no-operation recovery path. It re-proves the unchanged anchor UID and
resourceVersion plus organization, claim, generation, request, operation,
release scope, and install-intent digest; re-proves any owner-bound expired
Secret, exact Helm release/workload absence, and retained-PVC provenance and
mount absence; then CAS-retires the stale operation metadata while persisting
the next request identity before performing the ordinary expired-generation
Secret replacement and control-plane remint. Any changed, missing, foreign, or
ambiguous proof retains all recovery state and refuses. Kubernetes absence or
the CLI's local clock alone never authorizes retirement or remint.

The CLI runs Helm and post-install verification under a context whose hard
deadline cannot exceed `not_after`, sends bounded heartbeats, and cancels the
subprocess if the lease is lost or abort is requested. The approved Helm
timeout is itself bounded by that same deadline, so a paused stale holder
cannot begin a new mutation after its authority expires. Install completion is
accepted only for the active epoch, with no abort request, after the exact
claim is consumed and the exact release is ready; only then may it report
success and clean the Secret/anchor.

**LOCKED (founder-approved 2026-09-01, existing direction A; live-walk timeout
ownership clarification).** Helm's `--timeout` owns the ordinary atomic-install
timeout and its rollback/uninstall cleanup. The CLI must not cancel the Helm
process at that same instant: doing so can strand revision 1 as
`pending-install` before `--atomic` removes its workloads. The outer mutation
context may stop Helm only when the control-plane epoch is lost, abort is
requested, or the separately budgeted hard authority deadline wins. The hard
deadline remains the final bound; an ordinary Helm timeout is returned only
after Helm exits and its exact release/workload outcome is reconciled. If the
hard deadline wins first, recovery metadata stays durable for the existing
abort/takeover path. This ordering is provider-neutral and changes no token,
claim, chart, or Kubernetes ownership contract.

**LOCKED (founder-approved 2026-09-01, existing direction A; hook cleanup
clarification).** The chart's exact preflight hook is part of the gateway
workload absence invariant even though Helm does not retain hook resources in
the release manifest. Ordinary failed hooks use `hook-failed` cleanup. During
an abort takeover, after proving the exact zero-touch revision, the CLI may
delete only the canonical `<gateway-fullname>-preflight` Job whose live name,
UID, resourceVersion, release labels, component label, and Helm hook
annotations all match that release. Deletion uses UID/resourceVersion
preconditions with foreground propagation and waits for the Job plus its
controller-owned Pods to disappear before finalizing the control-plane abort.
Missing identity, an unexpected same-label workload, or foreign/malformed hook
provenance refuses; a selector-wide delete is never authorized. This is the
provider-neutral implementation of D13h's existing exact-workload
reconciliation rule.

**LOCKED (founder-approved 2026-09-01, direction A; release-journal-absent
hook provenance).** A canonical Job name, mutable Kubernetes labels, and Helm
hook annotations do not prove which zero-touch install created a stranded hook
after Helm's release journal is already absent. The install holder therefore
derives a non-secret canonical `sha256:` lifecycle-install proof from schema
version, lifecycle-anchor UID, organization, claim, generation, request,
operation UUID, holder epoch, requested duration, `not_after`, install-intent
digest, release namespace, and release name, and stamps that proof on the
preflight Job. This derived binding is not a credential and is excluded from
the canonical install-intent digest so the digest cannot become circular or
change when the anchor enters `installing`.

Abort takeover increments the holder's epoch exactly once. A proof created by
holder epoch E is eligible for cleanup only under an exact durable takeover at
epoch E+1 whose complete tuple matches the unchanged `aborting` anchor. The
CLI must re-read and revalidate that same anchor, including UID and resource
version, immediately before the UID/resourceVersion-preconditioned Job delete.
A missing, malformed, stale-operation, changed, or foreign proof refuses before
mutation and retains every recovery object. The already stranded disposable
walk Job predating this proof cannot be adopted retrospectively: its explicit
lab reset is recorded as pre-fix cleanup and never counted as zero-touch
success; the counted leg starts again with the proof-bearing candidate.

Abort atomically wins immediately when no install operation is active. If an
install epoch is active, abort records an exact durable request and does not
report success or delete recovery metadata. The active holder must stop, or a
takeover waits until holder release or the hard deadline, increments/fences the
epoch, aborts and revokes the exact control-plane identity, and reconciles the
exact Helm release and its workloads to absence before deleting the
owner-bound Secret and anchor. The PVC is always retained. Lost responses and
every crash point resume idempotently.

The invariant is bidirectional: successful abort means the claim is aborted
and no exact release, workload, Secret, or anchor can remain or later appear;
successful install means the claim is consumed and the exact release is ready.
The same lifecycle may never report both successes. A point-in-time anchor
read, a Kubernetes Lease without the control-plane epoch and hard deadline, or
permanent refusal of an `installing` anchor remain rejected.

#### D13i — Retained-state reuse-versus-purge fencing

**LOCKED (founder-approved 2026-08-30, direction A).** Reuse install and
`purge-state` share a provider-neutral Kubernetes Lease that serializes every
mutation of one retained PVC. The Lease is bound to the exact namespace,
release, claim name, and live PVC UID; its holder is an opaque operation UUID.
It is advisory only for this narrow retained-state boundary and does not replace
the D13h control-plane install epoch, abort ordering, or hard install deadline.

The Lease has a bounded lifetime and is renewed while Helm is running. A live
foreign holder refuses without mutation and is never deleted. A crashed holder
may be replaced only after expiry and a fresh exact reproof of the PVC binding
and operation preconditions; malformed, foreign, or differently bound Lease
objects refuse even when expired. Every create, renewal, takeover, and release
uses live UID/resourceVersion readback so stale holders cannot overwrite or
remove a successor's lock.

Reuse acquires the fence before its last retained-state read and before Helm.
It releases only after the exact claim is mounted by the ready release and the
post-install readback succeeds, or after a failure is freshly proven safe with
the release absent, the same PVC UID still present, and no live pod mounting
the claim. Ambiguous failures retain the fence until its bounded expiry.
`purge-state` acquires only after typed confirmation, then repeats release
absence, mount absence, exact PVC/provenance, and terminal control-plane claim
proofs under the fence before deleting the PVC by UID/resourceVersion. It
releases its own fence after a proved non-mutating failure or successful exact
deletion, and never removes a foreign live holder.

A point-in-time final check, delete preconditions on the PVC alone, or retrying
the check/delete sequence remains rejected: an existing-claim Helm install does
not mutate the PVC object, so UID/resourceVersion preconditions cannot detect a
concurrent reuse that starts between the purge check and delete.

#### D13j — Resume-stable install intent binding

**LOCKED (founder-approved 2026-08-30, direction A).** The D13h control-plane
operation is bound to a separate canonical `install_intent_digest`, not to the
display plan digest. The intent contains only immutable approved mutation
inputs: exact organization and lifecycle claim/generation/request, Kubernetes
context/namespace/release, gateway chart artifact SHA-256, image and
control-plane endpoint values, placement, Service/WireGuard settings, and the
storage/provenance intent. Maps and sets are normalized before canonical JSON
hashing. The lifecycle anchor and control plane store the resulting
`sha256:<hex>` value.

The CLI prints this digest inside the ordinary review plan, then recomputes it
and requires exact equality on every pre-Helm recovery before beginning or
resuming the same operation. Live observations such as anchor state,
resourceVersion, Secret/PVC phase, release status, or operation heartbeat do
not change it; changing any mutation input does. The ordinary display plan
digest remains a separate snapshot of live observations and approval text.
Reusing an old digest with newly resolved chart or values, rebinding an active
operation to a newly computed display digest, or making crash recovery
permanently refuse after the anchor becomes `installing` remain rejected.

#### D13k — Anchorless completed-install replay

**LOCKED (founder-approved 2026-08-31, direction A).** A successful install may
durably complete its control-plane install operation, CAS-delete the consumed
bootstrap Secret and lifecycle anchor as required by D2, and then lose the
final CLI response. Deletion remains correct: Kubernetes retains no success
tombstone and the CLI never recreates a Secret or anchor merely to report an
already completed install.

The control plane exposes authenticated, token-blind
`GET /api/v1/organizations/{orgId}/nodes/lifecycle-claims/{claim}/install-operations`
(`getLatestNodeLifecycleInstall`) on the existing Begin collection. It returns
the latest operation status for that immutable claim; typed domain absence is
`lifecycle_install_operation_not_found`, distinct from D13c's route-missing
envelope. A `completed` latest operation is terminal and prevents a later
remint, abort, or install epoch, so it is the durable replay authority after
Kubernetes cleanup removed the operation ID. No new provider branch, lifecycle
table, or Kubernetes tombstone is introduced.

When `tunnex k8s install` finds exactly one release and retained PVC but neither
bootstrap Secret nor lifecycle anchor, it may report idempotent success without
mutation only after all of these proofs agree:

1. the PVC is the exact Helm-owned, retained Tunnex state claim for the selected
   context, namespace, and release; carries canonical organization and
   lifecycle-claim provenance; and keeps the same UID, resourceVersion, and
   bound volume across approval and execution;
2. the selected organization and requested node name match the exact
   control-plane claim, which is consumed and bound to one node with the same
   generation and request returned by the operation;
3. the latest operation is `completed`, matches the claim, generation, request,
   namespace, and release, carries a canonical operation UUID, positive epoch,
   canonical install-intent digest and completion timestamp, and contains no
   abort, release, takeover, or aborted terminal metadata;
4. the exact zero-touch Helm release, Deployment, Service endpoint, and mounted
   PVC are ready, and the workload rollout revision derives from that completed
   install-intent digest; and
5. a final re-read still proves the same release/PVC identities and that no
   bootstrap Secret or lifecycle anchor appeared.

This replay performs no token mint, remint, acknowledgement, Begin/Complete
mutation, Helm operation, Kubernetes write, PVC change, or cloud/provider API
call. A missing, non-completed, foreign, malformed, changed, route-ambiguous, or
otherwise inexact proof refuses without mutation. A sanitized Kubernetes
tombstone remains rejected because it would contradict D2's delete-after-
success contract, create a second durable source of lifecycle truth, and still
require control-plane validation or a new signing/retention/garbage-collection
protocol.

### D14 — Desktop enrollment refuses a multi-organization guess

**LOCKED (founder-approved during the 2026-08-31 live walk).** The walk found a
measured correction to S12.5: the desktop polls routed ranges for a persisted
device's exact organization, but fresh managed enrollment still selected
`GET /organizations[0]`. A user who belonged to the clean walk organization
and an older organization therefore had no supported way to enroll this Mac in
the clean organization. Re-enrollment silently returned to the older tenant.
The server, policy grant, Kubernetes Service, and gateway were already ready;
the client discarded the tenant choice before device creation.

The desktop adopts the same explicit-selection law already locked for
`tunnex k8s install`:

1. zero organizations refuses as `no_organization`;
2. exactly one live membership is selected automatically; and
3. multiple live memberships require an explicit organization selection before
   helper installation, gateway lookup, or device creation. Index order is
   never a tenant decision.

Selection is main-process state, keyed by normalized control-plane origin and
the authenticated user ID, then revalidated against a fresh organization list
before every fresh enrollment. The renderer receives only organization ID,
name, slug, and selected state; it never receives the bearer or a WireGuard
configuration. The tray uses the same preflight. If selection is missing or
stale it performs no helper/device mutation, brings the client window forward,
and leaves the choice visible instead of swallowing the failure.

An already stored managed device remains bound to its persisted `orgId`, which
is the authority for status, revocation, health, routes, and DNS. Selecting a
different organization never moves that credential, reuses its key, or deletes
it silently. The existing confirmed **Remove device** flow must revoke and
clear the current managed device first; only then may the user select another
organization and perform the ordinary one-time enrollment. Imported profiles
remain separate and unchanged. Supporting simultaneous managed profiles for
multiple organizations is a separate product shape and is not introduced by
this walk fix.

**LOCKED (founder-approved 2026-09-01, direction A; stored-device human-owner
binding).** Organization binding alone is insufficient on a shared desktop:
the encrypted managed-device record also persists the authenticated human user
ID returned by the exact create response. Fresh enrollment verifies that the
server-returned device owner equals the user who passed the organization
preflight before the record is stored or the helper can be armed. A bound local
owner mismatch refuses before helper installation, status/mode mutation,
monitor startup, revoke, or local clear. An inconclusive ownership read never
turns a foreign credential into a usable one.

An older ownerless managed record may acquire an owner only after one complete,
successful live proof finds that exact device in its recorded organization and
proves the current authenticated user owns it. A foreign, absent, malformed,
ambiguous, or incompletely scanned record remains encrypted and refuses without
revoke or clear. Legacy records without an organization similarly require one
exact current-user match across every live membership before the existing
revoke-first migration may clear them. The confirmed **Remove device** path
uses the same owner proof. Once the local owner matches, the existing
same-owner fail-static behavior for transient status reads remains unchanged.

The renderer receives the exact envelope `{ organizations,
enrollmentLocked }`; each organization row contains only `{ id, name, slug,
selected }`. A stored managed record keeps `enrollmentLocked=true` even when
its organization is no longer a live membership, and a locked view never
persists that organization as the newly signed-in user's selection. The
renderer therefore cannot falsely offer a tenant switch that main will later
refuse, and no internal `enrolled` row flag crosses the preload bridge.

### D14c — Managed desktop lifecycle has one credential-fenced owner

**LOCKED (founder-approved 2026-09-01, direction A; app-lifetime serialized
lifecycle).** Every managed desktop transition is owned by one Electron-free,
app-lifetime `ManagedLifecycleCoordinator`. Its FIFO mutex serializes connect,
disconnect, login/logout, device removal, server/profile changes, and monitor
mutations. A monotonically increasing epoch invalidates all earlier work.

Each operation captures one exact credential snapshot — server, token,
fingerprint, and expiry — resolves the authenticated user through an API pinned
to that token, and carries the resulting lease through the operation. Current
means both the same epoch and byte-identical credential fields. The token
remains main-process-only and is never logged or sent across preload. Dynamic
bearer callbacks and app-global requested user, organization, or tunnel-mode
fields are forbidden.

Connect proves the fixed user, organization selection, and stored-device owner
before helper installation or tunnel mutation, then advances the epoch and
starts monitors carrying that lease. Remove proves ownership before
invalidating old monitors, stopping the tunnel, revoking, or clearing. Login
replacement invalidates first, stops every monitor, brings the old tunnel down,
and only then may save the new credential; a still-valid session refuses
replacement. Every revocation, approval, health, and routed-range mutation
enters the same mutex and no-ops when its captured lease is stale.

The singleton is created beside the stores in `main/index.ts` and injected into
`registerIpc`. `ipc.ts` owns the serialized transitions; `HttpDeviceApi`
accepts a fixed token; `TunnelController.up` receives an invocation-local
configuration provider; `deviceconfig.ts` and `orgselection.ts` receive the
lease's proven user and organization; monitor effects are epoch-guarded.

Deterministic Electron-free reds prove: FIFO operations never overlap; epochs
never repeat; changing only the token invalidates a lease; A-to-B login records
invalidate/stop/down before save-B; interleaved A/B connects never mix user and
organization; failed owner proof performs zero helper or monitor work; failed
remove proof performs zero stop/down/revoke/clear; successful removal orders
proof before revoke before clear; a token change during legacy discovery makes
no revoke POST or clear; and stale monitor callbacks perform zero server,
helper, store, or notification mutation.

**Rejected alternative B:** add scattered before/after `currentUserId` checks
while retaining mutable bearer callbacks, independent IPC handlers, and global
request fields. Those checks leave time-of-check/time-of-use windows, cannot
order login against helper or revoke work, and cannot make an already in-flight
monitor callback belong to the current session. Renderer `busy` state is also
not serialization: the tray and untrusted preload callers bypass it.

## Explicit non-goals

- Cloud account credentials or AWS/Azure/GCP SDKs.
- Cluster, VPC/VNet, subnet, public IP, firewall, NSG/security-group, load
  balancer, CSI driver, or StorageClass creation.
- CNI configuration-object mutation through Kubernetes/cloud APIs.
- GKE Autopilot or restricted-PSS support; they honestly refuse the current
  host-network/NET_ADMIN posture.
- Automatic HA pool creation/enablement, promotion, failback, or SLA changes.
- Automatic rotation/deletion of an enrolled identity.
- Cluster-scope access, multi-port exposure, or new GitOps CR behavior.
- Live EKS/GKE/self-managed support claims before their own wire legs.

## Acceptance and stop condition

### Static and unit proof

- Values schemas reject the old nested token shape, ambiguous enroll/reuse
  state, invalid Service modes, and mutable/empty image selection where an
  immutable release is required.
- Helm renders prove probes, exact placement, projected token isolation, the
  separate shared host-posture manager and credentialless gateway admission,
  state retention/reuse, optional consumed Secret, automatic LB discovery
  inputs, unique A/B releases, and operator RBAC/CRDs.
- Node tests cover journal-before-mutation host preparation, every v2 staged-
  link crash boundary, monotonic phase resume, same-ifindex mark/rename
  readback, v1 exact-active read-only continuation, v1 alias-empty failed-create refusal,
  manager-process restart, last-owner restoration, gateway rollout preservation, and
  every CNI result: not-applicable, ready, blocked, controller rewrite self-
  heal, CIDR replacement, withdrawal, and foreign-rule preservation.
- CLI tests use fake `kubectl`/Helm executors and prove stable redacted plans,
  no token in argv/output/Helm values, sole-org vs multi-org behavior, atomic
  install, failure retention, success deletion, readiness, rollback, retained
  uninstall, guarded purge, and redacted diagnostics. They also prove bounded
  cluster-wide singleton discovery, fixed manager RBAC/probes/placement,
  authoritative image-value clearing, exact gateway image/pull-secret/placement
  readback, and expired sealed-response rotation before any Secret is written.
- Database tests run migration 0130 against PostgreSQL and prove current and
  N-1 enrollment bind the exact lifecycle claim transactionally, legacy tokens
  remain unchanged, cross-transaction authorization cannot bleed, aborts do
  not leave bridge state, and a populated down migration refuses without data
  loss.
- Web DOM tests prove the old invalid commands are absent and every shown
  command names a real released surface.

### Live AKS wire proof

Start with no Tunnex Secret, PVC, Deployment, host patch, CNI patch, final
WireGuard link, or staged WireGuard link. A disposable node replacement used to
discard the specifically identified ambiguous v1 failed-create state must be
recorded before this baseline and earns no product-recovery credit. Then prove,
without any mutation outside the lifecycle/UI except the exact D10c and D7a
failure injections:

1. plan → one-command A install → connected exact node, with a v2 journal
   created before the staging link, exact staged mark/readback, same-ifindex
   rename to `wg0`, committed receipt, and no staging residue;
2. one-command B install with distinct identity/state;
3. cluster registration, pool configuration, HA enablement;
4. desktop client → private Service access;
5. pod delete/reschedule preserves node ID/cert/WG key and access;
6. strict host `rp_filter` and CNI controller rewrite heal automatically;
7. digest-pinned upgrade and Helm rollback preserve identity/access;
8. default uninstall retains state and explicit reuse reinstalls without a
   token; and
9. GitOps operator chart install plus one real CR reconcile; and
10. exact A and B Deployment scale-subresource failures produce automatic
    A→B and B→A ownership and fresh-request client recovery without a manual
    promotion.

Any `kubectl edit/patch` of a Secret/PVC/CNI object, manual sysctl/nft/route or
WireGuard mutation outside D10c's exact one-shot drift fixture, direct
Deployment restart used as a remedy, Deployment scale outside D7a's exact Leg
9 injection, or source hot-patch makes the leg **INCONCLUSIVE**, never passed.

The story stops after local gates, multi-finder review, and the committed-during-
walk evidence are ready for founder review. It does not commit, push, deploy,
publish, open a PR, or merge without a separate explicit instruction.
