# S14.2 — LAYOUT SHELL: NAV · RESPONSIVE GRID · BREAKPOINTS

**COMMIT-ONE PROPOSAL. NOTHING BUILT. Awaiting the founder's ruling.**

Imports **no generated types** (EPIC 14 slice-order rationale), so it still cannot conflict with S13.1.

## THE TWO DECIDE-ITEMS ARE ANSWERED — carried in as rulings

**1. MOBILE IS A TRIAGE SUBSET, not the full dashboard.**

| on mobile | surfaces |
|---|---|
| **full function** | gateway/fleet health · the device-approval queue · revoke |
| **READ-ONLY, with an honest line saying so** | everything that COMPOSES a policy — the access-rule builder with source, destination, port scope, expiry |

**Rationale, recorded:** a rule builder on a phone is a security surface where **a mis-tap grants access**, and
a bad one is **worse than none**. **Never a degraded editor** — read-only and honest about it.

**2. DENSITY IS CUT — as CUT-WITH-A-SEAM, not dropped.** Five widths × three themes is enough; at mobile width
*compact* and *cozy* are the same decision made twice. **Build the spacing scale so density COULD return; ship
one density.**

---

# D1 — THE BREAKPOINT SET, AND WHAT EACH IS *FOR*

**Named by what the user can DO at that width, not by device.** A pixel value is an implementation detail; the
capability is the decision.

| name | from | **what it is FOR** |
|---|---|---|
| **triage** | 0 | **Read state and act on queues.** One column. Approve, revoke, read health. **Composition is read-only here.** |
| **compose** | 768 | **Everything triage can do, plus authoring** — the rule builder becomes available, single-column forms. The first width where a policy may be composed. |
| **operate** | 1024 | **Multi-panel.** The full labelled nav rail; side-by-side detail + list. |
| **wide** | 1440 | **The wireframe's native design width.** Full dashboard grid, the layout the artifact was drawn at. |
| **max** | 1920 | **Clamp, do not stretch.** Content max-width holds; the extra space is margin. |

**The `compose` boundary at 768 is the load-bearing one** — it is where a security capability turns on, so it
is the only breakpoint whose value is a decision rather than a taste. **The others may be tuned freely; this one
is ruled.**

**And it is a FLOOR, not a guess about tablets:** a 768px viewport is the narrowest width at which a
source/destination/port/expiry form can be shown without collapsing fields into an order that invites a mis-tap.

---

# D2 — NAV BEHAVIOUR AT EACH WIDTH

The wireframe's nav is grouped: **NETWORK · ACCESS · OBSERVE · OPERATE · SETTINGS · OTHER SCREENS**. That
grouping is preserved at every width; only its *presentation* changes.

| width | nav |
|---|---|
| **triage** | Off-canvas drawer behind a menu button, **plus a persistent bottom bar carrying the triage set** (health · approvals · devices). Group headers retained in the drawer. |
| **compose** | Collapsed **icon rail**, labels on focus/hover, groups as separators. Drawer still reachable. |
| **operate**+ | **Full labelled rail** with group headers, exactly as drawn. |

**⛔ THE RULE THAT CONSTRAINS ALL OF IT: responsive may RE-ARRANGE, never REMOVE.** Every nav destination is
present in the DOM at every width. A link hidden by CSS is **invisible, not absent** — still announced to a
screen reader, still keyboard-reachable — and this epic has already ruled that shape a failure (the
theming-vs-edition rule). **Presentation collapses; the link set does not.**

**The one deliberate exception is composition**, and it is the opposite: those controls are **ABSENT** below
`compose`, not hidden. See D3.

---

# D3 — READ-ONLY ON MOBILE, EXPRESSED **ONCE**

## The shape: **width → CAPABILITY → render decision.** Never width → CSS visibility.

```
useLayoutCapability()  ->  { canCompose: boolean, … }
        │
        └── <ComposeGate>  ── canCompose ──> the editor
                            └─ else ──────> the read-only view + the honest line
```

**ONE component, one hook. Screens declare *what* they compose; they never ask *how wide* the window is.** A
screen that reads a breakpoint directly is the per-screen duplication this decide-item exists to prevent — and
it is the form the drift would take.

## WHY IT MUST BE ABSENCE, NOT `display:none`

**If the rule builder were merely hidden by CSS at triage width, it would still be in the DOM** — focusable,
announced, submittable. **A control that grants access, present to a keyboard and gone only to a sighted mouse
user, is the exact failure the epic's theming-vs-edition rule names: rendered INVISIBLE rather than ABSENT, a
security-adjacent surface failing open.**

**So `ComposeGate` does not render the editor at all below `compose`.** The honest line replaces it:

> *"Access rules are read-only on this screen size. Open Tunnex on a laptop or tablet to edit them."*

**Same seam, same reasoning, three consumers now:** edition (does it exist for this licence), capability (does
it exist at this width), and theme (what does it look like) — **the first two are RENDER decisions, the third
is a style, and they never merge.**

---

# D3b — THE TOPBAR, AND TWO FINDINGS IN IT

The wireframe's topbar carries: search · Select · Cozy/Compact · FREE/ENTERPRISE · ADMIN/USER · theme dots ·
notifications · help · org switcher · user menu.

**FINDING 1 — THE LIVE NAV COUNTS ARE A DATA DEPENDENCY, not decoration.** The nav renders `Gateways 3/7`,
`Sites 1 DOWN`, `Devices 3`, `Access Policies ENFORCING`. **Those are live values on the SHELL**, which means
the shell — rendered on every screen — acquires a data dependency that no screen owns.

**Consequences to rule at this commit-one:** which endpoint feeds them · what they render **before** it
resolves (**never a stale or defaulted count** — that is the `loadOne` law on the most-seen surface in the
product) · and what they render **when it fails**. **Proposal: absent until loaded, and absent on failure —
never `0`, never a remembered number.** A nav badge reading `0 DOWN` because a fetch failed is the
reassuring-empty defect in the one place every user looks.

**FINDING 2 — FREE/ENTERPRISE and ADMIN/USER ARE READ-ONLY BADGES, NOT TOGGLES** (founder-ruled). They are
wireframe demo controls: **a user cannot switch their own edition or role.** Rendering them as toggles would
offer a control that either does nothing or, worse, looks like a privilege the product does not grant.

**The edition badge READS THROUGH THE ONE GATING SEAM** — the same hook that decides whether enterprise
surfaces exist, never a second source. **The role badge reads the resolved role from the roster**, which is
where `Users.tsx` already gets it.

---

# D4 — HOW "RESPONSIVE" IS GATED, GIVEN QUERY RULE 4

**Rule 4 says no test may assume a viewport.** That is not an obstacle to gating responsive — it forces the
gate into the only place it is meaningful anyway.

**AND THERE IS A HARD TECHNICAL REASON: jsdom HAS NO LAYOUT ENGINE.** It does not evaluate media queries,
compute widths, or lay anything out. **A "responsive test" that rendered components in vitest and asserted
layout would be asserting nothing** — it would pass at every width because there are no widths. That is a
check that cannot fail, and this repo has a law for it.

## THREE LAYERS, and none of them assumes a viewport

**1. THE BREAKPOINT DECISION IS A PURE FUNCTION — unit-tested, no DOM.**

```ts
layoutIntent(width: number) -> "triage" | "compose" | "operate" | "wide" | "max"
capabilityFor(intent)       -> { canCompose: boolean, navMode: "drawer"|"rail"|"full" }
```

Tested exactly like `policyHealthBadge` and `crossesMultiSiteThreshold` are: **boundary values on both sides of
every threshold**, including `767 → triage` and `768 → compose`, which is the security boundary.

**2. THE COMPONENT TIER IS UNCHANGED AND MUST STAY WIDTH-BLIND.** Its tests assert decisions, never layout, so
they pass at any width **because they never mention one**. **If a component test ever needs a viewport to pass,
that is the finding** — it means a decision leaked into presentation.

**3. THE RESPONSIVE CONTRACT — the assertion that actually protects users.** Render each shell at each
`capability` (injected, not measured) and assert:

- **the nav link set is IDENTICAL across all capabilities** — responsive re-arranges, never removes
- **composition controls are ABSENT at `triage`** — asserted by role, so a `display:none` implementation FAILS
  this test. That is deliberate: **the test distinguishes "hidden" from "absent", which is the whole point**
- **the read-only line is PRESENT whenever the editor is absent** — never a blank space where an editor was

**Mutation proof, as always:** implement the gate as `display:none` and assertion 2 must go red; drop a nav link
at `triage` and assertion 1 must go red.

## WHAT IS *NOT* GATED HERE, AND SHOULD BE SAID

**Actual visual layout at real widths is NOT gated by this slice.** No screenshots, no headless-browser
viewport runs. That is honest scope: it would need Playwright with real viewports, which is `e2e`'s territory
and a different budget.

**PROPOSED: register a visual/responsive e2e leg with a trigger** — the first screen slice (S14.4) — rather
than pretending unit tests cover it. **The capability boundary is gated mechanically; the pixels are gated by
eye until that leg exists.**

---

# WHAT S14.2 DELIBERATELY DOES NOT DO

- **no screens** — it is the shell; screens are S14.4+
- **no command palette / toasts / density** — S14.3, and density is cut
- **no generated-type imports** — preserves the no-conflict property with S13.1

# OPEN QUESTIONS FOR THE FOUNDER

1. **Bottom bar at `triage`, or drawer only?** A bottom bar makes the triage set one tap away — which is the
   whole point of the on-call subset — but it is a second nav surface to keep in sync with the drawer. Proposal:
   **bottom bar**, precisely because the triage ruling says those three surfaces are why mobile exists.
2. **Does `wide` (1440) differ from `operate` (1024) in more than column count?** The wireframe is drawn at
   ~1440 with a 1280 floor. Proposal: **same structure, more columns** — no layout that exists only at one
   width, since that is a screen nobody tests.
3. **The honest read-only line — one wording for every gated surface, or per-surface?** Proposal: **one
   wording**, parameterised by surface name, so it cannot drift into fourteen slightly different sentences.

---

# THE THREE OPEN QUESTIONS — ANSWERED ON MY OWN RECOMMENDATION, PER THE APPROVAL

None was load-bearing, so each was built on the proposal rather than blocking the slice.

1. **BOTTOM BAR AT `triage` — BUILT.** `TriageBar` renders only when `navMode === "drawer"`, carrying
   `/dashboard`, `/devices`, `/access`. **It is DERIVED from `NAV_DESTINATIONS`, never re-listed** — a
   hand-written second copy is precisely how two nav surfaces drift apart, and the contract asserts the bar is a
   SUBSET of the real destination set so a bar-only destination fails.
2. **`wide` DIFFERS FROM `operate` IN COLUMN COUNT ONLY.** Same structure, more columns; `max` **clamps** to
   `wide`'s count rather than stretching. A layout that exists at exactly one width is a screen nobody tests,
   and the ordering assertion (`columns` never decreases, `max === wide`) makes that mechanical.
3. **ONE WORDING, PARAMETERISED.** `readOnlyByWidthMessage(surface)`. Asserted to name the surface **and to
   name the way out** — "read-only" without "open it on a laptop" is a dead end, not an explanation, and leaves
   the user guessing whether the restriction is their role or their window.

# WHAT WAS BUILT

| file | what it is |
|---|---|
| `apps/web/src/lib/layout.ts` | width → intent → capability, **pure**, no DOM |
| `apps/web/src/components/ComposeGate.tsx` | `LayoutCapabilityProvider` (measures ONCE, at the app edge) · `useLayoutCapability()` · `ComposeGate` |
| `apps/web/src/components/AppShell.tsx` | `NAV_GROUPS` (NETWORK/ACCESS/OBSERVE/OPERATE/SETTINGS) · `SidebarNav` with drawer/rail/full · `TriageBar` |
| `apps/web/src/pages/Access.tsx` | **the first consumer** — the rule builder behind the gate |
| `apps/web/test/layout.test.ts` | 17 tests, layer 1: boundaries on both sides of every threshold |
| `apps/web/test/responsivecontract.test.tsx` | 14 tests, layer 3: parity + absence-by-role |

**Access.tsx is deliberately wired now rather than in S14.4.** A gate with no consumer is dormant machinery,
and this epic has already ruled dormant machinery cannot be proven.

**THE ORDER OF THE TWO SEAMS IS THE DECISION:** `canManage && <ComposeGate>`. **PERMISSION FIRST, WIDTH
SECOND.** A member who may not manage rules sees nothing — never "read-only on this screen size", which would
imply a wider window grants what their role does not.

## The closed drawer IS absent, deliberately — and that is not a contradiction of "never remove"

An off-canvas panel whose links stay in the accessible tree is a **keyboard trap**: tab order walks
destinations the user cannot see. So the closed drawer is genuinely absent, and **the invariant it must satisfy
is that OPENING it yields the same destination set as the widest rail.** The contract clicks the menu button at
`triage` and compares the sets, so a destination dropped from the narrow build fails there.

# ⚠ FINDING — THE ABSENCE ASSERTION WAS CHECKING A DIFFERENT THING THAN ITS COMMENT CLAIMED

**Caught by mutation 1, which is the only reason it was caught at all.**

The test was written as `queryByRole("button", { name: /add rule/i })`, with a comment asserting that
`queryByRole` **finds** a `display:none` element and would therefore fail a hidden-not-absent implementation.

**THE CLAIM IS FALSE.** testing-library defaults to `hidden: false` and runs `isInaccessible`, which jsdom
evaluates against inline styles — so a `display:none` element is **EXCLUDED** from the query, `queryByRole`
returns `null`, and the assertion **passes**.

```
MUTATION 1 (gate reimplemented as display:none)  ->  test PASSED  ->  the guard did not cover the behaviour
```

**The test asserted "not in the ACCESSIBLE TREE" while its comment claimed "not in the DOM"** — and those two
differ on **exactly the failure mode being guarded against**. It belongs to the
ASSERTS-A-DIFFERENT-EVENT-THAN-IT-WAITS-ON family already in `docs/laws.md`.

**FIXED:** `{ name: /add rule/i, hidden: true }` searches the whole DOM regardless of visibility. Re-run under
the same mutation: **1 failed, 13 passed — the guard now rejects.**

**The sharpest part: the false claim was written IN the comment explaining why the assertion was rigorous.**
Prose confidence is not evidence; the mutation was.

# MUTATION PROOFS — three, one at a time, each preceded by `scripts/mutate.sh --self-test`

| # | mutation | result |
|---|---|---|
| **1** | `ComposeGate` reimplemented as `display:none` | **first: PASSED (finding above)** · after the fix: **1 failed / 13 passed** |
| **2** | drop the OBSERVE group globally | **5 failed / 9 passed** — parity red at every capability |
| **3** | drop OBSERVE **only in the drawer** (the actual defect the law forbids) | **exactly 1 failed** — the `triage` parity assertion, and only it |

Mutation 3's scaffold (a `dropForNarrow` prop) was **reverted, not shipped** — a prop that exists only to be
mutated is dormant machinery.

`mutate.sh` needed `BUILD_CMD` overridden for TypeScript; its default `go build ./...` fails at repo root and
was reported as "THE MUTATION DOES NOT COMPILE", which is the tool refusing correctly rather than a defect.

# GATE — `make web-gate` (typecheck + vitest + build — NOT Playwright; e2e runs in CI only) GREEN

Typecheck + **284 tests (22 files)** + production build, in the Node-20 container that mirrors CI.
`A SLICE IS NOT GREEN UNTIL make web-gate PASSES` — and vitest in a developer shell is not that gate.

# BRANCH TOPOLOGY — the one thing that was NOT a free choice

`story/S14.2-layout-shell` is cut from `story/S14.1-design-system` **with `story/web-component-tests` merged
in**. The responsive contract is a COMPONENT test, and neither the tier (PR #44) nor the tokens (PR #45) is on
`main` — a branch cut from `main` could not have run D4's proof at all. **This is not a new decision: the
approved commit-one specifies a contract that runs in the tier, so the branch has to carry it.** It does mean
**S14.2 cannot merge before #44 and #45.**

Three merge conflicts, all benign: two were the **same finding written twice in different words** (the tsconfig
test-scope fix, the duplicate `ruleRow` import) — the tier's fuller wording kept, the TS6305 note carried over.
`docs/laws.md` was both sides adding **different** laws at the same insertion point; **both kept, neither
replaced.**

# CI GREEN AT `ebf1447` (PR #46, draft, 2026-08-01)

**Recorded with the sha and every job, per `GATE-REPORT-NEEDS-SHA`. Re-earned by any further commit.**

| workflow | jobs | conclusion |
|---|---|---|
| **CI** | `gates` · `client (macos-latest)` · `client (windows-latest)` · `e2e` · `e2e-enterprise` | **all pass** |
| **Security** | `gofmt + vet parity` · `govulncheck` ×5 · `CodeQL (blocking on high/critical)` ×2 · `Trivy` ×2 | **all pass** |
| **Security** | plain `CodeQL` (the code-scanning aggregate) | **FAIL — see below** |

**All three REQUIRED checks pass** (`gates`, `client (macos-latest)`, `client (windows-latest)`).

# ⚠ HELD FOR DISPOSITION — the red `CodeQL` aggregate is the WIREFRAME, not product code

**Five alerts, all in `docs/design/TUNNEX-wireframe-v2.html` (since RENAMED — see the ruling below). ZERO in `apps/` or `packages/`.**

| severity | rule | count |
|---|---|---|
| high | `js/xss-through-dom` | 1 |
| medium | `js/cross-window-information-leak` | 4 |

Exactly the "1 high, 4 medium" the aggregate reports. **Both BLOCKING CodeQL jobs pass**; the failing check is
the code-scanning aggregate, which is not in branch protection's required set.

**PROVENANCE, and a correction to an earlier report.** The file entered at `5b2f4f7` on
`story/web-component-tests`. **PR #44 was green at `00a736d` — true at that sha — and the wireframe landed
after it, so #44 is RED at its current HEAD too.** #45 is clean (the file is not on that branch). #46 inherits
it purely by being stacked. **The wireframe is a reference artifact: never built, never served, never imported
by any bundle.**

**NOT FIXED — this is a decide-item, not a fold.** Narrowing a security scanner's scope is exactly the change
that gets dispositioned rather than decided mid-build. Ranked:

1. **Path-exclude `docs/**` from CodeQL** — honest scope statement, but silences the scanner for a directory
   permanently. *(recommended, scoped to `docs/**` only)*
2. **Dismiss the five alerts individually** — per-alert and auditable, no scope change; recurs if the file changes.
3. **Leave it red** — ranked last: a permanently failing security check trains everyone to ignore a red
   security check, which is worse than either fix.

## ⚖ RULED — NEITHER OPTION. **RENAME THE ARTIFACT.** (founder, 2026-08-01)

`docs/design/TUNNEX-wireframe-v2.html` → **`TUNNEX-wireframe-v2.html.txt`**. **CodeQL classifies by EXTENSION**,
so the misclassification is removed **at its source**: nothing excluded, nothing dismissed, and the scanner
keeps **full scope over everything that ships**. The file's header and the registration both say to copy it out
and rename the copy to `.html` to view it.

**WHY NOT (1), in the founder's words:** path-excluding `docs/**` is **broader than the problem and permanent**
— *"a guard-shaped hole nobody looks at again."* **This repo's own history is the argument:** `.gitignore`'s
unanchored `secrets/` silently kept `apps/api/internal/secrets` **OUT OF GIT** — fine on the machine that wrote
it, **broken on every fresh clone**.

**WHY NOT (2):** dismissals are honest but **recur**, and five "won't fix" clicks **train the same reflex as a
standing red**.

**The ranking of "leave it red" last was upheld, and for the stated reason.**

**FALLBACK, PRE-COMMITTED: if the rename does NOT clear the aggregate, fall back to (2) — per-alert dismissal —
and SAY SO, rather than reaching for the path exclusion.**

# STILL OPEN AFTER S14.2

- **A visual/responsive e2e leg is NOT covered here** and was never claimed to be. The capability boundary is
  gated mechanically; **the pixels are gated by eye** until that leg exists. **TRIGGER: the first screen slice
  (S14.4).**
- **Merge order is forced:** #46 cannot merge before #44 and #45.


# ⚖ THE RENAME WORKED — RE-VERIFIED, AND VERIFIED BY THE ALERT LIST RATHER THAN THE CHECK COLOUR

**CI GREEN AT `b6bc5c1` (PR #46). EVERY check passes, `CodeQL` aggregate included.**

```
code-scanning/alerts?ref=refs/pull/46/merge&tool_name=CodeQL  ->  (none)
```

**Zero, where it previously returned five.** The aggregate moved `failure` -> `neutral` -> `pass` as the
analyses re-ran. **The pre-committed fallback to per-alert dismissal was NOT needed, and is recorded as not
needed** rather than left ambiguous.

**Checked the ALERTS, not the badge.** A green check is a claim about a scan; the alert list is the scan. Given
this slice's own finding — an assertion that passed while not asserting — reading the badge alone would have
been the same mistake one layer up.

**PR #44 re-asserted in the same way at `598f133`.** Merge order unchanged: **#46 after #44 and #45.**
