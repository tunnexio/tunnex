# Separate entry gateway for the fenced connector pool

2026-09-05: user approved using the existing third worker for the entry
gateway required by the live HA bootstrap topology. No additional EC2 host.

Install fresh release `tunnex-s205-edge` from unchanged immutable candidate
61ecc5f on worker `i-0afe3c3ecd00d1411`, hostname
`ip-10-240-10-204.ap-south-1.compute.internal`, public 65.2.179.105,
NodePort UDP31083. Bind it to `S20.5 AWS EKS sandbox`, outside the A3/B2
connector pool, and select it through normal hub/device operations.

Retain A3/B2 endpoints and pool membership. Exact worker-SG ingress only:
client /32 to31083; edge public /32 to31081 and31082; A3/B2 public /32
to31083 for the peer path. Existing client-to-A/B rules remain until the
walk ends; do not broaden CIDRs, expose health ports, or remove old resources.
Apply through the controller CloudFormation stack with reviewed exact changes.

Normal CLI enrollment, UI binding and CP reconciliation must establish
authority. No database authority edits, manual WireGuard keys, promotion,
host repairs or remedial restarts. Failed prerequisites remain failures,
never proof of HA. Current public addresses are session endpoints, not a
host-replacement stability claim.
