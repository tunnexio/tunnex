# S8.7 — CIDR-source /32 precision + expired-grant conntrack flush (commit-one)

Decision-first paper. Branch `story/S8.7-cidr-conntrack`, stacked on `story/S8.6-hub-ha` (base `842cfb3`).
EPIC-8's last story. Two coupled features: (1) `src_kind='cidr'` grant-source precision (the S8.1 enum
seam's next customer), (2) the expired/revoked-grant conntrack flush that terminates established flows the
ACCEPT-rule removal alone leaves alive. Founder-ruled D1–D6 (carried from the ratified-but-void S8.7
commit-one gate — the RULINGS stand; the code + gate re-earn on this real base).

## Decide-items (D1–D6 ruled; D4 mechanism the one open item, this paper's verify)

**D1 — CIDR source, org-validated (CONFIRMED).** `src_kind='cidr'` (dst symmetry, resource-refs where they
exist). The CIDR is **org-validated at rule creation** — it must be meaningful in the org's address world
(inside an approved site subnet, a declared routed range, or the device pool). An out-of-world CIDR compiles
to a rule matching nothing — a silent no-op wearing a rule's face (the reassuring-rule class). **WARN, not
refuse (RATIFIED):** the pre-staging case (an admin writing rules for a range about to be declared — the
order RouteLAN encourages) makes refusal the wrong grain. A rendered warning (`cidr_outside_org_ranges` on
the rule ROW, not just at creation) keeps the reassuring-rule class visible for the rule's whole life. **The
honesty condition:** the warning derives from CURRENT org ranges at READ time — a rule out-of-world at
creation and in-world after the range lands SHEDS its warning without edits. Red proves both directions
(warning appears, warning clears). The `saddr` emission REUSES the site-src path (one-emitter discipline —
"mirrors proven machinery" made structural), never a parallel CIDR branch.

**D2 — no version bump (CONFIRMED).** The `LocalSubnets`/`DNSForwards` precedent holds: an old agent missing
a narrower allow is missing a PERMISSION — fail-closed-safe. Both faces papered: a CIDR-source grant is an
Allow entry an old agent simply doesn't apply (denies more, never less) → no `RequiredVersion` bump.

**D3 — modal source dropdown (CONFIRMED).** The Access modal's source dropdown gains Resource/CIDR; the
free-text fallback is right (resources are a convenience, not a gate). The D1 validation refusal/warning
renders verbatim per the typed-message convention (the warn-badge on the rule row).

**D4 — scoped conntrack flush on expiry AND deletion, never blanket (CONFIRMED). MECHANISM VERIFY (this
paper, before build):**
- **The reserved seam's real shape:** `flowlog.VerdictTerminated` + `accesslog.DecisionTerminated` + the
  `RuleID` binding on `nodepolicy.AllowEntry` (`flowlog/event.go:20-27`, observability-only, EXCLUDED from
  `CanonicalHash`). A READY CONTRACT — the "terminated" verdict + the revoked grant's RuleID. The kill code
  itself is UNBUILT (grep: no `conntrack -D`, no CT-netlink call anywhere). The agent's enforcement is
  **shell-out** (`egress_linux.go`: `exec.CommandContext(ctx, "nft"...)`, `"ip"...`), NOT direct netlink.
- **Presence-in-image (the deployment truth):** the zero-touch gateway image `deploy/docker/node.Dockerfile:17`
  installs `ca-certificates wireguard-tools iproute2 nftables` — **NO `conntrack-tools`.** A shelled
  `conntrack -D` would SILENTLY NO-OP on every real gateway (the REPORTED≢STORED packaging-tier trap the D4
  ruling names). The shell-out pattern is safe ONLY for binaries the image actually has (nft/ip are present);
  conntrack is not.
- **THE PICK — netlink-via-Go (RECOMMENDED; HELD for founder disposition, per "answered before build"):**
  the packaging verify is the decider. Two paths:
  - *(a) shelled `conntrack -D`* — consistent with the nft/ip pattern, BUT requires ADDING `conntrack-tools`
    to `node.Dockerfile` (a RUNTIME binary dep the compiler cannot verify — a future image rebuild dropping
    it silently disarms the security fix; drift with no guard).
  - *(b) netlink-via-Go* — the flush lives IN the agent binary (compile-verified, no image change), a
    filtered CT-netlink dump+delete scoped to the removed rule's tuple (src/dst/proto). Reuses the
    CAP_NET_ADMIN the agent already holds (nft/wg/egress). Adds ONE Go dep (a CT-netlink lib on
    `mdlayher/netlink`, already an indirect dep). Typed errors, not CLI-output parsing.
  - **Recommendation: (b).** The consistency argument for (a) fails at the packaging tier — shell-out works
    for present binaries, and conntrack is provably absent; (b) removes the unverifiable runtime dependency
    and binds the security fix to the compiled artifact. **HELD for the founder's mechanism ruling before
    the flush is built.** Failure mode (either path): a flush error is LOGGED + surfaced (the rule is still
    removed; the lingering flows are the pre-existing behavior — degraded-not-broken, NEVER silent).

**D5 — one function, two triggers (CONFIRMED).** The scoped flush rides the SAME code path for the
grant-expiry sweep AND manual rule deletion (the sweep gains the flush step; the delete handler calls the
same function). The founder's exact scenario is the walk leg: ping running → grant expires → ping stops
within the sweep interval.

**D6 — device-revocation EXEMPT (CONFIRMED, verified).** Device revocation = PEER REMOVAL
(`devices/service.go:496`) — a crypto-layer stop (the tunnel dies), EFFECTIVE, needing NO conntrack semantics.
The vulnerable class is grant-expiry / rule-deletion where the peer STAYS (device-grant removed but device
connected; site-grant removed but sites still peered). One line, proven not assumed.

## Slice cut
1. **enum + compiler + validation** — `src_kind='cidr'` on the S8.1 enum seam; `saddr` emission mirroring the
   site-src path (one emitter); org-range validation (warn-not-refuse, read-time-derived `cidr_outside_org_ranges`).
   (VOIDED belief-only gate re-earns here — Slice 1 was never built.)
2. **conntrack flush** — the D4 mechanism (pending ruling) on ONE shared function, expiry-sweep + manual-delete
   triggers; flush-failure logged+surfaced; reds: live-flow-dies · innocent-neighbor-survives · delete=expiry.
3. **modal UI** — Resource/CIDR source options, warn-badge rendering, typed messages verbatim (consumes Slice 1).

Story-end review → walk-ready @ sha → the batch-day manifest.

## Red family (D4/D5)
- **live-flow-dies:** a flow crossing the grant's expiry is torn down within the sweep interval (scripted
  conntrack state if the harness can't hold a real flow).
- **innocent-neighbor-survives:** same gateway, DIFFERENT tuple, alive after the flush — the scoped-not-blanket proof.
- **delete-triggers-same-as-expiry:** the manual-delete path flushes identically to the expiry sweep (one function).

### SLICE 2 BUILD OUTCOME — conntrack flush (netlink-via-Go), agent-side
Architecture (a refinement of D5's CP-side phrasing, forced by substrate): conntrack lives on the GATEWAY, so
the flush is AGENT-SIDE — the egress Manager, on a successful enforcing apply, diffs the new Allow set against
the last-applied one (removedTuples) and flushes the established flows of any grant that LEFT. This IS D5's
"one function, two triggers": expiry-sweep and manual-delete both reach the agent as a re-fetched artifact
with the grant absent — the agent is agnostic to WHY it left (TestFlushWiringOnRemoval). No new CP protocol.
- **netlink-via-Go** (D4 ratified): github.com/florianl/go-conntrack (on mdlayher/netlink, consistent with
  the existing florianl/go-nflog). NO conntrack-tools image dep — the flush is in the agent binary,
  compile-verified. The scoped CT dump+delete (conntrack_linux.go).
- **THE SECURITY INVARIANT — scoped to the exact tuple:** matchesTuple requires src ∧ dst ∧ (proto) ∧
  (dst-port) all to hold; a flow differing in ANY dimension SURVIVES. TestMatchesTupleScoped is the
  innocent-neighbor centerpiece — proven by SURVIVAL, not the filter's appearance.
- **flush runs OUTSIDE the status lock** (a netlink dump must not block status reads): captured under mu at
  apply-success (pendingFlush), drained by Reconcile after the lock releases. After the nft apply (the ACCEPT
  is already gone), so the flush can't race a re-accept.
- **CAP_NET_ADMIN reuse:** cited — the agent already holds it for nft/wg; conntrack.Open reuses it. Absence
  (some deployment shape) → the logged-and-surfaced path (TestFlushFailureSurfacedNotSilent), rule removal
  unaffected, NEVER silent (slog conntrack_flush_failed + Manager.flushErr).
- **Reds (green):** TestMatchesTupleScoped (innocent-neighbor) · TestRemovedTuplesDiff · TestFlushWiringOnRemoval
  (delete=expiry, exactly-the-removed-tuple) · TestFlushFailureSurfacedNotSilent (CAP-absent surfaced).
- **SUBSTITUTE (named):** live-flow-dies against a REAL kernel conntrack is the BOX-WALK leg (ping running →
  grant expires → ping stops within the reconcile interval); the unit reds SUBSTITUTE it — trigger = the S8.7
  walk. **RIDER:** the VerdictTerminated flow-STREAM event push (the reserved seam's ready contract) — the
  flush's surface is the structured log + flushErr; pushing the event into the flow-log stream rides the
  flowlog-sink threading (the buffer is pump-owned, not Manager-held). Register: S8.7 VerdictTerminated-emit.

### SLICE 2 DELTA — the four gaps ruled against 513f1ec (the acceptance described a richer Slice 2 than shipped)
The advisor's "acceptance" restated ruled conditions as delivered features. Reconciled + dispositioned:
- **GAP 1 (health-plane kind) — BUILT.** flushErr + slog was a log line; the ruling said SURFACED, on the
  health plane where every degradation lives. New health kind `conntrack_flush_unavailable`: agent
  Manager.ConntrackFlushFailing() (flushErr != nil, cleared on a successful flush) → control.PolicyStatus →
  CP AppliedPolicy/Capabilities → degradedKind (LOWEST priority — surfaced only when policy is otherwise
  synced; masked by any louder desync/apply/link fault) → the policy_degraded_kind enum → the web badge
  ("expiry-flush degraded", warn). Reds: TestDegradedKind (surfaced-when-synced + masked-by-desync) ·
  egress ConntrackFlushFailing set-on-failure + CLEAR-on-recovery · healthview badge.
- **GAP 2 (CapEff) — RULED reactive.** No proactive /proc read: an EPERM from the netlink op IS the
  CAP_NET_ADMIN-absent evidence, and (with gap 1) that failure surfaces as conntrack_flush_unavailable.
  "Verified-not-assumed" is satisfied by the reactive path. Red: TestFlushFailureSurfacedNotSilent (simulated
  permission-denied → the health-failing state fires, rule removal unaffected).
- **GAP 3 (reply-direction) — RULED: orig-tuple-only matching is CORRECT (documented design decision).** A
  conntrack entry is per-flow (orig+reply tuples, one record) — deleting by orig match kills BOTH directions
  of flows initiated under the revoked grant. A flow whose orig runs the other way (B→A) was authorized by a
  DIFFERENT grant and MUST survive; matching reply tuples would over-delete, violating innocent-neighbor from
  the opposite side. Red pinning it: the reply-direction (B-initiated) survivor in TestMatchesTupleScoped.
- **GAP 4 (library) — ACCEPTED: florianl/go-conntrack.** The ratification was mechanism-level (netlink-in-Go,
  no image dep); the specific lib was the advisor's fabrication (not vishvananda/netlink). Pinned v0.7.0,
  vendored via go.sum, on mdlayher/netlink (already present), consistent with the existing florianl/go-nflog.

### STORY-END REVIEW FOLD — 18 findings dispositioned (bounded cluster BUILT; [6] HELD)
Review over 842cfb3..ecf1f2f found 18 verified (0 refuted). Founder-dispositioned. Bounded cluster folded:
- **[0]+[9] — ONE REDUCE (the placement predicate).** cidrPlacementSite(cidr, siteCIDRs, siteNode) — a cidr
  places IFF its containing approved site subnet has a bound gateway. ONE function: the compiler bails where
  ok=false (a source that can't originate adds NO dst/hub ACCEPT — the [0] default-deny bypass closed AND
  site-src symmetrically tightened), the read-time warning fires where ok=false (the [9] node-less silent
  no-op closed). warn <=> won't-place by construction, BOTH directions. Reds: TestCIDRSourcePlacement
  Biconditional (in-world-places / [0] out-of-world->dst-site emits NOTHING / [9] node-less places nothing) +
  TestCIDRWarnShedsWhenRangeLands (warn appears out-of-world / stays for node-less site / sheds when subnet
  AND gateway land). LAW: warn<=>place biconditional — a warning and its compilation derive from ONE predicate.
- **[7]+[11]+[17] — flushTuples redesign.** Per-flow Delete errors + per-family Dump errors SURFACE via
  errors.Join (never a silent skip reporting healthy, [7]); familiesOf dumps ONLY the families the tuples
  span ([17]) so a v6-less kernel can't false-degrade on an all-v4 removal ([11]). Red: TestFamiliesOf.
- **[1] latch fix.** drainFlush on a no-removal pass, when flushErr is set, reactively PROBES conntrack
  capability (probeConntrack open+close) and CLEARS on recovery — the kind tracks current truth. Red:
  TestFlushLatchClearsOnProbe.
- **[3] priority inversion.** conntrack_flush_unavailable moved BELOW apply_failing/stuck_enforcing in the
  open-edition switch (lowest — a louder fault is never masked).
- **[5] migration down honest.** 0039 down DELETEs cidr rows before the reverted CHECK (a feature rollback
  drops the feature's rows loudly, not a wedged constraint violation — the 0037/0038 precedent).
- **[15] FOLD.** PolicyRuleCidrWarnings takes the already-fetched rules (no double ListPolicyRulesByOrg).

**[6] HELD for a FRESH HEAD (the one dangerous piece).** The restart flush baseline (appliedAllow memory-only
-> revoke-while-down leaks forever after restart). FIX = dump-and-reconcile at the first post-restart apply.
ARGUE (one line): dump-and-reconcile is the full-sweep sibling (no persistence, no staleness file) BUT the
innocent-neighbor constraint binds HARDER at restart — a sweep flushing "flows matching no current Allow"
must be SCOPED to our governed tuple-space (wg pool + site subnets), or it kills every legit flow; persisted-
baseline is simpler but adds a file with staleness. Lean: dump-and-reconcile with governed-space scoping —
but it is the most-dangerous new code (a restart-time conntrack sweep, outage-class if the scope is one
predicate wide), so per tail-of-turn it builds at a session head with its own undiscounted red family
(restart-with-live-legitimate-flows survives / restart-with-stale-flow dies / unrelated-flow survives).

### RE-REVIEW REDUCE — the flush/health STATE MODEL (paper-first, one build)
Re-review over ecf1f2f..2a2bf6f: bounded cluster CLEAN; 5 defects in the restart-sweep/health path (RE1-5).
Budget-rule reduce (2nd defect round → paper the state model, reduce not patch). STATE MACHINE (the spine):

**The restart sweep is a PENDING OBLIGATION, not an event.** It exists from agent start and discharges only
on VERIFIED SUCCESS with preconditions met. Two states, no third:
- **pending** — the obligation exists; preconditions unmet OR the last attempt failed. Every enforcing apply
  pass re-attempts if preconditions hold.
- **discharged** — one successful sweep ran against a KNOWN pool. Terminal.
- (No "attempted", no "skipped".)

**Precondition (RE3):** wg0 up with the pool known. A sweep judging governed-space WITHOUT the pool is
fail-open BY CONSTRUCTION (a device flow's src falls outside governed → survives) — so the obligation WAITS,
never runs blind. **Latch (RE1):** startupSwept moves at sweep-SUCCESS only, never at apply-time;
restartSweepPol persists until discharged, so a failed sweep re-attempts next pass. **Health:** flush-failures
OR pending-past-threshold → conntrack_flush_unavailable; discharged + a real probe passing → clear.

Four parts:
- **RE4+RE5 → ONE flush primitive** `sweepConntrack(families, selector)` — open + per-family dump + per-flow
  delete + errors.Join + count, ONCE. flushTuples (removed-tuple selector, families = familiesOf tuples) and
  reconcileFlush (governed+unmatched selector, families = familiesOfPrefixes governed) become SELECTORS. The
  family-scoping drift that caused RE4 is structurally impossible (one fix surface). Red: the v6-less fixture
  runs through BOTH callers.
- **RE1 → retry-until-discharged.** Reds: sweep-fails-then-succeeds (obligation persists across the failure,
  discharges + kills the revoked flow on retry) · the latch never moves on a failed attempt.
- **RE2 → the probe IS a scoped Dump.** probeConntrack does a single IPv4 conntrack Dump — cheap (one netlink
  round-trip at the reconcile cadence) but REAL: a seccomp/ENOBUFS block that would fail the flush also fails
  the probe (open-only was the false-green). Red: dump-blocked-but-socket-opens → the kind STAYS raised.
- **RE3 → the precondition gate.** The restart sweep pends until wg0/pool is up. Red: enforcing-apply-before-
  wg0 → pends (no fail-open judgment); wg0 arrives → sweep runs → the revoked flow dies. THE LIVE-PATH proof
  (cited): the ordinary removed-tuple flush is TUPLE-scoped (src = the removed grant's own src, a device /32
  or site subnet), NOT governed-space-scoped — it can never run "pool-blind", so the precondition is a
  restart-sweep-only concern (structurally, not by omission).

**OUTCOME (build 1 — 999eaf7, SUPERSEDED).** ONE primitive `sweepConntrack`; flushTuples + reconcileFlush thin
selectors (RE4/RE5). `probeConntrack` a real IPv4 Dump (RE2). Manager gained a `poolSource` + `restartPendingSince`;
the restart sweep stored a policy in `restartSweepPol` (a PENDING OBLIGATION), discharged on success. A final
targeted verify over 999eaf7 (high-effort, 16-agent) found this shape produced a THIRD defect round — see below.

## Round-3 HALT → reduce-by-REMOVAL of state (the S8.4 precedent)

The verify over 999eaf7 found 7 distinct findings (2 CONFIRMED), root: the stored obligation stored a policy and
executed it LATER under a moved world (F1), plus an IPv4-only assumption threaded through the new probe/precondition
(F2-F5). Per the budget-rule escalation the founder ruled a REDUCE, not a fourth patch. The obligation OBJECT is
DELETED; the restart sweep becomes a MODE-LESS boolean.

**The reduced state model (built — the shape that replaces RE1-5's obligation).**
- `bootSweepDone bool` + a THIS-PASS transient `pendingRestart *restartSweep{allow, localSubnets}` — no stored
  policy, no pending-error plumbing, no synthetic probe. `restartSweepPol`/`restartPendingSince`/`probeConntrack`/
  `ctProbe`/`errRestartSweepPending`/`restartSweepPendingThreshold` all DELETED.
- **Each apply pass** (apply + same-tick drain — proven co-located at Reconcile:300): enforcing ∧ ¬bootSweepDone →
  capture THIS pass's `pol.Allow`/`LocalSubnets`; the same-tick drain runs it against a KNOWN pool. Success →
  bootSweepDone=true. Pool unknown → the pass is DROPPED (transient consumed, never carried); the NEXT enforcing
  apply re-captures its OWN current policy. **F1's staleness is structurally impossible — nothing is stored to go
  stale.** (RE1's retry preserved without an obligation object.)
- **Mesh/off apply → bootSweepDone=true, discharged-as-MOOT** + pendingRestart=nil. Mesh permits everything, so a
  revoked-while-down flow has nothing to sweep; a later enforcing transition diffs from the live mesh baseline via
  the ordinary `appliedAllow` path. **F1's transition case DISSOLVES rather than being "cancelled."**
- **F4:** pending NEVER writes `flushErr`. A wg0-that-never-came-up is the tunnel's own health; the sweep silently
  waiting on it is correct, not a conntrack degradation. Root cause = reported cause, restored.
- **F2/F5 (RE2 probe DELETED):** recovery is observed on REAL work — `conntrack_flush_unavailable` clears on the
  next SUCCESSFUL actual flush (any caller of the shared primitive). No synthetic Dump, no probe cadence, no probe
  cost. Per-family error accounting stays (a joined v6 failure keeps the kind raised even when v4 succeeded). The
  trade — the kind may over-persist when no flushes occur — is the standing OVER-REPORT preference (annoyance
  heals, silence doesn't).
- **F3 (answer by citation):** the pool model is IPv4 by construction — `ipalloc.prefix`/`GatewayCIDR` reject
  non-v4 (`!p.Addr().Is4() → ErrBadCIDR`, survey-confirmed). A v6-only wg0 is not a supported shape today. Named
  trigger: a v6-pool-model story inherits this gate's family-parametrization.
- **F6 (FIX in the shared primitive):** the selector returns `(label, match)`; a failed delete NAMES the flow
  (`rule <id>` for the diff flush, `flow <src>-><dst>` for the boot sweep). Grant identity restored.
- **F7:** the restart branch now nil-guards `poolSource` too (symmetric with the ordinary path's `ctFlush==nil`).
- **Refuted (1):** `restartPendingSince` dead-after-discharge — moot, that field is GONE in the reduce.

**Reds (reduced model):** `TestRestartSweepStateMachine` (3 rules: pool-unknown-waits-no-health / pool-up-fails-
retries-against-CURRENT-policy / success-latches-terminal — asserts the sweep runs against THIS tick's policy,
not a stored one) · `TestBootSweepDischargedMootOnMesh` (F1's exact scenario: enforcing-pends → mesh-discharges-
moot → wg0-up → NO sweep EVER runs, zero live flows torn down) · `TestPartialFlushFailureKeepsKindRaised` (F2/RE5
per-family accounting: kills never mask a joined error) · `TestFamiliesOfPrefixes` (RE4 second caller). The probe
latch-clear red is DELETED with the probe.

**Pre-agreed escalation (on the record):** if THIS shape produces a FOURTH defect round, the restart-sweep ALONE
(not the live expiry/delete flush, which is proven + founder-walked) defers to S8.7b with the revoked-while-down
window as its named limitation — the terminal answer exists so round four is a one-message close, not a debate.

**Gates:** test-node GREEN (full node suite), node build GREEN. Diff is node-only (`apps/node/internal/egress`);
build-editions + generate-check byte-identical to 999eaf7 (green there), API/web untouched.

## Round-4 → escalation FIRED: boot restart-sweep DEFERRED to S8.7b (founder-ruled A)

The final verify over c978931 found a FOURTH-round CONFIRMED regression (F-r4): the mesh/off discharge latched
`bootSweepDone=true` on the COLD-START `pol==nil` deny-all apply (runs every restart BEFORE the first policy
fetch), so the first enforcing policy skipped the boot sweep entirely — defeating it on every cold start. Root: the
mode-boundary taxonomy again (genuine-mesh vs cold-nil vs enforcing) — the exact grain every round tripped on.

Founder RULED A (the pre-agreed escalation fires mechanically — a tripwire that yields to a sympathetic story is a
debate, not a tripwire; and the "one-line guard" needed a `policyReceived` distinction = new state on round four,
the seed of round five). **The boot restart-sweep machinery is REMOVED WHOLE** — `bootSweepDone`, `pendingRestart`/
`restartSweep`, `ctReconcile`/`reconcileFlush`, `shouldReconcileFlush`, `governedSpace`, `originSrc`/`inGoverned`,
`poolSource`, `conLabel`, and all their reds (TestRestartSweep*/TestBootSweepDischargedMootOnMesh/TestFamiliesOfPrefixes).

**KEPT (proven, founder-walked):** the LIVE expiry/delete flush — `removedTuples` → `flushTuples` diffs the new
applied allow-set against the last, flushing a grant's established flows when it leaves WHILE THE AGENT IS UP; the
shared `sweepConntrack` primitive with per-family `errors.Join` accounting; the F6 delete-error label (`rule <id>`);
the `conntrack_flush_unavailable` health kind on real flush failures with successful-flush (probe-less) recovery.
[4] cleanup FOLDED: the label is built only on a match (a removed-grant flow being deleted, a small bounded set) —
alloc ACCEPTED with a comment, not a lazy thunk (comparable closure cost).

**S8.7b REGISTERED** — conntrack reconcile across a broken enforcing baseline. Decision-first, its own state-model
paper before code, inheriting all four rounds' lessons as commit-one input (the mode-boundary taxonomy is where every
round tripped). **NAMED LIMITATION (in this paper + the eventual docs), honestly bounded — the general form is a grant
that leaves the enforcing set across a gap where NO continuous applied baseline spanned the change, so the removed-set
diff can't see it. Two concrete cases:** (1) a grant revoked/expired while the gateway agent is DOWN — its already-
established flows stay alive after agent restart; (2) a grant revoked during a mesh (ZT-OFF) interlude while the agent
is UP — an org toggling ZT OFF→ON drops the enforcing baseline (appliedAllow→nil), so a flow opened during the mesh
window to a destination the re-enabled policy no longer grants is NOT flushed on re-enable. In BOTH, the flow lives
until it ends naturally. **NEW connections are denied immediately** (the forward chain enforces on the first packet);
**device revocation is unaffected** (peer removal is crypto-death); and the STEADY-STATE case (a grant leaving while
ZT stays continuously enforcing) IS flushed today by the live path. S8.7b closes both gap cases with one reconcile.

**Gates (removal):** node build GREEN, full test-node GREEN, gofmt clean. Node-only diff; API/web + codegen untouched.
