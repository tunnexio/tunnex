# AWS NodePort continuation

2026-09-05: user explicitly dispositioned the account-restricted NLB path as
skipped. Continue the live AWS walk using the already implemented native CLI
NodePort path. NLB provisioning/discovery is NOT QUALIFIED by these results.

Use distinct fresh releases `tunnex-s205-a3` and `tunnex-s205-b2`, avoiding
retained identities and PVCs from failed NLB installs. Retire A2/B through
native typed abort only, preserving their PVCs. No Secret/PVC/CNI edits, manual
host repair, or rollout restart is permitted as a remedy.

Use current verified task worker addresses: A3 on 10.240.10.88, public
13.234.118.111 UDP NodePort 31081; B2 on 10.240.10.121, public
13.206.207.103 UDP NodePort 31082. The CLI endpoint must match its selected
NodePort. Preserve chart externalTrafficPolicy Local and explicit placement.
Allow only these two UDP ports from the freshly verified client IPv4 /32 on
the exact existing task worker security group, via the controller stack.
Do not expose health ports or the entire NodePort range. Existing NLB rules
remain unused; do not delete them or their stack as incidental cleanup.

These ordinary worker public IPs are current session endpoints, not reserved
addresses. Do not claim host replacement/reboot endpoint stability. Gateway
Pod lifecycle on the selected worker remains in scope; ordinary host reboot
rebootstrap remains S20.6. No new EC2 worker or product source/image change is
required. Normal private-service, identity, lifecycle, and HA proofs remain
mandatory; changing the endpoint design does not mark any of them passed.
