# S20.5 — AWS live-first diagnostic and qualification plan

Status: **infra diagnostic authorized; no live leg is passed by this paper.**

## Latest scope change — retain CP, retire old demo data-plane machines

The user explicitly directed: "except CP remove other machine make only infra
that is required for this walk" and "Fix ko final kro". This supersedes the
old gateway/private-host preservation below, not CP preservation or unrelated
local/Azure boundaries. The exact resolved removal list was shown before any
mutation: gateway `i-09e94cd620b7cde0e` (root `vol-0b818564ba3ea911e`) and
private Nginx `i-0e2df49c1bbfecc73` (root `vol-0a179ae250dd86e1c`), both in
default VPC `vpc-05d83034f7c1609a2`, account `735391218823`, Mumbai.
Gracefully stop, create private recoverable root-disk snapshots, then terminate
only these exact machines after the snapshots complete. Keep the snapshots
as explicitly reported recovery artifacts, not anonymous unused compute.

Keep CP `i-019a97f67dd40ce13` and the required clean EKS worker
`i-0afe3c3ecd00d1411`. Do not recreate a required EKS worker solely to satisfy
a literal "remove other machines" reading. Default-VPC routing, DNS, CP EIP,
CP volumes and data remain unchanged. Gateway-specific EIP retirement must
first verify exact allocation ownership; no broad network-resource cleanup.

After removal, existing CP plus up to three 2-vCPU EKS workers fit the current
8-vCPU quota. This removes the old-demo capacity blocker; it does not itself
authorize overwriting CP state or claim that its deployed version matches the
candidate. Verify the retained CP read-only and select a backed-up, isolated
candidate deployment path before changing it. Prioritize first real single-
gateway private-Service access, then the remaining independent/HA/lifecycle
proofs. Do useful work in parallel; no merge without exact final acceptance.

**Live correction:** the original T3 worker below was rejected by the account's
instance eligibility restriction. The same-size C7i-flex correction and updated
cost envelope are recorded in `S20.5-aws-free-tier-worker-repair.md`. Preserve
the original attempted inputs as evidence; do not count them as a working node.

On 2026-09-05 the user explicitly requested real AWS infrastructure proof before
any PR, permitted SCP changes to required machines, and offered removal of
unused infrastructure if compute quota is insufficient. This updates the
earlier execution order: begin isolated real-infra diagnosis while local gate
failures and ranked design findings remain visible. It does not make those
gates green, disposition every new product-authority choice, or authorize
calling a hot-patched deployment zero-touch.

## Preservation and current inventory

Fresh STS: account `735391218823`, principal
`arn:aws:iam::735391218823:user/aws-cli`, region `ap-south-1`.
No EKS clusters, ECR repositories, active CloudFormation stacks, Auto Scaling
groups, NAT gateways, or v2 load balancers were returned in this region.
That is not an inventory of other regions.

The Standard On-Demand quota is 8 vCPU. These three running instances consume
6 vCPU and are protected existing demo infrastructure, not unused resources:

| Instance | Existing role | Type | vCPU |
|---|---|---|---|
| `i-019a97f67dd40ce13` | Tunnex demo control plane | c7i-flex.large | 2 |
| `i-09e94cd620b7cde0e` | Tunnex demo gateway | t3.small | 2 |
| `i-0e2df49c1bbfecc73` | Private Nginx | t3.micro | 2 |

Preserve default VPC `vpc-05d83034f7c1609a2`, existing subnets, routes, DNS,
security groups, instance roles and attached IP allocations
`eipalloc-0f8de2bcddee7e221` / `eipalloc-0572e6c398180d1b8`.
Do not stop, terminate, resize, redeploy, or upgrade any of these resources.

Request the existing Standard On-Demand quota (`ec2/L-1216C47A`) increase from
8 to 16; history was empty at preflight, so no existing request is duplicated.
A request is not an approved increase or capacity reservation. Recheck the
actual quota and running/pending usage before each scale-up. If AWS does not
approve it, report the exact capacity blocker. No existing resource is deleted
without an exact removal list and separate user confirmation.

## Phase 1 — one-worker real EKS diagnostic

New resource namespace: `tunnex-s205-aws-20260905a`. Use a single new
CloudFormation stack with that exact name, in the verified account/region.
Tags identify project `Tunnex-S20.5-AWS-Walk`, this session namespace, and the
Tunnex repository. No resources are imported from existing stacks or the
default VPC.

| Resource | Exact planned scope |
|---|---|
| VPC | New `10.240.0.0/16`, DNS enabled, no peering or routes to the existing VPC |
| Subnets | New `10.240.10.0/24` in ap-south-1a and `10.240.20.0/24` in ap-south-1b |
| Internet egress | One new Internet Gateway and task-local routes; no NAT gateway or paid private endpoints |
| EKS | `tunnex-s205-aws-20260905a`, Kubernetes 1.35, standard support, not Auto Mode |
| API access | Private endpoint enabled; public endpoint restricted to the freshly read laptop IPv4 `/32`, never `0.0.0.0/0` |
| Worker | One managed Linux x86_64 `t3.medium` (2 vCPU/4 GiB), AL2023, in ap-south-1a |
| Capacity | min/desired/max initially 1; no autoscaler; no update/extra instance until quota headroom is reverified |
| Disk/metadata | 30 GiB encrypted gp3; IMDSv2 required; T3 standard credit mode to avoid Unlimited surplus charges |
| Network exposure | No SSH/RDP ingress, no workload/public load balancer yet; public worker address is for outbound access |
| IAM | New EKS cluster and EC2 worker roles; separate exact-cluster `kube-system/aws-node` IRSA role for VPC CNI; no changes to existing roles or user credentials |
| Add-ons | Pin the selected AWS versions below; no product-specific CNI patches or fabricated masquerade chains |

EKS API reports 1.35 in standard support until 2027-03-27. The local kubectl
is 1.34.1. Selected EKS 1.35 default add-ons observed from the AWS API:

- VPC CNI `v1.22.4-eksbuild.3`.
- kube-proxy `v1.35.3-eksbuild.21`.
- CoreDNS `v1.13.2-eksbuild.21`.

Pin and record the actual AL2023 release/image identity from the AWS public SSM
parameter before creation; do not hard-code an unverified AMI. Create managed
add-ons on the clean cluster without overwriting a prior installation. Use a
separate local kubeconfig outside Git; do not replace the user's current
context or write certificate/token contents to evidence.

First observe kernel, nft/iptables, VPC CNI rules, node capacity, native Pod
networking and DNS on the real worker. Diagnostic commands are read-only inside
the new environment; any created diagnostic Pod is explicitly recorded. Do
not create an empty `IP-MASQ-AGENT` chain to make a detector green.

### Known product compatibility risk

The candidate currently registers exactly the nft `ip nat IP-MASQ-AGENT`
adapter. An active tunnel without it is `blocked/no_registered_adapter` by D4;
provider metadata `aws/eks` does not override this. Whether the selected live
worker provides the exact mechanism is not inferred from a provider name.
Absent or incompatible mechanism is a real blocker to record and fix through
reviewed source, not permission for a manual networking repair or a claim of
working connectivity. Phase 1 is deliberately bounded before the larger walk
topology is launched.

## Phase 2 — full topology after quota and compatibility preflight

The planned full envelope is three `t3.medium` workers plus one fresh
`t3.medium` control-plane host: 8 new vCPU, 14 including the protected demo.
Two workers host A/B independently and a third supplies capacity for interrupted
bootstrap recovery and genuine retained-PVC relocation. The initial worker AZ
is shared for EBS relocation; this proves node-level failover, **not AZ-failure
resilience**. The managed control plane still spans two AZ subnets. A later
multi-AZ data-plane claim needs its own placement and spare-capacity proof.

The fresh control plane is isolated from the old CP and gets its own PostgreSQL,
Redis, identity/CA state, test organization, and TLS endpoints. No existing CP
data is copied. Specify its exact TLS/DNS/port access inputs and tagged host
identity before launching/deploying it. The agent mTLS listener must retain
end-to-end mTLS; ordinary HTTPS termination is not a substitute.

Install a pinned, working EBS CSI controller with its own scoped workload IAM
binding and a gp3 `WaitForFirstConsumer` StorageClass. Supply and verify the
actual UDP-capable AWS load-balancer controller/annotations before gateway
installation. Two public NLBs expose the independent UDP gateway Services.
Controller IAM permissions, exact version, target/health-check settings, and
ingress scope are separate explicit infrastructure inputs, not hidden product
mutations. Establish ordinary Pod DNS/egress and Service health before Tunnex.

Private registry namespace, before any image/chart publication:

`735391218823.dkr.ecr.ap-south-1.amazonaws.com/tunnex-s205-aws-20260905a/`

Repository ARN prefix:
`arn:aws:ecr:ap-south-1:735391218823:repository/tunnex-s205-aws-20260905a/`.

Use only new private repositories named `api`, `web`, `nginx`, `migrate`,
`node-agent`, `operator`, `tunnex-host-posture`, `tunnex-gateway`,
`tunnex-operator-crds`, and `tunnex-operator` under that prefix. Verify each
returned full ARN/registry ID/URI before login or push. Tags are immutable;
candidate manifests and deployment inputs select digests. No public release
registry or existing repository is updated. Runtime image VERSION and all CLI/
chart manifests must bind the same clean committed source; rebuild after fixes.

## Cost envelope, not a hard billing cap

Current public AWS pricing for Mumbai, researched 2026-09-05:

| Item | Base rate |
|---|---|
| EKS standard-support cluster | USD 0.10/hour |
| Linux shared On-Demand t3.medium | USD 0.0448/instance-hour |
| gp3 at included IOPS/throughput | USD 0.0912/GB-month |
| Public IPv4 | USD 0.005/address-hour |
| NLB | USD 0.0239/LB-hour plus USD 0.006/NLCU-hour |
| Private ECR storage | USD 0.10/GB-month |

Phase 1 (one cluster, one worker/address, 30 GB gp3) is approximately
**USD 0.154/hour, USD 1.23 for eight hours**, before usage/tax.
Full topology (one cluster, four hosts, two two-AZ public NLBs, eight IPv4s,
140 GB total gp3 allowance, 5 GB ECR) is approximately
**USD 0.386/hour, USD 3.09 for eight hours**, before usage/tax. Storage estimates
use 730 hours/month; 140 GB budgets 90 GB worker roots, 40 GB CP root and 10 GB
PVC allowance. One UDP NLCU per LB adds USD 0.012/hour. Data transfer, build
traffic, logs, excess storage/IOPS, other retained fixtures and tax are extra.
T3 standard mode may throttle after credits drain; it avoids claiming Unlimited
CPU usage is included in the instance base rate. No NAT gateway is budgeted.

These are estimates, not account-specific discounts, currency conversion, free
tier promises, or an automatic spending stop. EKS and retained resources keep
billing until explicitly cleaned up. Additional clean clusters for the CRD
matrix need a separately enumerated scope and cost; no automatic cluster fleet
is implied here.

Sources: [EKS pricing](https://aws.amazon.com/eks/pricing/),
[Mumbai EC2 price feed](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AmazonEC2/current/ap-south-1/index.csv),
[VPC pricing](https://aws.amazon.com/vpc/pricing/),
[Mumbai load-balancer price feed](https://pricing.us-east-1.amazonaws.com/offers/v1.0/aws/AWSELB/current/ap-south-1/index.json),
[ECR pricing](https://aws.amazon.com/ecr/pricing/).

## Proof and repair rules

- SCP is permitted for candidate transport and diagnostics under the user's
  latest instruction. Verify checksums and source identity; never copy a raw
  token into recorded argv or evidence.
- If a source hot patch or manual remedy is needed, record that affected leg
  as diagnostic/inconclusive. Fold an approved fix into source, rebuild the
  immutable candidate, and rerun from the appropriate clean baseline. Never
  present a repaired machine as zero-touch acceptance.
- Preserve every provider-neutral leg in the committed box-walk: plan without
  writes, A/B install, desktop FQDN/VIP/private-Service traffic, persistence,
  owned drift self-heal, immutable upgrade/rollback, retained reuse, real TTL/
  abort recovery, GitOps/CRD ownership matrix, and automatic A→B→A failover.
- Commit redacted observations during the session. No token, private key,
  certificate body, kubeconfig, or service-account credential is stored in Git.
- API gate failures and manager/startup decisions remain as recorded in the
  gate ledger/dispositions. Diagnostic deployment does not resolve them. Final
  source must re-earn all gates, full multi-finder review and required live legs.
- **No PR before successful required AWS proof.** After final non-pointer
  content, open a draft PR, obtain its real number, write PLAN last, obtain
  exact-SHA required checks, and use the required matched-head linear merge.
- No cleanup is performed from a tag match or an assumed unused state. Show the
  exact resources and obtain separate removal approval; preserve local lanes,
  stores and stashes as well as the protected AWS demo.
