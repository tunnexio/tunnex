# S14.12 — SESSION HANDOFF (for a fresh chat starting S14.13)

**Read `PLAN.md`'s "Story status (re-entry checkpoint)" plus `git log` first. Trust git over this file.**
If they disagree, git wins and this file is stale.

---

# 1. THE FOUNDER RULE — the working loop, in order

> ## **READ THE WIREFRAME → CUT → DESIGN → CODE → FOUNDER'S LOCALHOST REVIEW → GO AHEAD**

**Read the wireframe STRUCTURALLY, from `docs/design/TUNNEX-wireframe-v2.html.txt`, never from a screenshot.**
S14.12 lost two iterations correcting a panel from a picture before reading the source. When the source is
read, quote its numbers back — and then **apply** them; quoting geometry correctly and building it at 4× is a
separate failure from misreading it.

⛔ **AND THE DESIGN MAY DEMONSTRATE A RESULT WITHOUT SPECIFYING THE RULE THAT PRODUCES IT.** `polFlow` is a
hardcoded literal that never reads the rule table: 4 sources, 4 destinations, 5 hand-authored edges. **The
picture is legible because a human curated it; the product must derive it.** When they differ, the derivation
is **OURS to design** and is recorded as a decision, never as an implementation of the design.

**Nothing is built before the founder rules.** Decide-items are surfaced with a recommendation and held.

---

# 2. PRE-FLIGHT — six standing checks, run before any commit-one

1. **SCALE at 10×/100×**
2. ⛔ **N=1 AND N=MANY** — *this one has stopped three sections dead.* Run it against the **API PAYLOAD**, not
   psql; they disagreed in S14.10 and psql was wrong.
3. **ANIMATION is a requirement, not polish** — entry-once; continuous ONLY on genuinely live states.
4. **READ THE HANDOFF, and grep the registers for the name before arguing for any panel or library.** GSAP was
   re-litigated **three times**; the ruling is LICENCE (non-OSI inside a redistributed Apache-2.0 bundle),
   `docs/EPIC-14-ui-redesign.md:96`. **Being right for a weaker reason is how a ruling gets re-opened again.**
5. **THE LIVE SCHEMA is authority**, not migrations and not the spec. `policy_rules` carries four CHECK
   constraints, two of them discriminated unions — those are the truth about what can render.
6. **When a token's value moves, enumerate every foreground paired with it.**

## ⛔ CHECK 7 — THE ABSENCE QUESTIONS. **ASK WHAT THE SCREEN CANNOT DO, NOT WHETHER IT MATCHES.**

**S14.12's two best findings came from here, and NEITHER was in the wireframe:**

- **the verb gap** — *"a group is a name and a delete"* → three endpoints shipped in S7.5.2 with one consumer,
  and rules above using groups whose membership nobody could edit. **What is MISSING.**
- **the cascade** — *"what happens when I press it"* → `ON DELETE CASCADE` on `src_group_id`, `dst_group_id`
  and `dst_resource_id`, so deleting a group destroys every rule referencing it, silently. **What happens
  AFTER.**

> ## **A DESIGN CAN ONLY BE WRONG ABOUT WHAT IT DEPICTS. IT CANNOT BE WRONG ABOUT WHAT IT OMITS.**
> ## **The section protocol asks DOES THIS MATCH THE SOURCE — and the source was silent on both.**

**So two questions, answered from the SPEC AND SCHEMA, never from the design:**

**7a · WHAT CAN AN OPERATOR NOT DO ON THIS SCREEN THAT THE API ALLOWS?**
The verb census. For every mutating endpoint in the spec, **name its call site** — an endpoint with no caller
is either dead or missing a surface, and nothing distinguishes those without asking.
Measured once: **80 mutating operations, 54 call sites, 19 with none, 12 genuinely unreachable.**
⛔ **11 remain**, registered as their own story. **Run this per screen** — the sweep found the total, but which
screen OWNS each verb is a per-section question.
*Method note: exclude `edition.ts` — it is a path MANIFEST, so "the path string appears" is
correct-by-construction for the wrong question. Seed the sweep with a case whose answer you already know.*

**7b · WHAT HAPPENS AFTER EACH DESTRUCTIVE VERB, AND IS THE OPERATOR TOLD?**
Read the **FK actions** (`confdeltype` in `pg_constraint`) and the handler's response body. `CASCADE` that the
UI never mentions is the shape found here — and the 204 carried no body, so the server never said how many
rows it took. **If the server serves a preview, use its numbers; if it does not, name the RISK and omit the
NUMBER** — a client-computed count is a second source of truth about what the server is about to do.

**Both would have been missed on every screen so far had nobody asked.** The next eleven screens get the
matching review by default and these only if they are in the list — which is why they are.

---

**Em-dash placeholders are banned** (`"—"` → `"n/a"`, guarded by `test/placeholderglyph.test.ts`).
**Prose em-dashes** go to the global sweep + lint rule at EPIC 14 close — a *different* rule; do not conflate.

---

# 3. THE MERGE STANDARD

1. **CI green with `gates` cited at STEP LEVEL** — 14 steps, each named. A job badge is not evidence.
2. **`visual`'s `continue-on-error` steps cited INDIVIDUALLY.**
3. **Branch protection re-verified IMMEDIATELY BEFORE**, from a fresh read: `gates` · `client (macos-latest)` ·
   `client (windows-latest)`, `required_linear_history=true`.
4. ⛔ **THE POINTER NAMES THE CONTENT TIP, NOT `main`'s HEAD** (ruled S14.11, first used S14.12).
   A commit cannot contain its own hash, so the post-merge head is **unknowable before the merge** — for a
   fast-forward exactly as much as for a rebase. Naming the content tip makes the checkpoint knowable while the
   PR is open, so **it lands inside the PR and the direct-to-`main` bypass never increments.** Count is **8**
   and should stay there. Carry BOTH identifiers: the PR number (survives rebase) and the content-tip sha.
5. **Un-rendered states declared explicitly for acceptance.**
6. **Any push supersedes the cited CI.** Re-verify on the exact sha; `allow_auto_merge` is **false**.
7. ⛔ **Report EVERY LEG BY NAME.** *"CI is green"* and *"make web-gate green"* are not reports.
   `typecheck 0, N tests, build clean` is. And **`ran` and `passed` are different claims** — a skip reports as
   neither.

---

# 4. THE TWO REVIEW STACKS

```bash
export COMPOSE_PROJECT_NAME=tunnex-s141
make up-enterprise                          # :80   ENTERPRISE
TUNNEX_SEED_FORCE=true make seed-fixtures

make up-open-review                         # :8081 OPEN edition, its own DB
make seed-open                              # migrate + base seed + fixtures, in that order
```

**One `fixtures.sql`, one seeder, two databases — zero drift by construction.** The ONE legitimate difference
is `posture_blocked` (device-health reporting is enterprise-gated), enumerated in the `seed-open` target.

> **Two databases that differ are drifted or correctly different, and only a written note decides which.**
> An unexplained difference costs someone an investigation that usually ends in "make them match" — which is
> how a correct difference becomes a regression.

**A deploy is confirmed by the served bundle hash changing**, never by the command exiting zero:
`curl -s http://localhost/ | grep -o '/assets/index-[A-Za-z0-9_-]*\.js'`

## ⛔ BEFORE A REVIEW, GREP THE **SERVED** BUNDLE, NOT `dist`

**S14.13 shipped four panels that were invisible on both stacks while a correctly-run artifact grep said they
were present.** The grep searched `apps/web/dist` — the local build — and found all five consumer-only
strings. It was proof they reach *an* artifact. The one nobody was looking at. (Cause was mundane: containers
built 08:34, branch point 08:59, first story commit 09:21 — the rebuild was never run.)

**Three checks, three different questions, and only the first two are about the review:**

| check | subject | answers |
|---|---|---|
| **hash changed** | the port | **DEPLOY** — is the served artifact new at all? |
| **strings in the SERVED bundle** | the port | **REACH** — did this code get into what is being served? |
| strings in `dist` | the local build | **LINKAGE** — does this code have a caller, or did tree-shaking drop it? |

```bash
H=$(curl -s http://localhost/ | grep -o '/assets/index-[A-Za-z0-9_-]*\.js')
curl -s "http://localhost$H" | grep -c "a string only the new panel can produce"
```

**Keep running the `dist` grep** — it still enforces the no-caller law. It was simply never capable of
answering *can the founder see it*. **Say which question each grep answers.**
**Login:** `owner@demo.tunnex.local` / `tunnex-demo-password` (all fixture accounts share it).

---

# 5. OPEN THREADS — carried, each with a register row

| thread | state |
|---|---|
| ⛔ **`CountOwners` counts owner ROWS, not owners who can sign in** | **PROVEN unrecoverable lockout** — deactivate both owners and the invariant holds on paper while nobody can sign in. Red written: `docs/probes/lockout_probe_test.go.txt`. **Server change, its own story.** The client `ownerCount` mirrors it DELIBERATELY and must not be fixed independently. |
| ⛔ **Audit Log discards every named `actor_system`** | served on 19/78 rows, read nowhere in `apps/web`. Red written: `docs/probes/actor_system_unread_probe.test.tsx.txt` |
| **`Kubernetes.tsx:403`** | placeholder-glyph regression, S14.8's pass did not clear it |
| **51 omitted-and-read mocks** | `docs/probes/mock-census-output.txt`; known false-positive floor recorded |
| ⛔ **11 of 12 unreachable mutations** | **EPIC 14's largest carried finding, its own story.** IdP-sync is five endpoints with no surface at all |
| **D1 / D1b / D2** | MFA column · AUTH column (type-level tripwire armed) · per-member group edges |
| ⛔ **S14.12's THREE CARRIED ITEMS** | mode toggle as a real switch + blast-radius confirm · two-column layout · `SOURCE · DESTINATION · STATE` headers. **The section is OPEN.** |
| **server cascade-preview endpoint** | so the delete confirm can name counts the server owns |
| **the open-edition debt** | dissolved by S12.1's runtime license gate, not paid |

---

# 6. THE LAWS THAT KEPT FIRING — the short list

- **A composite result reported by its most favourable component** — in space (typecheck vs test count) and in
  time (an error one scroll back). Report every leg.
- **A mutation that failed to apply proves nothing, and looks exactly like one that was caught.** Before
  reading a guard's verdict, **confirm the mutation changed the subject.** Fourth instance of a check
  reporting on a state it never reached.
- **Run a sweep against a case you already know the answer to.** If the known case is missing, the sweep is
  measuring something else. (A path manifest made "the string appears" correct-by-construction for the wrong
  question.)
- **The artifact outlives the source that produced it** — a running container is not evidence its definition is
  on your branch. Three instances.
- **A threshold should measure the property that matters, not a proxy** — coverage, not N; class tokens, not
  substrings.
- **A wrong direction is worse than not having looked**, because it sends the fix to the wrong file.
- **An animation and a semantic encoding must not share a property** — the cascade favours the animation and
  the meaning fails silently.
- **A view-model with green tests and no caller is invisible to every gate we own.** Grep the ARTIFACT for a
  string only its consumer can produce; tree-shaking is the tell.
- **Writing a rule creates the feeling of having complied with it.** Ask *what in this change is asserted only
  in prose?*
