# S10.3 — in-cluster gateway (expose in-cluster Services) — commit-one

**Status: decision-first. Building on merit (founder delegated technical judgment); halt-and-surface
on genuine scope/cost/product forks. Five founder-ruled terms folded below; sub-forks ruled on merit
with reasoning. Tail-of-turn review on the privileged-pod + DNAT paths.**

The epic's value slice: expose in-cluster Kubernetes Services to the Tunnex fabric, ZT-governed. Highest
fork probability of EPIC 10 — three subsystems meet (K8s networking, the VIP map, the site/subnet model).
Verify pass = three file:line-grounded investigations (CP seam, agent data plane, gateway chart).

## Founder-ruled terms (folded)
1. **Preflight before privilege** — install fails loud on a blocking cluster (Autopilot / PSS-restricted)
   naming the missing capability, never a crash-looping pod. Refuse-loudly at the packaging tier.
2. **Gateway self-provisions verbatim** — join token → enroll → CSR → mTLS → DesiredState. No new
   enrollment path (verified: the agent reads `TUNNEX_JOIN_TOKEN` from env, runs the existing enroll flow).
3. **VIP-DNAT** with the four conditions live (range allocator-known + subnetguard-validated; mapping as
   desired state; VIP stability = identity-resolution at compile; removal = full sweep + honest surface).
4. **DNS-rewrite rides S8.4's rails** — confirmed: the agent already runs an in-process DNS responder
   (`dnsforward` package); the rewrite EXTENDS it, no parallel resolver.
5. **BindNode + multi-egress** — Deployment-first (one gateway = one site, D1); DaemonSet registered.

## Verify pass — mechanisms found (the design is mostly reuse)
- **Exposed Service → resource shape, resolved from identity.** A VIP/32 : proto : ports fits the
  `resources` wire shape (`0018_zero_trust.up.sql:51-65`), but the resource path carries a LITERAL,
  never-re-resolved CIDR (`compiler.go:385`) — fatal under VIP reassignment. So an exposed Service is a
  NEW entity resolved to the CURRENT VIP at compile, mirroring the device-source ruling
  (`ListActiveDevicesForOrg` `policy.sql:139-154` → `groupDeviceIPs` `compiler.go:254-276`).
- **VIP allocation reuses `ipalloc.Allocate` unchanged** (`ipalloc.go:32-58`, pure + CIDR-agnostic — a VIP
  range is a second `cidr` arg with its own used-set).
- **DNAT = a genuinely-new `prerouting dstnat` chain** in `table ip tunnex` (today only postrouting/srcnat),
  rendered into the atomic `add;flush;table` full-replace (`egress_linux.go:394-414`) → keyed + swept for
  FREE (no handle bookkeeping). NET_ADMIN already granted; no new capability.
- **DNS-rewrite = small.** A response-mutation at `dnsforward.go:169` (swap A-record ClusterIP→VIP via the
  already-imported `dnsmessage` codec) + a VIP↔ClusterIP map threaded `SetTable`-style. No new daemon.
- **DesiredState carrier** — a new out-of-hash `omitempty` VIP map on `policyspec.Compiled`/`nodepolicy.Compiled`
  (mirror `DNSForwards`/`PoolCIDR`), stapled in `finalizeArtifact` (`nodes/service.go:1253`), consumed in the
  agent `OnPolicy` fan-out (`main.go:182-189`) → egress (DNAT) + dnsFwd (rewrite). Apply-on-change/
  sweep-on-removal free via the atomic egress replace + atomic `SetTable` rebuild.

**Correction (record it):** the earlier D3 condition-1 cited "the OVPN transit subnet (D-S9.5)" as the
allocator precedent — that range does NOT exist (OVPN devices allocate from the org pool; `D-S9.5` is the
OVPN opt-in decision). The real precedent for a NEW disjoint range is the site-subnet / routed-range path
(`sites/service.go:284-342`) + pool-resize (`devices/service.go:497`).

## Sub-forks — ruled on merit (delegated judgment; flag to redirect)
- **F1 — exposed Service = NEW entity** (`k8s_service`: cluster/site, name, namespace, proto, ports; VIP
  allocated), NOT a plain `resources` row. RULED new-entity — it is the only VIP-reassignment-safe shape and
  it realizes the founder's condition 3 (identity-resolution at compile). It is a DESTINATION with a stable
  ID, not a source principal — does NOT touch D4(a)/the identity-binding invariant.
- **F2 — subnetguard registration.** RULED: add a typed **`ClassVIPRange`** (symmetric with site/pool, honest
  error text — matches "allocator-known + subnetguard-validated"), NOT fold-into-`reserved`. **Law rider:**
  every `Check` caller (pool-resize, site-approval, cluster-VIP-range creation) must feed the org's VIP ranges
  into its input set so disjointness stays BIDIRECTIONAL (the validator-input-filtering law, S8.1). This is a
  new instance of that law — noted.
- **F3 — VIP-map version discipline.** RULED **PoolCIDR-style** (content-derived `RequiredVersion` bump,
  refuse-not-silent), NOT DNSForwards-style (convenience, no trigger). A VIP→ClusterIP DNAT an old agent
  silently ignores = the exposed Service is DEAD-WHILE-GREEN — the exact PoolCIDR/Routes failure mode
  (`policyspec.go:88-92`). New `ProtocolVersion` 6→7, triggered only when the VIP map is non-empty (non-K8s
  orgs stay byte-identical).
- **F4 — hostNetwork.** RULED **required** for the gateway pod: the agent's `prerouting` DNAT (VIP→ClusterIP)
  must chain into kube-proxy's ClusterIP→pod DNAT, which lives in the node/host netns — so the agent's
  nftables must sit there. hostNetwork also inherits the node `ip_forward` + gives the real WG datapath.
  (Resolves the thread-B open item.)
- **F5 — join token.** RULED support both `joinToken` (chart mints the Secret) + `existingJoinTokenSecret`
  (operator pre-creates), PREFER existingSecret — the CP master-key posture (a token in helm history leaks).
- **F6 — endpoint chicken-and-egg.** RULED operator-supplied `endpoint` REQUIRED (a `required` guard); the LB
  external IP may not exist at install, so the operator reserves/knows it (or a static `loadBalancerIP`). This
  is a PD-4-style fabric promise the chart can't synthesize.
- **F7 — preflight mechanism.** RULED **(a) a pre-install,pre-upgrade hook Job** requesting the privileged/
  hostNetwork/NET_ADMIN/`/dev/net/tun` context + self-asserting → on a blocking cluster the Job pod is
  admission-REJECTED, so `helm install --wait` fails at the hook before the real Deployment (no crash-loop) —
  the guard-before-exec doctrine (`ovpnserver.go:112`). Complement with **(b)** a `required` privilege-ack
  value. Reject (c) (surfaces too late). NOTES mandates `--wait`.

## Slice cut
1. **CP data model** — `k8s_clusters` (VIP range, bound to a site/gateway) + `k8s_services` (exposed Service)
   tables + queries; VIP allocation via `ipalloc`; `ClassVIPRange` in subnetguard + the bidirectional law
   rider. Reds: alloc from VIP range, subnetguard rejects overlap both directions.
2. **Compiler resolution** — `serviceVIP` map in the Snapshot (`ListActiveExposedServicesForOrg`) + a
   `case "k8s_service"` in the compiler → `AllowEntry{DstCIDR: VIP/32, proto, ports}`, identity→current-VIP.
   Reds: identity resolves to current VIP, a reassigned VIP re-resolves, a removed Service vanishes from grants.
3. **DesiredState VIP map** — out-of-hash `omitempty` field on `policyspec.Compiled` + `nodepolicy.Compiled`,
   stapled in `finalizeArtifact`, `RequiredVersion` 7 trigger (PoolCIDR-style). Reds: out-of-hash byte-identical
   for non-K8s orgs, version bumps only when the map is non-empty, the law red (7-trigger + hash-blind).
4. **Agent DNAT** — new `prerouting dstnat` chain in `table ip tunnex`, rendered into the atomic template;
   VIP map consumed in `OnPolicy` → egress. Reds: twin-goldens move (the new chain), sweep on removal.
   **Tail-of-turn (DNAT path).**
5. **Agent DNS-rewrite** — extend `dnsforward.Forwarder`: ClusterIP→VIP answer rewrite + a VIP map setter.
   Reds: an exposed Service's A-answer is rewritten to its VIP; an un-exposed one is not (self-gates honestly).
6. **Full sweep + honest surface** — Service removal frees the VIP, drops the DNAT, and any grant referencing
   it compiles to nothing with a typed "service no longer exposed" surface (the `cidr_outside_org_ranges`
   precedent), never a silent no-op. Reds.
7. **CP API + web** — expose-Service CRUD (register cluster + VIP range, expose a Service, grant it via a
   resource-shaped rule). Governance = ENTERPRISE, default-OFF (D5); connectivity (the gateway on the mesh) is
   open. Edition-gated 403 parity.
8. **`tunnex-gateway` chart** — privileged + hostNetwork pod, preflight hook Job (F7) + ack, join-token Secret
   (F5), WG `:51820` LoadBalancer/NodePort Service, state PVC, NOTES (mandate `--wait`, endpoint, wipe-to-
   re-enroll). **Tail-of-turn (privileged pod).** SUBSTITUTE: helm lint + template; real install + wire proof =
   the EPIC-10 walk.

Sequencing: CP data → compiler → DesiredState → agent DNAT → agent DNS → sweep/surface → API/web → chart.
Each slice sha-first gated. Halt-and-surface on any NEW scope/cost/product fork.

## Slice 1 — VIP-stability disposition (RULED, condition from the F2/D3-condition-3 blessing)

The reassignment hazard is born in ExposeService's allocation. **RULED: identity-resolution alone is
sufficient — allocation does NOT quarantine freed VIPs, and this is safe by construction.** The used-set
(`ListUsedVIPsInCluster`) is LIVE Services only, so a soft-deleted Service's VIP is immediately reusable.
That is safe because a grant references a Service's STABLE ID and the compiler resolves ID → CURRENT VIP
at compile time and **NEVER caches a VIP** (Slice 2): a deleted Service vanishes from the resolution set
(its grant compiles to nothing), and a reused VIP belongs unambiguously to the NEW Service's identity. No
quarantine timer is needed (the rejected alternative — complexity for no gain once grants track identity).
The **reassignment-trap red belongs in Slice 2** (where the resolution is built): prove a deleted Service's
grant compiles to nothing AND a re-exposed Service reusing the freed VIP resolves to the NEW identity only.

## Slice 2 — compiler resolution (built)

A grant with dst_kind='k8s_service' resolves the Service's STABLE id to its CURRENT VIP at compile time
(serviceByID lookup, mirrors resourceByID) — never a snapshotted address. Migration 0049 mirrors the
site-dst 0033 (ON DELETE CASCADE: a hard Service delete drops dependent grants; a soft delete keeps the row
so the grant survives and compiles to nothing).

- **Placement (both-enforce, write it down).** The k8s_service case uses devGrantNodes(d.NodeID, svc.SiteID),
  so BOTH the device's own gateway AND the cluster's gateway authorize the flow. This is D-S9.1-2 both-enforce
  applied here for a specific reason: the CLUSTER gateway performs the VIP->ClusterIP DNAT, so it must be
  entitled to the flow — not only the device's gateway. A future reader seeing a two-node placement should
  read it as "the DNAT-performing gateway is entitled", not redundancy. The second (site/cidr-source) switch
  adds the cluster gateway to enforceNodes for the same reason.
- **One mapping site (checked, not WF-OVPN-10).** policy_rules rows become compiler.Rule in ONE place
  (BuildSnapshot); DstK8sServiceID is mapped there. The SELECT * query picks up the column automatically, but
  the struct mapping is manual — verified there is no second consumer of ListActivePolicyRulesForOrg.
- **Reassignment-trap red, both directions (PASS):** (a) same Service id, new VIP -> the grant follows;
  (b) THE ONE THAT MATTERS -- a different Service that reuses a freed VIP does NOT inherit the grant (the rule
  keys on the deleted id -> compiles to nothing; the reused VIP is not silently re-granted).
- **Compile-to-nothing red (PASS):** a rule -> a vanished Service produces zero AllowEntries.

**Carried to later slices (noted, not folded):**
- **Slice 7 (API):** the create-rule VALIDATION (enterprise/policy/service.go dst_kind must be resource/
  group/site) MUST widen to include 'k8s_service' + require dst_k8s_service_id; the policyspec RuleInput +
  the API/web rule builder gain the k8s_service destination kind. The compiler already resolves it.
- **Slice 7 (API/web) warn surface:** a rule pointing at a soft-deleted / vanished Service compiles to
  nothing SILENTLY at the compiler tier; the honest "this rule targets a Service that no longer exists"
  surface is the same reassuring-rule class as cidr_outside_org_ranges and belongs in the rule list UI.

## Slice 3 — DesiredState VIP map + ProtocolVersion 7 (F3's three conditions = the acceptance)

The VIP map (policyspec.Compiled.VIPMappings / nodepolicy.Compiled.VIPMappings) carries each exposed
Service's VIP -> identity (namespace + name) for the agent's DNAT + DNS-rewrite. PoolCIDR-style:

- **F3(a) content-derived version + zero-config golden.** ProtocolVersion 6->7 (lockstep: policyspec +
  nodes + nodepolicy). The version rises because the ARTIFACT carries a VIP map, not because a constant
  moved: RequiredVersion returns 7 ONLY when len(VIPMappings)>0. An org with no cluster emits no map,
  requires no bump, and its agents never see v7. The map is HASH-BLIND (out of projectForHash), so a
  re-allocated VIP never churns the policy hash; omitempty elides it so a non-cluster artifact is
  byte-identical. **Reds:** v7 trigger; no-map-not-bumped; hash-blind (map contents don't change the hash
  at the same version); omitempty-elides (no `vip_mappings` key in a non-cluster artifact's JSON).
- **F3(b) loud typed refusal + CP surfacing, both halves.** AGENT: an agent pinned at the pre-S10.3 max
  (6) REFUSES a v7 VIP-map artifact and renders DENY-ALL (fail-static), recording RefusedVersion=7
  (`TestInterlockV6AgentRefusesV7VIPMap`). CP: the agent reports RefusedVersion in PolicyStatus; policyhealth
  surfaces `KindUnsupportedPolicyVersion` (UnsupportedVersion, checked FIRST, remedy = upgrade the agent) —
  version-agnostic, so v7 rides the existing surfacing path (no CP change).
- **F3(c) the fail-static upgrade sentence (release notes / limitations):** *"A gateway running a pre-v7
  agent will refuse artifacts containing cluster VIP maps and continue serving its last-good configuration
  until upgraded; non-cluster orgs are unaffected."* Third such sentence in the ledger (OVPN reneg bound,
  OVPN re-home bound, now the K8s version gate) — they now form a real limitations section.

**Deferred to Slice 3b (pairs with the Slice-4 DNAT consumption):** finalizeArtifact POPULATION — querying
the exposed Services of the cluster this gateway fronts + stapling the VIP map onto its artifact. The
carrier + version machinery is in place; population fills it where the agent will consume it.

## Slice 3b + Slice 4 — ClusterIP resolution RULED (A): in-cluster CoreDNS, no K8s API

The gateway resolves <service>.<namespace> -> ClusterIP via in-cluster CoreDNS and programs a prerouting
DNAT VIP -> ClusterIP. The privilege argument decides it: F4 already takes hostNetwork + NET_ADMIN;
"the gateway needs kernel networking but reads NOTHING from your Kubernetes API" is a materially better
sentence for a customer's security team, and true only under (A). ClusterIP stability makes (B)'s
authoritativeness worth little.

**Ordering — DISPROVEN ON THE WIRE (WF-K5, box-walk 2026-07-27). The claim below is WRONG; kept, not
deleted, so the reasoning error is findable. See "WF-K5 — RULING A AMENDED: endpoint DNAT" at the end of
this doc.** The correction in one line: netfilter applies at most ONE destination-NAT per packet per hook,
so kube-proxy's ClusterIP->pod DNAT is a silent NO-OP after our VIP->ClusterIP DNAT already set the binding
— the packet dies addressed to the ClusterIP. The priority model below makes the double-DNAT look sound
(lower number runs first, both are nat-prerouting chains) but that one-dst-NAT-per-pass rule is invisible
from priorities and is the load-bearing constraint this ruling missed. Original (disproven) claim:

> **Ordering (SOUND, not fragile).** Client->VIP hits the gateway's prerouting nat in the NODE netns (F4
> hostNetwork is what puts our rules there). Double-DNAT in sequence: our chain VIP->ClusterIP, then
> kube-proxy's KUBE-SERVICES ClusterIP->pod -- so OURS MUST RUN FIRST, at a priority NUMERICALLY BELOW
> dstnat (-101). Deterministic in nftables; holds across kube-proxy iptables-nft/nft/IPVS modes; conntrack
> reverses both DNATs on return. Our DNAT lives in our OWN table ip tunnex (atomic add;flush;table wholesale
> replace), never interleaved into kube-proxy's chains -- kube-proxy's resync never touches it, and there is
> no foreign-chain drift matching (no S8.6b comparator risk).

**Four conditions (ruled):**
1. **hostNetwork DNS trap (the silent-install-killer).** A hostNetwork pod inherits the NODE's resolv.conf,
   NOT the cluster's -- *.svc.cluster.local won't resolve without `dnsPolicy: ClusterFirstWithHostNet`. The
   gateway chart MUST set it (commented). PREFLIGHT red: cluster DNS unresolvable at startup -> refuse
   loudly (k8s_cluster_dns_unreachable), never program nothing silently.
2. **No stale DNAT.** NXDOMAIN (Service deleted) or a changed ClusterIP -> the rule is removed / updated,
   never kept. A DNAT to a dead ClusterIP is dead-while-green (VIP answers, traffic blackholes). Reds:
   NXDOMAIN -> rule swept + surfaced; ClusterIP changed -> re-programmed within one tick.
3. **Headless Services refused, not guessed.** clusterIP:None returns pod IPs; DNAT-ing to a pod IP is wrong
   + unstable. Detect at reconcile (the agent sees it via resolution) -> refuse with a typed teaching error
   (headless Services aren't exposable through a VIP).
4. **Bounded staleness (paper):** ClusterIP changes propagate within ONE reconcile tick -- the fourth
   honest-bound sentence in the ledger (OVPN reneg, OVPN re-home, K8s version gate, now this).

**(B) is satisfied-by-the-operator, not deferred-as-a-gateway-change.** When S10.2's operator exists it
inherently has K8s API access (it watches CRDs), so it can supply authoritative ClusterIPs to the CP --
improving accuracy WITHOUT the gateway ever gaining API permission. Register (B) satisfied-by-operator;
trigger = a customer needing sub-tick ClusterIP propagation. The privilege boundary stays where it belongs.

## Slice 4 — agent DNAT (SPEC for the fresh session; highest-privilege, undiscounted review after)

Build in a fresh session. Prerouting DNAT VIP->ClusterIP at priority < dstnat in table ip tunnex (own
atomic-replaced table), fed by the resolved VIP map; CoreDNS resolver (<service>.<namespace> -> ClusterIP),
injectable. Resolution runs in the RECONCILE (async, network), producing a resolved VIP->ClusterIP map;
rulesetWith renders the prerouting chain PURELY from it (no DNS in the sync render path).

**The four ruled conditions** (see the ruling above): (1) chart dnsPolicy ClusterFirstWithHostNet +
PREFLIGHT k8s_cluster_dns_unreachable; (2) no stale DNAT (NXDOMAIN -> swept+surfaced, ClusterIP change ->
re-programmed within one tick); (3) headless refused; (4) bounded-staleness sentence (written).

**Two additions carried in (checkpoint framing):**
- **Red the atomic-replace-is-free-sweep property EXPLICITLY** (the whole shared-territory argument rests on
  it): a removed Service's DNAT is GONE after one reconcile with NO explicit delete logic, and a simulated
  kube-proxy full-resync in between changes nothing about our table. The S8.6b lesson INVERTED — we avoid the
  comparator entirely, and this red is what makes that structural rather than assumed.
- **Substitute vs walk honesty (WF-OVPN-1 / reaper-red lesson, twice-learned):** the injectable resolver
  means the REAL resolver path is untested by construction. SUBSTITUTE-PROVEN = the injectable path (render,
  sweep, NXDOMAIN/headless handling, priority ordering in the rendered text). OWED TO THE WALK = real CoreDNS
  resolution, real kube-proxy interaction, real priority ordering in a live netns. **The walk leg for this
  slice is the epic's most important: a client reaching a real in-cluster Service through the VIP.**

**FINDING-BEFORE-CODE (surfaced while mapping the resolver) — headless detection is DNS-limited under (A).**
Condition 3 says "detect + refuse headless." But via DNS alone (no K8s API), a normal ClusterIP Service
resolves to 1 IP (the ClusterIP) and a headless Service to N pod IPs — so **multi-pod headless is detectable
(>1 IP -> refuse), but a headless Service with exactly ONE ready pod is INDISTINGUISHABLE from a ClusterIP
at a single resolution** (both are 1 IP). The agent can't know the Service/Pod CIDRs without the K8s API to
classify the address. **Lean (rule at the fresh session's start):** refuse the detectable case (>1 resolved
IP -> headless_service_not_exposable, typed); the 1-pod-headless edge is treated as an (unstable) ClusterIP
and re-resolved each tick (condition-2's re-program-on-change bounds the instability) -- REGISTER it as a
residual, trigger = a customer exposing a 1-pod headless Service. This keeps (A)'s no-K8s-API boundary; the
authoritative fix is (B) satisfied-by-the-operator (the operator reads clusterIP from the API). This is the
one Slice-4 decide-item; everything else is ruled.

**Reds (fresh session):** DNAT present for a resolved Service -> absent after removal (atomic free-sweep,
no delete logic) · kube-proxy resync changes nothing about our table · idempotent across ticks · non-cluster
gateway renders NO prerouting chain · NXDOMAIN -> swept + surfaced · ClusterIP change -> re-programmed · >1-IP
(headless) -> refused + surfaced typed. Then the undiscounted review on the DNAT + privileged surfaces.

## Slice 4b — the three fail-closed terms (ratified; the fresh session opens on execution)

1. **Fail-closed is the default at EVERY branch.** The ONLY path that programs a DNAT is
   exactly-one-address-inside-the-registered-Service-CIDR. Every other outcome -- resolution error, DNS
   server unreachable, NXDOMAIN, >1 address, an address outside the Service CIDR, a missing/bad Service CIDR
   -- programs NOTHING. Structure it so `ok=true` is reachable at literally ONE return (a single classify()
   whose success is the last line), not a series of guards a future edit could reorder into a fail-open.
2. **No DNS in the sync path.** resolve-and-classify runs in the reconcile (async), the render is PURE. A
   slow or hanging resolver must NEVER stall the apply path (the KILL-SWITCH-NO-UNBOUNDED-I/O law's cousin):
   resolution stores the resolved map; rulesetWith reads the last-resolved map and does no I/O. Resolution
   is bounded (per-lookup timeout) and decoupled from the nft apply.
3. **Substitute vs walk, stated in the gate report.** The injectable resolver PROVES the classifier (every
   fail-closed branch + the single success). Real CoreDNS resolution, real kube-proxy chaining, and real
   priority ordering in a live netns are WALK-OWED. The walk leg -- a client reaching a real in-cluster
   Service through its VIP -- is the epic's most important proof.

## Slice 5 — DNS: RULED (A1) direct-answer-from-VIP-map + (B2) Tunnex per-cluster zone

**D3 AMENDED.** D3 ruled "forward to CoreDNS + REWRITE the ClusterIP answer -> VIP." That was written
BEFORE the gateway held an authoritative service->VIP map (Slices 3/4 created it). With the map, the
rewrite is unnecessary machinery around an answer we already have. **(A1) direct-answer:** the gateway
answers a Service name -> its VIP straight from the map; an UNEXPOSED Service (not in the map) -> NXDOMAIN.
The decisive argument is CORRECTNESS, not simplicity: forwarding+rewriting an unexposed Service would
return a ClusterIP that is NOT DNAT'd and NOT routable -- a confidently wrong answer. (A1) makes DNS
SELF-GATING: only exposed Services resolve, NXDOMAIN otherwise, matching the policy surface exactly.

**ONE-TRUTH COUPLING (record it):** the DNS answer and the DNAT program are now derived from the SAME
service->VIP map, so they CANNOT disagree -- one truth, two consumers, the pattern this project has landed
on repeatedly (uapi field omission, ranges-free mint, keyless-peer guard, shouldRenderQR, invalid_vip,
now DNS-answer==DNAT-target).

**(A1) conditions:** unexposed -> NXDOMAIN, NEVER a silent timeout (a client hanging on an unresolvable
name is the worst failure shape). Fail-closed like the classifier: an EMPTY or STALE map answers NOTHING
rather than something.

**(B2) Tunnex per-cluster zone** `<service>.<namespace>.<cluster>.<zone>`. (B1) native svc.cluster.local
REFUSED: it collides across clusters AND forwarding *.svc.cluster.local from a laptop HIJACKS the
well-known cluster-internal zone -- breaking any user who also runs kubectl against a local cluster, in a
way they'd never trace back to us. (B2) is collision-free by construction, self-describing at the point of
use, and rides S8.4's dns_forwarding rail UNMODIFIED (the CP pushes <cluster>.<zone> -> gateway-IP as an
ordinary entry -- reuse the rail, don't fork it).

**(B3) both REFUSED for v1:** accepting the native name buys single-cluster convenience and inherits (B1)'s
hijack the moment a second cluster or a local kubectl appears -- a footgun that arrives later, hardest to
diagnose. REGISTERED, trigger = a design partner explicitly wanting native-name convenience on a known-
single-cluster deployment.

**(B2) conditions:** <zone> operator-configurable (F6-like) with a sane default · the cluster NAME validated
as a DNS label at RegisterCluster (typed teaching error -- it's about to become part of a hostname) · the
zone must pass the existing S8.4 dns_domain_conflict check (one zone -> one resolver org-wide; a second
cluster claiming a colliding zone is REFUSED with the existing typed error, never a silent overwrite) · the
naming shape lands in the docs + the expose/export UI (the string every user types).

## Slice 5 — FINAL naming + resolver rulings (build spec)

**Naming (B2 refined): `<service>.<namespace>.svc.<cluster>.<zone>`** (K8s label order, INCL `.svc` — the
type marker in K8s's own `<svc>.<ns>.svc.<cluster-domain>` scheme; maps onto how admins configure
`--cluster-domain`; Istio/Submariner use the same `svc.<clusterset>` shape). The forwarded S8.4 entry stays
`<cluster>.<zone> -> resolver-IP`; only the labels to its left change. Zone is the CUSTOMER'S, supplied once
at RegisterCluster (F6-like), never Tunnex-branded — docs note it needn't be publicly registered (resolves
only inside the tunnel) but recommend a domain they own. `<cluster>` validated as a DNS label (typed teaching
error). The full resolvable FQDN renders in the expose/grant UI (copy, don't construct).

**COLLAPSE (single-cluster `<service>.<namespace>.svc.<zone>`) DROPPED — always-explicit.** Argued
(founder-delegated): the collapsed form is unambiguous ONLY with one cluster; a second cluster gives
`svc.<zone>` no clean dns_forwarding target (the VIP map is per-gateway; a collapsed name carries no cluster
to route on), so collapsed names in docs/bookmarks silently break on the second cluster — the B1/B3
"breaks-later-hardest-to-diagnose" footgun. The full form IS the K8s-native shape admins know, so
always-explicit is also the FAMILIAR one. Correctness beats brevity.

**Resolver (i): reserved DNS VIP.** Conditions: reserved at RegisterCluster (allocated WITH the range, never
handed to a Service — convention `.2`; avoid `.1`, conventionally a gateway). Red: ExposeService can never
allocate it. Rides the same sweep (deregister frees the DNS VIP + removes the dns_forwarding entry, no
orphan — full-sweep, 3rd application). The `:53` bind is fail-closed like the DNAT (DNS VIP not yet on the
interface -> the forwarder does NOT half-bind and does NOT answer; surfaced). **DNSDomainConflict passes for
FREE** — the S8.4 rail doing its job unmodified for a caller it was never written for (the reuse instinct
paying off).

## Slice 5 — mid-build FORK: cross-site push collides with SetDNSForward's D7 invariant (RULED (A))

**The claim above is RETRACTED (both kept so the reasoning survives).** The build spec asserted the entry
"rides the S8.4 rail unmodified" and "DNSDomainConflict passes for FREE." That assumed a compatibility the
rail forbids: `sites.SetDNSForward` enforces (S8.4 D7) that the resolver_ip live INSIDE one of the site's
APPROVED subnets ("the resolver lives on the LAN the site fronts"). The K8s DNS VIP is a SYNTHETIC address
deliberately DISJOINT from every site subnet — so SetDNSForward structurally REJECTS it
(`dns_resolver_not_in_site_subnet`). The two designs collide; the "unmodified reuse" was an illusion.

**RULED: (A) — ship Slice 5 as LOCAL direct-answer only; DEFER the cross-site dns_forwarding push.**
Cross-site forwarding to a cluster zone is NOT wired.

> **CORRECTION (story-end review, H1).** This section originally asserted the local answer was *"built,
> green"* — the cluster gateway *"binds `:53` on its reserved DNS VIP and answers its OWN zone from the VIP
> map."* **That was false for the AGENT half at the time it was written.** What shipped in `bfd53d1` was the
> **CP carriage only**: the VIP map + `DNSName` + `K8sDNSZones` populated, ProtocolVersion widened to v7,
> reserved-DNS-VIP allocation, the one-zone-one-resolver conflict checks — plus the `nodepolicy` *mirror
> struct*. **No agent code consumed either field**: `dnsforward.Forwarder` had no direct-answer path, no `:53`
> bind on the ListenVIP, and `main.go` fed the forwarder only the S8.4 table. The DNAT (Slice 4b) was real, so
> a client handed a VIP directly could reach the Service — but no client could LEARN the VIP by name. The
> headline capability (reach a Service by its name) was inert: a dead-while-green DNS path.
>
> **How the overclaim happened:** every Slice-5a red was CP-side and GREEN (VIP-map population, v7 trigger,
> hash-blindness, conflict refusal), so the paper's "green" was true of what the reds covered — but **no red
> asserted the agent answers a name**. Carriage was tested; capability was not. The gap was invisible to the
> test surface and to the paper, and survived to the story-end review, where finder P2 caught it against this
> very paragraph. (The self-catch is the discipline working: the finder contradicted the paper, the claim was
> verified by grep before ranking, and reported as a correction, not softened.)
>
> **What shipped in the H1 FOLD (the agent half, for real):** `Forwarder.SetK8sAnswers` + a K8s direct-answer
> path in `handle()` (exposed FQDN → its VIP as an authoritative A answer · in-zone-but-unexposed →
> authoritative NXDOMAIN · out-of-zone → fall through to S8.4 · empty/stale map → answer nothing, fail-closed);
> `main.go` feeds it `k8sAnswersFrom(p)` each reconcile; and `egress.ReconcileDNSVIPs` assigns each reserved
> DNS VIP as a `/32` on wg0 so the query is delivered locally AND the forwarder's bind-reconcile binds `:53` on
> it (fail-closed: no assignment → no local address → no bind → no answer). **The new red is capability-level,
> not carriage-level** — `TestK8sDirectAnswer`: *an agent given a VIP map answers the FQDN with the VIP*; it is
> the red that would have caught the original gap. The end-to-end wire proof (real CoreDNS + kube-proxy +
> live-netns ordering) remains walk-owed — the walk's decisive leg records HOW the client resolved the name.

- **Why not (B) puncture the subnet check (system-actor bypass):** it would weaken a shipped, security-adjacent
  invariant to satisfy a mechanism whose OTHER half is unproven — even with the row written, a remote gateway
  forwarding to a synthetic VIP needs the VIP range ROUTED cross-site, which nothing yet delivers. That is a
  dead-while-green pointer at an unreachable resolver — the exact anti-pattern this epic has refused four times
  (zombie hub, stale DNAT, unexposed-Service rewrite, mapless-agent version gate). Weakening a proven guard to
  enable a broken path is the worst trade.
- **Why not (C) now (first-class synthetic-resolver kind):** real design — needs a paper, a threat argument for
  why a resolver outside every approved subnet is legitimate, AND the cross-site routing question answered.
  A commit-one, not a mid-slice patch.

**(C) REGISTERED as the candidate fix. Trigger = the walk showing cross-site resolution is genuinely needed**
(client → in-cluster Service leg requires forwarding, not just a local answer).

**HONEST-BOUND SENTENCE (5th in the ledger; OVPN reneg, OVPN re-home, K8s version gate, mapless-agent, now
this):** *cluster-service names resolve for clients whose DNS reaches the cluster's gateway; cross-site DNS
forwarding to a cluster zone is not yet wired.* OWED in the K8s user docs too — lands with the gateway-chart
cut (no K8s user doc exists yet). The walk decides whether this is a real constraint or academic.

**One-zone-one-resolver KEPT (cheap check, both directions — the property was worth having even though the rail
didn't apply):** RegisterCluster refuses a cluster zone `<cluster>.<dns_zone>` that collides with a domain
already forwarded by a site; `sites.SetDNSForward` refuses a domain colliding with a registered cluster zone.
Both use the existing `DNSDomainConflict` + the typed `dns_domain_conflict` error. No import cycle: k8s→sites
for the pure helper; sites reads cluster zones via a sqlc query (`ListK8sClusterZonesForOrg`). Reds both sides.

**WALK MANIFEST condition (decisive, not incidental):** the client → in-cluster Service leg MUST record HOW the
client resolved the name — directly via the cluster gateway (local suffices, (C) never needed) or requiring
forwarding (then (C)'s trigger has fired WITH evidence).

## Slice 8 — tunnex-gateway chart (built; SUBSTITUTE = helm lint + template)

The in-cluster gateway ships as a SEPARATE chart from `tunnex-cp` (the CP stays an ordinary unprivileged
workload; the gateway is privileged). Files under `deploy/helm/tunnex-gateway/`.

- **F4 — hostNetwork.** Deployment sets `hostNetwork: true` + `dnsPolicy: ClusterFirstWithHostNet` (the DNS
  trap, commented DO-NOT-REMOVE), `securityContext.capabilities.add: [NET_ADMIN]`, a `/dev/net/tun` CharDevice
  hostPath, `TUNNEX_WG_BACKEND=wgctrl` (real datapath), `hostPort` WG. Recreate strategy (one WG identity).
- **F5 — join token.** `existingJoinTokenSecret` (preferred) OR `joinToken` (chart mints `*-join` Secret,
  NOTES warns it lands in helm history). The token env rides only when one is set (re-deploy off the state PVC
  needs none). Verified: both paths render.
- **F6 — endpoint.** `endpoint` is a `required` guard (the LB IP may not exist at install; operator reserves a
  static `service.loadBalancerIP` and supplies it). WG Service is LoadBalancer|NodePort, `externalTrafficPolicy:
  Local` (preserve peer source IP for the handshake).
- **F7 — preflight.** A `pre-install,pre-upgrade` hook Job requests the SAME privileged/hostNetwork/NET_ADMIN/
  `/dev/net/tun`/`ClusterFirstWithHostNet` context → admission-REJECTED on a blocking cluster, so `helm install
  --wait` fails at the hook (guard-before-exec), not in a crash-loop. It also asserts `/dev/net/tun` present +
  the cluster-DNS probe resolves (closes the hostNetwork DNS trap red). Complemented by `acknowledgePrivileged`
  (F7b) — a `fail`-guard so no one installs the privileged pod unseen. NOTES mandates `--wait`.
- State PVC (`*-state`, the enrolled cert survives restarts; emptyDir when persistence off — testing only).
  NOTES covers endpoint discovery, join-token minting, and wipe-to-re-enroll.

**SUBSTITUTE recorded:** `helm lint` + `helm template` PASS — ack-guard fails without acknowledgement; required
apiURL/agentURL/endpoint warn; the valid render emits Deployment+preflight-Job+PVC+Service+SA with hostNetwork +
ClusterFirstWithHostNet on both pods, NET_ADMIN, the CharDevice, both join-token paths, LB + NodePort. **NAMED
trigger for the real proof = the EPIC-10 walk** (real install on a cluster + client→in-cluster-Service via VIP;
real CoreDNS + kube-proxy + live-netns ordering are walk-owed).

## Story-end review — 12 findings dispositioned (fold record)

7 parallel finders (one per founder-named probe). 6 areas verified CLEAN (VIP lifecycle census — no
cached/snapshot VIP; v7 zero-config golden — byte-identical, goldens pass; Collect/OrgRanges census law — zero
leaks, fail-closed; full-sweep DB triad A–F; edition-gating/cross-org/render-floor/vanished-warn-read-time/RBAC;
DNAT classifier fail-closed single-success). 12 findings, all FIXED or REGISTERED per ruling:

- **H1 (HIGH) FIXED** — agent DNS answer was dead-while-green (CP carriage shipped, no agent consumer). Built
  the agent half + capability red + paper CORRECTION (see the H1 block above). The self-catch is credited: the
  finder contradicted this paper, the claim was grep-verified before ranking, reported as a correction.
- **H2 (HIGH) FIXED** — both sweeps now audit; DeregisterCluster records cascade counts (services_deleted +
  grants_deleted). Reds land the rows + prove the grant cascade.
- **M3 (MED) FIXED** — preflight DNS gate reads ANSWER CONTENT, not the busybox exit code (false-pass class);
  image pinned `busybox:1.37.0`.
- **M4 (MED) FIXED** — the `/dev/net/tun` check was dead code behind a CharDevice mount (tun-less node =
  FailedMount hang, not a loud fail). Now a non-blocking `/dev` DIRECTORY mount so the script runs and
  `test -c` fails LOUD.
- **M5 (MED) FIXED (grant half)** — sweeps pushOrg via the nodepush hub (<5s), not the ~25s long-poll.
  REGISTERED: the CORE-sites notifier gap is pre-existing (out of this story) → trigger = a customer needing
  <5s site-topology propagation.
- **M6 (MED) FIXED both callers** — RegisterCluster + ApproveSubnet take the org advisory lock (opportunistic
  close of the pre-existing sites gap, folded here because it is ONE change at a seam this story already
  modifies). Deterministic race red. NOTE (paper): the lock is the form because the forbidden overlap spans
  THREE tables (pool, site subnets, cluster VIP ranges); no single per-table `EXCLUDE` can represent it.
  REGISTERED: per-table `EXCLUDE`/btree_gist as structural defense-in-depth (trigger = a range-overlap
  incident or the next disjointness-touching story).
- **M7 (MED) FIXED** — gateway + preflight pods: `drop:[ALL]` + add-back-only NET_ADMIN,
  `allowPrivilegeEscalation:false`, `seccompProfile RuntimeDefault`. `readOnlyRootFilesystem` left OFF with a
  registered writable-mount audit (not silently claimed).
- **L8 FOLDED** — `nodes/service.go` calls `k8s.FQDN()` (the ONE constructor), no second name-format copy.
- **L9 FOLDED** — preflight declares the `hostPort` too (admission proxy not weaker than the Deployment).
- **L10 PROMOTED→FIXED** — `automountServiceAccountToken:false` (the gateway holds NO K8s API credential by
  construction — ruling A); posture stated in NOTES for the security team.
  **RE-AMENDED by WF-K5 (see end of doc): now TRUE.** Endpoint DNAT needs a read-only services +
  endpointslices watch, so the SA token is mounted and IS used — the credential for exactly one narrow
  capability, no longer the unused-token the L10 objection targeted. New posture sentence: "the gateway reads
  Service endpoints; it cannot read Secrets, cannot write, cannot escalate."
- **L11 REGISTERED** — `helm lint` does not enforce the ack `fail` guard (only `template`/`install` do). The
  real install path IS protected; a lint-only CI job is not. Trigger = adding a `values.schema.json` (the
  structural lint-time gate) — a chart-wide hardening, not S10.3-specific.
- **L12 REGISTERED** — `ListActiveK8sServicesForOrg` join omits the redundant `c.org_id` predicate (no leak —
  the service row's own org_id is authoritative + the query is org-scoped). Defensive-join hygiene; trigger =
  the next k8s.sql touch.

## NAMED COUPLING — the gateway DNS listener binds :53 as ROOT (re-review advisory, folded)

**The constraint:** the H1 DNS answer path (`dnsforward.Serve` → bind `:53` on the reserved DNS VIP) works
ONLY because the gateway pod runs as **root** (`runAsNonRoot: false`, deployment.yaml), which may bind a
privileged port (<1024) WITHOUT `CAP_NET_BIND_SERVICE`. The M7 hardening drops the ENTIRE default capability
set and adds back only `NET_ADMIN` — so `CAP_NET_BIND_SERVICE` is NOT present. Root is what makes the `:53`
bind legal; the capability set does not.

**Why it's a coupling worth naming (not a comment):** `runAsNonRoot: true` is one of the most common
security-review hardening asks. Someone flipping it later — with no visible connection to DNS — would break
in-cluster Service name resolution silently: the WG datapath (NET_ADMIN) keeps working, but the `:53` bind
fails, and the gateway stops answering cluster names. A two-hour mystery with no obvious cause. This is the
THIRD such coupling recorded this epic (alongside the resolve-must-not-stall-apply cousin of
KILL-SWITCH-NO-UNBOUNDED-I/O, and the DNSName⊂Zone invariant).

**Escape hatch (for a future non-root pod):** to run the gateway non-root, EITHER add
`capabilities.add: ["NET_BIND_SERVICE"]` (alongside NET_ADMIN) so a non-root process may bind `:53`, OR bind
the forwarder on a HIGH port (>1024) and add a node-netns REDIRECT (`nft`/`ip`) from `:53` on the DNS VIP to
that high port. Until one of those lands, `runAsNonRoot: false` is load-bearing for DNS — do not flip it in
isolation.

**Walk-check registered (NET_RAW):** the M7 `drop:[ALL]` keeps only `NET_ADMIN`. The core WG-netlink + nft +
`ip addr` datapath needs only NET_ADMIN, but `NET_RAW` is NOT proven unneeded by the LIVE datapath (a runtime
op crafting a raw socket — e.g. an ICMP/raw health probe — would fail under `drop:[ALL]`). Low likelihood;
the EPIC-10 wire proof is where it surfaces. Walk manifest carries an explicit NET_RAW check.

## Client-side push (fork-1) re-review — CLEAN; 3 LOW dispositioned

The security-critical seam (what a device crypto-routes into the tunnel + which resolver it trusts) re-reviewed
CLEAN — no high/medium defect. Strongest result (P2): the client-side zone match delegates to macOS
`/etc/resolver/<domain>` + Windows NRPT, both LABEL-BOUNDARY matched BY THE OS, so
`evil-prod.k8s.acme.com.attacker.com` can never route to the DNS-VIP — the redirection primitive is proven safe
by construction at the platform layer, not by our own string matching.

- **L2 — FOLDED.** `ListRoutedForwards` gated the zone forward on ≥1 LIVE exposed Service
  (`ListK8sServedZonesForOrg`), matching the gateway's own `K8sDNSZones` condition — so the client resolver and
  the gateway answer set agree BY CONSTRUCTION, never by coincidence. Class named: this was a
  **consumer-without-a-producer** (client installs a resolver for a zone the gateway REFUSEs until a Service
  exists) — the exact INVERSE of this epic's three producer-without-consumer findings (H1, the client push
  itself, the VIP-map staple). Red: zero-Service cluster → no zone forward; expose one → forward appears.
- **L1 — REGISTERED (inherited reachability class).** The zone forward is org-wide, so a device homed to a
  gateway that does NOT front the cluster (and lacks site-link transit to the one that does) SERVFAILs cluster
  names — SCOPED, so non-cluster DNS stays intact. Same shape as site-subnet / DNS-forward reachability, which
  the product already handles by the operator advertising what a gateway can actually reach. Not S10.3's to
  fix. WALK MANIFEST carries it as an OBSERVATION (the multi-gateway case is untested; a single-gateway walk
  homes the client to the fronting gateway and inherits the known class).
- **L3 — REGISTERED (inherited transport difference).** OpenVPN's `dhcp-option DNS` is a GLOBAL resolver by
  protocol design, versus WireGuard's per-domain OS scoping — an OpenVPN characteristic, not a Tunnex choice.
  The gateway's REFUSED (not NXDOMAIN) for out-of-zone names is the correct mitigation (the client falls back
  to its other resolvers). Limitations ledger only.

## WF-K5 (box-walk, 2026-07-27) — RULING A AMENDED: endpoint DNAT, not ClusterIP DNAT

**The wire finding.** The box-walk's decisive leg (a client reaching a real in-cluster Service by name)
FAILED end-to-end although every artifact rendered correctly. Root cause, proven on the wire on live k3s:

- Client→VIP SYN ARRIVES on the gateway `wg0` (tunnel + routing + peer all correct; DNS to the reserved DNS
  VIP `.2` answers fine — it is a LOCAL address, INPUT path).
- Our prerouting DNAT (`ip daddr <VIP> dnat to <ClusterIP>`, priority -101) is present and well-formed.
- ZERO conntrack entries for the flow; ZERO packets reach the pod; `curl <VIP>` times out.
- A scratch table at priority -102 retargeting the DNAT straight at the pod IP (`dnat to <podIP>`) → `curl
  <VIP>` = HTTP 200 immediately. Return path already covered by the existing `saddr <pool> oifname != wg0
  masquerade` (postrouting).

**Why the double-DNAT looked sound (the correction the next reader needs).** Slice 3b/4 ruled the sequence
"our chain VIP→ClusterIP at -101, THEN kube-proxy's ClusterIP→pod at -100" deterministic and mode-portable.
The priority model makes it look right: lower number runs first, both are nat-prerouting chains, so surely
the second re-DNATs the first's output. It does NOT. **netfilter applies at most ONE destination-NAT per
packet per hook.** The first chain to issue a `dnat` establishes the conntrack DNAT binding for the
prerouting hook; a `dnat` in any later chain at the same hook is a NO-OP. So our -101 rewrites VIP→ClusterIP,
kube-proxy's -100 matches the ClusterIP but its `dnat`→pod is silently ignored, and the packet is left
addressed to the ClusterIP — no route outside kube-proxy's own (skipped) DNAT → dropped before conntrack
confirms (hence zero conntrack entries). This one-dst-NAT-per-pass rule is not visible from the priority
ordering; it is the load-bearing constraint the original ruling missed. **A DNAT to a ClusterIP cannot be
completed by kube-proxy — the target must be a real endpoint.**

Third instance this epic of ARTIFACT-EXISTS ≠ ARTIFACT-WORKS (WF-OVPN-1 config-present≠config-starts; H1
field-carried≠field-consumed). Standing data-plane probe: the red proves a connection COMPLETES end-to-end,
never that a rule renders.

**RULED (founder): (1) endpoint DNAT.** The gateway DNATs VIP→a READY pod endpoint directly (single DNAT,
VIP→pod), never VIP→ClusterIP. Traffic stays FORWARDED (both faces non-local), so — both VERIFIED in code
before ruling, not assumed:
- the `ip tunnex` FORWARD chain still adjudicates the grant. Verified: grant enforcement is forward-hook
  ONLY (`egress_linux.go` `chain forward` policy-drop + per-grant accepts); there is NO input-path
  enforcement — a locally-terminated proxy (rejected (5)) would have delivered VIP traffic PREROUTING→INPUT
  and BYPASSED the grant chain entirely, and passed every unit red.
- S8.7 conntrack-flush revocation still works. Verified: `matchesTuple` keys on the client's orig-src /32
  (`conntrack_linux.go`; `SrcIP` = device /32); a forwarded VIP→pod flow carries the client src so a revoke
  flushes it. A proxy's gateway-sourced upstream leg (src = gateway) fails the src containment → would have
  SURVIVED the flush, stranding a revoked session.

**Rejected — (5) userspace VIP proxy:** moves VIP traffic FORWARD→INPUT, bypassing the grant chain AND
stranding revoked sessions' upstream legs — re-implementing enforcement + revocation in a second place (the
two-truths pattern refused six times). **Rejected — (3) Service externalIPs=VIP:** a per-Service API WRITE +
externalIPs claim-any-IP caveats; strictly worse surface than a read-only watch.

**Sub-fork RULED: raw REST watch (not client-go, not a build-tag split).** `apps/node` is a lean 10-require
binary shipped to EVERY gateway — AWS/Azure VMs, bare metal — K8s gateways a minority. client-go drags the
entire K8s API machinery (50+ transitive modules, tens of MB) onto boxes that never see a cluster —
dependency weight + supply-chain + CVE surface on the majority to serve the minority (the instinct that
refused native OpenVPN in the helper and a second liveness plane in the CP). A build-tag split is refused for
a sharper reason: two gateway binaries = two artifacts to build/sign/publish and a bug class that reproduces
on one and not the other; the single-image property is load-bearing for the release pipeline and the
one-image-one-command zero-touch story. The honest cost — ~150 LOC of hand-rolled LIST+WATCH — is acceptable
because the scope is a thin slice (two resource types, read-only, no cache/workqueue/leader-election) and the
failure mode is ruled fail-closed.

**RULING A AMENDED (not erased).** The DNS half is UNCHANGED: the CP-supplied VIP map still drives the direct
DNS answer (DNSName→VIP), H1 intact. What changes is TARGET RESOLUTION: ClusterIP-via-CoreDNS →
READY-ENDPOINTS-via-K8s-API-watch. The "reads NOTHING from your Kubernetes API" sentence is retired; the new
posture sentence is still strong and honest.

**L10 AMENDED (reason recorded).** `automountServiceAccountToken` false→TRUE: the gateway now needs its SA
token to authenticate the read-only watch. No longer an unused credential (the L10 objection) — the
credential for exactly one narrow capability.

**RBAC (minimal, stated in the security posture).** Read-only: `get,list,watch` on `services` and
`discovery.k8s.io/endpointslices`, nothing else; namespace-scoped to the exposed Services' namespaces where
feasible (a ClusterRole only if exposures span arbitrary namespaces). Posture sentence for the security team:
**"the gateway reads Service endpoints; it cannot read Secrets, cannot write, cannot escalate."**

**Conditions (ruled — each a red or a walk-owe):**
1. **Fail-closed on EVERY fault** (the security property): watch error, 410 Gone, connection drop, parse
   failure, or ZERO ready endpoints → NO DNAT programmed (or the existing rule REMOVED) — never a stale pod
   IP. A recycled pod IP (reassigned to another workload) is the reassignment trap at the DATAPATH tier —
   worse than the VIP case because pods churn constantly. Red per branch.
2. **Relist-on-410 is correctness-critical.** K8s expires resourceVersion routinely; a watch that silently
   stops updating is dead-while-green. Explicit red: 410 → full relist → DNAT reflects CURRENT endpoints.
3. **Port remap.** Endpoint DNAT loses kube-proxy's servicePort→targetPort remap, so the agent DNATs
   `VIP:svcPort → podIP:targetPort`, targetPort read from the EndpointSlice port (correlated to the exposed
   service port via the Service object — why RBAC names BOTH services + endpointslices).
4. **N ready endpoints → nft native LB** (`jhash` over src/dst for per-flow stickiness; no userspace
   round-robin, no state). One ready endpoint = a plain `dnat to podIP:port`.
5. **Substitute vs walk (stated in the gate report).** SUBSTITUTE = a fake in-cluster API server exercising
   every fail-closed branch + relist + the render. WALK-OWED = the real watch on live k3s AND a POD RESTART
   under a live client connection (endpoints change mid-flow) so the watch is proven on the wire, not just at
   steady state. The re-walk ENFORCEMENT leg (enforcing + no grant + VIP → DENIED at the forward chain with
   counter evidence) is mandatory.

**Residuals resolved / moved.** The 1-pod-headless residual (Slice 4 decide-item) is RESOLVED
authoritatively: EndpointSlices give ready pods directly, so headless vs ClusterIP stops mattering (we DNAT
to pods either way; a headless Service has no ClusterIP — visible from the API, no inference). The
`ServiceCIDR` classify input (Slice 4b term 1) is now VESTIGIAL for target selection; retained on the wire
(no version churn) and usable as a defense guard (a "ready endpoint" equal to the ClusterIP would be
malformed). Version stays v7 — the wire shape is unchanged; only the agent's resolution mechanism + the chart
RBAC change. **(B) satisfied-by-operator is SUPERSEDED for the datapath** — endpoint churn now reconciles
LOCALLY (watch → nft) with no CP round-trip, the responsive design the churn requirement wants.

**Salvaged unchanged:** the `table ip tunnex` prerouting chain, priority -101 (harmless now — DNAT to a pod
IP means kube-proxy's ClusterIP rules never match, no interference), atomic table-replace free-sweep, the
CP-string VIP validation, and the fail-closed classifier SHAPE (`ok=true` reachable at exactly one return).

### WF-K5 build — targeted re-review (5 finders) + dispositions (folded)

The endpoint-DNAT build (feature-sized) re-earned a review BEFORE the walk (5 finders: fail-closed/watch,
nft-injection, invariant-preservation, RBAC/honest-naming, port-remap/LB). 17 findings, ranked + dispositioned:

- **C1 (CRITICAL) FIXED — grant never matched DNAT'd VIP traffic.** The `k8s_service` grant is keyed on the
  VIP (`compiler.go` DstCIDR=VIP/32), but the prerouting DNAT (priority -101) rewrites dst VIP→podIP BEFORE
  the filter-forward chain (priority 0), so `ip daddr <VIP>` never matched the post-DNAT packet: enforcing
  mode DROPPED the granted flow, and a broad grant covering the pod CIDR (a site-route / full-tunnel
  0.0.0.0/0) would ACCEPT it WITHOUT the k8s_service grant (bypass). Latent in the original ClusterIP design
  too (never caught — the datapath never completed and the enforcing leg never ran). **Fix (ruled (a),
  universal):** `allowMatch` renders `ct original ip daddr` — matches the PRE-DNAT dst (the VIP) that
  conntrack recorded (hook order: conntrack -200 < nat -101 < filter 0), and off the DNAT path
  `ct original` == current dst (a no-op). This keys enforcement on the SAME tuple space as the S8.7 flush
  (ct-original src) — the ONE-TRUTH LAW: a flow adjudicated and torn down on one tuple, never two that can
  disagree. Edge cases closed: established/related is a separate `ct state` rule (unaffected); untracked
  packets have no ct entry so `ct original` can't match (fail-closed); `ct state invalid` gets an explicit
  drop ahead of the grants (an invalid packet can carry a stale ct entry). Every forward-chain golden
  updated `ip daddr`→`ct original ip daddr` and verified semantically identical off the DNAT path.
- **H2 FIXED** — the watch was unbounded (a silent partition froze it for the TCP-keepalive window while the
  ticker re-applied the frozen view = dead-while-green). Added `timeoutSeconds` (server close) +
  `watchMaxDuration` (client backstop) → a partition forces a relist.
- **H3 FIXED** — watch death no longer sleeps with a stale cache: relist IMMEDIATELY, and the list CLEARS on
  failure (backoff only there). relist-fast + clear-on-failure (never blip the VIP on a normal reconnect).
- **H4 FIXED** — an IPv6 VIP or endpoint would reach the v4-only render and make `nft -f` reject the WHOLE
  `table ip tunnex` (grant chain + kill-switch + conntrack) — one dual-stack default away from disarming
  enforcement wholesale. classify now `Is4()`-guards the VIP and drops non-v4 endpoints (mirrors the DNS-VIP
  path).
- **M5 FIXED** — endpoint + service ports bounded 1..65535 (an out-of-range port would render invalid nft).
- **M6 FIXED** — a gateway-LOCAL endpoint (a hostNetwork pod on this node) is refused: DNAT to a local addr
  would divert to INPUT and bypass the forward grant chain (the option-5 hazard, resurfacing). classify drops
  any target equal to a host-interface address.
- **M7 FIXED** — the NOTES "Security posture" block still claimed "NO K8s API credential, token NOT mounted,
  no Role" (a lie after the ruling) — rewritten to the read-only services+endpointslices reality.
- **M8/M9 FIXED (REFUSE)** — an all-ports (PortLow==0) exposure would silently DNAT to the wrong pod port,
  and a port RANGE would blackhole past PortLow. Both refused with a typed teaching error at expose time
  (`ExposeService`: `service_port_required` / `service_port_range_unsupported`) AND fail-closed at the agent
  classify. Range support REGISTERED (trigger: a customer need).
- **L10 FIXED** — gofmt. **L11 DISCHARGED** — a `nft -c` render-check test (`TestRenderedRulesetIsValidNft`,
  --cap-add=NET_ADMIN in test-node) proves the jhash LB map + `ct original` + DNAT are `nft`-valid — the
  artifact-WORKS probe applied to the render, so the LB syntax is no longer walk-owed. **L12 FIXED** — the
  watcher tracks per-resource `synced` flags so "API not live" (fail-closed, drives k8s_endpoints_unavailable)
  is distinct from "Service has no endpoints" (a quiet per-Service refuse).
- **REGISTERED:** L13 (a malformed DELETED watch event retains the stale entry until the next relist),
  L14 (a per-slice port-NAME mismatch silently drops that slice's ready pods), L15 (the jhash key `daddr` is
  constant per VIP, so LB granularity is per-client — distribution collapses behind a single NAT'd hub IP),
  L16 (410 relists without backoff — unreachable on a healthy API), L17 (a cross-goroutine port-name-rename
  window can pick a stale target port on a still-current pod IP).
- **Cleared (no regression):** kube-proxy interaction (−101 harmless — a pod-IP DNAT never matches
  kube-proxy's ClusterIP keys), the return-path masquerade, and the S8.7 conntrack flush.
