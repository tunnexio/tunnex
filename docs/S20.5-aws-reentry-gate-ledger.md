# S20.5 AWS re-entry — intermediate gate ledger

Status: **NOT merge-ready.** Local gates do not satisfy live qualification or
exact-final-SHA required CI. This is an intermediate work record, not PLAN's
final checkpoint. No AWS/Azure mutation, deployment, PR merge, or release has
been performed in this session.

## Subject and preservation

- Verified main integration baseline: `0f3d9b0495aebd6c01dfd1c6791d1e54390a0c8d`
  (still the remote main tip at the checkpoint verification).
- Imported S20.5 source: `8a1e411530eb477b9734db5b9c44db5df392f416`.
- Reviewed integration checkpoint: `ae458ffd89722117dce959e07eaa0c1144279111`.
- Latest product repair at this ledger: `18bd20af82d09c5a6bcec1d6f5d525bbf57550fa`
  (immutable claim binding). This is not the final story content tip.
- Isolated branch: `codex/s205-aws-reentry`.
- Original checkout remains clean on `codex/control-plane-ui-refinement` at
  `ae266442a42e408484e97bba024f37b7e85c6286`; no stash or other worktree changed.
- Local project/network/store: `tunnex-s205-aws-20260905a`,
  `tunnex-s205-aws-20260905a_default`,
  `tunnex-s205-aws-20260905a_postgres_data`. PostgreSQL identity and labels were
  re-verified before database tests. Default projects/stores were not used.
- Dependency caches use the same explicit task prefix. Task-owned dependency
  links remain local and untracked; the dependency ignore rule also excludes
  symlinks, so a native test harness cannot accidentally commit a laptop path.

Main's retention migrations 0127–0130 are byte-identical to main. Unmerged
S20.5 migrations moved intact to 0131–0136. Historical walk evidence retains
its originally observed schema numbers; it is not new AWS proof.

## Local runs on 2026-09-05

The first runs were on the integration draft that became `ae458ff`; subsequent
desktop and claim-binding fixes require their own reruns. These rows must not
be inherited as final-SHA green after further content changes.

| Gate | Observed result |
|---|---|
| `make migrate` | PASS, schema 136, dirty=false, fresh isolated PostgreSQL |
| `make generate-check` | PASS, actual Go/TS/RBAC/token/sqlc regeneration; no module/lockfile drift |
| `make test-editions` | PASS, complete open and enterprise suites against isolated PostgreSQL |
| `make build-editions` | PASS, both editions |
| `make test-cli` | PASS, complete build/vet/tests, including listener cases and archive tool |
| `make test-operator` | PASS |
| `make test-node` | PASS, real container OpenVPN/nft tools; rerun after integration commit also passed |
| `make test-helper` | PASS |
| helper `go test -race ./...` | PASS in disposable container |
| `make helper-crosscompile` | PASS, Darwin amd64/arm64 and Windows amd64 |
| `make test-k8s-charts` | PASS, pinned Helm 3.18.4 and Go 1.25.13; all six constituent contracts |
| installer provenance / release completion | PASS, actual release-step logic against closed local gh fixture; no publication |
| signed-main release / cache isolation | PASS |
| HA deployment contract | PASS with Helm, defaults OFF and explicit opt-in; an earlier no-Helm static-only skip is superseded, not counted as full proof |
| first `make web-gate` | Typecheck PASS; 1373 tests PASS, one stale latest-main button selector failed; build did not run after failure |
| web selector correction | Test now targets existing `View commands` button; production UI not reverted; complete rerun passed after the desktop slice, as recorded below |
| Windows enrollment fixture repair | 25/25 focused native tests PASS; actual Windows CI remains required |

After the foreign-anchor slice was frozen, the complete native client suite
passed **292/292**, client typecheck/build passed, and the full Docker
`make web-gate` rerun passed typecheck, **1377/1377 tests in 110 files**, and
production build. The first web failure is resolved without reverting main's
UI. An AST wiring test plus pure lifecycle tests remain substitutes for actual
Electron IPC/helper execution. Windows runner proof is not inferred from
these native macOS results.

### Latest API rerun supersedes the initial both-green observation

After the immutable claim-binding repair, `make generate-check` passed again.
The complete `internal/nodes` and migration packages passed in **both** API
editions, including rename, corruption rollback, deletion history, initial
enrollment bridges and observed first-consumption concurrency. However, the
overall API gates are now **RED**, not green:

- Open: `TestRuntimeServicePostgresContract` failed on candidate credential
  promotion. The PostgreSQL log confirmed unique-constraint violation
  `agent_runtime_credentials_one_current_key` (23505). The query/index/service
  mechanism is unchanged from the verified main baseline; its multi-row
  promotion update does not order removal of the old current row first.
- Enterprise, run independently after make stopped on open: runtime tests
  passed, but `TestManagedAgentBootstrapIsHashedSingleUseAndClientKeyed` failed
  on `agent_bootstrap_tokens_hash_key` (23505). Its repeated fixed expired-token
  fixture persists because organization cleanup invokes an audit FK UPDATE
  rejected by the append-only trigger, and the test discards the cleanup error.

Neither unrelated product surface was edited and neither red was retried away.
These are current gate blockers requiring disposition, not a claim that the
Kubernetes slice caused them or that an earlier successful run clears them.
The [baseline failure evidence](S20.5-baseline-gate-failures.md) records the
deterministic temporary-table reproduction, exact-main source comparison, and
fixture-cleanup log attribution. Main was not separately rerun end-to-end.

The chart gate now builds real ELF/Helm artifacts, checks decoded chart content
and binary strings for an ignored credential sentinel, and compares repeated
bundles. All four real charts also preserve file contents, Helm metadata, and
rendered resources after normalization. GET-only localhost API fixtures execute
the CRD preflight's actual three-name lookup loop and refusal cases. These are
offline substitutes, not live cluster witnesses. Actual final-source runtime
images and the production CLI bundle still need qualification.

## Remaining acceptance boundaries

- Desktop foreign-anchor and immutable API claim-binding repairs are separate
  reviewed slices. The latest full-gate outcomes are recorded above; the
  unrelated API reds still block acceptance.
- The remaining node startup/heartbeat model is explicitly proposed and HELD
  in `S20.5-node-timing-and-startup-decision-proposal.md`. No proposed state,
  timeout, probe, or readiness policy has been silently implemented.
- Original held findings remain held unless individually dispositioned with
  current evidence. Targeted finder reviews are not a complete clean story-end
  review; final multi-finder review and user disposition remain mandatory.
- CRD generation-marker/fingerprint policy and unsupported CRD rollback are
  still explicit boundaries. Direct-Helm retained-PVC adoption is not fully
  fixed by the CLI provenance check alone.
- No EKS cluster exists in the last verified account/region inventory. Before
  live work: propose exact isolated resource/cost plan, re-verify AWS identity,
  qualify the actual CNI mechanism, and preserve every patch-free walk leg.
- Current remote source CI is red; no exact-final-SHA CI exists for this work.
  Final content, draft PR number, last PLAN commit, pushed-SHA required checks,
  and matched-head linear merge remain outstanding.

S20.5.1, S20.5.2, and S20.6 named deferrals are unchanged. No cleanup is
authorized by this ledger; task stores and temporary tools remain available.
