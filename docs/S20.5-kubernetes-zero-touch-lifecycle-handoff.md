# S20.5 Kubernetes zero-touch lifecycle — branch re-entry state

**Later AWS continuation, 2026-09-06:** use
[targeted AWS acceptance](S20.5-aws-targeted-acceptance.md) and the current
[AWS evidence ledger](../walk-artifacts/S20.5/aws-20260905/README.md).
The user superseded the exhaustive eleven-leg replay requirement. Retain valid
proof; re-test changed/broken behavior. Do not restart the historical sequence
below or report all progress as zero solely because the source SHA changed.
This note does not rewrite the historical Git state or the final PLAN pointer.

> **Branch-local, non-authoritative checkpoint.** Read `PLAN.md`'s "Story
> status (re-entry checkpoint)" and `git log --oneline --decorate` first. Git
> is the authority for what landed. This file does not update, replace, or
> supersede the authoritative `PLAN.md` pointer. If it disagrees with Git, it
> is stale.

Status: **PAUSED IN PROGRESS; NOT WALK-COMPLETE, NOT GATE-COMPLETE, NOT
MERGE-READY.** This checkpoint grants no future push, deployment, cloud
mutation, PR, merge, release, or cleanup authority.

This checkpoint was written on 2026-09-01 for a deliberate pause. The
authoritative `PLAN.md` pointer is intentionally unchanged until the final
story/PR checkpoint is written last under the repository merge protocol.

## Exact Git boundary

- Story branch: `story/route-readback-hotfix`.
- Last product-content tip before this checkpoint:
  `df3d2639cbce8f5695231bc9a985a6b4809f190a`.
- Rebased main baseline:
  `b7a18f52b336f4d390ddfa382975115d740ba50a`.
- Story remote before the checkpoint push:
  `a229b0ebcd5b3dc9571bebd170380e17d433d578`.
- The checkpoint commit is intentionally the final commit for this pause. Its
  own hash is obtained from `git rev-parse HEAD`; it cannot name itself.
- No PR exists at this checkpoint. The PLAN pointer must be re-pointed again,
  after the final content commit and before any future merge.

Do not infer merge authorization from this handoff. A future merge requires
fresh, in-session user approval and the exact-head merge standard in
`AGENTS.md`.

## Committed scope at the content tip

The branch contains the provider-neutral Kubernetes path built through the
following committed slices:

- deterministic route readback, GitOps connector selection, projected service
  account token refresh, and configurable connector pools;
- common lifecycle claims and install operations across the OpenAPI contract,
  API, CLI, generated clients, database migrations through version 132, and
  recovery/replay/abort/purge paths;
- `tunnex k8s` plan/install/upgrade/rollback lifecycle, token-blind Kubernetes
  anchors, create-only bootstrap delivery, chart packaging, and provenance
  checks;
- one common gateway, host-posture, operator, and CRD chart family instead of
  provider-named datapath branches;
- node-side Kubernetes discovery, endpoint DNAT, mechanism-based host posture,
  network preparation, durable ownership, and HA fencing/convergence;
- Kubernetes lifecycle and connector-pool UI wiring plus desktop-managed,
  crash-safe enrollment recovery;
- release contracts, customer documentation, the S20.5 decisions paper, and a
  runsheet that explicitly requires a patch-free live proof.

The final source/test repairs before the checkpoint are:

- `33a0971f` — remove the deferred client recovery protocol;
- `2dd50baa` — reconcile generated enum compatibility aliases; and
- `df3d2639` — migrate HA fixtures through the authority schema.

## Explicit deferrals

These are committed dispositions, not silently missing work:

- Windows automatic terminal crash recovery is deferred to S20.5.1. The
  documented operator reinstall/support procedure receives no zero-touch
  acceptance credit.
- Automatic control-plane recovery of a failed private candidate rollout is
  deferred to S20.5.2. The retained publish/rollout prototypes are not part of
  this content tip.
- Ordinary host-reboot rebootstrap is deferred to S20.6. Manager/container
  restarts and Pod rescheduling remain in the current live-walk scope.
- EKS, GKE, and self-managed provider qualification require their own live-wire
  legs. Shared mechanism fixtures do not create provider support claims.

## Verification already obtained

These results are useful evidence, but they do not replace the pending exact
pushed-SHA gates or live walk:

- Web typecheck, build, and 1,296/1,296 tests passed at `33a0971f`; the web tree
  is byte-unchanged through the content tip.
- Client typecheck, build, and 289/289 tests passed at `33a0971f`; the committed
  client tree is byte-unchanged through the content tip.
- Node tests, helper vet/tests/cross-compiles, and all five Kubernetes chart
  contract groups passed on the applicable unchanged committed trees.
- Open and enterprise API production builds passed after the enum-alias repair;
  the content-tip change is test-only.
- A clean local migration reached schema version 132 with `dirty=false`.
- Focused HA migration-boundary and enum-alias API/CLI checks were reported
  green on the `df3d2639` content tip, but their exact command transcripts are
  not committed. Treat that report as navigation evidence only and rerun the
  focused checks before relying on them.

Do not summarize the branch as fully green. The exact final composite gates did
not all finish on stable infrastructure before this pause.

## Pending completion boundary

The next session must close all of the following before calling S20.5 complete:

1. Re-verify branch, HEAD, main ancestry, remote refs, index, preserved dirt,
   and the checkpoint's applicability.
2. Select a fresh, explicit, non-default `COMPOSE_PROJECT_NAME` for the resume
   run. Before any database-backed command, verify that its exact Compose
   network and stores belong to that isolated project. Refuse to use or mutate
   the default Compose project; cleanup remains separately approved.
3. Run the exact-final local gates, including `make generate-check`,
   `make migrate`, `make test-editions`, `make build-editions`,
   `make test-cli`, and `make test-operator`, plus any other gate required by
   the current `AGENTS.md`.
4. Inspect required GitHub checks for the exact pushed checkpoint SHA. A
   cancelled or superseded run is not evidence.
5. Complete the story-end multi-finder review. Hold ranked findings for user
   disposition before folding any result.
6. Run every leg in
   `docs/S20.5-kubernetes-zero-touch-lifecycle-boxwalk.md` on a clean AKS
   fixture. The committed ledger currently says every live leg is `NOT RUN`;
   static/database proofs are substitutes only.
7. Commit redacted evidence during the walk. Never commit a join token, machine
   token, private key, certificate body, kubeconfig, or service-account token.
8. When all non-pointer content is final, open or update a draft PR first so
   its real PR number is known. Then write the PLAN checkpoint as the final
   pre-merge commit, naming `PR #<n>` and the last pre-pointer content tip.
   Push that pointer, obtain required checks on the resulting exact SHA, and
   request fresh merge approval. If any content lands afterward, re-point it
   in the same breath.

The live walk is disqualified by SCP/source hot-patching, a Secret/PVC/Deployment
repair, manual host networking repair, a CNI edit, or a remedial rollout restart.
The allowed failure-injection exceptions are defined narrowly in the box-walk.

## Why the final gates stopped

The remaining local reruns were infrastructure-limited, not proven product
failures:

- Docker/Colima exhausted local capacity while bringing up the operator gate.
- A tmpfs PostgreSQL checkpointer was killed and restarted under the full
  database test load.

Rerun on stable clean infrastructure and treat pushed exact-head CI as the
authoritative completion boundary. Do not convert either limitation into a
green result.

## Preserved workspace boundary

At checkpoint creation the index was empty. The worktree intentionally retained
23 tracked dirty paths and 55 untracked status records outside the committed
story tip, including:

- incomplete Windows/helper crash-recovery work;
- uncommitted candidate publish/rollout prototypes and their Makefile wiring;
- user-owned `OneTimeSecret` web edits and S20.5 live-session evidence;
- older worktrees, environment backups, compose files, and local artifacts.

Do not stage, reset, delete, or describe those paths as shipped. Preservation
stash `86716f2561f7b031dbd119471c3f82ed8b494861` and the older stash below it
must remain untouched unless the user separately directs otherwise.

## Temporary resources left for later review

The pause intentionally leaves these resources for a later cleanup review:

- clean detached worktree `.d14-final-gates` at the content tip;
- healthy tmpfs PostgreSQL container `tunnexd14final-pg-tmpfs` on
  `127.0.0.1:55432` and network `tunnexd14final_tmpfs_net`;
- current audit directories and Go build caches under `/private/tmp` plus older
  same-day D14 scratch that must be attributed before deletion.

Do not clean these automatically. Show the exact deletion list to the user and
obtain separate cleanup approval first.

## Azure fail-closed boundary

Never touch any Bolster tenant, subscription, resource group, registry, or
resource. Before any future Azure read that could lead to mutation, verify all
of the following against the separately approved Tunnex boundary:

- signed-in account: `tunnexio@outlook.com`;
- tenant ID: `3ea292fe-e156-461e-9d2e-3969a7568257`;
- subscription ID: `a9a140db-bb16-4113-a5c5-2108c61aca3f`; and
- the full ACR resource ID and login server selected for that exact tenant and
  subscription.

Refuse before mutation if any value is absent, ambiguous, or mismatched. A
future approval to resume the walk does not expand this identity boundary or
authorize Azure mutation by itself.

## First commands on resume

Run read-only checks before mutation:

```bash
git status --short --branch
git log -8 --oneline --decorate
git merge-base --is-ancestor b7a18f52b336f4d390ddfa382975115d740ba50a HEAD
git ls-remote origin refs/heads/main refs/heads/story/route-readback-hotfix
git diff --cached --name-status
```

Then compare the exact pushed SHA and CI state. If Git or live infrastructure
disagrees with this checkpoint, report the difference and use the newer
evidence; do not replay old commands blindly.
