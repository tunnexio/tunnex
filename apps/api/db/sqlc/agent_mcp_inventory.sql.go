// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: agent_mcp_inventory.sql

package sqlc

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const getAgentMCPInventory = `-- name: GetAgentMCPInventory :one
SELECT i.device_id, i.snapshot, i.observed_at, i.created_at, i.updated_at
FROM agent_mcp_inventory i
JOIN devices d ON d.id = i.device_id
WHERE i.device_id = $1
  AND d.org_id = $2
  AND d.kind = 'agent'
  AND d.deleted_at IS NULL
`

type GetAgentMCPInventoryParams struct {
	DeviceID uuid.UUID `json:"device_id"`
	OrgID    uuid.UUID `json:"org_id"`
}

func (q *Queries) GetAgentMCPInventory(ctx context.Context, arg GetAgentMCPInventoryParams) (AgentMcpInventory, error) {
	row := q.db.QueryRow(ctx, getAgentMCPInventory, arg.DeviceID, arg.OrgID)
	var i AgentMcpInventory
	err := row.Scan(
		&i.DeviceID,
		&i.Snapshot,
		&i.ObservedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const upsertAgentMCPInventory = `-- name: UpsertAgentMCPInventory :one
INSERT INTO agent_mcp_inventory (device_id, snapshot, observed_at)
SELECT d.id, $1::jsonb, $2
FROM devices d
WHERE d.id = $3
  AND d.org_id = $4
  AND d.kind = 'agent'
  AND d.deleted_at IS NULL
ON CONFLICT (device_id) DO UPDATE
SET snapshot = EXCLUDED.snapshot, observed_at = EXCLUDED.observed_at
RETURNING device_id, snapshot, observed_at, created_at, updated_at
`

type UpsertAgentMCPInventoryParams struct {
	Snapshot   []byte    `json:"snapshot"`
	ObservedAt time.Time `json:"observed_at"`
	DeviceID   uuid.UUID `json:"device_id"`
	OrgID      uuid.UUID `json:"org_id"`
}

func (q *Queries) UpsertAgentMCPInventory(ctx context.Context, arg UpsertAgentMCPInventoryParams) (AgentMcpInventory, error) {
	row := q.db.QueryRow(ctx, upsertAgentMCPInventory,
		arg.Snapshot,
		arg.ObservedAt,
		arg.DeviceID,
		arg.OrgID,
	)
	var i AgentMcpInventory
	err := row.Scan(
		&i.DeviceID,
		&i.Snapshot,
		&i.ObservedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
