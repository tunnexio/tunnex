{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tunnex-cp.serviceAccountName" . }}
  labels:
    {{- include "tunnex-cp.labels" . | nindent 4 }}
{{- end }}
