# Load these into the root Compose invocation using an operator-owned private env file.
# Empty values deliberately fail Compose validation. Never commit filled values.
TUNNEX_AI_GATEWAY_ADMIN_USER=
TUNNEX_AI_GATEWAY_ADMIN_PASSWORD=
TUNNEX_AI_OPENROUTER_API_KEY=
TUNNEX_AI_ENGINE_ENCRYPTION_KEY=
